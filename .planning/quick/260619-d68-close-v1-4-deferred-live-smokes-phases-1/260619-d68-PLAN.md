---
quick_id: 260619-d68
title: Close v1.4 deferred live-smokes (Phases 14/15/16)
created: 2026-06-19
status: in-progress
mode: quick (interactive — human-in-loop live smoke, run inline not via worktree executor)
---

# Quick Task 260619-d68: Close v1.4 deferred live-smokes (Phases 14/15/16)

## Why inline (deviation note)

The deferred items are PROD live-smoke **verification**, not delegable code work. The
headline step (real-key classification on a fresh PROD account) requires real-time user
interaction — a fresh signup + pasting a real BYOK key — which a headless worktree executor
cannot perform. So this task runs in the main thread interactively, keeping GSD tracking
(quick dir + SUMMARY + STATE row + atomic commit) but skipping the planner/executor spawn.

## Pre-flight (done inline, read-only)

- ✓ `maxDuration = 60` present in code at `src/app/(app)/importar/page.tsx:13` (server-action
  architecture — bounds `ingestStatement` parse + ONE batched classify). CLSAI-06 satisfied in code.
- ✓ Migrations `0033_ai_settings.sql` + `0034_fix_ai_key_rpcs.sql` exist locally; Supabase linked
  (ref `pnjzrmlqyebyeecxvfzu`). PROD-applied state determined by the smoke (BYOK save exercises the RPCs).

## The single smoke (closes all three deferred phases)

One PROD session, run by the user, proves all three:

1. **Phase 14 — `0033`/`0034` live in PROD.** Fresh signup (`/auth/signup`; old account deleted in
   Phase 17 DATA-02) → `/conta/configuracoes-ia` → pick provider + paste real key → save.
   - Save **succeeds** → `ai_settings` table + `get/save/remove_ai_api_key` RPCs + RLS live in PROD.
   - Save **fails** with a "function/relation does not exist" class error → migrations not pushed →
     user runs `supabase login` + `supabase db push` (+ `npm run gen:types`), then retries.
2. **Phase 15 — real-key classify + `maxDuration` inheritance.** Upload a statement (OFX/CSV/PDF)
   containing a **NEW merchant** (never-confirmed descriptor = cache-miss) → upload completes without
   a function timeout → the new-merchant row receives a real `row.suggestion` (source `ia`) with a
   category the user owns; already-known merchants fire ZERO AI calls.
3. **Phase 16 — UI end-to-end on real data.** In the review grid the AI-suggested rows show the gold
   "IA" badge + Sparkles, the "Aplicar sugestão: {Categoria}" chip, the amber "baixa confiança" tag on
   rows with confidence < 0.6, and low-confidence rows sort first. Memory-hits show the neutral
   "memória" badge (no chip). Applying a suggestion fills the cell client-side — no auto-commit.

## Tasks

### T1 — Run the PROD live smoke (user-driven, guided inline)
- **action:** Guide the user through signup → save BYOK key → upload new-merchant statement → eyeball grid.
- **verify:** User confirms each observable; capture provider used + merchant + outcome.
- **done:** All three observables confirmed live in PROD (or migration push performed first if save failed).

### T2 — Flip the three VERIFICATION.md from human_needed → passed
- **files:** `.planning/phases/14-key-storage-byok-settings/14-VERIFICATION.md`,
  `.planning/phases/15-classification-wire/15-VERIFICATION.md`,
  `.planning/phases/16-review-grid-suggestion-affordances/16-VERIFICATION.md`
- **action:** Set `status: passed`; append a "Live Smoke (PROD)" closure note recording date, provider,
  merchant, and observed result to each phase's Human Verification section.
- **verify:** `grep "^status:"` returns `passed` for all three.
- **done:** Three files reflect the live smoke; no `human_needed` remains for 14/15/16.

### T3 — Reconcile PROJECT.md + STATE.md, write SUMMARY, commit
- **files:** `.planning/PROJECT.md`, `.planning/STATE.md`, this quick task's SUMMARY.md
- **action:** Move the v1.4 deferred live-smoke line from Active → Validated in PROJECT.md; update
  STATE.md Deferred Items table (14/15/16 → RESOLVED) + Quick Tasks Completed row; write SUMMARY.
- **verify:** No open v1.4 live-smoke debt in PROJECT.md Active / STATE.md Deferred Items.
- **done:** Artifacts committed in one docs commit.

## Must-haves (truths)
- All three `1[456]-VERIFICATION.md` carry `status: passed` with a live-smoke closure note.
- PROJECT.md Active no longer lists the deferred v1.4 live-smoke item.
- STATE.md Deferred Items shows 14/15/16 RESOLVED + a Quick Tasks Completed row for 260619-d68.
