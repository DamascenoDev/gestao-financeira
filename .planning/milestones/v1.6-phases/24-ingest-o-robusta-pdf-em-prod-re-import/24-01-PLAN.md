---
phase: 24-ingest-o-robusta-pdf-em-prod-re-import
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0038_statements_status_imported.sql
  - src/lib/parsers/pdf.test.ts
autonomous: false
requirements: [IMP-07, PDF-07, PDF-06]
user_setup:
  - service: supabase
    why: "PROD `supabase db push` of 0037 + 0038 requires interactive auth"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard -> Account -> Access Tokens (or `supabase login`)"

must_haves:
  truths:
    - "Migration 0038 widens statements.status CHECK to include 'imported' (re-runnable, non-destructive superset)"
    - "After 0038 applies, an UPDATE setting statements.status='imported' succeeds where it raised SQLSTATE 23514 before"
    - "Re-upload of a CONFIRMED statement returns alreadyImported (0 novas) — mock test stays green"
    - "Re-upload of an UNCONFIRMED statement re-parses and reuses the existing id — mock test stays green"
    - "Image-only PDF hard-blocks with a CSV/OFX-steering message — mock test stays green"
    - "Generic non-Santander noise degrades to an empty result without throwing AND with honest counts (rows AND dropped AND capped asserted)"
    - "next.config.ts force-includes the pdfjs worker in the serverless trace (source assertion only — no rebuild)"
    - "database.types.ts diff is empty (text+CHECK widening leaves the generated type `string`)"
  artifacts:
    - path: "supabase/migrations/0038_statements_status_imported.sql"
      provides: "Widened statements.status CHECK constraint including 'imported'"
      contains: "statements_status_check"
    - path: "src/lib/parsers/pdf.test.ts"
      provides: "Strengthened generic PDF degradation assertion (SC2 lock)"
      contains: "not.toThrow"
  key_links:
    - from: "supabase/migrations/0038_statements_status_imported.sql"
      to: "src/actions/import.ts"
      via: "the widened CHECK lets confirmImport's update({status:'imported'}) (import.ts:997) succeed, making the fast-path (import.ts:323-330) reachable"
      pattern: "status.*imported"
---

<objective>
Harden statement ingestion across three fronts (IMP-07, PDF-07, PDF-06). Two are already code-complete and need only verification; the third is the single real code build.

- **IMP-07 (the build):** add `supabase/migrations/0038_statements_status_imported.sql` that widens the `statements.status` CHECK to include `'imported'`, so `confirmImport`'s `update({status:'imported'})` (import.ts:997) stops failing the CHECK silently and the "already confirmed → block re-review" fast-path (import.ts:323-330) becomes reachable. Mirrors `0032_statements_format_pdf.sql` exactly. Per D (CONTEXT "A migration do IMP-07").
- **PDF-07 (verify + lock):** strengthen the EXISTING generic degradation test at `src/lib/parsers/pdf.test.ts:122-125` with `.not.toThrow()` + a full empty-result shape assert (non-Santander noise → `{ rows: [], dropped: <n>, capped: false }`). No new parser, no new file. Per D (CONTEXT "PDF-07 = code-complete ... Acrescentar 1 teste de degradação genérica").
- **PDF-06 (verify-only):** assert `next.config.ts` contains the `outputFileTracingIncludes` worker include (grep-level source assertion). No rebuild. SC1 "works in PROD" is a deferred human-verify (UAT) item. Per D (CONTEXT "PDF-06 = code-complete ... NÃO rebuildar").

Purpose: make the re-import fast-path actually function in PROD (currently the status update is logged-and-swallowed), and lock the already-shipped PDF robustness behind explicit assertions.
Output: one new migration file, one strengthened test, and a deferred `autonomous:false` PROD-push task that bundles the still-pending 0037 with 0038.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-ingest-o-robusta-pdf-em-prod-re-import/24-RESEARCH.md
@.planning/phases/24-ingest-o-robusta-pdf-em-prod-re-import/24-CONTEXT.md
@supabase/migrations/0032_statements_format_pdf.sql
@supabase/migrations/0019_statements.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the 0038 status-widening migration (IMP-07)</name>
  <files>supabase/migrations/0038_statements_status_imported.sql</files>
  <read_first>
    - supabase/migrations/0032_statements_format_pdf.sql (the EXACT drop-if-exists + named-add template to mirror — same table, same shape)
    - supabase/migrations/0019_statements.sql:20-21 (the inline unnamed CHECK Postgres auto-named `statements_status_check`, value set `('uploaded','parsing','parsed','failed')` — the widening target) and 0019:33-37 (the `"own statements"` RLS policy this migration must NOT touch)
    - .planning/phases/24-ingest-o-robusta-pdf-em-prod-re-import/24-RESEARCH.md ("Pattern 1: CHECK-widening migration" + the full recommended SQL at lines 139-179)
    - src/actions/import.ts:997-1001 (the `update({ status: 'imported' })` that fails the CHECK today and is logged-and-swallowed — DO NOT EDIT, shown for traceability) and import.ts:323-330 (the fast-path this unlocks — DO NOT EDIT)
  </read_first>
  <action>Create `supabase/migrations/0038_statements_status_imported.sql` mirroring `0032_statements_format_pdf.sql` exactly. Two statements, in order: first `alter table public.statements drop constraint if exists statements_status_check;` (drop-if-exists makes it re-runnable and covers a divergent live name defensively, exactly as 0032 did); then `alter table public.statements add constraint statements_status_check check (status in ('uploaded', 'parsing', 'parsed', 'failed', 'imported'));`. Re-use the canonical name `statements_status_check` (Claude's Discretion per D-CONTEXT "Nome exato do constraint"; canonical-name choice keeps drop/add symmetric and self-covering on re-run, matching what 0037 did for `transactions_classification_source_check`). Keep `text + CHECK` — do NOT convert to a Postgres enum (that would break the zero-diff invariant on database.types.ts). The new value set is a strict SUPERSET of the old one, so it is non-destructive — every existing uploaded/parsing/parsed/failed row stays valid; NO backfill. Touch ONLY the CHECK — no `alter policy`, no `grant`, no `disable row level security`, no index change (preserve the V4 access-control RLS). Add a header comment block (mirroring 0032's prose) explaining: why it exists (confirmImport's status update was failing CHECK 23514 and being swallowed, leaving the fast-path unreachable), that it is part of the SCHEMA PUSH GATE (tsc/build pass without it; gap only shows at runtime), that gen:types diff is EMPTY (status stays `string`), and that the PROD `supabase db push` is a deferred human action (autonomous:false, Task 4) bundling 0037. Do NOT place the literal status string set in head-comment prose in a way that a negative grep would trip — this task uses only positive greps, so no allowlist marker is needed.</action>
  <verify>
    <automated>test -f supabase/migrations/0038_statements_status_imported.sql && grep -qE "drop constraint if exists statements_status_check" supabase/migrations/0038_statements_status_imported.sql && grep -qE "add constraint statements_status_check" supabase/migrations/0038_statements_status_imported.sql && grep -qE "check \(status in \('uploaded', 'parsing', 'parsed', 'failed', 'imported'\)\)" supabase/migrations/0038_statements_status_imported.sql && ! grep -qiE "alter policy|disable row level security|create type|as enum" supabase/migrations/0038_statements_status_imported.sql && echo MIGRATION_OK</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/0038_statements_status_imported.sql` exists.
    - Contains `drop constraint if exists statements_status_check` (re-runnable defensive drop).
    - Contains `add constraint statements_status_check` with `check (status in ('uploaded', 'parsing', 'parsed', 'failed', 'imported'))` — the 5-value superset.
    - Contains NO `alter policy`, NO `disable row level security`, NO `create type ... as enum` (constraint-only; RLS/enum untouched).
    - The verify command prints `MIGRATION_OK`.
  </acceptance_criteria>
  <done>The migration file exists, mirrors 0032's drop-if-exists + named-add pattern, widens the status CHECK to the 5-value superset under the canonical name, and touches nothing but the CHECK.</done>
</task>

<task type="auto">
  <name>Task 2: Strengthen the generic PDF degradation test (PDF-07 / SC2) + assert next.config worker include (PDF-06) + assert empty database.types diff</name>
  <files>src/lib/parsers/pdf.test.ts</files>
  <read_first>
    - src/lib/parsers/pdf.test.ts:116-126 (the existing `parseSantanderText — resilience` describe block; the empty-text case at :117-119 already asserts `.not.toThrow()` + full shape `{ rows: [], dropped: 0, capped: false }`; the garbage case at :122-125 currently asserts only `rows.toHaveLength(0)` — this is what to strengthen)
    - .planning/phases/24-ingest-o-robusta-pdf-em-prod-re-import/24-RESEARCH.md ("Pattern 3: PDF-07 generic-degradation test" + the exact recommended edit at lines 310-317, and "Validation Architecture" row (e))
    - next.config.ts:11-30 (the `PDFJS_RUNTIME_ASSETS` worker list + `outputFileTracingIncludes` mapping `/importar` + `/importar/[statementId]`, and `serverExternalPackages: ["pdf-parse"]` — PDF-06 code-complete, asserted source-only, NOT rebuilt)
  </read_first>
  <action>Strengthen the EXISTING garbage test at `src/lib/parsers/pdf.test.ts:122-125` (do NOT create a new file — the pure-function seam lives here). Wrap the garbage call in `expect(() => parseSantanderText('total garbage\nno tx lines here\n', VENC)).not.toThrow()` and add a full-shape assertion `expect(parseSantanderText('total garbage\nno tx lines here\n', VENC)).toEqual({ rows: [], dropped: expect.any(Number), capped: false })` so the test pins "no silently-wrong rows" across rows AND dropped AND capped, matching the rigor of the empty-text case above it. Rename the `it(...)` title to note it locks SC2 generic degradation. Reuse the existing `VENC` fixture constant already in scope in that describe block. Do NOT touch `import.test.ts` (its image-only hard-block at :508-518 and text-present-0-rows-not-a-block at :520-531 already cover the action-level seam and stay green unchanged). The PDF-06 and database.types.ts assertions are NOT new tests — they are verify-step shell assertions in this task's `<verify>` block (grep on next.config.ts for the worker include; `git diff --quiet` on database.types.ts). Do NOT run `npm run gen:types` (a text+CHECK widening leaves the generated type byte-identical; running-and-committing a no-op would fight the pre-commit hook that rewrites the file — assert the diff is empty instead).</action>
  <verify>
    <automated>npx vitest run src/lib/parsers/pdf.test.ts && grep -qE "outputFileTracingIncludes" next.config.ts && grep -qE "pdf\.worker\.mjs" next.config.ts && grep -qE 'serverExternalPackages.*pdf-parse' next.config.ts && grep -qE "not\.toThrow" src/lib/parsers/pdf.test.ts && grep -qE "capped: false" src/lib/parsers/pdf.test.ts && git diff --quiet src/types/database.types.ts && echo PDF_VERIFY_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/parsers/pdf.test.ts` passes (all cases green, including the strengthened garbage case).
    - The strengthened garbage case asserts `.not.toThrow()` AND `.toEqual({ rows: [], dropped: expect.any(Number), capped: false })` (grep finds `not.toThrow` and `capped: false`).
    - `next.config.ts` still contains `outputFileTracingIncludes`, `pdf.worker.mjs`, and `serverExternalPackages` with `pdf-parse` (PDF-06 source assertion — no rebuild).
    - `git diff --quiet src/types/database.types.ts` exits 0 (empty diff — no gen:types no-op committed).
    - The verify command prints `PDF_VERIFY_OK`.
    - import.test.ts is NOT modified.
  </acceptance_criteria>
  <done>The generic degradation test asserts no-throw + full honest-counts shape; next.config.ts worker include is confirmed present by source assertion; database.types.ts is byte-identical.</done>
</task>

<task type="auto">
  <name>Task 3: Local replay-validate the migration (DB-side proof of IMP-07) + keep the re-import mock contract green</name>
  <files>supabase/migrations/0038_statements_status_imported.sql</files>
  <read_first>
    - .planning/phases/24-ingest-o-robusta-pdf-em-prod-re-import/24-RESEARCH.md ("Pattern 2: Local replay validation" lines 181-198 + "Validation Architecture" rows (a)/(b)/(c)/(d)/(f))
    - .planning/STATE.md:179 (0037 deferred-push precedent) and the project precedent that the local stack is often left running (`supabase migration up --local` / `npm run db:reset`)
    - src/actions/import.test.ts:410-432 (the re-import contract: CONFIRMED → alreadyImported; UNCONFIRMED → re-parse) and :508-531 (image-only hard-block + 0-rows-not-a-block) — these already pass; this task only re-confirms they stay green
    - package.json:10-13 (`db:push`, `db:reset`, `gen:types`, `test` scripts)
  </read_first>
  <action>Prove the migration applies cleanly LOCALLY (no PROD required — environment-adaptive, Claude's Discretion per D-CONTEXT "Como replay-validar"). Detect whether the local Supabase stack is up (e.g. `supabase status` succeeds / the API responds). IF UP: apply the new file with `supabase migration up --local` (preferred — applies only 0038) or `npm run db:reset` (replays 0001…0038 from scratch); a clean exit proves the SQL applies and is correctly ordered. Then prove the behavior the CONTEXT calls for ("o replay da migration é a prova do lado-DB"): against the local DB, run an `update public.statements set status = 'imported'` on any existing local statement id and confirm it returns `UPDATE 1` (before 0038 this raised `ERROR 23514 check constraint "statements_status_check"`). IF the local stack is NOT up and cannot be started: fall back to a SQL dry-parse — confirm the file is syntactically valid and idempotent by structural comparison against the proven 0032/0037 templates (two statements, drop-if-exists then named-add, superset value set). Do NOT add a new live-Supabase integration test (env-flaky per project history — MEMORY + CONTEXT "Pular um teste de integração live-Supabase novo"); the existing mock tests + the replay are the agreed proof. Re-run the re-import + PDF mock contract to confirm it stays green. Record in the SUMMARY which replay path was taken (local stack vs dry-parse) and the observed result (`UPDATE 1` vs structural validation).</action>
  <verify>
    <automated>npx vitest run src/actions/import.test.ts -t "alreadyImported" && npx vitest run src/actions/import.test.ts -t "RE-PARSES" && npx vitest run src/actions/import.test.ts -t "image-only" && npx vitest run src/actions/import.test.ts -t "0 matching" && echo IMPORT_CONTRACT_OK</automated>
  </verify>
  <acceptance_criteria>
    - The migration applies cleanly via the chosen replay path (local `supabase migration up --local`/`db:reset` clean exit, OR documented SQL dry-parse against the 0032/0037 template when the stack is down).
    - If the local stack is up: an `update ... set status = 'imported'` on a local statement returns `UPDATE 1` (was 23514) — captured in the SUMMARY.
    - `npx vitest run src/actions/import.test.ts -t "alreadyImported"` passes (CONFIRMED → alreadyImported, import.test.ts:410).
    - `npx vitest run src/actions/import.test.ts -t "RE-PARSES"` passes (UNCONFIRMED → re-parse, import.test.ts:422).
    - `npx vitest run src/actions/import.test.ts -t "image-only"` passes (import.test.ts:508).
    - `npx vitest run src/actions/import.test.ts -t "0 matching"` passes (import.test.ts:520).
    - NO new live-Supabase integration test file is added.
    - The verify command prints `IMPORT_CONTRACT_OK`.
  </acceptance_criteria>
  <done>The migration is replay-validated locally (or dry-parse-validated when the stack is down), the DB-side update-to-'imported' proof is captured, and the re-import + PDF mock contracts stay green.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 4: [DEFERRED / autonomous:false] PROD `supabase db push` of 0037 + 0038</name>
  <action>DEFERRED HUMAN ACTION (cannot run autonomously — interactive auth / `SUPABASE_ACCESS_TOKEN` required). Run `supabase db push` against the LIVE PROD project, which replays ALL un-applied migrations in order and so applies BOTH the still-pending 0037 AND the new 0038 in one push. Then `npm run gen:types` and confirm `git diff --quiet src/types/database.types.ts` exits 0. Does NOT block code-side phase closure — defer if PROD credentials are not at hand.</action>
  <files>supabase/migrations/0037_*.sql, supabase/migrations/0038_statements_status_imported.sql</files>
  <what-built>The autonomous tasks wrote `supabase/migrations/0038_statements_status_imported.sql` (widens `statements.status` CHECK to include `'imported'`) and replay-validated it locally. The still-pending `0037` (the `transactions.classification_source` 'palavra-chave' widening, deferred since Phase 21 — STATE.md:179) is also un-pushed to PROD.</what-built>
  <how-to-verify>
    This step CANNOT run autonomously — `supabase db push` against the LIVE production project needs interactive auth / `SUPABASE_ACCESS_TOKEN` (it is a credential gate, not an implementation gap). Per D-CONTEXT "PROD push é human-gated" and the schema-push gate.

    Run, with PROD credentials available:
    1. Ensure `SUPABASE_ACCESS_TOKEN` is set OR run `supabase login` (interactive).
    2. `supabase db push` — this replays ALL un-applied migrations in order, so it applies **both 0037 AND 0038** in the same push (do NOT push 0038 alone, or PROD drifts behind the repo — Pitfall 4). Confirm the output lists 0037 then 0038 applied.
    3. `npm run gen:types` — then `git diff --quiet src/types/database.types.ts` MUST exit 0 (empty diff — a text+CHECK widening leaves the generated type `string`; do NOT commit a byte-identical no-op as a change). The known pre-commit hook that rewrites this file should leave it byte-identical.
    4. (Optional sanity) Against PROD, an `update public.statements set status='imported'` on any statement now succeeds instead of raising 23514.

    This bundles the deferred 0037 push (STATE.md:179) with the new 0038 in one push, mirroring how Phase 21 deferred 0037.
  </how-to-verify>
  <resume-signal>Type "pushed" once `supabase db push` applied 0037 + 0038 to PROD and `git diff --quiet src/types/database.types.ts` exited 0; or "defer" to leave the PROD push pending and close the phase in code (per D-CONTEXT — the push does not block code-side phase closure).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app server → Postgres | `confirmImport`/`ingestStatement` write `statements.status` server-side; the CHECK is the DB-side backstop. No user input crosses into `status`. |
| developer machine → PROD Postgres | `supabase db push` applies migrations with elevated credentials (human-gated). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Elevation of Privilege | `0038` migration on `statements` | mitigate | Migration touches ONLY the CHECK — no `alter policy`, no `grant`, no `disable row level security`. Task 1 verify negative-greps for `alter policy`/`disable row level security` so the `"own statements"` RLS (0019:33-37) is provably preserved. |
| T-24-02 | Tampering / integrity | PROD schema drift (0037 un-pushed) | mitigate | The deferred push task bundles 0037 + 0038 in the same `supabase db push` (which replays all un-applied migrations in order). The human-action task body names both (Pitfall 4). |
| T-24-03 | Tampering | `statements.status` value injection | accept | `status` is set only by server code (`ingestStatement`/`confirmImport`), never from client input; the widened CHECK backstops it. No new surface. |
| T-24-SC | Tampering | npm/pip/cargo installs | accept | This phase installs ZERO external packages (RESEARCH "Package Legitimacy Audit — Not applicable"). No install task; no legitimacy checkpoint needed. |
</threat_model>

<verification>
Phase-gate checks (run before /gsd-verify-work):
- `npm test` — full suite green (re-import + PDF degradation contracts intact).
- `npx vitest run src/lib/parsers/pdf.test.ts src/actions/import.test.ts` — the targeted ingestion + degradation tests pass.
- Local migration replay clean (`supabase migration up --local`/`db:reset` exit 0) OR documented dry-parse when the stack is down.
- `git diff --quiet src/types/database.types.ts` exits 0 (empty — no gen:types no-op).
- `grep outputFileTracingIncludes next.config.ts` confirms the PDF-06 worker include is present (source assertion, no rebuild).
- Migration negative-greps clean: NO `alter policy` / `disable row level security` / `create type ... as enum` in 0038.

Deferred (autonomous:false / human):
- PROD `supabase db push` of 0037 + 0038 + `npm run gen:types` empty-diff check (Task 4).
- SC1 live PDF upload in PROD after deploy — human-verify UAT (like Phase 22), tracked as a deferred verification item, not a code gate.
</verification>

<success_criteria>
- `supabase/migrations/0038_statements_status_imported.sql` exists, mirrors 0032, widens the status CHECK to the 5-value superset under `statements_status_check`, and is re-runnable + non-destructive (IMP-07).
- The strengthened `pdf.test.ts` garbage case asserts no-throw + full honest-counts shape `{ rows: [], dropped: <n>, capped: false }` (PDF-07 / SC2).
- Source assertion confirms `next.config.ts` force-includes the pdfjs worker (PDF-06 — no rebuild; SC1 is a deferred UAT item).
- `database.types.ts` diff is empty; no Postgres enum introduced; RLS/grants/indexes untouched.
- Re-import + PDF mock contracts (import.test.ts:410/422/508/520) stay green; local replay proves the DB-side `update → status='imported'` succeeds.
- The PROD push is encoded as a single `autonomous:false` blocking-human task bundling 0037 + 0038 (does not block code-side phase closure).
</success_criteria>

<output>
Create `.planning/phases/24-ingest-o-robusta-pdf-em-prod-re-import/24-01-SUMMARY.md` when done. Record: the replay path taken (local stack vs dry-parse) and its observed result (`UPDATE 1` vs structural validation); confirmation that database.types.ts stayed byte-identical; and that the PROD push (0037 + 0038) + the SC1 live-PDF UAT remain deferred human items.
</output>
