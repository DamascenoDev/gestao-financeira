---
phase: 21-match-wildcard-proced-ncia-persistida
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0037_transactions_classification_source_palavra_chave.sql
  - src/types/database.types.ts
autonomous: false
requirements: [KW-10]
must_haves:
  truths:
    - "A migration 0037 dropa o CHECK anônimo de 0020 e o recria ampliado para incluir 'palavra-chave'"
    - "Após o db push, um INSERT de classification_source='palavra-chave' em transactions SUCEDE (não falha 23514)"
    - "O conjunto antigo ('memória','manual','sugerida',null) continua aceito (sem regressão)"
  artifacts:
    - path: "supabase/migrations/0037_transactions_classification_source_palavra_chave.sql"
      provides: "drop+recreate do CHECK de transactions.classification_source ampliando para 'palavra-chave'"
      contains: "palavra-chave"
  key_links:
    - from: "supabase/migrations/0037_transactions_classification_source_palavra_chave.sql"
      to: "live PROD transactions CHECK"
      via: "supabase db push aplica a constraint ampliada"
      pattern: "drop constraint if exists transactions_classification_source_check"
---

<objective>
KW-10 (substrato DB). Hoje o CHECK ANÔNIMO da migration `0020_transactions_import.sql:25-27` só aceita `('memória','manual','sugerida')` + null, então o pipeline grava o coarse `'memória'` mesmo para hits de palavra-chave (o bug exato documentado em STATE.md/memória do projeto). Esta migration amplia o conjunto permitido para incluir `'palavra-chave'` (per D persisted provenance em 21-CONTEXT.md).

Mantém `text` + CHECK (NÃO converte para enum Postgres — isso mudaria database.types.ts e quebraria a premissa de tipo inalterado). SEM backfill das linhas históricas coarse `'memória'` (não reconstruíveis — locked). O `database.types.ts` é um no-op diff (coluna continua `text` → `string | null`), mas `gen:types` roda por disciplina do projeto.

LANDMINE (RESEARCH Assumption A1 / Open Question Q1): o CHECK de 0020 foi criado anonimamente (`add column … check(…)` sem `constraint <nome>`), então o Postgres o nomeou por convenção `transactions_classification_source_check`. A migration DEVE dropar pelo nome live correto — se o nome live divergir, o `DROP CONSTRAINT IF EXISTS` vira no-op e o CHECK estreito sobrevive → inserts de `'palavra-chave'` falham com 23514. VERIFICAR o nome live ANTES de escrever a migration.

Purpose: Habilitar a gravação honesta da procedência keyword — o Plano 04 só consegue persistir `'palavra-chave'` se este CHECK existir live.
Output: migration 0037 + db push aplicado (local e PROD) + gen:types confirmado no-op.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-match-wildcard-proced-ncia-persistida/21-CONTEXT.md
@.planning/phases/21-match-wildcard-proced-ncia-persistida/21-RESEARCH.md
@supabase/migrations/0020_transactions_import.sql
@supabase/migrations/0036_category_keywords.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verificar o nome live do CHECK e escrever a migration 0037</name>
  <files>supabase/migrations/0037_transactions_classification_source_palavra_chave.sql</files>
  <read_first>
    - supabase/migrations/0020_transactions_import.sql (o CHECK anônimo nas linhas 25-27 — o alvo do widening; conjunto atual 'memória','manual','sugerida' + null)
    - supabase/migrations/0036_category_keywords.sql (template de estilo de migration do repo — RLS/grants/índices/comentário de cabeçalho)
    - .planning/phases/21-match-wildcard-proced-ncia-persistida/21-RESEARCH.md (§Runtime State "The canonical question (CHECK constraint)"; §Code Examples "The 0037 migration shape"; Assumption A1; Open Question Q1)
  </read_first>
  <action>
    PRIMEIRO verifique o nome live do CHECK contra a DB local (e idealmente PROD), porque um nome divergente faz o DROP virar no-op (RESEARCH Q1). Use o stack local do Supabase: rode `supabase db` localmente e inspecione com `\d+ public.transactions`, OU rode `select conname from pg_constraint where conrelid = 'public.transactions'::regclass and contype='c';` para listar os CHECKs e achar o de classification_source. O nome quase certamente é `transactions_classification_source_check` (convenção `<table>_<column>_check`), mas NÃO adivinhe — confirme o nome real e use-o no DROP.

    Escreva a migration em `supabase/migrations/0037_transactions_classification_source_palavra_chave.sql` (nome de arquivo à discrição per D Claude's Discretion — use um slug descritivo `0037_*`). Conteúdo: (a) `alter table public.transactions drop constraint if exists <NOME_LIVE_VERIFICADO>;` (b) `alter table public.transactions add constraint transactions_classification_source_check check (classification_source is null or classification_source in ('memória','manual','sugerida','palavra-chave'));`. Mantenha `text` + CHECK — NÃO converta para enum Postgres (RESEARCH Anti-Patterns). NÃO faça backfill (locked). Adicione um comentário de cabeçalho no estilo de 0036 explicando KW-10, o CHECK anônimo de 0020, a ausência de backfill, e que o tipo TS não muda. NÃO toque em RLS (inalterada).
  </action>
  <verify>
    <automated>test -f supabase/migrations/0037_transactions_classification_source_palavra_chave.sql && grep -c "palavra-chave" supabase/migrations/0037_transactions_classification_source_palavra_chave.sql</automated>
  </verify>
  <acceptance_criteria>
    - O arquivo `supabase/migrations/0037_*.sql` existe
    - `grep -i "drop constraint if exists" supabase/migrations/0037_*.sql` cita o nome live VERIFICADO do CHECK (não um palpite — registre no SUMMARY como o nome foi confirmado)
    - `grep "palavra-chave" supabase/migrations/0037_*.sql` aparece no `add constraint … check (… in (…,'palavra-chave'))` E o conjunto antigo ('memória','manual','sugerida') continua presente
    - A migration NÃO converte a coluna para enum (continua `text`); NÃO contém UPDATE/backfill de linhas existentes
    - O comentário de cabeçalho explica KW-10 + a constraint anônima de 0020
  </acceptance_criteria>
  <done>Migration 0037 escrita com o nome live correto do CHECK, ampliando para 'palavra-chave', sem enum, sem backfill.</done>
</task>

<task type="auto">
  <name>[BLOCKING] Task 2: Aplicar a migration (db push) e confirmar gen:types no-op</name>
  <files>supabase/migrations/0037_transactions_classification_source_palavra_chave.sql, src/types/database.types.ts</files>
  <read_first>
    - supabase/migrations/0037_transactions_classification_source_palavra_chave.sql (a migration do Task 1)
    - .planning/phases/21-match-wildcard-proced-ncia-persistida/21-RESEARCH.md (§4b db push + gen:types workflow; §Environment Availability — dev-env: pre-commit hook reescreve database.types.ts; rodar local primeiro, depois PROD)
    - src/types/database.types.ts (a coluna classification_source ~linha 663 já é `string | null` — o gen:types deve ser no-op diff)
  </read_first>
  <action>
    [BLOCKING] — a schema-push-detection está OFF no config, então esta etapa NÃO é auto-injetada; ela é obrigatória aqui. Aplique a migration 0037 com `supabase db push` (para o stack local primeiro, depois PROD). Para execução não-TTY, exporte `SUPABASE_ACCESS_TOKEN` antes do push. Build/type-checks passam SEM o CHECK live ampliado, então uma migration não-aplicada é um falso-positivo — o push tem que acontecer ANTES da verificação do Plano 04. Depois rode `npm run gen:types` (per disciplina do CLAUDE.md) — espere ZERO diff funcional em database.types.ts (a coluna é `text`; o pre-commit hook o reescreve de qualquer forma). Se o push exigir auth interativa que não pode ser suprimida com o token, PARE e sinalize ao usuário (esta task é `autonomous: false` por isso). Confirme o sucesso provando que um INSERT de `'palavra-chave'` é aceito (ex.: contra o local: insira uma transaction de teste com classification_source='palavra-chave' e verifique que não falha 23514; ou via a verificação do Plano 04).
  </action>
  <verify>
    <human-check>
      1. `supabase db push` aplicou 0037 ao local e ao PROD sem erro
      2. Um INSERT de transactions com classification_source='palavra-chave' SUCEDE (não 23514) — provando o CHECK ampliado está live
      3. `npm run gen:types` produz zero diff funcional em src/types/database.types.ts (coluna continua string | null)
    </human-check>
  </verify>
  <acceptance_criteria>
    - A migration 0037 aparece como aplicada no histórico do Supabase (local e PROD)
    - Um INSERT de classification_source='palavra-chave' em transactions é aceito live (sem SQLSTATE 23514) — esta é a prova de KW-10 que o Plano 04 depende
    - `git diff src/types/database.types.ts` após gen:types é vazio ou apenas cosmético (sem mudança de tipo da coluna)
  </acceptance_criteria>
  <done>0037 aplicada live (local + PROD); INSERT de 'palavra-chave' aceito; gen:types no-op confirmado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration SQL → live PROD schema | a constraint live de transactions é estado de produção; um drop pelo nome errado deixa o CHECK estreito sobreviver |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-06 | Tampering (stale/wrong CHECK name) | migration 0037 DROP | mitigate | verificar o nome live do CHECK (pg_constraint/`\d+`) ANTES de escrever; testar que um INSERT de 'palavra-chave' sucede pós-migration (RESEARCH Q1, A1, §Security "Wrong/stale CHECK name") |
| T-21-07 | Tampering (constraint bypass) | classification_source value set | mitigate | o CHECK pós-0037 é o gate DB-level; o Plano 04 só produz valores do conjunto permitido |
| T-21-08 | Access Control | RLS de transactions | accept | a 0037 muda só um CHECK — RLS (`auth.uid() = user_id`) intocada (RESEARCH §Security V4) |
| T-21-SC | Tampering | npm/pip/cargo installs | n/a | Nenhum pacote instalado (RESEARCH §Package Legitimacy Audit: not applicable). `supabase` CLI já em uso desde 0036 |
</threat_model>

<verification>
- Migration 0037 existe, cita o nome live verificado do CHECK, amplia para 'palavra-chave' sem enum/backfill
- db push aplicado local + PROD; INSERT de 'palavra-chave' aceito (sem 23514)
- gen:types confirmado no-op
</verification>

<success_criteria>
- transactions.classification_source aceita 'palavra-chave' live (CHECK ampliado), conjunto antigo intacto
- Sem conversão para enum; sem backfill histórico
- database.types.ts inalterado em tipo
</success_criteria>

<output>
Create `.planning/phases/21-match-wildcard-proced-ncia-persistida/21-03-SUMMARY.md` when done
</output>
