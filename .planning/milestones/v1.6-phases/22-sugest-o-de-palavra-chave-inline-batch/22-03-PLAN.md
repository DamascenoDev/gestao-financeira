---
phase: 22-sugest-o-de-palavra-chave-inline-batch
plan: 03
type: execute
wave: 2
depends_on: ["22-02"]
files_modified:
  - src/components/keyword-suggestions-dialog.tsx
  - src/components/keyword-suggestions-launcher.tsx
  - src/app/(app)/categorias/page.tsx
  - src/components/keyword-suggestions-dialog.test.tsx
autonomous: true
requirements: [KW-08]
tags: [keyword, suggestions, dialog, categorias]

must_haves:
  truths:
    - "A 'Sugerir palavras-chave' button in the /categorias header opens a global dialog that, on open, calls getKeywordSuggestions() and lists the returned candidates (checkbox · editable term · editable category · '{N} usos' · discard X)."
    - "Each candidate's term and category are editable before approval; 'Aprovar selecionadas (N)' calls approveKeywordSuggestions(selected) and toasts the created/skipped count, removing approved rows."
    - "Discarding a candidate (X) removes it from the list with NO server call (session-only); the empty/zero-candidate state renders the Empty primitive."
    - "/categorias/page.tsx stays an RSC; a small client launcher component owns the dialog open state (mirrors CategoryRowActions)."
  artifacts:
    - path: "src/components/keyword-suggestions-dialog.tsx"
      provides: "Global batch dialog: load-on-open, multi-select, edit-before-approve, bulk approve, session discard, Empty state"
      min_lines: 60
    - path: "src/components/keyword-suggestions-launcher.tsx"
      provides: "Client component owning open state + the 'Sugerir palavras-chave' trigger, rendering the dialog"
      contains: "useState"
    - path: "src/app/(app)/categorias/page.tsx"
      provides: "Header trigger mounting the launcher beside 'Nova categoria' (RSC unchanged otherwise)"
      contains: "KeywordSuggestionsLauncher"
    - path: "src/components/keyword-suggestions-dialog.test.tsx"
      provides: "jsdom tests: load-on-open lists candidates; approve calls approveKeywordSuggestions; discard makes NO server call; Empty state"
  prohibitions:
    - "Discard must make NO server call (no approveKeywordSuggestions, no removeKeyword) — it is pure local-state removal (session-only, no dismissed table)."
    - "The candidate 'already-covered' filter is NOT done in the client — the dialog renders whatever getKeywordSuggestions returns (server already filtered)."
    - "No new shadcn primitive, no new token, no third-party registry — compose only already-vendored primitives."
    - "The trigger button is NOT a primary gold CTA — use the default/outline variant matching the existing 'Nova categoria' affordance (UI-SPEC §Color)."
    - "page.tsx must remain a server component (no 'use client') — open state lives in the launcher."
  key_links:
    - from: "src/components/keyword-suggestions-dialog.tsx"
      to: "src/actions/category-keywords.ts (getKeywordSuggestions, approveKeywordSuggestions)"
      via: "getKeywordSuggestions() on open; approveKeywordSuggestions(selected) on bulk approve"
      pattern: "getKeywordSuggestions|approveKeywordSuggestions"
    - from: "src/app/(app)/categorias/page.tsx"
      to: "src/components/keyword-suggestions-launcher.tsx"
      via: "rendered in the header row beside CategoriaForm"
      pattern: "KeywordSuggestionsLauncher"
---

<objective>
Deliver the KW-08 client side: a global "Sugestões de palavras-chave" dialog on `/categorias` that loads candidates from `getKeywordSuggestions()` on open, lets the user multi-select + edit term/category, bulk-approves via `approveKeywordSuggestions()`, and discards session-only — plus a small client launcher mounted in the (still-RSC) `/categorias` header.

Purpose: Give the user a global, opt-in surface to convert confirmed merchant patterns into keywords in bulk, with no auto-creation and no schema changes.
Output: New `keyword-suggestions-dialog.tsx` + `keyword-suggestions-launcher.tsx`, modified `categorias/page.tsx`, new `keyword-suggestions-dialog.test.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-sugest-o-de-palavra-chave-inline-batch/22-CONTEXT.md
@.planning/phases/22-sugest-o-de-palavra-chave-inline-batch/22-RESEARCH.md
@.planning/phases/22-sugest-o-de-palavra-chave-inline-batch/22-PATTERNS.md
@.planning/phases/22-sugest-o-de-palavra-chave-inline-batch/22-UI-SPEC.md

@src/components/category-keywords-dialog.tsx
@src/components/category-row-actions.tsx
@src/app/(app)/categorias/page.tsx
@src/components/category-keywords-dialog.test.tsx
@.planning/phases/22-sugest-o-de-palavra-chave-inline-batch/22-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build keyword-suggestions-dialog.tsx (load-on-open, multi-select, edit, bulk approve, session discard)</name>
  <read_first>
    - src/components/category-keywords-dialog.tsx — the skeleton to mirror (1-188): `'use client'`; controlled `{ open, onOpenChange }` props (32-37); `useTransition` + `isPending` (60); `React.useId()` for label ids (66); `Dialog`/`DialogContent`/`DialogHeader`/`DialogTitle`/`DialogDescription`/`DialogFooter className="mt-6"`/`DialogClose render={<Button variant="outline">Fechar</Button>}` (114-185); `Empty`/`EmptyHeader`/`EmptyTitle`/`EmptyDescription` (125-134); `Badge variant="secondary" className="gap-1"` chip recipe (138) with an `X className="size-3"` button (140-148); the action-call + result branching in onSubmit (96-111).
    - src/components/import-review-table.tsx — the inline `Select`/`SelectTrigger`/`SelectValue`/`SelectContent`/`SelectItem` + `CategoryBadge` grammar (947-970) for the per-candidate editable category control; `Checkbox` import (line 39) for multi-select.
    - src/actions/category-keywords.ts (after Plan 02) — the new exports to import: `getKeywordSuggestions(): Promise<{ ok: true; suggestions: KeywordSuggestion[] } | { error }>`, `approveKeywordSuggestions(items): Promise<{ ok: true; created; skipped } | { error }>`, `type KeywordSuggestion`.
    - .planning/phases/22-sugest-o-de-palavra-chave-inline-batch/22-UI-SPEC.md — §Copywriting Contract (Batch table: dialog title "Sugestões de palavras-chave", description, "{N} usos", discard aria-label "Descartar sugestão {keyword}", "Selecionar todas", "Aprovar selecionadas ({N})"/"Aprovando…", "Fechar", success toast "{N} palavras-chave criadas." / "{N} criadas · {M} já cadastradas.", Empty heading/body, error fallbacks); §Interaction & Layout (dialog anatomy, candidate row order checkbox→term→category→usos→discard, scroll region, bulk approve keeps dialog open, discard = local-only).
  </read_first>
  <files>src/components/keyword-suggestions-dialog.tsx</files>
  <action>
    Create `src/components/keyword-suggestions-dialog.tsx` (`'use client'`), mirroring `category-keywords-dialog.tsx`. It needs the user's categories for the per-candidate category `Select`, so accept props `{ open: boolean; onOpenChange: (open: boolean) => void; categories: { id: string; name: string; color: string | null }[] }` (the launcher passes the same category list the RSC already has). Import `getKeywordSuggestions, approveKeywordSuggestions, type KeywordSuggestion` from `@/actions/category-keywords`; `toast` from sonner; `useTransition`/`React.useState`/`React.useId`; the `Dialog*` shell, `Empty*`, `Checkbox`, `Select*`, `Input`, `Badge`, `Button`, `CategoryBadge`, and the `X` lucide icon — ALL already vendored (do NOT add primitives).

    Behavior:
    1. Local state: `candidates` (an editable working copy: `Array<{ key: string; descriptorNorm: string; term: string; categoryId: string; categoryName: string; hitCount: number; selected: boolean }>` — derive `key` from descriptorNorm+categoryId; `term` starts as `descriptorNorm`), a `loaded`/`loading` flag, and `isPending` from `useTransition`.
    2. Load-on-open: when `open` flips to true (a `React.useEffect` keyed on `open`, or load inside the launcher and pass down — Claude's discretion), call `getKeywordSuggestions()` inside a transition; on `{ ok }` seed `candidates` from `suggestions` (selected:false, term:descriptorNorm); on `{ error }` `toast.error(r.error)` with the fallback "Não foi possível carregar as sugestões. Tente novamente.". Show a brief pending affordance while loading (disabled controls / "Carregando…" — do NOT block with a spinner overlay; matches the dialog's loading convention).
    3. Candidate list: a scrollable `flex flex-col gap-2` region (`max-h-[...] overflow-y-auto`) so header/footer stay pinned. Optional list-header "Selecionar todas" `Checkbox`. Each candidate row is `flex items-center gap-2` in DOM order: a `Checkbox` (multi-select, toggles `selected`); the editable term `Input` (`maxLength={60}`, bound to `term`, labeled via `aria-label` keyed to the descriptor so SR users disambiguate rows); the editable category control (a `Select` of `categories` rendering `CategoryBadge`, prefilled with `categoryId`); the "{hitCount} usos" hint (`text-xs text-muted-foreground tabular-nums`); a neutral muted discard `<button>` with `X className="size-3"` and `aria-label={\`Descartar sugestão ${term}\`}`.
    4. Discard: removes that candidate from local state ONLY — NO action call (Pitfall 5: never wire to removeKeyword/approve). Session-only.
    5. Empty state: when `candidates.length === 0` after load, render `Empty` with heading "Nenhuma sugestão por enquanto" + the body copy from UI-SPEC, INSTEAD of the list.
    6. Footer: primary "Aprovar selecionadas" — disabled when 0 selected; show count when ≥1 ("Aprovar selecionadas ({N})"); pending label "Aprovando…". On click, `startTransition(async () => { const r = await approveKeywordSuggestions(selected.map((c) => ({ categoryId: c.categoryId, keyword: c.term }))); if ('error' in r) { toast.error(r.error /* fallback "Não foi possível aprovar. Tente novamente." */); return } toast.success(r.skipped > 0 ? \`${r.created} criadas · ${r.skipped} já cadastradas.\` : \`${r.created} ${r.created === 1 ? 'palavra-chave criada' : 'palavras-chave criadas'}.\`); /* remove the approved (selected) rows from local state; keep dialog open */ })`. Plus a `DialogClose render={<Button variant="outline">Fechar</Button>}`.
    7. Do NOT re-filter candidates client-side — render exactly what the server returned (the already-covered exclusion is server-side per Plan 02). Edited terms are re-validated + normalized server-side by `approveKeywordSuggestions`; an empty/`*`-only term blocks only that row's approval (counted as skipped), not the whole batch.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "keyword-suggestions-dialog" || echo "no type errors in keyword-suggestions-dialog"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "getKeywordSuggestions" src/components/keyword-suggestions-dialog.tsx` ≥ 1 and `grep -c "approveKeywordSuggestions" src/components/keyword-suggestions-dialog.tsx` ≥ 1.
    - The discard handler makes NO action call: `grep -n "removeKeyword" src/components/keyword-suggestions-dialog.tsx` returns nothing.
    - The literal pt-BR strings "Sugestões de palavras-chave", "Aprovar selecionadas", "Nenhuma sugestão por enquanto", "usos" appear in the file (UI-SPEC §Copywriting).
    - `grep -c "'use client'" src/components/keyword-suggestions-dialog.tsx` === 1.
    - `npx tsc --noEmit` reports no new errors.
  </acceptance_criteria>
  <done>The batch dialog loads candidates on open via `getKeywordSuggestions`, renders the multi-select/edit/usos/discard rows (or the Empty state), bulk-approves via `approveKeywordSuggestions` with the created/skipped toast, and discards session-only with no server call — composing only vendored primitives.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the launcher + wire the /categorias header trigger (RSC preserved)</name>
  <read_first>
    - src/components/category-row-actions.tsx — the client-owns-open-state pattern (1-109): `'use client'`, `React.useState(false)` per dialog, the trigger toggles it, the controlled `<Dialog open onOpenChange>` is rendered alongside. Mirror this for the launcher.
    - src/app/(app)/categorias/page.tsx — the RSC (1-162): the header row `<div className="flex items-start justify-between gap-4"> <h1>Categorias</h1> <CategoriaForm /> </div>` (94-97); the categories fetch (`id, name, kind, color, is_archived, sort`, 36-41) — the launcher needs `{ id, name, color }[]`, which is already in `rows`/`categories`. The page must stay a server component (no `'use client'`).
    - .planning/phases/22-sugest-o-de-palavra-chave-inline-batch/22-UI-SPEC.md — §Color (the trigger is default/outline, NOT primary gold — align variant with the existing "Nova categoria" button) and §Interaction (entry point: "Sugerir palavras-chave" beside "Nova categoria").
  </read_first>
  <files>src/components/keyword-suggestions-launcher.tsx, src/app/(app)/categorias/page.tsx</files>
  <action>
    Step A — create `src/components/keyword-suggestions-launcher.tsx` (`'use client'`), mirroring `CategoryRowActions`'s client-owns-open-state pattern: accept prop `categories: { id: string; name: string; color: string | null }[]`; hold `const [open, setOpen] = React.useState(false)`; render a trigger `<Button>` labeled "Sugerir palavras-chave" (variant matching the existing "Nova categoria"/CategoriaForm trigger — default or `outline`, NOT a gold primary CTA per UI-SPEC §Color) that calls `setOpen(true)`, and render `<KeywordSuggestionsDialog open={open} onOpenChange={setOpen} categories={categories} />`. Export `KeywordSuggestionsLauncher`.

    Step B — edit `src/app/(app)/categorias/page.tsx`: import `KeywordSuggestionsLauncher`, and in the header row (lines 94-97) add it beside `<CategoriaForm />`, passing the category list the RSC already fetched mapped to `{ id, name, color }` (e.g. `rows.map((c) => ({ id: c.id, name: c.name, color: c.color }))`). Make NO other change to the page; it MUST remain an RSC (do NOT add `'use client'`, do NOT change the existing fetches/table). Place the launcher so the header reads `<h1>Categorias</h1>` then the two affordances (CategoriaForm + the new launcher) — group them in a small `flex gap-2` wrapper if needed so they sit together on the right.
  </action>
  <verify>
    <automated>grep -L "use client" src/app/\(app\)/categorias/page.tsx && npx tsc --noEmit 2>&1 | grep -E "categorias/page|keyword-suggestions-launcher" || echo "page stays RSC, no type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'use client'" "src/app/(app)/categorias/page.tsx"` === 0 (the page stays a server component).
    - `grep -c "KeywordSuggestionsLauncher" "src/app/(app)/categorias/page.tsx"` ≥ 1 (mounted in the header).
    - `grep -c "'use client'" src/components/keyword-suggestions-launcher.tsx` === 1 and it holds `React.useState` for the open flag.
    - The launcher's trigger uses a non-primary variant (default/outline) — NOT a gold primary CTA (verify the Button variant prop matches CategoriaForm's trigger family).
    - `npx tsc --noEmit` reports no new errors.
  </acceptance_criteria>
  <done>A `'Sugerir palavras-chave'` trigger sits in the `/categorias` header beside "Nova categoria", owned by a client launcher that holds the dialog open state and passes the category list; `/categorias/page.tsx` remains an RSC.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: keyword-suggestions-dialog.test.tsx — load, approve, discard-no-call, Empty</name>
  <read_first>
    - src/components/category-keywords-dialog.test.tsx — the jsdom harness (1-106): `render`/`screen`/`fireEvent` from @testing-library/react; `vi.mock('@/actions/category-keywords', ...)` stubbing the actions; `vi.mock('sonner', ...)`; the `await Promise.resolve()` double-flush; `expect(action).toHaveBeenCalledWith(...)` assertions; the Empty-state render assertion.
    - src/components/keyword-suggestions-dialog.tsx (this plan, Task 1) — the props + behavior under test.
    - .planning/phases/22-sugest-o-de-palavra-chave-inline-batch/22-RESEARCH.md §Validation Architecture — the KW-08 component rows (select/edit/approve/discard; discard = no server call).
  </read_first>
  <files>src/components/keyword-suggestions-dialog.test.tsx</files>
  <action>
    Create `src/components/keyword-suggestions-dialog.test.tsx`, mirroring `category-keywords-dialog.test.tsx`. Mock the actions: `vi.mock('@/actions/category-keywords', () => ({ getKeywordSuggestions: vi.fn(async () => ({ ok: true, suggestions: [{ descriptorNorm: 'uber trip', categoryId: 'cat-1', categoryName: 'Transporte', hitCount: 3 }, { descriptorNorm: 'ifood centro', categoryId: 'cat-1', categoryName: 'Transporte', hitCount: 1 }] })), approveKeywordSuggestions: vi.fn(async () => ({ ok: true, created: 1, skipped: 0 })) }))` and `vi.mock('sonner', () => ({ toast: { success: vi.fn(), info: vi.fn(), error: vi.fn() } }))`. Provide a `CATEGORIES` fixture `[{ id: 'cat-1', name: 'Transporte', color: 'blue' }]`.

    Cases (render `<KeywordSuggestionsDialog open onOpenChange={() => {}} categories={CATEGORIES} />`, flushing the open-load transition with the double `await Promise.resolve()`):
    1. "loads candidates on open and lists them": after flush, assert `getKeywordSuggestions` was called and both candidate descriptors ("uber trip", "ifood centro") render as editable term inputs / rows.
    2. "approve calls approveKeywordSuggestions with the selected items": select the first candidate (click its checkbox), click "Aprovar selecionadas" (now "Aprovar selecionadas (1)"), flush; assert `approveKeywordSuggestions` was called with an array containing `{ categoryId: 'cat-1', keyword: 'uber trip' }`, and `toast.success` fired.
    3. "discard makes NO server call": click a candidate's discard control (`aria-label` "Descartar sugestão uber trip"); assert the row is removed AND neither `approveKeywordSuggestions` nor any other action was called (the discard handler is local-only).
    4. "renders the Empty state when there are no candidates": override `getKeywordSuggestions` for this case to resolve `{ ok: true, suggestions: [] }` (`vi.mocked(getKeywordSuggestions).mockResolvedValueOnce(...)`), render, flush; assert "Nenhuma sugestão por enquanto" is in the document and no candidate rows render.
    5. "approve is disabled with nothing selected": after load, assert the "Aprovar selecionadas" button is disabled until at least one checkbox is checked.

    Mirror the existing dialog test's flush + query style.
  </action>
  <verify>
    <automated>npx vitest run src/components/keyword-suggestions-dialog.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/keyword-suggestions-dialog.test.tsx` passes (≥ 5 cases).
    - The suite asserts `getKeywordSuggestions` is called on open and both candidates render.
    - The suite asserts `approveKeywordSuggestions` is called with the selected `{ categoryId, keyword }` items and that discard calls NO action (assert `approveKeywordSuggestions` not called after a discard-only interaction).
    - The suite asserts the Empty state renders for an empty suggestions result.
  </acceptance_criteria>
  <done>`keyword-suggestions-dialog.test.tsx` proves load-on-open lists candidates, bulk approve calls `approveKeywordSuggestions(selected)` + toasts, discard is side-effect-free (no action call), the Empty state renders, and approve is disabled at zero selection — all with the actions mocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (dialog) → server actions | The selected candidates' edited terms + category_ids cross into `approveKeywordSuggestions` (server re-validates). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-07 | Tampering | dialog → approveKeywordSuggestions | accept (mitigated in Plan 02) | The dialog cannot bypass validation — `approveKeywordSuggestions` re-runs Zod + normalize + owner-gate + RLS per item (Plan 02). This plan adds only client composition; the edited term/category are untrusted and fully re-checked server-side. |
| T-22-08 | Information Disclosure | candidate list | accept (mitigated in Plan 02) | The client only ever receives the pre-filtered candidate shape from `getKeywordSuggestions` (RLS-scoped, no raw merchant rows). The dialog does not fetch merchant_patterns directly. |
| T-22-SC | Tampering | npm/pip/cargo installs | mitigate | RESEARCH §Package Legitimacy Audit: ZERO packages installed (no `npm install`, no `npx shadcn add` — every primitive is vendored). No `[ASSUMED]`/`[SUS]` package → no install checkpoint. |
</threat_model>

<verification>
- `npx vitest run src/components/keyword-suggestions-dialog.test.tsx` — green.
- `npx tsc --noEmit` — no new type errors.
- `grep -c "'use client'" "src/app/(app)/categorias/page.tsx"` === 0 — the page stays an RSC.
- `npm test` (full Vitest run) green at the wave-merge gate, covering Plans 01-03 together.
</verification>

<success_criteria>
- KW-08 client surface satisfied: a global dialog on `/categorias` loads server-computed candidates, supports multi-select + edit-before-approve, bulk-approves via `approveKeywordSuggestions`, discards session-only (no schema, no server call), and shows the Empty state when there are none.
- `/categorias/page.tsx` remains an RSC; the launcher owns the open state (mirrors `CategoryRowActions`).
- The trigger is a non-primary affordance that does not compete with "Nova categoria" (UI-SPEC §Color).
- NO new shadcn primitive / token / registry; NO migration / gen:types / new package.
</success_criteria>

<output>
Create `.planning/phases/22-sugest-o-de-palavra-chave-inline-batch/22-03-SUMMARY.md` when done.
</output>
