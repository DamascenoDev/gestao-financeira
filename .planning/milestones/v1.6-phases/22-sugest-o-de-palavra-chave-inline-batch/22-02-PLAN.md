---
phase: 22-sugest-o-de-palavra-chave-inline-batch
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/schemas/category-keyword.ts
  - src/actions/category-keywords.ts
  - src/actions/category-keywords.test.ts
autonomous: true
requirements: [KW-08]
tags: [keyword, suggestions, server-action]

must_haves:
  truths:
    - "getKeywordSuggestions() reads the caller's confirmed merchant_patterns (RLS-scoped), excludes any descriptor already covered by an existing keyword (via matchKeyword over compileRule'd category_keywords), sorts the rest by hit_count desc, and returns { ok: true, suggestions: KeywordSuggestion[] } | { error }."
    - "approveKeywordSuggestions(items) creates the supplied candidates as category_keywords behind ONE owner-gate and ONE revalidatePath('/categorias'), per-item validating/normalizing/deduping exactly like addKeyword, and returns { ok: true, created, skipped } | { error }."
    - "A single invalid or duplicate item never aborts the batch — it is counted as skipped and the loop continues."
  artifacts:
    - path: "src/actions/category-keywords.ts"
      provides: "getKeywordSuggestions + approveKeywordSuggestions + KeywordSuggestion type + ApproveSuggestionsResult type"
      exports: ["getKeywordSuggestions", "approveKeywordSuggestions"]
      contains: "matchKeyword"
    - path: "src/lib/schemas/category-keyword.ts"
      provides: "Zod schema for a batch-approve item (categoryId uuid + keyword) reused server-side"
      contains: "keywordSuggestionItemSchema"
    - path: "src/actions/category-keywords.test.ts"
      provides: "Extended action tests for both new actions (filter/sort/shape; owner-gate; dedupe→skipped; single revalidate; batch-not-aborted)"
  prohibitions:
    - "No new table, no migration, no gen:types, no new package — this plan touches only TS source over the existing schema."
    - "No manual .eq('user_id', ...) filter on reads — RLS scopes them (repo convention); inserts pass user_id from getClaims only."
    - "approveKeywordSuggestions calls revalidatePath('/categorias') exactly ONCE (after the loop), never per item."
    - "No re-normalization of descriptor_norm (it is already the normalized key); only the client-supplied edited keyword is re-validated + normalized."
  key_links:
    - from: "src/actions/category-keywords.ts (getKeywordSuggestions)"
      to: "src/lib/classifier/keywords.ts (matchKeyword, compileRule)"
      via: "build KeywordRule[] from the user's category_keywords, exclude patterns where matchKeyword(descriptor_norm, rules) !== null"
      pattern: "matchKeyword\\("
    - from: "src/actions/category-keywords.ts (approveKeywordSuggestions)"
      to: "category_keywords table"
      via: "per-item insert behind one getClaims owner-gate + one revalidatePath"
      pattern: "from\\('category_keywords'\\)"
---

<objective>
Deliver the KW-08 server side: two new server actions in `src/actions/category-keywords.ts` — `getKeywordSuggestions()` (mine confirmed `merchant_patterns`, exclude already-covered descriptors, sort by `hit_count` desc) and `approveKeywordSuggestions(items)` (bulk-create the chosen candidates as `category_keywords` behind one owner-gate + one revalidate) — plus a small batch-item Zod schema and extended action tests.

Purpose: Provide the data + write contract the batch dialog (Plan 03) composes, keeping ALL candidate computation and the "already-covered" filter server-side so raw merchant data never crosses to the client.
Output: Modified `category-keywords.ts` (+ `category-keyword.ts` schema), extended `category-keywords.test.ts`. ZERO migration / gen:types / new packages.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-sugest-o-de-palavra-chave-inline-batch/22-CONTEXT.md
@.planning/phases/22-sugest-o-de-palavra-chave-inline-batch/22-RESEARCH.md
@.planning/phases/22-sugest-o-de-palavra-chave-inline-batch/22-PATTERNS.md

@src/actions/category-keywords.ts
@src/lib/classifier/keywords.ts
@src/lib/schemas/category-keyword.ts
@src/actions/category-keywords.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the batch-item schema + getKeywordSuggestions + approveKeywordSuggestions to category-keywords.ts</name>
  <read_first>
    - src/actions/category-keywords.ts — the full file (1-117): the header conventions, `AddKeywordResult` union (line 31), `CATEGORIAS_PATH = '/categorias'` (33), `idSchema = z.string().uuid(...)` (40), and `addKeyword`'s exact per-item logic to mirror — `idSchema.safeParse(categoryId)` guard (47), `keywordSchema.safeParse` (54), `normalizeKeyword(parsed.data)` (58), reject `normalized === ''` (59), reject `normalized.replace(/\*/g, '') === ''` (63), owner-gate `getClaims().claims.sub` (67-70), `maybeSingle` dup pre-check (73-79), insert (81-85), 23505 backstop (88-91), `revalidatePath(CATEGORIAS_PATH)` (93).
    - src/lib/classifier/keywords.ts — `compileRule(categoryId, keyword, sort): KeywordRule | null` (77-93, returns null for '' / literal-count-0); `matchKeyword(descriptorNorm, rules): KeywordMatch | null` (127-165); the `KeywordRule` interface (52-62). These are PURE/sync — import them, never re-implement.
    - src/types/database.types.ts — merchant_patterns Row (449-459): `{ category_id, created_at, descriptor_norm, hit_count, id, last_used_at, reserva_id, user_id }`. category_keywords + categories Row shapes for the joins (select only the columns you use).
    - src/lib/schemas/category-keyword.ts — the existing `keywordSchema` (13-17, bare trimmed string min1/max60). Add the batch-item schema beside it.
    - .planning/phases/22-sugest-o-de-palavra-chave-inline-batch/22-RESEARCH.md §Architecture Patterns 2 & 3 — the EXACT shapes of both functions (lines ~205-285); §Security Domain (V4/V5 controls).
  </read_first>
  <files>src/lib/schemas/category-keyword.ts, src/actions/category-keywords.ts</files>
  <action>
    Step A — `src/lib/schemas/category-keyword.ts`: add (do NOT remove the existing `keywordSchema`) a Zod object schema for one batch-approve item, e.g. `export const keywordSuggestionItemSchema = z.object({ categoryId: z.string().uuid('Identificador inválido'), keyword: keywordSchema })` and `export type KeywordSuggestionItem = z.infer<typeof keywordSuggestionItemSchema>`. Keep the existing pt-BR messages. This is the new Zod schema allowed by the planning constraints (analog: the existing schema file).

    Step B — `src/actions/category-keywords.ts`: add two new exported async server actions in the SAME file (it is already `'use server'`), mirroring `addKeyword`'s conventions. Add imports at the top: `import { compileRule, matchKeyword, type KeywordRule } from '@/lib/classifier/keywords'` and (optionally) the new item schema/type from the schema file.

    1. Result + candidate types (export both, mirroring the discriminated-union convention at line 31):
       - `export type KeywordSuggestion = { descriptorNorm: string; categoryId: string; categoryName: string; hitCount: number }`
       - `export type ApproveSuggestionsResult = { ok: true; created: number; skipped: number } | { error: string }`

    2. `getKeywordSuggestions(): Promise<{ ok: true; suggestions: KeywordSuggestion[] } | { error: string }>` — follow RESEARCH Pattern 3 verbatim:
       - `createClient()`; owner-gate `const { data: claims } = await supabase.auth.getClaims(); if (!claims?.claims.sub) return { error: 'Sessão expirada.' }`.
       - Three RLS-scoped reads (NO manual `user_id` filter): `merchant_patterns` selecting `descriptor_norm, category_id, hit_count`; `category_keywords` selecting `category_id, keyword`; `categories` selecting `id, name, sort` with `.eq('is_archived', false)`. (Use `Promise.all` or sequential — either is fine; keep it readable.)
       - Build `sortById` + `nameById` Maps from categories. Build `rules: KeywordRule[]` by mapping each keyword through `compileRule(k.category_id, k.keyword, sortById.get(k.category_id) ?? 0)` and filtering out `null`.
       - `suggestions = (patterns ?? []).filter((p) => matchKeyword(p.descriptor_norm, rules) === null).map((p) => ({ descriptorNorm: p.descriptor_norm, categoryId: p.category_id, categoryName: nameById.get(p.category_id) ?? p.category_id, hitCount: p.hit_count })).sort((a, b) => b.hitCount - a.hitCount)`.
       - Return `{ ok: true, suggestions }`. Do NOT call `revalidatePath` (this is a read).
       - Do NOT re-normalize `descriptor_norm` — it is already the normalized match key (re-normalizing re-strips `*`, the documented landmine).

    3. `approveKeywordSuggestions(items: { categoryId: string; keyword: string }[]): Promise<ApproveSuggestionsResult>` — follow RESEARCH Pattern 2 verbatim:
       - `if (items.length === 0) return { ok: true, created: 0, skipped: 0 }`.
       - `createClient()`; ONE owner-gate `const userId = claims?.claims.sub; if (!userId) return { error: 'Sessão expirada.' }`.
       - `let created = 0, skipped = 0`. Loop each item, mirroring `addKeyword`'s four guards — `idSchema.safeParse(item.categoryId)` (skip+continue on fail), `keywordSchema.safeParse(item.keyword)` (skip+continue), `normalizeKeyword(parsed.data)`, reject `normalized === ''` (skip), reject `normalized.replace(/\*/g, '') === ''` (skip). Then the `maybeSingle` dup pre-check (`category_id` + `keyword` eq); if existing → `skipped++; continue`. Then `.insert({ user_id: userId, category_id: item.categoryId, keyword: normalized })`; on any insert error (including `error.code === '23505'`) → `skipped++; continue` (never throw, never leak). Otherwise `created++`.
       - After the loop: `revalidatePath(CATEGORIAS_PATH)` ONCE. Return `{ ok: true, created, skipped }`.
       - A single bad/duplicate item must NEVER abort the batch (use `continue`, never `return`/`throw` inside the loop).

    Keep both new actions consistent with the existing file (no new normalization, no new RLS plumbing — reuse `idSchema`, `keywordSchema`, `normalizeKeyword`, `CATEGORIAS_PATH`).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "category-keyword" || echo "no type errors in category-keywords"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export async function getKeywordSuggestions" src/actions/category-keywords.ts` === 1 and `grep -c "export async function approveKeywordSuggestions" src/actions/category-keywords.ts` === 1.
    - `grep -c "matchKeyword" src/actions/category-keywords.ts` ≥ 1 and `grep -c "compileRule" src/actions/category-keywords.ts` ≥ 1 (the already-covered filter is server-side).
    - `grep -c "revalidatePath" src/actions/category-keywords.ts` count increases by exactly 1 over baseline (one new call, inside approveKeywordSuggestions only — getKeywordSuggestions adds none).
    - `grep -c "keywordSuggestionItemSchema" src/lib/schemas/category-keyword.ts` ≥ 1.
    - No manual `.eq('user_id'` appears on the new reads (`grep -n "eq('user_id'" src/actions/category-keywords.ts` returns nothing for the new functions — RLS scopes reads).
    - `npx tsc --noEmit` reports no new errors.
  </acceptance_criteria>
  <done>`category-keywords.ts` exports `getKeywordSuggestions` (RLS-scoped mine + matchKeyword exclusion + hit_count-desc sort + `{descriptorNorm,categoryId,categoryName,hitCount}` shape) and `approveKeywordSuggestions` (one owner-gate, per-item addKeyword-mirrored validation/dedupe, one revalidate, `{created,skipped}` counts, batch never aborted), plus the batch-item schema — with no migration/gen:types/new package.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend category-keywords.test.ts — both new actions</name>
  <read_first>
    - src/actions/category-keywords.test.ts — the FULL harness (1-103): `revalidatePath` mock (17-20); `calls[]` recorder (24-29); settable `insertResult`/`dupPreCheckResult`/`claimsSub` (31-35); `makeBuilder(from)` (37-70) — note `maybeSingle` returns the SHARED `dupPreCheckResult` and `then`/`single` return `resolveResult`; `supabaseMock.from` (72-79); `beforeEach` reset (95-103); REAL `normalizeKeyword`/`normalizeDescriptor` import (89); `CAT_ID` uuid (92). Study how `addKeyword`'s tests assert payload + revalidate + dup + 23505 + session gate (107-246).
    - src/lib/classifier/keywords.test.ts — the pure matchKeyword/compileRule fixtures (substring, longest-wins, '' guard, sort tie-break) to reuse the rule shapes when asserting the already-covered exclusion.
    - .planning/phases/22-sugest-o-de-palavra-chave-inline-batch/22-RESEARCH.md §Validation Architecture — Test Analogs + Phase Requirements→Test Map for KW-08 (filter/sort/shape; owner-gate; dedupe→skipped; single revalidate; batch-not-aborted).
  </read_first>
  <files>src/actions/category-keywords.test.ts</files>
  <action>
    Extend `src/actions/category-keywords.test.ts` (keep the existing `addKeyword`/`removeKeyword` suites intact). Import the two new actions: `import { addKeyword, removeKeyword, getKeywordSuggestions, approveKeywordSuggestions } from './category-keywords'`. Use the REAL `normalizeKeyword`/`matchKeyword` (already real in the file) — do NOT mock them.

    Note on the harness: `getKeywordSuggestions` issues THREE table reads (merchant_patterns, category_keywords, categories). The current `makeBuilder` resolves every `select(...).then(...)` to a single shared `resolveResult` (default `{ data: null, error: null }`). Extend the harness minimally to let a test supply per-table read data — add a settable map (e.g. `let readResults: Record<string, QueryResult>` keyed by table name) and have `makeBuilder` resolve its `then`/terminal to `readResults[from] ?? resolveResult` for select ops. Reset it in `beforeEach`. Keep this change additive and backward-compatible so the existing `addKeyword` tests still pass (default to the prior behavior when a table has no entry). Document the harness delta in a comment, mirroring the file's existing "Delta vs the categories.test.ts harness" note.

    Add `describe('getKeywordSuggestions', ...)`:
    1. "excludes descriptors already covered by an existing keyword (matchKeyword) and returns the candidate shape": seed `merchant_patterns` with two patterns (e.g. `{ descriptor_norm: 'uber trip sp', category_id: CAT_ID, hit_count: 3 }` and `{ descriptor_norm: 'padaria centro', category_id: CAT_ID, hit_count: 1 }`), `category_keywords` with `{ category_id: CAT_ID, keyword: 'uber' }`, and `categories` with `{ id: CAT_ID, name: 'Transporte', sort: 0 }`. Assert the result excludes `'uber trip sp'` (covered by `uber`) and includes `'padaria centro'` with shape `{ descriptorNorm, categoryId, categoryName: 'Transporte', hitCount: 1 }`.
    2. "sorts remaining candidates by hit_count desc": seed two uncovered patterns with hit_counts 2 and 5; assert the returned order is hit_count 5 then 2.
    3. "returns the session error when there is no authenticated user": set `claimsSub = null`; assert `{ error: 'Sessão expirada.' }` and that NO insert/revalidate happens.

    Add `describe('approveKeywordSuggestions', ...)`:
    4. "creates the selected candidates with the owner and revalidates ONCE": approve two valid items; assert `created === 2`, two inserts to `category_keywords` carrying `user_id: 'user-1'` + normalized keyword, and `revalidatePath` called exactly once with `'/categorias'`.
    5. "a duplicate item is counted as skipped and never aborts the batch": make the dup pre-check return an existing row for the first item only (or use the 23505 insertResult variant for one item) while the second is novel; assert `created === 1`, `skipped === 1`, and the second still inserted.
    6. "an invalid item (bad uuid / empty / literal-count-0 term) is skipped, the rest proceed": include `{ categoryId: 'not-a-uuid', keyword: 'x' }` and `{ categoryId: CAT_ID, keyword: '*' }` alongside one valid item; assert `skipped >= 2`, `created === 1`, and the invalid items produced NO insert.
    7. "empty items array → { ok: true, created: 0, skipped: 0 } with no DB call and no revalidate": assert no inserts and `revalidatePath` not called.
    8. "returns the session error with no authenticated user and writes nothing": `claimsSub = null`; assert `{ error: 'Sessão expirada.' }`, no inserts.

    For the per-item dup/insert control in tests 5/6, follow the existing pattern of toggling `dupPreCheckResult` / `insertResult` (and, if you need per-item divergence, drive it via the recorded `calls` order or a small per-call counter in the mock — keep it simple and mirror the file's style).
  </action>
  <verify>
    <automated>npx vitest run src/actions/category-keywords.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/actions/category-keywords.test.ts` passes, including the new `getKeywordSuggestions` (≥ 3 cases) and `approveKeywordSuggestions` (≥ 5 cases) blocks.
    - The suite asserts the already-covered exclusion via a real keyword + matchKeyword (a descriptor matched by an existing keyword is NOT a candidate).
    - The suite asserts `revalidatePath('/categorias')` is called exactly once per `approveKeywordSuggestions` invocation (and zero times for `getKeywordSuggestions`).
    - The suite asserts a duplicate/invalid item is `skipped` while valid siblings are still `created` (batch never aborted).
    - The existing `addKeyword`/`removeKeyword` suites remain green (no regression).
  </acceptance_criteria>
  <done>`category-keywords.test.ts` proves the candidate filter/sort/shape, the one-owner-gate/one-revalidate batch contract, dedupe→skipped, invalid→skipped, the empty-array and session-gate paths, and that a single bad item never aborts the batch — using the existing mock harness extended for per-table reads.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (batch dialog, Plan 03) → server actions | The selected candidates' edited terms + category_ids cross from untrusted client state into `approveKeywordSuggestions`. |
| caller's session → RLS-scoped reads | `getKeywordSuggestions` reads merchant_patterns/category_keywords/categories — must stay scoped to the caller. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03 | Information Disclosure | getKeywordSuggestions reads | mitigate | RLS `auth.uid() = user_id` scopes all three reads; NO manual user_id filter and NO raw merchant data is shipped to the client (only the computed candidate shape). Owner-gate via getClaims before any read. |
| T-22-04 | Tampering | approveKeywordSuggestions term | mitigate | Server re-runs `keywordSchema` (max60) + `normalizeKeyword` + literal-count-0 reject on EVERY item — never trusts the client's edited term (V5). |
| T-22-05 | Elevation | approveKeywordSuggestions foreign categoryId | mitigate | `idSchema` (uuid) per item + RLS `with check (auth.uid() = user_id)` + FK to the caller's own categories: a foreign/garbage category_id inserts nothing (counted as skipped). user_id comes ONLY from getClaims (V4). |
| T-22-06 | Tampering | catch-all keyword (`*`/`**`) | mitigate | Per-item literal-count-0 reject mirrors addKeyword; `compileRule`/`matchKeyword` also skip a zero-literal rule (defense-in-depth). |
| T-22-SC | Tampering | npm/pip/cargo installs | mitigate | RESEARCH §Package Legitimacy Audit: ZERO packages installed this plan (no `npm install`, no migration, no gen:types). No `[ASSUMED]`/`[SUS]` package → no install checkpoint. |
</threat_model>

<verification>
- `npx vitest run src/actions/category-keywords.test.ts` — green (existing + new blocks).
- `npx tsc --noEmit` — no new type errors.
- `git diff --stat` confirms NO change under `supabase/migrations/` and NO change to `src/types/database.types.ts` (no schema, no gen:types).
</verification>

<success_criteria>
- KW-08 server contract satisfied: `getKeywordSuggestions` mines confirmed `merchant_patterns`, excludes already-covered descriptors via the existing `matchKeyword`, and returns candidates sorted by `hit_count` desc; `approveKeywordSuggestions` bulk-creates the chosen candidates with one owner-gate + one revalidate, returning `{created, skipped}` and never aborting on a bad item.
- All access control (V4) and input validation (V5) reuse `addKeyword`'s exact server-side logic.
- ZERO migration / gen:types / new package; the schema-push gate stays OFF.
</success_criteria>

<output>
Create `.planning/phases/22-sugest-o-de-palavra-chave-inline-batch/22-02-SUMMARY.md` when done.
</output>
