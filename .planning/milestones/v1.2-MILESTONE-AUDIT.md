---
milestone: v1.2
milestone_name: Carro
audited: 2026-06-18T00:21:53Z
status: tech_debt
scores:
  requirements: 6/6
  phases: 4/4
  integration: 4/4
  flows: 1/1
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 10-abastecimento-h-brido-consumo
    items:
      - "WR-02: v_abastecimento_consumo understates km/l and overstates R$/km when two tanque_cheio fills share the EXACT same odometro_km — the later fill's liters/cost are swept into the prior interval's litros_intervalo/custo_intervalo_cents subquery (0028_carros_fix.sql:90-103). The km_rodados>0 guard (0028:107) drops the zero-length interval but not the sibling sweep-in. Isolated to consumption averages of one car; does NOT touch gasto_total_cents (sums transactions, not the view) — no money/accounting corruption. Near-impossible single-user data shape. Fix = future migration 0029 anchoring each interval subquery lower bound on the prior full-tank fill's id, not odometro_km. Originally mis-noted in 10-VERIFICATION as 'addressed_in Phase 11 via 0029'; Phase 11 knowingly downgraded it to a documented known limitation (11-03-PLAN:165, 11-VERIFICATION:107) and shipped no 0029."
  - phase: cross-phase-hygiene
    items:
      - "SUMMARY frontmatter requirements_completed left empty in 09-01/02/03 and 10-01/02/03 — CAR-02, CAR-03, CAR-04 are only carried by the REQUIREMENTS.md checkbox + phase VERIFICATION (9: 4/4 passed, 10: 18/18 passed), not by per-plan SUMMARY frontmatter. 3-source cross-ref still resolves them as satisfied (verification is authoritative), but the frontmatter audit trail is thinner than CAR-01/05/06."
      - "REQUIREMENTS.md traceability detail rows for CAR-02 (line 217) and CAR-03 (line 218) still read 'In progress' from mid-phase (09-01 / 10-02) although the checkboxes are [x] and the phases passed verification. Stale prose, not a coverage gap."
---

# Milestone v1.2 "Carro" — Audit

**Status:** ⚡ tech_debt (all requirements met, no blockers, 1 deferred view-math edge + frontmatter hygiene)
**Audited:** 2026-06-18

## Definition of Done (ROADMAP v1.2)

Módulo de veículo autocontido, espelhando o MEI. Fatias: substrato/schema + CRUD de carro + nav → seam de etiquetagem → log de abastecimento + consumo → detalhe do carro com gráfico. Invariantes honrados: dinheiro em centavos inteiros, RLS `auth.uid()=user_id`, etiqueta `carro_id` não-destrutiva (D4), custo de abastecimento XOR (D2, CHECK no banco).

## Requirements Coverage (3-source cross-reference)

| REQ | Phase | VERIFICATION | SUMMARY frontmatter | REQUIREMENTS `[x]` | Final |
|-----|-------|--------------|---------------------|--------------------|-------|
| CAR-01 | 8 | passed 5/5 | 08-01, 08-03 listed | `[x]` | **satisfied** |
| CAR-02 | 9 | passed 4/4 | empty | `[x]` | **satisfied** (verification authoritative; frontmatter thin) |
| CAR-03 | 10 | passed 18/18 | empty | `[x]` | **satisfied** (verification authoritative; frontmatter thin) |
| CAR-04 | 10 | passed 18/18 | empty | `[x]` | **satisfied** (verification authoritative; frontmatter thin) |
| CAR-05 | 11 | passed 5/5 | 11-01/02/03 listed | `[x]` | **satisfied** |
| CAR-06 | 8 | passed 5/5 | 08-01, 08-03 listed | `[x]` | **satisfied** |

**6/6 satisfied. 0 unsatisfied. 0 orphaned.** No FAIL-gate trigger.

## Phase Verifications

| Phase | Status | Score | Notes |
|-------|--------|-------|-------|
| 8 — Substrato Carro + CRUD + nav | ✓ passed | 5/5 | — |
| 9 — Etiquetar gastos ao carro | ✓ passed | 4/4 | D4 non-destructive proven (tag+untag byte-identical category/value/goal-agg) |
| 10 — Abastecimento híbrido + consumo | ✓ passed | 18/18 | 1 deferred: WR-02 (see tech debt) |
| 11 — Detalhe do carro + gráfico | ✓ passed | 5/5 | capstone human-verify (11-04) signed off light+dark+mobile |

## Cross-Phase Integration (gsd-integration-checker)

**Verdict: SHIP-READY.** All 4 active seams OK. `tsc --noEmit` clean (no view↔consumer type drift).

1. **P8 views → P11 KPIs/chart + P10 consumo** — OK. `gasto_total_cents`, `km_por_litro_medio`, `reais_por_km_medio`, `km_por_litro`, `custo_intervalo_cents`, `litros_intervalo` match across SQL (0027/0028), `database.types.ts:681-766`, and consumers.
2. **P9 carro_id tagging → P11 gasto aggregation** — OK. `v_carro_resumo.gasto_total_cents = Σ transactions.amount_cents WHERE carro_id IS NOT NULL` (0027:213-226); detail page + inline gasto-por-categoria read the same tagged rows.
3. **P10 linked-txn fuel cost → P11 total spend, no double-count** — OK (resolved). From-fatura fueling sets `transactions.carro_id` (`abastecimentos.ts:128-131`); fuel cost enters `gasto_total_cents` exactly once via the tagged transaction. Manual fueling lives only in `abastecimentos.amount_cents`, surfaced via R$/km, never re-added to gasto-total. **No double-count.**
4. **P8 nav → routes under auth** — OK. `(app)` layout `getClaims()` → redirect guards `/carros` + `/carros/[id]`; card links to detail.

## Headline E2E Flow

create car → tag maintenance expense (carro_id-only payload, D4) → log fueling (manual XOR + linked-txn) → open `/carros/[id]`: total spend (`v_carro_resumo`), fueling history (`abastecimentos`), km/l chart (`v_abastecimento_consumo`). **COMPLETE — no breaks, no double-count, no broken wiring.**

## Tech Debt

**1. WR-02 — same-odometer km/l averaging edge (Phase 10 view).** Two `tanque_cheio` fills at the EXACT same `odometro_km` → later fill's liters/cost swept into the prior interval; that car's displayed km/l understated / R$/km overstated. Isolated to consumption averages; `gasto_total_cents` unaffected. Near-impossible single-user data shape. Fix = migration 0029 (anchor subquery lower bound on prior full-tank fill's id). Tracked, deliberately deferred (not a blocker).

**2. Frontmatter / traceability hygiene.** CAR-02/03/04 absent from per-plan SUMMARY `requirements_completed` frontmatter (carried by checkbox + phase VERIFICATION instead); REQUIREMENTS.md detail rows 217/218 read stale "In progress". Doc hygiene, not a coverage gap.

## Conclusion

All 6 v1.2 requirements satisfied and wired end-to-end; all phase verifications passed; cross-phase integration ship-ready; no blockers. One deliberately-deferred near-impossible view-math edge (WR-02) + minor doc hygiene carried forward as tech debt. Milestone is completable.
