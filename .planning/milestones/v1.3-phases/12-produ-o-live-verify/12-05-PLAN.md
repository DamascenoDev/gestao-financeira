---
phase: 12-produ-o-live-verify
plan: 05
type: execute
wave: 5
depends_on: ["12-04"]
files_modified: []
autonomous: false
requirements: [DEPLOY-04, DEPLOY-05, IMP-01, IMP-02, IMP-05, CLS-03, CLS-04]
user_setup:
  - service: app (production)
    why: "Core-value live-verify against the PRODUCTION *.vercel.app URL from 12-02 (D-08 — no re-deploy): upload a REAL OFX/CSV → file persists to the private Storage bucket by user_id → server-side parse on the Vercel Node runtime → review → memory-classify (known merchant auto / new merchant manual pick) → confirm (learns the pattern) → re-upload auto-classifies. Browser-interactive."
    dashboard_config:
      - task: "Open the *.vercel.app production URL from 12-02-SUMMARY.md, signed in with the personal account; have a real Nubank OFX (from fixtures/faturas-pdf/nubank/, gitignored) reachable by the browser file picker (D specifics — OFX is the deterministic production verify path)"
        location: "browser (production)"
      - task: "After the upload, confirm in Supabase Dashboard → Storage → statements that the uploaded object is stored under the {user_id}/ prefix and the bucket remains PRIVATE (DEPLOY-04 / Storage RLS, D-12)"
        location: "Supabase Dashboard → Storage"

must_haves:
  truths:
    - "DEPLOY-04: a real OFX/CSV uploaded on the production /importar persists to the PRIVATE statements bucket under the {user_id}/ prefix (Storage RLS), and the parsing runs server-side on the Vercel Node runtime from the downloaded buffer (the review grid renders parsed rows = proof the server-side parse ran in production)"
    - "DEPLOY-05 (memory half — the core value): a NEW merchant is a MANUAL category pick (NOT an AI suggestion — the AI classifier was never built, returns null, deferred to v1.4 per D-01/02/03); after Confirmar learns the pattern (CLS-04), a re-upload of the SAME merchant auto-classifies with origem 'Memória' — proven live in production"
    - "IMP-01/IMP-02 confirmed live: OFX drag→progress→review lands with parsed memory-classified rows; an ambiguous CSV opens the column-mapping dialog and a saved profile is reused silently on a second same-layout upload"
    - "IMP-05/CLS-03 confirmed live: inline + bulk manual classify of memory-miss rows then Confirmar persists the rows to the remote DB and they appear in the Extrato in the right month; a byte-identical re-upload shows '0 novas' and does NOT duplicate"
  artifacts: []
  key_links: []
---

<objective>
Live-verify the CORE VALUE in PRODUCTION — upload a real fatura → private Storage by user_id + server-side parse on Vercel Node runtime (DEPLOY-04) → review → memory-only classification with manual pick for new merchants → confirm learns the pattern → re-upload auto-classifies, with goal adherence (proven in Plan 12-04) reflecting the new spend (DEPLOY-05) — by SEQUENCING AND EXECUTING the already-written walkthrough `.planning/phases/04-upload-classifica-o-inteligente/04-04-PLAN.md` against the single *.vercel.app bundle Plan 12-02 deployed (D-07 step 4, fourth in the sequence). The original 04-04 ran on the LOCAL stack against synthetic fixtures; this plan runs the same checkpoint against the PRODUCTION URL + remote Supabase + remote private Storage, using a REAL Nubank OFX (the deterministic production path), with NO re-deploy (D-08).

CRITICAL (D-01/D-02/D-03 — DEPLOY-05 REINTERPRETED): the core value is **memory-only classification + manual pick**, NOT AI. `src/lib/classifier/suggest.ts` is a deferred seam that always returns null; no AI SDK is installed; CLS-02/AI is deferred to v1.4. A NEW merchant is classified by a MANUAL category pick that BECOMES a learned pattern — there is NO AI suggestion to verify, and verifying one would be a FALSE gap. Do NOT author any AI/LLM-classification verification step.

This plan carries DEPLOY-04 (upload→Storage→server-side parse) and the upload→classify→learn half of DEPLOY-05 (the goal-adherence half is Plan 12-04). Every step is browser-interactive against production → autonomous: false.

Purpose: prove the highest-risk, highest-value slice end-to-end live in production — DEPLOY-04 + DEPLOY-05 (memory-only) + IMP-01/02/05 + CLS-03/04.
Output: a production sign-off (or punch list) for the upload → (CSV mapping) → review → manual-classify → confirm → re-upload-auto-classify loop; verification evidence in 12-05-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-produ-o-live-verify/12-CONTEXT.md
@.planning/phases/04-upload-classifica-o-inteligente/04-04-PLAN.md
@.planning/phases/12-produ-o-live-verify/12-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Core-value live-verify in production — upload → private Storage + server parse → memory-classify → learn → auto-classify (executes 04-04 against the 12-02 bundle; DEPLOY-04 + DEPLOY-05)</name>
  <files>none (verification only — produces sign-off, no symbols)</files>
  <read_first>
    - .planning/phases/04-upload-classifica-o-inteligente/04-04-PLAN.md (the authoritative walkthrough — its Task 2 checkpoint holds the exact upload/mapping/classify/confirm/re-upload steps; this plan RUNS those steps, do not re-author them; deltas: target is the PRODUCTION *.vercel.app URL + remote Supabase + remote private Storage, the fixture is a REAL Nubank OFX not the synthetic itau-sample, and the NEW-merchant case is a MANUAL pick — there is NO AI suggestion to verify per D-01/02/03)
    - .planning/phases/12-produ-o-live-verify/12-02-SUMMARY.md (the recorded production *.vercel.app URL — the single bundle to verify against; NO re-deploy per D-08; 12-02 already confirmed the statements bucket is PRIVATE remotely)
    - .planning/phases/12-produ-o-live-verify/12-CONTEXT.md §decisions D-01/D-02/D-03 (the AI classifier was NEVER built — suggest.ts returns null; new merchant = manual pick that becomes a learned pattern; DO NOT verify AI classification) and §specifics (the Nubank OFX in fixtures/faturas-pdf/nubank/ is the deterministic production verify path)
    - src/lib/classifier/suggest.ts + src/lib/classifier/memory.ts (confirm the production code path: memory.ts does the point-read auto-classify; suggest.ts is the null seam — the new-merchant path is the manual pick, never an AI call)
  </read_first>
  <what-built>The complete upload → (CSV mapping) → review → manual-classify → confirm → re-upload-auto-classify flow, now served by the production *.vercel.app bundle from 12-02 against the remote Supabase + the remote PRIVATE statements bucket. The upload handler persists the file to Storage under {user_id}/ and parses server-side on the Vercel Node runtime from the downloaded buffer (in-house parsers src/lib/parsers/{csv,ofx}.ts; no external OFX dep). Classification is MEMORY-ONLY: memory.ts auto-classifies known merchants; a new merchant is a MANUAL category pick (the suggest.ts AI seam returns null — CLS-02/AI deferred to v1.4). All parse/dedup/normalize/memory/learn/IDOR logic was automated GREEN in the source phase; this is the production interaction + Storage/server-runtime confirmation.</what-built>
  <how-to-verify>
Open the production *.vercel.app URL from 12-02-SUMMARY.md (the SAME deploy — do NOT run `vercel --prod` again, D-08), signed in as the personal account, with a real Nubank OFX reachable by the file picker. Walk 04-04's scenarios AGAINST PRODUCTION:

1. **OFX upload → private Storage + server-side parse → review (DEPLOY-04 / IMP-01):** Go to /importar. Drag (or pick) the real Nubank OFX. Confirm the dropzone shows "Enviando…" then "Processando…", then you land on the review screen with parsed rows (Data dd/MM, Descritor cru + the normalizado helper, Valor in R$, neutral never red). The review grid rendering is the PROOF the server-side parse ran on the Vercel Node runtime from the Storage buffer. Memory-miss rows show the amber left-border + "Não classificada" badge; the summary header shows the counts.
   - Then in Supabase Dashboard → Storage → statements, confirm the uploaded object sits under the {user_id}/ prefix and the bucket is PRIVATE (DEPLOY-04 / Storage RLS, D-12).

2. **CSV mapping (IMP-02):** Upload an ambiguous-columns CSV. Confirm the "Mapear colunas do CSV" dialog opens. Map Data / Descritor / Valor to distinct columns (preview updates live, dd/MM + R$). Mapping two fields to the same column shows the "Cada campo… precisa de uma coluna diferente." error blocking "Salvar mapeamento". Fix, keep "Salvar como perfil" on, save → reach review. Re-upload a same-layout CSV and confirm the mapping is reused silently (toast "Mapeamento reaproveitado.").

3. **Manual-classify + confirm + learn (DEPLOY-05 memory half / IMP-05 / CLS-03):** On the review screen, classify an amber memory-miss (NEW merchant) row by MANUALLY picking a category — there is NO AI suggestion (the classifier seam returns null; that is correct, not a bug). Select ≥2 amber rows and bulk-"Classificar" via the SelectionActionBar; classify one row into the Reserva category and confirm the "Qual reserva?" picker appears. Hit "Confirmar importação"; if there are still-unclassified rows the "Importar sem classificar?" guard appears — confirm through it. Success toast "{M} transações importadas"; you land on the Extrato with the new rows in the right month (persisted to the remote DB).

4. **Learn → auto-classify (DEPLOY-05 memory half / CLS-04):** Re-upload an OFX whose merchants you just classified. Confirm those merchants now auto-classify with origem "Memória" (no amber, no manual pick) — the confirmation taught the memory. Confirm on /dashboard the new spend moved the adherence rows (the goal-adherence half verified in Plan 12-04 now reflects real uploaded spend — DEPLOY-05 end-to-end).

5. **Re-upload "0 novas" (IMP-04):** Upload the byte-identical OFX a second time. Confirm the "0 novas — este arquivo já foi importado." line + the "Nada novo neste arquivo" empty state, and that the Extrato did NOT duplicate any row.
  </how-to-verify>
  <resume-signal>Type "approved" once: the OFX upload lands the review grid (server-side parse ran in prod) and the file is under {user_id}/ in the PRIVATE bucket; the CSV mapping dialog + profile reuse work; a NEW merchant is a manual pick that, after Confirmar, makes a re-upload auto-classify "Memória"; the new spend reflects in goal adherence; the byte-identical re-upload shows "0 novas". Or describe any issue — issues become a `/gsd-plan-phase 12 --gaps` punch list. (Do NOT report "no AI suggestion" as an issue — AI is deferred to v1.4 by design, D-01/02/03.)</resume-signal>
  <action>HUMAN CHECKPOINT (autonomous:false) — execute the 04-04 walkthrough against the production *.vercel.app URL from 12-02 (NOT the local stack, with a REAL Nubank OFX, and NO re-deploy per D-08). The exact steps live in <how-to-verify>; the source-of-truth steps live in 04-04-PLAN.md Task 2. Pause for the user to sign in to production and walk upload→Storage→server-parse (DEPLOY-04) and memory-classify→learn→auto-classify (DEPLOY-05). The new-merchant case is a MANUAL pick — do NOT expect or verify an AI suggestion (D-01/02/03). Browser interaction, remote Storage, and the server-runtime parse cannot be automated.</action>
  <verify><human-check>On the production *.vercel.app URL: a real OFX upload lands the review grid (server-side parse on Vercel Node runtime) and the file is stored under {user_id}/ in the PRIVATE statements bucket (DEPLOY-04); a new merchant is manually classified, Confirmar persists rows + learns the pattern, and a re-upload auto-classifies "Memória" (DEPLOY-05 memory half / CLS-04); the new spend reflects in goal adherence; a byte-identical re-upload shows "0 novas" with no duplication. No AI suggestion appears and that is correct (D-01/02/03).</human-check></verify>
  <acceptance_criteria>
    - Logged-in personal user uploads a real Nubank OFX on the production /importar → the dropzone progresses Enviando→Processando → the review grid renders parsed rows (server-side parse on the Vercel Node runtime, proven by the grid populating in production).
    - In Supabase Dashboard → Storage → statements, the uploaded object appears under the {user_id}/ prefix and the bucket is PRIVATE (DEPLOY-04 / Storage RLS).
    - An ambiguous CSV opens the "Mapear colunas do CSV" dialog; mapping Data/Descritor/Valor to distinct columns + saving a profile reaches review, and a second same-layout CSV reuses the profile silently ("Mapeamento reaproveitado.").
    - A memory-miss (NEW merchant) row is classified by a MANUAL category pick — NO AI suggestion is offered (suggest.ts returns null by design, D-01/02/03) — and after "Confirmar importação" the rows appear in the Extrato in the correct month (persisted remotely).
    - Re-uploading an OFX whose merchants were just classified auto-classifies them with origem "Memória" (CLS-04), and the new spend is reflected in the dashboard goal adherence (DEPLOY-05 end-to-end against the 12-04 goal surface).
    - A byte-identical re-upload shows the "0 novas — este arquivo já foi importado." line and does NOT duplicate any Extrato row.
  </acceptance_criteria>
  <done>DEPLOY-04 (upload→private Storage by user_id + server-side Node-runtime parse) and DEPLOY-05 (memory-only classify + manual pick + learn→auto-classify, with goal adherence reflecting the spend) confirmed live in production against the 12-02 bundle; IMP-01/02/05 + CLS-03/04 re-proven in prod; NO AI verification authored (D-01/02/03); no re-deploy occurred (D-08). Sign-off (or punch list) recorded.</done>
</task>

</tasks>

<artifacts_produced>
This live-verify plan produces NO new source symbols. It produces VERIFICATION EVIDENCE only:
- `.planning/phases/12-produ-o-live-verify/12-05-SUMMARY.md` — the production sign-off (or punch list) for DEPLOY-04 + DEPLOY-05 (memory-only) + IMP-01/02/05 + CLS-03/04.
- The uploaded OFX/CSV files land in the user's own PRIVATE production Storage bucket under {user_id}/ (this is itself the DEPLOY-04 evidence); the imported transactions live in the user's production account scoped by RLS. The real Nubank OFX fixture (fixtures/faturas-pdf/nubank/) is pre-existing + gitignored — this plan does NOT create it. No test fixtures, no migrations, no code.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser file upload → production upload handler | untrusted file crosses into the server; it must land in the PRIVATE bucket under the uploader's {user_id}/ prefix, never world-readable |
| Storage object → server-side parser (Vercel Node runtime) | parsing runs server-side from the downloaded buffer (not on the client); the parsed rows go to the review grid, never auto-committed |
| production app → remote private Storage / remote Supabase | Storage RLS + table RLS must scope the file + the imported rows to the uploader's user_id on the remote project |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05-storage (I) | Information Disclosure | the statements bucket public, or files readable across users on the remote project | mitigate | 12-02 asserted the bucket is PRIVATE remotely; this walkthrough confirms the uploaded object sits under the {user_id}/ prefix in the PRIVATE bucket (DEPLOY-04 / Storage RLS, D-12) |
| T-12-05-import-rls (I) | Information Disclosure | imported transactions visible to another user on the remote DB | mitigate | the import persists under the signed-in user's user_id; the walkthrough confirms rows appear only in the uploader's Extrato (single-user v1 + remote RLS from 12-02) |
| T-12-05-runtime (D) | Denial of Service | a large statement parsed on the client / a too-short maxDuration timing out the parse | mitigate | parsing runs server-side on the Vercel Node runtime from the buffer (DEPLOY-04); 12-02 noted there is no route-handler maxDuration to set yet (PDF parsing route is Phase 13) — the OFX/CSV parse is fast; the review grid rendering proves it completed |
| T-12-05-SC (T) | Tampering | npm/pip/cargo installs | accept | no packages installed — verification-only plan against an already-deployed bundle; in-house parsers, no AI SDK (AI deferred to v1.4) |
</threat_model>

<verification>
- The walkthrough runs against the production *.vercel.app URL from 12-02 (the SAME bundle, no re-deploy — D-08).
- DEPLOY-04 human-verified live: real OFX/CSV upload persists to the PRIVATE statements bucket under {user_id}/; server-side parse on the Vercel Node runtime renders the review grid in production.
- DEPLOY-05 (memory half) human-verified live: a NEW merchant is a MANUAL pick (no AI — deferred per D-01/02/03); Confirmar learns the pattern; a re-upload auto-classifies "Memória" (CLS-04); the new spend reflects in the dashboard goal adherence (the monthly+annual half from Plan 12-04).
- IMP-01/02/05 + CLS-03/04 re-proven in production; the byte-identical re-upload shows "0 novas" with no duplication.
- NO AI/LLM-classification verification step authored (D-01/02/03 — verifying a nonexistent AI would be a false gap).
</verification>

<success_criteria>
- DEPLOY-04: upload writes to the private Storage bucket by user_id in production and parsing runs server-side on the Vercel Node runtime from the buffer — confirmed live.
- DEPLOY-05: the core value (memory-only classification + manual pick that becomes a learned pattern → auto-classify on re-upload) is proven live in production, with goal adherence (Plan 12-04) reflecting the uploaded spend.
- The new-merchant path is a manual pick, not an AI suggestion — verified as correct per the memory-only reinterpretation (D-01/02/03); no AI verification authored.
- No re-deploy occurred. Reviewer signs off "approved" (or a punch list is captured for gap closure).
</success_criteria>

<output>
Create `.planning/phases/12-produ-o-live-verify/12-05-SUMMARY.md` when done (record the production sign-off or the issue list, and note that the AI classifier remains deferred to v1.4 by design — DEPLOY-05 was verified as memory-only per D-01/02/03).
</output>
