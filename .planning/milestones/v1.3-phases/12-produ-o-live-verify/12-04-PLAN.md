---
phase: 12-produ-o-live-verify
plan: 04
type: execute
wave: 4
depends_on: ["12-03"]
files_modified: []
autonomous: false
requirements: [BUD-02, RSV-01, RSV-02, RSV-05]
user_setup:
  - service: app (production)
    why: "Live-verify of the metas/aderência dashboard (monthly + annual) and the reservas sub-flow against the PRODUCTION *.vercel.app URL from 12-02 (D-08 — no re-deploy). Browser-interactive against the personal account."
    dashboard_config:
      - task: "Open the *.vercel.app production URL from 12-02-SUMMARY.md, signed in with the personal account; the receitas from Plan 12-03 must already exist (net income is the goal denominator)"
        location: "browser (production)"

must_haves:
  truths:
    - "The production dashboard (12-02 bundle, remote Supabase) renders direction-aware adherence color live: a teto category over 100% is RED 'Estourou', ≥80%<100% is amber 'Aproximando', an alvo below target is muted 'Abaixo', an alvo ≥100% is GREEN 'Atingido'; a gasto is NEVER painted red just for being a gasto (BUD-02)"
    - "Goal adherence is confirmed in production for BOTH windows — the 'Mensal' tab AND the 'Anual (YTD)' tab show the same category order + color logic, and for a single populated month the monthly and annual % read consistently (this is DEPLOY-05's 'aderência às metas mensal E anual' half, verified live)"
    - "The 'Qual reserva?' sub-flow appears inside the transação dialog when the Reserva category is picked, and the aporte links to the chosen reserva's ledger; it raises the alocação line, never a consumo line (RSV-02)"
    - "A reserva progress bar appears ONLY when an alvo is set (no-alvo reserva shows no bar, not a 0% bar); a saída larger than the saldo is blocked, a valid saída drops the derived saldo (RSV-01/05)"
  artifacts: []
  key_links: []
---

<objective>
Live-verify the Phase-3 manual-only behaviors in PRODUCTION — direction-aware dashboard adherence color (BUD-02), the "qual reserva?" sub-flow (RSV-02), and the alvo-only progress bar + saída guard (RSV-01/05) — by SEQUENCING AND EXECUTING the already-written walkthrough `.planning/phases/03-metas-ader-ncia-e-reservas/03-06-PLAN.md` against the single *.vercel.app bundle Plan 12-02 deployed (D-07 step 4, third in the sequence). The original 03-06 was written to run against the LOCAL stack; this plan runs the same checkpoint against the PRODUCTION URL + remote Supabase, with NO re-deploy (D-08). This plan carries the **goal-adherence half of DEPLOY-05**: the success criterion "aderência às metas funcionando (mensal **e** anual)" is proven here against the receitas/net-income confirmed in Plan 12-03 (the goal denominator). The upload→classify→learn half of DEPLOY-05 is proven in Plan 12-05. Every step is browser-interactive against production → autonomous: false.

Purpose: prove BUD-02 / RSV-01 / RSV-02 / RSV-05 live in production AND the monthly+annual goal-adherence half of DEPLOY-05, building on the production receitas confirmed in 12-03.
Output: a confirmed production sign-off (or a punch list) for the metas/aderência dashboard and the reservas surface; verification evidence in 12-04-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-produ-o-live-verify/12-CONTEXT.md
@.planning/phases/03-metas-ader-ncia-e-reservas/03-06-PLAN.md
@.planning/phases/12-produ-o-live-verify/12-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live-verify metas/aderência (mensal + anual) and reservas in production (executes 03-06 against the 12-02 bundle)</name>
  <files>none (verification only — produces sign-off, no symbols)</files>
  <read_first>
    - .planning/phases/03-metas-ader-ncia-e-reservas/03-06-PLAN.md (the authoritative walkthrough — its checkpoint holds the exact BUD-02 / RSV-02 / RSV-01/05 steps; this plan RUNS those steps, do not re-author them; the ONLY delta is the target is the PRODUCTION *.vercel.app URL + remote Supabase, not the local stack)
    - .planning/phases/12-produ-o-live-verify/12-02-SUMMARY.md (the recorded production *.vercel.app URL — the single bundle to verify against; NO re-deploy per D-08)
    - .planning/phases/12-produ-o-live-verify/12-03-SUMMARY.md (confirms the production receitas/net-income exists — it is the goal % denominator; if net income is missing, add it via /receitas first)
    - .planning/phases/03-metas-ader-ncia-e-reservas/03-UI-SPEC.md (the dashboard color/semantics design contract)
  </read_first>
  <what-built>The Phase-3 surface (metas/aderência dashboard with Mensal + Anual tabs, direction-aware AdherenceRows, 80/100 alerts, combined alocação line; the MetaDialog; the Reservas screen with derived saldo + conditional progress bar + saída form + per-reserva ledger; the "qual reserva?" sub-flow inside the transação dialog), now served by the production *.vercel.app bundle from 12-02 against the remote Supabase. All core math (aporte grouping, derived balance, saída-never-negative, monthly↔YTD consistency) was automated GREEN in the source phase; this is the production interaction confirmation.</what-built>
  <how-to-verify>
Open the production *.vercel.app URL from 12-02-SUMMARY.md (the SAME deploy — do NOT run `vercel --prod` again, D-08), signed in as the personal account. Ensure the current month has a receita líquida from Plan 12-03 (the goal denominator). Then walk 03-06's three checks AGAINST PRODUCTION:

1. Dashboard direction-aware color + monthly/annual adherence (BUD-02 — DEPLOY-05 goal half):
   - On /dashboard click "Definir metas". Set a TETO meta on a consumo category (e.g. Alimentação 30%) and an ALVO meta on Investimentos (e.g. 20%).
   - Log transactions in /extrato so Alimentação exceeds its teto and Investimentos is below its alvo.
   - On /dashboard confirm: the over-teto row is RED with "Estourou" + octagon-alert; a row at ≥80% but <100% teto shows amber "Aproximando" + triangle-alert; the alvo row below target is muted "Abaixo"; an alvo at ≥100% is GREEN "Atingido" + check. Confirm a gasto is NEVER painted red just for being a gasto.
   - Switch to the "Anual (YTD)" tab; confirm the SAME category order + color logic and the "Acumulado de 2026 (jan–jun)" window label. For a single populated month the Mensal and Anual % MUST read consistently. (This is DEPLOY-05's "aderência às metas mensal E anual" proven live.)
   - Set a meta but pick a month with no receita; confirm the "Sem receita líquida em {período}…" copy appears, never NaN%.

2. "Qual reserva?" sub-flow (RSV-02):
   - On /reservas create at least one reserva.
   - In /extrato open "Novo lançamento", pick the "Reserva" category; confirm the "Qual reserva?" picker appears BELOW the categoria select (same dialog, progressive disclosure) with the helper "Este lançamento será registrado como aporte nesta reserva." Submit and confirm a linked entrada appears in that reserva's ledger (/reservas/[id]).
   - Inline-re-tag an existing transaction to "Reserva" in the Extrato; confirm a focused picker dialog opens and the aporte links.
   - Confirm on /dashboard the aporte raised the Investimentos/alocação line and did NOT raise any consumo line.

3. Reserva progress bar only with alvo (RSV-01/05):
   - Create one reserva WITH an alvo and one WITHOUT. Confirm the progress bar appears only on the one with an alvo (the no-alvo card shows no bar at all, not a 0% bar).
   - Register a saída larger than the saldo; confirm it is blocked with the "A saída não pode ser maior que o saldo da reserva ({R$})." FieldError. Then a valid saída; confirm the derived saldo drops (persisted to the remote DB).
  </how-to-verify>
  <resume-signal>Type "approved" once direction-aware dashboard color, the monthly AND annual adherence tabs, the qual-reserva sub-flow, and the alvo-only progress bar + saída guard all behave correctly on the PRODUCTION URL, or describe any issue — issues become a `/gsd-plan-phase 12 --gaps` punch list.</resume-signal>
  <action>HUMAN CHECKPOINT (autonomous:false) — execute the 03-06 walkthrough against the production *.vercel.app URL from 12-02 (NOT the local stack the original 03-06 targeted, and with NO re-deploy per D-08). The exact steps live in <how-to-verify>; the source-of-truth steps live in 03-06-PLAN.md. Pause for the user to sign in to production and walk BUD-02 / RSV-02 / RSV-01/05, including the Mensal AND Anual adherence tabs (DEPLOY-05 goal half). Browser interaction + remote-DB persistence cannot be automated.</action>
  <verify><human-check>On the production *.vercel.app URL: the dashboard paints direction-aware adherence color correctly (teto red-over, ≥80% amber, alvo below muted, alvo-at-target green; gasto never red just for being a gasto); the Mensal and Anual (YTD) tabs show consistent % for a single populated month; the qual-reserva sub-flow links an aporte and raises only the alocação line; the progress bar shows only with an alvo and the saída guard blocks an over-saldo withdrawal.</human-check></verify>
  <acceptance_criteria>
    - Logged-in personal user on the production *.vercel.app URL (12-02 bundle) sets a teto + an alvo meta and, after logging transactions, sees the over-teto row RED "Estourou", a ≥80%<100% row amber "Aproximando", an alvo-below row muted "Abaixo", and an alvo-at-target row GREEN "Atingido" — and confirms no gasto is red merely for being a gasto.
    - Switching between the "Mensal" and "Anual (YTD)" dashboard tabs shows the same category order and color logic, and for a single populated month the two windows report consistent adherence % (DEPLOY-05 monthly+annual goal adherence, live).
    - Picking the "Reserva" category in the Novo lançamento dialog reveals the "Qual reserva?" picker; submitting links an aporte to the chosen reserva's ledger (/reservas/[id]) and raises the dashboard alocação line, not any consumo line.
    - A reserva created WITH an alvo shows a progress bar; a reserva WITHOUT an alvo shows none; a saída larger than the saldo is blocked with the FieldError and a valid saída lowers the derived saldo on reload (persisted remotely).
  </acceptance_criteria>
  <done>BUD-02 / RSV-01 / RSV-02 / RSV-05 confirmed live in production against the 12-02 bundle; the monthly+annual goal-adherence half of DEPLOY-05 proven live; no re-deploy occurred (D-08). Sign-off (or punch list) recorded.</done>
</task>

</tasks>

<artifacts_produced>
This live-verify plan produces NO new source symbols. It produces VERIFICATION EVIDENCE only:
- `.planning/phases/12-produ-o-live-verify/12-04-SUMMARY.md` — the production sign-off (or punch list) for BUD-02 / RSV-01 / RSV-02 / RSV-05 and the monthly+annual goal-adherence half of DEPLOY-05.
- Seed data the user creates (metas, transactions, reservas, aportes/saídas) lives in the user's own production account, scoped by RLS. No test fixtures, no migrations, no code.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → production app (authenticated) | the signed-in session drives meta/reserva/transação writes to the remote Supabase; RLS must scope each to the user's user_id |
| reserva saldo math | derived balance + saída-never-negative must hold against the remote DB exactly as locally (server-enforced, not UI-only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04-rls (I) | Information Disclosure | metas/reservas reads/writes leaking across users on the remote project | mitigate | 12-02 already asserted no table is "Unrestricted" remotely; the walkthrough operates only as the single signed-in user and confirms reads/writes reflect only that account |
| T-12-04-saida (T) | Tampering / data integrity | a saída larger than the saldo persisting (negative balance) against the remote DB | mitigate | the walkthrough explicitly attempts an over-saldo saída and confirms it is BLOCKED with the FieldError, proving the server guard (not just the UI) holds remotely |
| T-12-04-SC (T) | Tampering | npm/pip/cargo installs | accept | no packages installed — verification-only plan against an already-deployed bundle |
</threat_model>

<verification>
- The walkthrough runs against the production *.vercel.app URL from 12-02 (the SAME bundle, no re-deploy — D-08).
- Human-verified live in production: direction-aware adherence color (BUD-02); monthly AND annual adherence consistency (DEPLOY-05 goal half); the qual-reserva sub-flow (RSV-02); the alvo-only progress bar + over-saldo saída guard (RSV-01/05).
</verification>

<success_criteria>
- BUD-02 / RSV-01 / RSV-02 / RSV-05 confirmed live in production (re-proven against the remote stack, not just the local stack 03-06 originally targeted).
- DEPLOY-05's "aderência às metas mensal E anual" half proven live, against the production receitas/net-income from Plan 12-03.
- No re-deploy occurred. Reviewer signs off "approved" (or a punch list is captured for gap closure).
</success_criteria>

<output>
Create `.planning/phases/12-produ-o-live-verify/12-04-SUMMARY.md` when done (record the production sign-off or the issue list).
</output>
