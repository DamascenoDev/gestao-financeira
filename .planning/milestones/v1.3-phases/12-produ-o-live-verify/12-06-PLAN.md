---
phase: 12-produ-o-live-verify
plan: 06
type: execute
wave: 6
depends_on: ["12-05"]
files_modified: []
autonomous: false
requirements: [MEI-01, MEI-02, MEI-03, MEI-04, MEI-05, MEI-06]
user_setup:
  - service: app (production)
    why: "Live-verify of the MEI module end-to-end against the PRODUCTION *.vercel.app URL from 12-02 (D-08 — no re-deploy). Browser-interactive + a CSV download against the personal account."
    dashboard_config:
      - task: "Open the *.vercel.app production URL from 12-02-SUMMARY.md, signed in with the personal account; the MEI routes (/mei, /mei/notas, /mei/configuracoes, /mei/relatorio) are served by the 12-02 bundle"
        location: "browser (production)"

must_haves:
  truths:
    - "The MEI module works end-to-end in production against the 12-02 bundle: register NF → list + year total + dashboard gauge/status update; the proportional first-year limit reflects the configured start date (MEI-01/02/03)"
    - "The gauge/status colors + the ≥80% alert row render correctly live against the remote DB; crossing 80% surfaces the amber 'Você atingiu {%}…' alert (MEI-02/05)"
    - "The DASN-SIMEI report (MEI-04) shows the right Receita bruta total, the Comércio+Serviços split summing to total, the employee flag, and the '31 de maio de {ano+1}' deadline; 'Exportar CSV' downloads dasn-{ano}.csv with correct pt-BR encoding (BOM + ; columns); print view keeps the disclaimer without the sidebar"
    - "The 'Este módulo é informativo e não constitui consultoria fiscal.' disclaimer (MEI-06) is visible above the content on every MEI screen; year isolation holds (changing the YearSelector reflects THAT year's NFs/limit/report)"
  artifacts: []
  key_links: []
---

<objective>
Live-verify the MEI module end-to-end in PRODUCTION — NF CRUD + proportional first-year limit + gauge/status/alert + DASN-SIMEI report + CSV export + always-visible disclaimer + year isolation — by SEQUENCING AND EXECUTING the already-written walkthrough `.planning/phases/05-m-dulo-mei-dasn-simei/05-04-PLAN.md` against the single *.vercel.app bundle Plan 12-02 deployed (D-07 step 4, fifth in the sequence). The original 05-04 ran against the LOCAL stack; this plan runs the same eight-check walkthrough against the PRODUCTION URL + remote Supabase, with NO re-deploy (D-08). Every step is browser-interactive against production → autonomous: false.

Purpose: prove MEI-01/02/03/04/05/06 work live in production (re-proven against the remote stack, not just the local stack 05-04 originally targeted).
Output: a production sign-off (or punch list) for the MEI dashboard, NF list, settings, and DASN report; verification evidence in 12-06-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-produ-o-live-verify/12-CONTEXT.md
@.planning/phases/05-m-dulo-mei-dasn-simei/05-04-PLAN.md
@.planning/phases/12-produ-o-live-verify/12-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live-verify the MEI module end-to-end in production (executes 05-04 against the 12-02 bundle)</name>
  <files>none (verification only — produces sign-off, no symbols)</files>
  <read_first>
    - .planning/phases/05-m-dulo-mei-dasn-simei/05-04-PLAN.md (the authoritative walkthrough — its Task 2 checkpoint holds the exact eight MEI checks; this plan RUNS those steps, do not re-author them; the ONLY delta is the target is the PRODUCTION *.vercel.app URL + remote Supabase, not the local stack)
    - .planning/phases/12-produ-o-live-verify/12-02-SUMMARY.md (the recorded production *.vercel.app URL — the single bundle to verify against; NO re-deploy per D-08)
    - .planning/phases/05-m-dulo-mei-dasn-simei/05-UI-SPEC.md (the gauge/status/disclaimer design contract)
  </read_first>
  <what-built>The complete MEI module (schema + summary view, IDOR-safe NF/settings actions, the dashboard with LimiteGauge + status badge + alert, the NF form/list, the settings form, the DASN report + CSV export, the persistent disclaimer), now served by the production *.vercel.app bundle from 12-02 against the remote Supabase. All logic was automated GREEN in the source phase; this is the production interaction confirmation of the human-judged behaviors (gauge/status colors, computed proportional-limit copy, alert rows, report layout + CSV file, always-visible disclaimer).</what-built>
  <how-to-verify>
Open the production *.vercel.app URL from 12-02-SUMMARY.md (the SAME deploy — do NOT run `vercel --prod` again, D-08), signed in as the personal account. Walk 05-04's eight checks AGAINST PRODUCTION:

1. Configurações (MEI-03): visit /mei/configuracoes. Set a MEI start date in the CURRENT year but NOT January (e.g. a date in abril) so the first-year cap is proportional. Toggle "Tinha funcionário em {ano}?" on, save. Confirm the toast "Configurações salvas".
2. Dashboard proportional limit (MEI-02): visit /mei. Confirm the limit line reads a COMPUTED proportional value (e.g. "Limite proporcional {ano} · R$ 60.750,00 — N meses a partir de {mês}/{ano}") and NOT a bare "R$ 81.000". With no NFs yet the gauge is verde at 0%.
3. Registrar NF (MEI-01/03): visit /mei/notas, click "Registrar NF". Add a few NFs across the year mixing activity types (Comércio/Indústria and Serviços), with values that together reach roughly 60–85% of the proportional limit. Confirm each saves (toast), appears in the list with the right Atividade badge + green + value, and the year-total footer matches.
4. Gauge + status + alert (MEI-02/05): return to /mei. Confirm the hero gross matches the year total, the gauge fill + % reflect gross ÷ applicable limit, the status badge reads the right tier, and — once you cross 80% — the amber "Você atingiu {%}…" alert row appears.
5. Edit/delete an NF: edit one NF's value and delete another; confirm the list, year total, and dashboard gauge all update (persisted to the remote DB).
6. Relatório DASN-SIMEI (MEI-04): visit /mei/relatorio. Confirm "Receita bruta total" equals the year gross, the Comércio + Serviços split sums to the total, "Empregado durante o ano-calendário" reads Sim (from step 1), and the DASN deadline reads "31 de maio de {ano+1}". Click "Exportar CSV" and open the downloaded dasn-{ano}.csv — confirm it contains the same total, split, employee flag, and that pt-BR amounts render correctly (open in a spreadsheet to confirm the BOM/; make columns split). Use the print affordance; confirm the print view keeps the disclaimer + report on white without the sidebar.
7. Disclaimer (MEI-06): on EACH of /mei, /mei/notas, /mei/configuracoes, /mei/relatorio confirm the "Este módulo é informativo e não constitui consultoria fiscal." banner is visible above the content (not a tiny footer) and is calm/neutral (not teal, not red).
8. Year isolation: change the YearSelector to a different year and confirm the NFs/limit/report reflect THAT year (the previous year's NFs do not bleed in).
  </how-to-verify>
  <resume-signal>Type "approved" once all eight MEI checks pass on the PRODUCTION URL, or describe any issue (which screen, expected vs saw) — issues become a `/gsd-plan-phase 12 --gaps` punch list.</resume-signal>
  <action>HUMAN CHECKPOINT (autonomous:false) — execute the 05-04 walkthrough against the production *.vercel.app URL from 12-02 (NOT the local stack the original 05-04 targeted, and with NO re-deploy per D-08). The exact steps live in <how-to-verify>; the source-of-truth steps live in 05-04-PLAN.md Task 2. Pause for the user to sign in to production and walk the eight MEI checks (settings → proportional limit → NF CRUD → gauge/alert → report + CSV → disclaimer → year isolation). Browser interaction, the CSV download, and remote-DB persistence cannot be automated.</action>
  <verify><human-check>On the production *.vercel.app URL: settings save with a non-January start date; /mei shows a COMPUTED proportional limit (not a bare R$ 81.000); NF CRUD updates the list + year total + gauge; crossing 80% surfaces the amber alert; /mei/relatorio shows the correct total + Comércio/Serviços split + employee flag + "31 de maio de {ano+1}" deadline and exports a pt-BR dasn-{ano}.csv (BOM + ; columns); the disclaimer is visible above content on all four MEI screens; the YearSelector isolates per-year data.</human-check></verify>
  <acceptance_criteria>
    - Logged-in personal user on the production *.vercel.app URL (12-02 bundle) saves a MEI start date in a non-January month and /mei then renders a COMPUTED proportional first-year limit line (not a bare "R$ 81.000").
    - Registering several NFs across Comércio/Indústria + Serviços updates the list (right Atividade badge), the year-total footer, and the dashboard gauge/% — and crossing 80% surfaces the amber "Você atingiu {%}…" alert row.
    - Editing one NF and deleting another updates the list, year total, and gauge on reload (persisted to the remote DB).
    - /mei/relatorio shows Receita bruta total = year gross, a Comércio+Serviços split summing to the total, the employee flag = Sim, and the "31 de maio de {ano+1}" deadline; "Exportar CSV" downloads dasn-{ano}.csv that opens in a spreadsheet with correct pt-BR columns (BOM + ; separator).
    - The "Este módulo é informativo e não constitui consultoria fiscal." disclaimer is visible above the content on /mei, /mei/notas, /mei/configuracoes, and /mei/relatorio; changing the YearSelector reflects only that year's NFs/limit/report.
  </acceptance_criteria>
  <done>MEI-01/02/03/04/05/06 confirmed live in production against the 12-02 bundle; no re-deploy occurred (D-08). Sign-off (or punch list) recorded.</done>
</task>

</tasks>

<artifacts_produced>
This live-verify plan produces NO new source symbols. It produces VERIFICATION EVIDENCE only:
- `.planning/phases/12-produ-o-live-verify/12-06-SUMMARY.md` — the production sign-off (or punch list) for MEI-01..06.
- Seed data the user creates (MEI settings, NFs) and the exported dasn-{ano}.csv live in / derive from the user's own production account, scoped by RLS. No test fixtures, no migrations, no code.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → production app (authenticated) | the signed-in session drives MEI NF/settings writes to the remote Supabase; RLS + IDOR-safe actions must scope each to the user's user_id |
| CSV export → user's disk | the dasn-{ano}.csv leaves the app; it contains the user's own financial totals only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06-idor (E) | Elevation of Privilege | MEI NF/settings actions reachable for another user's rows on the remote project | mitigate | the source-phase actions are IDOR-safe (asserted by automated tests); the walkthrough operates only as the single signed-in user; 12-02 confirmed no remote table is "Unrestricted" |
| T-12-06-year (T) | Tampering / data integrity | year isolation broken (one year's NFs bleeding into another's totals/report) on the remote DB | mitigate | check 8 explicitly switches the YearSelector and confirms per-year isolation against the remote data |
| T-12-06-SC (T) | Tampering | npm/pip/cargo installs | accept | no packages installed — verification-only plan against an already-deployed bundle |
</threat_model>

<verification>
- The walkthrough runs against the production *.vercel.app URL from 12-02 (the SAME bundle, no re-deploy — D-08).
- Human-verified live in production: proportional limit copy (MEI-02/03); NF CRUD + split (MEI-01); gauge/status/alert (MEI-02/05); DASN report + CSV + print (MEI-04); disclaimer on every MEI screen (MEI-06); year isolation.
</verification>

<success_criteria>
- MEI-01/02/03/04/05/06 confirmed live in production (re-proven against the remote stack, not just the local stack 05-04 originally targeted).
- No re-deploy occurred. Reviewer signs off "approved" (or a punch list is captured for gap closure).
</success_criteria>

<output>
Create `.planning/phases/12-produ-o-live-verify/12-06-SUMMARY.md` when done (record the production sign-off or the issue list).
</output>
