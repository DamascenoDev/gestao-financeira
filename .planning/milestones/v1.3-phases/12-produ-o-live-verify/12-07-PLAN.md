---
phase: 12-produ-o-live-verify
plan: 07
type: execute
wave: 7
depends_on: ["12-06"]
files_modified: []
autonomous: false
requirements: [DATA-01, DATA-02, SEC-01]
user_setup:
  - service: app (production)
    why: "Live-verify of the LGPD/export/delete surface + the deployed-bundle secret gate against the PRODUCTION *.vercel.app URL from 12-02 (D-08 — no re-deploy). The destructive delete is run ONLY against a THROWAWAY production signup, never the personal account."
    dashboard_config:
      - task: "Open the *.vercel.app production URL from 12-02-SUMMARY.md; the transactions CSV + LGPD bundle checks run on the personal account; the type-to-confirm APAGAR delete runs on a fresh THROWAWAY signup created for this test"
        location: "browser (production)"

must_haves:
  truths:
    - "DATA-01 confirmed live in production: the transactions CSV downloads from /extrato AND /conta as transacoes-{yyyy-MM}.csv with correct pt-BR encoding (BOM, ; columns, comma decimals, resolved category names)"
    - "DATA-02 (export) confirmed live: 'Baixar meus dados' downloads meus-dados-{yyyy-MM-dd}.json with all the table sections + the csv section"
    - "DATA-02 (delete) confirmed live against a THROWAWAY production user: the 'Apagar conta' button stays disabled until exactly 'APAGAR' is typed; confirming signs the user out to /auth/login, the throwaway account can no longer sign in, and the personal account is untouched (surgical delete)"
    - "SEC-01 confirmed on the DEPLOYED bundle: no sb_secret_/service_role string appears in the production client JS (re-confirmed against the live *.vercel.app site, complementing the 12-02 build-time gate) — even though LGPD delete uses the service role server-side"
  artifacts: []
  key_links: []
---

<objective>
Live-verify the Phase-6 hardening surface in PRODUCTION — the transactions CSV export (DATA-01), the LGPD "Baixar meus dados" bundle + the type-to-confirm account delete (DATA-02), and the deployed-bundle secret gate (SEC-01) — by SEQUENCING AND EXECUTING the already-written walkthrough `.planning/phases/06-endurecimento-lgpd-isolamento-auditoria/06-05-PLAN.md` against the single *.vercel.app bundle Plan 12-02 deployed (D-07 step 4, the SIXTH and FINAL walkthrough in the sequence). The original 06-05 ran against the LOCAL stack with a `.next/static` secret grep; this plan runs the browser-only confirmations against the PRODUCTION URL + remote Supabase, and re-confirms SEC-01 against the LIVE deployed client JS (the build-time secret gate already ran in 12-02 Task 1; this complements it with the deployed-bundle devtools check). The destructive delete is run ONLY against a fresh THROWAWAY production signup — NEVER the personal account. NO re-deploy (D-08). Every step is browser-interactive against production → autonomous: false.

This is the FINAL plan of Phase 12 — completing the D-07 sequence (01-04→02-05→03-06→04-04→05-04→06-05) and closing the v1.0 deploy backlog live in production.

Purpose: prove DATA-01 / DATA-02 / SEC-01 live in production (re-proven against the remote stack + deployed bundle, not just the local stack 06-05 originally targeted).
Output: a production sign-off (or punch list) for the CSV export, the LGPD bundle, the type-to-confirm delete, and the deployed-bundle secret gate; verification evidence in 12-07-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-produ-o-live-verify/12-CONTEXT.md
@.planning/phases/06-endurecimento-lgpd-isolamento-auditoria/06-05-PLAN.md
@.planning/phases/12-produ-o-live-verify/12-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live-verify LGPD export + type-to-confirm delete + deployed-bundle secret gate in production (executes 06-05 against the 12-02 bundle)</name>
  <files>none (verification only — produces sign-off, no symbols)</files>
  <read_first>
    - .planning/phases/06-endurecimento-lgpd-isolamento-auditoria/06-05-PLAN.md (the authoritative walkthrough — its Task 2 checkpoint holds the exact CSV / LGPD-bundle / type-to-confirm-delete steps; this plan RUNS those steps, do not re-author them; deltas: target is the PRODUCTION *.vercel.app URL + remote Supabase; the secret check is the DEPLOYED client JS via devtools, not the local `.next/static` grep — the build-time grep already ran in 12-02; the throwaway user is a fresh PRODUCTION signup)
    - .planning/phases/12-produ-o-live-verify/12-02-SUMMARY.md (the recorded production *.vercel.app URL — the single bundle to verify against; NO re-deploy per D-08; 12-02 already ran the build-time secret-bundle gate + a deployed-bundle devtools spot-check)
    - .planning/phases/06-endurecimento-lgpd-isolamento-auditoria/06-UI-SPEC.md (the CSV/LGPD/delete-dialog design contract)
  </read_first>
  <what-built>The DATA-01/DATA-02 browser surfaces (the transactions CSV export on /extrato + /conta, the LGPD "Baixar meus dados" bundle, and the type-to-confirm "APAGAR" account-delete that signs the user out) + the SEC-01 secret isolation, now served by the production *.vercel.app bundle from 12-02 against the remote Supabase. All logic was automated GREEN in the source phase (transactions-csv, lgpd-export, lgpd-delete, lgpd-delete-isolation, isolation-matrix, storage-isolation, bundle-secret-grep, pii-guard); this is the production browser-only confirmation that the files download correctly, the delete friction feels right + signs out, and the live bundle carries no secret.</what-built>
  <how-to-verify>
Open the production *.vercel.app URL from 12-02-SUMMARY.md (the SAME deploy — do NOT run `vercel --prod` again, D-08). Walk 06-05's checks AGAINST PRODUCTION:

1. **Transactions CSV (DATA-01)** — signed in as the personal account: on /extrato pick a month with transactions (the uploads from Plan 12-05 give you data), click "Exportar transações (CSV)". Confirm a transacoes-{yyyy-MM}.csv downloads and opens in a spreadsheet pt-BR with: a BOM (no mojibake on acentos), ; columns, comma decimals (e.g. R$ 1.234,56), columns Data (dd/MM/yyyy) · Descrição · Categoria · Tipo (Consumo/Alocação) · Valor, and the category NAME resolved. Repeat the same button on /conta.
2. **LGPD bundle (DATA-02 export)** — on /conta, click "Baixar meus dados". Confirm a meus-dados-{yyyy-MM-dd}.json downloads; open it and confirm a `tables` object with all sections (receitas, transações, categorias, metas, reservas, MEI, padrões, statements metadata) and a `csv` section with transactions + MEI strings.
3. **Type-to-confirm delete (DATA-02 delete) — THROWAWAY PRODUCTION USER ONLY:** Sign up a NEW throwaway account on the production URL (e.g. descartavel+prod@example.test; open signup, email confirm OFF), add a row or two so there's data, then on /conta open the "Apagar conta" dialog. Confirm: the consequences list is visible; the "Apagar conta" button is DISABLED until you type exactly APAGAR (lowercase/partial stays disabled); initial focus is on Cancelar (a stray Enter cancels); on confirm it shows "Apagando…", deletes, and signs you out to /auth/login. Then try to sign in again as that throwaway user — it MUST fail (the account is gone). Sign back in as your PERSONAL account and confirm it still works + its data is intact (the delete was surgical, not collateral).
4. **SEC-01 deployed-bundle secret gate:** In the live deployed site's devtools → Sources (or via "view-source"/a network search of the served JS), confirm NO sb_secret_/service_role string appears in the production client JS — even though LGPD delete uses the service role SERVER-SIDE. (This complements the build-time grep gate already run in 12-02 Task 1; here you confirm it on the actually-served bundle.)
  </how-to-verify>
  <resume-signal>Type "approved" once the transactions CSV downloads pt-BR-correct from /extrato + /conta, the LGPD bundle downloads with all sections, the type-to-confirm APAGAR delete (on a THROWAWAY production user) signs out + is surgical (personal account intact), and no secret appears in the deployed client JS. Or describe any issue — issues become a `/gsd-plan-phase 12 --gaps` punch list.</resume-signal>
  <action>HUMAN CHECKPOINT (autonomous:false) — execute the 06-05 walkthrough against the production *.vercel.app URL from 12-02 (NOT the local stack the original 06-05 targeted, and with NO re-deploy per D-08). The exact steps live in <how-to-verify>; the source-of-truth steps live in 06-05-PLAN.md Task 2. Pause for the user to: (a) download the transactions CSV from /extrato + /conta; (b) download the LGPD bundle; (c) run the type-to-confirm delete ONLY against a fresh THROWAWAY production signup and confirm the personal account survives; (d) confirm no secret in the deployed client JS. File downloads, browser session/cookie behavior, and devtools inspection cannot be automated.</action>
  <verify><human-check>On the production *.vercel.app URL: transactions CSV downloads pt-BR-correct (BOM + ; columns, resolved category names) from /extrato AND /conta; the LGPD bundle JSON downloads with all table sections + the csv section; the type-to-confirm APAGAR delete on a THROWAWAY production user signs out to /auth/login, the throwaway account can no longer sign in, and the PERSONAL account is intact; no sb_secret_/service_role string appears in the deployed client JS.</human-check></verify>
  <acceptance_criteria>
    - Logged-in personal user on the production *.vercel.app URL (12-02 bundle) clicks "Exportar transações (CSV)" on /extrato and on /conta → transacoes-{yyyy-MM}.csv downloads and opens in a spreadsheet with a BOM, ; column separator, comma decimals, the Data/Descrição/Categoria/Tipo/Valor columns, and the category NAME resolved (DATA-01).
    - "Baixar meus dados" on /conta downloads meus-dados-{yyyy-MM-dd}.json containing a `tables` object with every section (receitas, transações, categorias, metas, reservas, MEI, padrões, statements metadata) and a `csv` section (DATA-02 export).
    - On a FRESH THROWAWAY production signup (never the personal account), the "Apagar conta" button stays disabled until exactly "APAGAR" is typed; confirming shows "Apagando…", deletes, and redirects to /auth/login; re-signing-in as the throwaway user fails; the personal account still signs in with its data intact (DATA-02 delete, surgical).
    - Inspecting the live deployed client JS (devtools Sources / served bundle) shows NO sb_secret_/service_role string, confirming SEC-01 on the actually-served production bundle (complements the 12-02 build-time gate).
  </acceptance_criteria>
  <done>DATA-01 / DATA-02 / SEC-01 confirmed live in production against the 12-02 bundle; the destructive delete was run only against a throwaway production user and proven surgical; no re-deploy occurred (D-08). This completes the D-07 walkthrough sequence. Sign-off (or punch list) recorded.</done>
</task>

</tasks>

<artifacts_produced>
This live-verify plan produces NO new source symbols. It produces VERIFICATION EVIDENCE only:
- `.planning/phases/12-produ-o-live-verify/12-07-SUMMARY.md` — the production sign-off (or punch list) for DATA-01 / DATA-02 / SEC-01, and the note that Phase 12's D-07 walkthrough sequence is complete.
- The downloaded transacoes-{yyyy-MM}.csv + meus-dados-{yyyy-MM-dd}.json land on the user's disk (DATA-01/DATA-02 evidence). The throwaway production account is created AND deleted within the walkthrough. No test fixtures, no migrations, no code.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| account-delete action → service role (server-side) | LGPD delete uses the service-role secret SERVER-SIDE; that secret must never reach the client bundle |
| deployed client JS → public internet | the live bundle is world-readable; a secret there is fully exposed |
| destructive delete → real data | a delete run against the wrong account is irreversible; it must target only a throwaway production user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07-secret (I) | Information Disclosure | sb_secret_/service_role string present in the DEPLOYED client JS | mitigate | the 12-02 build-time grep gate exited 0 AND this plan re-confirms on the served bundle via devtools (SEC-01); the secret is a non-public server var (set in 12-02) |
| T-12-07-deldata (T) | Tampering / irreversible data loss | the type-to-confirm delete run against the personal account (collateral) | mitigate | the walkthrough mandates a FRESH THROWAWAY production signup for the delete and explicitly confirms the personal account survives intact afterward |
| T-12-07-delisolation (I) | Information Disclosure | the delete leaking into / failing to fully remove another user's data on the remote DB | mitigate | the source-phase lgpd-delete-isolation test is GREEN; the live check confirms the throwaway account is fully gone (cannot re-sign-in) and the personal account is untouched |
| T-12-07-SC (T) | Tampering | npm/pip/cargo installs | accept | no packages installed — verification-only plan against an already-deployed bundle |
</threat_model>

<verification>
- The walkthrough runs against the production *.vercel.app URL from 12-02 (the SAME bundle, no re-deploy — D-08).
- Human-verified live in production: pt-BR transactions CSV from /extrato + /conta (DATA-01); the LGPD bundle JSON with all sections (DATA-02 export); the type-to-confirm APAGAR delete on a THROWAWAY production user, signing out + surgical (DATA-02 delete); no secret in the deployed client JS (SEC-01, complementing the 12-02 build-time gate).
- This is the sixth and final walkthrough of the D-07 sequence; the v1.0 deploy backlog is now verified live in production.
</verification>

<success_criteria>
- DATA-01: transactions CSV export confirmed in production (pt-BR correct) from /extrato + /conta.
- DATA-02: LGPD bundle download + type-to-confirm delete + sign-out confirmed in production (throwaway production user only; personal account intact).
- SEC-01: no secret in the deployed client JS, confirmed on the served bundle (complements the 12-02 build-time gate).
- No re-deploy occurred. The D-07 walkthrough sequence is complete. Reviewer signs off "approved" (or a punch list is captured for gap closure).
</success_criteria>

<output>
Create `.planning/phases/12-produ-o-live-verify/12-07-SUMMARY.md` when done (record the production sign-off or the issue list, and note that Phase 12's full D-07 walkthrough sequence is verified live in production).
</output>
