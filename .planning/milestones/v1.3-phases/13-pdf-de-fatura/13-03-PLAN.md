---
phase: 13-pdf-de-fatura
plan: 03
type: execute
wave: 2
depends_on: [13-01, 13-02]
files_modified:
  - src/actions/import.ts
  - src/lib/schemas/import.ts
  - src/app/(app)/importar/page.tsx
autonomous: true
requirements: [PDF-01, PDF-02, PDF-04, PDF-05]
user_setup: []

must_haves:
  truths:
    - "A .pdf path routes through ingestStatement to the PDF dispatch branch (extSchema accepts 'pdf', ext detection is 3-way)"
    - "An image-only PDF (extractPdfText returns empty/whitespace) returns a clear { error } steering to CSV/OFX — distinct from a text-present 0-row parse, which shows the review screen"
    - "A PDF that extracts text but matches 0 transaction lines is NOT a hard block — it flows to the review grid with honest 0/J counts"
    - "confirmImport persists estorno rows with the server-derived kind ('credit'), read from r.base.kind, never the client payload"
    - "The /importar route segment pins runtime='nodejs' + maxDuration so pdf-parse runs on Node, never Edge"
    - "A throw inside PDF extraction degrades to a friendly { error }, never a 500 (V12)"
  artifacts:
    - path: "src/actions/import.ts"
      provides: "'pdf' in extSchema, 3-way ext detection, PDF dispatch branch with image-only block, kind threading in confirmImport"
      contains: "'pdf'"
    - path: "src/app/(app)/importar/page.tsx"
      provides: "runtime='nodejs' + maxDuration exports (D-08)"
      contains: "runtime"
  key_links:
    - from: "src/actions/import.ts"
      to: "src/lib/parsers/pdf.ts"
      via: "ext==='pdf' → extractPdfText(bytes) then parseSantanderText(text, venc)"
      pattern: "parseSantanderText"
    - from: "src/actions/import.ts (confirmImport)"
      to: "transactions.kind column"
      via: "insert kind: r.base.kind ?? 'expense' (server-derived, WR-01 safe)"
      pattern: "r\\.base\\.kind"
---

<objective>
Wire the `'pdf'` parser into the existing `ingestStatement` server action as the third dispatch branch, add the PDF-04 image-only hard block (distinct from a text-present 0-row parse), thread the estorno `kind` through `confirmImport`, and pin the `/importar` route segment to the Node runtime with `maxDuration` (D-08). Per the spike (24–182 ms parse), the action stays synchronous — no new Route Handler.

Purpose: PDF is overwhelmingly additive — dedup, memory-classify, and the confirm/learn loop are reused verbatim. The only server changes are the dispatch branch, the image-only block, the `kind` read from the server-persisted base row (WR-01 safe), and the runtime pin honoring CLAUDE.md's locked Node-runtime + maxDuration guidance (per D-08 §"D-08 Resolution"). Document the 24–182 ms evidence so the runtime-pinned-server-action choice (vs. a literal Route Handler) is auditable.

Output: `import.ts` (extSchema + 3-way ext + PDF dispatch + kind threading); `schemas/import.ts` (optional kind enum if the payload carries it); `importar/page.tsx` (runtime exports + helper copy).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-pdf-de-fatura/13-CONTEXT.md
@.planning/phases/13-pdf-de-fatura/13-RESEARCH.md
@.planning/phases/13-pdf-de-fatura/13-PATTERNS.md
@.planning/phases/13-pdf-de-fatura/13-UI-SPEC.md
@src/actions/import.ts
@src/lib/schemas/import.ts
@src/lib/parsers/types.ts
@src/lib/parsers/csv.ts
@src/lib/dedupe.ts
@src/app/(app)/importar/page.tsx
@.planning/phases/04-upload-classifica-o-inteligente/04-CONTEXT.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 13-03 portion)
- `'pdf'` added to `extSchema` (`z.enum(['ofx','csv','pdf'])`) in `src/actions/import.ts`
- 3-way ext detection branch (`.csv` → csv, `.pdf` → pdf, else ofx)
- a new `ext === 'pdf'` dispatch branch in `ingestStatement` (calls `extractPdfText` + `parseSantanderText`; image-only `{ error }` block)
- `confirmImport` insert changed from hard-coded `kind: 'expense'` to `kind: r.base.kind ?? 'expense'`
- optional `kind` enum on the confirm schema in `src/lib/schemas/import.ts` (only if the payload carries it)
- `runtime`/`maxDuration` exports on `src/app/(app)/importar/page.tsx`
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 'pdf' to extSchema + 3-way ext detection + PDF dispatch branch with image-only block</name>
  <files>src/actions/import.ts</files>
  <read_first>
    - src/actions/import.ts lines 104 (extSchema), 122-128 (decodeStatement latin1 — CSV/OFX ONLY), 187-189 + 245 + 327 (the format-naming friendly-error strings), 238 (the 2-way ext detection), 282-329 (the dispatch wrapper `try { if (ext==='ofx'){...} else {...} } catch { return { error } }`), 399-404 (the IngestSummary build incl. `descartadas: dropped`)
    - src/lib/parsers/pdf.ts (the 13-01 exports: `extractPdfText`, `parseSantanderText`, `findStatementVencimento`)
    - .planning/phases/13-pdf-de-fatura/13-RESEARCH.md §"Code Examples" → "Image-only detection" (the exact branch body: extract → `text.trim().length===0 ? return {error: <CSV/OFX-steering msg>}` → `parseSantanderText(text, venc)`; the IMPORTANT note: image-only is DISTINCT from "parsed 0 rows")
    - .planning/phases/13-pdf-de-fatura/13-PATTERNS.md §"src/actions/import.ts" (extSchema extension, ext detection 3-way, dispatch branch, the Pitfall-7 caveat: do NOT run decodeStatement on PDF bytes)
    - .planning/phases/13-pdf-de-fatura/13-UI-SPEC.md §4 (the PDF-04 copy) + Copywriting Contract (the exact image-only error message)
  </read_first>
  <behavior>
    - `extSchema.safeParse('pdf')` succeeds; `extSchema.safeParse('xml')` still fails.
    - A `.pdf` path routes to the PDF branch; a `.csv` → csv; everything else → ofx (3-way).
    - PDF branch with empty/whitespace extracted text returns `{ error }` whose message steers to CSV/OFX (PDF-04 hard block).
    - PDF branch with text present but `parseSantanderText` returns 0 rows returns a normal review result (statementId + empty rows + honest summary), NOT an error.
    - A throw inside the PDF branch is caught by the existing dispatch wrapper → friendly `{ error }`, never an unhandled 500 (V12).
    - `decodeStatement` (latin1) is NOT applied to PDF bytes; the buffer goes straight to `extractPdfText`.
  </behavior>
  <action>
    In `src/actions/import.ts`: extend `extSchema` (line 104) to `z.enum(['ofx', 'csv', 'pdf'])`. Update the format-naming friendly-error strings (lines 189, 245, 327) to include PDF. Extend the 2-way ext detection (line 238) to 3-way: `.csv` → `'csv'`, else `.pdf` → `'pdf'`, else `'ofx'`. Add a `pdf` branch INSIDE the existing dispatch `try/catch` wrapper (lines 299-329) so any residual throw degrades to `{ error }` (V12 / T-13-06): for `ext === 'pdf'`, call `extractPdfText(bytes)` (the downloaded Storage buffer — do NOT run `decodeStatement`, Pitfall 7), then if `text.trim().length === 0` return `{ error }` with the UI-SPEC image-only message steering to CSV/OFX (PDF-04 hard block, DISTINCT from a 0-row parse), else `findStatementVencimento(text)` and `parseSantanderText(text, venc)` → the same `ParseResult` flow as OFX/CSV. The downstream dedup (`dedupeKey` auto-uses the csv basis when fitid is absent — NO change to `dedupe.ts`) and the memory-classify loop are untouched. Keep the action synchronous (parse measured 24–182 ms — document this in the SUMMARY).

    If 13-01 tests already cover `parseSantanderText`, add a focused test here only for the image-only-vs-0-rows distinction at the action seam if a server-action test harness exists; otherwise rely on the existing parser tests + tsc and cover the distinction with the manual local upload (VALIDATION manual gate).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/lib/parsers/pdf.test.ts && grep -q "'pdf'" src/actions/import.ts && grep -q "extractPdfText" src/actions/import.ts</automated>
  </verify>
  <acceptance_criteria>
    - `extSchema` is `z.enum(['ofx', 'csv', 'pdf'])` (grep `'pdf'` in import.ts matches)
    - ext detection is 3-way (`.pdf` → `'pdf'`)
    - The PDF dispatch branch calls `extractPdfText` then `parseSantanderText` and returns `{ error }` on `text.trim().length === 0`
    - `decodeStatement` is NOT called on the PDF path (grep confirms the latin1 path stays in the ofx/csv branches only)
    - `npx tsc --noEmit` passes; `npx vitest run src/lib/parsers/pdf.test.ts` GREEN
  </acceptance_criteria>
  <done>'pdf' wired into extSchema + ext detection + dispatch with the image-only block; latin1 path excluded from PDF.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread estorno kind through confirmImport + optional confirm-schema kind</name>
  <files>src/actions/import.ts, src/lib/schemas/import.ts</files>
  <read_first>
    - src/actions/import.ts lines 578-609 (the WR-01 trusted-content re-read: `parsed_rows` re-read, keyed by dedupe_key; `r.base` is server-persisted authoritative content), line 651 (the hard-coded `kind: 'expense'` in the transactions insert), lines 522-570 (per-FK ownership re-derive)
    - src/lib/schemas/import.ts lines 36-46 (`confirmImportRowSchema`, the optional `reservaId`/`carroId` grammar)
    - .planning/phases/13-pdf-de-fatura/13-RESEARCH.md Pitfall 3 + Open Question 1 (read `kind` from `r.base.kind ?? 'expense'`, NEVER the client payload — WR-01)
    - .planning/phases/13-pdf-de-fatura/13-PATTERNS.md §"src/actions/import.ts" (kind threading) + §"src/lib/schemas/import.ts" + §"WR-01 trusted-content re-read"
    - supabase/migrations/0005_transactions.sql (the kind column — widened to accept 'credit' by Plan 13-02, which this plan depends_on)
  </read_first>
  <behavior>
    - A confirmed estorno row (its server-persisted `base.kind === 'credit'`) is inserted into `transactions` with `kind: 'credit'`.
    - A normal compra row (no `kind`, or `kind === 'expense'`) is inserted with `kind: 'expense'` (the `?? 'expense'` default).
    - The `kind` value comes from `r.base.kind`, NEVER from the client confirm payload (WR-01: client supplies only category/reserva/carro choice).
  </behavior>
  <action>
    In `src/actions/import.ts` `confirmImport`, change the transactions insert (line 651) from the hard-coded `kind: 'expense'` to `kind: r.base.kind ?? 'expense'` — reading from the server-persisted authoritative base row (lines 599-609), so the estorno marker rides through WR-01-safely. This insert now relies on the live `transactions.kind` CHECK accepting `'credit'` (Plan 13-02 / depends_on). In `src/lib/schemas/import.ts`: ONLY if the confirm payload actually carries `kind` (verify against the current `confirmImportRowSchema` and how the client builds the payload), add an optional `kind: z.enum(['expense', 'credit']).optional()` mirroring the `reservaId`/`carroId` optional grammar — but since the server re-reads `kind` from `r.base` (WR-01), this is belt-and-suspenders; if the payload does NOT carry kind, do NOT add the field (avoid dead schema surface) and note that in the SUMMARY.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "r.base.kind ?? 'expense'" src/actions/import.ts</automated>
  </verify>
  <acceptance_criteria>
    - The transactions insert reads `kind: r.base.kind ?? 'expense'` (grep matches)
    - No `kind` is read from the client confirm payload (WR-01 preserved — server-derived only)
    - `npx tsc --noEmit` passes
    - If a schema field was added, it is optional and mirrors the existing optional grammar; if not added, the SUMMARY explains the payload does not carry kind
  </acceptance_criteria>
  <done>confirmImport persists the server-derived kind ('credit' for estornos); WR-01 intact.</done>
</task>

<task type="auto">
  <name>Task 3: Pin Node runtime + maxDuration on /importar; update helper copy (D-08)</name>
  <files>src/app/(app)/importar/page.tsx</files>
  <read_first>
    - src/app/(app)/importar/page.tsx (the route segment; the helper copy at lines ~18-20)
    - .planning/phases/13-pdf-de-fatura/13-RESEARCH.md §"D-08 Resolution" (the exact exports `runtime = 'nodejs'` + `maxDuration = 30`; the 24–182 ms evidence; why a Route Handler is NOT introduced) + §"Runtime State Inventory"
    - .planning/phases/13-pdf-de-fatura/13-PATTERNS.md §"src/app/(app)/importar/page.tsx"
    - .planning/phases/13-pdf-de-fatura/13-UI-SPEC.md Copywriting Contract ("Importar page subhelper" copy including PDF)
  </read_first>
  <action>
    At the top of `src/app/(app)/importar/page.tsx` add the route-segment exports `export const runtime = 'nodejs'` (pdf-parse needs Node APIs — never Edge) and `export const maxDuration = 30` (generous margin; measured parse <0.2s), satisfying the Node-runtime + maxDuration letter of the CLAUDE.md-locked guidance WITHOUT a Route Handler (D-08). Update the helper/subhelper copy (lines ~18-20) to include PDF per the UI-SPEC Copywriting Contract ("Suba o OFX, CSV ou PDF do seu banco. Vamos extrair, deduplicar e pré-classificar pela memória."). Do NOT introduce a `route.ts` (the spike disproved the performance premise; the runtime-pinned server action is the recommended path — record the 24–182 ms evidence in the SUMMARY).
  </action>
  <verify>
    <automated>grep -q "runtime = 'nodejs'" "src/app/(app)/importar/page.tsx" && grep -q "maxDuration" "src/app/(app)/importar/page.tsx" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `/importar/page.tsx` exports `runtime = 'nodejs'` and `maxDuration`
    - The subhelper copy names PDF alongside OFX/CSV
    - No new `route.ts` was created (the server action stays the ingest path)
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Node runtime + maxDuration pinned on /importar; helper copy includes PDF; no Route Handler added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → ingestStatement (path) | untrusted file path / ext; validated by `extSchema` + `path.startsWith(userId/)` (existing) |
| downloaded PDF buffer → extractPdfText | untrusted bytes parsed server-side on Node runtime |
| client → confirmImport | untrusted category/reserva choice only; `kind`/descriptor/amount are server-derived (WR-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06 | Info disclosure | extractPdfText throwing escaping the 'use server' boundary as a 500 | mitigate | the PDF dispatch lives INSIDE the existing `try/catch` wrapper → friendly `{ error }` (mirror the OFX/CSV wrapper) |
| T-13-07 | Tampering | learning-poisoning via forged estorno kind/descriptor | mitigate | UNCHANGED WR-01: confirmImport re-reads server-persisted `parsed_rows`; `kind` read from `r.base.kind`, descriptor_norm server-derived; client supplies only category/reserva |
| T-13-08 | Info disclosure | cross-user PDF/statement access (IDOR on statementId/category_id/reserva_id) | mitigate | existing `getClaims()` + `path.startsWith(userId/)` + per-FK ownership re-derive (lines 522-570) reused verbatim; PDF adds no new FK surface |
| T-13-09 | Denial of Service | huge/hostile PDF on the Node runtime | mitigate | `runtime='nodejs'` + `maxDuration=30` bounds parse time; `MAX_PARSED_ROWS` cap bounds memory + jsonb size (13-01) |
| T-13-10 | Spoofing/AuthZ | parse runs off-Node (Edge) where pdf-parse breaks | mitigate | `export const runtime = 'nodejs'` forces Node; Edge is forbidden, not a fallback |
| T-13-SC | Tampering | no external LLM call (IA deferred) | accept | no PII egress — descriptor stays local; the memory-only seam (suggestCategory→null) is unchanged |
</threat_model>

<verification>
- `npx tsc --noEmit` passes; `npx vitest run` (full) GREEN; `npm run build` succeeds
- `'pdf'` in extSchema; 3-way ext detection; PDF dispatch with image-only block inside the try/catch wrapper
- `confirmImport` inserts `kind: r.base.kind ?? 'expense'`
- `/importar` pins `runtime='nodejs'` + `maxDuration`; no `route.ts` added
</verification>

<success_criteria>
- A .pdf uploads, persists in Storage by user_id, and routes to the PDF parser (PDF-01/PDF-02)
- An image-only PDF returns a clear CSV/OFX-steering error; a text-present 0-row PDF shows the review screen — never a silent empty result (PDF-04)
- After confirm, PDF rows (incl. estornos as kind:'credit') enter the same memory pipeline and count in metas (PDF-05)
</success_criteria>

<output>
Create `.planning/phases/13-pdf-de-fatura/13-03-SUMMARY.md` when done. Record the extSchema/ext/dispatch changes, the kind threading, the runtime-pin decision + the 24–182 ms evidence, and whether the confirm schema gained a kind field.
</output>
