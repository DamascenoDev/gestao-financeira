---
phase: 10-abastecimento-h-brido-consumo
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - src/components/abastecimento-form.tsx
  - src/components/transacao-picker.tsx
  - src/components/abastecimento-history.tsx
  - src/app/(app)/carros/[id]/page.tsx
autonomous: false
requirements: [CAR-03, CAR-04]
must_haves:
  truths:
    - "From /carros/[id] the user opens a 'Novo abastecimento' dialog, picks a cost source (Da fatura | Manual), and saves a fuel-up"
    - "The 'Da fatura' toggle shows a searchable picker of the user's recent UNLINKED expenses (by descrição/valor); 'Manual' shows a MoneyInput"
    - "tanque-cheio defaults ON; combustível defaults to the carro's combustivel_padrao when present"
    - "The carro detail page lists the abastecimento history (date, odômetro, litros, custo, tanque-cheio badge, km/l do intervalo) reusing the Phase-7 table→card mobile pattern"
    - "km/l médio and R$/km averages render as numbers (mono tabular-nums); an invalid/null interval shows '—'"
  artifacts:
    - path: "src/components/abastecimento-form.tsx"
      provides: "Novo/editar abastecimento dialog with segmented cost-source toggle"
      min_lines: 80
    - path: "src/components/abastecimento-history.tsx"
      provides: "Abastecimento history table→card list + averages display"
      min_lines: 60
    - path: "src/components/transacao-picker.tsx"
      provides: "Searchable picker of the user's recent unlinked expenses"
      min_lines: 40
  key_links:
    - from: "src/components/abastecimento-form.tsx"
      to: "createAbastecimento / updateAbastecimento"
      via: "action call on submit"
      pattern: "createAbastecimento"
    - from: "src/app/(app)/carros/[id]/page.tsx"
      to: "v_abastecimento_consumo + v_carro_resumo"
      via: "RLS-scoped reads feeding the history + averages"
      pattern: "v_abastecimento_consumo"
---

<objective>
Ship the user-visible vertical slice (CAR-03 + CAR-04): from `/carros/[id]` the user records an abastecimento via a dialog with a segmented cost-source toggle, and the same page lists the abastecimento history plus the km/l-médio and R$/km averages as numbers. After this plan, the phase user story is true end to end — register a fuel-up (cost from a fatura lançamento OR manual) and see consumption.

Purpose: This is the thinnest UI over the proven server contract (10-02) and corrected views (10-01). The rich consumption CHART + full detail layout are DEFERRED to Phase 11 — this plan adds only the abastecimento section + average numbers to the existing minimal detail page.

Output: `abastecimento-form.tsx`, `transacao-picker.tsx`, `abastecimento-history.tsx`, and the `/carros/[id]` page additions, plus a human-verify checkpoint for look/flow in light + dark.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-abastecimento-h-brido-consumo/10-CONTEXT.md
@.planning/phases/10-abastecimento-h-brido-consumo/10-02-SUMMARY.md
@src/components/reserva-form.tsx
@src/components/carro-form.tsx
@src/components/carro-picker.tsx
@src/components/money-input.tsx
@src/components/nf-table.tsx
@src/lib/carro/consumo.ts
@src/lib/money.ts
@src/lib/month.ts
@src/app/(app)/carros/[id]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: AbastecimentoForm dialog (segmented cost-source toggle) + TransacaoPicker (searchable unlinked-expense picker)</name>
  <files>src/components/abastecimento-form.tsx, src/components/transacao-picker.tsx</files>
  <read_first>
    - src/components/carro-form.tsx (the exact dialog skeleton to clone: controlled/uncontrolled open, re-seed on open without useEffect, useTransition + sonner, client safeParse mirroring the schema, the Select for combustivel)
    - src/components/reserva-form.tsx (the trigger/controlled-open pattern + Field/FieldError shape)
    - src/components/carro-picker.tsx (the entity-picker analog: Field + Select + onChange decoding; TransacaoPicker mirrors its shape but renders a searchable list of expenses)
    - src/components/money-input.tsx (MoneyInput + isValidMoney for the Manual cost path)
    - src/lib/schemas/abastecimento.ts (the schema the form mirrors client-side)
    - src/lib/month.ts (todaySP for the occurred_on default)
  </read_first>
  <action>
    Create `src/components/abastecimento-form.tsx` — a create/edit dialog cloning carro-form's manual-state + useTransition + sonner pattern, launched with a "Novo abastecimento" trigger (and a controlled mode for edit). Fields: data (default todaySP()), odômetro (numeric input, int>0), litros (numeric/decimal input >0 — a VOLUME, not money, so NO MoneyInput here), tanque-cheio (Switch, default ON), combustível (Select with the Flex/Gasolina/Etanol/Diesel/GNV enum, default = the carro's combustivel_padrao passed in as a prop when present). A segmented cost-source toggle (two buttons / a ToggleGroup labelled "Da fatura" / "Manual"): "Da fatura" renders the TransacaoPicker (transactionId source); "Manual" renders MoneyInput (amountCents source). Switching the toggle clears the other source's value so the submitted input always carries exactly one (the schema XOR enforces it; the UI should never submit both). On submit: build the AbastecimentoInput, client safeParse with abastecimentoSchema (mirror carro-form), then call createAbastecimento / updateAbastecimento; on { error } toast.error, on ok toast.success + close. Pass carroId + combustivelPadrao + the unlinked-expense list as props from the page.

    Create `src/components/transacao-picker.tsx` — a searchable picker of the user's recent UNLINKED expenses (server-fetched list passed as a prop: id, descrição, occurred_on, amount_cents). Mirror carro-picker's Field shape; render a searchable list (filter client-side by descrição substring and by formatted valor) and emit the selected transactionId. Show a muted hint when the list is empty ("Nenhum lançamento disponível para vincular."). Format valor with formatCents at the display edge only.
  </action>
  <verify>
    <automated>cd /home/dmsc/Dev/Projetos/Pessoal/gestao-financeira && npx tsc --noEmit && grep -q createAbastecimento src/components/abastecimento-form.tsx && grep -q tanque src/components/abastecimento-form.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The dialog opens from a "Novo abastecimento" trigger; data defaults to todaySP(); tanque-cheio defaults ON; combustível defaults to the carro's combustivel_padrao when present.
    - The segmented toggle switches between TransacaoPicker ("Da fatura") and MoneyInput ("Manual"); switching clears the other source so submit carries exactly one.
    - litros uses a plain decimal input (never MoneyInput) — it is a volume, not money.
    - TransacaoPicker filters the unlinked-expense list by descrição and valor and emits a transactionId; empty list shows the muted hint.
    - On { error } a sonner toast shows; on ok the dialog closes.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>AbastecimentoForm + TransacaoPicker compile and wire to createAbastecimento/updateAbastecimento with the segmented cost-source toggle, tanque-cheio default ON, combustível default from the carro, and searchable unlinked-expense picking.</done>
</task>

<task type="auto">
  <name>Task 2: AbastecimentoHistory (table→card) + averages, wired into /carros/[id]</name>
  <files>src/components/abastecimento-history.tsx, src/app/(app)/carros/[id]/page.tsx</files>
  <read_first>
    - src/components/nf-table.tsx (the Phase-7 table→card mobile pattern + AmountCell + dd/MM date formatter + tabular-nums money cells to clone for the history list)
    - src/app/(app)/carros/[id]/page.tsx (the minimal RSC detail page this plan extends — keep its identity header + definition list, ADD the abastecimento section below)
    - src/lib/carro/consumo.ts (the presentation helpers: km/l + R$/km labels + the '—' fallback for null intervals)
    - src/lib/money.ts (formatCents/centsToBigInt for custo display)
  </read_first>
  <action>
    Create `src/components/abastecimento-history.tsx` — a component rendering the abastecimento history as a dense table on desktop and a card stack on mobile (clone nf-table's table→card structure + tabular-nums). Columns: data (dd/MM), odômetro (km, tabular-nums), litros (tabular-nums), custo (formatCents — from the linked transaction's amount_cents OR the manual amount_cents, whichever the row carries), tanque-cheio badge, and km/l do intervalo (from v_abastecimento_consumo joined by abastecimento id; render '—' when the interval is invalid/null via the consumo helper). Each row offers edit/delete affordances calling updateAbastecimento/deleteAbastecimento (mirror nf-table's dropdown + AlertDialog delete-confirm). Above the list, render the averages block: km/l médio and R$/km from v_carro_resumo as numbers in mono tabular-nums, '—' when null.

    Extend `src/app/(app)/carros/[id]/page.tsx` (RSC): below the existing identity card, add an "Abastecimentos" section. Fetch RLS-scoped: the carro's abastecimentos (with the linked transaction's amount_cents via a join or a second select), the carro's v_abastecimento_consumo rows (for the per-interval km/l keyed by abastecimento id), the carro's v_carro_resumo row (for the averages), and the user's recent UNLINKED expenses (transactions with no abastecimento link — to feed the form's TransacaoPicker). Render the "Novo abastecimento" trigger (AbastecimentoForm with carroId + combustivelPadrao + unlinked list as props) and the AbastecimentoHistory (rows + consumo map + averages). Do NOT add a chart or rich KPI layout — those are Phase 11. Keep the page free of any new dependency.
  </action>
  <verify>
    <automated>cd /home/dmsc/Dev/Projetos/Pessoal/gestao-financeira && npx tsc --noEmit && npm run build && grep -q v_abastecimento_consumo "src/app/(app)/carros/[id]/page.tsx" && grep -q AbastecimentoHistory "src/app/(app)/carros/[id]/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - The carro detail page shows an "Abastecimentos" section below the identity card with the "Novo abastecimento" trigger.
    - The history renders date/odômetro/litros/custo/tanque-cheio badge/km-l-do-intervalo, with '—' for invalid/null intervals; reuses the Phase-7 table→card mobile structure.
    - km/l médio + R$/km averages render as numbers in mono tabular-nums, '—' when null, read from v_carro_resumo.
    - The TransacaoPicker is fed only the user's UNLINKED expenses.
    - No chart / rich KPI layout added (deferred to Phase 11).
    - `npx tsc --noEmit` clean and `npm run build` exits 0.
  </acceptance_criteria>
  <done>The /carros/[id] page lists the abastecimento history (table→card) + km/l-médio and R$/km averages as numbers, fed by RLS-scoped reads of the carro's abastecimentos + v_abastecimento_consumo + v_carro_resumo; build + tsc clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The abastecimento registration slice on /carros/[id]: the "Novo abastecimento" dialog (segmented Da fatura | Manual cost-source toggle, tanque-cheio default ON, combustível default from the carro, searchable unlinked-expense picker), the abastecimento history (table on desktop, cards on mobile), and the km/l-médio + R$/km averages.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open a carro detail page at `/carros/[id]` (one of your carros).
    2. Click "Novo abastecimento". Confirm: data defaults to today, tanque-cheio is ON, combustível pre-selects the carro's combustivel_padrao if it has one.
    3. Toggle "Manual" → enter a value in the MoneyInput → save. Confirm a success toast and the row appears in the history with the custo formatted as R$.
    4. Toggle "Da fatura" → search the picker by part of a lançamento's descrição or valor → pick one → save. Confirm the row appears AND that lançamento now shows the carro tag on the extrato (carro_id was set).
    5. Add at least two tanque-cheio fills with increasing odometers; confirm the km/l do intervalo column shows a number on the closing interval and '—' on the first/invalid one, and that km/l médio + R$/km show as numbers (mono).
    6. Resize to mobile width: confirm the history collapses to the card layout.
    7. Flip light↔dark: confirm both render cleanly (identity navy+gold, tabular-nums alignment).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (look, flow, toggle behavior, average numbers, light/dark).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client form → server actions | The form is pure wiring; all cost-source XOR + dual ownership re-derive happen server-side (10-02). The UI must never submit both cost sources. |
| RSC page reads → consumption views | The page reads v_abastecimento_consumo / v_carro_resumo under the RLS-active server client (security_invoker scopes to the caller). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-08 | Tampering | cost-source toggle (client) | mitigate | The toggle clears the inactive source on switch so submit carries exactly one; the server XOR (10-02) + DB CHECK (10-01) are the authoritative guards — the UI cannot bypass them. |
| T-10-09 | Information Disclosure | TransacaoPicker unlinked-expense list | accept | The list is server-fetched under RLS (only the caller's own transactions) and filtered to unlinked expenses; no foreign rows are reachable. Low risk, no PII beyond the user's own descriptors. |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | No new package-manager installs (uses existing shadcn/ui Switch/ToggleGroup/Table, sonner, lucide). N/A — no [ASSUMED]/[SUS] package introduced. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm run build` exits 0 (compiles /carros/[id] with the abastecimento section).
- Full suite `npm test` ≥ 664 + Plan-01/02 tests green; `check-bundle-secrets` exit 0 (no service-role key in client bundle).
- Human-verify checkpoint approved: form flow, cost-source toggle, carro_id-on-link visible in extrato, km/l + R$/km numbers, table→card mobile, light/dark.
- grep confirms the page reads v_abastecimento_consumo + renders AbastecimentoHistory.
</verification>

<success_criteria>
- The user registers an abastecimento from /carros/[id] choosing Da fatura (searchable unlinked-expense picker) OR Manual (MoneyInput), with tanque-cheio default ON and combustível defaulting to the carro's combustivel_padrao.
- Linking from a fatura sets carro_id on that lançamento (visible in the extrato).
- The history lists date/odômetro/litros/custo/tanque-cheio/km-l-do-intervalo (table→card mobile), '—' for invalid intervals.
- km/l médio + R$/km show as numbers in mono tabular-nums, '—' when null.
- No chart / rich KPI layout (deferred to Phase 11).
- tsc + build clean; human-verify approved in light + dark.
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 03)
- `src/components/abastecimento-form.tsx` — Novo/editar abastecimento dialog with the segmented cost-source toggle.
- `src/components/transacao-picker.tsx` — searchable picker of the user's recent unlinked expenses.
- `src/components/abastecimento-history.tsx` — abastecimento history (table→card) + km/l-médio + R$/km averages.
- `src/app/(app)/carros/[id]/page.tsx` — extended with the Abastecimentos section (form trigger + history + averages), RLS-scoped reads of v_abastecimento_consumo + v_carro_resumo.
</artifacts>

<output>
Create `.planning/phases/10-abastecimento-h-brido-consumo/10-03-SUMMARY.md` when done.
</output>
