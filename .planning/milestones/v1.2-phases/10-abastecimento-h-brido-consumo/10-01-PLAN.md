---
phase: 10-abastecimento-h-brido-consumo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0028_carros_fix.sql
  - src/types/database.types.ts
  - tests/carro-consumo.test.ts
autonomous: true
requirements: [CAR-04]
must_haves:
  truths:
    - "v_abastecimento_consumo returns null km/l and null R$/km for an interval whose km_rodados <= 0 (negative or zero odometer delta)"
    - "An interval with km_rodados <= 0 is EXCLUDED from the km/l and R$/km averages in v_carro_resumo"
    - "Two tanque_cheio fills with the same odometro_km produce a deterministic interval (tie-broken by occurred_on then created_at), not a random lag"
    - "carros rejects an ano outside 1900..2100 and a combustivel_padrao not in (Flex,Gasolina,Etanol,Diesel,GNV); null ano and null combustivel_padrao remain valid"
    - "Both views keep security_invoker = true (a second user reads zero of the first user's consumption rows)"
    - "preco_litro is never a stored column; the view derives custo / litros only"
    - "Migration 0028 applies via supabase migration up and gen:types regenerates with no schema drift"
  artifacts:
    - path: "supabase/migrations/0028_carros_fix.sql"
      provides: "Fix-only migration: WR-05/06 view guards + tie-break, WR-01 carros CHECK constraints"
      contains: "create or replace view public.v_abastecimento_consumo"
    - path: "tests/carro-consumo.test.ts"
      provides: "Consumption-view integration test against the local stack"
      contains: "km_rodados"
  key_links:
    - from: "v_abastecimento_consumo"
      to: "v_carro_resumo"
      via: "averages built off the view (one interval-logic source)"
      pattern: "from public.v_abastecimento_consumo"
    - from: "src/types/database.types.ts"
      to: "supabase migration up + gen:types"
      via: "regenerated typed client, no drift"
      pattern: "v_abastecimento_consumo"
---

<objective>
Ship migration `0028_carros_fix.sql` — the deferred Phase-8 review fixes (WR-05, WR-06, WR-01) — applied to the LOCAL stack, and prove the corrected consumption math with a Wave-0 integration test. This is the BLOCKING substrate for the rest of Phase 10: the server layer (10-02) and UI (10-03) consume the regenerated types and the now-trustworthy view rows.

Purpose: The Phase-8 views silently produced wrong consumption rows for bad odometer data (negative km_rodados flowing into averages; non-deterministic ties) and the `carros` table accepted `ano`/`combustivel_padrao` values the app layer rejects. Phase 10 surfaces these views in the UI, so the math must be correct and the averages must exclude invalid intervals BEFORE any number is shown. (CAR-04; closes WR-05/06/01.)

Output: `supabase/migrations/0028_carros_fix.sql`, regenerated `src/types/database.types.ts` (no drift), and `tests/carro-consumo.test.ts` (Wave-0 consumption-view proof).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-abastecimento-h-brido-consumo/10-CONTEXT.md
@.planning/phases/10-abastecimento-h-brido-consumo/10-VALIDATION.md
@.planning/phases/08-substrato-carro-crud-navega-o/08-REVIEW.md
@supabase/migrations/0027_carros.sql
@tests/carro-view-leak.test.ts
@tests/carro-rls.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 0028_carros_fix.sql (WR-05/06 view guards + tie-break, WR-01 carros CHECKs), apply LOCAL, regenerate types</name>
  <read_first>
    - supabase/migrations/0027_carros.sql (the abastecimentos table, the full_fills/fills/intervals CTE chain of v_abastecimento_consumo, and v_carro_resumo — the file this migration rewrites the two views from and adds CHECKs against)
    - .planning/phases/08-substrato-carro-crud-navega-o/08-REVIEW.md (WR-01 exact CHECK SQL, WR-05 tie-break SQL, WR-06 non-positive km_rodados guard)
    - supabase/migrations/0026_mei_views.sql (analog: a versioned create-or-replace-view migration with security_invoker in this repo)
  </read_first>
  <action>
    Create `supabase/migrations/0028_carros_fix.sql` as a FIX-ONLY, idempotent migration that does NOT touch tables/columns/RLS/grants/indexes from 0027 — only create-or-replace-view for the two views and alter-table add-constraint guards for carros.

    WR-01 CHECK constraints on public.carros — use idempotent guards (drop constraint if exists, then add), and confirm existing rows satisfy them:
    - carros_ano_chk: ano is null or (ano between 1900 and 2100) — a FIXED constant upper bound 2100, NOT extract(year from now()), so the constraint never drifts at year rollover (08-REVIEW WR-01 note).
    - carros_combustivel_padrao_chk: combustivel_padrao is null or combustivel_padrao in ('Flex','Gasolina','Etanol','Diesel','GNV').

    WR-05 + WR-06 rewrite of public.v_abastecimento_consumo via create-or-replace-view with (security_invoker = true) (KEEP security_invoker — a DEFINER view leaks every user's rows, T-08-02):
    - WR-05 deterministic tie-break: in the full_fills CTE, change the lag window from "order by a.odometro_km" to "order by a.odometro_km, a.occurred_on, a.created_at, a.id" so two tanque_cheio fills with equal odometro_km resolve deterministically. Apply the same composite ordering anywhere fill order matters.
    - WR-06 non-positive km_rodados guard: change the km_por_litro and reais_por_km CASE branches from guarding only "= 0" to guarding "<= 0" on km_rodados (keep the existing litros_intervalo null/zero guard on km_por_litro). Additionally add "and (f.odometro_km - f.prev_full_odometro) > 0" to the intervals CTE WHERE clause so a negative/zero-delta interval never reaches v_carro_resumo's averages.
    - Keep preco_litro DERIVED inside the view (custo ÷ litros) — never add a stored column.

    Keep v_carro_resumo averages built OFF v_abastecimento_consumo (one interval-logic source). Read 0027's v_carro_resumo: its avg(...) filter (where ... is not null) already excludes the now-null bad intervals, so re-issue it with create-or-replace-view + security_invoker only if any edit is needed; otherwise leave 0027's definition in place (do not duplicate unchanged SQL).

    Apply to the LOCAL stack ONLY: supabase migration up (NOT db push remote, NOT db reset). Then npm run gen:types. Confirm git diff on src/types/database.types.ts is empty (view COLUMNS are unchanged; only row VALUES change) — no unexpected drift.
  </action>
  <verify>
    <automated>cd /home/dmsc/Dev/Projetos/Pessoal/gestao-financeira && supabase migration up && npm run gen:types && git diff --quiet src/types/database.types.ts && grep -q carros_ano_chk supabase/migrations/0028_carros_fix.sql && grep -q carros_combustivel_padrao_chk supabase/migrations/0028_carros_fix.sql && test "$(grep -c 'security_invoker = true' supabase/migrations/0028_carros_fix.sql)" -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - `supabase migration up` applies 0028 to the LOCAL stack with no error; `db push`/`db reset` are NOT used.
    - `npm run gen:types` leaves `src/types/database.types.ts` byte-identical (`git diff --quiet` exits 0) — no schema drift.
    - 0028 contains both `carros_ano_chk` and `carros_combustivel_padrao_chk`; the ano upper bound is the literal `2100`, never `now()`/`extract(year from now())`.
    - Inserting a carro with `ano = 1700` or `combustivel_padrao = 'Querosene'` (via the service client) is rejected by the new CHECKs; `ano = null` and `combustivel_padrao = null` still insert.
    - `v_abastecimento_consumo` still carries `with (security_invoker = true)`.
  </acceptance_criteria>
  <done>Migration 0028 applied locally, types regenerated with no drift, both carros CHECKs present, the consumption view rewritten with security_invoker intact and the <=0 / tie-break guards in place.</done>
</task>

<task type="auto">
  <name>Task 2: Wave-0 consumption-view integration test (full-tank km/l, negative/zero km guard, R$/km, derived preco_litro, average exclusion)</name>
  <read_first>
    - tests/carro-view-leak.test.ts (the closest analog: it already seeds a carro + two tanque_cheio abastecimentos forming ONE interval and reads v_abastecimento_consumo/v_carro_resumo over the local stack — clone its createUser/userClient/serviceClient setup)
    - tests/carro-rls.test.ts (abastecimento insert shape: user_id, carro_id, occurred_on, odometro_km, litros, tanque_cheio, combustivel, amount_cents vs transaction_id)
    - tests/helpers/local-supabase.ts (readLocalConfig/serviceClient/userClient helpers)
  </read_first>
  <action>
    Create `tests/carro-consumo.test.ts` — a Vitest integration test against the local Supabase stack (clone the createUser/beforeAll/afterAll harness from carro-view-leak.test.ts). Seed ONE user A with one carro, then exercise the corrected v_abastecimento_consumo + v_carro_resumo math:

    Happy-path full-tank interval: insert two tanque_cheio=true fills with a positive odometer delta and known litros (e.g. odometro 10000 then 10500, litros 40 on the closing fill) so km_rodados = 500 and km_por_litro = 500/40 = 12.5; assert v_abastecimento_consumo returns that interval row with km_rodados = 500 and km_por_litro toBeCloseTo 12.5, and reais_por_km = custo_intervalo_cents / 500.

    Negative/zero km guard (WR-06): insert a third tanque_cheio fill whose odometro_km is LOWER than the prior full-tank odometer (e.g. 9000 — a mistyped/rolled-back reading); assert the resulting interval either does not appear OR appears with km_por_litro = null AND reais_por_km = null (matching the implemented guard — assert null, not a negative number). Then assert v_carro_resumo.km_por_litro_medio for the carro EQUALS the single valid interval's value (toBeCloseTo 12.5) — i.e. the bad interval is EXCLUDED from the average, not averaged in.

    preco_litro derived: assert there is no preco_litro column stored on abastecimentos (select preco_litro from abastecimentos must error / column absent) — the value is only ever derived in the view/presentation. Optionally assert v_carro_resumo.preco_litro_medio_cents is the Σcusto/Σlitros over valid intervals.

    Use the RLS-active userClient for view reads (security_invoker scopes to user A). Numeric assertions tolerate float formatting (toBeCloseTo for km/l).
  </action>
  <verify>
    <automated>cd /home/dmsc/Dev/Projetos/Pessoal/gestao-financeira && npm test -- carro-consumo.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - A full-tank interval with positive delta yields the expected km_rodados (500) and km_por_litro (toBeCloseTo 12.5) and a non-null reais_por_km.
    - A fill with an odometer LOWER than the previous full tank produces km_por_litro = null and reais_por_km = null (negative km guard), NOT a negative number.
    - v_carro_resumo.km_por_litro_medio equals the single valid interval's value — the invalid interval is excluded from the average.
    - The view reads run under the RLS-active user client.
    - Test is green; full suite baseline (~664) is not regressed.
  </acceptance_criteria>
  <done>tests/carro-consumo.test.ts proves full-tank km/l, the negative/zero-km null guard, average exclusion of bad intervals, R$/km, and that preco_litro is never stored — all green against the local stack.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → consumption views | Each user must see ONLY their own carros' consumption rows; the views run as the caller (security_invoker), not the definer |
| any write path → carros | A direct insert/update (bypassing the action layer) must not be able to persist out-of-range ano or off-enum combustivel_padrao |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | v_abastecimento_consumo / v_carro_resumo | mitigate | Keep `with (security_invoker = true)` on both views in 0028; carro-view-leak.test.ts (Phase 8) + the RLS-active reads in carro-consumo.test.ts prove a second user reads zero foreign rows. A DEFINER view would leak every user's consumption. |
| T-10-02 | Tampering | carros.ano / carros.combustivel_padrao | mitigate | DB CHECK constraints (carros_ano_chk 1900..2100, carros_combustivel_padrao_chk enum) so no write path — direct insert, future action, or test — can persist values the app assumes impossible (WR-01). |
| T-10-03 | Tampering | consumption math (km_rodados) | mitigate | Guard km_rodados <= 0 → null and filter it out of the intervals CTE so a rolled-back/mistyped odometer never produces a negative km/l or R$/km, and never corrupts the v_carro_resumo averages (WR-05/06). Proven by carro-consumo.test.ts. |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | No new package-manager installs in this plan (migration + test only, on existing deps). N/A — no [ASSUMED]/[SUS] package introduced. |
</threat_model>

<verification>
- `supabase migration up` applies 0028 LOCAL; `npm run gen:types` no drift (`git diff --quiet src/types/database.types.ts`).
- `npm test -- carro-consumo.test.ts` green (full-tank km/l, negative-km null guard + average exclusion, R$/km, preco_litro never stored).
- Full suite `npm test` ≥ 664 green; `npx tsc --noEmit` clean; `npm run build` exit 0.
- `grep` confirms both carros CHECKs and security_invoker present in 0028.
</verification>

<success_criteria>
- Migration 0028 applied locally, no remote push, no db reset, types regenerated with zero drift.
- Both views keep security_invoker = true.
- carros rejects out-of-range ano and off-enum combustivel_padrao; nulls still valid.
- Consumption view returns null km/l + R$/km for km_rodados <= 0 and excludes those intervals from averages; tie-broken deterministically.
- preco_litro is derived, never stored.
- Wave-0 consumption-view integration test green.
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 01)
- `supabase/migrations/0028_carros_fix.sql` — fix-only migration: WR-01 carros CHECK constraints + WR-05/06 v_abastecimento_consumo rewrite (tie-break + non-positive km guard), security_invoker preserved.
- `src/types/database.types.ts` — regenerated, no drift (consumes the migration).
- `tests/carro-consumo.test.ts` — Wave-0 consumption-view integration test.
</artifacts>

<output>
Create `.planning/phases/10-abastecimento-h-brido-consumo/10-01-SUMMARY.md` when done.
</output>
