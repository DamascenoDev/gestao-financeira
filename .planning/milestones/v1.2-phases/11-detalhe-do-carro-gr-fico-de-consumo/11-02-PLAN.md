---
phase: 11-detalhe-do-carro-gr-fico-de-consumo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/carro-card.tsx
  - src/app/(app)/carros/page.tsx
autonomous: true
requirements: [CAR-05]
must_haves:
  truths:
    - "Each CarroCard on /carros shows gasto total and km/l médio from v_carro_resumo"
    - "A car with no spend shows '—' for gasto total; a car with no consumption interval shows '—' for km/l médio — never R$ 0,00, never 0 km/l"
    - "Card identity (apelido link, modelo·placa·ano, badges, Editar/Arquivar) is unchanged"
  artifacts:
    - path: "src/components/carro-card.tsx"
      provides: "CarroCardData extended with gastoTotalCents + kmPorLitroMedio; additive KPI strip (mono tabular-nums, '—' null)"
      contains: "gastoTotalCents"
    - path: "src/app/(app)/carros/page.tsx"
      provides: "RSC reads v_carro_resumo KPIs and passes them per carro to CarroCard"
      contains: "v_carro_resumo"
  key_links:
    - from: "src/app/(app)/carros/page.tsx"
      to: "v_carro_resumo"
      via: "supabase.from('v_carro_resumo').select(...) RLS-scoped"
      pattern: "v_carro_resumo"
    - from: "src/components/carro-card.tsx"
      to: "src/lib/carro/consumo.ts"
      via: "kmPerLitroLabel for the km/l médio KPI"
      pattern: "kmPerLitroLabel"
    - from: "src/components/carro-card.tsx"
      to: "src/lib/money.ts"
      via: "formatCents for the gasto total KPI"
      pattern: "formatCents"
---

<objective>
Complete the deferred Phase-8 promise on the `/carros` list: extend the identity-only `CarroCard` with a two-up KPI strip (gasto total + km/l médio) read from the existing `v_carro_resumo` view, and wire the `/carros` RSC to fetch and pass those KPIs per carro. Additive only — card identity and actions are untouched; null KPIs render `—`.

Purpose: Deliver CAR-05.2's list half (gasto total + km/l médio per carro) using the view that already exists from Phase 8/10. File-disjoint from the detail-page slice (Plan 03), so it runs in parallel.
Output: `CarroCardData` gains `gastoTotalCents` + `kmPorLitroMedio`; the card renders the KPI strip; the list RSC joins `v_carro_resumo`. (CAR-05.2)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-detalhe-do-carro-gr-fico-de-consumo/11-CONTEXT.md
@.planning/phases/11-detalhe-do-carro-gr-fico-de-consumo/11-UI-SPEC.md
@src/components/carro-card.tsx
@src/app/(app)/carros/page.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend CarroCard with the additive KPI strip</name>
  <files>src/components/carro-card.tsx, tests/carro-card-kpis.test.tsx</files>
  <read_first>
    - src/components/carro-card.tsx — the FROZEN identity card to extend: `CarroCardData` type, `identityLine`, `toCarroEdit`, the `'use client'` card body, badges, DropdownMenu. ADD KPIs below the identity/badges block; do NOT touch identity/actions.
    - src/components/receita-gasto-chart.tsx — the labeled-KPI `dl` strip pattern (`flex flex-wrap gap-x-6 gap-y-1`, `text-muted-foreground` label over a `font-mono font-semibold tabular-nums` value) to mirror for the two-up strip.
    - src/lib/carro/consumo.ts — `kmPerLitroLabel(n: number | null)` → "12,4 km/l" or '—'.
    - src/lib/money.ts — `formatCents`; note money cents may arrive as number; the strip needs a null path that renders '—' (not formatCents(0)).
    - .planning/phases/11-detalhe-do-carro-gr-fico-de-consumo/11-UI-SPEC.md §Screen Contracts item 2 (the KPI strip spec + null discipline) + §Copywriting (labels "Gasto total", "km/l médio").
  </read_first>
  <behavior>
    - `CarroCardData` has new fields `gastoTotalCents: number | null` and `kmPorLitroMedio: number | null`.
    - A card with `gastoTotalCents` non-null renders `formatCents` for "Gasto total"; a card with `gastoTotalCents === null` renders `—` (never "R$ 0,00").
    - A card with `kmPorLitroMedio` non-null renders `kmPerLitroLabel`; a card with `kmPorLitroMedio === null` renders `—` (never "0 km/l").
    - The strip always renders (both labels present), showing `—` where data is absent — it is never hidden.
  </behavior>
  <action>
    Extend `CarroCardData` (in `carro-card.tsx`) with `gastoTotalCents: number | null` and `kmPorLitroMedio: number | null` (per UI-SPEC §Screen Contracts item 2). Keep `toCarroEdit` and identity/actions exactly as-is.

    Below the identity/badges block (still inside the `CardContent`, after the `<div className="flex items-start justify-between gap-2">…</div>`), add a `dl` KPI strip mirroring `ReceitaGastoChart`'s legend grammar: `<dl className="flex flex-wrap gap-x-6 gap-y-1">` with two items, each `<div className="flex flex-col gap-0.5">` containing a `<dt className="text-xs text-muted-foreground">` label and a `<dd className="font-mono text-sm font-semibold tabular-nums">` value:
    - "Gasto total" → `carro.gastoTotalCents === null ? '—' : formatCents(carro.gastoTotalCents)`.
    - "km/l médio" → `kmPerLitroLabel(carro.kmPorLitroMedio)` (the helper already returns '—' for null).
    The KPIs are additive, neutral `foreground` (no gold, not red). Null discipline: NEVER `formatCents(0)` as a stand-in for "no data" — use `—` (UI-SPEC §Money). Do not hide the strip; show `—` (per D4/CONTEXT null rule).

    Create `tests/carro-card-kpis.test.tsx` (@testing-library/react): (a) a card with `gastoTotalCents: 324000, kmPorLitroMedio: 12.4` renders the formatCents string and "12,4 km/l"; (b) a card with both null renders two `—` and asserts NO "R$ 0,00" and NO "0 km/l" text appears; (c) identity (apelido link href `/carros/{id}`) still present. Mock `@/actions/carros` if the existing card test pattern requires it (check whether a carro-card test already exists — reuse its mock setup).
  </action>
  <verify>
    <automated>npm test -- carro-card && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `carro-card-kpis.test.tsx` green: non-null KPIs format correctly, null KPIs render `—` with NO "R$ 0,00" / "0 km/l", identity link intact.
    - `CarroCardData` exports the two new fields; `toCarroEdit` and identity/actions unchanged (grep: identity link + DropdownMenu present).
    - KPI values are mono `tabular-nums`, neutral foreground; no gold/red on the KPIs.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>CarroCard renders the additive gasto-total + km/l-médio strip with the '—' sentinel for missing data, identity/actions untouched, fully unit-tested for the null discipline.</done>
</task>

<task type="auto">
  <name>Task 2: Wire /carros RSC to read v_carro_resumo KPIs per carro</name>
  <files>src/app/(app)/carros/page.tsx</files>
  <read_first>
    - src/app/(app)/carros/page.tsx — the FROZEN list RSC: reads carros RLS-scoped with the `?arquivados=1` filter, maps to `CarroCardData`, renders the grid/empty/error. EXTEND the mapping to attach KPIs; keep the archived filter + states.
    - src/types/database.types.ts — `v_carro_resumo` Row: `carro_id`, `gasto_total_cents`, `km_por_litro_medio`, `reais_por_km_medio` (all nullable). The view is `security_invoker=true` (Phase 8/10) — RLS already scopes it to the caller.
    - .planning/phases/11-detalhe-do-carro-gr-fico-de-consumo/11-UI-SPEC.md §Screen Contracts item 2.
  </read_first>
  <action>
    In `/carros/page.tsx`, after the existing carros query, add a second RLS-scoped read: `supabase.from('v_carro_resumo').select('carro_id, gasto_total_cents, km_por_litro_medio')` (no `.eq` filter needed — RLS scopes to the caller; the view returns one row per owned carro). Build a `Map<string, { gastoTotalCents: number | null; kmPorLitroMedio: number | null }>` keyed by `carro_id`.

    Important null discipline (D4/UI-SPEC): the view `coalesce`s `gasto_total_cents` to 0 for a carro with NO tagged spend (see 0027 `v_carro_resumo`). The UI must show `—` for "no data", NOT "R$ 0,00". So in the mapping, treat a `gasto_total_cents` of 0 as "no spend" → pass `gastoTotalCents: null` when the resumo value is 0 or missing; pass the actual cents only when > 0. `km_por_litro_medio` is genuinely null when there is no closed interval (the view uses `avg(...) filter`), so pass it through as-is (null stays null). Extend the `carros` map to set `gastoTotalCents` and `kmPorLitroMedio` from the resumo map (defaulting to null when a carro has no resumo row).

    Do NOT change the archived filter, the order, the empty/error/loading states, or the grid. The KPI read is additive. If the resumo read errors, fall back to null KPIs (the cards still render identity + `—`) — do not fail the whole page on a KPI read error.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `/carros` RSC reads `v_carro_resumo` RLS-scoped and passes `gastoTotalCents` + `kmPorLitroMedio` per carro to `CarroCard`.
    - A carro with `gasto_total_cents = 0` (no tagged spend) passes `gastoTotalCents: null` so the card shows `—`, never "R$ 0,00" (D4 null rule).
    - Archived filter, ordering, empty/error states unchanged; build compiles `/carros`.
    - The KPI read is RLS-scoped (no service-role client); no secret import.
  </acceptance_criteria>
  <done>The /carros list shows real gasto-total + km/l-médio KPIs per card from v_carro_resumo, with '—' for no-data, and the page still builds with its existing filter/states intact.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (this plan)

- `src/components/carro-card.tsx` — extended: `CarroCardData` gains `gastoTotalCents` + `kmPorLitroMedio`; additive two-up KPI strip (mono `tabular-nums`, `—` null sentinel), identity/actions unchanged.
- `src/app/(app)/carros/page.tsx` — extended: reads `v_carro_resumo` RLS-scoped, treats a 0 gasto as no-data (`—`), passes KPIs per carro to `CarroCard`.
- `tests/carro-card-kpis.test.tsx` — Wave-0 component test (non-null format, null `—` discipline, identity intact).
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Postgres (RSC) | The `/carros` RSC queries `v_carro_resumo` with the caller's RLS-scoped client; the view is `security_invoker=true` so it inherits the caller's RLS — no other user's gasto/consumo can surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Information Disclosure | `v_carro_resumo` read in `/carros` RSC | mitigate | View is already `security_invoker=true` (0027/0028, proven by tests/carro-view-leak.test.ts); the RSC uses the standard RLS-scoped `createClient()` server client, never `admin.ts`. No new view, no migration this plan. |
| T-11-04 | Tampering | `transactions.carro_id` total via the view | accept | Read-only presentation; the KPI read never writes and never reads into metas (D4 lente). `gasto_total_cents` is the existing non-accounting tag total. |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No new dependency this plan. Supply-chain surface unchanged. |
</threat_model>

<verification>
- `npm test -- carro-card` green.
- `npx tsc --noEmit` clean; `npm run build` compiles `/carros`.
- Full suite green at wave close: `npm test` (baseline ~720).
</verification>

<success_criteria>
- Every `CarroCard` shows gasto total + km/l médio from `v_carro_resumo`, with `—` for no-data (never R$ 0,00, never 0 km/l).
- Card identity + actions unchanged; KPI strip is additive and neutral.
- The KPI read is RLS-scoped via the existing `security_invoker` view; no new view/migration; no secret.
- `npx tsc --noEmit` clean; build compiles `/carros`; full suite green.
</success_criteria>

<output>
Create `.planning/phases/11-detalhe-do-carro-gr-fico-de-consumo/11-02-SUMMARY.md` when done.
</output>
