---
phase: 20-auto-classifica-o-por-palavra-chave-no-upload
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - src/components/origin-badge.tsx
  - src/components/import-review-table.tsx
  - src/app/(app)/importar/[statementId]/page.tsx
  - src/components/import-review-table.test.tsx
autonomous: true
requirements: [KW-02, KW-05]

must_haves:
  truths:
    - "A grid de revisão deriva origin='palavra-chave' do classification_source persistido (não mais hardcoda 'memória' para toda linha classificada) (KW-02)"
    - "O ProvenanceBadge (célula Categoria) renderiza o pill neutro 'palavra-chave' (lowercase, sem ícone) espelhando 'memória' (KW-05)"
    - "O OriginBadge (coluna Origem, desktop + mobile) renderiza a variante 'palavra-chave' (Title Case + ícone Tags, neutro) (KW-05)"
    - "Uma linha de keyword é sobrescrevível na grid pelo mesmo Select; trocar a categoria roda classifyRow → origin='manual', derrubando o pill (idêntico a sobrescrever memória) (KW-05)"
  prohibitions:
    - "O pill de keyword NUNCA usa o tratamento gold da IA (bg-primary) — é neutro como memória"
    - "O OriginBadge de keyword NUNCA reusa o ícone Brain (reservado para memória) — usa Tags, distinto"
    - "Uma linha de keyword (category_id set) NUNCA co-renderiza com 'não classificada' ou 'IA' (o gate category_id !== null já garante)"
    - "page.tsx NUNCA descarta o classification_source ao derivar origin"
  artifacts:
    - path: "src/components/origin-badge.tsx"
      provides: "OriginVariant += 'palavra-chave' + entrada no VARIANT map (Tags icon, neutro, Title Case)"
      contains: "palavra-chave"
    - path: "src/components/import-review-table.tsx"
      provides: "ReviewRow.origin += 'palavra-chave' + branch no ProvenanceBadge (lowercase, sem ícone)"
      contains: "palavra-chave"
    - path: "src/app/(app)/importar/[statementId]/page.tsx"
      provides: "origin derivado de r.classification_source (o fix load-bearing)"
      contains: "classification_source"
  key_links:
    - from: "src/app/(app)/importar/[statementId]/page.tsx"
      to: "src/components/import-review-table.tsx (ReviewRow.origin)"
      via: "origin derivado de r.classification_source === 'palavra-chave'"
      pattern: "classification_source === 'palavra-chave'"
    - from: "src/components/import-review-table.tsx (ProvenanceBadge + OriginBadge call sites)"
      to: "src/components/origin-badge.tsx (VARIANT map)"
      via: "origin==='palavra-chave' rota para o pill/variante neutra"
      pattern: "palavra-chave"
---

<objective>
Fazer o badge **palavra-chave** efetivamente RENDERIZAR na grid de revisão (KW-02/KW-05). A peça load-bearing é o fix em `page.tsx:204`: hoje a RSC re-deriva `origin` SÓ de `category_id` e DESCARTA o `classification_source` que o PASS 1 (Plan 20-01) persistiu — sem este fix o pill nunca aparece.

Purpose: A proveniência review-time precisa atravessar a fronteira RSC. Com o `classification_source='palavra-chave'` já persistido na parsed row (Plan 20-01), a UI só precisa lê-lo e mapear para os dois pills neutros existentes.
Output: `page.tsx` deriva origin do source; os dois unions de UI (`ReviewRow.origin`, `OriginVariant`) ganham 'palavra-chave'; o ProvenanceBadge ganha o branch neutro lowercase sem ícone; o OriginBadge ganha a variante neutra Title-Case com ícone `Tags`; o teste de componente cobre as duas superfícies.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-auto-classifica-o-por-palavra-chave-no-upload/20-CONTEXT.md
@.planning/phases/20-auto-classifica-o-por-palavra-chave-no-upload/20-RESEARCH.md
@.planning/phases/20-auto-classifica-o-por-palavra-chave-no-upload/20-UI-SPEC.md
@.planning/phases/20-auto-classifica-o-por-palavra-chave-no-upload/20-01-SUMMARY.md

# Ground edits in the actual source — read these first.
@src/components/origin-badge.tsx
</context>

<artifacts_this_phase_produces>
NEW symbols introduced by THIS plan (consome o `'palavra-chave'` ClassificationSource do Plan 20-01):
- `'palavra-chave'` membro do union `OriginVariant` (src/components/origin-badge.tsx) + sua entrada no `VARIANT` map (label 'Palavra-chave', `bg-muted text-muted-foreground`, ícone `Tags`)
- `'palavra-chave'` membro do union `ReviewRow.origin` (src/components/import-review-table.tsx) + o branch no `ProvenanceBadge` (pill `bg-secondary text-secondary-foreground`, lowercase, sem ícone)
- A derivação de `origin` a partir de `r.classification_source` em `page.tsx:204` (o fix load-bearing)

NENHUM arquivo novo. NENHUMA dependência nova (`Tags` já vem de `lucide-react`).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: page.tsx deriva origin de classification_source + os dois unions de UI</name>
  <files>src/app/(app)/importar/[statementId]/page.tsx, src/components/import-review-table.tsx, src/components/origin-badge.tsx</files>
  <read_first>
    - src/app/(app)/importar/[statementId]/page.tsx linhas 191-215 (o map parsedRows → ReviewRow; a linha 204 é o hardcode `origin: r.category_id === null ? 'não classificada' : 'memória'`)
    - src/components/import-review-table.tsx linhas 205-218 (ReviewRow.origin union ~218) e 524-535 + 814-815 (os dois call sites de OriginBadge)
    - src/components/origin-badge.tsx (OriginVariant union ~16 + VARIANT map ~18-42 — espelhe a entrada `memória`)
    - 20-RESEARCH.md Pattern 4 (o diff de page.tsx:204 + por que é seguro), Pattern 5 (os três unions + as duas branches), Pitfall 1 (o badge nunca renderiza sem este fix)
    - 20-UI-SPEC.md (a tabela ⚠️ Flag: tokens por-superfície, casing, ícone Tags)
  </read_first>
  <action>
    Edite `src/app/(app)/importar/[statementId]/page.tsx` na linha ~204 (dentro do `parsedRows.map`): em vez de hardcodar 'memória' para toda linha classificada, derive de `r.classification_source`:
    - `category_id === null` → 'não classificada' (inalterado).
    - senão `r.classification_source === 'palavra-chave'` → 'palavra-chave'.
    - senão → 'memória' (fallback correto: no ingest, uma parsed row classificada só tem source 'memória' ou 'palavra-chave' — manual/IA não se aplicam em parse-time; A2 verificado).

    Edite `src/components/import-review-table.tsx` — estenda o union `ReviewRow.origin` (~linha 218) para incluir `'palavra-chave'`: `'memória' | 'palavra-chave' | 'manual' | 'não classificada'`. NÃO mexa nos call sites de OriginBadge (524-535, 814-815) — eles já passam `r.origin` direto, então a nova variante flui sozinha assim que o VARIANT map a tiver.

    Edite `src/components/origin-badge.tsx` — estenda o union `OriginVariant` (~linha 16) para incluir `'palavra-chave'`. Importe `Tags` de `lucide-react` (junto de Brain/Pencil/Sparkles/TriangleAlert). Adicione a entrada no `VARIANT` map espelhando a entrada `memória`, porém com label Title-Case `'Palavra-chave'`, className neutro `'bg-muted text-muted-foreground'` (NÃO o gold `bg-primary`), e `Icon: Tags` (distinto de `Brain`, que fica reservado para memória — a11y: o ícone diferencia além do label).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx deriva origin lendo `r.classification_source` (não descarta mais o source).
    - `OriginVariant` e `ReviewRow.origin` incluem 'palavra-chave'; o VARIANT map tem a entrada neutra com `Tags`.
    - `npx tsc --noEmit` limpo (os unions batem com os call sites; sem `any`).
    - A entrada de keyword NÃO usa `bg-primary` (gold) nem o ícone `Brain`.
  </acceptance_criteria>
  <done>page.tsx surfacing wired ao source; os dois unions e o VARIANT map cobrem 'palavra-chave' com tratamento neutro e ícone Tags; tsc limpo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Branch 'palavra-chave' no ProvenanceBadge + teste de componente (duas superfícies)</name>
  <files>src/components/import-review-table.tsx, src/components/import-review-table.test.tsx</files>
  <read_first>
    - src/components/import-review-table.tsx linhas 123-151 (ProvenanceBadge — o branch `row.origin === 'memória'` ~133 que será espelhado), 524-535 + 814-815 (OriginBadge desktop + mobile)
    - src/components/import-review-table.test.tsx linhas 47-65 (makeRow helper + override de origin), 90-130 (describe + os testes de badge existentes: o teste 'memoria-badge' ~117 é o template), 220-230 (o uso de origin:'memória')
    - 20-UI-SPEC.md §Copywriting (lowercase 'palavra-chave' no ProvenanceBadge, Title-Case 'Palavra-chave' no OriginBadge)
  </read_first>
  <behavior>
    - Test "keyword-provenance-badge": uma ReviewRow com category_id set e origin='palavra-chave' renderiza o pill lowercase 'palavra-chave' no ProvenanceBadge (sem ícone), e NÃO renderiza o chip de IA nem 'memória'.
    - Test "keyword-origin-badge": a mesma row renderiza a variante OriginBadge Title-Case 'Palavra-chave' na coluna Origem.
    - Test "keyword-distinct-from-memoria": uma row de keyword NÃO mostra o label 'memória'/'Memória'; uma row de memória NÃO mostra 'palavra-chave'/'Palavra-chave' (mutuamente exclusivas).
  </behavior>
  <action>
    Edite `src/components/import-review-table.tsx` — no `ProvenanceBadge` (~linha 131), dentro do bloco `if (row.category_id !== null) { ... }`, adicione um branch para `row.origin === 'palavra-chave'` espelhando EXATAMENTE o branch de 'memória': um `<AffordancePill className="bg-secondary text-secondary-foreground">palavra-chave</AffordancePill>` — label literal lowercase `palavra-chave`, SEM ícone (igual a memória). Mantenha o branch de 'memória' intocado.

    NÃO toque no OriginBadge call sites — a variante já flui via o VARIANT map (Task 1). NÃO altere `classifyRow` (~344): trocar a categoria já seta `origin: 'manual'`, derrubando o pill de keyword — idêntico a sobrescrever memória (KW-05), sem mudança.

    Estenda `src/components/import-review-table.test.tsx` — reuse o helper `makeRow` (override `category_id` + `origin: 'palavra-chave'`) e o `renderTable`; adicione os testes em `<behavior>` no describe existente, espelhando o teste 'memoria-badge' (~117). Use matchers de regex ancorados (`/^palavra-chave$/` lowercase para ProvenanceBadge; `/^Palavra-chave$/` Title-Case para OriginBadge) para não confundir as duas superfícies.

    Atenção comment-text: NÃO inclua a string literal `palavra-chave` dentro de comentários de "o que NÃO fazer" no código editado — escreva-a apenas como label JSX/valor de union.
  </action>
  <verify>
    <automated>npx vitest run src/components/import-review-table.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Os 3 testes de componente (ProvenanceBadge lowercase, OriginBadge Title-Case, exclusão mútua memória/keyword) passam.
    - O branch de keyword no ProvenanceBadge usa `bg-secondary text-secondary-foreground` sem ícone (mirror de memória, NÃO gold).
    - A suíte existente de `import-review-table.test.tsx` permanece verde.
    - `npx tsc --noEmit` limpo.
  </acceptance_criteria>
  <done>O pill 'palavra-chave' renderiza nas duas superfícies (Categoria lowercase sem ícone; Origem Title-Case com Tags), mutuamente exclusivo de memória, com testes verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parsed row (Postgres jsonb) → RSC (page.tsx) | classification_source persistido cruza para a UI; derivado, não confiado do client |
| RSC → client component (grid) | ReviewRow.origin é props read-only; sem nova chamada de rede/DB do client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04 | Information Disclosure | page.tsx lê parsed_rows | accept | A parsed row já foi lida sob RLS pela RSC (statement do caller); a derivação de origin é pura leitura de um campo já escopado. Sem nova superfície. |
| T-20-05 | Tampering | render do badge (client) | accept | O pill é um `<span>` não-interativo (mirror de memória); nenhum novo elemento interativo, nenhuma escrita. Sobrescrever roda o `classifyRow` existente (sem auto-commit; só o confirm escreve). |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo. `Tags` é um named export do `lucide-react` já vendorizado. Gate de legitimidade: não aplicável. |
</threat_model>

<verification>
- `npx vitest run src/components/import-review-table.test.tsx` — as duas superfícies de badge (KW-02/KW-05) verde.
- `npx tsc --noEmit` — unions de UI batem com os call sites.
- Por-wave merge: `npx vitest run src/components/import-review-table.test.tsx`.
- Phase gate (antes de /gsd-verify-work): `npx vitest run` (suíte completa) verde + `npx tsc --noEmit` limpo.
</verification>

<success_criteria>
- page.tsx deriva origin de classification_source (fix load-bearing — sem ele o badge nunca renderiza) (KW-02).
- OriginVariant + ReviewRow.origin incluem 'palavra-chave'; VARIANT map neutro com Tags (KW-05).
- ProvenanceBadge mostra o pill lowercase sem ícone; OriginBadge mostra Title-Case com Tags (KW-05).
- Sobrescrever via Select flipa para 'manual' (classifyRow inalterado) (KW-05).
- Testes de componente verdes; tsc limpo; suíte completa verde.
</success_criteria>

<output>
Create `.planning/phases/20-auto-classifica-o-por-palavra-chave-no-upload/20-02-SUMMARY.md` when done.
</output>
