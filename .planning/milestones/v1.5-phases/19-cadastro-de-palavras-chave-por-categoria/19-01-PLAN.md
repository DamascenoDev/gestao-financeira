---
phase: 19-cadastro-de-palavras-chave-por-categoria
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0036_category_keywords.sql
  - src/types/database.types.ts
  - src/lib/schemas/category-keyword.ts
  - src/actions/category-keywords.ts
  - src/actions/category-keywords.test.ts
autonomous: true
requirements: [KW-01, KW-06]

must_haves:
  truths:
    - "addKeyword insere uma palavra-chave NORMALIZADA (via normalizeDescriptor) com o user_id do dono (getClaims) e chama revalidatePath('/categorias')"
    - "removeKeyword deleta a palavra-chave por id e chama revalidatePath('/categorias')"
    - "addKeyword com termo já cadastrado na mesma categoria retorna {duplicate:true} (não é erro) — via pré-check maybeSingle E via backstop 23505"
    - "Entrada vazia / só espaço / que normaliza para '' retorna 'Informe uma palavra-chave.' sem inserir"
    - "Entrada >60 caracteres é rejeitada pelo Zod (keywordSchema) sem inserir"
    - "category_keywords tem RLS 'own' (using + with check (select auth.uid())=user_id, to authenticated) + grants + unique(user_id,category_id,keyword) + ON DELETE CASCADE no category_id"
    - "database.types.ts contém o bloco category_keywords (Row/Insert/Update) após gen:types e npx tsc --noEmit passa limpo"
  prohibitions:
    - "removeKeyword NUNCA deleta keyword de outro usuário — RLS escopa o delete + WR-06 (idSchema uuid) rejeita id malformado antes do DB"
    - "addKeyword NUNCA insere com user_id vindo do cliente — o owner vem SEMPRE de getClaims().claims.sub"
    - "keyword NUNCA é guardado sem passar por normalizeDescriptor (a chave precisa ser do mesmo espaço que descriptor_norm para o match do Phase 20)"
    - "NUNCA vazar erro de DB cru (23505/22P02/23502) num resultado — sempre mapear para mensagem pt-BR amigável"
    - "NUNCA re-derivar normalização fora de normalize.ts — chamar normalizeDescriptor uma única vez no addKeyword"
    - "NADA de matching/auto-classificação aqui (isso é Phase 20)"
  artifacts:
    - path: "supabase/migrations/0036_category_keywords.sql"
      provides: "Tabela category_keywords + índices + RLS 'own' + grants + unique"
      contains: "create table if not exists public.category_keywords"
    - path: "src/lib/schemas/category-keyword.ts"
      provides: "keywordSchema (trim/min1/max60) + KeywordInput type"
      exports: ["keywordSchema", "KeywordInput"]
    - path: "src/actions/category-keywords.ts"
      provides: "addKeyword / removeKeyword server actions"
      exports: ["addKeyword", "removeKeyword", "AddKeywordResult", "ActionResult"]
    - path: "src/actions/category-keywords.test.ts"
      provides: "Testes unitários mockados de addKeyword/removeKeyword"
    - path: "src/types/database.types.ts"
      provides: "Tipos regenerados incluindo category_keywords"
      contains: "category_keywords"
  key_links:
    - from: "src/actions/category-keywords.ts"
      to: "src/lib/normalize.ts"
      via: "normalizeDescriptor(parsed.data) antes do insert"
      pattern: "normalizeDescriptor"
    - from: "src/actions/category-keywords.ts"
      to: "supabase/migrations/0036_category_keywords.sql"
      via: "supabase.from('category_keywords') (insert/select/delete)"
      pattern: "from\\('category_keywords'\\)"
    - from: "src/actions/category-keywords.ts"
      to: "src/lib/schemas/category-keyword.ts"
      via: "keywordSchema.safeParse(keyword) no boundary"
      pattern: "keywordSchema"
---

<objective>
Camada de dados + servidor da Phase 19: criar a tabela `category_keywords` (escopada por `user_id` + RLS), regenerar os tipos, e entregar as server actions `addKeyword`/`removeKeyword` (normalização, validação Zod, owner via getClaims, guarda WR-06 uuid, duplicata amigável, revalidatePath) com cobertura de testes unitários mockados.

Purpose: Entregar KW-01 (CRUD manual de palavras-chave) + KW-06 (isolamento por usuário via RLS) na camada backend, deixando a superfície de UI (Plan 19-02) com actions estáveis para consumir. É CRUD puro — NENHUM matching/auto-classificação (Phase 20).
Output: migration `0036`, `database.types.ts` regenerado, `src/lib/schemas/category-keyword.ts`, `src/actions/category-keywords.ts` + `.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-cadastro-de-palavras-chave-por-categoria/19-CONTEXT.md
@.planning/phases/19-cadastro-de-palavras-chave-por-categoria/19-RESEARCH.md
@.planning/phases/19-cadastro-de-palavras-chave-por-categoria/19-PATTERNS.md
@.planning/phases/19-cadastro-de-palavras-chave-por-categoria/19-VALIDATION.md

# Analog source files (mirror these exactly — change only the noted deltas)
@supabase/migrations/0002_categories.sql
@supabase/migrations/0021_merchant_patterns.sql
@src/actions/categories.ts
@src/actions/categories.test.ts
@src/lib/schemas/category.ts
@src/lib/normalize.ts
</context>

<artifacts_this_phase_produces>
NEW symbols introduced in this plan (downstream consumers in Plan 19-02 + Phase 20 depend on these):
- Tabela `public.category_keywords` (migration `0036_category_keywords.sql`) — colunas `id, user_id, category_id, keyword, created_at`; unique `(user_id, category_id, keyword)`; RLS "own category_keywords".
- Tipos `Database['public']['Tables']['category_keywords']['Row' | 'Insert' | 'Update']` em `database.types.ts` (regenerados via `gen:types`).
- `keywordSchema`, `KeywordInput` em `src/lib/schemas/category-keyword.ts`.
- `addKeyword(categoryId, keyword)`, `removeKeyword(keywordId)` em `src/actions/category-keywords.ts`.
- Tipos de resultado `AddKeywordResult = { ok: true } | { duplicate: true } | { error: string }` e `ActionResult = { error: string } | { ok: true }`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — esqueleto de testes mockados (RED) para as actions de palavra-chave</name>
  <read_first>
    @src/actions/categories.test.ts
    @src/lib/normalize.ts
    @.planning/phases/19-cadastro-de-palavras-chave-por-categoria/19-PATTERNS.md
    @.planning/phases/19-cadastro-de-palavras-chave-por-categoria/19-VALIDATION.md
  </read_first>
  <files>src/actions/category-keywords.test.ts</files>
  <behavior>
    - addKeyword: insere com user_id do dono ('user-1'), category_id e keyword NORMALIZADO; chama revalidatePath('/categorias'); retorna {ok:true}
    - addKeyword normaliza a entrada: passar "Uber " (maiúscula/acento/ruído) deve produzir o payload keyword normalizado pela função REAL normalizeDescriptor (NÃO mockar normalize) — asserir o payload.keyword === normalizeDescriptor(entrada)
    - addKeyword com entrada vazia / só espaço / que normaliza para '' → retorna { error } (mensagem "Informe uma palavra-chave.") e NÃO insere
    - addKeyword com entrada >60 caracteres → retorna { error } (Zod max60) e NÃO insere
    - addKeyword duplicata via PRÉ-CHECK: maybeSingle retorna uma linha existente → { duplicate: true } e NÃO insere
    - addKeyword duplicata via BACKSTOP 23505: maybeSingle retorna null mas o insert falha com error.code '23505' → { duplicate: true } (não erro)
    - addKeyword categoryId não-UUID → { error } (WR-06) antes de tocar o DB
    - removeKeyword: deleta por id na tabela category_keywords (filtro ['id', keywordId]) + revalidatePath('/categorias') → {ok:true}
    - removeKeyword keywordId não-UUID → { error } (WR-06) sem delete
    - session gate: claimsSub=null → { error: 'Sessão expirada.' } em ambas as actions
    - owner (with-check half): asserir que o payload do insert carrega user_id === 'user-1'
  </behavior>
  <action>
    Crie `src/actions/category-keywords.test.ts` espelhando o harness de `src/actions/categories.test.ts` (mock de `next/cache` revalidatePath; `makeBuilder`/`supabaseMock`; `vi.mock('@/lib/supabase/server', ...)`; toggle `claimsSub`; constantes de UUID real). Importe a função REAL `normalizeDescriptor` de `@/lib/normalize` (NÃO mocke — é determinística e já testada) para asserir o payload normalizado.

    Adição obrigatória ao harness (delta vs o analog): o builder de `categories.test.ts` resolve só via `.single()`/`.then`. Acrescente ao builder um `maybeSingle = vi.fn(() => Promise.resolve(dupPreCheckResult))` e uma variável settável `dupPreCheckResult` (default `{ data: null, error: null }`), além de manter `insertResult` settável com uma variante de erro `{ data: null, error: { code: '23505' } }` para cobrir os DOIS caminhos de duplicata (pré-check e backstop). Resete essas variáveis no `beforeEach`, como o analog faz.

    Importe `addKeyword`, `removeKeyword` de `./category-keywords` — o import vai falhar (RED) até a Task 3 existir, e os `.from('category_keywords')` só compilam após a Task 2. Use constantes de UUID real para categoryId/keywordId (mirror `CAT_ID` em categories.test.ts). Cubra TODOS os comportamentos listados em <behavior> com nomes de teste que casem os filtros `-t` da 19-VALIDATION (add, normaliz, empty, long, duplicate, remove, uuid, session, owner).

    Este é um esqueleto Wave 0: é esperado que falhe (módulo de action ainda não existe). Não escreva a implementação aqui.
  </action>
  <verify>
    <automated>npx vitest run src/actions/category-keywords.test.ts 2>&1 | grep -qiE "Cannot find module|category-keywords|Failed to resolve|does not provide" && echo "RED-as-expected (action module missing)" || echo "check: tests should fail until Task 3"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `src/actions/category-keywords.test.ts` existe e cobre os 11 comportamentos de <behavior>
    - Harness inclui `maybeSingle` settável + variante de insert 23505 (delta documentado em PATTERNS)
    - normalizeDescriptor é importado REAL (não mockado); ao menos um teste asserta payload.keyword === normalizeDescriptor(entrada)
    - Suite está RED (módulo de action ainda não existe) — esperado nesta task
  </acceptance_criteria>
  <done>O arquivo de teste existe com todos os describes/its dos comportamentos KW-01/KW-06; falha apenas por falta do módulo de action (e dos tipos), o que as Tasks 2-3 resolvem.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Migration 0036 + aplicar no stack LOCAL + gen:types + commit dos tipos</name>
  <read_first>
    @supabase/migrations/0002_categories.sql
    @supabase/migrations/0021_merchant_patterns.sql
    @.planning/phases/19-cadastro-de-palavras-chave-por-categoria/19-RESEARCH.md
    @.planning/phases/19-cadastro-de-palavras-chave-por-categoria/19-PATTERNS.md
  </read_first>
  <files>supabase/migrations/0036_category_keywords.sql, src/types/database.types.ts</files>
  <action>
    [BLOCKING — esta task PRECEDE a Task 3: o `.from('category_keywords')` tipado NÃO compila em TS-estrito até `database.types.ts` ser regenerado.]

    Passo 1 — escreva `supabase/migrations/0036_category_keywords.sql` espelhando o shape uniforme de RLS de `0002_categories.sql` + `0021_merchant_patterns.sql` (a SQL exata está em 19-RESEARCH.md §Pattern 1 e 19-PATTERNS.md). A tabela tem: `id uuid pk default gen_random_uuid()`; `user_id uuid not null references auth.users(id) on delete cascade`; `category_id uuid not null references public.categories(id) on delete cascade`; `keyword text not null`; `created_at timestamptz not null default now()`; `unique (user_id, category_id, keyword)`. Crie índices `category_keywords_user_id_idx` em `(user_id)` e `category_keywords_category_id_idx` em `(category_id)`. `enable row level security`. `grant select, insert, update, delete ... to authenticated, service_role`. Policy "own category_keywords" `for all to authenticated using ((select auth.uid()) = user_id) with check ((select auth.uid()) = user_id)`. Inclua o comentário-cabeçalho explicando: keyword guardado NORMALIZADO (mesmo espaço de descriptor_norm, para o match apples-to-apples do Phase 20); CRUD only; ON DELETE CASCADE no category_id (keyword é metadado da categoria, diferente de transactions que usa RESTRICT). NÃO copie o bloco `handle_new_user()` de `0002` (é específico de categorias). `keyword` é `text` puro (sem enum/check).

    Passo 2 — aplique ao stack LOCAL (o Supabase local está rodando, db em 127.0.0.1:55322). Rode `npm run db:push` (ou `npm run db:reset` se precisar de re-apply limpo). Esta etapa é AUTÔNOMA — NÃO é deploy para PROD. NÃO rode `supabase db push` contra PROD nesta task (isso é ação do dono, item de deploy, não gate da Phase 19).

    Passo 3 — `npm run gen:types` (script = `supabase gen types typescript --local > src/types/database.types.ts`) para regenerar os tipos com o bloco `category_keywords`. O hook de pre-commit pode reescrever `database.types.ts` (memória do projeto dev-env-testing-gotchas) — tudo bem, é o comportamento esperado.

    Passo 4 — confirme que `database.types.ts` agora contém um bloco `category_keywords` (Row/Insert/Update) ao lado do bloco `categories` e commite o arquivo regenerado.
  </action>
  <verify>
    <automated>test -f supabase/migrations/0036_category_keywords.sql && grep -q "category_keywords" src/types/database.types.ts && npx tsc --noEmit && echo "OK: migration written, types regenerated, tsc clean"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/0036_category_keywords.sql` existe com tabela + 2 índices + RLS "own" (using + with check) + grants + unique(user_id,category_id,keyword) + FK category_id ON DELETE CASCADE
    - Migration NÃO contém bloco handle_new_user / seed
    - Migration aplicada ao stack LOCAL (não PROD)
    - `src/types/database.types.ts` contém o bloco `category_keywords`
    - `npx tsc --noEmit` passa limpo
    - NENHUM push para PROD executado
  </acceptance_criteria>
  <done>A migration 0036 existe, foi aplicada localmente, os tipos foram regenerados (category_keywords presente) e o typecheck passa — destravando a Task 3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: keywordSchema + actions addKeyword/removeKeyword (GREEN)</name>
  <read_first>
    @src/actions/categories.ts
    @src/lib/schemas/category.ts
    @src/lib/normalize.ts
    @.planning/phases/19-cadastro-de-palavras-chave-por-categoria/19-RESEARCH.md
    @.planning/phases/19-cadastro-de-palavras-chave-por-categoria/19-PATTERNS.md
  </read_first>
  <files>src/lib/schemas/category-keyword.ts, src/actions/category-keywords.ts</files>
  <behavior>
    - keywordSchema: z.string().trim().min(1, 'Informe uma palavra-chave.').max(60, 'Use até 60 caracteres.')
    - addKeyword(categoryId, keyword): guarda WR-06 uuid em categoryId → Zod keywordSchema → normalizeDescriptor → '' vira erro "Informe uma palavra-chave." → getClaims owner → pré-check maybeSingle (dup → {duplicate:true}) → insert {user_id, category_id, keyword normalizado} → 23505 backstop → {duplicate:true} → revalidatePath → {ok:true}
    - removeKeyword(keywordId): guarda WR-06 uuid → getClaims session gate → delete().eq('id', keywordId) → revalidatePath → {ok:true}
    - Erros de DB nunca vazam crus: insert→'Não foi possível salvar a palavra-chave.'; delete→'Não foi possível remover a palavra-chave.'
  </behavior>
  <action>
    Implemente as actions para fazer o teste da Task 1 passar (GREEN), seguindo a SQL/shape de 19-RESEARCH.md §Pattern 2-3 e 19-PATTERNS.md (que trazem o código de referência linha-a-linha).

    Crie `src/lib/schemas/category-keyword.ts` exportando `keywordSchema` (string standalone, trim/min1/max60, mensagens pt-BR da UI-SPEC §Copywriting: "Informe uma palavra-chave." e "Use até 60 caracteres.") e o tipo `KeywordInput = z.infer<typeof keywordSchema>`. Espelha o `nameSchema` de `category.ts` mas como string solta (a action recebe um arg string, não FormData).

    Crie `src/actions/category-keywords.ts` espelhando `categories.ts`: `'use server'`; imports de `revalidatePath`, `z`, `keywordSchema`, `normalizeDescriptor`, `createClient`; `const CATEGORIAS_PATH = '/categorias'`; `const idSchema = z.string().uuid('Identificador inválido')` (WR-06); tipos `ActionResult = { error: string } | { ok: true }` e `AddKeywordResult = { ok: true } | { duplicate: true } | { error: string }`.

    `addKeyword(categoryId: string, keyword: string): Promise<AddKeywordResult>` — ordem: (1) `idSchema.safeParse(categoryId)` falha → `{ error: 'Identificador inválido.' }`; (2) `keywordSchema.safeParse(keyword)` falha → `{ error: <primeira issue> }`; (3) `const normalized = normalizeDescriptor(parsed.data)` — chamar UMA vez (REUSE, nunca re-derivar); se `normalized === ''` → `{ error: 'Informe uma palavra-chave.' }`; (4) `getClaims()` → `userId = claims?.claims.sub`; sem userId → `{ error: 'Sessão expirada.' }`; (5) pré-check `from('category_keywords').select('id').eq('category_id', categoryId).eq('keyword', normalized).maybeSingle()` — se existir → `{ duplicate: true }`; (6) `insert({ user_id: userId, category_id: categoryId, keyword: normalized })`; se `error.code === '23505'` → `{ duplicate: true }` (backstop de corrida, espelha o 23503 de deleteCategory), outro erro → `{ error: 'Não foi possível salvar a palavra-chave.' }`; (7) `revalidatePath(CATEGORIAS_PATH)` → `{ ok: true }`.

    `removeKeyword(keywordId: string): Promise<ActionResult>` — `idSchema` guard → getClaims session gate → `from('category_keywords').delete().eq('id', keywordId)`; erro → `{ error: 'Não foi possível remover a palavra-chave.' }`; revalidatePath → `{ ok: true }`. RLS escopa o delete ao caller — um id estranho/forjado não deleta nada.

    NÃO adicione RPC, pré-check de tx-count, nem arms de archive/reassign. NÃO implemente nenhum matching/classificação. Mapeie SEMPRE erros de DB para mensagens pt-BR — nunca retorne `error.message`.
  </action>
  <verify>
    <automated>npx vitest run src/actions/category-keywords.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/schemas/category-keyword.ts` exporta `keywordSchema` (trim/min1/max60, pt-BR) + `KeywordInput`
    - `src/actions/category-keywords.ts` exporta `addKeyword`, `removeKeyword`, `AddKeywordResult`, `ActionResult` com `'use server'`
    - addKeyword: WR-06 → Zod → normalizeDescriptor → ''→erro → getClaims owner → pré-check maybeSingle → insert → 23505 backstop → revalidatePath
    - removeKeyword: WR-06 → session gate → delete by id → revalidatePath
    - Todos os testes da Task 1 passam GREEN; `npx tsc --noEmit` limpo
    - Nenhuma mensagem de DB crua vazada; user_id vem só de getClaims; normalizeDescriptor chamado uma única vez
  </acceptance_criteria>
  <done>keywordSchema e ambas as actions existem, a suite mockada está toda verde e o typecheck passa; KW-01 (CRUD) + KW-06 (owner/WR-06/RLS estrutural) provados na camada de servidor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → server action | Entrada não confiável (categoryId/keywordId, keyword) cruza para `addKeyword`/`removeKeyword` |
| server action → Postgres | Insert/select/delete em `category_keywords` sob RLS do caller |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Information Disclosure / Elevation | category_keywords (cross-user read/write, KW-06) | mitigate | RLS "own category_keywords" `for all` `using` + `with check ((select auth.uid()) = user_id)`; a action seta `user_id` SEMPRE de `getClaims().claims.sub` no insert (nunca do cliente) |
| T-19-02 | Tampering / Elevation | removeKeyword(keywordId) / addKeyword(categoryId) (IDOR) | mitigate | WR-06: `idSchema = z.string().uuid()` valida `keywordId`/`categoryId` antes do `.eq('id', …)`; RLS torna um id estranho um no-op (delete/select de 0 linhas) |
| T-19-03 | Information Disclosure | erros de DB (23505/22P02/23502) | mitigate | mapear para mensagens pt-BR amigáveis; 23505 → `{duplicate:true}`; nunca retornar `error.message` (espelha o backstop 23503 de deleteCategory) |
| T-19-04 | Tampering | injeção via texto do keyword | mitigate | parametrizado via supabase-js (sem SQL string); `normalizeDescriptor` + Zod constrangem o conteúdo; substring matching é Phase 20 e usa `descriptor_norm`, não SQL montado pelo usuário |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | Zero pacotes novos instalados nesta fase (VERIFIED contra package.json em 19-RESEARCH §Package Legitimacy Audit) — nenhum gate de legitimidade aplicável |
</threat_model>

<verification>
- `npx vitest run src/actions/category-keywords.test.ts` — verde (KW-01 + KW-06 estrutural)
- `npx tsc --noEmit` — limpo (gate TS-estrito; só passa após gen:types da Task 2)
- `npm test` — suite completa verde (gate de merge de wave)
- Estrutural KW-06: a policy "own" existe na 0036 com using + with check; a action seta user_id de getClaims; idSchema guarda ids de linha
</verification>

<success_criteria>
- KW-01: addKeyword insere palavra-chave normalizada com owner + revalidate; removeKeyword deleta por id + revalidate; duplicata é no-op amigável; vazio/longo rejeitados sem inserir
- KW-06: tabela category_keywords escopada por user_id com RLS "own" (using + with check); insert carrega user_id de getClaims; WR-06 guarda ids de linha
- Tipos regenerados (category_keywords presente) + tsc limpo
- Zero matching/classificação (escopo Phase 20 respeitado)
</success_criteria>

<output>
Create `.planning/phases/19-cadastro-de-palavras-chave-por-categoria/19-01-SUMMARY.md` when done
</output>
