---
phase: 19-cadastro-de-palavras-chave-por-categoria
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - src/components/category-keywords-dialog.tsx
  - src/components/category-row-actions.tsx
  - src/app/(app)/categorias/page.tsx
  - src/components/category-keywords-dialog.test.tsx
autonomous: true
requirements: [KW-01]

must_haves:
  truths:
    - "Na tela /categorias, cada linha tem um item de menu 'Palavras-chave (N)' (ou 'Palavras-chave' quando N=0) entre 'Editar' e 'Excluir' que abre um Dialog dedicado"
    - "O Dialog lista as palavras-chave da categoria como chips removíveis (Badge + botão X) e mostra um Empty state quando não há nenhuma"
    - "Adicionar (Enter ou botão 'Adicionar') chama addKeyword e, em {ok:true}, limpa e re-focaliza o input; em {duplicate:true} mostra toast.info 'já está cadastrada'; em {error} mostra toast.error"
    - "Clicar no X de um chip chama removeKeyword e, em sucesso, mostra toast.success 'Palavra-chave removida.'"
    - "A página /categorias (RSC) faz um fetch agrupado das palavras-chave (Map por category_id, espelhando txCountByCategory) e passa a lista por categoria ao CategoryRowActions"
    - "Ao abrir, o foco vai para o input do keyword; cada chip X tem aria-label 'Remover palavra-chave {keyword}'"
  prohibitions:
    - "O Dialog NUNCA tem botão 'Salvar'/save em lote — persistência é imediata por ação (add/remove), igual setKind/setColor; o footer é só um DialogClose 'Fechar'"
    - "O X de remover NUNCA é destructive-red nem abre alert-dialog de confirmação — remoção é imediata e reversível (re-add)"
    - "O Dialog NUNCA mostra preview de match, contagem de transações casadas, source='palavra-chave' ou sugestão automática — isso é Phase 20 / KW-F1"
    - "A página NUNCA filtra keywords por .eq('user_id', …) na mão — RLS é o gate (como os fetches existentes de categories/totals)"
    - "O chip exibe o keyword NORMALIZADO (como veio do servidor) — sem mensagem 'você digitou X, salvou Y'"
  artifacts:
    - path: "src/components/category-keywords-dialog.tsx"
      provides: "Dialog controlado de CRUD de palavras-chave (chips + input + Empty)"
      exports: ["CategoryKeywordsDialog"]
    - path: "src/components/category-row-actions.tsx"
      provides: "Item de menu 'Palavras-chave (N)' + estado keywordsOpen + render do dialog"
      contains: "CategoryKeywordsDialog"
    - path: "src/app/(app)/categorias/page.tsx"
      provides: "Fetch agrupado de category_keywords + thread de keywords[] ao RowActions"
      contains: "keywordsByCategory"
  key_links:
    - from: "src/app/(app)/categorias/page.tsx"
      to: "src/components/category-row-actions.tsx"
      via: "prop keywords={keywords} dentro do category object"
      pattern: "keywords="
    - from: "src/components/category-row-actions.tsx"
      to: "src/components/category-keywords-dialog.tsx"
      via: "render de <CategoryKeywordsDialog open={keywordsOpen} …>"
      pattern: "CategoryKeywordsDialog"
    - from: "src/components/category-keywords-dialog.tsx"
      to: "src/actions/category-keywords.ts"
      via: "addKeyword / removeKeyword em useTransition"
      pattern: "addKeyword|removeKeyword"
---

<objective>
Superfície de UI da Phase 19: um novo Dialog controlado de palavras-chave por categoria (chips removíveis + input "Adicionar" + Empty state), acessível por um item de menu "Palavras-chave (N)" no `CategoryRowActions`, alimentado por um fetch agrupado no RSC de `/categorias`. Entrega a parte visível de KW-01 conforme o contrato aprovado em 19-UI-SPEC.md.

Purpose: Dar ao usuário o lugar para adicionar/remover palavras-chave de cada categoria, com persistência imediata por ação (sem save em lote), consumindo as actions estáveis do Plan 19-01. Reuso total do sistema base-nova (navy+gold) — zero tokens/componentes novos.
Output: `src/components/category-keywords-dialog.tsx` (+ teste opcional), edits em `category-row-actions.tsx` e `categorias/page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-cadastro-de-palavras-chave-por-categoria/19-CONTEXT.md
@.planning/phases/19-cadastro-de-palavras-chave-por-categoria/19-UI-SPEC.md
@.planning/phases/19-cadastro-de-palavras-chave-por-categoria/19-PATTERNS.md
@.planning/phases/19-cadastro-de-palavras-chave-por-categoria/19-VALIDATION.md

# A action consumida nesta UI (entregue pelo Plan 19-01)
@src/actions/category-keywords.ts

# Analog source files (mirror these — change only the noted deltas)
@src/components/categoria-form.tsx
@src/components/category-row-actions.tsx
@src/components/category-filter.tsx
@src/app/(app)/categorias/page.tsx
</context>

<artifacts_this_phase_produces>
NEW symbols introduced in this plan:
- `CategoryKeywordsDialog` (componente client controlado) em `src/components/category-keywords-dialog.tsx` — props `{ open, onOpenChange, category: {id, name}, keywords: {id, keyword}[] }`.
- Item de menu "Palavras-chave (N)" + estado `keywordsOpen` em `CategoryRowActions` (prop nova `keywords` no objeto category).
- `keywordsByCategory` (Map<category_id, {id, keyword}[]>) no RSC `categorias/page.tsx`, threaded ao RowActions.

Depende de (Plan 19-01): `addKeyword`, `removeKeyword`, tipos de `category_keywords`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: CategoryKeywordsDialog — chips removíveis + input "Adicionar" + Empty</name>
  <read_first>
    @src/components/categoria-form.tsx
    @src/components/category-filter.tsx
    @src/actions/category-keywords.ts
    @.planning/phases/19-cadastro-de-palavras-chave-por-categoria/19-UI-SPEC.md
    @.planning/phases/19-cadastro-de-palavras-chave-por-categoria/19-PATTERNS.md
  </read_first>
  <files>src/components/category-keywords-dialog.tsx</files>
  <action>
    Crie `src/components/category-keywords-dialog.tsx` ('use client'), espelhando o esqueleto controlado de `categoria-form.tsx` (controlled `open`/`onOpenChange`, `useTransition` → `isPending`, sonner `toast`) e a receita de chip removível de `category-filter.tsx:106-122`. Props: `{ open: boolean; onOpenChange: (open: boolean) => void; category: { id: string; name: string }; keywords: { id: string; keyword: string }[] }`. SEMPRE controlado (sem trigger inline). Importe `addKeyword`, `removeKeyword` de `@/actions/category-keywords`.

    Anatomia do Dialog (topo → base), compondo SÓ primitivos já vendados (Dialog/Badge/Input/Button/Field/Empty — nenhum componente shadcn novo):
    1. `DialogHeader` → `DialogTitle` "Palavras-chave — {category.name}" + `DialogDescription` "Adicione termos que identificam gastos desta categoria. Eles serão usados para classificar automaticamente futuras faturas."
    2. Região de chips — `div className="flex flex-wrap gap-2"`, um `Badge variant="secondary" className="gap-1"` por keyword com um botão `X` (size-3) de remover. Quando `keywords.length === 0`, renderize o `Empty` (EmptyHeader/EmptyTitle "Nenhuma palavra-chave" / EmptyDescription com a copy da UI-SPEC) NO LUGAR da linha de chips. O chip exibe o keyword normalizado (já vem normalizado do servidor); SEM `CategoryDot` (keyword não tem cor). Botão X: `<button type="button" aria-label={\`Remover palavra-chave ${kw.keyword}\`} className="ml-0.5 inline-flex">` que dispara `startTransition(async () => { const r = await removeKeyword(kw.id); if ('error' in r) toast.error(r.error); else toast.success('Palavra-chave removida.') })`.
    3. Linha de adicionar — um `<form onSubmit>` real (Enter submete) com um `Field`: `FieldLabel` "Nova palavra-chave" + `Input` (`maxLength={60}`, `placeholder="Ex.: uber"`, `aria-invalid` ligado ao erro inline, foco ao abrir via `autoFocus`/initialFocus) numa linha com um `Button type="submit"` "Adicionar" (`flex gap-2`), com `FieldError` abaixo. onSubmit: `e.preventDefault()`; valide vazio local ("Informe uma palavra-chave."); `startTransition(async () => { const r = await addKeyword(category.id, value); ... })` — em `{ok:true}`: `toast.success('Palavra-chave adicionada.')` + limpar o input + devolver o foco ao input (NÃO fechar o dialog); em `{duplicate:true}`: `toast.info('"{value}" já está cadastrada nesta categoria.')`; em `{error}`: `toast.error(r.error)`. Botão pendente: `{isPending ? 'Adicionando…' : 'Adicionar'}` + `disabled={isPending}`.
    4. `DialogFooter` → um único `DialogClose` renderizando `Button variant="outline"` "Fechar" (SEM "Salvar" — persistência é imediata, não em lote).

    Toda a copy/labels/estados estão TRAVADOS em 19-UI-SPEC.md §Copywriting Contract — use-os verbatim (pt-BR, sem exclamação). A fonte da verdade do estado é o servidor (após `revalidatePath` na action, a página re-renderiza e a lista atualizada flui de volta via props); manter uma cópia otimista local para feedback instantâneo é discricionário, desde que o servidor continue sendo a fonte da verdade. Honre o focus management (foco no input ao abrir; trap padrão da Base UI; Esc fecha; foco volta ao trigger ao fechar — não sobrescrever). NÃO adicione preview de match, contagem de transações, source nem sugestão automática (Phase 20).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "CategoryKeywordsDialog" src/components/category-keywords-dialog.tsx && grep -qE "addKeyword|removeKeyword" src/components/category-keywords-dialog.tsx && echo "OK: dialog compiles + wires actions"</automated>
  </verify>
  <acceptance_criteria>
    - `CategoryKeywordsDialog` exportado; 'use client'; props `{ open, onOpenChange, category:{id,name}, keywords:{id,keyword}[] }`
    - Chips removíveis (Badge variant="secondary" + botão X size-3 com aria-label "Remover palavra-chave {keyword}"); Empty state quando keywords.length===0
    - Form de adicionar com Enter-to-add, limpa+refoca input no sucesso, foco no input ao abrir, maxLength=60, placeholder "Ex.: uber"
    - Toasts pt-BR: add success/info-duplicate/error; remove success/error — verbatim da UI-SPEC
    - Footer só com DialogClose "Fechar" (sem "Salvar"); X muted (não destructive-red); sem alert-dialog de confirmação
    - Zero componente shadcn/token novo; zero superfície de Phase 20
    - `npx tsc --noEmit` limpo
  </acceptance_criteria>
  <done>O Dialog renderiza a lista de chips removíveis + Empty + form de adicionar conforme a UI-SPEC, fia addKeyword/removeKeyword com useTransition+toast, e compila limpo.</done>
</task>

<task type="auto">
  <name>Task 2: Wire no CategoryRowActions + fetch agrupado no RSC categorias/page.tsx</name>
  <read_first>
    @src/components/category-row-actions.tsx
    @src/app/(app)/categorias/page.tsx
    @src/components/category-keywords-dialog.tsx
    @.planning/phases/19-cadastro-de-palavras-chave-por-categoria/19-PATTERNS.md
  </read_first>
  <files>src/components/category-row-actions.tsx, src/app/(app)/categorias/page.tsx</files>
  <action>
    Edite `src/components/category-row-actions.tsx` (self-edit, espelhando o padrão de dialog controlado editOpen/deleteOpen existente):
    1. Import: `import { CategoryKeywordsDialog } from '@/components/category-keywords-dialog'`.
    2. Tipo `Category`: adicione `keywords: { id: string; keyword: string }[]`.
    3. Estado: `const [keywordsOpen, setKeywordsOpen] = React.useState(false)` (junto de editOpen/deleteOpen).
    4. Item de menu ENTRE "Editar" e o destructive "Excluir" (pra manter o destructive por último): `<DropdownMenuItem onClick={() => setKeywordsOpen(true)}>{category.keywords.length > 0 ? \`Palavras-chave (${category.keywords.length})\` : 'Palavras-chave'}</DropdownMenuItem>`.
    5. Render do dialog controlado junto dos outros dois: `<CategoryKeywordsDialog open={keywordsOpen} onOpenChange={setKeywordsOpen} category={{ id: category.id, name: category.name }} keywords={category.keywords} />`.

    Edite `src/app/(app)/categorias/page.tsx` (self-edit, espelhando o Map `txCountByCategory`):
    1. Após o fetch de `totals`, adicione um fetch agrupado (RLS escopa ao caller — NÃO filtrar por user_id na mão, igual os fetches de categories/totals): `const { data: keywordRows } = await supabase.from('category_keywords').select('id, category_id, keyword').order('keyword', { ascending: true })`. Construa `const keywordsByCategory = new Map<string, { id: string; keyword: string }[]>()` iterando `keywordRows ?? []` (pular `!row.category_id`), agrupando por `category_id`.
    2. Dentro de `rows.map`, ao lado de `txCount`: `const keywords = keywordsByCategory.get(row.id) ?? []`.
    3. No `<CategoryRowActions>`, adicione `keywords` ao objeto `category` passado como prop.

    Não use join/view nem segundo query de contagem — o Map agrupado em JS é a forma escolhida (RESEARCH A3, discricionário confirmado).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "CategoryKeywordsDialog" src/components/category-row-actions.tsx && grep -q "keywordsByCategory" "src/app/(app)/categorias/page.tsx" && grep -q "category_keywords" "src/app/(app)/categorias/page.tsx" && echo "OK: row-actions + page wired"</automated>
  </verify>
  <acceptance_criteria>
    - CategoryRowActions: import + tipo keywords + estado keywordsOpen + item de menu "Palavras-chave (N)" entre Editar e Excluir + render do CategoryKeywordsDialog controlado
    - page.tsx: fetch `from('category_keywords').select('id, category_id, keyword')` (sem .eq user_id), Map keywordsByCategory, `const keywords` no rows.map, `keywords` threaded ao RowActions
    - `npx tsc --noEmit` limpo
    - Sem join/view/segundo-count query
  </acceptance_criteria>
  <done>O item "Palavras-chave (N)" aparece em cada linha de /categorias e abre o dialog populado pela lista vinda do RSC; o typecheck passa.</done>
</task>

<task type="auto">
  <name>Task 3: (Opcional) Teste de componente do dialog</name>
  <read_first>
    @src/components/category-keywords-dialog.tsx
    @.planning/phases/19-cadastro-de-palavras-chave-por-categoria/19-VALIDATION.md
    @src/components/categoria-form.tsx
  </read_first>
  <files>src/components/category-keywords-dialog.test.tsx</files>
  <behavior>
    - Renderiza um chip por keyword passado em props; cada chip tem um botão com aria-label "Remover palavra-chave {keyword}"
    - Renderiza o Empty state ("Nenhuma palavra-chave") quando keywords=[]
    - Submeter o form (Adicionar) chama a action addKeyword (mockada) com (category.id, valor) e mostra toast de sucesso no {ok:true}
    - Clicar no X de um chip chama removeKeyword (mockada) com o id do chip
  </behavior>
  <action>
    Crie `src/components/category-keywords-dialog.test.tsx` (Testing Library + jsdom), mockando `@/actions/category-keywords` (addKeyword/removeKeyword vi.fn) e `sonner`. Renderize `<CategoryKeywordsDialog open onOpenChange={()=>{}} category={{id, name}} keywords={[...]} />`. Cubra os 4 comportamentos de <behavior>. Espelhe os padrões de teste de componente existentes (ex.: `src/components/receita-row-actions.test.tsx`).

    Este teste é OPCIONAL (a 19-VALIDATION marca como opcional — o gate de KW-01/KW-06 é a suite mockada de actions do Plan 19-01). Se a infra de Testing Library para Dialog (Base UI portal) exigir setup que não exista, documente no SUMMARY e deixe o gate na suite de actions; não bloqueie a fase por este teste.
  </action>
  <verify>
    <automated>npx vitest run src/components/category-keywords-dialog.test.tsx 2>&1 | tail -5 || echo "optional component test — see SUMMARY if skipped"</automated>
  </verify>
  <acceptance_criteria>
    - Se entregue: teste cobre render de chips, Empty, add chama addKeyword, remove chama removeKeyword — verde
    - Se pulado: justificativa registrada no SUMMARY (infra de portal/Dialog) e o gate permanece na suite de actions do Plan 19-01
  </acceptance_criteria>
  <done>Ou o teste de componente está verde, ou foi conscientemente pulado com justificativa no SUMMARY (o gate da fase continua sendo a suite mockada de actions).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (dialog) → server action | Input do keyword + ids cruzam para addKeyword/removeKeyword (validados no servidor — Plan 19-01) |
| RSC fetch → Postgres | select de category_keywords sob RLS do caller (sem filtro user_id na app) |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | Information Disclosure | categorias/page.tsx fetch de category_keywords | mitigate | RLS escopa o select ao caller; a página NÃO filtra por user_id na app (igual categories/totals) — o gate é o Postgres, não a camada de app |
| T-19-06 | Tampering / Elevation | dialog dispara removeKeyword/addKeyword | accept | A UI só repassa os args; toda validação (WR-06 uuid, owner getClaims, RLS, normalização) está no servidor (Plan 19-01). A UI não é o gate de segurança |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | Zero pacotes novos (VERIFIED contra package.json em 19-RESEARCH §Package Legitimacy Audit; UI-SPEC §Registry Safety: nenhum componente/registry novo) |
</threat_model>

<verification>
- `npx tsc --noEmit` — limpo (gate TS-estrito de todo o wiring de UI)
- `npm test` — suite completa verde (inclui o teste opcional de componente se entregue + a suite de actions do 19-01)
- Visual/manual (não-gate, para /gsd-verify-work): em /categorias, o menu de cada linha mostra "Palavras-chave (N)"; abrir mostra chips + input com foco; Enter adiciona e re-foca; X remove; Empty quando vazio; toasts pt-BR
</verification>

<success_criteria>
- KW-01 (superfície de UI): o usuário adiciona/remove palavras-chave de uma categoria via o Dialog em /categorias, com persistência imediata por ação e contagem (N) no menu
- Reuso total do base-nova; zero token/componente/registry novo; zero superfície de Phase 20
- Acessibilidade da UI-SPEC: chips com aria-label, Enter-to-add, foco no input ao abrir, Empty state, toasts pt-BR
- Typecheck limpo; suite verde
</success_criteria>

<output>
Create `.planning/phases/19-cadastro-de-palavras-chave-por-categoria/19-02-SUMMARY.md` when done
</output>
