---
milestone: v1.4
milestone_name: IA de Classificação (BYOK)
audited: 2026-06-19
status: tech_debt
scores:
  requirements: 17/17 mapped + wired (code) · 4/17 PROD/live-verified
  phases: 4/4 code-complete (1 passed, 3 human_needed deferred-smoke)
  integration: WIRED (0 blockers, 0 warnings, 0 orphans)
  flows: E2E chain complete at code level
gaps:
  requirements: []   # no UNSATISFIED requirements — all wired + LOCAL-proven
  integration: []
  flows: []
tech_debt:
  - phase: 14-key-storage-byok-settings
    items:
      - "Deferred (deploy): PROD push of migration 0033 (ai_settings + Vault + 3 RPCs + RLS). LOCAL applied+proven (797/797). NOTE: commit 9c5d270 'hotfix migration 0034 — re-apply corrected ai-key RPCs to reach PROD' suggests this may ALREADY be live in PROD — needs confirmation (supabase db push status + gen:types)."
  - phase: 15-classification-wire
    items:
      - "Deferred (credential): real-key classification smoke — configure a real Gemini/Claude key, upload a fatura with a NEW merchant (cache-miss), observe a real AI suggestion on the row. All no-key/error/malformed/enum-drift paths are 100% unit-proven (812/812); only the live provider call is unexercised."
      - "Deferred (deploy): confirm PROD (Vercel) import route segment inherits maxDuration ≥ 60 covering parse + 1 batched AI call. `export const maxDuration = 60` present + correct; live inheritance is deploy-gated."
  - phase: 16-review-grid-suggestion-affordances
    items:
      - "Deferred (credential): real-key end-to-end grid render — with a real key + new merchant, eyeball the 'Aplicar sugestão' chip + gold IA badge + amber 'baixa confiança' tag + low-confidence-first ordering. UI proven by component test (7/7); depends on Phase 15's real-key smoke."
  - milestone-wide:
    items:
      - "BLOCKER ON DEFERRED SMOKES: the PROD account was DELETED 2026-06-19 (Phase 17 DATA-02). The real-key smokes (15, 16) and any PROD confirmation now require a FRESH account at /auth/signup + re-entering the BYOK AI key before they can be performed."
      - "Traceability staleness (doc): BYOK-01..05 / CLSAI-01..08 rows read 'Pending' but are code-complete + WIRED + LOCAL-proven. CLSAI-01..08 were not listed in the 15/16 SUMMARY `requirements_completed` frontmatter (minor doc gap; integration checker confirmed all WIRED)."
---

# Milestone v1.4 — IA de Classificação (BYOK) — Audit

**Status: tech_debt** — no code blockers; the milestone is code-complete and fully wired, with a set of deferred deploy/credential-gated live-verify items to consciously accept before closing (the same pattern v1.0 was archived under).

## Definition of Done vs. Reality

v1.4 wires AI classification into the `suggestCategory()` seam with BYOK key storage, across a strict dependency chain (14 key-storage → 15 AI-call → 16 grid) plus an isolated v1.3 debt-cleanup phase (17).

| Phase | Plans | Verification | Reality |
|-------|-------|--------------|---------|
| 14 Key Storage + BYOK | 5/5 | human_needed | Code-complete + LOCAL-verified (Vault + RLS + key-never-on-client proven). **Deferred:** PROD push of 0033 (possibly already live via 0034 hotfix). |
| 15 Classification Wire | 2/2 | human_needed | Code-complete + LOCAL-verified (memory-first / 1-call / enum-gate / no-auto-commit / PII-safe all unit-proven). **Deferred:** real-key smoke + PROD maxDuration. |
| 16 Review-Grid Affordances | 1/1 | human_needed | Complete + grid live in PROD; component-test proven. **Deferred:** real-key suggestion render (depends on 15). |
| 17 v1.3 Debt Cleanup | 4/4 | **passed** | Fully complete — SC1/SC2/SC4 verified live + DATA-02 destructive delete executed live 2026-06-19. |

## Requirements Coverage (17/17 mapped + wired)

Integration checker (gsd-integration-checker) verified the full 14→15→16 chain WIRED at code level with E2E traces — **0 blockers, 0 warnings, 0 orphans, 0 shape mismatches**:

- **BYOK-01..05** (P14) → WIRED. migration 0033 `ai_settings`+Vault+RPCs → `getDecryptedAiSettings` (settings.server.ts) → consumed by suggest.ts + import.ts. *LOCAL-proven; PROD push deferred.*
- **CLSAI-01..06** (P15) → WIRED. memory-first PASS1 (zero-call on all-hits) → one batched enum-restricted `classifyDescriptors` per upload → `row.suggestion` non-binding hint → never-throw + try/catch guard (no upload failure on AI path) → no auto-commit (learn only at confirm).
- **CLSAI-07..08** (P16) → WIRED. `ProvenanceBadge` memória/IA + `SuggestionSlot`; `LOW_CONFIDENCE=0.6` tag + low-confidence-first partition; bridge threaded through the route page.
- **DEBT-03..06** (P17) → satisfied (operational; verified live).

No UNSATISFIED requirements. The deferred items are live-smoke verification, not implementation holes.

## Recommendation

This is the **v1.0 deferred-verify pattern**: close the milestone with the deferred live smokes tracked, OR keep it open until they're performed. Two wrinkles to weigh:
1. The deferred smokes now require re-creating a PROD account + re-entering the BYOK key (the prod account was deleted in Phase 17).
2. P14's PROD migration may already be live (the 0034 hotfix) — worth confirming cheaply.

Cross-phase code is sound and verified — the milestone's *engineering* is done. What remains is operational/credential-gated live confirmation.
