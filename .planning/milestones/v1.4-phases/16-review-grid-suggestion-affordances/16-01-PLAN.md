---
phase: 16-review-grid-suggestion-affordances
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/import-review-table.test.tsx
  - src/components/import-review-table.tsx
  - src/app/(app)/importar/[statementId]/page.tsx
autonomous: true
requirements: [CLSAI-07, CLSAI-08]

must_haves:
  truths:
    - "Uma linha com row.suggestion.categoryId não-null mostra o chip 'Aplicar sugestão: {name}' + a badge 'IA' na célula Categoria"
    - "Uma linha com row.suggestion.categoryId === null não mostra chip nem badge (slot inerte '—', idêntico ao v1.3)"
    - "Uma linha já classificada por memória (category_id setado, origin 'memória') mostra a badge 'memória' neutra e nenhum chip — mutuamente exclusiva com IA"
    - "Uma linha com confidence < 0.6 mostra a tag 'baixa confiança' âmbar; >= 0.6 não mostra tag de confiança"
    - "Quando há sugestões de IA, as linhas de baixa confiança ordenam PRIMEIRO no carregamento inicial; sem IA a ordem é a do v1.3"
    - "Clicar 'Aplicar sugestão' preenche o Select da linha (onApply seta category_id no estado) e NÃO escreve merchant_patterns — confirmImport intocado"
    - "Sem nenhuma row.suggestion no upload, a grid renderiza idêntica ao v1.3 (sem badges, sem chips, ordem original)"
  artifacts:
    - path: "src/components/import-review-table.test.tsx"
      provides: "Component test pinning the 7 VALIDATION edges (chip-on-ai, no-chip-on-none-fits, memoria-badge, low-confidence-tag, low-confidence-first-sort, no-suggestions-v1.3-identical, apply-no-commit)"
      contains: "ImportReviewTable"
    - path: "src/components/import-review-table.tsx"
      provides: "Suggestion field on ReviewRow + SuggestionSlot bridge + provenance badge + confidence tag + LOW_CONFIDENCE const + low-confidence-first comparator"
      contains: "LOW_CONFIDENCE"
    - path: "src/app/(app)/importar/[statementId]/page.tsx"
      provides: "Threads parsedRows[].suggestion into the ReviewRow mapping"
      contains: "suggestion"
  key_links:
    - from: "src/app/(app)/importar/[statementId]/page.tsx"
      to: "src/components/import-review-table.tsx"
      via: "reviewRows.map threads r.suggestion onto each ReviewRow"
      pattern: "suggestion:\\s*r\\.suggestion"
    - from: "src/components/import-review-table.tsx"
      to: "src/components/suggestion-slot.tsx"
      via: "category cell resolves suggestion.categoryId -> name and passes { categoryId, name } to <SuggestionSlot>"
      pattern: "<SuggestionSlot"
---

<objective>
Render the AI suggestion the Phase 15 wire already produces (`row.suggestion`) in the existing review grid: feed the `SuggestionSlot` chip, add a per-row provenance badge (memória vs IA), add a per-row low-confidence hint, and order low-confidence AI rows first on initial load. Pure additive UI over a proven pipeline — NO auto-commit (the learn loop stays in `confirmImport`, untouched).

Purpose: Closes CLSAI-07 (provenance) + CLSAI-08 (confidence hint + sort). The Phase 15 suggestion already flows into `statements.parsed_rows` as `ParsedReviewRow.suggestion`; today the route drops it on the floor and the cell renders an always-inert `<SuggestionSlot />`. This plan plumbs the existing data through to the existing slot and adds the two small affordances the UI-SPEC fixes.

Output: a new component test (`import-review-table.test.tsx`, Wave-0 RED→GREEN), the threaded `suggestion` on `ReviewRow` + the route map, the `SuggestionSlot` bridge, the provenance badge, the `LOW_CONFIDENCE` const + confidence tag, and the low-confidence-first sort comparator.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# The design contract (LOCKED) — tokens, copy, placement, sort, render matrix
@.planning/phases/16-review-grid-suggestion-affordances/16-UI-SPEC.md
# The 7 validation edges seeded into this plan's per-task verify map
@.planning/phases/16-review-grid-suggestion-affordances/16-VALIDATION.md

# The surface (category cell ~744-771, ReviewRow type ~100-130, route map below)
@src/components/import-review-table.tsx
@src/components/suggestion-slot.tsx
@src/components/category-badge.tsx
@src/lib/parsers/types.ts
@src/app/(app)/importar/[statementId]/page.tsx

# Test patterns to mirror (testing-library/react component render + Base UI G-01 pin)
@src/components/br-date-field.test.tsx
@src/components/select-value-label.test.tsx
</context>

<artifacts_produced>
This phase produces (named for the downstream executor + verifier):
- **The review-table component test** — `src/components/import-review-table.test.tsx` (new; the grid had no test).
- **The `suggestion` field on `ReviewRow`** + the route threading (`reviewRows.map` → `suggestion: r.suggestion`).
- **The `SuggestionSlot` bridge** — resolves `suggestion.categoryId` → `name` from `categories` and passes `{ categoryId, name }`; null categoryId → no chip.
- **The provenance badge** — "memória" (neutral `bg-secondary`) vs "IA" (gold `bg-primary/10` + `Sparkles`), mutually exclusive per row, in the category cell.
- **The `LOW_CONFIDENCE` const** (`= 0.6`, single tunable) + the **confidence tag** ("baixa confiança", `bg-consumption/10 text-consumption`, shown only when `confidence < LOW_CONFIDENCE`).
- **The low-confidence-first sort comparator** — initial order puts low-confidence AI rows first, only when AI suggestions exist.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — RED component test for the 7 review-grid suggestion edges + thread suggestion onto ReviewRow</name>
  <files>src/components/import-review-table.test.tsx, src/components/import-review-table.tsx, src/app/(app)/importar/[statementId]/page.tsx</files>
  <read_first>
    - src/components/import-review-table.tsx — the ReviewRow type (~100-130, NO suggestion field yet), the InlineReviewCategoryCell (~703-812, renders an always-inert `<SuggestionSlot />` at ~771), the columns memo + useReactTable sorting state (~194-196, 427-437).
    - src/lib/parsers/types.ts:82 — the input contract `ParsedReviewRow.suggestion?: { categoryId: string | null; confidence: number; source: 'ia' }`.
    - src/app/(app)/importar/[statementId]/page.tsx:193-210 — the reviewRows.map() that currently omits suggestion.
    - src/components/br-date-field.test.tsx + src/components/select-value-label.test.tsx — the exact component-test idiom (render from @testing-library/react, screen/getByRole, describe/it/expect/vi from vitest, fireEvent for interaction).
  </read_first>
  <behavior>
    The test file pins (initially RED, then GREEN after Tasks 2-3) the 7 VALIDATION.md edges by rendering `<ImportReviewTable>` with crafted `initialRows`/`categories`:
    - chip-on-ai-suggestion: a row with category_id null + suggestion { categoryId: <a real category id>, confidence: 0.9, source 'ia' } → the cell shows "Aplicar sugestão: {name}" and an "IA" badge.
    - no-chip-on-none-fits: a row with category_id null + suggestion { categoryId: null, confidence: 0.9, source 'ia' } → NO chip text, NO "IA" badge (inert state).
    - memoria-badge: a row with category_id set + origin 'memória' → a "memória" badge, NO chip, NO "IA" badge (mutually exclusive).
    - low-confidence-tag: a row with suggestion confidence 0.3 → "baixa confiança" tag present; a row with confidence 0.9 → that tag absent.
    - low-confidence-first-sort: given rows where a low-confidence AI row sits after a high-confidence/no-suggestion row in input order, the FIRST rendered data row is the low-confidence one (assert via row order in the rendered table body).
    - no-suggestions-v1.3-identical: rows with NO suggestion field anywhere → no "IA" badge, no "memória" badge, no "baixa confiança" tag, no "Aplicar sugestão" text; rendered row order equals input order.
    - apply-no-commit: clicking the "Aplicar sugestão" chip fills the Select (the cell now shows the suggested CategoryBadge name and the chip disappears) WITHOUT any import/confirm being invoked — assert confirmImport is not called (mock @/actions/import so the test proves no write path fires on apply).
    Mock `@/actions/import` (vi.mock) and `next/navigation` useRouter so render + apply do not hit server/router. Reuse the G-01 awareness: the Select trigger renders CategoryBadge children — assert on the rendered category NAME text, not the raw id.
  </behavior>
  <action>
    Create `src/components/import-review-table.test.tsx` following the br-date-field.test.tsx idiom (import { render, screen, fireEvent } from '@testing-library/react'; describe/it/expect/vi from vitest). Write all 7 cases above as RED expectations against the CURRENT component (they will fail because the bridge/badges/sort/threading do not exist yet — that is the RED state). Mock `@/actions/import` and `next/navigation` at module top. Build a small `makeRow` helper + a fixed two-category fixture so each case is a few lines. Do NOT add fenced implementation into the component yet — this task is RED scaffolding only, PLUS the one non-UI plumbing change below that the tests compile against.

    In the SAME task, add the `suggestion` field to the `ReviewRow` type in `import-review-table.tsx` (mirror the `ParsedReviewRow.suggestion` shape: `suggestion?: { categoryId: string | null; confidence: number; source: 'ia' }`, optional so old call sites + the v1.3 path stay back-compat), and thread it in the route map at `src/app/(app)/importar/[statementId]/page.tsx` (`reviewRows.map` → add `suggestion: r.suggestion`). This is the data-availability seam the UI tasks consume; adding it now lets the test fixtures type-check. No rendering behavior changes from this plumbing alone — the cell still ignores `suggestion` until Task 2, so the v1.3-identical case still holds for the no-suggestion fixtures.
  </action>
  <verify>
    <automated>npx vitest run src/components/import-review-table.test.tsx 2>&1 | tail -25</automated>
  </verify>
  <done>
    `import-review-table.test.tsx` exists with all 7 named cases; the suite RUNS (no import/type errors) and FAILS on the suggestion-dependent cases (RED) while any no-suggestion/v1.3-identical assertions that only depend on existing behavior may pass. `ReviewRow` carries an optional `suggestion`; the route threads `suggestion: r.suggestion`. `tsc --noEmit` is clean for the two touched non-test files.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SuggestionSlot bridge + provenance badge (memória vs IA) — CLSAI-07</name>
  <files>src/components/import-review-table.tsx, src/components/import-review-table.test.tsx</files>
  <read_first>
    - src/components/import-review-table.tsx:703-812 — InlineReviewCategoryCell: the `<Select>` (~746-769), the inert `{row.category_id === null ? <SuggestionSlot /> : null}` at ~771, the cell wrapper `<div className="flex flex-col gap-1">` at ~745. NOTE the cell is rendered in BOTH the desktop column (~353-360) and the mobile card (~617-622) — wire once inside InlineReviewCategoryCell so both surfaces get it.
    - src/components/suggestion-slot.tsx — `suggestion?: { categoryId: string; name: string } | null` (categoryId is NON-null here) + `onApply(categoryId)`. Feed it the resolved name; null categoryId → pass null so it stays inert.
    - src/components/category-badge.tsx:53-67 — the `KindBadge` pill markup to mirror for the provenance pills (`inline-flex h-5 w-fit items-center rounded-4xl px-2 py-0.5 text-xs font-medium`).
    - 16-UI-SPEC.md §Color "Provenance badge token map" + §Interaction "Per-row rendering matrix" + §Apply mechanics (LOCKED).
  </read_first>
  <behavior>
    - Bridge: inside InlineReviewCategoryCell, when `row.category_id === null` AND `row.suggestion?.categoryId` is non-null, resolve the name via `categories.find(c => c.id === row.suggestion!.categoryId)?.name`; pass `{ categoryId, name }` to `<SuggestionSlot onApply={...} />`. When `row.suggestion?.categoryId` is null OR there is no suggestion, pass null (or render the existing inert slot) so it shows the inert "—" exactly as v1.3.
    - onApply: clicking the chip calls the existing classify path for THIS row with the suggested category (`onClassify(row.id, categoryId, null)` — the same client-state setter the manual pick uses) so the Select fills and origin flips to a classified state. It writes nothing to the DB. confirmImport stays the sole merchant_patterns write.
    - Provenance badge (mutually exclusive per row), placed in the cell on the chip's reserved row (one wrap-friendly line, `flex flex-wrap items-center gap-1`, vertical order Select → badge → chip per UI-SPEC):
      · memória: `category_id` set AND `origin === 'memória'` → neutral pill "memória" (`bg-secondary text-secondary-foreground`), no icon, no chip.
      · IA: `suggestion` present, `suggestion.categoryId !== null`, AND `category_id === null` (unapplied) → gold pill "IA" (`bg-primary/10 text-primary`) + `Sparkles` size-3.
      · none: neither → no badge.
    - Every pill carries its text label (a11y — color never the sole signal).
  </behavior>
  <action>
    In InlineReviewCategoryCell, replace the always-inert `<SuggestionSlot />` with the bridged version: compute `const sug = row.suggestion?.categoryId ? { categoryId: row.suggestion.categoryId, name: categories.find(c => c.id === row.suggestion!.categoryId)?.name ?? row.suggestion.categoryId } : null` and render `<SuggestionSlot suggestion={sug} onApply={(catId) => onClassify(row.id, catId, null)} />` only on the `category_id === null` branch (memória/applied rows keep no chip). Add a small `ProvenanceBadge` inline span (a local function or inline JSX) per the token map above, using the KindBadge pill classes; render "memória" when `category_id` set + `origin === 'memória'`, "IA" (+ Sparkles) when there is an unapplied non-null suggestion on an unclassified row, nothing otherwise. Mutual exclusivity falls out of the `category_id === null` gate (memória rows have category_id set). Keep the cell wrapper reflow-free (reuse the reserved `min-h-5` slot height; do not introduce new spacing). Do NOT extend the SuggestionSlot prop type (the bridge keeps confidence/source out of it — preferred per CONTEXT discretion). Respect G-01: leave the `SelectValue` children (`CategoryBadge`) intact when the applied state fills the Select.

    Make the chip-on-ai, no-chip-on-none-fits, memoria-badge, and apply-no-commit test cases GREEN.
  </action>
  <verify>
    <automated>npx vitest run src/components/import-review-table.test.tsx -t "chip|memória|memoria|aplicar|apply|none.fits|nenhuma" 2>&1 | tail -20</automated>
  </verify>
  <done>
    A non-null-categoryId suggestion on an unclassified row renders "Aplicar sugestão: {name}" + the gold "IA" badge; clicking it fills the Select (CategoryBadge shows, chip gone) and calls NO import/confirm. A null-categoryId suggestion renders the inert "—" (no chip, no badge). A memória-classified row renders the neutral "memória" badge and no chip. Badges are mutually exclusive. confirmImport is untouched (grep: still the only merchant_patterns write).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: LOW_CONFIDENCE const + confidence tag + low-confidence-first initial sort — CLSAI-08</name>
  <files>src/components/import-review-table.tsx, src/components/import-review-table.test.tsx</files>
  <read_first>
    - src/components/import-review-table.tsx:194-196 (initial `sorting` state `[{ id: 'occurred_on', desc: true }]`), :262-265 (`visibleRows` memo), :427-437 (`useReactTable` with `getSortedRowModel` + `onSortingChange`), and InlineReviewCategoryCell (Task 2's edited cell) where the confidence tag joins the chip row.
    - 16-UI-SPEC.md §Copywriting "Confidence display rule (LOCKED)" (tag only, never a number, `LOW_CONFIDENCE = 0.6`, `confidence < 0.6`) + §Color "Confidence hint color" (`bg-consumption/10 text-consumption`) + §Interaction "Sort (LOCKED)" (low-confidence AI rows first, INITIAL only, ONLY when AI suggestions exist, user can re-sort).
    - src/components/category-badge.tsx KindBadge pill classes (reuse for the amber tag).
  </read_first>
  <behavior>
    - `LOW_CONFIDENCE = 0.6` as a single exported/module const (tunable). A row is low-confidence iff it has a suggestion with `confidence < LOW_CONFIDENCE`.
    - Confidence tag: in the cell (Task 2's chip row), when the row has an unapplied AI suggestion (`category_id === null` AND `suggestion?.categoryId !== null`) AND `suggestion.confidence < LOW_CONFIDENCE` → render the amber pill "baixa confiança" (`bg-consumption/10 text-consumption`, KindBadge markup). Never a number/percentage; never red. At/above threshold → no tag.
    - Initial sort: ONLY when at least one row in the upload has a non-null-categoryId AI suggestion, the grid's initial order puts low-confidence AI rows FIRST, then the existing order. Implement as an initial ordering of the data (a comparator that ranks low-confidence-AI rows above the rest while preserving the prior relative order for the tail) — NOT a forced/persisted sort, so the user can still click column headers to re-sort (`@tanstack/react-table` onSortingChange stays wired). With NO AI suggestions anywhere, the order is byte-identical to v1.3 (the comparator is a no-op / not applied).
  </behavior>
  <action>
    Add `const LOW_CONFIDENCE = 0.6` near the top of import-review-table.tsx (module scope, exported if a unit test imports it). In InlineReviewCategoryCell, append the "baixa confiança" amber pill to the chip row when the row is an unapplied low-confidence AI suggestion (gate on `category_id === null && row.suggestion?.categoryId != null && row.suggestion.confidence < LOW_CONFIDENCE`). Add a `lowConfidenceFirst` comparator (pure, exported for the unit edge) that returns rows reordered so low-confidence-AI rows lead; derive `hasAiSuggestions = rows.some(r => r.suggestion?.categoryId != null)` and apply the reordering to the `visibleRows`/initial data ONLY when `hasAiSuggestions` is true — leave `getSortedRowModel` + `onSortingChange` intact so user column-sorts still work and override the initial lead. Do NOT mutate `rows` state (keep the reorder in the memo feeding the table). Ensure the no-suggestions path leaves data order untouched (v1.3-identical).

    Make low-confidence-tag, low-confidence-first-sort, and no-suggestions-v1.3-identical GREEN. Then run the full file suite.
  </action>
  <verify>
    <automated>npx vitest run src/components/import-review-table.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>
    `LOW_CONFIDENCE = 0.6` is a single const. A `confidence < 0.6` unapplied-AI row shows the amber "baixa confiança" pill; `>= 0.6` shows none; the tag is never a number and never red. With AI suggestions present, low-confidence AI rows render first initially and the user can still re-sort by columns. With no suggestions, row order + cells are byte-identical to v1.3. All 7 cases in the file are GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted parsed_rows (jsonb) → client review grid | `statement.parsed_rows` (already RLS-scoped to the owner, written by the Phase 15 ingest) is read by the route and rendered. This phase only READS `suggestion`; it makes no new network/DB call and adds no new input boundary. |
| user click "Aplicar sugestão" → client state | Apply mutates client component state only (fills the Select). NO write crosses to the DB until the existing `confirmImport` (untouched), which remains the single merchant_patterns write behind explicit human confirmation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | "Aplicar sugestão" apply path | mitigate | onApply calls the existing client-state classify setter ONLY (`onClassify(row.id, …)`); it invokes no server action — the apply-no-commit test asserts confirmImport is never called on apply, pinning that no merchant_patterns write happens without explicit confirm. |
| T-16-02 | Information disclosure | suggestion bridge name resolution | accept | `suggestion.categoryId` is resolved to a name only from the user's own `categories` (already RLS-scoped, already in the grid). No cross-user data, no new fetch. Low risk. |
| T-16-03 | Tampering | confirmImport / import.ts pipeline | mitigate | Scope fence: this plan touches only the review-grid component, its test, and the route map's suggestion thread. NO change to import.ts/classify.ts/confirmImport — the learn loop stays the v1.3 path (verified by leaving those files untouched + grep "still the only merchant_patterns write"). |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (testing-library + vitest already vendored, no new deps). Legitimacy gate not applicable. |
</threat_model>

<verification>
- `npx vitest run src/components/import-review-table.test.tsx` — all 7 edges GREEN (chip-on-ai, no-chip-on-none-fits, memoria-badge, low-confidence-tag, low-confidence-first-sort, no-suggestions-v1.3-identical, apply-no-commit).
- `npm test` (full `vitest run`) — green, including the existing import.test.ts no-auto-commit pins (unchanged).
- `npx tsc --noEmit` — clean (the new `suggestion` field is optional/back-compat).
- `npm run build` — succeeds (route + component compile).
- Scope-fence grep: `git diff --name-only` lists ONLY `src/components/import-review-table.test.tsx`, `src/components/import-review-table.tsx`, `src/app/(app)/importar/[statementId]/page.tsx` (no import.ts/classify.ts/confirmImport, no schema/migration, no Settings).
- merchant_patterns write site unchanged: `grep -rn "merchant_patterns" src/actions/import.ts` still shows the single confirmImport write; no new write introduced.
</verification>

<success_criteria>
- CLSAI-07: each unclassified row with a non-null-categoryId AI suggestion shows the "Aplicar sugestão: {name}" chip + "IA" badge; memória-classified rows show the neutral "memória" badge and no chip; "nenhuma encaixa" (null categoryId) shows the inert "—"; applying fills the Select with NO commit.
- CLSAI-08: a per-row "baixa confiança" tag appears only when `confidence < LOW_CONFIDENCE` (0.6); low-confidence AI rows sort first on initial load when AI suggestions exist, and the user can re-sort.
- Empty-state parity: with no `row.suggestion` anywhere, the grid is byte-identical to v1.3 (no badges/chips/tags, original order).
- No-auto-commit + scope fence held: `confirmImport`/`import.ts`/`classify.ts` untouched; the only `merchant_patterns` write stays in `confirmImport`.
- Full suite + `tsc --noEmit` + `build` green.
</success_criteria>

<output>
Create `.planning/phases/16-review-grid-suggestion-affordances/16-01-SUMMARY.md` when done.
</output>
