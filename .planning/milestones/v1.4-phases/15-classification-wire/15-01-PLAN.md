---
phase: 15-classification-wire
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/ai/classify.ts
  - src/lib/ai/classify.test.ts
  - src/lib/parsers/types.ts
autonomous: true
requirements: [CLSAI-03, CLSAI-04, CLSAI-06]
must_haves:
  truths:
    - "classifyDescriptors makes exactly ONE doGenerate call for N>0 unique descriptors, ZERO for an empty input"
    - "An AI-returned categoryId not in the owned list resolves to null (enum-gated, never trusted)"
    - "Any provider error / malformed JSON / Zod failure returns an empty Map without throwing"
    - "The prompt sent to the model carries ONLY descriptor_norm strings — no amount/date/raw"
    - "ParsedReviewRow carries an optional suggestion hint that is never the category_id field"
  artifacts:
    - path: "src/lib/ai/classify.ts"
      provides: "classifyDescriptors batched doGenerate call, flat schema, enum-gate, try/catch fallback"
      exports: ["classifyDescriptors"]
      min_lines: 60
    - path: "src/lib/ai/classify.test.ts"
      provides: "RED-first unit coverage: 1-call, enum-drift, null escape, fallback paths, PII payload"
      min_lines: 80
    - path: "src/lib/parsers/types.ts"
      provides: "ParsedReviewRow.suggestion optional field"
      contains: "suggestion?"
  key_links:
    - from: "src/lib/ai/classify.ts"
      to: "src/lib/ai/provider-factory.ts"
      via: "modelFor(provider, model, apiKey) → LanguageModelV3.doGenerate"
      pattern: "modelFor\\("
    - from: "src/lib/ai/classify.ts"
      to: "src/lib/classifier/suggest.ts"
      via: "validateSuggestion gates each returned categoryId to an owned id"
      pattern: "validateSuggestion\\("
---

<objective>
Build the new `src/lib/ai/classify.ts` — `classifyDescriptors(descriptors, categories, aiSettings)` — the single batched, schema-constrained `doGenerate` call that is the only genuinely new code in Phase 15. It returns `Map<descriptorNorm, { categoryId, confidence }>`, gates every returned id through the existing `validateSuggestion`, and wraps the whole call+parse+validate in one inner `try/catch` → empty Map (never throws). Also extend `ParsedReviewRow` with the optional `suggestion` field that Plan 02 attaches.

Purpose: This is the load-bearing seam body. Plan 02 (the `import.ts` wire + `suggestCategory` delegation) depends on this module and the new type. Building it TDD-first locks the four CONTEXT invariants this plan owns — one batched call (CLSAI-03), enum-gate (CLSAI-04), graceful fallback + PII-only-descriptorNorm (CLSAI-06/SEC-03) — before the wire consumes it.

Output: `src/lib/ai/classify.ts` (+ `classify.test.ts` RED→GREEN), and the `suggestion?` field on `ParsedReviewRow`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-classification-wire/15-RESEARCH.md
@.planning/phases/15-classification-wire/15-CONTEXT.md

# Existing modules this plan reuses as-is (read for exact signatures, do NOT modify):
@src/lib/ai/provider-factory.ts
@src/lib/ai/settings.ts
@src/lib/classifier/suggest.ts
@src/lib/parsers/types.ts
</context>

<artifacts_this_phase_produces>
This plan produces two of the four phase artifacts the downstream consumer needs:
- **`classifyDescriptors(descriptors: string[], categories: {id,name}[], aiSettings: {provider, model, apiKey})`** → `Promise<Map<string, { categoryId: string | null; confidence: number }>>` — in `src/lib/ai/classify.ts`.
- **The classify output schema** — flat Zod `{ results: [{ descriptor, categoryId: string|null, confidence: number }] }`, converted to a flat `$ref`-free JSONSchema7 for `responseFormat.schema`.
- **`ParsedReviewRow.suggestion?: { categoryId: string | null; confidence: number; source: 'ia' }`** — in `src/lib/parsers/types.ts` (additive jsonb field).

(Plan 02 produces the remaining two: the `import.ts` two-pass wire and the `suggestCategory` wrapper rewrite.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend ParsedReviewRow with the optional suggestion hint (CLSAI-01 seam)</name>
  <files>src/lib/parsers/types.ts</files>
  <read_first>
    - src/lib/parsers/types.ts (the ParsedReviewRow interface — extend it additively, mirror the existing JSDoc grammar on duplicate?/category_id)
  </read_first>
  <action>
    Add one optional field to the `ParsedReviewRow` interface (after `duplicate?`): `suggestion?: { categoryId: string | null; confidence: number; source: 'ia' }`. Add a JSDoc comment in the existing pt-context style noting it is the non-binding AI hint for a memory-miss row, that it is NEVER applied to `category_id` (no auto-commit, the hint stays a hint), and that Phase 16 renders it while the user applies it on the grid. It is optional because old persisted rows lack it and the pure parsers never set it. Do NOT add any other field, do NOT touch `RawTransaction`, `ParseResult`, or `ClassificationSource`. Keep `category_id`/`reserva_id`/`classification_source` exactly as they are.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>`ParsedReviewRow` compiles with a `suggestion?: { categoryId: string | null; confidence: number; source: 'ia' }` field; `npx tsc --noEmit` passes; no other interface changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED — write classify.test.ts (1-call, enum-drift→null, null escape, fallback, PII payload)</name>
  <files>src/lib/ai/classify.test.ts</files>
  <read_first>
    - src/lib/ai/provider-factory.ts (modelFor signature — the test mocks this module so doGenerate is a spy; the real factory is never hit)
    - src/lib/classifier/suggest.ts (validateSuggestion — the real enum gate runs for real inside classify; the test passes real owned ids + a non-owned id)
    - src/lib/ai/settings.ts (DEFAULT_MODEL, AiProvider — for a fake aiSettings object)
    - .planning/phases/15-classification-wire/15-RESEARCH.md (Pattern 1 build-accurate doGenerate result shape: result.content is an array; the structured JSON arrives as ONE { type:'text', text } part on BOTH providers — the test's doGenerate mock returns exactly that)
    - .planning/phases/15-classification-wire/15-VALIDATION.md (the per-task verify map — this test covers edges enum-drift→null, fallback-error, 1-call-N-unique, PII-descriptorNorm-only)
  </read_first>
  <behavior>
    - vi.mock('@/lib/ai/provider-factory') so modelFor returns a fake LanguageModelV3 whose doGenerate is a vi.fn the test controls; restore between tests.
    - Test (1-call-N-unique, CLSAI-03): call classifyDescriptors with 3 unique descriptors → doGenerate called exactly ONCE; the prompt it received lists all 3 descriptors.
    - Test (empty input → 0 calls): classifyDescriptors([], cats, settings) and classifyDescriptors(['x'], [], settings) both return an empty Map and doGenerate is NOT called (guards before the call).
    - Test (happy path): doGenerate resolves content [{ type:'text', text: JSON.stringify({ results: [{ descriptor:'padaria sao joao', categoryId: MERCADO_ID, confidence: 0.9 }] }) }] → Map has descriptor → { categoryId: MERCADO_ID, confidence: 0.9 }.
    - Test (enum-drift→null, CLSAI-04): model returns a categoryId NOT in the owned list → the Map entry's categoryId is null (validateSuggestion gate ran), confidence preserved.
    - Test (null escape, CLSAI-04): model returns categoryId: null ("nothing fits") → Map entry categoryId null, no throw.
    - Test (fallback-error, CLSAI-06): doGenerate rejects (throw) → returns empty Map, no throw. Also: doGenerate resolves a text part that is malformed JSON → empty Map. Also: valid JSON failing the flat Zod schema → empty Map.
    - Test (PII-descriptorNorm-only, SEC-03): inspect the prompt passed to doGenerate; it contains the descriptor_norm strings and the `id: name` category lines, and contains NO amount/currency/date/raw-descriptor tokens (assert .not.toMatch on a R$/dd-mm-yyyy/amount/occurred_on/descriptor_raw pattern).
    Use real UUID-shaped CATEGORIES (mirror suggest.test.ts ids) and a FAKE_SETTINGS = { provider:'gemini', model: DEFAULT_MODEL.gemini, apiKey:'fake' }.
  </behavior>
  <action>
    Write `src/lib/ai/classify.test.ts` as the RED scaffold described in `<behavior>`. Import `classifyDescriptors` from `./classify` (it does not exist yet → this file fails to import/compile, which is the intended RED state for Task 3). Mock `@/lib/ai/provider-factory` so `modelFor` returns `{ doGenerate: vi.fn() }`; control its resolved/rejected value per test. Do NOT mock `validateSuggestion` — the real enum gate must run (that is the CLSAI-04 assertion). Keep tests fast (no real network, no Supabase). Follow the existing vitest grammar in `src/lib/classifier/suggest.test.ts` (describe/it, afterEach restore). This is RED-first: do not write `classify.ts` in this task.
  </action>
  <verify>
    <automated>npx vitest run src/lib/ai/classify.test.ts 2>&1 | grep -Eq "Cannot find module|Failed to resolve import|No test|fail" && echo "RED as expected"</automated>
  </verify>
  <done>`classify.test.ts` exists with the seven behaviors from `<behavior>`; the run is RED because `./classify` does not yet export `classifyDescriptors` (or its tests fail). Commit as `test(15-01): add failing classify tests`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN — implement classify.ts (doGenerate + flat schema + enum-gate + try/catch→empty Map)</name>
  <files>src/lib/ai/classify.ts</files>
  <read_first>
    - .planning/phases/15-classification-wire/15-RESEARCH.md (Patterns 1/2/3/5 — the build-accurate doGenerate call shape: prompt is the internal LanguageModelV3Prompt [{role:'system',content:STRING},{role:'user',content:[{type:'text',text}]}], responseFormat {type:'json',schema} with schema REQUIRED for Anthropic, maxOutputTokens, temperature:0; extract result.content.find(c=>c.type==='text') → JSON.parse; flat Zod schema; per-result validateSuggestion gate; inner try/catch→empty Map, NO retries)
    - src/lib/ai/provider-factory.ts (modelFor — instantiate the model from aiSettings)
    - src/lib/classifier/suggest.ts (validateSuggestion — call per returned categoryId)
    - src/lib/schemas/ai-settings.ts (AiProvider type for the aiSettings param)
    - src/lib/ai/classify.test.ts (the contract this must satisfy — written in Task 2)
  </read_first>
  <action>
    Implement `classifyDescriptors(descriptors: string[], categories: { id: string; name: string }[], aiSettings: { provider: AiProvider; model: string; apiKey: string })` returning `Promise<Map<string, { categoryId: string | null; confidence: number }>>`.

    Guard first: if `descriptors.length === 0 || categories.length === 0`, return an empty Map WITHOUT instantiating a model or calling doGenerate (this is the CLSAI-03 zero-call path and an input guard).

    Build a flat Zod schema `{ results: [{ descriptor: string, categoryId: string().nullable(), confidence: number().min(0).max(1) }] }`. `categoryId` is a FREE string in-schema — never a UUID enum (the multi-provider trap per RESEARCH Pattern 2). Convert it to a flat, $ref-free JSONSchema7 for `responseFormat.schema`; if the project's zod→JSON-schema path emits `$ref`/`$defs`, hand-write the tiny JSONSchema7 literal instead (RESEARCH Open Q2 / Assumption A5 — Claude requires a flat schema). Anthropic requires a non-null schema (RESEARCH Pattern 1) — always pass one.

    Build the prompt PII-safely: a system instruction (pt-BR, per RESEARCH Pattern 3) and a user text containing ONLY the category `id: name` lines and the list of normalized descriptors. NEVER include amount/date/raw descriptor (SEC-03). The user descriptors array is `descriptor_norm` strings only — do not accept or forward any other row field.

    Wrap the ENTIRE call+extract+parse+validate in one `try/catch`: `modelFor(...).doGenerate({ prompt, responseFormat:{type:'json',schema}, maxOutputTokens, temperature:0 })` → `result.content.find(c=>c.type==='text')` → `JSON.parse` → flat-schema parse → for each result, set `out.set(r.descriptor, { categoryId: validateSuggestion(r.categoryId, categories), confidence: r.confidence })`. On ANY throw (provider error, JSON.parse, schema failure), log a generic server-side message (NO key, NO raw provider body — V7) and `return new Map()`. NO retries (no maxRetries; an unrecoverable parse degrades to manual). Add a head comment tying the file to CLSAI-03/04/06 + SEC-03 in the existing module grammar — describe the invariants by concept, do not paste any acceptance-criteria literal that a negative grep would match.
  </action>
  <verify>
    <automated>npx vitest run src/lib/ai/classify.test.ts</automated>
  </verify>
  <done>All `classify.test.ts` behaviors pass GREEN: one batched call for N>0 unique descriptors, zero for empty input, enum-drift→null, null escape, fallback→empty Map on throw/malformed, and the prompt carries only descriptor_norm. `npx tsc --noEmit` passes. Commit as `feat(15-01): implement classifyDescriptors batched call`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| import action → LLM provider | `descriptor_norm` strings cross the server→third-party-LLM boundary (BYOK key). Untrusted model output crosses back. |
| model output → row data | The model's `categoryId`/`confidence` are untrusted until enum-gated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Information Disclosure | classify.ts prompt builder | mitigate | `buildUserText` takes `string[]` (descriptor_norm) + `id: name` lines only; classify.test.ts asserts the doGenerate prompt has no amount/date/raw (SEC-03 / LGPD) |
| T-15-02 | Tampering | classify.ts categoryId handling | mitigate | every returned `categoryId` runs through `validateSuggestion` (z.enum over owned ids) → null if invented/renamed; a prompt-injection descriptor can at worst yield a rejected value |
| T-15-03 | Denial of Service | classify.ts call boundary | mitigate | the whole call+parse+validate is in one inner `try/catch` → empty Map, no retries; an empty Map is "no suggestions", never a throw |
| T-15-04 | Information Disclosure | classify.ts error log | mitigate | catch logs a generic message — never the key or raw provider body (V7) |
| T-15-SC | Tampering | npm installs | accept | Phase 15 installs ZERO packages (all @ai-sdk deps vetted + installed in Phase 14; RESEARCH Package Legitimacy Audit) — no install task, no checkpoint required |
</threat_model>

<verification>
- `npx vitest run src/lib/ai/classify.test.ts` — all classify behaviors GREEN.
- `npx tsc --noEmit` — the new `suggestion?` field + classify.ts typecheck clean.
- Per-task fast subset: `npx vitest run src/lib/ai src/lib/classifier`.
- No new package added: `node -e "const p=require('./package.json');const d={...p.dependencies,...p.devDependencies};console.log(Object.keys(d).filter(k=>k==='ai'||k.startsWith('@ai-sdk')).sort())"` → `[ '@ai-sdk/anthropic', '@ai-sdk/google' ]` (no `ai`).
</verification>

<success_criteria>
- `classifyDescriptors` exists, makes exactly ONE `doGenerate` call for N>0 unique descriptors and ZERO for empty input (CLSAI-03).
- Every model-returned `categoryId` is enum-gated via `validateSuggestion` → null if not owned (CLSAI-04).
- Any provider error / malformed JSON / schema failure returns an empty Map without throwing (CLSAI-06).
- The prompt carries ONLY `descriptor_norm` strings + `id: name` lines — no PII (SEC-03).
- `ParsedReviewRow` carries an additive optional `suggestion` field, never written to `category_id`.
</success_criteria>

<output>
Create `.planning/phases/15-classification-wire/15-01-SUMMARY.md` when done.
</output>
