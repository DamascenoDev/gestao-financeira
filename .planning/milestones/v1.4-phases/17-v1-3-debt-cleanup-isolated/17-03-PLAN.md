---
phase: 17-v1-3-debt-cleanup-isolated
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/17-v1-3-debt-cleanup-isolated/17-SC3-DELETE-RUNBOOK.md
autonomous: true
requirements: [DEBT-05]

must_haves:
  truths:
    - "A committed safety runbook exists that a human can follow to execute the destructive throwaway-account delete on production with all five mandatory guard-rails in order"
  artifacts:
    - path: ".planning/phases/17-v1-3-debt-cleanup-isolated/17-SC3-DELETE-RUNBOOK.md"
      provides: "Exact ordered human-executed runbook for the SC3 destructive PROD delete with five guard-rails"
      contains: "APAGAR"
  key_links:
    - from: ".planning/phases/17-v1-3-debt-cleanup-isolated/17-SC3-DELETE-RUNBOOK.md"
      to: "production /conta Apagar conta gate + Supabase RLS cascade"
      via: "ordered guard-rails: DB backup → throwaway user_id → double-confirm APAGAR gate → PROD-site-only → verify RLS-scoped cascade"
      pattern: "backup|throwaway|APAGAR|RLS"
---

<objective>
Author the SC3 destructive-delete SAFETY RUNBOOK as a committed doc artifact. The agent NEVER runs the delete — it produces an exact, ordered, human-executable runbook. This is the autonomous doc-write half of DEBT-05 (plan 17-04 is the human execution against the runbook).

Purpose: The SC3 step is a destructive account delete on PRODUCTION, and the dev server points at the PROD Supabase — so the protocol must be precise and the human, not the agent, must pull the trigger. Per CONTEXT decision "You run it, my runbook" with five mandatory ordered guard-rails.
Output: `.planning/phases/17-v1-3-debt-cleanup-isolated/17-SC3-DELETE-RUNBOOK.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-v1-3-debt-cleanup-isolated/17-CONTEXT.md
@.planning/phases/12-produ-o-live-verify/12-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the SC3 destructive-delete safety runbook</name>
  <files>.planning/phases/17-v1-3-debt-cleanup-isolated/17-SC3-DELETE-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/17-v1-3-debt-cleanup-isolated/17-CONTEXT.md (the five mandatory ordered guard-rails in the "SC3 Destructive Delete Safety Protocol" decision: 1 DB backup BEFORE; 2 throwaway user_id explicitly created + confirmed; 3 double-confirm the type-to-APAGAR gate; 4 ONLY via the live PROD site, NEVER the dev server which points at PROD; 5 verify the RLS cascade was scoped to the throwaway user)
    - .planning/phases/12-produ-o-live-verify/12-07-SUMMARY.md (proven mechanism: `/conta` "Apagar conta" dialog focuses Cancelar, the button enables ONLY on the exact "APAGAR"; SEC-01 already confirmed — service-role secret stays server-side; the throwaway-delete EXECUTION was the deferred DATA-02 item this runbook drives)
  </read_first>
  <action>
    Create `.planning/phases/17-v1-3-debt-cleanup-isolated/17-SC3-DELETE-RUNBOOK.md` as a numbered, ordered, human-executable runbook. The doc MUST encode the five guard-rails IN ORDER as discrete numbered steps, each with an explicit confirmation gate the human ticks before proceeding to the next:
    1. **DB backup taken BEFORE the delete** — capture a Supabase backup (point-in-time / dashboard backup) and record the backup identifier/timestamp; do not proceed until the backup is confirmed.
    2. **Throwaway user_id explicitly created + confirmed** — sign up a FRESH throwaway account on production (NOT the personal account), add one trivial row, and record the throwaway `user_id`; explicitly confirm it is the throwaway, not the personal user_id, before any delete.
    3. **Double-confirm the type-to-APAGAR gate** — on `/conta` open "Apagar conta"; verify the dialog focuses Cancelar and the button is disabled until the input is exactly the string APAGAR; require the human to re-read the consequences list and double-confirm they are signed in as the THROWAWAY user before typing APAGAR.
    4. **Run ONLY via the live PROD site, NEVER the dev server** — state explicitly that the dev server (`npm run dev`) points at the PROD Supabase, so the delete must be performed only through the live production UI at https://gestao-financeira-ebon-mu.vercel.app/ ; forbid running it via dev server or any direct DB call.
    5. **Verify the RLS cascade was scoped to the throwaway user** — after the delete: confirm "Apagando…" → sign-out to `/auth/login`; the throwaway account can no longer sign in; the PERSONAL account still signs in with all data intact (surgical, RLS-scoped cascade). Record the post-delete verification.
    Include at the top: the exact PROD URL https://gestao-financeira-ebon-mu.vercel.app/ , the exact gate string APAGAR, a bold "THE AGENT NEVER RUNS THIS — the human executes each step" banner, an ABORT/rollback note (if anything looks wrong, stop and restore from the step-1 backup), and an explicit "if you choose to defer the destructive delete, Phase 17 stays open only on DATA-02" note (matches CONTEXT Deferred Ideas). Do NOT include fenced code blocks; use numbered prose + checkboxes.
  </action>
  <verify>
    <automated>test -f .planning/phases/17-v1-3-debt-cleanup-isolated/17-SC3-DELETE-RUNBOOK.md && grep -q 'APAGAR' .planning/phases/17-v1-3-debt-cleanup-isolated/17-SC3-DELETE-RUNBOOK.md && grep -q 'gestao-financeira-ebon-mu.vercel.app' .planning/phases/17-v1-3-debt-cleanup-isolated/17-SC3-DELETE-RUNBOOK.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/17-v1-3-debt-cleanup-isolated/17-SC3-DELETE-RUNBOOK.md` exists.
    - It contains five ordered numbered guard-rail steps in this order: (1) DB backup before, (2) throwaway user_id created + confirmed, (3) double-confirm the type-to-APAGAR gate, (4) PROD-site-only / never dev server, (5) verify RLS-scoped cascade post-delete.
    - It contains the literal gate string `APAGAR`.
    - It contains the exact PROD URL `https://gestao-financeira-ebon-mu.vercel.app/`.
    - It explicitly states the agent never runs the delete and the human executes each step.
    - It explicitly warns the dev server points at PROD Supabase, so the delete must be done only via the live production UI.
    - It includes an abort/rollback note referencing the step-1 backup.
  </acceptance_criteria>
  <done>The SC3 safety runbook exists as a committed doc with all five guard-rails in order, the exact PROD URL, the exact APAGAR gate string, the agent-never-runs banner, and the abort/rollback note.</done>
</task>

</tasks>

<verification>
- The runbook file exists and encodes all five guard-rails in order.
- It carries the exact PROD URL and the exact APAGAR gate string.
- It is unambiguous that the human (not the agent) executes the destructive step.
</verification>

<success_criteria>
The doc half of DEBT-05 is satisfied: an exact, ordered, human-executable SC3 delete safety runbook exists as a committed artifact, ready for the human to follow in plan 17-04.
</success_criteria>

<output>
Create `.planning/phases/17-v1-3-debt-cleanup-isolated/17-03-SUMMARY.md` when done.
</output>
