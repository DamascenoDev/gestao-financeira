---
phase: 17-v1-3-debt-cleanup-isolated
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [DEBT-03, DEBT-04]

must_haves:
  truths:
    - "The G-07/G-08 cosmetic fixes (import-grid carro label shows 'Nenhum' not '__none__'; all-duplicate re-import shows the 'already in extrato' copy not '0 importadas') are observable in the LIVE production bundle — and if NOT observed, DEBT-03 stays open pending a redeploy of 2ae93fb (separate plan)"
    - "The G-08 all-duplicate re-confirm is proven non-destructive by an OBSERVED before/after PROD row count (N_after == N_before), not assumed"
    - "DEBT-04 sign-off: the MEI dasn-2026.csv downloaded from production opens with the correct UTF-8 BOM / ';' delimiter / pt-BR comma-decimal formatting (the data-bundle JSON is a DATA-01 companion, NOT required for DEBT-04)"
  artifacts: []
  key_links:
    - from: "production import review grid (/importar/[id])"
      to: "commit 2ae93fb (InlineReviewCarroCell items map + confirmToastMessage)"
      via: "live read-only browser MCP inspection of the served bundle"
      pattern: "Nenhum|j[aá] estavam no extrato"
    - from: "production /mei/relatorio Exportar CSV"
      to: "downloaded dasn-2026.csv file content"
      via: "non-destructive download + spreadsheet/text inspection of encoding + delimiter + decimals"
      pattern: "BOM|;|R\\$"
---

<objective>
Confirm two NON-DESTRUCTIVE live-production outcomes that close DEBT-03 and DEBT-04:
- **SC1 / DEBT-03:** the G-07/G-08 cosmetic fixes from commit `2ae93fb` are present in the LIVE production bundle (read-only check — no deploy, no writes). `2ae93fb` is an ancestor of both HEAD and the prod hotfix `97366e5`, so it is presumably already in the served bundle and this is confirm-only. FALLBACK: if the live bundle still shows OLD G-07/G-08 behavior, DEBT-03 is NOT closed by this plan — a redeploy of `2ae93fb` is required and must be owned by a separate redeploy plan (do not mark DEBT-03 done on a "needs redeploy" finding).
- **SC2 / DEBT-04:** strictly the MEI `dasn-2026.csv` content walkthrough — the download opens with the correct pt-BR encoding the 12-06 walkthrough left unopened (UTF-8 BOM, `;` delimiter, comma decimals). The LGPD data-bundle JSON inspection runs alongside as a non-destructive **DATA-01 companion** but is **NOT required for DEBT-04 sign-off**; DEBT-04 pass/fail hinges ONLY on the dasn-2026.csv assertion.

Purpose: These are live PROD reads the executor subagent cannot reliably drive in an isolated worktree (a shared browser session). The ORCHESTRATOR drives Chrome DevTools MCP during the verification step against the production URL; the USER confirms the observed results. No feature code, no destructive action. The one PROD-account write risk (re-confirming an all-duplicate import for G-08) is guarded by an OBSERVED before/after row-count check — non-destructiveness is proven, not assumed.
Output: live evidence recorded in this plan's SUMMARY (G-07/G-08 confirmed in prod bundle + observed unchanged row count; dasn-2026.csv content confirmed; data-bundle JSON noted as DATA-01 companion).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-v1-3-debt-cleanup-isolated/17-CONTEXT.md
@.planning/phases/12-produ-o-live-verify/12-VERIFICATION.md
@.planning/phases/12-produ-o-live-verify/12-06-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Confirm G-07/G-08 cosmetic fixes live in the production bundle (SC1 / DEBT-03)</name>
  <read_first>
    - .planning/phases/12-produ-o-live-verify/12-VERIFICATION.md (G-07 = import-grid carro select rendered raw `__none__`; G-08 = all-duplicate re-import toast read "0 transações importadas". Both FIXED in quick-task commit `2ae93fb`, GREEN locally, "Needs redeploy". Commit is ancestor of HEAD + of prod hotfix `97366e5` so it is presumably already in the served bundle — this step CONFIRMS that.)
  </read_first>
  <what-built>
    No new code. The orchestrator drives Chrome DevTools MCP (read-only) against the live production site to confirm the already-committed G-07/G-08 fixes (commit `2ae93fb`) are in the served bundle.
  </what-built>
  <action>
    No code is written. The orchestrator drives Chrome DevTools MCP (read-only) against https://gestao-financeira-ebon-mu.vercel.app/ to observe the import-grid carro select label and the all-duplicate re-import toast, then surfaces the results for the user to confirm. Do NOT deploy, write, or run any destructive action. Follow the steps in <human_verification>.
  </action>
  <human_verification>
    Driven by the orchestrator via Chrome DevTools MCP against the production URL (read-only — NO writes, NO destructive action). Use the separate browser profile per the dev-env-testing-gotchas memory.

    PROD URL: https://gestao-financeira-ebon-mu.vercel.app/

    G-07 (import-grid carro label):
    1. Sign in to production with the personal account.
    2. Open an existing imported statement review grid at `/importar/{statementId}` (or upload a small OFX/CSV to reach the review grid).
    3. In the review grid, inspect the per-row "Vincular a carro" select trigger when set to no car. EXPECTED: the collapsed trigger renders "Nenhum" — NOT the raw sentinel string "__none__".

    G-08 (all-duplicate re-import toast) — PROD-DATA-WRITE SAFETY, observed non-destructive:
    This step re-confirms an import on the personal PROD account. "No new rows are written" is the EXPECTED behavior of dedup, but it is NOT assumed — it is OBSERVED via a before/after count. If dedup does NOT fully skip (exactly the bug-class G-08 lives near), this write would corrupt the personal PROD account, so the count guard is mandatory.

    4. CHOOSE TARGET: pick a statement that is KNOWN to be already fully imported (every row a guaranteed duplicate) — e.g. the 22-row Nubank OFX from the 12-05 walkthrough that is already in the extrato. Do NOT pick a partially-imported or fresh statement.
    5. RECORD BEFORE-COUNT (pre-step, mandatory): before re-confirming, record the account's transaction/row count — read the transactions total on `/extrato` (or the visible row count for the target period). Note this number as `N_before`.
    6. RE-CONFIRM: re-confirm the all-duplicate import. EXPECTED toast: a success-tone "Todas as N transações já estavam no extrato" (or the partial "N importadas (D já existiam)" form). The toast must NOT read "0 transações importadas".
    7. RECORD AFTER-COUNT + ASSERT UNCHANGED (mandatory acceptance): re-read the same transaction/row count as `N_after`. ASSERT `N_after == N_before`. If `N_after > N_before`, dedup did NOT fully skip — STOP, do not approve, and flag that re-confirm wrote rows on PROD (a real G-08-adjacent defect, not a cosmetic one).

    This is a presentation read PLUS an observed-count safety check — the orchestrator must observe the unchanged count as acceptance, not assume it.
  </human_verification>
  <verify>
    <human-check>Orchestrator confirms via MCP, user confirms: import-grid carro select shows "Nenhum" (not "__none__") AND an all-duplicate re-import shows the "já estavam no extrato" copy (not "0 transações importadas") on the LIVE production bundle AND the orchestrator OBSERVED the PROD transaction/row count UNCHANGED (`N_after == N_before`) across the re-confirm.</human-check>
  </verify>
  <acceptance_criteria>
    - On https://gestao-financeira-ebon-mu.vercel.app/ the import review grid carro select trigger renders the label "Nenhum", never the literal string "__none__".
    - On the same production site, confirming an all-duplicate import shows a toast containing "já estavam no extrato" (or the partial-import "já existiam" copy), and does NOT show the exact string "0 transações importadas".
    - OBSERVED non-destructive guarantee: the PROD account transaction/row count recorded BEFORE the re-confirm equals the count recorded AFTER (`N_after == N_before`). The orchestrator must observe this unchanged count — it is acceptance, not an assumption. A larger after-count means re-confirm wrote rows and is a defect, not a pass.
    - No deploy, no destructive action were performed during this confirmation (and the observed count proves no rows were written).
    - FALLBACK (SC1/DEBT-03 redeploy): if G-07 and/or G-08 are NOT observed in the live bundle (old behavior still served), DEBT-03 is NOT closed by this plan — a redeploy of commit `2ae93fb` is genuinely required and must be owned by a separate redeploy plan. Do NOT mark DEBT-03 done on a "needs redeploy" finding.
  </acceptance_criteria>
  <resume-signal>Type "approved" once both G-07 and G-08 are confirmed in the live production bundle AND the orchestrator observed the PROD row count unchanged across the re-confirm; OR describe what still shows the old behavior. If old behavior is still served, DEBT-03 is NOT closed here — a redeploy plan for `2ae93fb` is required.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Confirm MEI dasn-2026.csv (+ data-bundle JSON) download content/encoding (SC2 / DEBT-04)</name>
  <read_first>
    - .planning/phases/12-produ-o-live-verify/12-06-SUMMARY.md (MEI-01..06 confirmed live; the ONLY residual: the downloaded `dasn-2026.csv` content — BOM/`;`/pt-BR — "was not opened here". This step opens it. **DEBT-04 is exactly this dasn-2026.csv content walkthrough — its pass/fail hinges ONLY on the CSV assertion.**)
    - .planning/phases/12-produ-o-live-verify/12-07-SUMMARY.md (DATA-01 transactions CSV + DATA-02 export-bundle JSON content also left as hands-on; confirming the data-bundle JSON download content here is a non-destructive **DATA-01 companion** — it complements the CSV check but is **NOT part of DEBT-04 sign-off**.)
  </read_first>
  <what-built>
    No new code. The orchestrator drives the MEI DASN CSV export (and the LGPD data-bundle JSON export) download via Chrome DevTools MCP against production, then the file CONTENT is inspected for correct pt-BR encoding. Downloads are non-destructive.
  </what-built>
  <action>
    No code is written. The orchestrator drives Chrome DevTools MCP against https://gestao-financeira-ebon-mu.vercel.app/ to trigger the MEI DASN CSV download (`/mei/relatorio` → Exportar CSV) and the LGPD data-bundle JSON download (`/conta` → Baixar meus dados), then the user opens the downloaded files and confirms the pt-BR encoding (UTF-8 BOM, `;` delimiter, comma decimals) and JSON structure. Downloads only — NO account delete, NO destructive action. Follow the steps in <human_verification>.
  </action>
  <human_verification>
    Driven by the orchestrator via Chrome DevTools MCP against the production URL (non-destructive — downloads only, NO delete). Separate browser profile.

    PROD URL: https://gestao-financeira-ebon-mu.vercel.app/

    MEI DASN CSV (the 12-06 gap):
    1. Sign in to production; go to `/mei/relatorio`.
    2. Click "Exportar CSV". A file named `dasn-2026.csv` (or `dasn-{year}.csv` for the selected year) downloads.
    3. Open the downloaded `dasn-2026.csv` in a spreadsheet AND a plain-text/hex viewer. CONFIRM: a UTF-8 BOM at the start (no mojibake when opened in Excel/Sheets), `;` as the column delimiter, comma decimal separator in BRL values (e.g. `1.234,56`), and the expected DASN rows (receita bruta total + Comércio/Indústria + Serviços split).

    LGPD data-bundle JSON — DATA-01 COMPANION, **NOT required for DEBT-04 sign-off**:
    The steps below are a non-destructive companion check for the DATA-01/DATA-02 export surface. They are explicitly OUT OF SCOPE for DEBT-04's pass/fail — DEBT-04 hinges ONLY on the `dasn-2026.csv` assertion in steps 1-3 above. Run them while we are confirming download content (same non-destructive class), but a JSON-section discrepancy does NOT fail DEBT-04 (it would be a separate DATA-01 finding).
    4. Go to `/conta`; click "Baixar meus dados". Open the downloaded `meus-dados-{yyyy-MM-dd}.json`. CONFIRM it contains a `tables` object (receitas, transações, categorias, metas, reservas, MEI, padrões, statements metadata) and a `csv` section.
    5. (Optional, same non-destructive class) click "Exportar transações (CSV)" on `/extrato` or `/conta`; open `transacoes-{yyyy-MM}.csv` and confirm BOM + `;` columns + comma decimals + resolved category NAMES (DATA-01).

    NOTE: The destructive account DELETE (DATA-02 delete) is NOT part of this plan — it is plan 17-04 under the safety runbook. Do nothing destructive here.
  </human_verification>
  <verify>
    <human-check>DEBT-04 sign-off (required): orchestrator drives the dasn-2026.csv download via MCP, user opens the file and confirms UTF-8 BOM + ';' delimiter + comma decimals + correct DASN rows. DATA-01 companion (NOT required for DEBT-04): meus-dados JSON has tables + csv sections.</human-check>
  </verify>
  <acceptance_criteria>
    DEBT-04 sign-off hinges ONLY on the dasn-2026.csv assertion:
    - The downloaded `dasn-2026.csv` from production `/mei/relatorio` opens with a UTF-8 BOM (no mojibake), `;` column delimiter, and comma decimal separators in pt-BR BRL values.
    - The `dasn-2026.csv` rows match the on-screen DASN figures (receita bruta total + Comércio/Indústria + Serviços split) the 12-06 walkthrough verified.
    DATA-01 companion (informational — does NOT gate DEBT-04 pass/fail):
    - The `meus-dados-{date}.json` from `/conta` "Baixar meus dados" contains a `tables` object and a `csv` section. A discrepancy here is a separate DATA-01 finding, not a DEBT-04 failure.
    - Only downloads were performed — no account deletion, no destructive action.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the dasn-2026.csv content + encoding (UTF-8 BOM + `;` + comma decimals) is confirmed from the downloaded file — that alone closes DEBT-04. Note the data-bundle JSON structure result separately (DATA-01 companion, does not block DEBT-04), or describe the dasn CSV formatting/content discrepancy.</resume-signal>
</task>

</tasks>

<verification>
- SC1: G-07 + G-08 cosmetic fixes confirmed present in the live production bundle (read-only). FALLBACK: if NOT observed in the live bundle, DEBT-03 is NOT closed here — a redeploy of `2ae93fb` is required (separate plan).
- SC1 safety: the G-08 re-confirm was proven non-destructive by an OBSERVED unchanged PROD row count (`N_after == N_before`), not assumed.
- SC2 / DEBT-04: dasn-2026.csv content/encoding confirmed (UTF-8 BOM / `;` / pt-BR comma decimals + matching DASN rows). This alone closes DEBT-04.
- DATA-01 companion (does NOT gate DEBT-04): data-bundle JSON structure noted — non-destructive.
- No deploy, no writes (proven by count), no delete performed in this plan.
</verification>

<success_criteria>
DEBT-04 satisfied iff the dasn-2026.csv download content/encoding is confirmed live (UTF-8 BOM + `;` + comma decimals + matching DASN rows) — the data-bundle JSON is a DATA-01 companion and does not gate DEBT-04.
DEBT-03 satisfied iff the G-07/G-08 fixes are CONFIRMED present in the live PROD bundle AND the G-08 re-confirm was observed non-destructive (PROD row count unchanged). If the live bundle still serves old G-07/G-08 behavior, DEBT-03 is NOT closed by this plan — a redeploy of `2ae93fb` is required (separate redeploy plan); do not mark DEBT-03 done on a "needs redeploy" finding.
</success_criteria>

<output>
Create `.planning/phases/17-v1-3-debt-cleanup-isolated/17-02-SUMMARY.md` when done, recording the live evidence for G-07/G-08 and the dasn CSV / data-bundle JSON content.
</output>
