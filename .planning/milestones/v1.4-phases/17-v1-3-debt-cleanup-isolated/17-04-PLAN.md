---
phase: 17-v1-3-debt-cleanup-isolated
plan: 04
type: execute
wave: 2
depends_on: ["17-03"]
files_modified: []
autonomous: false
requirements: [DEBT-05]

must_haves:
  truths:
    - "The destructive throwaway-account delete (DATA-02) was executed on production by the human under the runbook, with a DB backup taken first, a confirmed throwaway user_id, a double-confirmed APAGAR gate, via the live PROD site only, and a verified RLS-scoped cascade leaving the personal account intact"
  artifacts: []
  key_links:
    - from: "17-SC3-DELETE-RUNBOOK.md"
      to: "production /conta Apagar conta destructive delete (DATA-02)"
      via: "human executes the runbook end-to-end; cascade verified scoped to the throwaway user_id"
      pattern: "APAGAR|throwaway|RLS"
---

<objective>
Execute the SC3 destructive throwaway-account delete (DATA-02) on production by following the safety runbook from plan 17-03. The HUMAN performs every step; the agent does NOT trigger the delete. This closes DEBT-05 — confirming DATA-*/SEC-01 fully live, including the destructive path the 12-07 walkthrough deferred.

Purpose: This is the one destructive PROD step in the milestone — deliberately isolated, gated behind the runbook, human-executed. The dev server points at PROD Supabase, so the delete must run only via the live production UI under the five guard-rails.
Output: live evidence recorded in this plan's SUMMARY (backup id, throwaway user_id, gate double-confirm, surgical RLS cascade verification) — or, if the human defers the delete, the phase stays open only on DATA-02 (per CONTEXT Deferred Ideas).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-v1-3-debt-cleanup-isolated/17-CONTEXT.md
@.planning/phases/17-v1-3-debt-cleanup-isolated/17-SC3-DELETE-RUNBOOK.md
@.planning/phases/12-produ-o-live-verify/12-07-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Execute the SC3 throwaway-account destructive delete per the runbook (DATA-02)</name>
  <read_first>
    - .planning/phases/17-v1-3-debt-cleanup-isolated/17-SC3-DELETE-RUNBOOK.md (the ordered runbook produced in 17-03 — the human follows it step by step; the five guard-rails are mandatory and ordered)
    - .planning/phases/12-produ-o-live-verify/12-07-SUMMARY.md (the deferred DATA-02 destructive-delete item this plan closes; gate mechanism + SEC-01 already proven non-destructively)
  </read_first>
  <what-built>
    No new code. The HUMAN executes the destructive throwaway-account delete on production by following the 17-SC3-DELETE-RUNBOOK.md exactly. The agent does NOT run the delete; it only confirms the recorded evidence afterward.
  </what-built>
  <action>
    No code is written and the agent NEVER triggers the delete. The agent surfaces `.planning/phases/17-v1-3-debt-cleanup-isolated/17-SC3-DELETE-RUNBOOK.md` and waits while the HUMAN executes each of its five ordered guard-rails against https://gestao-financeira-ebon-mu.vercel.app/ (never the dev server, which points at PROD Supabase). Afterward the agent records the human-reported evidence (backup id, throwaway user_id, APAGAR gate double-confirm, surgical RLS cascade) — or the explicit DATA-02 deferral. Follow the steps in <human_verification>.
  </action>
  <human_verification>
    The HUMAN executes the destructive delete by following `.planning/phases/17-v1-3-debt-cleanup-isolated/17-SC3-DELETE-RUNBOOK.md`. The agent must NOT trigger the delete and must NOT run the dev server (it points at PROD Supabase).

    PROD URL: https://gestao-financeira-ebon-mu.vercel.app/
    Gate string: APAGAR

    Ordered guard-rails (must all hold, in order):
    1. DB backup taken BEFORE the delete — record the backup id/timestamp.
    2. A throwaway production account is created + confirmed (NOT the personal account) — record the throwaway user_id; add one trivial row.
    3. The "Apagar conta" dialog on `/conta` focuses Cancelar and enables only on the exact string APAGAR; double-confirm signed-in-as-throwaway before typing APAGAR.
    4. The delete is performed ONLY via the live production UI — NEVER via the dev server.
    5. After delete: "Apagando…" → sign-out to `/auth/login`; the throwaway account can no longer sign in; the PERSONAL account still signs in with all data intact (surgical, RLS-scoped cascade).

    If the human chooses to DEFER the destructive delete at this point, that is an allowed outcome (CONTEXT Deferred Ideas): Phase 17 then stays open only on DATA-02 and the rest of the phase still closes. Record the deferral explicitly in the SUMMARY.
  </human_verification>
  <verify>
    <human-check>The human reports the delete was executed under all five guard-rails (backup id, throwaway user_id, double-confirmed APAGAR gate, PROD-site-only, surgical RLS cascade with personal account intact) — OR explicitly defers DATA-02.</human-check>
  </verify>
  <acceptance_criteria>
    - A DB backup id/timestamp taken BEFORE the delete is recorded.
    - The deleted account is a confirmed THROWAWAY production user_id (recorded), never the personal account.
    - The "Apagar conta" gate was double-confirmed and enabled only on the exact string APAGAR.
    - The delete was performed via the live production UI at https://gestao-financeira-ebon-mu.vercel.app/ only — never the dev server.
    - Post-delete: the throwaway account can no longer sign in; the PERSONAL account still signs in with data intact (RLS-scoped surgical cascade).
    - OR: the human explicitly deferred DATA-02 and the deferral is recorded — leaving Phase 17 open only on DATA-02.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the recorded backup id / throwaway user_id / cascade-verification, OR type "defer DATA-02" to defer the destructive delete (phase stays open only on that item).</resume-signal>
</task>

</tasks>

<verification>
- The destructive delete was executed strictly under the runbook's five ordered guard-rails, OR explicitly deferred.
- If executed: the cascade is verified RLS-scoped to the throwaway user; the personal account is intact.
- No dev-server execution; no agent-triggered delete.
</verification>

<success_criteria>
DEBT-05 satisfied: the LGPD destructive throwaway-account delete (DATA-02) was executed live on production under the safety runbook (backup + throwaway user_id + double-confirmed APAGAR gate + PROD-only + verified RLS-scoped cascade), confirming DATA-*/SEC-01 fully live — OR DATA-02 was explicitly deferred per CONTEXT, leaving the phase open only on that single item.
</success_criteria>

<output>
Create `.planning/phases/17-v1-3-debt-cleanup-isolated/17-04-SUMMARY.md` when done, recording the backup id, throwaway user_id, gate double-confirm, and surgical RLS cascade verification (or the explicit DATA-02 deferral).
</output>
