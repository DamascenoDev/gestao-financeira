---
phase: 14-key-storage-byok-settings
plan: 04
type: execute
wave: 3
depends_on: ["14-03"]
files_modified:
  - src/actions/ai-settings.ts
files_modified_tests:
  - src/actions/ai-settings.test.ts
autonomous: true
requirements: [BYOK-02, BYOK-03, BYOK-05]

must_haves:
  truths:
    - "saveAiSettings validates with aiSettingsSchema, checks the owner via getClaims, and persists the key only through the save_ai_api_key RPC (Vault) — never an app column"
    - "testConnection decrypts server-only, runs a ~1-token generateText ping, and returns { ok } | { error } — never the key, never a stack"
    - "mapProviderError maps 401/403 → invalid-key, 429 → no-credits, else → generic — all pt-BR, never echoing sk-/AIza/raw provider text"
    - "removeAiKey checks the owner and calls remove_ai_api_key (row + Vault secret deleted), returning the app to the pre-IA state"
  artifacts:
    - path: "src/actions/ai-settings.ts"
      provides: "saveAiSettings, testConnection, removeAiKey Server Actions + exported mapProviderError"
      exports: ["saveAiSettings", "testConnection", "removeAiKey"]
  key_links:
    - from: "src/actions/ai-settings.ts saveAiSettings"
      to: "public.save_ai_api_key()"
      via: "supabase.rpc('save_ai_api_key', { p_provider, p_model, p_key })"
      pattern: "rpc\\(['\"]save_ai_api_key"
    - from: "src/actions/ai-settings.ts testConnection"
      to: "getDecryptedAiSettings + modelFor + generateText"
      via: "server-only decrypt → per-call model → 1-token ping inside a total try/catch"
      pattern: "getDecryptedAiSettings|generateText"
---

<objective>
Author `src/actions/ai-settings.ts` — the three Server Actions (`saveAiSettings`, `testConnection`, `removeAiKey`) that the form drives — mirroring the `actions/mei.ts` grammar. Turn the Plan 01 Wave 0 action test (error mapping) GREEN.

Purpose: These actions are the only write/test/remove surface for the BYOK key. `saveAiSettings` hands the pasted key straight to Vault via the `save_ai_api_key` RPC (the key never lands in an app column — BYOK-02); `testConnection` decrypts server-only and runs a cheap ping with friendly pt-BR error mapping, never leaking the key or a stack (BYOK-03, Pitfall 5); `removeAiKey` drops the row + Vault secret so the app returns to the pre-IA manual-pick state (BYOK-05). All three return `{ ok: true } | { error }` and never throw/leak.

Output: `src/actions/ai-settings.ts` with `saveAiSettings`, `testConnection`, `removeAiKey`, and an exported `mapProviderError`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-key-storage-byok-settings/14-RESEARCH.md
@.planning/phases/14-key-storage-byok-settings/14-UI-SPEC.md

# THE action grammar to clone ('use server' + safeParse boundary → {error}|{ok} + getClaims + revalidatePath)
@src/actions/mei.ts
# The layer this plan consumes (built in Plan 03)
@src/lib/ai/settings.server.ts
@src/lib/ai/provider-factory.ts
@src/lib/ai/settings.ts
@src/lib/schemas/ai-settings.ts
# The Wave 0 RED test this plan must turn GREEN
@src/actions/ai-settings.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: saveAiSettings + removeAiKey (Vault write + remove via RPC, mei.ts grammar)</name>
  <files>src/actions/ai-settings.ts</files>
  <action>
    Create `src/actions/ai-settings.ts` with `'use server'` at the top, mirroring `actions/mei.ts`:
    `export type ActionResult = { error: string } | { ok: true }` and a single `AI_PATH =
    '/conta/configuracoes-ia'` revalidate target.

    `saveAiSettings(formData: FormData): Promise<ActionResult>` (BYOK-02): `aiSettingsSchema.safeParse`
    `{ provider: formData.get('provider'), apiKey: formData.get('apiKey') }` at the boundary → on
    failure return `{ error: <first issue message> }`. Build the RLS client (`@/lib/supabase/server`),
    `supabase.auth.getClaims()`; if no `claims.sub` return `{ error: 'Sessão expirada.' }`. Persist via
    `supabase.rpc('save_ai_api_key', { p_provider: provider, p_model: DEFAULT_MODEL[provider], p_key:
    apiKey })` — the model is the hard-coded cheap default from `settings.ts`, NOT user-supplied. On
    rpc error return the 14-UI-SPEC copy `{ error: 'Não foi possível salvar. Tente novamente.' }`. On
    success `revalidatePath(AI_PATH)` and return `{ ok: true }`. The plaintext key flows formData →
    RPC → Vault; it is NEVER written to an app column and NEVER read back.

    `removeAiKey(): Promise<ActionResult>` (BYOK-05): owner check via getClaims, then
    `supabase.rpc('remove_ai_api_key')`; on error `{ error: 'Não foi possível remover a chave. Tente
    novamente.' }`; on success `revalidatePath(AI_PATH)` + `{ ok: true }`. After removal the app
    returns to the pre-IA state — `suggestCategory()` already returns null safely, so nothing breaks.
  </action>
  <verify>
    <automated>grep -q "'use server'" src/actions/ai-settings.ts && grep -q "rpc('save_ai_api_key'" src/actions/ai-settings.ts && grep -q "rpc('remove_ai_api_key')" src/actions/ai-settings.ts && grep -q "getClaims" src/actions/ai-settings.ts && npx tsc --noEmit</automated>
  </verify>
  <done>saveAiSettings persists via the Vault RPC with the default model + owner check; removeAiKey deletes via the RPC; both return {ok}|{error} and revalidate; tsc passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: testConnection + mapProviderError (1-token ping, friendly pt-BR, no leak)</name>
  <files>src/actions/ai-settings.ts</files>
  <behavior>
    - mapProviderError(401-or-403 error) === 'Chave inválida. Confira se você copiou a chave correta do provedor.'
    - mapProviderError(429 error) === 'Sem créditos ou cota esgotada no provedor. Verifique sua conta no provedor.'
    - mapProviderError(network/unknown error) === 'Não foi possível testar agora. Tente novamente em instantes.'
    - mapProviderError output never contains 'sk-', 'AIza', or raw provider text
  </behavior>
  <action>
    Add `testConnection(): Promise<ActionResult>` (BYOK-03) to `src/actions/ai-settings.ts`, wrapped in
    a TOTAL `try/catch`. Inside: `getDecryptedAiSettings()` (server-only, Plan 03); if null return
    `{ error: 'Nenhuma chave configurada.' }`. Else `await generateText({ model: modelFor(settings.
    provider, settings.model, settings.apiKey), prompt: 'ping', <token cap> })` capping output to ~1
    token. Resolve the exact AI SDK v6 option for the cap (`maxOutputTokens` vs `maxTokens`) and the
    error guard (`APICallError.isInstance` vs `AI_APICallError`) against the installed `ai@6.0.x` types
    — RESEARCH Open Question 3; do not guess, read the package's exported types. On success return
    `{ ok: true }`. On any throw return `{ error: mapProviderError(e) }`.

    Export `mapProviderError(e: unknown): string`: narrow the AI SDK error (via the verified
    `APICallError` guard) to read a `statusCode`; map 401/403 → the invalid-key copy, 429 → the
    no-credits copy, everything else / network / non-APICallError → the generic copy. Use the EXACT
    pt-BR strings from 14-UI-SPEC §Copywriting Contract. NEVER include the raw error message, headers,
    a stack, or the decrypted key in the returned string or in any `console.log`. mapProviderError is
    exported so the Wave 0 unit test (mocked errors) can assert the mapping without a real provider call.
  </action>
  <verify>
    <automated>npx vitest run src/actions/ai-settings.test.ts && grep -q "export function mapProviderError\|export const mapProviderError" src/actions/ai-settings.ts && grep -q "generateText" src/actions/ai-settings.ts && npx tsc --noEmit</automated>
  </verify>
  <done>The Wave 0 action test is GREEN: mapProviderError returns the three friendly pt-BR messages and never leaks; testConnection decrypts server-only, pings ~1 token, and is fully wrapped in try/catch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client form → Server Actions | Untrusted provider/apiKey + action invocations cross here; validated + owner-checked server-side |
| provider error → UI | A raw provider 401/429/network message must be mapped, never passed through (could echo the key) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-07 | Information Disclosure | testConnection error path leaking key/stack | mitigate | Total try/catch; `mapProviderError` returns only the three fixed pt-BR strings; never log the decrypted key or raw provider error. (Pitfall 5) |
| T-14-08 | Spoofing / Access Control | Action invoked without a valid session | mitigate | `getClaims()` owner check in saveAiSettings + removeAiKey (mei.ts grammar); RLS + the auth.uid()-filtered RPCs are the structural backstop |
| T-14-09 | Information Disclosure | Key written to an app column instead of Vault | mitigate | saveAiSettings persists ONLY through `save_ai_api_key` (Vault); no `.from('ai_settings').insert/update` with a key column exists |
</threat_model>

<verification>
- `npx vitest run src/actions/ai-settings.test.ts` GREEN (error mapping + no-leak).
- saveAiSettings/removeAiKey use the RPCs + getClaims; testConnection is fully try/catch-wrapped.
- `npx tsc --noEmit` passes.
</verification>

<success_criteria>
- saveAiSettings persists the key only via Vault RPC with the default model (BYOK-02).
- testConnection pings ~1 token and maps errors to friendly pt-BR, never leaking (BYOK-03).
- removeAiKey deletes row + Vault secret; app returns to pre-IA state (BYOK-05).
</success_criteria>

<output>
Create `.planning/phases/14-key-storage-byok-settings/14-04-SUMMARY.md` when done
</output>
