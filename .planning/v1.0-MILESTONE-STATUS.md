# Milestone v1.0 — Status final

**Data:** 2026-06-17
**Projeto:** Gestão Financeira Pessoal
**Estado:** **Construído, verificado e endurecido localmente.** Pendências user-owned: deploy remoto + UAT no browser + (opcional) IA.

---

## Resumo

As 6 fases foram construídas autonomamente (discuss → research → validation → plan → plan-check → execute → verify → code-review → fix), cada uma testada contra o Supabase **local** e revisada com correção dos achados de segurança.

| Fase | Estado | Requisitos | Achados corrigidos |
|------|--------|-----------|--------------------|
| 1 Fundação (auth/RLS/dinheiro/schema) | Local ✓ (deploy 01-04 adiado) | AUTH-01..04, CAT-01, SEC-02 | 10 (2 HIGH) |
| 2 Receitas/Categorias/Extrato | Completa ✓ | INC-01..04, CAT-02/03, TXN-01..04 | 13 (3 HIGH incl. IDOR) |
| 3 Metas/Aderência/Reservas | Completa ✓ | BUD-01..04, RSV-01..05 | 6 (1 HIGH) + TOCTOU pego no test-first |
| 4 Upload + classificação (memória) | Completa ✓ | IMP-01..05, CLS-01/03/04/05/06, RSV-06, SEC-03 | 8 (1 CRITICAL parse robustness) |
| 5 Módulo MEI / DASN-SIMEI | Completa ✓ | MEI-01..06 | 4 (números 2026 verificados) |
| 6 Endurecimento (LGPD/isolamento/auditoria) | Completa ✓ | DATA-01/02, SEC-01 | 6 (1 CRITICAL CSV-injection, 1 HIGH storage-orphan) |

**Requisitos v1: 46/47 Complete.** Único pendente: **CLS-02** (IA sugere categoria) — adiado por decisão do usuário; a costura plugável (`suggestCategory` → null, output enum-validated) está pronta.

**Métricas:** 187 commits · 584 testes (vitest unit + integração RLS contra stack local) · `tsc --noEmit` limpo · `npm run build` OK (20 rotas) · auditoria de segredo no bundle real: service-role ABSENT · matriz de isolamento 4-verbos × 14-tabelas verde.

---

## Punch-list pendente (user-owned)

### 1. Deploy remoto + credenciais (Plano 01-04, `autonomous:false`)
- Colar a URL + chave publishable do Supabase pessoal em `.env.local`
- Desligar confirmação de email no painel Supabase
- `supabase db push` das migrations 0001-0026 no projeto remoto (aplicar com cuidado)
- Deploy na Vercel + env vars
- **Segurança:** a chave **secret/service-role** (`SUPABASE_SECRET_KEY`) é usada só no servidor pelo delete LGPD — configurar como env server-only na Vercel, NUNCA `NEXT_PUBLIC_`.

### 2. UAT no browser (os planos `*-NN` human-verify de cada fase)
Rodar `npm run dev` contra o stack local e confirmar os fluxos visuais (login/sessão, extrato/filtros/bulk, dashboard de metas mensal/anual, qual-reserva, upload+revisão+aprende, MEI gauge/relatório/CSV, export LGPD + **delete com type-to-confirm APAGAR em usuário descartável**). Detalhe em cada `0N-0M-SUMMARY.md` (human walkthrough).

### 3. IA de classificação (CLS-02, deferida)
Quando quiser, plugar o provider no seam `suggestCategory` (AI Gateway, modelo barato, só descritor normalizado sem PII, output preso ao enum). O guard PII garante que nada vaza enquanto está null.

### 4. Amostras reais de fatura
Validar o parser OFX/CSV contra exports reais (anônimos) dos teus bancos; PDF (IMP-06) fica pra v2 com spike sobre amostras reais.

---

## Decisões-chave registradas
Ver `.planning/PROJECT.md` (Key Decisions) e `.planning/STATE.md` (Locked Decisions). Destaques: dinheiro em centavos inteiros; RLS `(select auth.uid())=user_id` USING+WITH CHECK em toda tabela; views `security_invoker`; ownership re-derive contra IDOR; reserva saldo derivado; metas teto/alvo; MEI limite aplicável (nunca 81k hardcoded); IA memória-primeiro.

## Próximo passo sugerido
Após deploy + UAT (+ IA se desejado), rodar `/gsd-complete-milestone v1.0` para arquivar o milestone e preparar o próximo (ex: MUL-01 — conta compartilhada da esposa, cujo pré-requisito de isolamento já está provado por teste).

---
*Milestone construído autonomamente em 2026-06-16/17.*
