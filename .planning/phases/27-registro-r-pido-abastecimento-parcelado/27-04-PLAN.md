---
phase: 27-registro-r-pido-abastecimento-parcelado
plan: 04
type: execute
wave: 3
depends_on: ["27-03"]
files_modified:
  - src/components/carro-card.tsx
  - src/app/(app)/carros/page.tsx
autonomous: false
requirements: [CAR-07]
must_haves:
  truths:
    - "Cada CarroCard na lista /carros expõe um botão 'Novo abastecimento' visível na face do card (não no DropdownMenu) (D-03)"
    - "O botão abre o AbastecimentoForm em modo manual-only (só Manual | Parcelado; sem 'Da fatura') (D-01/D-02)"
    - "A page /carros NÃO passa a buscar transacoes (lançamentos não-vinculados) — continua lendo só carros + v_carro_resumo (D-01)"
    - "Após registrar pela lista, o abastecimento aparece no histórico do carro (createAbastecimento já revalida /carros)"
  artifacts:
    - path: "src/components/carro-card.tsx"
      provides: "AbastecimentoForm manual-only hospedado na face do CarroCard com trigger 'Novo abastecimento'"
      contains: "AbastecimentoForm"
  key_links:
    - from: "src/components/carro-card.tsx"
      to: "src/components/abastecimento-form.tsx"
      via: "CarroCard renderiza o AbastecimentoForm com manual-only + carroId=carro.id + combustivelPadrao; transacoes vazio"
      pattern: "AbastecimentoForm"
    - from: "src/app/(app)/carros/page.tsx"
      to: "src/components/carro-card.tsx"
      via: "RSC passa CarroCardData (id + combustivelPadrao já presentes) sem buscar transacoes"
      pattern: "CarroCard"
---

<objective>
Hospedar o `AbastecimentoForm` em modo **manual-only** na face do `CarroCard` (`src/components/carro-card.tsx`), com um botão **"Novo abastecimento"** visível (não escondido no `DropdownMenu`, D-03), reusando o `combustivelPadrao` + `id` que já vêm em `CarroCardData`. A page `/carros` (`src/app/(app)/carros/page.tsx`) NÃO passa a buscar `transacoes` (D-01) — ela continua lendo apenas `carros` + `v_carro_resumo`, e o form manual-only não precisa de lançamentos não-vinculados.

Purpose: Entrega CAR-07 — registro rápido direto da lista, sem navegar para `/carros/[id]`. Mantém a page leve (sem o fetch de `transacoes` que o detalhe faz, L181-212 do `/carros/[id]`). O `createAbastecimento` já revalida `/carros`, então o novo registro aparece sem fetch extra. É a Wave 3: depende do prop manual-only criado em 27-03.

Output: `CarroCard` hospeda o `AbastecimentoForm` manual-only na face; a page passa os dados sem buscar `transacoes`. O `DropdownMenu` segue só com Editar/Arquivar.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-registro-r-pido-abastecimento-parcelado/27-CONTEXT.md
@src/components/abastecimento-form.tsx
</context>

<artifacts_this_phase_produces>
**Novos símbolos / mudanças neste plano** (não há arquivos novos):
- O `CarroCard` passa a importar e renderizar o `AbastecimentoForm` com o prop manual-only (de 27-03), `carroId={carro.id}`, `combustivelPadrao={carro.combustivelPadrao}` e `transacoes={[]}` (vazio — manual-only não usa o picker).
- Um botão/trigger "Novo abastecimento" visível na face do card (abaixo da faixa de KPIs `dl`, D-03; layout fino = discrição), passado como `trigger` ao form OU usando o trigger default do form.
- A page `/carros` NÃO ganha nenhum fetch de `transacoes` — apenas confirma que `CarroCardData` já carrega `id` + `combustivelPadrao` (já carrega). Nenhuma nova query.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hospedar o AbastecimentoForm manual-only na face do CarroCard (D-03/D-01)</name>
  <files>src/components/carro-card.tsx</files>
  <read_first>
    - src/components/carro-card.tsx (TODO o componente: CarroCardData L23-35 — já tem id + combustivelPadrao; o DropdownMenu L121-142 com Editar/Arquivar; a faixa de KPIs dl L152-167; o CarroForm controlado hospedado L170-174 como molde)
    - src/components/abastecimento-form.tsx (a assinatura com o prop manual-only + carroId + combustivelPadrao + transacoes, conforme 27-03 — confirmar o nome exato do prop manual-only)
    - src/app/(app)/carros/[id]/page.tsx (L181-212 — como o DETALHE monta transacoes; contexto do que a lista deliberadamente NÃO faz)
  </read_first>
  <action>
    Importar o `AbastecimentoForm` no `CarroCard` e renderizá-lo na FACE do card (abaixo da faixa de KPIs `dl`, antes do `CarroForm` hospedado), passando o prop manual-only (nome de 27-03), `carroId={carro.id}`, `combustivelPadrao={carro.combustivelPadrao}` e `transacoes={[]}` (vazio — o modo manual-only não renderiza o picker). Expor um botão "Novo abastecimento" visível: ou via o trigger default do form, ou via um `trigger` custom (layout fino = discrição: ícone + label, outline/gold). NÃO mover a ação para o `DropdownMenu` (que segue só Editar/Arquivar, D-03). NÃO buscar nem passar `transacoes` reais. Sem fenced code; descrever por identificadores e posição no JSX.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>`tsc --noEmit` + lint limpos; o `CarroCard` renderiza o `AbastecimentoForm` manual-only na face com um botão "Novo abastecimento" visível; o DropdownMenu segue só com Editar/Arquivar; `transacoes` passado é vazio.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Confirmar que a page /carros NÃO busca transacoes (D-01)</name>
  <files>src/app/(app)/carros/page.tsx</files>
  <read_first>
    - src/app/(app)/carros/page.tsx (TODO o RSC: a query de carros L46-50, o read de v_carro_resumo L56-73, o map para CarroCardData L75-89 — confirmar que id + combustivel_padrao já são selecionados)
    - src/components/carro-card.tsx (CarroCardData — os campos que a page já fornece)
  </read_first>
  <action>
    Verificar que `CarroCardData` já carrega `id` + `combustivelPadrao` (já carrega: a query L48 seleciona `combustivel_padrao` e o map L84 o popula) — portanto a page NÃO precisa de nenhuma nova query. Garantir explicitamente que NENHUM fetch de `transacoes`/lançamentos não-vinculados é adicionado (D-01: a lista fica leve; vincular fatura é o detalhe/Phase 28). Se o `CarroCard` já recebe tudo de que precisa, este task é uma asserção de não-mudança: o único arquivo a tocar, se houver, é manter a page como está. Adicionar um comentário curto na page documentando que a lista deliberadamente NÃO busca `transacoes` (o form da lista é manual-only). Sem fenced code.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -v '^[[:space:]]*//' 'src/app/(app)/carros/page.tsx' | grep -c 'transacoes'</automated>
  </verify>
  <done>`tsc --noEmit` limpo; o grep (excluindo comentários) retorna 0 ocorrências de `transacoes` na page `/carros` — a lista não busca lançamentos não-vinculados; `CarroCardData` fornece id + combustivelPadrao para o form da face.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify — botão "Novo abastecimento" na face do card + form manual-only pela lista</name>
  <files>src/components/carro-card.tsx, src/app/(app)/carros/page.tsx</files>
  <action>Pausa para verificação humana do registro rápido pela lista `/carros` — descoberta do botão na face, o form manual-only (sem "Da fatura") e o registro ponta-a-ponta. Nenhuma edição de produção: só inspeção guiada pelos passos em how-to-verify.</action>
  <what-built>Cada carro na lista `/carros` ganhou um botão "Novo abastecimento" na face do card que abre o form em modo manual-only (Manual | Parcelado, sem "Da fatura"). A lista continua leve (não busca lançamentos).</what-built>
  <how-to-verify>
    1. `npm run dev` e abra `/carros` (com ao menos um carro cadastrado).
    2. Confirme que cada card mostra um botão "Novo abastecimento" visível na face (não escondido no menu "⋯"); o menu "⋯" segue só com Editar/Arquivar.
    3. Clique no botão: o form abre com APENAS as abas "Manual" e "Parcelado" — sem "Da fatura".
    4. Registre um abastecimento manual/à-vista (litros + odômetro + valor) e salve; confirme o toast e que ele aparece no histórico do carro (`/carros/[id]`) sem precisar navegar antes.
    5. Repita marcando "Parcelado" (valor total + nº parcelas) e salve; confirme que o registro foi salvo (o vínculo por valor é a Phase 28 — aqui só persiste).
    6. Confirme que a lista `/carros` continua carregando rápido (não há fetch novo de lançamentos).
  </how-to-verify>
  <verify>
    <human-check>Sign-off do usuário: o botão "Novo abastecimento" está visível na face de cada card, abre o form manual-only (sem "Da fatura"), registra à-vista e parcelado pela lista, e o registro aparece no histórico do carro; a lista segue leve.</human-check>
  </verify>
  <done>O usuário aprovou o registro rápido pela lista (botão na face + form manual-only + registro ponta-a-ponta) — ou listou problemas que viram gaps.</done>
  <resume-signal>Digite "aprovado" se o botão na face, o form manual-only e o registro pela lista estão corretos — ou descreva os problemas.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| usuário (lista /carros) → AbastecimentoForm manual-only → createAbastecimento | O registro pela lista usa a MESMA server action do detalhe |
| RSC /carros → CarroCard | A page só fornece identidade + KPIs; nenhum lançamento não-vinculado cruza para o cliente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-13 | Elevation of Privilege (IDOR) | registro pela lista /carros | mitigate | O botão da lista chama `createAbastecimento`, a MESMA action do detalhe — `assertOwnedCarro` (src/actions/abastecimentos.ts L85) re-deriva posse antes de qualquer write; um carroId forjado escreve nada (provado nos testes de 27-02) |
| T-27-14 | Information Disclosure | page /carros | mitigate | A lista NÃO busca `transacoes` (lançamentos não-vinculados) — o modo manual-only não usa o picker; nenhum lançamento cruza para o card (D-01) |
| T-27-15 | Tampering (double-count) | registro parcelado pela lista | mitigate | Reusa o write parcelado de 27-02 (transaction_id/amount_cents null, sem `abastecimento_parcelas`) — o vínculo por valor é a Phase 28; nada é double-contado aqui |
| T-27-16 | Tampering | npm/pip/cargo installs | mitigate | Nenhum pacote novo neste plano (AbastecimentoForm/Button já presentes); sem superfície de supply-chain |
</threat_model>

<verification>
- `npx tsc --noEmit` + `npm run lint` limpos.
- `grep -v '^[[:space:]]*//' 'src/app/(app)/carros/page.tsx' | grep -c 'transacoes'` retorna 0 (a lista não busca lançamentos).
- O `CarroCard` renderiza o `AbastecimentoForm` manual-only na face com botão "Novo abastecimento" visível.
- O DropdownMenu segue só com Editar/Arquivar (D-03).
- Registrar pela lista persiste via `createAbastecimento` (IDOR-safe) e aparece no histórico (revalidatePath('/carros') já existe).
</verification>

<success_criteria>
A lista `/carros` expõe "Novo abastecimento" por carro (face do card), abrindo o form manual-only, sem que a page passe a buscar `transacoes`; o registro reusa `createAbastecimento` (IDOR-safe).
</success_criteria>

<output>
Create `.planning/phases/27-registro-r-pido-abastecimento-parcelado/27-04-SUMMARY.md` when done
</output>
