---
phase: 27-registro-r-pido-abastecimento-parcelado
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - src/actions/abastecimentos.ts
  - src/actions/abastecimentos.test.ts
autonomous: true
requirements: [CAR-08]
must_haves:
  truths:
    - "createAbastecimento salva um parcelado com parcelas_total + valor_total_cents e transaction_id NULL + amount_cents NULL"
    - "Um parcelado registrado manualmente não pula o gate assertOwnedCarro (IDOR-safe)"
    - "Um parcelado NÃO dispara o pre-check de tx 1:1 nem o sync de carro_id (sem transactionId)"
    - "O caminho à-vista (manual + da-fatura) continua escrevendo exatamente como antes (sem regressão)"
  artifacts:
    - path: "src/actions/abastecimentos.ts"
      provides: "abastecimentoWriteFields mapeia parcelas_total/valor_total_cents por estado de custo"
      contains: "valor_total_cents"
    - path: "src/actions/abastecimentos.test.ts"
      provides: "Cobertura do write parcelado (transaction_id/amount_cents null) + não-regressão dos 2 caminhos à-vista"
      contains: "parcela"
  key_links:
    - from: "src/actions/abastecimentos.ts"
      to: "src/lib/schemas/abastecimento.ts"
      via: "abastecimentoWriteFields consome AbastecimentoInput (valorTotalCents/parcelasTotal)"
      pattern: "valorTotalCents|parcelasTotal"
---

<objective>
Mapear os campos de parcelamento (`valorTotalCents`/`parcelasTotal`) do `AbastecimentoInput` (criados em 27-01) para as colunas `valor_total_cents`/`parcelas_total` do insert/update em `abastecimentoWriteFields` (L55-67), por **estado de custo**. Um parcelado grava `parcelas_total > 1` + `valor_total_cents`, com `transaction_id` NULL **e** `amount_cents` NULL (o XOR estrutural já vem do schema 27-01 + do CHECK do `0039`). O caminho à-vista (manual + da-fatura) fica inalterado.

Purpose: Persistir o parcelado sem double-count e IDOR-safe, materializando no write o XOR estrutural de 3 estados (D-05): o parcelado grava `valor_total_cents` (centavos positivo, D-09) com `transaction_id`/`amount_cents` null. O gate `assertOwnedCarro` já existe em `createAbastecimento` (L85) e cobre o parcelado de graça (mesma action). O pre-check de tx 1:1 (L95-109) só roda quando há `transactionId` — o parcelado pula naturalmente (nasce sem `abastecimento_parcelas`; o vínculo por valor é a Phase 28). Satisfaz CAR-08 (parcelado validado e salvo) e a parte server da Success Criteria 4 (sem double-count: parcelado manual sem transação vinculada).

Output: `abastecimentoWriteFields` parcelado-aware; testes de action cobrindo o write parcelado + a não-regressão dos 2 caminhos à-vista.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-registro-r-pido-abastecimento-parcelado/27-CONTEXT.md
</context>

<artifacts_this_phase_produces>
**Novos símbolos / mudanças neste plano** (não há arquivos novos):
- `abastecimentoWriteFields` ganha duas chaves no objeto de write: `parcelas_total` e `valor_total_cents`, preenchidas por estado de custo (parcelado → `parcelas_total = input.parcelasTotal`, `valor_total_cents = input.valorTotalCents`, `transaction_id = null`, `amount_cents = null`; à-vista → `parcelas_total` segue a convenção do à-vista de 27-01, `valor_total_cents = null`, e os campos de custo à-vista inalterados).
- Convenção do à-vista para `parcelas_total` (Discrição C): seguir a convenção fixada em 27-01 (ausente/`1`). Documentar no comentário de `abastecimentoWriteFields`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Testes do write parcelado + não-regressão à-vista (action level)</name>
  <files>src/actions/abastecimentos.test.ts</files>
  <read_first>
    - src/actions/abastecimentos.test.ts (harness de mock supabase: makeBuilder, insertOn/updateOn helpers, fixtures manualInput/fromFaturaInput; describe blocks de manual/XOR/from-fatura/guards a NÃO regredir)
    - src/actions/abastecimentos.ts (createAbastecimento L70-140 — gate assertOwnedCarro L85, pre-check tx 1:1 L95-109, insert via abastecimentoWriteFields L112-114)
    - src/lib/schemas/abastecimento.ts (estado parcelado validado em 27-01 — fonte do AbastecimentoInput)
  </read_first>
  <behavior>
    - createAbastecimento(parcelado) → { ok: true } e o insert em abastecimentos carrega payload.parcelas_total === N (>= 2), payload.valor_total_cents === valorTotalCents, payload.transaction_id === null, payload.amount_cents === null
    - createAbastecimento(parcelado) NÃO faz update em transactions (sem carro_id sync — não há transactionId)
    - createAbastecimento(parcelado) com carroId forjado (carrosSelect vazio) → { error: 'Carro inválido.' } e NENHUM insert em abastecimentos (assertOwnedCarro cobre o parcelado)
    - Não-regressão: createAbastecimento(manualInput) ainda grava amount_cents com transaction_id null + valor_total_cents null; createAbastecimento(fromFaturaInput) ainda grava transaction_id + sync de carro_id, com valor_total_cents null
  </behavior>
  <action>
    Adicionar uma fixture `parceladoInput` ao `abastecimentos.test.ts` (carroId/occurredOn/odometroKm/litros/tanqueCheio/combustivel + valorTotalCents + parcelasTotal, SEM transactionId/amountCents) reusando os UUIDs já no arquivo. Adicionar um describe block `createAbastecimento — parcelado` cobrindo o `<behavior>`: o write parcelado (asserções em `ins.payload` via o helper `insertOn('abastecimentos')`), a ausência de update em transactions (`updateOn('transactions')` undefined), e o gate de posse (carrosSelect vazio → erro + sem insert). Acrescentar asserções de não-regressão garantindo que os payloads à-vista agora carregam `valor_total_cents: null` (e `parcelas_total` na convenção do à-vista de 27-01). Manter intactos os describe blocks existentes.
  </action>
  <verify>
    <automated>npm test -- src/actions/abastecimentos.test.ts</automated>
  </verify>
  <done>Os testes do parcelado falham contra a action atual (write parcelado ainda não mapeado) — RED comprovado; os describe blocks existentes continuam verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: abastecimentoWriteFields parcelado-aware (GREEN)</name>
  <files>src/actions/abastecimentos.ts</files>
  <read_first>
    - src/actions/abastecimentos.ts (abastecimentoWriteFields L55-67 — alvo; createAbastecimento L70-140 e updateAbastecimento L149-210 que o chamam; comentário-doutrina do XOR de custo L20-31)
    - supabase/migrations/0039_abastecimento_parcelado.sql (L41-78 — colunas + CHECK que o write deve satisfazer: parcelado tem valor_total_cents + transaction_id/amount_cents null; à-vista tem valor_total_cents null)
    - src/types/database.types.ts (o tipo do insert de abastecimentos já tem parcelas_total/valor_total_cents — substrato da P26; conferir os nomes/nulabilidade das colunas)
  </read_first>
  <action>
    Reescrever `abastecimentoWriteFields` para ser parcelado-aware, derivando o estado pelo mesmo critério do schema 27-01 (`parcelasTotal !== undefined && parcelasTotal > 1`). Estado PARCELADO → `parcelas_total: input.parcelasTotal`, `valor_total_cents: input.valorTotalCents`, `transaction_id: null`, `amount_cents: null`. Estado À-VISTA → `valor_total_cents: null`, `parcelas_total` na convenção fixada em 27-01 (ausente→`null` ou `1`; escolher e comentar), `transaction_id`/`amount_cents` mantidos como hoje (`input.transactionId ?? null` / `input.amountCents ?? null`). NÃO tocar no fluxo de `createAbastecimento`/`updateAbastecimento` além de chamar este helper — o gate `assertOwnedCarro` e o pre-check de tx 1:1 (que só roda com `transactionId`) já tratam o parcelado corretamente. Atualizar o comentário do helper para documentar os 3 estados. Sem fenced code; descrever por identificadores e nulabilidade por estado.
  </action>
  <verify>
    <automated>npm test -- src/actions/abastecimentos.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>`abastecimentos.test.ts` todo verde (parcelado + à-vista sem regressão); `tsc --noEmit` limpo; o payload parcelado tem `transaction_id` e `amount_cents` ambos null.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → createAbastecimento (server action) | Input não-confiável + carroId forjável cruzam a borda server |
| createAbastecimento → abastecimentos (FK write) | FKs não são RLS-aware: posse é re-derivada antes de qualquer write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-05 | Elevation of Privilege (IDOR) | createAbastecimento(parcelado) | mitigate | O parcelado usa a MESMA action — `assertOwnedCarro` (L85) re-deriva posse antes do insert; um carroId forjado escreve NADA (asserção de teto na Task 1) |
| T-27-06 | Tampering (double-count) | abastecimentoWriteFields(parcelado) | mitigate | Parcelado grava `transaction_id` NULL + `amount_cents` NULL → custo só em `valor_total_cents` uma vez; o pre-check de tx 1:1 (L95-109) não roda sem `transactionId`; sem `abastecimento_parcelas` nesta fase (vínculo é P28) |
| T-27-07 | Tampering (estado misto) | abastecimentoWriteFields | mitigate | O write por estado nunca mistura custo à-vista com `valor_total_cents`; o CHECK `abastecimentos_cost_xor` do `0039` é o guard final (defense-in-depth com o schema 27-01) |
| T-27-08 | Tampering | npm/pip/cargo installs | mitigate | Nenhum pacote novo neste plano; sem superfície de supply-chain |
</threat_model>

<verification>
- `npm test -- src/actions/abastecimentos.test.ts` verde.
- `npx tsc --noEmit` limpo.
- O insert parcelado tem `transaction_id: null` e `amount_cents: null` (sem double-count).
- O gate `assertOwnedCarro` rejeita um parcelado com carroId forjado sem nenhum write.
- Os 2 caminhos à-vista (manual/da-fatura) escrevem `valor_total_cents: null` e seguem inalterados.
</verification>

<success_criteria>
`createAbastecimento` persiste um parcelado (parcelas_total + valor_total_cents, transaction_id/amount_cents null) IDOR-safe e sem double-count; o à-vista continua inalterado.
</success_criteria>

<output>
Create `.planning/phases/27-registro-r-pido-abastecimento-parcelado/27-02-SUMMARY.md` when done
</output>
