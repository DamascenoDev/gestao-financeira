---
phase: 18-ai-classifica-compras-corretamente
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01-ai-classifica-compras-corretamente
files_modified: []
autonomous: false
requirements:
  - MKT-01
user_setup:
  - service: supabase
    why: "Aplicar a migration 0035 (Marketplace) no projeto PROD — mutação em PROD = ação do dono, Claude NÃO roda push autonomamente"
    dashboard_config:
      - task: "Rodar `supabase db push` (ou `npm run db:push`) a partir do ambiente linkado ao projeto PROD, caso `0035` ainda não esteja em Remote"
        location: "Terminal local linkado ao Supabase PROD"
      - task: "Re-signup em PROD + re-entrada da chave BYOK (PROD foi wiped 2026-06-19) e confirmar a categoria 'Marketplace' em /categorias"
        location: "App em PROD"
must_haves:
  truths:
    - "A migration 0035 está aplicada em PROD (aparece na coluna Remote de `supabase migration list`)"
    - "Após re-signup em PROD, a categoria default 'Marketplace' (consumo) aparece em /categorias"
    - "Num upload de OFX com um descritor de marketplace nunca visto (ex.: AliExpress/Mercado Livre), a sugestão da IA cai numa categoria de consumo — nunca Investimentos/Reserva"
  prohibitions:
    - "Claude NUNCA roda `supabase db push` contra PROD autonomamente — só verifica com `supabase migration list` e instrui"
    - "NENHUM `npm run gen:types` / regen de database.types.ts (0035 é dados + trigger, sem mudança de schema)"
    - "NENHUMA migration nova é criada nesta fase (0035 já existe no repo)"
  artifacts: []
  key_links:
    - from: "supabase/migrations/0035_categories_marketplace.sql"
      to: "PROD public.categories"
      via: "supabase db push (rodado pelo usuário) → re-seed handle_new_user + backfill idempotente"
      pattern: "Marketplace"
---

<objective>
Confirmar MKT-01: a categoria default "Marketplace" (migration `0035`, já no repo) está aplicada em PROD e presente na conta, dando à IA *kind-aware* (Plan 18-01) um bucket de compras sensato — fechando a classe de erro "AliExpress/Mercado Livre → Investimentos" ponta a ponta.

Purpose: `0035` é uma migration de dados + trigger (sem mudança de schema). A aplicação em PROD é uma mutação do dono — o USUÁRIO roda `supabase db push`; Claude apenas verifica com `supabase migration list` e instrui. Como PROD foi wiped (2026-06-19), "presente na conta" exige re-signup: o `handle_new_user` re-seeded (parte 1 da 0035) cria Marketplace na conta nova; o backfill idempotente (parte 2) cobre qualquer conta pré-existente. É human-verify por definição (PROD data + wipe → não automatizável).
Output: `0035` em Remote confirmado; "Marketplace" visível em /categorias após re-signup; uma sugestão de consumo (nunca alocação) num descritor de marketplace.
</objective>

<artifacts_this_phase_produces>
NENHUM artefato de código. Este plan é um checkpoint de verificação humana de uma migration pré-existente (`0035`) aplicada em PROD. Sem novos símbolos, sem novos arquivos, sem migration nova, sem regen de types.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-ai-classifica-compras-corretamente/18-CONTEXT.md
@.planning/phases/18-ai-classifica-compras-corretamente/18-RESEARCH.md

@supabase/migrations/0035_categories_marketplace.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verificar (read-only) se 0035 está em PROD</name>
  <read_first>
    - supabase/migrations/0035_categories_marketplace.sql (re-seed handle_new_user + backfill idempotente; Marketplace sort 9 consumo)
    - 18-RESEARCH.md "MKT-01 Verification (exact commands)"
  </read_first>
  <action>
    Rodar `supabase migration list` (read-only) e inspecionar se `0035` aparece na coluna Remote do projeto PROD linkado. Se presente → o lado de schema do MKT-01 está satisfeito (anotar para o checkpoint). Se AUSENTE de Remote → NÃO rodar push; preparar a instrução para o usuário no checkpoint (sugerir `supabase db push` ou `npm run db:push`). NÃO rodar `gen:types` (0035 não muda schema). NÃO criar migration nova.
  </action>
  <verify>
    <automated>supabase migration list</automated>
  </verify>
  <done>
    Saída de `supabase migration list` capturada; estado de `0035` em Remote (presente/ausente) determinado e registrado para o checkpoint.
  </done>
  <acceptance_criteria>
    - `supabase migration list` foi executado (read-only) e a presença/ausência de `0035` em Remote está registrada.
    - Nenhum `db push`, `gen:types` ou migration nova foi executado por Claude.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Human-verify — Marketplace em PROD + sugestão de consumo (MKT-01)</name>
  <what-built>
    A IA kind-aware (Plan 18-01) + a categoria default "Marketplace" (`0035`). Claude verificou `supabase migration list`; agora o usuário aplica (se faltar) e confirma o resultado em PROD.
  </what-built>
  <action>
    Nenhum código é escrito e Claude NUNCA roda `supabase db push` em PROD. Claude apresenta o estado de `0035` (da Task 1) e aguarda enquanto o USUÁRIO aplica a migration (se faltar) e verifica em PROD os passos de <human_verification>. Depois, Claude registra a evidência reportada (0035 em Remote, "Marketplace" visível, sugestão de consumo) — ou a falha observada. NÃO rodar `gen:types`. NÃO criar migration nova.
  </action>
  <human_verification>
    1. Se a Task 1 reportou `0035` AUSENTE de Remote: rode `supabase db push` (ou `npm run db:push`) a partir do ambiente linkado ao PROD. (Se já estava em Remote, pule.)
    2. Faça re-signup em PROD (a conta de teste foi apagada em 2026-06-19) e re-entre a chave BYOK em Settings.
    3. Abra `/categorias` e confirme que a categoria "Marketplace" (consumo) aparece na lista.
    4. Faça upload de um OFX contendo um descritor de marketplace nunca visto (ex.: AliExpress, Mercado Livre, Shopee). Na grid de revisão, confirme que a sugestão da IA cai em "Marketplace" (ou outra categoria de consumo) — NUNCA em Investimentos/Reserva.
    Esperado: "Marketplace" presente na conta; a sugestão para o descritor de marketplace é uma categoria de consumo (o kind gate do 18-01 garante que alocação jamais é sugerida para um gasto).
  </human_verification>
  <verify>
    <human-check>O usuário reporta que `0035` está em Remote, que "Marketplace" aparece em /categorias após re-signup, e que a sugestão da IA para um descritor de marketplace nunca visto cai numa categoria de consumo (nunca Investimentos/Reserva).</human-check>
  </verify>
  <acceptance_criteria>
    - `0035` aparece na coluna Remote (Claude verificou ou o usuário aplicou via `db push` e re-verificou).
    - Após re-signup em PROD, "Marketplace" (consumo) aparece em /categorias.
    - Um upload com descritor de marketplace nunca visto gera sugestão de categoria de consumo — nunca de alocação.
    - Claude nunca rodou `db push`; nenhum `gen:types`; nenhuma migration nova.
  </acceptance_criteria>
  <resume-signal>Digite "aprovado" se "Marketplace" aparece e a sugestão é de consumo; senão, descreva o que viu (ex.: 0035 não aplicou, sugestão caiu em alocação).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev local → Supabase PROD | `db push` aplica dados/trigger em PROD — mutação de produção, restrita ao dono. |
| novo signup → handle_new_user (trigger) | O trigger semeia categorias default (incl. Marketplace) na conta nova. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04 | Elevation of Privilege / Tampering | aplicação de migration em PROD | mitigate | Claude NUNCA roda `db push` autonomamente (CONTEXT.md locked) — só `migration list` read-only + instrução; o dono aplica. Checkpoint blocking-human. |
| T-18-05 | Information Disclosure | dados de conta em PROD | accept | `0035` é dados+trigger; `categories` já é RLS-escopada por `user_id` (V4 ASVS L1, inalterada). Nenhuma policy nova. |
| T-18-SC | Tampering | npm/pip/cargo installs | mitigate | NÃO APLICÁVEL — zero pacotes instalados nesta fase (18-RESEARCH "Package Legitimacy Audit: Not applicable"). |

Nenhuma ameaça de severidade alta nova.
</threat_model>

<verification>
- `supabase migration list` — `0035` aparece em Remote (ou o usuário aplica via `db push` e re-verifica).
- Human-verify: "Marketplace" visível em /categorias após re-signup; sugestão de consumo (nunca alocação) num descritor de marketplace.
</verification>

<success_criteria>
- MKT-01 confirmado: `0035` em PROD + "Marketplace" presente na conta + sugestão de marketplace cai em categoria de consumo.
- Claude nunca rodou `db push` em PROD; nenhum `gen:types`; nenhuma migration nova.
</success_criteria>

<output>
Create `.planning/phases/18-ai-classifica-compras-corretamente/18-02-SUMMARY.md` when done
</output>
