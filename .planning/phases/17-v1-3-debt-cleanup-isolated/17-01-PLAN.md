---
phase: 17-v1-3-debt-cleanup-isolated
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/12-produ-o-live-verify/12-VALIDATION.md
  - .planning/phases/13-pdf-de-fatura/13-VALIDATION.md
autonomous: true
requirements: [DEBT-06]

must_haves:
  truths:
    - "Phase 12 has a complete VALIDATION.md with an honest nyquist_compliant flag and a per-plan verification map covering every 12-xx plan"
    - "Phase 13's draft VALIDATION.md is finalized (all {N}/placeholder tokens replaced, nyquist_compliant set honestly)"
  artifacts:
    - path: ".planning/phases/12-produ-o-live-verify/12-VALIDATION.md"
      provides: "Retroactive Nyquist validation contract for Phase 12 (live-verify phase)"
      contains: "nyquist_compliant"
    - path: ".planning/phases/13-pdf-de-fatura/13-VALIDATION.md"
      provides: "Finalized Nyquist validation contract for Phase 13 (PDF parsing)"
      contains: "nyquist_compliant"
  key_links:
    - from: ".planning/phases/12-produ-o-live-verify/12-VALIDATION.md"
      to: ".planning/phases/12-produ-o-live-verify/12-0*-SUMMARY.md"
      via: "Per-Task Map rows grounded in what each 12-xx plan actually shipped + live-verified"
      pattern: "12-0[0-9]"
    - from: ".planning/phases/13-pdf-de-fatura/13-VALIDATION.md"
      to: ".planning/phases/13-pdf-de-fatura/13-VERIFICATION.md"
      via: "Test infrastructure + per-task map grounded in the 9/9 VERIFICATION facts"
      pattern: "pdf\\.test|import\\.test|tsc"
---

<objective>
Produce the two missing/draft Nyquist VALIDATION.md docs that close DEBT-06: create `12-VALIDATION.md` from scratch and finalize the draft `13-VALIDATION.md`. Both follow the canonical `13-VALIDATION.md` format (frontmatter + Test Infrastructure + Sampling + Per-Task Map + Wave 0 + Manual-Only + Sign-Off) and use the "pragmatic retroactive" depth per CONTEXT D — maps grounded ONLY in what actually shipped and was live-verified, with an honest `nyquist_compliant` flag and residual manual-only items listed. No fabricated test runs.

Purpose: Both phases (12 + 13) shipped and were verified by other means (Phase 12 live MCP, Phase 13 VERIFICATION 9/9) but never got a Nyquist VALIDATION.md. This is the SC4 doc-generation half of the phase — pure file writes the executor does autonomously.
Output: `12-VALIDATION.md` (new) and a finalized `13-VALIDATION.md` (no placeholders).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-v1-3-debt-cleanup-isolated/17-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize 13-VALIDATION.md (replace all placeholders, set honest nyquist_compliant)</name>
  <files>.planning/phases/13-pdf-de-fatura/13-VALIDATION.md</files>
  <read_first>
    - .planning/phases/13-pdf-de-fatura/13-VALIDATION.md (the DRAFT to finalize — canonical format source; has `{N}` placeholders, `status: draft`, `nyquist_compliant: false`, `wave_0_complete: false`)
    - .planning/phases/13-pdf-de-fatura/13-VERIFICATION.md (status: passed, 9/9 must-haves; source of the real test facts: `pdf.test.ts` GREEN, `import.test.ts` 36 passed, `dedupe.test.ts`, `npx tsc --noEmit` exit 0, synthetic fixture `tests/fixtures/santander-sample.txt`, parser exercised 5 rows/0 dropped)
    - .planning/phases/13-pdf-de-fatura/13-01-PLAN.md through 13-04-PLAN.md (requirements per plan: 13-01→PDF-02; 13-02→PDF-05; 13-03→PDF-01/02/04/05; 13-04→PDF-01/03/04)
  </read_first>
  <action>
    Finalize the existing draft in place. Replace EVERY `{N}` placeholder with concrete values drawn from 13-VERIFICATION.md — do not invent numbers; where a precise runtime is unknown, state the actual command and an honest qualitative latency note rather than a fabricated second-count. Update frontmatter: set `status: complete`, `wave_0_complete: true` (Wave 0 fixtures + stubs shipped — synthetic `tests/fixtures/santander-sample.txt` exists and `pdf.test.ts`/`import.test.ts` are GREEN per VERIFICATION), and set `nyquist_compliant: true` ONLY because the parser is behaviorally tested AND the full pipeline was live-verified; the residual real-PDF and foreign-currency items belong in Manual-Only, not as compliance blockers. Populate the Per-Task Verification Map with one row per real task across 13-01..13-04 using the actual automated commands from VERIFICATION (e.g. `vitest run pdf.test.ts`, `vitest run import.test.ts`, `npx tsc --noEmit`) and requirement IDs PDF-01..PDF-05; mark each row green per the VERIFICATION evidence. Keep the existing Manual-Only rows (real Santander PDF end-to-end; foreign-currency `convertido` path — both gitignored personal data / never observed against real data) and add nothing fabricated. Complete the Sign-Off checklist honestly and set Approval accordingly. Do NOT add fenced code blocks; this is a markdown doc with tables and prose.
  </action>
  <verify>
    <automated>test -f .planning/phases/13-pdf-de-fatura/13-VALIDATION.md && ! grep -qE '\{N\}|nyquist_compliant: false|status: draft' .planning/phases/13-pdf-de-fatura/13-VALIDATION.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/13-pdf-de-fatura/13-VALIDATION.md` contains zero `{N}` placeholder tokens.
    - Frontmatter key `nyquist_compliant` is present and set to `true` (honest — parser behaviorally tested + pipeline live-verified per 13-VERIFICATION 9/9).
    - Frontmatter `status` is `complete` (not `draft`) and `wave_0_complete: true`.
    - The Per-Task Verification Map lists at least one row for each of plans 13-01, 13-02, 13-03, 13-04 and references requirement IDs from the set PDF-01..PDF-05.
    - Each Per-Task Map automated command references a real test artifact (`pdf.test.ts`, `import.test.ts`, `dedupe.test.ts`, or `tsc --noEmit`) — no fabricated/invented commands.
    - The Manual-Only section still lists the real-Santander-PDF and foreign-currency residual items.
  </acceptance_criteria>
  <done>13-VALIDATION.md is finalized: no placeholders, honest nyquist_compliant=true, per-task map covers all four 13-xx plans grounded in 13-VERIFICATION facts, residual manual-only items preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Author 12-VALIDATION.md from scratch (pragmatic retroactive, mirrors 13 format)</name>
  <files>.planning/phases/12-produ-o-live-verify/12-VALIDATION.md</files>
  <read_first>
    - .planning/phases/13-pdf-de-fatura/13-VALIDATION.md (the now-finalized canonical FORMAT to mirror: frontmatter + Test Infrastructure + Sampling Rate + Per-Task Verification Map + Wave 0 Requirements + Manual-Only Verifications + Validation Sign-Off)
    - .planning/phases/12-produ-o-live-verify/12-VERIFICATION.md (source of truth: status human_needed; G-01..G-08 all FIXED; what was live-verified vs deferred)
    - 12-01..12-11 SUMMARY.md frontmatter (requirements_completed per plan: 12-01 DEBT-01/02; 12-02 DEPLOY-01/02/03; 12-03 INC-02/TXN-03/TXN-04 live; 12-04 BUD-02 + RSV-*; 12-05 DEPLOY-04/05 core value; 12-06 MEI-01..06 live; 12-07 SEC-01 live + DATA-01/02 human_needed; 12-08 G-01; 12-09 G-02/03/04; 12-10 G-05 receitas delete; 12-11 G-06 pt-BR date)
  </read_first>
  <action>
    Create `.planning/phases/12-produ-o-live-verify/12-VALIDATION.md` mirroring the finalized 13-VALIDATION.md structure exactly (same section headers and frontmatter keys: `phase`, `slug`, `status`, `nyquist_compliant`, `wave_0_complete`, `created`). Phase 12 is a LIVE-VERIFY phase, so the Test Infrastructure note must be honest: it was validated primarily by live production checks via Chrome DevTools MCP (production URL https://gestao-financeira-ebon-mu.vercel.app/) plus the existing repo vitest suite (the gap-closure plans 12-08..12-11 shipped GREEN unit coverage — e.g. `select-value-label.test.tsx` for G-01/G-07). Build the Per-Task Verification Map with one row per 12-xx plan grounded in its SUMMARY: gap-closure plans (12-08 G-01, 12-09 G-02/03/04, 12-10 G-05, 12-11 G-06) cite their automated test files where they exist; live-verify plans (12-02..12-07) cite "live MCP, production" as the verification method with the requirement IDs they confirmed. Set `nyquist_compliant` HONESTLY to `false` because Phase 12's core verification was manual/live (not automated test sampling) AND DATA-01/DATA-02 remained human-pending at authoring time — the honest residual belongs in Manual-Only. In Manual-Only list exactly: the MEI `dasn-2026.csv`/JSON download CONTENT inspection (12-06 left unopened — BOM/`;`/pt-BR), the LGPD transactions-CSV + data-bundle JSON content (DATA-01), and the destructive throwaway-account delete (DATA-02). Note that those residual live walkthroughs are themselves closed within THIS phase 17 (plans 02 + 04) — reference them so the map is traceable, but do NOT claim them done here. No fabricated test runs; no fenced code blocks.
  </action>
  <verify>
    <automated>test -f .planning/phases/12-produ-o-live-verify/12-VALIDATION.md && grep -qE '^nyquist_compliant:' .planning/phases/12-produ-o-live-verify/12-VALIDATION.md && grep -q 'Per-Task' .planning/phases/12-produ-o-live-verify/12-VALIDATION.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/12-produ-o-live-verify/12-VALIDATION.md` exists.
    - Frontmatter has key `nyquist_compliant` set to a literal boolean (`false`, honestly — Phase 12 verified by live MCP, not automated sampling; DATA-01/02 were human-pending).
    - Frontmatter includes `phase`, `slug`, `status`, `wave_0_complete`, `created` (same key set as 13-VALIDATION.md).
    - Section headers mirror 13-VALIDATION.md: Test Infrastructure, Sampling Rate, Per-Task Verification Map, Wave 0 Requirements, Manual-Only Verifications, Validation Sign-Off.
    - The Per-Task Verification Map lists a row for every shipped 12-xx plan (12-01 through 12-11).
    - The Manual-Only section lists the MEI download content (BOM/`;`/pt-BR), the LGPD export content (DATA-01), and the destructive delete (DATA-02), and references phase-17 plans 02 + 04 as where they are closed.
    - No fabricated/invented automated-test commands appear for the live-verify plans (12-02..12-07); their verification method is recorded as live MCP / production.
  </acceptance_criteria>
  <done>12-VALIDATION.md exists, mirrors the 13 format, has an honest nyquist_compliant=false, a per-task map covering 12-01..12-11 grounded in summaries, and a Manual-Only section pointing at the phase-17 walkthroughs that close the residuals.</done>
</task>

</tasks>

<verification>
- Both VALIDATION.md files exist and parse as markdown with the canonical section set.
- `13-VALIDATION.md` has no `{N}` placeholders and `nyquist_compliant: true`.
- `12-VALIDATION.md` has `nyquist_compliant: false` and a per-task map covering all 11 plans.
- No fabricated test runs in either file (live-verify plans cite live MCP; tested plans cite real test files only).
</verification>

<success_criteria>
DEBT-06 satisfied: Nyquist VALIDATION.md generated for Phase 12 (was absent) and finalized for Phase 13 (was draft), both pragmatic-retroactive with honest compliance flags and residual manual-only items listed.
</success_criteria>

<output>
Create `.planning/phases/17-v1-3-debt-cleanup-isolated/17-01-SUMMARY.md` when done.
</output>
