---
phase: 05-m-dulo-mei-dasn-simei
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/lib/schemas/mei.ts
  - src/actions/mei.ts
  - src/actions/mei.test.ts
  - src/components/app-sidebar.tsx
  - src/components/year-selector.tsx
  - src/components/mei-disclaimer.tsx
  - src/components/limite-gauge.tsx
  - src/components/limite-status-badge.tsx
  - src/app/(app)/mei/layout.tsx
  - src/app/(app)/mei/page.tsx
autonomous: true
requirements: [MEI-01, MEI-02, MEI-03, MEI-05, MEI-06]
user_setup: []

must_haves:
  truths:
    - "A user can register an NF (issued_on, amount, tomador, descricao, activity_type) and set MEI settings (mei_start_date, has_employee per year) through Server Actions that Zod-validate, getClaims, parse money to positive cents, and IDOR-re-derive on edit/delete (MEI-01/03)"
    - "The /mei dashboard shows accumulated gross revenue vs the COMPUTED applicable limit for the selected year, the tiered status (verde/âmbar/vermelho-banda/vermelho-fora), and the % — never a hardcoded R$81.000 (MEI-02)"
    - "At ≥80% of the applicable limit the dashboard surfaces an inline amber alert row; at ≥100% the corresponding red consequence line (within-band vs over-band) — the dashboard IS the alert surface (MEI-05)"
    - "The MeiDisclaimer banner is rendered once in the /mei segment layout and is visible (not a footer) on every MEI screen (MEI-06)"
    - "A MEI nav item appears after Reservas; the YearSelector drives ?ano=YYYY across MEI sub-pages and the global MonthSelector is not shown inside /mei"
  artifacts:
    - path: "src/actions/mei.ts"
      provides: "NF CRUD + settings + year-flag Server Actions (incomes.ts shape, IDOR-checked)"
      exports: ["createMeiInvoice", "updateMeiInvoice", "deleteMeiInvoice", "upsertMeiSettings", "upsertMeiYearFlag"]
    - path: "src/lib/schemas/mei.ts"
      provides: "Zod schemas shared form↔action (NF, settings, year flag)"
      exports: ["meiInvoiceSchema", "meiSettingsSchema", "meiYearFlagSchema"]
    - path: "src/components/limite-gauge.tsx"
      provides: "gross-vs-limit gauge reusing the AdherenceBar pattern (80% tick + 100% marker)"
      min_lines: 30
    - path: "src/components/mei-disclaimer.tsx"
      provides: "the persistent MEI-06 informational banner"
      contains: "consultoria fiscal"
    - path: "src/app/(app)/mei/layout.tsx"
      provides: "MEI segment layout: disclaimer + YearSelector header, MonthSelector suppressed"
      min_lines: 15
    - path: "src/app/(app)/mei/page.tsx"
      provides: "the dashboard RSC reading v_mei_year_summary for ?ano and mapping via mei/status.ts"
      contains: "v_mei_year_summary"
  key_links:
    - from: "src/actions/mei.ts"
      to: "src/lib/ownership.ts"
      via: "assertOwnedMeiInvoice on edit/delete + getClaims owner"
      pattern: "assertOwnedMeiInvoice"
    - from: "src/app/(app)/mei/page.tsx"
      to: "public.v_mei_year_summary"
      via: "RLS-scoped select for the selected year"
      pattern: "v_mei_year_summary"
    - from: "src/app/(app)/mei/page.tsx"
      to: "src/lib/mei/status.ts"
      via: "meiStatus(ratio_bp, gross, band) + isNearLimit for the badge/alert"
      pattern: "meiStatus|isNearLimit"
    - from: "src/components/limite-gauge.tsx"
      to: "src/components/adherence-bar.tsx"
      via: "reuses the track + tiered fill + tick/marker pattern"
      pattern: "role=\"progressbar\""
---

<objective>
Ship the first user-visible MEI slice end-to-end: the Server Actions that persist NFs and settings, and the MEI dashboard that reads the summary view and renders the gross-vs-applicable-limit gauge, the tiered status, the 80% alert, and the always-visible disclaimer — plus the MEI nav item, the segment layout, and the YearSelector.

After this plan a user can (via the actions, exercised by tests now and the NF form in Plan 03) record revenue and SEE, on /mei, where they stand against the computed limit for the year, with the disclaimer present. The MonthSelector is suppressed inside /mei because MEI is a calendar-year module.

Purpose: deliver the headline screen (MEI-02/05) and lock the IDOR-safe action contract (MEI-01/03) and the disclaimer (MEI-06), consuming the Plan-01 substrate with zero new schema or fiscal constants.
Output: `actions/mei.ts` + `schemas/mei.ts` + action unit tests; `year-selector`, `mei-disclaimer`, `limite-gauge`, `limite-status-badge` components; the `/mei` segment layout + dashboard page; the nav delta.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-m-dulo-mei-dasn-simei/05-RESEARCH.md
@.planning/phases/05-m-dulo-mei-dasn-simei/05-UI-SPEC.md
@.planning/phases/05-m-dulo-mei-dasn-simei/05-01-SUMMARY.md

# Patterns to clone:
@src/actions/incomes.ts                  # the exact Server-Action shape (Zod→getClaims→parseBRLToCents→moneyWriteError→revalidatePath)
@src/lib/ownership.ts                    # assertOwnedMeiInvoice (added in Plan 01) for edit/delete
@src/lib/mei/status.ts                   # meiStatus + isNearLimit (Plan 01)
@src/lib/mei/limit.ts                    # applicableLimitCents for the proportional-label copy
@src/lib/month.ts                        # currentYear (default ?ano, TZ SP)
@src/components/adherence-bar.tsx        # the gauge pattern LimiteGauge reuses
@src/components/month-selector.tsx       # the segmented-control shape YearSelector mirrors
@src/components/app-sidebar.tsx          # NAV_ITEMS — add the MEI item after Reservas
@src/app/(app)/layout.tsx                # how the shell renders MonthSelector (suppress inside /mei)
@src/components/amount-cell.tsx          # mono money cell for the hero/limit figures
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MEI Zod schemas + NF/settings Server Actions (IDOR-checked)</name>
  <files>src/lib/schemas/mei.ts, src/actions/mei.ts, src/actions/mei.test.ts</files>
  <read_first>
    src/actions/incomes.ts (the verbatim action shape: safeParse→firstIssue, getClaims, parseBRLToCents try/catch, moneyWriteError, revalidatePath; idSchema uuid guard), src/lib/schemas/reserva.ts (schema style), src/lib/ownership.ts (assertOwnedMeiInvoice), src/actions/incomes.test.ts (the chainable-mock unit-test style).
  </read_first>
  <behavior>
    createMeiInvoice: valid FormData → inserts mei_invoices row (user_id from getClaims, amount_cents positive via parseBRLToCents, activity_type in the enum) → { ok:true }; non-money amount → { error:'Valor monetário inválido.' }; bad activity_type / missing tomador → { error } from Zod; no session → { error:'Sessão expirada.' }.
    updateMeiInvoice(id, formData): non-uuid id → { error:'Identificador inválido.' }; a forged id not owned (assertOwnedMeiInvoice false) → { error } and NO update issued; owned id → updates the row.
    deleteMeiInvoice(id): non-uuid → error; owned → deletes under RLS.
    upsertMeiSettings: parses mei_start_date (YYYY-MM-DD), upserts the single (user_id) settings row; bad date → { error }.
    upsertMeiYearFlag(year, hasEmployee): upserts the (user_id, year) flag row; invalid year → { error }.
    Every write revalidates '/mei'.
  </behavior>
  <action>
    Create `src/lib/schemas/mei.ts` with `meiInvoiceSchema` (issuedOn YYYY-MM-DD regex; amount raw pt-BR string; tomador trimmed min 1 max 120; descricao optional string; activityType enum ['comercio_industria','servicos']), `meiSettingsSchema` (meiStartDate YYYY-MM-DD regex), `meiYearFlagSchema` (year int 2000..2100; hasEmployee boolean). Create `src/actions/mei.ts` (`'use server'`) mirroring incomes.ts EXACTLY: reuse the `firstIssue` + `idSchema` helpers and import `parseBRLToCents`, `moneyWriteError` + `assertOwnedMeiInvoice` from `@/lib/ownership`, `createClient` from `@/lib/supabase/server`. Implement `createMeiInvoice`, `updateMeiInvoice(id, formData)`, `deleteMeiInvoice(id)`, `upsertMeiSettings`, `upsertMeiYearFlag(year, hasEmployee)`. On update/delete, validate the uuid then `assertOwnedMeiInvoice` before the `.eq('id', id)` write (defense-in-depth + clean errors, IDOR Pitfall 7). `amount_cents` is the GROSS billed value (comment it: never net). `revalidatePath('/mei')` on success. Write `src/actions/mei.test.ts` with the chainable supabase mock (clone incomes.test.ts) covering the behaviors above. NO new npm deps. Do NOT place fenced code in this action — follow the incomes.ts source.
  </action>
  <verify>
    <automated>npx vitest run src/actions/mei.test.ts && npx tsc --noEmit && echo MEI_ACTIONS_GREEN</automated>
  </verify>
  <done>actions/mei.ts exports the 5 actions; mei.test.ts GREEN (positive insert, money rejection, enum rejection, IDOR-reject-no-write, settings/year-flag upsert, session gate); tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: MEI nav + segment layout + YearSelector + MeiDisclaimer</name>
  <files>src/components/app-sidebar.tsx, src/components/year-selector.tsx, src/components/mei-disclaimer.tsx, src/app/(app)/mei/layout.tsx</files>
  <read_first>
    src/components/month-selector.tsx (the ‹ › segmented control to mirror for years), src/components/app-sidebar.tsx (NAV_ITEMS), src/app/(app)/layout.tsx (shell header — the YearSelector lives in the MEI segment, NOT the global header), 05-UI-SPEC.md §"Disclaimer (MEI-06)" and §"Nav delta".
  </read_first>
  <action>
    Add a MEI nav item to `NAV_ITEMS` in app-sidebar.tsx after Reservas: `{ href: '/mei', label: 'MEI', icon: FileText }` (import `FileText` from lucide-react; the existing `pathname.startsWith('/mei/')` active logic already covers sub-pages). Create `year-selector.tsx` ('use client') mirroring MonthSelector: reads `?ano` (default `currentYear()`), ‹ › Buttons shifting the year by ±1 via `router.replace`, arrows `aria-label` "Ano anterior"/"Próximo ano", the year shown mono/600. Create `mei-disclaimer.tsx`: a full-width `--muted` rounded notice (NOT a thin footer), an `Info` lucide glyph (calm, not destructive), `role="note"`, body in `text-muted-foreground` with the key clause "Este módulo é informativo e não constitui consultoria fiscal." in `text-foreground` font-semibold, plus the second line from the UI-SPEC copy; not dismissible. Create `src/app/(app)/mei/layout.tsx` — a server component segment layout that renders, above `{children}`, a header row containing the page area's `YearSelector` and the `MeiDisclaimer` banner directly below (so it is on every /mei screen). This layout does NOT re-render the sidebar/shell (the parent (app)/layout owns that); it must NOT render the global MonthSelector. (The MonthSelector stays in the shell header for non-MEI routes — acceptable; the MEI screens simply read ?ano via YearSelector and ignore ?mes.)
  </action>
  <verify>
    <automated>grep -q "FileText" src/components/app-sidebar.tsx && grep -q "consultoria fiscal" src/components/mei-disclaimer.tsx && grep -q "Ano anterior" src/components/year-selector.tsx && test -f "src/app/(app)/mei/layout.tsx" && npx tsc --noEmit && echo MEI_SHELL_OK</automated>
  </verify>
  <done>MEI nav item present; YearSelector mirrors MonthSelector with year arrows + aria labels; MeiDisclaimer renders the MEI-06 copy as a muted standing banner; the /mei segment layout renders disclaimer + YearSelector on every MEI screen; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: MEI dashboard — LimiteGauge + status badge + alert (RSC reads the summary view)</name>
  <files>src/components/limite-gauge.tsx, src/components/limite-status-badge.tsx, src/app/(app)/mei/page.tsx</files>
  <read_first>
    src/components/adherence-bar.tsx (the gauge to reuse — track + clamped tiered fill + tick + aria), src/lib/mei/status.ts (meiStatus/isNearLimit), src/lib/mei/limit.ts (applicableLimitCents for the proportional-label copy), src/lib/money.ts (formatCents), src/lib/month.ts (currentYear), 05-UI-SPEC.md §"1. MEI dashboard" + §"Semantic Color — MEI Limit Status" + §"Copywriting Contract".
  </read_first>
  <action>
    Create `limite-gauge.tsx` reusing the AdherenceBar pattern verbatim: an `h-2` `bg-muted` track + a tiered fill (token chosen from the meiStatus result — income/consumption/destructive per the UI-SPEC color table) clamped at 100% width with the % label able to read >100% + an 80% threshold tick + a 100% limite marker; `role="progressbar"` + `aria-valuenow`/`min`(0)/`max`(limite cents) + `aria-valuetext` ("Receita bruta R$ X de R$ Y — 74,2% do limite, aproximando"). Create `limite-status-badge.tsx`: a `badge` rendering the status label + lucide glyph in the status token (verde=`check-circle-2`/income, âmbar=`triangle-alert`/consumption, vermelho-banda=`alert-octagon`/destructive, vermelho-fora=`octagon-x`/destructive) — the single source of the status wording (the four labels from the Copywriting Contract). Replace `src/app/(app)/mei/page.tsx` (the dashboard RSC): read `?ano` (default currentYear), select the user's `v_mei_year_summary` row for that year (RLS-scoped); if no mei_settings row exists → render the "Configure seu MEI para começar" empty state with the CTA to /mei/configuracoes (never a wrong/zero limit). Otherwise render the limite card: hero "Receita bruta em {ano}" + accumulated gross (28px mono, status-colored), the COMPUTED applicable-limit line (proportional label via applicableLimitCents when year==opening, "Limite anual {ano}" otherwise — NEVER a bare "R$ 81.000"), the LimiteGauge, the LimiteStatusBadge, the remaining-to-limit line when under 100%. Alert affordance (MEI-05): when `isNearLimit(ratio_bp)` render the inline amber alert row; at ≥100% render the within-band vs over-band consequence line per the Copywriting Contract (decided by gross ≤ band_ceiling_cents). Quick links to Registrar NF / Relatório / Configurações. Empty (configured, no NFs) → R$0,00 verde 0% + the "Registre sua primeira NF" hint. Use formatCents for all money; percentages via `Intl.NumberFormat('pt-BR',{style:'percent',maximumFractionDigits:1})`. No fiscal literal in this file (all numbers come from the view + rules/limit/status libs).
  </action>
  <verify>
    <automated>grep -q "v_mei_year_summary" "src/app/(app)/mei/page.tsx" && grep -Eq "meiStatus|isNearLimit" "src/app/(app)/mei/page.tsx" && grep -q "role=\"progressbar\"" src/components/limite-gauge.tsx && ! grep -qE "\b81000\b|\b97200\b" "src/app/(app)/mei/page.tsx" && npx next build 2>&1 | grep -q "/mei" && echo MEI_DASH_OK</automated>
  </verify>
  <done>/mei RSC reads the summary view for ?ano, maps via mei/status.ts, renders hero + computed-limit line + LimiteGauge + status badge + 80%/100% alert; empty/no-config + empty/no-NFs states present; no hardcoded 81k/97200 in the page; build compiles /mei.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → createMeiInvoice / updateMeiInvoice / upsertMeiSettings | Untrusted FormData + a client-supplied mei_invoice_id cross into DB writes |
| RSC read → v_mei_year_summary | The dashboard reads aggregated revenue/limit; RLS+security_invoker must scope it to the caller |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Tampering / Elevation | updateMeiInvoice / deleteMeiInvoice (forged id) | mitigate | uuid schema + `assertOwnedMeiInvoice` re-derive BEFORE the `.eq('id', id)` write (Task 1) |
| T-05-06 | Tampering | non-money / wrong-sign amount injected | mitigate | Zod safeParse + `parseBRLToCents` strictly-positive cents; `moneyWriteError` maps 23514 (Task 1) |
| T-05-07 | Information Disclosure | dashboard reading another user's summary | mitigate | RLS-active server client + `v_mei_year_summary` security_invoker (proven in Plan 01); no service-role on the read path |
| T-05-08 | Repudiation | implied tax advice / liability | mitigate | persistent visible `MeiDisclaimer` in the /mei segment layout; framed as informational (MEI-06, Task 2) |
| T-05-04 | Tampering | hardcoded 81k leaking into the page | mitigate | the dashboard reads the computed limit from the view + limit.ts; the grep gate forbids `81000`/`97200` in page.tsx (Task 3) |
</threat_model>

<verification>
- `npx vitest run src/actions/mei.test.ts` — action contract GREEN (insert, money/enum reject, IDOR no-write, settings/year-flag, session gate).
- `npx tsc --noEmit` — clean.
- `npx next build` — compiles `/mei`.
- `grep -E "\b81000\b|\b97200\b" "src/app/(app)/mei/page.tsx"` returns nothing.
- Manual (Plan 04 walkthrough): disclaimer visible on /mei; gauge + status reflect seeded NFs; ≥80% shows the amber alert.
</verification>

<success_criteria>
- The 5 MEI Server Actions exist, mirror incomes.ts, and are IDOR-checked on edit/delete; unit tests GREEN.
- The /mei dashboard reads `v_mei_year_summary` for ?ano and renders the gross hero, the COMPUTED applicable-limit line, the LimiteGauge, the status badge, and the 80%/100% alert — with no hardcoded fiscal literal.
- The MeiDisclaimer is rendered once in the /mei segment layout and visible on every MEI screen (MEI-06).
- The MEI nav item + YearSelector are in place; MonthSelector is not shown inside /mei.
</success_criteria>

<output>
Create `.planning/phases/05-m-dulo-mei-dasn-simei/05-02-SUMMARY.md` when done.
</output>
