---
phase: 05-m-dulo-mei-dasn-simei
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - src/components/atividade-badge.tsx
  - src/components/nf-form.tsx
  - src/components/nf-table.tsx
  - src/components/mei-settings-form.tsx
  - src/components/dasn-report-view.tsx
  - src/components/export-csv-button.tsx
  - src/app/(app)/mei/notas/page.tsx
  - src/app/(app)/mei/configuracoes/page.tsx
  - src/app/(app)/mei/relatorio/page.tsx
autonomous: true
requirements: [MEI-01, MEI-03, MEI-04, MEI-06]
user_setup: []

must_haves:
  truths:
    - "A user can register/edit/delete an emitted NF (date, value, tomador, descrição, activity_type comércio-indústria vs serviços) via a dialog form, and see them listed for the selected year with a gross-revenue year total (MEI-01/03)"
    - "A user can set the MEI start date (drives the proportional first-year cap) and the per-year has_employee flag (a DASN field) (MEI-03)"
    - "The DASN report shows EXACTLY the DASN-SIMEI fields — total gross revenue + comércio/serviços split (that sums to the total) + employee Sim/Não — for the selected year, and exports them to CSV (MEI-04)"
    - "The MeiDisclaimer is present on the notas, configuracoes, and relatorio screens (inherited from the segment layout) and included in the report's print view (MEI-06)"
  artifacts:
    - path: "src/components/nf-form.tsx"
      provides: "NF create/edit dialog (RHF + zodResolver, transacao-form pattern), activity_type control"
      min_lines: 40
    - path: "src/components/nf-table.tsx"
      provides: "dense NF list for the year + gross year total footer"
      min_lines: 30
    - path: "src/components/mei-settings-form.tsx"
      provides: "mei_start_date + per-year has_employee form"
      min_lines: 25
    - path: "src/components/dasn-report-view.tsx"
      provides: "print-friendly DASN report card (total + split + employee)"
      contains: "Receita bruta total"
    - path: "src/components/export-csv-button.tsx"
      provides: "CSV download button driving meiReportToCsv (the Phase-6 DATA-01 pattern)"
      contains: "meiReportToCsv"
    - path: "src/app/(app)/mei/relatorio/page.tsx"
      provides: "report RSC reading v_mei_year_summary for ?ano"
      contains: "v_mei_year_summary"
  key_links:
    - from: "src/components/nf-form.tsx"
      to: "src/actions/mei.ts"
      via: "createMeiInvoice / updateMeiInvoice"
      pattern: "createMeiInvoice|updateMeiInvoice"
    - from: "src/components/mei-settings-form.tsx"
      to: "src/actions/mei.ts"
      via: "upsertMeiSettings + upsertMeiYearFlag"
      pattern: "upsertMeiSettings|upsertMeiYearFlag"
    - from: "src/components/export-csv-button.tsx"
      to: "src/lib/mei/csv.ts"
      via: "meiReportToCsv(report) → Blob download"
      pattern: "meiReportToCsv"
    - from: "src/app/(app)/mei/relatorio/page.tsx"
      to: "public.v_mei_year_summary"
      via: "RLS-scoped read of the consolidated row for ?ano"
      pattern: "v_mei_year_summary"
---

<objective>
Complete the MEI module surfaces: the NF entry form + list (register/edit/delete emitted NFs, with the per-NF comércio/serviços split), the settings form (start date + per-year employee flag), and the DASN-SIMEI report (the exact declaration fields, on screen + CSV export + print-friendly).

This consumes the Plan-02 actions and segment layout with no new actions or schema — pure UI assembly over the shipped substrate. The CSV export establishes the reusable pattern the roadmap notes Phase 6 (DATA-01) will reuse.

Purpose: close MEI-01/03 (NF CRUD + activity split + employee flag) and MEI-04 (consolidated DASN report + CSV), with the disclaimer (MEI-06) inherited from the segment layout and carried into the print view.
Output: `atividade-badge`, `nf-form`, `nf-table`, `mei-settings-form`, `dasn-report-view`, `export-csv-button` components; the `/mei/notas`, `/mei/configuracoes`, `/mei/relatorio` pages.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-m-dulo-mei-dasn-simei/05-UI-SPEC.md
@.planning/phases/05-m-dulo-mei-dasn-simei/05-02-SUMMARY.md

# Patterns to clone:
@src/actions/mei.ts                      # the actions this slice calls (Plan 02)
@src/lib/mei/csv.ts                      # meiReportToCsv (Plan 01)
@src/components/transacao-form.tsx       # the RHF + zodResolver + dialog form pattern for NfForm
@src/components/extrato-table.tsx        # the dense-table grammar NfTable mirrors
@src/components/money-input.tsx          # the MoneyInput for the NF amount
@src/components/amount-cell.tsx          # the mono money cell for NF values + report figures
@src/components/category-badge.tsx       # neutral badge styling AtividadeBadge mirrors
@src/app/(app)/reservas/page.tsx         # an RSC page reading a security_invoker view + empty/error states
</context>

<tasks>

<task type="auto">
  <name>Task 1: NF form + list — register/edit/delete NFs for the year (MEI-01/03)</name>
  <files>src/components/atividade-badge.tsx, src/components/nf-form.tsx, src/components/nf-table.tsx, src/app/(app)/mei/notas/page.tsx</files>
  <read_first>
    src/components/transacao-form.tsx (RHF + zodResolver + dialog + Server Action call), src/components/extrato-table.tsx (dense table + dropdown actions + delete alert-dialog), src/components/money-input.tsx, src/components/amount-cell.tsx, src/lib/schemas/mei.ts (meiInvoiceSchema), src/actions/mei.ts (createMeiInvoice/updateMeiInvoice/deleteMeiInvoice), 05-UI-SPEC.md §"2. Registrar NF" + §"Copywriting Contract".
  </read_first>
  <action>
    Create `atividade-badge.tsx`: a neutral (grayscale chrome, NOT a money color) `badge` rendering "Comércio/Indústria" vs "Serviços" from `activity_type`. Create `nf-form.tsx` ('use client') mirroring transacao-form: fields issued_on (date input/popover+calendar, default today SP), amount (MoneyInput), tomador (input), descricao (input), activity_type (a `select` or two-option control: Comércio/Indústria ↔ Serviços) — RHF + zodResolver(meiInvoiceSchema); create routes to `createMeiInvoice`, edit (seeded from a row) routes to `updateMeiInvoice(id, ...)`; sonner toast "Nota fiscal salva". Create `nf-table.tsx` mirroring the extrato grammar: columns Data (dd/MM), Tomador (truncate+tooltip), Descrição (truncate+tooltip), Atividade (AtividadeBadge), Valor (AmountCell, kind='income' so it shows green `+` — NF value is money-in gross), ações (dropdown: Editar → NfForm edit, Excluir → alert-dialog "Excluir nota fiscal / Esta ação não pode ser desfeita." → deleteMeiInvoice, toast "Nota fiscal excluída"); sort by data desc; a totais footer summing the year's gross (mono/600, right-aligned — the same number the dashboard hero shows). Create `src/app/(app)/mei/notas/page.tsx` (RSC): read `?ano` (default currentYear), select mei_invoices between yearBounds(ano) ordered by issued_on desc (RLS-scoped); h1 "Notas fiscais" + primary "Registrar NF" CTA; pass rows to NfTable; empty state ("Nenhuma nota em {ano}" + CTA) and inline error per the UI-SPEC. The disclaimer + YearSelector come from the segment layout (do not re-render them here).
  </action>
  <verify>
    <automated>grep -Eq "createMeiInvoice|updateMeiInvoice" src/components/nf-form.tsx && grep -q "deleteMeiInvoice" src/components/nf-table.tsx && test -f "src/app/(app)/mei/notas/page.tsx" && npx tsc --noEmit && npx next build 2>&1 | grep -q "/mei/notas" && echo NF_SLICE_OK</automated>
  </verify>
  <done>NfForm creates/edits NFs via the actions with the activity_type control; NfTable lists the year's NFs with AtividadeBadge + gross year total + edit/delete; /mei/notas RSC reads the year's NFs with empty/error states; tsc clean; build compiles /mei/notas.</done>
</task>

<task type="auto">
  <name>Task 2: MEI settings — start date + per-year employee flag (MEI-03)</name>
  <files>src/components/mei-settings-form.tsx, src/app/(app)/mei/configuracoes/page.tsx</files>
  <read_first>
    src/components/transacao-form.tsx (RHF + zodResolver form + Server Action), src/actions/mei.ts (upsertMeiSettings, upsertMeiYearFlag), src/lib/schemas/mei.ts, src/components/ui/switch (the funcionário toggle), 05-UI-SPEC.md §"3. Configurações MEI".
  </read_first>
  <action>
    Create `mei-settings-form.tsx` ('use client'): a small single-column form with `mei_start_date` (date popover/calendar; helper "Usamos esta data para calcular seu limite proporcional no primeiro ano.") wired to `upsertMeiSettings`, and "Tinha funcionário em {ano}?" (a `switch`, default Não, scoped to the selected ?ano; helper "Campo exigido pela declaração DASN-SIMEI.") wired to `upsertMeiYearFlag(year, hasEmployee)`. Save → sonner "Configurações salvas". Create `src/app/(app)/mei/configuracoes/page.tsx` (RSC): read `?ano`, read the user's mei_settings row + the mei_year_flags row for ?ano (RLS-scoped, may be null on first run), seed the form; h1 "Configurações MEI". The disclaimer + YearSelector come from the segment layout. First-run renders empty start date + funcionário=Não; inline error state per the UI-SPEC.
  </action>
  <verify>
    <automated>grep -Eq "upsertMeiSettings|upsertMeiYearFlag" src/components/mei-settings-form.tsx && test -f "src/app/(app)/mei/configuracoes/page.tsx" && npx tsc --noEmit && npx next build 2>&1 | grep -q "/mei/configuracoes" && echo SETTINGS_OK</automated>
  </verify>
  <done>MeiSettingsForm saves the start date + per-year employee flag via the actions; /mei/configuracoes seeds the form from the user's rows for ?ano; first-run + error states present; tsc clean; build compiles /mei/configuracoes.</done>
</task>

<task type="auto">
  <name>Task 3: DASN report view + CSV export + print (MEI-04)</name>
  <files>src/components/dasn-report-view.tsx, src/components/export-csv-button.tsx, src/app/(app)/mei/relatorio/page.tsx</files>
  <read_first>
    src/lib/mei/csv.ts (meiReportToCsv — Plan 01), src/components/amount-cell.tsx, src/app/(app)/reservas/page.tsx (RSC reading a security_invoker view + empty state), 05-UI-SPEC.md §"4. Relatório DASN-SIMEI" + §"Disclaimer (print)" + §"Copywriting Contract".
  </read_first>
  <action>
    Create `dasn-report-view.tsx`: a print-friendly `--card` two-column label/value grid showing the EXACT DASN-SIMEI fields for {ano} — cabeçalho "Ano-base {ano} · período jan–dez/{ano}" + DASN deadline reference "Prazo de entrega: 31 de maio de {ano+1}." (deadline from `rules.ts` DASN_DEADLINE, not a literal); "Receita bruta total" (hero mono — the accumulated gross); the split "Comércio, indústria e transporte" total + "Prestação de serviços" total (two mono figures that sum to the total); "Empregado durante o ano-calendário: Sim / Não" (from has_employee). No teal, no decorative color — reads like a document. Include the MeiDisclaimer in the report header so it survives print. Create `export-csv-button.tsx` ('use client'): an outline `button` "Exportar CSV" that calls `meiReportToCsv(report)` and triggers a Blob download (filename `dasn-{ano}.csv`); sonner "Relatório exportado". Create `src/app/(app)/mei/relatorio/page.tsx` (RSC): read `?ano`, select the `v_mei_year_summary` row for ?ano (RLS-scoped); pass the consolidated fields to DasnReportView + ExportCsvButton; h1 "Relatório DASN-SIMEI"; a print affordance ("Imprimir" / browser print). Empty (no NFs) → still render the report with zeros + the "Nenhuma receita registrada em {ano}." note (a zero-revenue MEI still declares — do NOT block). Empty (no mei_start_date) → an info row linking to Configurações for the applicable-limit reference, but the DASN totals (which do not need the limit) still render. Inline error state per the UI-SPEC. Add a print-targeted treatment so the sidebar/nav are hidden and the disclaimer + report render on white.
  </action>
  <verify>
    <automated>grep -q "meiReportToCsv" src/components/export-csv-button.tsx && grep -q "Receita bruta total" src/components/dasn-report-view.tsx && grep -q "v_mei_year_summary" "src/app/(app)/mei/relatorio/page.tsx" && npx tsc --noEmit && npx next build 2>&1 | grep -q "/mei/relatorio" && echo REPORT_OK</automated>
  </verify>
  <done>DasnReportView shows total + split (summing to total) + employee for ?ano with the disclaimer in the print header; ExportCsvButton downloads the DASN CSV via meiReportToCsv; /mei/relatorio reads the summary view with zero-revenue + no-config + error states; tsc clean; build compiles /mei/relatorio.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client form → mei actions | NF + settings FormData crosses into DB writes (validated in Plan 02 actions) |
| RSC reads → mei_invoices / v_mei_year_summary | Year-scoped reads of the user's NFs + consolidated summary; RLS+security_invoker scope them |
| browser → CSV file | The exported report leaves the app as a downloaded file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Information Disclosure | NfTable / report reading another user's NFs | mitigate | RLS-active server client; reads scoped to the caller + the selected year; `v_mei_year_summary` security_invoker (Plan 01) |
| T-05-10 | Tampering | edit/delete from the NF list (forged id) | mitigate | the dropdown calls updateMeiInvoice/deleteMeiInvoice which re-derive ownership via assertOwnedMeiInvoice (Plan 02) |
| T-05-11 | Information Disclosure | CSV export leaking another user's data | accept | the export serializes ONLY the RLS-scoped row already read by the caller's own page; no cross-user data is reachable |
| T-05-08 | Repudiation | implied tax advice on the report | mitigate | MeiDisclaimer inherited from the segment layout + included in the report print header (MEI-06, Task 3) |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | none — zero packages installed this phase (RESEARCH audit N/A); no install task |
</threat_model>

<verification>
- `npx tsc --noEmit` — clean.
- `npx next build` — compiles `/mei/notas`, `/mei/configuracoes`, `/mei/relatorio`.
- `grep meiReportToCsv src/components/export-csv-button.tsx` — the CSV button drives the Plan-01 serializer.
- `SUPABASE_DISABLE_TELEMETRY=1 npx vitest run` — full suite still GREEN (no regression).
- Manual (Plan 04 walkthrough): register NFs → list + year total + dashboard gauge update; settings drive the proportional limit; report shows the split + employee; CSV downloads; disclaimer on every screen + in print.
</verification>

<success_criteria>
- A user can register/edit/delete NFs with the comércio/serviços split and see them listed for the year with a gross total (MEI-01/03).
- A user can set the start date + per-year employee flag (MEI-03).
- The DASN report shows total + split (summing to total) + employee for the year and exports them to CSV (MEI-04).
- The disclaimer is present on all three screens and in the report print view (MEI-06).
- tsc clean; build compiles all three MEI sub-routes.
</success_criteria>

<output>
Create `.planning/phases/05-m-dulo-mei-dasn-simei/05-03-SUMMARY.md` when done.
</output>
