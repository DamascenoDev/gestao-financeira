---
phase: 05-m-dulo-mei-dasn-simei
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0025_mei.sql
  - supabase/migrations/0026_mei_views.sql
  - src/types/database.types.ts
  - src/lib/mei/rules.ts
  - src/lib/mei/rules.test.ts
  - src/lib/mei/limit.ts
  - src/lib/mei/limit.test.ts
  - src/lib/mei/status.ts
  - src/lib/mei/status.test.ts
  - src/lib/mei/csv.ts
  - src/lib/mei/csv.test.ts
  - src/lib/ownership.ts
  - tests/mei-invoice-rls.test.ts
  - tests/mei-report.test.ts
  - tests/mei-view-leak.test.ts
  - tests/mei-idor.test.ts
  - .planning/phases/05-m-dulo-mei-dasn-simei/05-VALIDATION.md
autonomous: true
requirements: [MEI-01, MEI-02, MEI-03, MEI-04, MEI-05, MEI-06]
user_setup: []

must_haves:
  truths:
    - "The four MEI rule numbers (R$81.000, R$6.750/mês, 20% band, 31-May deadline) live ONLY in src/lib/mei/rules.ts, and a test asserts the SQL view literals equal those constants (never a hardcoded 81k elsewhere — D-MEI-RULES, MEI-02)"
    - "v_mei_year_summary is security_invoker and computes gross + comércio/serviços split + applicable_limit + band_ceiling + ratio_bp per (user, year); user B reads ZERO of user A's summary (MEI-02/04)"
    - "Applicable limit is proportional in the opening calendar year (R$6.750 × active months, opening month full) and R$81.000 thereafter — Jul→6→R$40.500, Mar→10→R$67.500 (MEI-02)"
    - "Tiered status maps verde<8000bp / âmbar 8000–9999 / vermelho-banda (≥10000, gross≤band) / vermelho-fora (gross>band); 80% alert fires at exactly 8000bp (MEI-02/05)"
    - "mei_settings, mei_year_flags, mei_invoices each have RLS USING+WITH CHECK auth.uid()=user_id, grants, indexes; a forged mei_invoice_id is rejected by assertOwnedMeiInvoice (MEI-01/03, IDOR)"
  artifacts:
    - path: "supabase/migrations/0025_mei.sql"
      provides: "mei_settings (mei_start_date), mei_year_flags (has_employee per year), mei_invoices (activity_type) + uniform RLS/grants/indexes"
      contains: "create table"
    - path: "supabase/migrations/0026_mei_views.sql"
      provides: "v_mei_year_summary security_invoker view: gross + split + applicable limit + band + ratio"
      contains: "security_invoker = true"
    - path: "src/lib/mei/rules.ts"
      provides: "the SINGLE source of the four verified 2026 MEI rule constants"
      contains: "MEI_ANNUAL_LIMIT_CENTS"
    - path: "src/lib/mei/limit.ts"
      provides: "applicableLimitCents + bandCeilingCents pure oracle (parity with the SQL view)"
      exports: ["applicableLimitCents", "bandCeilingCents"]
    - path: "src/lib/mei/status.ts"
      provides: "pure tiered-status mapper + isNearLimit (adherence.ts twin)"
      exports: ["meiStatus", "isNearLimit"]
    - path: "src/lib/mei/csv.ts"
      provides: "DASN-ready CSV serializer (establishes the Phase-6 DATA-01 export pattern)"
      exports: ["meiReportToCsv"]
  key_links:
    - from: "src/lib/mei/rules.test.ts"
      to: "supabase/migrations/0026_mei_views.sql"
      via: "reads the migration text and asserts the literals equal the constants"
      pattern: "8100000|MEI_ANNUAL_LIMIT_CENTS"
    - from: "supabase/migrations/0026_mei_views.sql"
      to: "public.mei_invoices"
      via: "sum(amount_cents) grouped by year, filtered by activity_type"
      pattern: "v_mei_year_summary"
    - from: "src/lib/ownership.ts"
      to: "public.mei_invoices"
      via: "assertOwnedMeiInvoice re-derive (clone of assertOwnedStatement)"
      pattern: "assertOwnedMeiInvoice"
---

<objective>
Lay the MEI module substrate BEFORE any UI slice: the database schema (3 tables + 1 security_invoker summary view), the pure rule/limit/status/csv libs that own all fiscal numbers and math, the IDOR ownership helper, and the failing Wave-0 tests that pin the applicable-limit math, the tiered status, the DASN report fields, RLS isolation, the view-leak proof, and the SQL↔TS parity guard.

This is the foundation plan (the Phase 2–4 lesson: shared schema + helpers used by multiple slices land in one Wave-1 substrate plan, so no later slice cross-imports a sibling within a wave). [BLOCKING]: the migrations must be applied to the LOCAL stack and types regenerated before the dependent integration tests (and Plans 02–03) can run.

Purpose: 90% of this phase is assembling shipped primitives; the 10% genuinely new code is the applicable-limit math, and PITFALLS 12/13/14 say that is exactly where the bugs live — so it is TDD'd hardest here, off the verified 2026 numbers, with the SQL view and the TS oracle held in parity by a test.
Output: migrations 0025/0026 applied LOCAL + regenerated types; `src/lib/mei/{rules,limit,status,csv}.ts` + their unit tests; `assertOwnedMeiInvoice` in ownership.ts; 4 local-DB integration tests; VALIDATION.md marked wave_0_complete.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-m-dulo-mei-dasn-simei/05-CONTEXT.md
@.planning/phases/05-m-dulo-mei-dasn-simei/05-RESEARCH.md
@.planning/phases/05-m-dulo-mei-dasn-simei/05-VALIDATION.md

# Patterns to clone (read the cited sections, do not re-derive):
@supabase/migrations/0013_reservas.sql          # uniform per-table RLS + grants + index shape
@supabase/migrations/0014_adherence_views.sql   # security_invoker view + integer-cents math + /0 guard
@src/lib/adherence.ts                            # the tiered-status mapper this twins (status.ts)
@src/lib/ownership.ts                            # assertOwnedStatement → clone as assertOwnedMeiInvoice
@src/lib/money.ts                               # bigint-centavos invariant (rules constants are cents)
@src/lib/month.ts                               # currentYear/yearBounds (TZ America/Sao_Paulo)
@tests/helpers/local-supabase.ts                # the two-user local-DB harness (createUser/userClient)
@tests/view-leak.test.ts                        # the security_invoker view-leak proof to clone
@tests/reserva-idor.test.ts                     # the IDOR two-half proof to clone
</context>

<tasks>

<task type="auto">
  <name>Task 1 [BLOCKING]: MEI schema migrations + apply local + regen types</name>
  <files>supabase/migrations/0025_mei.sql, supabase/migrations/0026_mei_views.sql, src/types/database.types.ts</files>
  <read_first>
    supabase/migrations/0013_reservas.sql (RLS+grants+index shape), supabase/migrations/0014_adherence_views.sql (security_invoker view + integer math + /0 guard), 05-RESEARCH.md §"View sketch (v_mei_year_summary)" and §"Applicable-Limit Computation".
  </read_first>
  <action>
    Create `0025_mei.sql` with THREE tables using the uniform 0013 shape (every table: `user_id uuid not null references auth.users(id) on delete cascade`; `enable row level security`; `grant select,insert,update,delete ... to authenticated, service_role`; a `for all to authenticated using ((select auth.uid()) = user_id) with check ((select auth.uid()) = user_id)` policy; a `user_id` index):
    (a) `mei_settings` — `id uuid pk default gen_random_uuid()`, `user_id`, `mei_start_date date not null`, `created_at timestamptz default now()`, plus `unique(user_id)` (one settings row per user; the start date is single-valued per D-MEI in CONTEXT).
    (b) `mei_year_flags` — resolves Open Question 1 as the per-year flag table: `id`, `user_id`, `year int not null`, `has_employee boolean not null default false`, `unique(user_id, year)`. Clean RLS, clean join in the view, no JSON.
    (c) `mei_invoices` — `id`, `user_id`, `issued_on date not null` (civil date, no time — year-boundary safety, Pitfall 4), `amount_cents bigint not null check (amount_cents > 0)` (GROSS receita bruta — comment the column "receita bruta, nunca líquida"), `tomador text not null`, `descricao text not null default ''`, `activity_type text not null check (activity_type in ('comercio_industria','servicos'))` (per-NF split, MEI-03), `created_at`. Index on `(user_id, issued_on)`.
    Create `0026_mei_views.sql` with `v_mei_year_summary` `with (security_invoker = true)` per the RESEARCH view sketch: group mei_invoices by `(user_id, to_char(issued_on,'YYYY')::int as year)`; emit `gross_cents`, `comercio_cents` (sum filter activity_type='comercio_industria'), `servicos_cents` (sum filter ='servicos'); join mei_settings for `opening_year`/`opening_month` via `extract(...)`; left join mei_year_flags for `coalesce(has_employee,false)`. Compute `applicable_limit_cents` via the CASE (year<opening→0; year=opening→`675000 * (12 - opening_month + 1)`; else `8100000`); `band_ceiling_cents` = applicable `* 12000 / 10000` (integer ×1.20); `ratio_bp` = `case when applicable=0 then null else gross_cents*10000/applicable end` (guard /0, matching the 0014 sem-receita pattern). Repeat the limit CASE or wrap in a sub-CTE to avoid drift. Add a header comment on the view literals: `-- MUST match src/lib/mei/rules.ts (asserted by src/lib/mei/rules.test.ts)`. `grant select on public.v_mei_year_summary to authenticated`.
    Apply to the LOCAL stack and regenerate types: run `npm run db:reset` (replays 0001–0026 clean against the running local stack) then `npm run gen:types`. Confirm `mei_invoices`, `mei_settings`, `mei_year_flags`, and `v_mei_year_summary` appear in `src/types/database.types.ts`.
  </action>
  <verify>
    <automated>npm run db:reset && npm run gen:types && grep -q "mei_invoices" src/types/database.types.ts && grep -q "v_mei_year_summary" src/types/database.types.ts && grep -q "security_invoker = true" supabase/migrations/0026_mei_views.sql && echo MIGRATIONS_OK</automated>
  </verify>
  <done>db:reset replays 0001–0026 cleanly; types regenerated and contain the 3 tables + the view; the view carries `security_invoker = true`; the view literal `8100000` appears with the "MUST match rules.ts" comment.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rule constants + limit/status/csv pure libs (RED→GREEN unit oracle)</name>
  <files>src/lib/mei/rules.ts, src/lib/mei/rules.test.ts, src/lib/mei/limit.ts, src/lib/mei/limit.test.ts, src/lib/mei/status.ts, src/lib/mei/status.test.ts, src/lib/mei/csv.ts, src/lib/mei/csv.test.ts</files>
  <read_first>
    05-RESEARCH.md §"Centralized constant module", §"Applicable-limit helper", §"Tiered-status mapper"; src/lib/adherence.ts (the mapper pattern to twin); src/lib/money.ts (formatCents for the CSV pt-BR amounts).
  </read_first>
  <behavior>
    rules.test.ts: MEI_ANNUAL_LIMIT_CENTS === 8_100_000; MEI_MONTHLY_RATE_CENTS === 675_000; MEI_TOLERANCE_BP === 2000; MEI_ALERT_BP === 8000; DASN_DEADLINE === {month:5,day:31}; MEI_RULES_YEAR === 2026. PARITY: read `supabase/migrations/0026_mei_views.sql` as text and assert it contains the literal `8100000` AND `675000` AND `12000` (the x1.20 numerator) — so an SQL/TS drift fails loudly (the never-hardcode-81k-elsewhere property). Also assert (grep-style) that NO `.ts` file under `src/` except `src/lib/mei/rules.ts` contains a bare `81000`/`97200` literal.
    limit.test.ts: applicableLimitCents(2026,'2026-07-01') === 4_050_000 (Jul→6 months→R$40.500); applicableLimitCents(2026,'2026-03-15') === 6_750_000 (Mar→10→R$67.500); applicableLimitCents(2027,'2026-03-15') === 8_100_000 (full year after opening); applicableLimitCents(2025,'2026-03-15') === 0 (before opening). bandCeilingCents(8_100_000) === 9_720_000; bandCeilingCents(4_050_000) === 4_860_000 (proportional band, NOT the full-year 97.200).
    status.test.ts: meiStatus(7999, …) → 'verde'; meiStatus(8000, gross<band) → 'ambar'; meiStatus(9999,…) → 'ambar'; meiStatus(10000, gross===band) → 'vermelho-banda'; meiStatus(12500, gross>band) → 'vermelho-fora'; meiStatus(null,…) → 'verde' (pre-opening). isNearLimit(7999)===false; isNearLimit(8000)===true (the MEI-05 80% edge); isNearLimit(null)===false.
    csv.test.ts: meiReportToCsv(report) emits a header row + a data row containing ano, receita bruta total, comércio total, serviços total, funcionário Sim/Não, limite aplicável — all money via formatCents (pt-BR), prefixed with the UTF-8 byte-order mark, `;`-delimited (Excel pt-BR). A zero-revenue year still emits a valid row (R$ 0,00).
  </behavior>
  <action>
    Write the four pure modules and their tests TDD-style (tests first, RED, then implement to GREEN). `rules.ts` is the verbatim constant module from RESEARCH §"Centralized constant module" — the ONLY place the four numbers live (D-MEI-RULES). `limit.ts` exports `applicableLimitCents(reportYear, meiStartDate)` and `bandCeilingCents(applicable)` exactly per RESEARCH §"Applicable-limit helper" (integer math, opening month counts full via `12 - openingMonth + 1`). `status.ts` exports `meiStatus(ratioBp, grossCents, bandCeilingCents): 'verde'|'ambar'|'vermelho-banda'|'vermelho-fora'` and `isNearLimit(ratioBp)` per RESEARCH §"Tiered-status mapper" — NO DB, NO limit recompute, consumes the view's numbers; use BigInt comparison for gross vs band. `csv.ts` exports `meiReportToCsv(report)` building the DASN-ready CSV string; reuse `formatCents` for amounts; prepend the UTF-8 byte-order mark, generated in code as `String.fromCharCode(0xFEFF)` (code point U+FEFF) so Excel pt-BR opens it correctly — NEVER paste a literal invisible BOM character into the source file; use `;` as the delimiter; this establishes the export pattern the roadmap notes Phase 6 (DATA-01) reuses. NO new npm deps. Run `npx vitest run src/lib/mei/` and confirm all GREEN.
  </action>
  <verify>
    <automated>npx vitest run src/lib/mei/ && npx tsc --noEmit && echo MEI_LIBS_GREEN</automated>
  </verify>
  <done>rules/limit/status/csv unit suites GREEN; the parity test reads the 0026 migration and confirms the SQL literals match the constants; the "no bare 81000 in src outside rules.ts" guard passes; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: assertOwnedMeiInvoice + 4 local-DB Wave-0 integration tests (failing harness)</name>
  <files>src/lib/ownership.ts, tests/mei-invoice-rls.test.ts, tests/mei-report.test.ts, tests/mei-view-leak.test.ts, tests/mei-idor.test.ts, .planning/phases/05-m-dulo-mei-dasn-simei/05-VALIDATION.md</files>
  <read_first>
    src/lib/ownership.ts (assertOwnedStatement — the verbatim clone source), tests/view-leak.test.ts (the security_invoker proof shape + createUser/userClient harness), tests/reserva-idor.test.ts (the two-half IDOR proof shape), 05-VALIDATION.md (the 8-row Wave-0 map).
  </read_first>
  <action>
    Add `assertOwnedMeiInvoice(supabase, id)` to `src/lib/ownership.ts` as a verbatim clone of `assertOwnedStatement` applied to `mei_invoices` (select id where id = $1 under the RLS-active client → exactly 1 row = owned; 0 ⇒ reject). Keep the existing JSDoc style (cite IDOR Pitfall 7, FK-not-RLS-aware).
    Write the four local-DB integration tests against the running local stack, cloning the harness usage (createUser → userClient → seed under user A's RLS, two users):
    (1) `mei-invoice-rls.test.ts` — user A inserts NFs with both activity_types + sets mei_settings + a mei_year_flags row; assert they persist; user B reads ZERO of user A's mei_invoices/mei_settings/mei_year_flags rows (two-user RLS isolation, MEI-01/03).
    (2) `mei-report.test.ts` — seed user A: mei_start_date, has_employee=true for the year, several NFs split across comercio_industria and servicos; read `v_mei_year_summary` for the year and assert gross_cents == sum, comercio_cents + servicos_cents == gross_cents, has_employee==true, and applicable_limit_cents/band_ceiling_cents/ratio_bp match the `src/lib/mei/limit.ts` oracle for that start date (the SQL↔TS parity at the data level — MEI-04/02).
    (3) `mei-view-leak.test.ts` — clone view-leak.test.ts for `v_mei_year_summary`: user A sees its own summary row; user B reads ZERO of user A's summary rows (security_invoker proof, MEI-02/04 / Pitfall 6).
    (4) `mei-idor.test.ts` — clone reserva-idor.test.ts two-half shape for `mei_invoices`: a forged `mei_invoice_id` belonging to user B → `assertOwnedMeiInvoice` under user A's client returns false (0 owned); user A's own id → true (MEI-01, IDOR / Pitfall 7).
    Run the full suite. These are Wave-0 tests but, because the substrate (migrations + libs) lands in THIS plan, they should be GREEN immediately (not it.todo) — the schema and the view already exist. After they pass, set `wave_0_complete: true` and `nyquist_compliant: true` in 05-VALIDATION.md frontmatter and tick the Wave-0 checklist rows.
  </action>
  <verify>
    <automated>SUPABASE_DISABLE_TELEMETRY=1 npx vitest run mei-invoice-rls mei-report mei-view-leak mei-idor && grep -q "assertOwnedMeiInvoice" src/lib/ownership.ts && echo WAVE0_GREEN</automated>
  </verify>
  <done>assertOwnedMeiInvoice exported; the 4 integration tests pass against the local stack (RLS isolation, report split+employee+limit parity, view-leak, IDOR); VALIDATION.md marked wave_0_complete + nyquist_compliant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Server Action | Untrusted FormData (NF fields, mei_invoice_id, year) crosses into the DB write path |
| user A ↔ user B (RLS) | Per-user row isolation on 3 tables + 1 aggregate view is the security boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | v_mei_year_summary | mitigate | `with (security_invoker = true)` so the view inherits RLS; proven by `tests/mei-view-leak.test.ts` (Task 3) |
| T-05-02 | Information Disclosure | mei_settings / mei_year_flags / mei_invoices | mitigate | `ENABLE ROW LEVEL SECURITY` + USING + WITH CHECK `auth.uid()=user_id` per table; two-user isolation test `mei-invoice-rls.test.ts` (Task 3) |
| T-05-03 | Tampering / Elevation | forged `mei_invoice_id` FK | mitigate | `assertOwnedMeiInvoice` re-derive under RLS-active client (FK is not RLS-aware, Pitfall 7); `mei-idor.test.ts` (Task 3) |
| T-05-04 | Tampering | hardcoded 81k / wrong applicable limit | mitigate | constants only in `rules.ts`; SQL view literals asserted equal to constants by `rules.test.ts`; "no bare 81000 in src outside rules.ts" grep guard (Task 2) |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | none — this phase installs ZERO packages (RESEARCH Package Legitimacy Audit: N/A); no install task exists |
</threat_model>

<verification>
- `npm run db:reset && npm run gen:types` — migrations 0001–0026 replay clean; types contain the 3 tables + the view.
- `npx vitest run src/lib/mei/` — rules/limit/status/csv unit oracle GREEN, including the SQL↔TS parity guard.
- `SUPABASE_DISABLE_TELEMETRY=1 npx vitest run` — full suite GREEN (the 4 new integration tests + all prior phases).
- `npx tsc --noEmit` — clean.
- `grep -rn "81000\|97200" src --include=*.ts | grep -v "src/lib/mei/rules.ts"` returns nothing (no scattered fiscal literals).
</verification>

<success_criteria>
- Migrations 0025/0026 applied to the LOCAL stack; `database.types.ts` regenerated and committed; `v_mei_year_summary` is security_invoker.
- The four MEI numbers live ONLY in `src/lib/mei/rules.ts`; the parity test holds the SQL view literals equal to them.
- `applicableLimitCents` / `bandCeilingCents` / `meiStatus` / `isNearLimit` / `meiReportToCsv` exist with passing unit tests at the verified edges.
- `assertOwnedMeiInvoice` exists; RLS isolation, report-split-parity, view-leak, and IDOR integration tests pass against the local stack.
- `05-VALIDATION.md` frontmatter set `wave_0_complete: true` / `nyquist_compliant: true`.
</success_criteria>

<output>
Create `.planning/phases/05-m-dulo-mei-dasn-simei/05-01-SUMMARY.md` when done.
</output>
