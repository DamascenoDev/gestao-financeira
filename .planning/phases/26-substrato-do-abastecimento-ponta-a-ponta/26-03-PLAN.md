---
phase: 26-substrato-do-abastecimento-ponta-a-ponta
plan: 03
type: execute
wave: 1
depends_on: ["26-01"]
files_modified:
  - supabase/migrations/0040_categorias_combustivel.sql
autonomous: true
requirements: [FUEL-01]

must_haves:
  truths:
    - "handle_new_user() is re-seeded with 'Combustível' (kind consumo) at sort 4, shifting Saúde→Marketplace +1, Outros last (sort 13)"
    - "An idempotent backfill inserts 'Combustível' only where the account lacks it (where not exists name = 'Combustível')"
    - "The migration is data + trigger-body only — NO gen:types diff (mirrors 0035)"
    - "handle_new_user() keeps security definer + set search_path = public (search-path hijack mitigation)"
  artifacts:
    - path: "supabase/migrations/0040_categorias_combustivel.sql"
      provides: "Combustível default category seed + idempotent backfill"
      contains: "Combustível"
  key_links:
    - from: "supabase/migrations/0040_categorias_combustivel.sql"
      to: "public.handle_new_user"
      via: "create or replace function ... security definer set search_path = public — seeds Combustível at sort 4"
      pattern: "Combustível"
    - from: "supabase/migrations/0040_categorias_combustivel.sql"
      to: "public.categories"
      via: "idempotent backfill insert ... select where not exists"
      pattern: "where not exists"
---

<objective>
Wave 1 seed migration `0040_categorias_combustivel.sql` — add the default "Combustível"
category (kind `consumo`) for all users, mirroring 0035 (Marketplace) exactly (D-07):
re-seed `handle_new_user()` with Combustível at sort 4 for new signups (D-06), plus an
idempotent backfill for existing accounts. Data + trigger-body only → no gen:types diff (D-07).

Purpose: deliver the "categoria default" half of FUEL-01 (SC1). The apply-on-confirm half
is Phase 28; P26 only seeds the category.
Output: one seed migration (NO gen:types diff).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-substrato-do-abastecimento-ponta-a-ponta/26-RESEARCH.md
@.planning/phases/26-substrato-do-abastecimento-ponta-a-ponta/26-PATTERNS.md
@supabase/migrations/0035_categories_marketplace.sql
</context>

<artifacts_produced>
## Artifacts this plan produces
- `supabase/migrations/0040_categorias_combustivel.sql` (seed — NO gen:types diff).
- Re-seeded `handle_new_user()` with the 13-row category VALUES block (Combustível @ sort 4).
- Seeded category "Combustível" (kind `consumo`, sort 4) on new + existing accounts.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Write 0040_categorias_combustivel.sql — re-seed handle_new_user + idempotent backfill</name>
  <files>supabase/migrations/0040_categorias_combustivel.sql</files>
  <read_first>
    - supabase/migrations/0035_categories_marketplace.sql (the EXACT pattern to mirror: header rationale L1-10 "no database.types.ts change"; re-seed function L13-37 with `security definer set search_path = public`; idempotent backfill L41-48 `insert ... select ... where not exists (... name = 'Marketplace')`)
    - .planning/phases/26-substrato-do-abastecimento-ponta-a-ponta/26-RESEARCH.md Pattern 4 (the full 13-row VALUES block with Combustível at sort 4 + the sort math; the insert-only / no-renumber decision A2; Pitfall 6 no-types-diff)
    - .planning/phases/26-substrato-do-abastecimento-ponta-a-ponta/26-PATTERNS.md "0040" section
  </read_first>
  <action>
    Create migration `supabase/migrations/0040_categorias_combustivel.sql` mirroring 0035 1:1. Header comment must state: adds the default "Combustível" category; two parts (re-seed handle_new_user for new signups + idempotent backfill for existing accounts); data + trigger redef only — `database.types.ts` is unaffected (mirror 0035 L9-10).

    Part 1 — re-seed `handle_new_user()` (per D-07: 0035 pattern + Combustível in the new slot; per D-06: sort 4 right after Transporte, shifting the rest +1, Outros last): copy the 0035 L13-37 body VERBATIM (`create or replace function public.handle_new_user() returns trigger language plpgsql security definer set search_path = public as $$ ... $$;`) and change ONLY the VALUES block to the 13-row ordering from RESEARCH Pattern 4: Moradia(1), Alimentação(2), Transporte(3), Combustível(4, kind consumo, is_reserva false) [NEW per D-06], Saúde(5), Educação(6), Lazer(7), Vestuário(8), Assinaturas(9), Marketplace(10), Investimentos(11, alocacao), Reserva(12, alocacao, is_reserva true), Outros(13, consumo). Preserve `security definer` and `set search_path = public` exactly (search-path hijack mitigation) and the `insert into public.profiles (id, user_id) values (new.id, new.id);` line.

    Part 2 — idempotent backfill (per D-07: backfill existing accounts where not exists): copy 0035 L41-48 verbatim, swapping `'Marketplace'`/sort 9 → `'Combustível'`/sort 4: `insert into public.categories (user_id, name, kind, sort, is_reserva) select p.user_id, 'Combustível', 'consumo', 4, false from public.profiles p where not exists (select 1 from public.categories c where c.user_id = p.user_id and c.name = 'Combustível');`. Insert-only — do NOT renumber existing accounts' categories (RESEARCH A2 — parity with 0035; the sort-4 tie with existing Saúde is a cosmetic display-order tie, not a correctness issue; `categories.sort` has no unique constraint). Keep ALL schema DDL out of this file (no table/column changes) so the no-gen:types-diff claim holds (D-07: data/trigger only → no gen:types diff).
  </action>
  <verify>
    <automated>grep -v '^--' supabase/migrations/0040_categorias_combustivel.sql | grep -c "set search_path = public" | grep -qvx 0 && echo SEARCH-PATH-PINNED || echo MISSING-SEARCH-PATH</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/0040_categorias_combustivel.sql` exists with exactly two parts: the re-seeded `handle_new_user()` and the idempotent backfill.
    - The VALUES block has 13 rows with 'Combustível' at sort 4 (kind 'consumo', is_reserva false), Outros at sort 13.
    - `handle_new_user()` retains `security definer` and `set search_path = public`.
    - The backfill uses `where not exists (... name = 'Combustível')` (idempotent) and does NOT renumber existing categories.
    - The file contains NO schema DDL (no create/alter table, no add column) — data + function-body only.
  </acceptance_criteria>
  <done>0040 seeds "Combustível" at sort 4 for new signups and backfills existing accounts idempotently, mirroring 0035, with search_path pinned and no schema change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| new signup → handle_new_user() | SECURITY DEFINER trigger runs as its owner; an unpinned search_path is a privilege-escalation surface. |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-07 | Elevation of Privilege | SECURITY DEFINER `handle_new_user()` search_path hijack | mitigate | keep `set search_path = public` on the re-seeded function (the 0035 pattern, preserved verbatim); verified by the grep gate on this task. |
| T-26-08 | Tampering | seed accidentally carries a schema change → false "no types diff" claim | mitigate | the file contains only the function body + a data backfill; Plan 04 asserts an EMPTY gen:types diff after applying 0040. |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (SQL only); nothing to verify. |
</threat_model>

<verification>
- `grep` confirms `set search_path = public` present and no `create table`/`alter table`/`add column` in 0040.
- After Plan 04 applies 0040: `git diff --exit-code src/types/database.types.ts` is EMPTY (no types diff).
- `tests/categorias-combustivel.test.ts` (Plan 01 Task 3) turns GREEN after 0040 applies.
</verification>

<success_criteria>
"Combustível" (kind consumo, sort 4) is seeded for new accounts via the re-seeded
handle_new_user() and backfilled idempotently for existing accounts, mirroring 0035, with
search_path pinned and zero gen:types diff.
</success_criteria>

<output>
Create `.planning/phases/26-substrato-do-abastecimento-ponta-a-ponta/26-03-SUMMARY.md` when done
</output>
