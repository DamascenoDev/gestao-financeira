---
phase: 26-substrato-do-abastecimento-ponta-a-ponta
plan: 04
type: execute
wave: 2
depends_on: ["26-02", "26-03"]
files_modified: []
autonomous: true
requirements: [FUEL-01]

must_haves:
  truths:
    - "npm run db:reset replays 0001→0040 clean (exit 0) on the local Docker stack (SC5)"
    - "npm run gen:types leaves database.types.ts diff scoped to ONLY the new junction table + 2 columns (schema), with the seed (0040) contributing NO diff"
    - "The full vitest suite is green — all four Wave 0 tests pass + carro-view-leak + carro-consumo stay green (no regression)"
  artifacts: []
  key_links:
    - from: "npm run db:reset"
      to: "supabase/migrations/0039_abastecimento_parcelado.sql + 0040_categorias_combustivel.sql"
      via: "clean ordered replay 0001→0040 — the SC5 gate"
      pattern: "db:reset"
    - from: "npm test"
      to: "tests/abastecimento-cost-check.test.ts + abastecimento-parcelas + categorias-combustivel + carro-consumo + carro-view-leak"
      via: "full vitest run against the local stack — all green"
      pattern: "vitest"
---

<objective>
Wave 2 phase gate [BLOCKING] — apply BOTH migrations to the local stack via a clean
ordered replay, regenerate types with a scope assertion, and run the full vitest suite
green. This is the SC5 gate and the false-positive guard required by the schema-gate
contribution: build/type checks against stale config-derived types would otherwise pass
on a broken schema.

Purpose: prove SC5 (clean replay + scoped types regen) and that all Phase 26 success
criteria (SC1–SC5 + no-double-count + anti-leak) hold together, with both migrations
present, before `/gsd-verify-work`.
Output: a verified-green local stack + scoped types diff (no new source files).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-substrato-do-abastecimento-ponta-a-ponta/26-RESEARCH.md
@.planning/phases/26-substrato-do-abastecimento-ponta-a-ponta/26-VALIDATION.md
@supabase/migrations/0039_abastecimento_parcelado.sql
@supabase/migrations/0040_categorias_combustivel.sql
</context>

<artifacts_produced>
## Artifacts this plan produces
- No new source files. Produces a verified state: clean local replay (0001→0040),
  a scope-asserted `src/types/database.types.ts` regeneration, and a green full suite.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Clean replay (db:reset) + gen:types scope assertion + full suite green</name>
  <files></files>
  <read_first>
    - supabase/migrations/0039_abastecimento_parcelado.sql + supabase/migrations/0040_categorias_combustivel.sql (both must be present from Plans 02/03)
    - .planning/phases/26-substrato-do-abastecimento-ponta-a-ponta/26-RESEARCH.md "Repo npm scripts" + "Replay safety" + Pitfalls 1/2/6
    - .planning/phases/26-substrato-do-abastecimento-ponta-a-ponta/26-VALIDATION.md "Sampling Rate" (phase gate: db:reset exit 0 → gen:types → full suite green) + the Manual-Only Verifications (gen:types diff scope)
    - tests/carro-view-leak.test.ts + tests/carro-consumo.test.ts (must stay green — anti-leak + WR-02/05/06 regression)
  </read_first>
  <action>
    This is the [BLOCKING] schema-push/replay gate. It depends on BOTH migration plans (02, 03) being complete — do not run until 0039 AND 0040 exist on disk. Steps:

    1. Run `npm run db:reset` — the clean ordered replay of 0001→0040 against the local Supabase Docker stack. It MUST exit 0. If it errors on 0039 (likely an `add constraint` without prior `drop`, an `add column` without `if not exists`, or a constraint referencing a not-yet-added column) or on 0040 (likely a function-body typo), STOP and return the error to the executor to fix the offending migration; do not proceed.

    2. Run `npm run gen:types` to regenerate `src/types/database.types.ts` from the freshly replayed local schema. Then assert the diff scope with `git diff src/types/database.types.ts`: it MUST contain the new `abastecimento_parcelas` table type AND `parcelas_total` + `valor_total_cents` on the `abastecimentos` Row/Insert/Update types, and MUST NOT touch `categories` or `handle_new_user` (the 0040 seed contributes NO types diff — Pitfall 6). If the diff is broader, a schema change leaked into 0040 or 0039 has an unintended effect — STOP and fix. (The combined schema+seed diff equals the 0039 schema diff alone, since 0040 adds none.)

    3. Run `npm test` (full `vitest run`) against the local stack. ALL must be green: the four Wave 0 tests (`tests/abastecimento-cost-check.test.ts`, `tests/abastecimento-parcelas.test.ts`, `tests/categorias-combustivel.test.ts`, and the extended `tests/carro-consumo.test.ts` parcelado fixture) now PASS, AND the pre-existing `tests/carro-view-leak.test.ts` (security_invoker anti-leak) + the existing `tests/carro-consumo.test.ts` WR-02/05/06 assertions stay green (no regression). If any test that should be green is red, return the failure for the migration to be fixed — do NOT weaken a test to make it pass.

    Do NOT run a production `supabase db push` here — prod apply is a deferred deploy-time concern (needs SUPABASE_ACCESS_TOKEN) handled at milestone close per this project's established deferred-deploy-plan pattern; this phase gates on the LOCAL replay only.
  </action>
  <verify>
    <automated>npm run db:reset > /tmp/p26_reset.log 2>&1 && npm run gen:types > /tmp/p26_gentypes.log 2>&1 && npm test > /tmp/p26_test.log 2>&1 && grep -q abastecimento_parcelas src/types/database.types.ts && echo PHASE-GATE-GREEN || echo PHASE-GATE-FAILED</automated>
  </verify>
  <acceptance_criteria>
    - `npm run db:reset` exits 0 — clean ordered replay 0001→0040 on the local Docker stack (SC5).
    - `npm run gen:types` regenerates `src/types/database.types.ts`; the diff is scoped to the new `abastecimento_parcelas` table type + `parcelas_total`/`valor_total_cents` on `abastecimentos`, with NOTHING touching `categories`/`handle_new_user` (0040 = no types diff).
    - `npm test` is fully green: the four Wave 0 tests pass, and `tests/carro-view-leak.test.ts` + the existing `tests/carro-consumo.test.ts` WR-02/05/06 assertions stay green (no regression).
    - No production `supabase db push` was run (local-only gate).
  </acceptance_criteria>
  <done>Both migrations replay clean in order on the local stack, the regenerated types are diff-scoped to exactly the new table + two columns, and the full vitest suite is green — SC5 proven and all Phase 26 criteria hold together.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stale config-derived types → build/verify | Without an explicit local replay + gen:types, type/build checks pass against an outdated schema (false-positive verification). |
| local Docker stack → assertions | All gate assertions run against the freshly replayed LOCAL stack, never a remote project. |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-09 | Tampering | false-positive verification on stale types | mitigate | the [BLOCKING] db:reset + gen:types + full-suite gate runs against the freshly replayed schema before /gsd-verify-work (the schema-gate contribution). |
| T-26-10 | Information Disclosure | running the gate against a remote/prod stack | mitigate | gate uses `npm run db:reset`/`gen:types --local` only; production `supabase db push` is explicitly deferred to a credential-gated deploy step. |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (commands + git assertions only); nothing to verify. |
</threat_model>

<verification>
- `npm run db:reset` exits 0 (clean replay through 0040).
- `git diff src/types/database.types.ts` scoped to the new junction table + 2 columns; no `categories`/`handle_new_user` change.
- `npm test` green across the full suite (Wave 0 tests + carro-view-leak + carro-consumo).
</verification>

<success_criteria>
SC5 is proven: both migrations replay clean in order on the local stack, types are
regenerated with a scoped diff, and the entire vitest suite (new + pre-existing) is green
with no regression. Phase 26 is ready for `/gsd-verify-work`.
</success_criteria>

<output>
Create `.planning/phases/26-substrato-do-abastecimento-ponta-a-ponta/26-04-SUMMARY.md` when done
</output>