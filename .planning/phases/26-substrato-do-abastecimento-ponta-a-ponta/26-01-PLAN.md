---
phase: 26-substrato-do-abastecimento-ponta-a-ponta
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/abastecimento-cost-check.test.ts
  - tests/abastecimento-parcelas.test.ts
  - tests/categorias-combustivel.test.ts
  - tests/carro-consumo.test.ts
autonomous: true
requirements: [FUEL-01]

must_haves:
  truths:
    - "The 9-row CHECK truth table is encoded as 9 insert assertions (PASS / Postgres 23514 reject)"
    - "Junction unique(transaction_id) + unique(abastecimento_id, parcela_num) + double-link + RLS isolation are asserted"
    - "New account has 'Combustível' (kind consumo, sort 4) + backfill idempotency is asserted"
    - "A parcelado fixture asserts custo_intervalo_cents == valor_total_cents counted once (no double-count)"
    - "All four tests are RED (fail) before the migrations exist — they are the Nyquist gates for Wave 1"
  artifacts:
    - path: "tests/abastecimento-cost-check.test.ts"
      provides: "SC2/SC3 — the 9-row relaxed-CHECK truth table"
      contains: "abastecimento_cost"
    - path: "tests/abastecimento-parcelas.test.ts"
      provides: "SC4 — junction unique + double-link + RLS"
      contains: "abastecimento_parcelas"
    - path: "tests/categorias-combustivel.test.ts"
      provides: "SC1 — Combustível seed on new account + backfill idempotency"
      contains: "Combustível"
    - path: "tests/carro-consumo.test.ts"
      provides: "no-double-count parcelado fixture (extension)"
      contains: "parcelas_total"
  key_links:
    - from: "tests/abastecimento-cost-check.test.ts"
      to: "supabase/migrations/0039_abastecimento_parcelado.sql"
      via: "inserts into abastecimentos exercising the relaxed abastecimentos_cost_xor"
      pattern: "from\\('abastecimentos'\\)"
    - from: "tests/abastecimento-parcelas.test.ts"
      to: "supabase/migrations/0039_abastecimento_parcelado.sql"
      via: "inserts into the abastecimento_parcelas junction"
      pattern: "from\\('abastecimento_parcelas'\\)"
    - from: "tests/categorias-combustivel.test.ts"
      to: "supabase/migrations/0040_categorias_combustivel.sql"
      via: "reads back the seeded Combustível category for a fresh user"
      pattern: "eq\\('name', 'Combustível'\\)"
---

<objective>
Wave 0 — write the four failing integration tests that encode every Phase 26 success
criterion BEFORE any migration exists. These are the Nyquist gates the Wave 1 migration
plans (0039 schema, 0040 seed) must turn green. Pure-data phase: every DB invariant is
proven by vitest-against-the-local-Supabase-Docker-stack (no pgTAP in this repo).

Purpose: lock the relaxed-CHECK truth table (D-01 attach-later, D-05 parcelado), the
junction constraints (D-03), the seed presence (D-06/D-07 Combustível at sort 4), and the
parcelado no-double-count behavior (D-02/D-05) as executable assertions so the migrations
cannot silently regress or under-deliver.
Output: 3 new test files + 1 extension of `tests/carro-consumo.test.ts`. All four RED.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-substrato-do-abastecimento-ponta-a-ponta/26-RESEARCH.md
@.planning/phases/26-substrato-do-abastecimento-ponta-a-ponta/26-VALIDATION.md
@.planning/phases/26-substrato-do-abastecimento-ponta-a-ponta/26-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts this phase produces (full phase, for executor awareness)
- Migrations: `supabase/migrations/0039_abastecimento_parcelado.sql` (schema — Plan 02),
  `supabase/migrations/0040_categorias_combustivel.sql` (seed — Plan 03).
- New table: `abastecimento_parcelas` (junction).
- New columns: `abastecimentos.parcelas_total` (int), `abastecimentos.valor_total_cents` (bigint).
- Relaxed constraint: `abastecimentos_cost_xor` (drop-then-add, relaxed CASE predicate).
- Seeded category: "Combustível" (kind `consumo`, sort 4).
- Regenerated `src/types/database.types.ts` (new table + 2 columns; Plan 02 / Plan 04).
- New tests (this plan): `tests/abastecimento-cost-check.test.ts`,
  `tests/abastecimento-parcelas.test.ts`, `tests/categorias-combustivel.test.ts`;
  extension of `tests/carro-consumo.test.ts`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: tests/abastecimento-cost-check.test.ts — the 9-row relaxed-CHECK truth table (SC2/SC3)</name>
  <files>tests/abastecimento-cost-check.test.ts</files>
  <read_first>
    - tests/carro-rls.test.ts L33-49 (createUser flow), L51-94 (beforeAll: seed a carro + a real tx via userClient), L96-100 (afterAll deleteUser), L139-152 (the `expect(error).not.toBeNull()` CHECK-rejection assertion shape)
    - tests/abastecimento-action.test.ts L101-119 (seedTx helper — produces a real transaction_id for the linked/attach-later/parcelado-with-tx cases)
    - tests/helpers/local-supabase.ts (readLocalConfig L38, serviceClient L86, userClient L97 — hard-guarded to 127.0.0.1)
    - .planning/phases/26-substrato-do-abastecimento-ponta-a-ponta/26-VALIDATION.md (the CHECK truth table, lines 60-70) and 26-RESEARCH.md (truth table L430-440, Pattern 1 predicate)
  </read_first>
  <action>
    Create a new vitest integration test cloning the carro-rls harness (createUser → signInWithPassword → {id, jwt}; beforeAll seeds one carro for userA via the RLS-active userClient; afterAll deletes the user). Seed two distinct real expense transactions for userA (clone seedTx from abastecimento-action.test.ts) — one for the "attach-later" and "à-vista linked" PASS rows, one fresh tx for the "parcelado + tx" REJECT row (each linked tx must be unique to avoid tripping the preserved abastecimentos_transaction_uniq partial index). Implement EXACTLY the 9 rows of the truth table in 26-VALIDATION.md, each as one insert into `abastecimentos` via userClient with valid litros/odometro_km/tanque_cheio, asserting `expect(error).toBeNull()` for PASS rows and `expect(error).not.toBeNull()` for REJECT rows. Columns referenced by the inserts: `transaction_id`, `amount_cents`, `parcelas_total`, `valor_total_cents`. PASS rows: à-vista manual (amount_cents only), à-vista linked (transaction_id only), attach-later (transaction_id + amount_cents both present, valor_total_cents null), parcelado valid (parcelas_total=3 + valor_total_cents set, transaction_id null + amount_cents null). REJECT rows (each must surface a Postgres check_violation): à-vista neither (all cost sources null), à-vista with valor_total_cents leak (valor_total_cents set on a non-parcelado row), parcelado + tx (parcelas_total>1 with transaction_id), parcelado + amount (parcelas_total>1 with amount_cents), parcelado no valor_total_cents (parcelas_total>1 with valor_total_cents null). Use distinct odometro_km per insert so no insert collides on the consumo math. For at least one REJECT row, additionally assert the Postgres error `code === '23514'` (check_violation) to pin that it is the CHECK firing, not some other constraint — read the error object returned by supabase-js (`error.code`). Add a short header comment block explaining each truth-table row's P/T/A/V state and expected result, mirroring the carro-rls.test.ts header style. Do NOT reference the literal constraint name in a way that becomes a negative-grep gate; this is a runtime-behavior test, not a string-presence test.
  </action>
  <verify>
    <automated>npx vitest run tests/abastecimento-cost-check.test.ts 2>&1 | grep -qE 'FAIL|relation .*abastecimento|does not exist|column .*does not exist' && echo RED-AS-EXPECTED || echo CHECK-OUTPUT</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/abastecimento-cost-check.test.ts` exists and is valid TypeScript (no `any` smuggled to bypass strict mode; `tsc --noEmit` clean for this file).
    - The test contains exactly 9 truth-table insert assertions matching 26-VALIDATION.md rows (4 PASS, 5 REJECT).
    - At least one REJECT row asserts `error.code === '23514'`.
    - Running the test now is RED because `valor_total_cents`/`parcelas_total` columns and the relaxed CHECK do not yet exist (Wave 1 makes it green). The test must NOT pass against the pre-0039 schema.
  </acceptance_criteria>
  <done>The 9-row truth table is encoded as a single new integration test that fails against the current schema and will pass only after 0039 relaxes the CHECK and adds the parcelamento columns.</done>
</task>

<task type="auto">
  <name>Task 2: tests/abastecimento-parcelas.test.ts — junction unique + double-link + RLS (SC4)</name>
  <files>tests/abastecimento-parcelas.test.ts</files>
  <read_first>
    - tests/carro-rls.test.ts L33-49 (createUser), L51-94 (beforeAll seed carro + tx), L119-135 (two-user "reads ZERO" RLS isolation), L168-193 (first insert ok / duplicate not-null on a unique index), L96-100 (afterAll)
    - tests/helpers/local-supabase.ts (userClient / serviceClient)
    - .planning/phases/26-substrato-do-abastecimento-ponta-a-ponta/26-RESEARCH.md Pattern 2 (junction shape, the two double-link hazards), 26-PATTERNS.md "tests/abastecimento-parcelas.test.ts" section
  </read_first>
  <action>
    Create a new vitest integration test cloning the carro-rls harness for two users (userA, userB). In beforeAll, as userA: seed a carro, seed a PARCELADO abastecimento (parcelas_total=3, valor_total_cents set, transaction_id null, amount_cents null — the only legal parcelado shape under the relaxed CHECK), and seed two distinct real expense transactions to act as parcela links. Assertions against the `abastecimento_parcelas` junction via userClient: (1) inserting a parcela row {user_id, abastecimento_id, transaction_id, parcela_num: 1} succeeds (`error` null); (2) inserting a SECOND parcela row with the SAME transaction_id (different abastecimento or same) violates `unique (transaction_id)` → `error` not null (a tx is at most one parcela); (3) inserting a second parcela with the SAME (abastecimento_id, parcela_num) pair violates `unique (abastecimento_id, parcela_num)` → `error` not null (no two "parcela 1"); (4) RLS isolation — userB selecting `from('abastecimento_parcelas').select('*').eq('user_id', userA.id)` returns ZERO rows (`expect(data ?? []).toHaveLength(0)`); (5) re-link enablement — assert that a transaction can be attached to a pre-existing abastecimento (junction insert against an abastecimento created earlier, not at the same moment), proving attach-later is legal at the DB layer (SC4). For the cross-table double-link residual (a tx that is both an à-vista `abastecimentos.transaction_id` AND a junction parcela on a different abastecimento): document in a comment that this residual is an action-layer invariant enforced in Phases 27/28 (RESEARCH A1) — do not assert it as a DB-level guarantee. Use the same header-comment discipline as carro-rls.test.ts.
  </action>
  <verify>
    <automated>npx vitest run tests/abastecimento-parcelas.test.ts 2>&1 | grep -qE 'FAIL|relation .*abastecimento_parcelas|does not exist' && echo RED-AS-EXPECTED || echo CHECK-OUTPUT</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/abastecimento-parcelas.test.ts` exists, valid strict TypeScript.
    - Asserts: unique(transaction_id) rejection, unique(abastecimento_id, parcela_num) rejection, two-user RLS "reads ZERO", and at least one attach-to-pre-existing-abastecimento (re-link) insert succeeding.
    - The cross-row double-link residual is documented in a comment as a 27/28 action-layer invariant, NOT asserted at DB level.
    - Running the test now is RED because the `abastecimento_parcelas` table does not yet exist. Must not pass against the pre-0039 schema.
  </acceptance_criteria>
  <done>The junction unique constraints, double-link rejection, re-link enablement, and RLS isolation are encoded as a new integration test that fails against the current schema and passes only after 0039 creates the junction.</done>
</task>

<task type="auto">
  <name>Task 3: tests/categorias-combustivel.test.ts (SC1) + parcelado no-double-count fixture in tests/carro-consumo.test.ts</name>
  <files>tests/categorias-combustivel.test.ts, tests/carro-consumo.test.ts</files>
  <read_first>
    - tests/carro-rls.test.ts L33-49 (createUser flow — createUser triggers handle_new_user, the seed), L96-100 (afterAll deleteUser)
    - tests/helpers/local-supabase.ts (readLocalConfig / serviceClient / userClient)
    - tests/carro-consumo.test.ts L40-43 (fixture constants), L142-186 (same-odometer carro seeding block), L276-301 (the custo_intervalo_cents assertion block to mirror)
    - .planning/phases/26-substrato-do-abastecimento-ponta-a-ponta/26-RESEARCH.md Pattern 3 ("why no double-count"), Pattern 4 (seed sort math — Combustível at sort 4), 26-PATTERNS.md "categorias-combustivel" + "carro-consumo (EXTEND)" sections
  </read_first>
  <action>
    Part A — create `tests/categorias-combustivel.test.ts`: clone the carro-rls createUser/afterAll harness, drop all carro seeding. createUser fires `handle_new_user()`, which (after 0040) seeds the default categories. Assert via userClient: (1) a fresh user has exactly one category named 'Combustível' (`.from('categories').select('name, sort').eq('name', 'Combustível')` → length 1) at `sort === 4` and `kind === 'consumo'`; (2) backfill idempotency — re-running the backfill insert pattern (insert 'Combustível' where not exists for this user_id) via the serviceClient does NOT create a duplicate (count stays 1). Read `kind` back too to confirm 'consumo'.
    Part B — extend `tests/carro-consumo.test.ts`: add a new carro `carroParceladoId` and seed it in beforeAll mirroring the same-odometer block style (L142-186). Seed an OPENING full-tank fill (à-vista, amount_cents) and a CLOSING full-tank fill that is PARCELADO (parcelas_total = 3, valor_total_cents = a known full fuel cost constant, transaction_id null, amount_cents null) with a strictly positive odometer delta closing exactly one valid interval. Add a new `describe` block mirroring L276-301 asserting that the parcelado interval's `custo_intervalo_cents === valor_total_cents` (counted ONCE — not summed across parcelas, not pulled from tagged transactions). Also seed one transaction tagged with `carro_id = carroParceladoId` (a "parcela" cash-flow tag) and assert it does NOT inflate `v_abastecimento_consumo.custo_intervalo_cents` for that carro (the consumo view reads cost from `abastecimentos`, never from tagged transactions — RESEARCH Pattern 3). Add a fixture constant `PARCELADO_VALOR_TOTAL_C` near L40-43. Do NOT modify the existing happy/guard/same-odometer fixtures or assertions — purely additive.
  </action>
  <verify>
    <automated>npx vitest run tests/categorias-combustivel.test.ts tests/carro-consumo.test.ts 2>&1 | grep -qE 'FAIL|Combustível|parcelas_total|valor_total_cents|does not exist' && echo RED-OR-NEW-FIXTURE-PRESENT || echo CHECK-OUTPUT</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/categorias-combustivel.test.ts` exists, valid strict TypeScript; asserts Combustível present at sort 4 / kind consumo on a fresh user + backfill idempotency (count stays 1).
    - `tests/carro-consumo.test.ts` gains a parcelado fixture (`carroParceladoId`, `PARCELADO_VALOR_TOTAL_C`) and a new describe block asserting `custo_intervalo_cents === valor_total_cents` once + that a carro_id-tagged transaction does not inflate consumo cost.
    - The existing carro-consumo happy/guard/same-odometer assertions are UNCHANGED (purely additive).
    - Both tests are RED now (no Combustível seed, no parcelado columns) and pass only after 0039 + 0040.
  </acceptance_criteria>
  <done>SC1 (Combustível seed + backfill idempotency) and the parcelado no-double-count behavior are encoded as failing tests that gate the Wave 1 migrations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → local Supabase stack | Tests use the service/secret key (RLS-bypassing); the harness is HARD-GUARDED to 127.0.0.1 (`tests/helpers/local-supabase.ts` `assertLocal`). |
| userClient (JWT) → Postgres RLS | Each user-scoped assertion runs under the caller's bearer token so RLS policies are exercised as a real user, not the admin. |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Information Disclosure | test helper pointed at a remote project (would expose real financial data with the secret key) | mitigate | Reuse `tests/helpers/local-supabase.ts` only — its `assertLocal` throws on any non-127.0.0.1 URL. Do not introduce a new client factory. |
| T-26-02 | Tampering | a test that passes against the OLD schema (false green) | mitigate | Each new test is REJECT-gated on objects that do not yet exist (junction table, parcelamento columns, relaxed CHECK); acceptance requires RED before Wave 1. |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this phase (vitest + harness already present); nothing to verify. |
</threat_model>

<verification>
- `npx vitest run tests/abastecimento-cost-check.test.ts tests/abastecimento-parcelas.test.ts tests/categorias-combustivel.test.ts tests/carro-consumo.test.ts` — all four are RED against the pre-0039/0040 schema (failing for the right reason: missing columns/table/seed).
- `npx tsc --noEmit` clean for the new test files (strict TypeScript, no `any` smuggling).
</verification>

<success_criteria>
Three new test files + one extension exist, are strict-TypeScript-clean, and FAIL against
the current schema for the correct reason (missing parcelamento columns, missing junction,
missing Combustível seed, missing relaxed CHECK). They form the complete Nyquist gate set
for Phase 26's success criteria SC1–SC4 and the no-double-count view invariant.
</success_criteria>

<output>
Create `.planning/phases/26-substrato-do-abastecimento-ponta-a-ponta/26-01-SUMMARY.md` when done
</output>
