---
phase: 04-upload-classifica-o-inteligente
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - src/actions/import.ts
  - src/actions/import.test.ts
  - src/app/(app)/importar/[statementId]/page.tsx
  - src/components/import-review-table.tsx
  - src/components/import-summary-header.tsx
  - src/components/origin-badge.tsx
  - src/components/recorrente-tag.tsx
  - src/components/suggestion-slot.tsx
autonomous: true
requirements: [IMP-05, CLS-03, CLS-04, CLS-05, CLS-06, RSV-06, SEC-03, CLS-02-deferred]

user_setup: []

must_haves:
  truths:
    - "User reviews the parsed transactions in a dense pre-persist grid (sibling of ExtratoTable) BEFORE anything is written to transactions"
    - "A memory-miss (unclassified) row is visually obvious (amber left-border + 'Não classificada' OriginBadge) and one-tap classifiable inline; bulk-classify works via the reused SelectionActionBar; classifying into Reserva opens the 'Qual reserva?' picker"
    - "On Confirmar importação, the rows persist to transactions (point-in-time category on the row), and ONLY THEN does the merchant→category pattern get learned (UPSERT merchant_patterns) — only for classified rows, only on human confirm"
    - "A Reserva-classified row, on confirm, saves merchant→reserva AND creates the aporte 'in' ledger entry by reusing the Phase-3 createTransactionWithReserva path (RSV-06)"
    - "The transaction-level dedupe_key collapses overlapping statements on confirm (ON CONFLICT DO NOTHING) so re-confirming or an overlapping statement never duplicates rows"
    - "Renaming a category afterward does NOT rewrite the imported transactions' recorded category (keyed by category_id, point-in-time — CLS-05)"
    - "Recurring merchants (same descriptor_norm across ≥3 months) are flagged with the RecorrenteTag; the SuggestionSlot is reserved but inert (suggestCategory returns null in v1) and does not reflow the column"
    - "A forged statement_id / category_id / reserva_id on confirm is rejected server-side before any FK write (IDOR)"
  artifacts:
    - path: "src/actions/import.ts"
      provides: "confirmImport (persist + learn + reserva aporte + dedupe + IDOR re-derive)"
      exports: ["confirmImport"]
    - path: "src/app/(app)/importar/[statementId]/page.tsx"
      provides: "Review screen RSC reading the parsed rows + summary by statementId"
      min_lines: 20
    - path: "src/components/import-review-table.tsx"
      provides: "Pre-persist review grid (ExtratoTable sibling) with origem/recorrente/suggestion columns"
      min_lines: 40
    - path: "src/components/import-summary-header.tsx"
      provides: "N/M/K/J counts + '0 novas' re-upload line + click-to-filter unclassified"
    - path: "src/components/origin-badge.tsx"
      provides: "memória / manual / não classificada / (reserved) sugerida badge"
    - path: "src/components/suggestion-slot.tsx"
      provides: "deferred-AI affordance — inert in v1, no column reflow"
  key_links:
    - from: "src/components/import-review-table.tsx"
      to: "src/components/selection-action-bar.tsx"
      via: "bulk-classify of memory-miss rows (reused verbatim, Reserva excluded from picker)"
      pattern: "SelectionActionBar"
    - from: "src/actions/import.ts"
      to: "merchant_patterns"
      via: "UPSERT on confirm, only classified rows, ON CONFLICT (user_id, descriptor_norm)"
      pattern: "merchant_patterns"
    - from: "src/actions/import.ts"
      to: "src/actions/transactions.ts"
      via: "reuse createTransactionWithReserva / syncReservaLedgerForTransaction for the aporte"
      pattern: "syncReservaLedgerForTransaction"
---

<objective>
Close the loop: the pre-persist review grid + the confirm-that-learns. The user reviews parsed transactions in a dense grid (a sibling of ExtratoTable reusing its column grammar, SelectionActionBar, and the focused "Qual reserva?" dialog), classifies memory-miss rows, and on Confirmar importação the rows persist to `transactions` (point-in-time category), the merchant→category (and merchant→reserva) patterns are LEARNED — only for classified rows, only on human confirm — and Reserva rows create the aporte ledger entry via the proven Phase-3 path.

After this task a real user can do something they could not before: confirm an import and have the system auto-classify the same merchant on the NEXT upload. This is the phase's core value made real on synthetic data; it turns the Plan-01 RED learn-on-confirm / point-in-time / recurring / reserva-aporte / IDOR integration tests GREEN.

Purpose: deliver IMP-05 + CLS-03/04/05/06 + RSV-06 + the SEC-03 seam, reusing the Phase-2/3 reserva aporte path verbatim (one consistent ledger code path, no new ledger write).
Output: confirmImport, the review RSC + ImportReviewTable + summary header + origin/recorrente badges + the inert suggestion slot.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-upload-classifica-o-inteligente/04-CONTEXT.md
@.planning/phases/04-upload-classifica-o-inteligente/04-RESEARCH.md
@.planning/phases/04-upload-classifica-o-inteligente/04-UI-SPEC.md

# The ingest output + statement record this slice consumes (Plan 02)
@src/actions/import.ts

# Reuse VERBATIM: the reserva aporte path (do NOT invent a new ledger write) + the IDOR helpers
@src/actions/transactions.ts

# The grid grammar to mirror: ExtratoTable (getRowId, selection, InlineCategoryCell, the focused "Qual reserva?" dialog) + the self-contained bulk bar + the picker
@src/components/extrato-table.tsx
@src/components/selection-action-bar.tsx
@src/components/reserva-picker.tsx
@src/components/category-badge.tsx
@src/components/amount-cell.tsx

# Pure libs (Plan 01)
@src/lib/parsers/types.ts
@src/lib/normalize.ts
@src/lib/classifier/suggest.ts
@src/lib/schemas/import.ts
@src/lib/money.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: confirmImport — persist + learn + reserva aporte + dedupe + IDOR re-derive</name>
  <files>src/actions/import.ts, src/actions/import.test.ts</files>
  <read_first>
    - src/actions/transactions.ts (REUSE: assertOwnedCategories, assertOwnedReserva, isReservaCategory, syncReservaLedgerForTransaction — do NOT re-implement; import or factor them so the aporte ledger path is identical to manual entry; mirror moneyWriteError + revalidatePath)
    - 04-RESEARCH.md Pattern 8 (confirm + learn, reuse reserva aporte), Pattern 9 (recurring), Pitfall 6 (IDOR), Pitfall 8 (learning poisoning / history rewrite)
    - src/lib/schemas/import.ts (confirmImportRowSchema), src/lib/classifier/suggest.ts (enum wrapper)
    - tests/import-learn-on-confirm.test.ts, tests/import-point-in-time.test.ts, tests/import-recurring.test.ts, tests/import-reserva-aporte.test.ts, tests/import-idor.test.ts (the RED tests this task turns GREEN)
  </read_first>
  <behavior>
    - confirmImport(statementId, rows[]): Zod safeParse rows (confirmImportRowSchema); getClaims → userId; ownership re-derive of statementId (a new assertOwnedStatement clone of assertOwnedCategories), of every category_id (assertOwnedCategories), and of every reserva_id (assertOwnedReserva) BEFORE any FK write — a forged id rejects the WHOLE write (IDOR, Pitfall 6).
    - Persist: INSERT transactions { user_id, statement_id, category_id (point-in-time on the row — CLS-05), amount_cents, kind 'expense', occurred_on, description: descriptor_raw, descriptor_norm, dedupe_key, classification_source, is_recurring } ON CONFLICT (user_id, dedupe_key) DO NOTHING. The count actually inserted = M (novas); rows whose dedupe_key already existed = J (duplicadas, silently skipped — Pitfall 4). Manual rows elsewhere (null dedupe_key) are unaffected by the partial unique index.
    - Reserva rows (is_reserva category, by the flag — never name): reuse syncReservaLedgerForTransaction / the createTransactionWithReserva path so the aporte 'in' ledger entry is created IDENTICALLY to manual entry (RSV-02/03) and counts as alocação, never consumo. Save merchant→reserva onto the merchant_patterns row (RSV-06).
    - Learn (CLS-03/04): UPSERT merchant_patterns (descriptor_norm → category_id [, reserva_id]) ON CONFLICT (user_id, descriptor_norm) DO UPDATE bump hit_count + set last_used_at — ONLY for CLASSIFIED rows, ONLY here on human confirm (no poisoning, Pitfall 8). Unclassified rows persist category-less (honest, not lost) and learn nothing.
    - Recurring (CLS-06): set is_recurring on a persisted row when its descriptor_norm is flagged by v_recurring_descriptors (≥3 distinct months) — computed at confirm from the view (the imported rows now count toward the months).
    - revalidatePath('/extrato'), '/reservas', '/dashboard'. Return { imported: M, duplicated: J }.
    - import.test.ts (extend): UPSERT only for classified rows; no learn for unclassified; IDOR rejection of forged statement/category/reserva; dedupe_key ON CONFLICT path; reserva row → ledger 'in' + merchant_patterns.reserva_id; point-in-time category persisted on the row.
  </behavior>
  <action>
Extend src/actions/import.ts with confirmImport, mirroring transactions.ts shape (Zod safeParse → { error }, getClaims gate, revalidatePath). REUSE the IDOR + reserva helpers from transactions.ts — either import them (if exported) or, if they are currently file-private, extract assertOwnedCategories / assertOwnedReserva / isReservaCategory / syncReservaLedgerForTransaction into a shared module (e.g. src/actions/_ownership.ts or src/lib/ownership.ts) and have BOTH transactions.ts and import.ts consume it (NO cross-sibling drift; one consistent ledger path — the Phase 2-3 lesson). Add assertOwnedStatement as a verbatim clone of assertOwnedCategories applied to statements.

Persist with the two-layer dedup's transaction half: ON CONFLICT (user_id, dedupe_key) DO NOTHING. supabase-js can express this via `.upsert(rows, { onConflict: 'user_id,dedupe_key', ignoreDuplicates: true })` (or an equivalent per-row insert that swallows the unique violation) — count actually-inserted rows for M and the skipped ones for J.

Learn ONLY after a successful persist and ONLY for rows with a non-null category_id — UPSERT merchant_patterns. For is_reserva rows, drive the aporte through the shared syncReservaLedgerForTransaction so the 'in' entry is created exactly as manual entry does, and set merchant_patterns.reserva_id (RSV-06). Never learn from an unclassified row.

Set is_recurring at confirm by checking v_recurring_descriptors for the row's descriptor_norm (CLS-06). The category lands on the transaction row at confirm and is never rewritten by a later rename (CLS-05 — keyed by category_id).

Flip the RED markers in tests/import-learn-on-confirm.test.ts, tests/import-point-in-time.test.ts, tests/import-recurring.test.ts, tests/import-reserva-aporte.test.ts, tests/import-idor.test.ts to live assertions now that confirmImport exists.
  </action>
  <verify>
    <automated>cd /home/dmsc/Dev/Projetos/Pessoal/gestao-financeira && SUPABASE_DISABLE_TELEMETRY=1 npx vitest run src/actions/import.test.ts tests/import-learn-on-confirm.test.ts tests/import-point-in-time.test.ts tests/import-recurring.test.ts tests/import-reserva-aporte.test.ts tests/import-idor.test.ts 2>&1 | tail -25 && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>confirmImport persists point-in-time rows, learns merchant_patterns only for classified rows on confirm, creates the reserva aporte via the reused Phase-3 path, dedupes by dedupe_key, and rejects forged FKs; all five named integration tests + import.test.ts GREEN; tsc clean; the reserva/IDOR helpers are shared (not duplicated) between transactions.ts and import.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Review screen — ImportReviewTable + summary header + origin/recorrente badges + suggestion slot</name>
  <files>src/app/(app)/importar/[statementId]/page.tsx, src/components/import-review-table.tsx, src/components/import-summary-header.tsx, src/components/origin-badge.tsx, src/components/recorrente-tag.tsx, src/components/suggestion-slot.tsx</files>
  <read_first>
    - 04-UI-SPEC.md §3 (review screen — the core), §Component Inventory (ImportReviewTable columns, OriginBadge/RecorrenteTag/SuggestionSlot/ImportSummaryHeader contracts), §4 (Dedup feedback / "0 novas"), §Color (amber attention = --consumption, never --destructive for unclassified; teal only on Confirmar + selection), §Copywriting Contract (every pt-BR string), §Deferred-AI Seam (slot reserved, no reflow), §Accessibility, §Responsive (cards <md)
    - src/components/extrato-table.tsx (mirror VERBATIM: getRowId, rowSelection, InlineCategoryCell, the focused "Qual reserva?" Dialog + ReservaPicker; this table is its SIBLING operating on un-persisted rows)
    - src/components/selection-action-bar.tsx (reuse verbatim; pass "Classificar" label; Reserva excluded from the bulk picker)
    - src/actions/import.ts (confirmImport — Task 1)
  </read_first>
  <action>
Create origin-badge.tsx: variants `memória` (neutral `bg-muted text-muted-foreground` + history/brain glyph), `manual` (neutral + pencil glyph), `não classificada` (`bg-consumption/10 text-consumption` + triangle-alert), and the RESERVED `sugerida` (`bg-primary/10 text-primary` + sparkles) — defined now, rendered only when a non-null suggestion exists (always false in v1). Text label always present (color never the sole signal).

Create recorrente-tag.tsx: a small `--consumption`-tinted `badge` "Recorrente" + repeat glyph + tooltip "Aparece em vários meses." Shown when the row's merchant is recurrence-flagged.

Create suggestion-slot.tsx: the deferred-AI affordance. v1 renders nothing / a faint non-interactive "—", `aria-hidden`. Contract: when a non-null suggestion arrives it renders a one-tap "Aplicar sugestão: {Categoria}" chip (sparkles, --primary). Reserve the space so adding the chip later does NOT reflow the Categoria column.

Create import-summary-header.tsx: "**{N}** transações · {M} novas · {K} não classificadas · {J} duplicadas ignoradas". Counts mono tabular-nums. "{K} não classificadas" uses `text-consumption` + triangle-alert when K>0 and is a real click-to-filter button ("Ver não classificadas"). "{J} duplicadas ignoradas" muted. Re-upload zero-state (M===0): swap to "0 novas — este arquivo já foi importado." N may step to the 20px mono hero (allowed exception).

Create import-review-table.tsx: a SIBLING of ExtratoTable. @tanstack/react-table, getRowId by the parsed row's stable key (dedupe_key/temp id), the SAME checkbox/select model, the SAME inline CategoryBadge Select cell (reuse the InlineCategoryCell pattern), and the SAME focused "Qual reserva?" dialog (ReservaPicker) when a row is classified into the Reserva category. Operates on UN-PERSISTED parsed rows (state held client-side; nothing written until Confirmar). Columns L→R: select · Data (dd/MM mono muted) · Descritor (cru body truncate+tooltip; normalizado xs muted mono below; RecorrenteTag inline when flagged) · Categoria (inline Select; empty → SuggestionSlot + amber "Classificar" affordance) · Origem (OriginBadge) · Valor (AmountCell kind="expense" signed={false}, neutral never red). Memory-miss rows: `border-l-2 border-l-consumption` accent + amber Origem badge; classifying drops the accent and flips Origem to `manual`. Feed SelectionActionBar for bulk-classify (label "Classificar", Reserva excluded — same rule as Extrato). Primary CTA "Confirmar importação" (teal, top-right + sticky bottom on <md) calls confirmImport(statementId, rows); on success toast "{M} transações importadas" + route to /extrato. Unclassified-on-confirm guard (K>0): an alert-dialog "Importar sem classificar? — Há {K} transações não classificadas..." [Voltar] [Confirmar] (do NOT hard-block). Mobile <md: one card per parsed transaction (keep the amber accent), floating bottom SelectionActionBar.

Create /importar/[statementId]/page.tsx (RSC): read the parsed rows + summary for this statementId (own user via RLS) from the statement record (the parsed_rows/summary payload Plan 02 stored). Render ImportSummaryHeader + ImportReviewTable, passing the user's categories (with the is_reserva flag) + reservas (from v_reserva_balance) for the inline Select + picker. States: all-classified calm confirm; needs-attention (K>0); re-upload/all-duplicates (M===0) → the `empty` primitive "Nada novo neste arquivo — Todas as {J} transações deste arquivo já estão no seu extrato. Nada foi duplicado." + "Voltar para o extrato"; loading skeleton; error inline "Não foi possível carregar a revisão. Tente recarregar a página."
  </action>
  <verify>
    <automated>cd /home/dmsc/Dev/Projetos/Pessoal/gestao-financeira && npx tsc --noEmit 2>&1 | tail -5 && npm run lint 2>&1 | tail -5 && npm run build 2>&1 | grep -E "importar|Compiled|error" | tail -10 && grep -q "SelectionActionBar" src/components/import-review-table.tsx && grep -q "border-l-consumption" src/components/import-review-table.tsx</automated>
  </verify>
  <done>/importar/[statementId] compiles in `npm run build`; tsc + lint clean (modulo the known useReactTable warning); ImportReviewTable reuses SelectionActionBar + the focused ReservaPicker dialog and renders the amber memory-miss accent; OriginBadge/RecorrenteTag/SuggestionSlot present; the SuggestionSlot is inert (aria-hidden) in v1; Confirmar importação calls confirmImport and routes to /extrato with the unclassified guard when K>0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → confirmImport | The reviewed rows + statement_id/category_id/reserva_id arrive untrusted; Zod + ownership re-derive gate them before any FK write. |
| memory write | A pattern is learned only from a human-confirmed classified row (poisoning boundary). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Elevation / Tampering | confirmImport FK writes (statement_id / category_id / reserva_id) | mitigate | assertOwnedStatement + assertOwnedCategories + assertOwnedReserva re-derive ownership server-side before any write; a forged id rejects the whole write (Pitfall 6); tests/import-idor.test.ts. |
| T-04-04 | Tampering | re-confirm / overlapping statement duplication | mitigate | INSERT transactions ON CONFLICT (user_id, dedupe_key) DO NOTHING; M = actually-inserted; tests/import-dedup.test.ts + the confirm path in tests/import-learn-on-confirm.test.ts. |
| T-04-08 | Tampering | learning poisoning / history rewrite | mitigate | UPSERT merchant_patterns only in confirmImport, only for classified rows; category is point-in-time on the transaction row; patterns keyed by category_id, never name (Pitfall 8); tests/import-point-in-time.test.ts. |
| T-04-09 | Tampering | aporte double-count / new ledger write | mitigate | Reuse the Phase-3 syncReservaLedgerForTransaction (kind 'in', derived balance) — no new ledger write; aporte counts as alocação, never consumo; tests/import-reserva-aporte.test.ts. |
| T-04-05 | Tampering | injection descriptor → suggestion | mitigate | SuggestionSlot inert (suggestCategory null in v1); the enum-validation wrapper constrains any future output to an owned category_id (SEC-03); pinned by suggest.test.ts (Plan 01). |
</threat_model>

<verification>
- `npm run build` compiles /importar/[statementId]; `npx tsc --noEmit` + `npm run lint` clean.
- Full suite GREEN: import.test.ts + the five named integration tests (learn-on-confirm, point-in-time, recurring, reserva-aporte, idor) now live and passing.
- A confirmed import auto-classifies the same merchant on a second ingest (CLS-04); a renamed category does not rewrite imported rows (CLS-05); a Reserva row creates the aporte 'in' entry (RSV-06).
- Nothing is written to transactions until Confirmar; unclassified rows persist category-less and learn nothing.
- The reserva/IDOR helpers are shared between transactions.ts and import.ts (no cross-sibling duplication/drift).
</verification>

<success_criteria>
- Pre-persist review grid (ExtratoTable sibling) with memory-miss attention, inline + bulk classify, the "Qual reserva?" picker, recorrente flag, and the reserved-but-inert suggestion slot.
- Confirmar persists point-in-time rows, dedupes by dedupe_key, learns merchant_patterns only for classified rows on confirm, and creates the reserva aporte via the reused Phase-3 path.
- IMP-05, CLS-03/04/05/06, RSV-06 delivered end-to-end; SEC-03 / CLS-02 seam present and enum-validated (LLM call deferred).
</success_criteria>

<output>
Create `.planning/phases/04-upload-classifica-o-inteligente/04-03-SUMMARY.md` when done. As the executor completes requirements, update REQUIREMENTS.md traceability (IMP/CLS/RSV-06/SEC-03 → Complete; CLS-02 stays Pending/deferred). Leave the LOCAL stack running for the Plan 04 human-verify walkthrough.
</output>
