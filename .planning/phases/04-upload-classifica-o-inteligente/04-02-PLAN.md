---
phase: 04-upload-classifica-o-inteligente
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/actions/import.ts
  - src/actions/import.test.ts
  - src/app/(app)/importar/page.tsx
  - src/components/upload-dropzone.tsx
  - src/components/upload-progress.tsx
  - src/components/csv-column-mapper.tsx
  - src/components/import-uploader.tsx
  - src/components/app-sidebar.tsx
autonomous: true
requirements: [IMP-01, IMP-02, IMP-03, IMP-04, CLS-01]

user_setup: []

must_haves:
  truths:
    - "User can open /importar (new 'Importar' nav item) and pick or drag an OFX/CSV file"
    - "The browser uploads the file bytes DIRECT to the private statements bucket via a signed upload URL scoped to {user_id}/ — the Server Action only ever receives the storage path, never the bytes"
    - "The server downloads the object, decodes latin1/UTF-8, parses (in-house OFX parser or papaparse CSV), normalizes to integer-cents rows, two-layer-deduplicates, and classifies memory-first — returning review rows WITHOUT persisting any transaction"
    - "Re-uploading the exact same file hits the content_hash and yields '0 novas' (no duplicate statement, no parse re-run side effects)"
    - "A CSV with ambiguous headers opens the CsvColumnMapper dialog (map Data/Descritor/Valor + save a reusable profile); OFX skips it; a saved profile matching the header signature reuses the mapping silently"
    - "A wrong file type / too-large / unparseable file shows the pt-BR error (destructive) and never persists anything"
  artifacts:
    - path: "src/actions/import.ts"
      provides: "createSignedStatementUpload, ingestStatement (parse→dedup→classify→review rows), saveCsvProfile"
      exports: ["createSignedStatementUpload", "ingestStatement"]
    - path: "src/app/(app)/importar/page.tsx"
      provides: "Upload screen (dropzone → signed-URL upload → ingest → redirect to review)"
      min_lines: 20
    - path: "src/components/upload-dropzone.tsx"
      provides: "Native file input + drag area, client-side type/size validation"
    - path: "src/components/csv-column-mapper.tsx"
      provides: "CSV column-mapping dialog + reusable profile"
    - path: "src/components/app-sidebar.tsx"
      provides: "Importar nav item added to NAV_ITEMS"
      contains: "Importar"
  key_links:
    - from: "src/components/import-uploader.tsx"
      to: "src/actions/import.ts"
      via: "createSignedStatementUpload then uploadToSignedUrl then ingestStatement(path)"
      pattern: "ingestStatement"
    - from: "src/actions/import.ts"
      to: "statements"
      via: "INSERT statements ... ON CONFLICT (user_id, content_hash) DO NOTHING"
      pattern: "content_hash"
    - from: "src/actions/import.ts"
      to: "src/lib/classifier/memory.ts"
      via: "lookupMemory per parsed row"
      pattern: "lookupMemory"
---

<objective>
Ship the upload vertical slice: a real, working OFX/CSV upload that lands the user on a review screen with parsed, deduplicated, memory-classified rows — nothing persisted to `transactions` yet. The browser uploads bytes direct to the private Storage bucket by signed URL (the function never sees bytes, sidestepping the 4.5MB limit); the server downloads, decodes, parses (the in-house OFX parser / papaparse CSV from Plan 01), normalizes, two-layer-deduplicates, and classifies memory-first.

After this task a real user can do something they could not before: drop a statement and SEE their extracted, pre-classified transactions (the review grid that consumes these rows is Plan 03; this slice produces and stores the parsed-rows payload + statement record and routes to it). The CSV column-mapping dialog + reusable profile resolves the ambiguous-header branch.

Purpose: prove the highest-risk path (browser→Storage→parse→dedup→classify) end-to-end on synthetic fixtures, turning the Plan-01 RED dedup/storage-rls tests GREEN.
Output: import.ts Server Actions, the /importar upload screen, the dropzone/progress/uploader/CSV-mapper components, the Importar nav item.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-upload-classifica-o-inteligente/04-CONTEXT.md
@.planning/phases/04-upload-classifica-o-inteligente/04-RESEARCH.md
@.planning/phases/04-upload-classifica-o-inteligente/04-UI-SPEC.md

# The parsers/normalize/dedupe/classifier this slice consumes (Plan 01 output)
@src/lib/parsers/types.ts
@src/lib/parsers/ofx.ts
@src/lib/parsers/csv.ts
@src/lib/normalize.ts
@src/lib/dedupe.ts
@src/lib/classifier/memory.ts
@src/lib/classifier/suggest.ts
@src/lib/schemas/import.ts

# Server Action shape to mirror VERBATIM (Zod safeParse → getClaims → ownership re-derive → typed write)
@src/actions/transactions.ts

# Storage policy (path must start with the caller uid) + nav to extend
@supabase/migrations/0003_storage_statements.sql
@src/components/app-sidebar.tsx
@src/lib/supabase/server.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: import.ts — signed upload URL + ingestStatement (parse→dedup→classify) + CSV profile</name>
  <files>src/actions/import.ts, src/actions/import.test.ts</files>
  <read_first>
    - src/actions/transactions.ts (the exact Server Action shape: 'use server', Zod safeParse → { error }, getClaims() → userId, assertOwned* re-derive before any FK write, revalidatePath; mirror moneyWriteError style)
    - 04-RESEARCH.md Pattern 1 (createSignedUploadUrl, {user_id}/ path), Pattern 2 (download + decodeStatement latin1 fallback), Pattern 5 (two-layer dedup ON CONFLICT DO NOTHING), Pattern 7 (lookupMemory), Pitfall 1 (decode), Pitfall 7 (path scoping)
    - src/lib/parsers/{ofx,csv,types}.ts, src/lib/normalize.ts, src/lib/dedupe.ts, src/lib/classifier/memory.ts, src/lib/schemas/import.ts (Plan 01 — consume, do not re-implement)
    - src/lib/supabase/server.ts (createClient)
    - tests/import-dedup.test.ts + tests/import-storage-rls.test.ts (the RED tests this task turns GREEN)
  </read_first>
  <behavior>
    - createSignedStatementUpload(filename, ext): getClaims → userId; build path `${userId}/${crypto.randomUUID()}.${ext}` (ext ∈ ofx|csv); `supabase.storage.from('statements').createSignedUploadUrl(path)`; return { path, token, signedUrl }. Rejects ext not in {ofx,csv}.
    - ingestStatement(path, originalFilename, mapping?): getClaims → userId; reject a path not prefixed by `${userId}/` (defense-in-depth atop Storage RLS). Download the object; contentHash(bytes); INSERT statements {storage_path, format, content_hash, original_filename, status} ON CONFLICT (user_id, content_hash) DO NOTHING. If the file was already present (content_hash hit) → return { statementId, rows: [], alreadyImported: true } (the "0 novas" path) WITHOUT re-parsing side effects. Else decode (utf-8, fall back to latin1 on U+FFFD), dispatch by ext to parseOfx / parseCsv. For CSV with ambiguous headers and no usable mapping/profile → return { needsMapping: true, headers, sample } (no statement rows yet). Normalize each row, compute dedupeKey, classify memory-first via lookupMemory (HIT → category_id [+reserva_id], source 'memória'; MISS → suggestCategory() → null → source null/unclassified). Persist the parsed-review payload so the review page (Plan 03) can read it: store rows on the statement record (a jsonb column added here via a tiny additive migration is allowed, OR re-derive at review time by re-downloading+parsing — choose the simpler within-budget path and document it; do NOT persist into transactions). Return { statementId, rows, novas, duplicadas }.
    - saveCsvProfile(headerSignature, mapping, name): UPSERT csv_import_profiles ON CONFLICT (user_id, header_signature). lookupCsvProfile(headerSignature): point-read for silent reuse.
    - import.test.ts (mocked-supabase unit, like transactions.test.ts): ext validation, path-prefix rejection, content_hash-hit "0 novas" path returns empty rows + alreadyImported, memory-hit sets source 'memória', memory-miss leaves category null, needsMapping branch for ambiguous CSV.
  </behavior>
  <action>
Create src/actions/import.ts (`'use server'`) mirroring transactions.ts verbatim in shape: Zod safeParse at the boundary → `{ error }` (never throws/leaks), `getClaims()` for the owner, `revalidatePath` on success. Implement createSignedStatementUpload, ingestStatement, saveCsvProfile, lookupCsvProfile.

Decode (Pitfall 1): attempt `new TextDecoder('utf-8', { fatal: false })`, detect U+FFFD replacement chars, fall back to `new TextDecoder('latin1')`. Normalize AFTER decoding so the descriptor_norm key is stable across encodings.

Two-layer dedup (IMP-04): the file layer is the statements content_hash unique + ON CONFLICT DO NOTHING (re-upload → existing row → "0 novas"). The transaction layer (dedupe_key unique) is enforced at confirm (Plan 03) — but ingest computes each row's dedupeKey and pre-marks rows whose dedupe_key already exists in the user's transactions as duplicadas (so the summary header in Plan 03 can show J). Use the proven IDOR discipline: ingestStatement writes ONLY the statement row (own user_id) — no client FK yet; the FK-bearing writes are all in Plan 03's confirmImport.

Sync vs deferred parse (RESEARCH A5 / Open Question 3): start SYNCHRONOUS (download+parse inside the Server Action) — adequate for LOCAL synthetic file sizes; keep ingestStatement pure enough to later move behind a Route Handler + after() if files grow. Document this choice in a comment.

For storing the parsed-rows payload for the review page: prefer the simplest within-budget option. Recommended: add a small additive migration `0024_statements_parsed_rows.sql` (a `parsed_rows jsonb` + `summary jsonb` column on statements, same RLS already covers it) so the review RSC reads the rows back by statementId without re-parsing. Apply LOCAL + regen types as part of this task (it is additive; reset replays clean). If you instead re-derive at review time, document why. Either way, NOTHING lands in transactions until confirm.

import.test.ts: mirror transactions.test.ts's chainable Supabase mock; cover the behaviors above. The live integration assertions (dedup "0 novas", storage RLS path) live in the Plan-01 tests/import-dedup.test.ts + tests/import-storage-rls.test.ts — flip their RED markers to live assertions now that ingestStatement exists.
  </action>
  <verify>
    <automated>cd /home/dmsc/Dev/Projetos/Pessoal/gestao-financeira && SUPABASE_DISABLE_TELEMETRY=1 npx vitest run src/actions/import.test.ts tests/import-dedup.test.ts tests/import-storage-rls.test.ts 2>&1 | tail -20 && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>import.test.ts unit tests pass; tests/import-dedup.test.ts ("0 novas" content_hash + cross-statement dedupe_key) and tests/import-storage-rls.test.ts (path-scoped upload) GREEN against the LOCAL stack; ingestStatement persists ONLY a statement row (no transactions); tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Upload screen — dropzone, progress, signed-URL uploader, CSV mapper, nav item</name>
  <files>src/app/(app)/importar/page.tsx, src/components/upload-dropzone.tsx, src/components/upload-progress.tsx, src/components/import-uploader.tsx, src/components/csv-column-mapper.tsx, src/components/app-sidebar.tsx</files>
  <read_first>
    - 04-UI-SPEC.md §1 (Upload screen), §2 (CsvColumnMapper), §Component Inventory (UploadDropzone/UploadProgress/CsvColumnMapper contracts), §Copywriting Contract (exact pt-BR strings), §Color (teal accent rules, destructive only for genuine failures), §Accessibility (labeled file input, aria-live progress)
    - src/components/app-sidebar.tsx (NAV_ITEMS shape + active-item styling to mirror)
    - src/components/reserva-form.tsx or categoria-form.tsx (the existing dialog + field form grammar to mirror)
    - src/components/ui/progress (Phase-3 component, reused for the upload bar)
    - src/actions/import.ts (Task 1 — createSignedStatementUpload, ingestStatement, save/lookupCsvProfile)
  </read_first>
  <action>
Add the Importar nav item to app-sidebar.tsx NAV_ITEMS: `{ href: '/importar', label: 'Importar', icon: Upload }` (lucide `Upload`), keeping the existing `data-active:bg-muted data-active:text-primary` styling. Place it after Extrato.

Create upload-dropzone.tsx: a `--card`/dashed-border drag area (`min-h-48`) with a centered `upload` icon + "Selecionar arquivo" teal CTA + "ou arraste aqui · OFX (recomendado) ou CSV · até {limite}" hint. Backed by a real labeled `<input type="file" accept=".ofx,.csv">` (aria-label "Selecionar arquivo OFX ou CSV"). On drag-over: teal `--primary` ring + `bg-primary/5`. Client-side validate extension + size; on failure show inline `text-destructive` ("Formato não suportado..." / "Arquivo muito grande..."). Emits the chosen File.

Create upload-progress.tsx: wraps `progress` (h-2) + mono `%` + filename + a status line transitioning "Enviando…" → "Processando…" (the parse phase is indeterminate — `loader-2` spin). `role="progressbar"` + an `aria-live="polite"` status. Cancel/retry on error.

Create import-uploader.tsx ('use client'): orchestrates the upload lifecycle. On file chosen → `createSignedStatementUpload(filename, ext)` → `supabase.storage.from('statements').uploadToSignedUrl(path, token, file)` (direct browser→Storage) → swap dropzone for UploadProgress → `ingestStatement(path, filename)`. Branch on the result: `alreadyImported`/novas===0 → toast "Este arquivo já foi importado — 0 novas transações." + route to review (which shows the duplicate-only empty state); `needsMapping` (CSV) → open CsvColumnMapper; otherwise `router.push('/importar/' + statementId)`. Parse-failure → the "Não foi possível ler este arquivo..." block + "Tentar outro arquivo".

Create csv-column-mapper.tsx: a `dialog` (title "Mapear colunas do CSV") shown only on the needsMapping branch. Three `field` `Select`s mapping detected headers → Data / Descritor / Valor (each previews the column's first sample value). A live preview `table` (≤5 rows) rendered as-parsed (dd/MM, formatCents valor, descritor). A "Salvar como perfil" switch (default on) + optional name. Validate Data/Descritor/Valor map to DISTINCT columns; inline FieldError "Cada campo (data, descritor, valor) precisa de uma coluna diferente." and "Esta coluna não parece conter valores monetários." on a bad valor column. Primary CTA "Salvar mapeamento" → saveCsvProfile + ingestStatement(path, filename, mapping) → review. "Cancelar" discards. Empty-preview muted note. On a profile-signature match the uploader skips this dialog and toasts "Mapeamento reaproveitado."

Create /importar/page.tsx (RSC shell): h1 "Importar fatura" + helper "Suba o OFX ou CSV do seu banco. Vamos extrair, deduplicar e pré-classificar pela memória." + render the client ImportUploader. Use only `formatCents`/`centsToEditableBRL` for any money preview; the global MonthSelector is NOT the driver here.
  </action>
  <verify>
    <automated>cd /home/dmsc/Dev/Projetos/Pessoal/gestao-financeira && npx tsc --noEmit 2>&1 | tail -5 && npm run lint 2>&1 | tail -5 && npm run build 2>&1 | grep -E "/importar|Compiled|error" | tail -10 && grep -q "Importar" src/components/app-sidebar.tsx</automated>
  </verify>
  <done>/importar compiles in `npm run build`; tsc + lint clean (modulo the known pre-existing React-Compiler useReactTable warning); the Importar nav item is present; the dropzone is a real labeled file input with client-side type/size validation; uploading routes to /importar/[statementId] (or the duplicate empty-state) and the CSV ambiguous branch opens the mapper.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Storage | File bytes cross here via the signed upload URL; the `{user_id}/` path scope (0003, split per-verb in Plan 01) is the gate. |
| client → Server Action | filename/ext/path/mapping arrive untrusted; Zod + path-prefix re-check + getClaims gate them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Information Disclosure | signed upload path | mitigate | createSignedStatementUpload builds `${userId}/${uuid}.${ext}`; ingestStatement rejects a path not prefixed by the caller's uid (defense-in-depth over Storage RLS); never `getPublicUrl()` for statements; tests/import-storage-rls.test.ts. |
| T-04-04 | Tampering | duplicate import | mitigate | statements content_hash unique + ON CONFLICT DO NOTHING → re-upload "0 novas"; per-row dedupeKey pre-marked; tests/import-dedup.test.ts. |
| T-04-06 | Information Disclosure | pt-BR/latin1 mojibake → wrong memory key | mitigate | decodeStatement tries utf-8, falls back to latin1 on U+FFFD before normalizeDescriptor (Pitfall 1) — wrong-key memory misses avoided. |
| T-04-07 | Denial of Service | large file through the function | accept | LOCAL-only v1 with small synthetic files + direct-to-Storage upload (function never streams bytes); the Route Handler + after() scale path is a documented later swap (RESEARCH A5). |
| T-04-SC | Tampering | npm installs | mitigate | No new package this task (papaparse already in Plan 01); the dropzone is a native file input, no registry component (UI-SPEC Registry Safety). |
</threat_model>

<verification>
- `npm run build` compiles /importar; `npx tsc --noEmit` + `npm run lint` clean.
- import.test.ts unit GREEN; tests/import-dedup.test.ts + tests/import-storage-rls.test.ts GREEN against LOCAL.
- An OFX upload (synthetic fixture) reaches the review route with parsed memory-classified rows; a byte-identical re-upload shows "0 novas"; an ambiguous CSV opens the mapper.
- ingestStatement writes ONLY a statement row — zero transactions persisted before confirm.
</verification>

<success_criteria>
- The browser uploads direct to Storage by signed URL; the Server Action receives only the path.
- Server parses (in-house OFX / papaparse CSV), decodes latin1, normalizes to integer-cents, two-layer-deduplicates, classifies memory-first.
- Re-upload of the same file → "0 novas"; ambiguous CSV → CsvColumnMapper + reusable profile.
- Importar nav item live; upload screen with dropzone + progress + error states per the UI contract.
</success_criteria>

<output>
Create `.planning/phases/04-upload-classifica-o-inteligente/04-02-SUMMARY.md` when done. Leave the LOCAL stack running for Plan 03.
</output>
