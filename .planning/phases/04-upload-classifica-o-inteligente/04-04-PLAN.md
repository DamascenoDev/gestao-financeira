---
phase: 04-upload-classifica-o-inteligente
plan: 04
type: execute
wave: 4
depends_on: [04-01, 04-02, 04-03]
files_modified: []
autonomous: false
requirements: [IMP-01, IMP-02, IMP-05, CLS-03, CLS-04]

user_setup: []

must_haves:
  truths:
    - "A real OFX upload drops → progress → review screen lands with parsed, memory-classified rows"
    - "An ambiguous CSV upload opens the column-mapping dialog; mapping Data/Descritor/Valor + saving a profile reaches review, and a second same-layout CSV reuses the profile silently"
    - "Classifying a memory-miss row (inline + bulk) then Confirmar persists the rows AND learns the patterns — a re-upload of the same merchants now auto-classifies (origem 'Memória')"
    - "A byte-identical re-upload shows the '0 novas' line + the duplicate-only empty state (no duplication in the Extrato)"
  artifacts: []
  key_links: []
---

<objective>
Human-verify the Phase-4 browser-interaction behaviors that automated tests cannot cover (file drag/drop, the CSV mapping dialog, the visual memory-miss attention state, and the end-to-end learn→auto-classify loop across two uploads). Everything underneath is already automated and GREEN (parse/dedup/normalize/memory/learn/IDOR — Plans 01-03); this plan is the manual confirmation that the assembled UI flow works in the browser against the LOCAL stack on synthetic fixtures.

Purpose: confirm the highest-risk slice end-to-end as a human before the phase is declared done — the manual-only items from 04-VALIDATION.md.
Output: a human sign-off (or a punch-list of issues) for the upload → mapping → review → confirm → re-upload-auto-classify flow.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-upload-classifica-o-inteligente/04-VALIDATION.md
@.planning/phases/04-upload-classifica-o-inteligente/04-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Boot the LOCAL stack + dev server and confirm the suite is green before the walkthrough</name>
  <files></files>
  <read_first>
    - 04-VALIDATION.md (Manual-Only Verifications table — the three browser items)
  </read_first>
  <action>
Ensure the LOCAL Supabase stack is running (API on 127.0.0.1:55321; `npx supabase status` confirms) with migrations 0001-0023 applied. Run the full suite once (`SUPABASE_DISABLE_TELEMETRY=1 npx vitest run && npx tsc --noEmit`) to confirm everything Plans 01-03 produced is GREEN — the manual walkthrough should only ever surface UI/interaction issues, never logic regressions. Start the dev server (`npm run dev`) and confirm /importar renders and the Importar nav item is present. Confirm the synthetic fixtures (tests/fixtures/*.ofx, *.csv) are available to hand to the file picker (or copy them somewhere the browser file dialog can reach). Report the dev URL + that the suite is green, then hand off to the human checkpoint.
  </action>
  <verify>
    <automated>cd /home/dmsc/Dev/Projetos/Pessoal/gestao-financeira && SUPABASE_DISABLE_TELEMETRY=1 npx vitest run 2>&1 | tail -5 && npx tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <done>LOCAL stack up (0001-0023), full suite GREEN, dev server running, /importar reachable, fixtures available for the file picker.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The complete upload → (CSV mapping) → review → confirm → re-upload-auto-classify flow, assembled from Plans 01-03, running against the LOCAL stack on synthetic fixtures. Underneath, parse/dedup/normalize/memory/learn/IDOR are all automated and GREEN.</what-built>
  <how-to-verify>
1. **OFX upload → review (IMP-01):** Go to /importar. Drag (or pick) `tests/fixtures/itau-sample.ofx`. Confirm: the dropzone shows "Enviando…" then "Processando…", then you land on the review screen with parsed rows (Data dd/MM, Descritor cru + the normalizado helper, Valor in R$, neutral never red). Memory-miss rows show the amber left-border + "Não classificada" badge; the summary header shows the N/M/K/J counts.
2. **CSV mapping (IMP-02):** Upload `tests/fixtures/ambiguous-cols.csv`. Confirm the "Mapear colunas do CSV" dialog opens. Map Data / Descritor / Valor to distinct columns (the preview table updates live, dd/MM + R$). Try mapping two fields to the same column → the "Cada campo... precisa de uma coluna diferente." error blocks "Salvar mapeamento". Fix it, keep "Salvar como perfil" on, save → you reach review. Then re-upload a same-layout CSV and confirm the mapping is reused silently (toast "Mapeamento reaproveitado.").
3. **Classify + confirm + learn (IMP-05 / CLS-03):** On the review screen, classify an amber memory-miss row inline (pick a category); select ≥2 amber rows and bulk-"Classificar" via the SelectionActionBar; classify one row into the Reserva category and confirm the "Qual reserva?" picker appears. Hit "Confirmar importação". If K>0 the "Importar sem classificar?" guard appears — confirm through it. Success toast "{M} transações importadas"; you land on the Extrato with the new rows in the right month.
4. **Learn → auto-classify (CLS-04):** Re-upload an OFX whose merchants you just classified (e.g. itau-sample again after classifying). Confirm those merchants now show origem "Memória" automatically (no amber, no manual pick needed).
5. **Re-upload "0 novas" (IMP-04):** Upload the byte-identical `itau-sample.ofx` a second time with no changes. Confirm the "0 novas — este arquivo já foi importado." line + the "Nada novo neste arquivo" empty state, and that the Extrato did NOT duplicate any row.

Report PASS, or list any visual/interaction issues (wrong copy, missing amber accent, dialog not opening, column reflow when imagining the suggestion chip, mobile card layout, etc.).
  </how-to-verify>
  <resume-signal>Type "approved" or describe the issues to fix.</resume-signal>
</task>

</tasks>

<verification>
- The full automated suite is GREEN before the human walkthrough begins (no logic regressions to chase during manual testing).
- The five manual scenarios above pass in the browser against the LOCAL stack.
</verification>

<success_criteria>
- OFX/CSV upload, the CSV mapping dialog + profile reuse, inline + bulk classify, the Reserva picker, Confirmar with the unclassified guard, the learn→auto-classify loop, and the "0 novas" re-upload all work in the browser.
- Human sign-off recorded (or a punch-list returned for gap closure).
</success_criteria>

<output>
Create `.planning/phases/04-upload-classifica-o-inteligente/04-04-SUMMARY.md` when done with the human-verify outcome. On "approved", the phase is ready for `/gsd-verify-work`. The remote db push + Vercel deploy stay deferred (per the standing Phase-1 blocker — credentials gathered at the end).
</output>
