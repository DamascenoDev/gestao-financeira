---
phase: 01-funda-o-auth-rls-dinheiro-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - vitest.config.ts
  - vitest.setup.ts
  - src/lib/money.ts
  - src/lib/money.test.ts
  - tests/rls-isolation.test.ts
  - tests/seed-categories.test.ts
  - tests/bundle-secret-grep.test.ts
  - tests/helpers/local-supabase.ts
  - scripts/check-bundle-secrets.sh
  - supabase/config.toml
  - .env.example
  - .gitignore
autonomous: true
requirements: [SEC-02]
must_haves:
  truths:
    - "Running `npx vitest run` executes the full Wave-0 suite (some RED until later waves)"
    - "money helper: R$ 0,10 + R$ 0,20 sums to exactly R$ 0,30 (integer centavos, no float)"
    - "Next.js 16 App Router (TS strict) scaffold lives in the repo root with .planning/ and agents/ preserved"
    - "supabase/config.toml exists so `supabase start` boots a local stack"
  artifacts:
    - path: "src/lib/money.ts"
      provides: "parseBRLToCents + formatCents centavos convention helper"
      exports: ["parseBRLToCents", "formatCents"]
    - path: "src/lib/money.test.ts"
      provides: "SEC-02 money exactness unit test (centavos, no float)"
      contains: "parseBRLToCents"
    - path: "tests/rls-isolation.test.ts"
      provides: "two-user RLS isolation test against local Supabase (RED until Wave 2)"
      contains: "rls"
    - path: "tests/seed-categories.test.ts"
      provides: "signup seeds 11 BR categories test (RED until Wave 2)"
      contains: "categories"
    - path: "tests/bundle-secret-grep.test.ts"
      provides: "SEC-02 client-bundle secret-leak grep gate (RED until Wave 3 build exists)"
      contains: "sb_secret_"
    - path: "scripts/check-bundle-secrets.sh"
      provides: "grep gate over .next/static for sb_secret_ / service_role"
      contains: "sb_secret_"
    - path: "supabase/config.toml"
      provides: "local Supabase stack configuration"
      contains: "project_id"
  key_links:
    - from: "src/lib/money.test.ts"
      to: "src/lib/money.ts"
      via: "import { parseBRLToCents, formatCents }"
      pattern: "from '\\.\\./.*money'|from '@/lib/money'"
    - from: "scripts/check-bundle-secrets.sh"
      to: ".next/static"
      via: "grep over built client bundle"
      pattern: "\\.next/static"
---

<objective>
Stand up the Walking-Skeleton substrate: scaffold Next.js 16 (App Router, TS strict) into the existing repo root without clobbering `.planning/` or `agents/`, install the Phase-1 dependency set, initialize the local Supabase stack config, and install the test framework. Then write the four Nyquist tests from `01-VALIDATION.md` — money exactness, two-user RLS isolation, seed-categories count, and client-bundle secret grep — RED first. Implement only the one production artifact whose test runs entirely in-process now: `src/lib/money.ts` (centavos convention). The RLS, seed, and bundle tests stay RED until later waves create the migrations (Wave 2) and the client build (Wave 3).

Purpose: establish the money-exactness convention and the secret-leak gate (SEC-02) before any feature code can introduce float or leak a key, and create the failing test scaffold that every later wave drives to green.
Output: a buildable, lint-clean Next.js project with a passing money unit test and three intentionally-RED integration/smoke tests wired to their Wave-0 helpers.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-funda-o-auth-rls-dinheiro-schema/01-CONTEXT.md
@.planning/phases/01-funda-o-auth-rls-dinheiro-schema/01-RESEARCH.md
@.planning/phases/01-funda-o-auth-rls-dinheiro-schema/01-VALIDATION.md
@.planning/phases/01-funda-o-auth-rls-dinheiro-schema/SKELETON.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 16 into the existing repo + install Phase-1 deps (preserve .planning/ and agents/)</name>
  <files>package.json, tsconfig.json, next.config.ts, .gitignore, .env.example, src/app/layout.tsx, src/app/globals.css, components.json, supabase/config.toml</files>
  <read_first>
    - .planning/phases/01-funda-o-auth-rls-dinheiro-schema/01-RESEARCH.md (Standard Stack → "Installation (Phase 1)"; Recommended Project Structure; Runtime State Inventory → "Build artifacts" row; Assumptions Log A6)
  </read_first>
  <action>
    Scaffold into the repo root WITHOUT destroying `.planning/`, `agents/`, `CLAUDE.md`, or `.git`. `create-next-app .` refuses a non-empty dir, so scaffold into a temp dir then move files in: run `npx create-next-app@latest gf-tmp --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --use-npm --turbopack` (auto-answer any remaining prompt with `--yes`), then move every generated entry (including dotfiles like `.gitignore`, `next.config.ts`, but NOT `gf-tmp/.git`) up into the repo root, merging — never overwriting — the existing `.gitignore` (append Next/Supabase ignores; ensure `.env.local`, `.next/`, `node_modules/` are ignored). Remove `gf-tmp`. After moving, verify `.planning/` and `agents/` still exist and are untouched.
    Pin the verified majors from RESEARCH (Next 16.2.9 / React 19.2.7 / Tailwind 4.3.1) — let npm resolve patches within them. Install the Phase-1 runtime deps: `npm install @supabase/supabase-js @supabase/ssr zod react-hook-form @hookform/resolvers sonner server-only`. Install dev deps: `npm install -D supabase`. Initialize shadcn (`npx shadcn@latest init`, Tailwind v4 flow) and add only the components this phase needs: `npx shadcn@latest add button card input label form sonner`.
    Enable TypeScript strict in `tsconfig.json` (`"strict": true`, `"noUncheckedIndexedAccess": true`). Run `supabase init` to create `supabase/config.toml` (CLI convention keeps `supabase/` at repo root, not under `src/`). Create `.env.example` with documented placeholders only (no real values): `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, `SUPABASE_SECRET_KEY`. Do NOT create `.env.local` here (credential paste is Plan 04, autonomous:false). Add npm scripts to `package.json`: `"db:push": "supabase db push"`, `"db:reset": "supabase db reset"`, `"gen:types": "supabase gen types typescript --local > src/types/database.types.ts"`, `"test": "vitest run"`, `"test:watch": "vitest"`. Do NOT install any deferred library (no `ai`, `pdf-parse`, `papaparse`, `decimal.js`, `date-fns`, `recharts`, `@tanstack/*`) — those belong to Phases 2-5.
  </action>
  <verify>
    <automated>test -d .planning && test -d agents && test -f package.json && test -f supabase/config.toml && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>`npm run build` succeeds; `.planning/` and `agents/` are intact; `supabase/config.toml` exists; no deferred-phase library is in `package.json` dependencies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Install vitest + write money exactness test (RED), then implement src/lib/money.ts (GREEN)</name>
  <files>vitest.config.ts, vitest.setup.ts, src/lib/money.test.ts, src/lib/money.ts, package.json</files>
  <read_first>
    - .planning/phases/01-funda-o-auth-rls-dinheiro-schema/01-RESEARCH.md (Code Examples §10 money helper; Code Examples §11 npm scripts; Validation Architecture → Test Framework + Wave 0 Gaps)
    - .planning/research/PITFALLS.md (Pitfall 1: float money — parse pt-BR "1.234,56" → Math.round(value*100))
  </read_first>
  <behavior>
    - Test: parseBRLToCents("1.234,56") === 123456 (strips thousands dot, comma is decimal)
    - Test: parseBRLToCents("0,10") + parseBRLToCents("0,20") === 30 (exact, no float drift)
    - Test: formatCents(30) === "R$ 0,30" and formatCents(123456) === "R$ 1.234,56" (pt-BR, non-breaking space tolerated)
    - Test: round-trip — formatCents(parseBRLToCents("1.234,56")) === "R$ 1.234,56"
    - Test: parseBRLToCents returns an integer for any valid input (Number.isInteger true)
  </behavior>
  <action>
    Install the test framework: `npm install -D vitest @vitejs/plugin-react @testing-library/react @testing-library/jest-dom jsdom`. Create `vitest.config.ts` using `@vitejs/plugin-react`, `environment: 'jsdom'`, `setupFiles: ['./vitest.setup.ts']`, and a `test.include` covering both `src/**/*.test.ts(x)` and `tests/**/*.test.ts`. Create `vitest.setup.ts` importing `@testing-library/jest-dom`. Write `src/lib/money.test.ts` FIRST encoding the behavior cases above; run it and confirm it FAILS (no `money.ts` yet) — this is the RED step. Then create `src/lib/money.ts` exporting `parseBRLToCents(input: string): number` (trim, strip `.`, replace `,`→`.`, `Math.round(Number(x)*100)`) and `formatCents(cents: number): string` (`Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'})` over `cents/100`). Centavos are the only money representation; parse once at ingest, format only at the UI edge. Make the test GREEN. The pt-BR locale uses a non-breaking space after `R$` — assert with a normalized comparison (replace ` ` with a regular space) so the test is robust across Node ICU builds.
  </action>
  <verify>
    <automated>npx vitest run src/lib/money.test.ts</automated>
  </verify>
  <done>`src/lib/money.test.ts` passes; `0,10 + 0,20` yields exactly `30` centavos; `formatCents` renders pt-BR currency; helper returns integers only.</done>
</task>

<task type="auto">
  <name>Task 3: Write the three remaining Nyquist tests RED + the bundle-secret grep gate</name>
  <files>tests/rls-isolation.test.ts, tests/seed-categories.test.ts, tests/bundle-secret-grep.test.ts, tests/helpers/local-supabase.ts, scripts/check-bundle-secrets.sh</files>
  <read_first>
    - .planning/phases/01-funda-o-auth-rls-dinheiro-schema/01-VALIDATION.md (Per-Task Verification Map; Wave 0 Requirements; Manual-Only Verifications)
    - .planning/phases/01-funda-o-auth-rls-dinheiro-schema/01-RESEARCH.md (Validation Architecture → Phase Requirements → Test Map; Code Examples §6-9 for the schema the tests assert against; Pitfall 2 RLS; Pitfall 3 secret key)
  </read_first>
  <action>
    Create `tests/helpers/local-supabase.ts` — a helper that reads the LOCAL Supabase URL + keys from `supabase status` output (or the documented local defaults the CLI prints), and exposes: a `serviceClient()` (local-only secret key, used solely by tests to create two throwaway users via the admin API) and `userClient(jwt)` factories. Guard the helper so it ONLY ever targets `127.0.0.1`/localhost (never a remote project). These helpers will be exercised once migrations exist (Wave 2); writing them now defines the test contract.
    Write `tests/rls-isolation.test.ts` (AUTH-03 / threat T2+T4): create user A and user B against the local stack; A inserts a `categories` row (or relies on the seed); assert that B, using B's JWT, gets 0 rows selecting A's data AND that B's INSERT/UPDATE/DELETE targeting A's `user_id` all fail or affect 0 rows — exercise all four verbs on both `categories` and `profiles`. Mark it so it runs against `supabase start`. It WILL be RED now (no tables) — confirm it fails for the right reason (relation/policy missing), not a helper crash.
    Write `tests/seed-categories.test.ts` (CAT-01): create a fresh user via the admin API; assert exactly 11 categories are seeded for that `user_id`, and that `Investimentos` and `Reserva` have `kind = 'alocacao'` while the rest are `'consumo'`. RED now.
    Write `tests/bundle-secret-grep.test.ts` (SEC-02 / threat T1): shell out to `scripts/check-bundle-secrets.sh` and assert exit 0 (no secret found). Create `scripts/check-bundle-secrets.sh`: after a build, grep `.next/static` recursively for `sb_secret_`, `service_role`, and `SUPABASE_SECRET_KEY` — exit non-zero if any match, exit 0 if `.next/static` is missing OR clean. Filter out comment-only lines before counting (`grep -v '^#'` style); never gate on an unfiltered `== 0`. RED now (no `.next/static` build artifact wired into the test path yet — the test asserts the script's contract and goes green once Wave 3 builds the client).
    For every test file whose dependency is a later wave, add a top-of-file comment `// RED until Wave N: <reason>` so the executor knows the failure is expected.
  </action>
  <verify>
    <automated>bash scripts/check-bundle-secrets.sh; npx vitest run tests/ 2>&1 | grep -Eq 'rls-isolation|seed-categories|bundle-secret' </automated>
  </verify>
  <done>All three test files exist and are collected by vitest (visible in output); `check-bundle-secrets.sh` runs and exits 0 on a clean/absent bundle; each RED test carries a `// RED until Wave N` marker explaining the expected failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build output → browser | `.next/static` ships to every visitor; any secret here is world-readable |
| test code → local DB | tests use a local-only service key; must never point at the user's remote project |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 (T1) | Information Disclosure | secret key in `.next/static` client bundle (SEC-02) | mitigate | `scripts/check-bundle-secrets.sh` greps the built bundle for `sb_secret_`/`service_role`; `tests/bundle-secret-grep.test.ts` enforces it; gate goes green at Wave 3 build |
| T-1-02 | Tampering | npm installs (create-next-app, supabase, shadcn, vitest) | accept | All Phase-1 packages cleared `[Approved]` in RESEARCH "Package Legitimacy Audit"; no `[ASSUMED]`/`[SUS]` packages remain → no install checkpoint required |
| T-1-03 | Information Disclosure | test helper accidentally targeting remote Supabase | mitigate | `tests/helpers/local-supabase.ts` hard-guards to `127.0.0.1`/localhost; no remote credentials in test scope |
</threat_model>

<verification>
- `npm run build` succeeds and `npx tsc --noEmit` is clean (TS strict).
- `npx vitest run src/lib/money.test.ts` passes (SEC-02 money exactness).
- `npx vitest run tests/` collects all three integration/smoke tests; RLS + seed are RED with clear "relation/policy missing" reasons (driven green in Wave 2); bundle grep exits 0.
- `.planning/` and `agents/` directories survived the scaffold.
</verification>

<success_criteria>
- Buildable Next.js 16 App Router + TS-strict project in the repo root, `.planning/`/`agents/` preserved.
- `src/lib/money.ts` proven exact for centavos (no float) by a green unit test.
- Four Wave-0 tests exist; the three later-wave tests are RED for the right reasons and annotated; the bundle-secret gate script is in place.
- `supabase/config.toml` present so Wave 2 can `supabase start` / `db reset` locally.
</success_criteria>

<output>
Create `.planning/phases/01-funda-o-auth-rls-dinheiro-schema/01-01-SUMMARY.md` when done.
</output>
