---
phase: 07-identidade-visual-e-polimento
plan: 06
type: execute
wave: 4
depends_on: ["07-01", "07-02", "07-03", "07-04"]
files_modified:
  - src/components/table-skeleton.tsx
  - src/components/table-skeleton.test.tsx
  - src/components/card-skeleton.tsx
  - src/components/chart-skeleton.tsx
  - src/app/(app)/dashboard/loading.tsx
  - src/app/(app)/extrato/loading.tsx
  - src/app/(app)/mei/loading.tsx
autonomous: true
requirements: [UI-08]
must_haves:
  truths:
    - "As rotas pesadas (dashboard, extrato, mei) mostram skeletons (nunca spinners) enquanto o corpo carrega, com a chrome visível"
    - "Empty states usam o primitivo shadcn empty com ícone + heading + body + uma CTA gold, consistentes nas ~20 rotas"
    - "Erros aparecem como bloco inline text-destructive com caminho de recuperação, nunca stack cru"
    - "Superfícies interativas têm transition 150ms ease-out e foco gold visível; prefers-reduced-motion respeitado"
  artifacts:
    - path: "src/components/table-skeleton.tsx"
      provides: "Skeleton de tabela (header + N linhas shimmer) espelhando colunas do extrato"
      exports: ["TableSkeleton"]
    - path: "src/components/card-skeleton.tsx"
      provides: "Skeleton de grid de cards"
      exports: ["CardSkeleton"]
    - path: "src/components/chart-skeleton.tsx"
      provides: "Skeleton de chart (aspect box + shimmer)"
      exports: ["ChartSkeleton"]
    - path: "src/app/(app)/dashboard/loading.tsx"
      provides: "Loading RSC do dashboard usando ChartSkeleton + TableSkeleton"
      contains: "Skeleton"
  key_links:
    - from: "src/components/table-skeleton.tsx"
      to: "src/components/ui/skeleton.tsx"
      via: "wrap do primitivo Skeleton (animate-pulse bg-muted)"
      pattern: "Skeleton"
    - from: "src/app/(app)/dashboard/loading.tsx"
      to: "src/components/chart-skeleton.tsx + table-skeleton.tsx"
      via: "loading.tsx do segmento renderiza os skeletons"
      pattern: "ChartSkeleton|TableSkeleton"
---

<objective>
Fecha o polimento (UI-08): cria os skeletons consistentes (`TableSkeleton`, `CardSkeleton`, `ChartSkeleton`) sobre o primitivo shadcn `skeleton`, instala `loading.tsx` por segmento nas rotas pesadas (dashboard, extrato, mei) para streaming com a chrome visível, e padroniza a gramática empty/error + micro-interações (transition 150ms, foco gold, prefers-reduced-motion) nas telas existentes. Skeletons, nunca spinners (UI-SPEC §Polish).

Cobre D-locked: empty/loading/error states + micro-interações consistentes (CONTEXT §Polimento+mobile; UI-SPEC §Polish §Copywriting). Implementa UI-08.

Purpose: elevar de "esqueleto funcional" a "produto premium coeso" — estados de carregamento/vazio/erro consistentes em todas as telas.
Output: três skeletons + teste Wave-0, loading.tsx por segmento, varredura de empty/error/transição.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-identidade-visual-e-polimento/07-UI-SPEC.md
@.planning/phases/07-identidade-visual-e-polimento/07-PATTERNS.md

@src/components/ui/skeleton.tsx
@src/components/ui/empty.tsx
@src/components/extrato-table.tsx
@src/app/(app)/dashboard/page.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TableSkeleton + CardSkeleton + ChartSkeleton (sobre shadcn skeleton)</name>
  <read_first>
    - src/components/ui/skeleton.tsx (o primitivo a envolver — animate-pulse rounded-md bg-muted)
    - src/components/extrato-table.tsx (header de colunas ~374-407: larguras w-10 select / w-16 Data — o TableSkeleton espelha)
    - .planning/phases/07-identidade-visual-e-polimento/07-UI-SPEC.md (§Polish loading → skeletons nunca spinners; TableSkeleton header+N rows, CardSkeleton grid, ChartSkeleton aspect box; tw-animate-css shimmer; prefers-reduced-motion)
    - .planning/phases/07-identidade-visual-e-polimento/07-PATTERNS.md (§Skeletons — analog ui/skeleton.tsx, espelhar colunas do extrato; §RESEARCH Validation Wave 0 table-skeleton.test)
  </read_first>
  <behavior>
    - TableSkeleton({ rows = 8 }): renderiza um header + `rows` linhas de Skeleton com larguras espelhando as colunas do extrato (select/Data/Descrição/Categoria/Valor).
    - CardSkeleton({ count }): renderiza `count` cards Skeleton em grid.
    - ChartSkeleton(): renderiza uma caixa de aspect fixo com shimmer.
    - Todos usam o primitivo Skeleton (sem spinner); reduzem a opacity-only sob prefers-reduced-motion.
  </behavior>
  <action>Cria `src/components/table-skeleton.test.tsx` PRIMEIRO (Wave-0 RED): assere que `TableSkeleton` renderiza ≥1 header + N linhas de placeholder usando o primitivo Skeleton e que `ChartSkeleton`/`CardSkeleton` renderizam (smoke). Depois cria os três componentes envolvendo `<Skeleton>` de `src/components/ui/skeleton.tsx`: `src/components/table-skeleton.tsx` (`TableSkeleton({ rows = 8 })` — header + linhas com larguras espelhando o header do extrato-table), `src/components/card-skeleton.tsx` (`CardSkeleton({ count = 3 })` — grid de cards), `src/components/chart-skeleton.tsx` (`ChartSkeleton()` — `aspect-video`/`h-[240px]` shimmer box). Usa o shimmer do `tw-animate-css` (já dep) e respeita `prefers-reduced-motion` (reduz a opacity-only). Faz o teste virar GREEN.</action>
  <verify>
    <automated>npm test -- table-skeleton 2>&1 | tail -10 && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Os três componentes existem e exportam `TableSkeleton` / `CardSkeleton` / `ChartSkeleton`, cada um usando o primitivo `Skeleton` (grep `Skeleton` e import de `ui/skeleton`).
    - Nenhum dos três usa spinner (sem `animate-spin`/`Loader` — grep `animate-spin` retorna 0 nos três).
    - `table-skeleton.test.tsx` existe e está GREEN.
    - `npm test -- table-skeleton` GREEN; `npx tsc --noEmit` limpo.
  </acceptance_criteria>
  <done>TableSkeleton, CardSkeleton e ChartSkeleton existem sobre o primitivo Skeleton (nunca spinner), respeitam reduced-motion, e o teste Wave-0 está GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: loading.tsx por segmento + varredura empty/error/transição</name>
  <read_first>
    - src/components/table-skeleton.tsx + card-skeleton.tsx + chart-skeleton.tsx (Task 1)
    - src/app/(app)/dashboard/page.tsx (chrome a manter visível: h1 + metaDialog; corpo a esqueletar = charts + lista de aderência)
    - src/components/ui/empty.tsx (Empty > EmptyHeader > EmptyTitle + EmptyDescription + EmptyContent com CTA — gramática a padronizar)
    - .planning/phases/07-identidade-visual-e-polimento/07-UI-SPEC.md (§Polish empty/loading/error/micro-interactions; §Copywriting error genérico "Não foi possível carregar {recurso}. Tente recarregar a página."; §Accessibility foco gold + reduced-motion)
    - .planning/phases/07-identidade-visual-e-polimento/07-PATTERNS.md (§Empty-state grammar — usage canônico no dashboard; varredura nas rotas reservas/mei/receitas/categorias/extrato/importar; manter copy)
  </read_first>
  <action>Cria `loading.tsx` por segmento para as rotas pesadas (Next App Router renderiza o `loading.tsx` do segmento enquanto o RSC streama, mantendo a chrome do layout visível): `src/app/(app)/dashboard/loading.tsx` (ChartSkeleton ×2 no grid + TableSkeleton para a lista de aderência), `src/app/(app)/extrato/loading.tsx` (TableSkeleton espelhando o extrato), `src/app/(app)/mei/loading.tsx` (CardSkeleton para a gauge card). Faz a varredura de consistência de polimento nas telas existentes SEM mudar comportamento: confirma/ajusta que os empty-states já usam o primitivo `Empty` com ícone lucide + EmptyTitle + EmptyDescription + uma CTA gold (a CTA já fica gold pelos tokens da Plan 01 — `--primary`); padroniza os blocos de erro inline para `text-destructive` com caminho de recuperação usando a copy genérica da UI-SPEC §Copywriting onde faltar; garante `transition-colors`/`transition-[background,border,box-shadow]` ~150ms em superfícies interativas e foco gold (`--ring`) visível, com `prefers-reduced-motion` reduzindo a opacity-only. Não toca copy herdada das fases 2-6 (mantém verbatim). Roda a suíte + build como gate de não-regressão.</action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5 && npm test 2>&1 | tail -6 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Os três `loading.tsx` existem e referenciam os skeletons (grep `ChartSkeleton`/`TableSkeleton`/`CardSkeleton`); nenhum usa `animate-spin`.
    - `grep -rc "animate-spin" "src/app/(app)"` para os loading.tsx == 0 (skeletons, nunca spinners).
    - Os empty-states usam o primitivo `Empty` (grep nas páginas com empty-state); blocos de erro usam `text-destructive` com a copy de recuperação.
    - `npx tsc --noEmit` limpo; `npm test` ≥559 passed; `npm run build` compila.
  </acceptance_criteria>
  <done>As rotas pesadas streamam com skeletons (chrome visível, nunca spinner), os empty/error states são consistentes nas ~20 rotas, e as micro-interações (150ms, foco gold, reduced-motion) estão padronizadas. Build + suíte verdes — UI-08 completo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client render ↔ estados de UI | skeletons/empty/error são presentation puro; nenhuma query/action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-13 | Information Disclosure | error blocks | mitigate | Erros viram bloco inline com caminho de recuperação, NUNCA stack/erro cru — evita vazar detalhe de implementação ao usuário (UI-SPEC §Polish error). |
| T-07-14 | Tampering | loading.tsx por segmento | accept | `loading.tsx` é boundary de Suspense do App Router (presentation); não muda dados nem o RSC subjacente. Suíte existente é o gate. |
</threat_model>

<verification>
- `npm test` ≥559 passed; `npx tsc --noEmit` limpo; `npm run build` compila.
- `npm test -- table-skeleton` GREEN.
- loading.tsx por segmento usa skeletons (sem `animate-spin`); empty via primitivo `Empty`; error via `text-destructive`.
</verification>

<success_criteria>
- UI-08: empty/loading/error states + micro-interações consistentes em todas as telas; skeletons nunca spinners; foco gold; reduced-motion respeitado.
- Suíte verde + build limpo — polimento sem regressão.
</success_criteria>

<output>
Create `.planning/phases/07-identidade-visual-e-polimento/07-06-SUMMARY.md` when done
</output>
