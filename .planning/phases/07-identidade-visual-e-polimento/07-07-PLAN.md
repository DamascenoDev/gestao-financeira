---
phase: 07-identidade-visual-e-polimento
plan: 07
type: execute
wave: 5
depends_on: ["07-01", "07-02", "07-03", "07-04", "07-05", "07-06"]
files_modified: []
autonomous: false
requirements: [UI-01, UI-02, UI-03, UI-04, UI-05, UI-06, UI-07, UI-08]
must_haves:
  truths:
    - "O phase gate roda verde: suíte completa + tsc + next build + auditoria de segredo do bundle"
    - "Nenhuma cor hardcoded sobreviveu (grep de oklch/hex/195 em src/components limpo, exceto o swatch sancionado do category-badge)"
    - "O humano confirma contraste e flip-integrity em light E dark (income verde, expense neutro, teto vermelho, alvo verde, gold só marca/ação)"
    - "O humano confirma os charts, a navegação mobile (BottomNav + tabelas→cards) e a tela de auth com identidade"
  artifacts: []
  key_links: []
---

<objective>
Fecha a Phase 7 com o gate de fase automatizado + a verificação humana das dimensões que o jsdom não mede: contraste e flip-integrity light↔dark (RESEARCH Assumption A2 — valores OKLCH estimados, não medidos por ferramenta), ausência de cor hardcoded (A3), e a inspeção visual dos charts, da navegação mobile e da tela de auth. Re-roda a auditoria de segredo do bundle (SEC-01 do Phase 6 não pode regredir após o chart client component).

Cobre a verificação travada pela UI-SPEC §Checker Sign-Off (Color/Typography/Spacing/Visuals) + RESEARCH §Validation (contraste é human-verify, não jsdom). Verifica todos os UI-01..UI-08 ponta-a-ponta.

Purpose: a Phase 7 é a mais subjetiva — só o usuário confirma que "parece um banco privado" e que a semântica de cor sobrevive ao flip em ambos os modos.
Output: sign-off humano + phase gate verde; nenhum arquivo de produção alterado.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-identidade-visual-e-polimento/07-UI-SPEC.md
@.planning/phases/07-identidade-visual-e-polimento/07-RESEARCH.md
@.planning/phases/07-identidade-visual-e-polimento/07-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase gate automatizado + grep de cor hardcoded</name>
  <read_first>
    - scripts/check-bundle-secrets.sh (auditoria de segredo do bundle)
    - .planning/phases/07-identidade-visual-e-polimento/07-RESEARCH.md (§Validation Sampling → phase gate; Assumption A3 grep oklch/#/195; §Security secret-bundle audit)
    - src/components/category-badge.tsx (a única exceção sancionada de swatch hardcoded — lines 16-25)
  </read_first>
  <action>Roda o phase gate completo como verificação (sem editar produção): `npm test` (suíte completa deve ficar ≥559 verde), `npx tsc --noEmit` (limpo), `npm run build` (compila com recharts + override + Inter Tight), e `bash scripts/check-bundle-secrets.sh .next/static` (exit 0 — segredo service-role ausente do bundle mesmo com o chart client component). Roda o grep de flip-integrity (RESEARCH A3): nenhum hue teal `195` e nenhuma cor literal `oklch(`/`#` sobrevivendo em `src/components` (a ÚNICA exceção sancionada é o swatch fixo de `category-badge.tsx` — documenta a contagem esperada). Reporta qualquer falha como gap antes do checkpoint humano.</action>
  <verify>
    <automated>npm test 2>&1 | tail -6 && npx tsc --noEmit 2>&1 | tail -3 && npm run build 2>&1 | tail -8 && bash scripts/check-bundle-secrets.sh .next/static; echo "secret-exit=$?" && grep -rn "195" src/components/*.tsx | grep -i oklch; echo "teal-hits=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` ≥559 passed; `npx tsc --noEmit` limpo; `npm run build` compila.
    - `scripts/check-bundle-secrets.sh .next/static` exit 0.
    - Nenhum token com hue `195` (teal antigo) em `src/components`; cores literais `oklch(`/`#` ausentes de componentes salvo o swatch sancionado de `category-badge.tsx`.
  </acceptance_criteria>
  <done>O phase gate está verde (suíte + tsc + build + auditoria de segredo) e o grep de flip-integrity confirma zero cor hardcoded fora da exceção. Pronto para o checkpoint humano.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify — identidade, flip light↔dark, charts, mobile, auth</name>
  <action>Pausa para verificação humana das dimensões visuais que o jsdom não mede (contraste, flip-integrity, charts, mobile, identidade de auth). Nenhuma edição de produção — só inspeção guiada pelos passos em how-to-verify.</action>
  <what-built>
    A Phase 7 inteira: re-skin navy+gold (tokens OKLCH light+dark), dark mode alternável/persistente (ThemeToggle no UserMenu + BottomNav mobile), marca (BrandMark na sidebar + AuthShell de auth), data-viz (ReceitaGastoChart + CategoryDistributionChart no dashboard, gauge MEI + adherence direction-aware), mobile (tabelas→cards nas 4 tabelas densas + BottomNav), e polimento (skeletons, empty/error, micro-interações). O stack local está rodando (API 127.0.0.1:55321 por STATE.md).
  </what-built>
  <how-to-verify>
    Com o app rodando localmente (`npm run dev` se necessário), logado num usuário local de teste com receitas/gastos/metas/NFs lançados:

    1. **Identidade light (UI-01):** abra /dashboard em LIGHT mode. Confirme chrome navy+gold (não grayscale/teal): sidebar navy, item ativo gold com indicador à esquerda, CTAs gold. A marca (tile navy + trio gold) aparece no topo da sidebar.
    2. **Dark mode + flip-integrity (UI-02) — o check crítico:** no UserMenu, alterne Tema → Escuro. Confirme: SEM flash ao trocar, a escolha PERSISTE após refresh (F5). Em DARK mode percorra /dashboard, /extrato, /receitas, /reservas, /mei, /conta e verifique a semântica de cor em AMBOS os modos: receita = VERDE (nunca gold), gasto = neutro (nunca vermelho), teto estourado = VERMELHO, alvo atingido = VERDE, gold aparece SÓ em marca/ação/nav-ativo/foco. Nenhum número verde/gold ilegível sobre navy; foco gold visível ao tabular. Teste também "Sistema".
    3. **Charts (UI-04/05/06):** no /dashboard veja o gráfico de evolução mensal receita vs gasto e a distribuição por categoria do mês — valores no tooltip em R$ pt-BR, cores que flipam com o tema, e um total/legenda rotulado acompanhando cada um. Em /mei a gauge mostra o tier (verde/âmbar/vermelho) corretamente.
    4. **Mobile (UI-07):** estreite a janela para <768px (ou use o device mode). Confirme: a BottomNav aparece com os destinos primários (ativo gold, alvos confortáveis ao toque); /extrato, /importar (revisão), /mei/notas e o ledger de reserva colapsam em UM CARD POR LINHA (Descrição/Categoria em cima, Data/Valor mono embaixo); seleção/bulk no Extrato segue funcionando com a barra flutuante.
    5. **Auth (UI-03):** faça logout. Em /auth/login confirme a AuthShell de duas colunas (painel navy com marca + "Financeira" em gold + value prop "Sua gestão financeira pessoal — privada, precisa e sob seu controle."), e que no mobile o painel vira faixa-cabeçalho. O favicon na aba é navy+gold.
    6. **Polimento (UI-08):** recarregue rotas pesadas e confirme skeletons (não spinners) enquanto carrega, com a chrome visível; um empty-state (ex: dashboard sem metas, ou /mei sem config) usa ícone + título + CTA gold; transições suaves de 150ms em hover/foco.

    Se você usa prefers-reduced-motion no SO, confirme que as animações reduzem para opacity-only.
  </how-to-verify>
  <verify>
    <human-check>Sign-off do usuário em light E dark: identidade navy+gold, flip sem regressão de semântica/contraste, charts, navegação mobile (BottomNav + tabelas→cards) e tela de auth com identidade.</human-check>
  </verify>
  <done>O usuário aprovou a identidade, o flip light↔dark, os charts, o mobile e a auth — ou listou problemas que viram gaps. Phase 7 sign-off concluído.</done>
  <resume-signal>Digite "aprovado" se a identidade, o flip light↔dark, os charts, o mobile e a auth estão corretos — ou descreva os problemas (ex.: "verde ilegível no dark em /mei", "card do ledger sem o valor") para gerar gaps.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verificação ↔ produção | esta plan não altera código de produção; só roda gates e confirma visualmente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-SC | Tampering | npm/recharts no bundle final | mitigate | Re-roda `scripts/check-bundle-secrets.sh .next/static` no phase gate — confirma que o chart client component não trouxe segredo service-role; SEC-01 do Phase 6 permanece verde. |
| T-07-15 | Information Disclosure | flip-integrity / contraste | mitigate | Verificação humana das dimensões que o jsdom não mede (RESEARCH A2/A3); garante que a semântica de dinheiro/status sobrevive em ambos os modos com contraste ≥4.5:1 — a regra load-bearing da UI-SPEC §Flip integrity. |
</threat_model>

<verification>
- Phase gate: `npm test` ≥559 passed, `npx tsc --noEmit` limpo, `npm run build` compila, `check-bundle-secrets.sh` exit 0.
- Grep de flip-integrity: zero cor hardcoded fora do swatch sancionado.
- Sign-off humano: identidade + dark flip + charts + mobile + auth confirmados em light E dark.
</verification>

<success_criteria>
- Todos os UI-01..UI-08 verificados ponta-a-ponta (automatizado onde possível, humano para contraste/visual).
- A semântica de dinheiro/status sobrevive ao flip light↔dark com contraste correto.
- SEC-01 não regrediu — segredo ausente do bundle.
</success_criteria>

<output>
Create `.planning/phases/07-identidade-visual-e-polimento/07-07-SUMMARY.md` when done
</output>
