---
phase: 07-identidade-visual-e-polimento
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - package.json
  - src/components/ui/chart.tsx
  - src/components/receita-gasto-chart.tsx
  - src/components/receita-gasto-chart.test.tsx
  - src/components/category-distribution-chart.tsx
  - src/components/category-distribution-chart.test.tsx
  - src/components/limite-gauge.tsx
  - src/components/adherence-bar.tsx
  - src/app/(app)/dashboard/page.tsx
autonomous: true
requirements: [UI-04, UI-05, UI-06]
must_haves:
  truths:
    - "O dashboard mostra um gráfico de evolução mensal receita vs gasto (últimos ~12 meses)"
    - "O dashboard mostra a distribuição de gastos por categoria do mês selecionado"
    - "Os gráficos auto-temizam (light/dark) lendo CSS-vars e formatam todo dinheiro via formatCents"
    - "Nenhum gráfico é o único portador de um número — sempre há total/legenda rotulada"
    - "A gauge MEI e as adherence bars seguem direction-aware com a nova paleta, sem mudar a lógica de fill/clamp/aria"
    - "recharts instala com o override react-is e a suíte de 559 + next build seguem verdes"
  artifacts:
    - path: "package.json"
      provides: "recharts dependency + overrides react-is 19.2.4"
      contains: "react-is"
    - path: "src/components/ui/chart.tsx"
      provides: "ChartContainer/ChartConfig/ChartTooltip vendored (shadcn add chart)"
      exports: ["ChartContainer", "ChartTooltip", "ChartTooltipContent"]
    - path: "src/components/receita-gasto-chart.tsx"
      provides: "Recharts evolução mensal receita(--income) vs gasto(--consumption)"
      exports: ["ReceitaGastoChart"]
    - path: "src/components/category-distribution-chart.tsx"
      provides: "Recharts distribuição por categoria (ramp --chart-1..5)"
      exports: ["CategoryDistributionChart"]
    - path: "src/app/(app)/dashboard/page.tsx"
      provides: "RSC lê v_income_month (série) + v_category_totals (mês) e passa aos charts — sem query/view nova"
      contains: "ReceitaGastoChart"
  key_links:
    - from: "src/app/(app)/dashboard/page.tsx"
      to: "v_income_month + v_category_totals (views existentes)"
      via: "leituras RLS-scoped existentes alimentando os charts (sem migration)"
      pattern: "v_(income_month|category_totals)"
    - from: "src/components/receita-gasto-chart.tsx"
      to: "src/lib/money.ts formatCents"
      via: "formatter de tooltip/eixo"
      pattern: "formatCents"
    - from: "src/components/ui/chart.tsx"
      to: "globals.css --chart-1..5 / --income / --consumption"
      via: "ChartConfig color → var(--token), auto-tema no .dark"
      pattern: "var\\(--(chart|income|consumption)"
---

<objective>
Adiciona a data-viz do dashboard: instala `recharts` (única dep runtime nova) com o `overrides: { "react-is": "19.2.4" }` obrigatório, vendora o `chart.tsx` do shadcn, e constrói os dois gráficos — `ReceitaGastoChart` (evolução mensal receita vs gasto) e `CategoryDistributionChart` (distribuição por categoria do mês) — lendo APENAS dados de views existentes já buscados pelo RSC (sem migration, sem view nova, sem query nova de servidor). Re-skina a `LimiteGauge` e a `AdherenceBar` para a paleta direction-aware sem tocar a lógica de fill/clamp/aria.

Cobre D-locked: shadcn chart/Recharts é o caminho oficial (CONTEXT §Data-viz; UI-SPEC §Data-Viz — decisão travada), aderência rica + gauge MEI elevado (UI-SPEC §Data-Viz mantém div-bars custom). Implementa UI-04 (evolução receita vs gasto), UI-05 (distribuição por categoria), UI-06 (aderência rica + gauge).

Purpose: tornar o "ver aderência às metas" visualmente rico, dentro da fronteira re-skin (charts consomem dados RLS-scoped já fornecidos pelo RSC).
Output: package.json (recharts + override), chart.tsx vendored, dois charts + testes Wave-0, gauge/adherence re-skinnados, dashboard RSC alimentando os charts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-identidade-visual-e-polimento/07-UI-SPEC.md
@.planning/phases/07-identidade-visual-e-polimento/07-RESEARCH.md
@.planning/phases/07-identidade-visual-e-polimento/07-PATTERNS.md

@src/app/(app)/dashboard/page.tsx
@src/components/limite-gauge.tsx
@src/components/adherence-bar.tsx
@src/lib/money.ts
@src/components/amount-cell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar recharts + override react-is + vendorar chart.tsx (gated build)</name>
  <read_first>
    - package.json (sem chave overrides hoje; react 19.2.4; recharts ausente)
    - .planning/phases/07-identidade-visual-e-polimento/07-RESEARCH.md (§Supporting deps — recharts 3.8.1; §Package Legitimacy Audit — recharts OK, no postinstall; Pitfall 1 react-is mismatch 16.13.1 vs 19.2.4; §Installation)
    - .planning/phases/07-identidade-visual-e-polimento/07-PATTERNS.md (§charts — prereq shadcn add chart + override)
    - scripts/check-bundle-secrets.sh (auditoria de segredo do bundle — SEC-01 não pode regredir)
  </read_first>
  <action>Roda `npx shadcn@latest add chart` que vendora `src/components/ui/chart.tsx` E instala `recharts` (verdict de legitimidade OK por RESEARCH §Package Legitimacy Audit — 52.8M dl/wk, no postinstall; sem checkpoint humano necessário). Adiciona ao `package.json` a chave `"overrides": { "react-is": "19.2.4" }` (casa EXATAMENTE a versão de React instalada — RESEARCH Pitfall 1; a chave overrides NÃO existe hoje, precisa ser criada) e roda `npm install` para re-resolver a árvore. Confirma a versão antes de pinar via `npm view recharts version` (3.8.x esperado) e que `node_modules/react/package.json` casa com o override. Roda `npm run build` como gate — se lançar erro de react-is/isFragment, o override não pegou. Roda a suíte completa para confirmar que recharts não regrediu nada. Re-roda `bash scripts/check-bundle-secrets.sh .next/static` para provar que o componente client de chart NÃO trouxe segredo para o bundle (SEC-01 do Phase 6 segue verde). NÃO escreve componentes de chart ainda (Task 2) — esta task é só a dependência + vendoração + gates.</action>
  <verify>
    <automated>npm view recharts version 2>&1 | tail -1 && npx tsc --noEmit 2>&1 | tail -5 && npm test 2>&1 | tail -6 && npm run build 2>&1 | tail -10 && bash scripts/check-bundle-secrets.sh .next/static; echo "secret-audit-exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contém `"recharts"` em dependencies e `"overrides": { "react-is": "19.2.4" }`.
    - `src/components/ui/chart.tsx` existe e exporta `ChartContainer` + `ChartTooltip` + `ChartTooltipContent`.
    - `node -e "process.exit(require('react-is/package.json').version.startsWith('19')?0:1)"` retorna 0 (override resolveu).
    - `npm run build` compila sem erro de react-is/isFragment.
    - `npm test` ≥559 passed; `scripts/check-bundle-secrets.sh .next/static` exit 0 (segredo ausente do bundle).
  </acceptance_criteria>
  <done>recharts está instalado com o override react-is casando React 19.2.4, chart.tsx está vendored, o build compila e a auditoria de segredo do bundle segue verde. Pronto para os componentes de chart.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ReceitaGastoChart + CategoryDistributionChart (Recharts, token-aware, formatCents)</name>
  <read_first>
    - src/components/ui/chart.tsx (vendored na Task 1 — ChartContainer/ChartConfig/ChartTooltipContent)
    - src/lib/money.ts (formatCents — reusar em todo tooltip/eixo)
    - src/components/amount-cell.tsx (convenção de uso de formatCents)
    - .planning/phases/07-identidade-visual-e-polimento/07-RESEARCH.md (§Pattern 1 ChartContainer lendo CSS-vars; §Code Examples tooltip formatCents; Pitfall 6 chart não é o único portador)
    - .planning/phases/07-identidade-visual-e-polimento/07-UI-SPEC.md (§Data-Viz tabela de charts — receita=--income, gasto=--consumption; distribuição usa --chart-1..5 NÃO os money tokens; §Copywriting empty-states dos charts; §Accessibility aria-label + total fallback)
    - .planning/phases/07-identidade-visual-e-polimento/07-PATTERNS.md (§charts — sem analog no repo; copiar RESEARCH Pattern 1)
  </read_first>
  <behavior>
    - ReceitaGastoChart: dado uma série `{ mes, receita, gasto }[]`, renderiza barras/linhas com `fill="var(--color-receita)"`/`var(--color-gasto)` (config mapeando receita→var(--income), gasto→var(--consumption)); tooltip formata centavos via formatCents; com série vazia mostra o empty-state "Sem dados para o gráfico" (UI-SPEC copy); tem aria-label "Evolução de receita e gasto por mês".
    - CategoryDistributionChart: dado `{ categoria, cents }[]`, renderiza donut/bar com cores do ramp `--chart-1..5` (repetindo), centro/legenda com total via formatCents; vazio → "Nenhum gasto neste mês"; aria-label "Distribuição de gastos por categoria em {mês}".
    - Ambos: nenhum número é exclusivo do gráfico — há total/legenda rotulada acompanhando (Pitfall 6).
  </behavior>
  <action>Cria `src/components/receita-gasto-chart.test.tsx` e `src/components/category-distribution-chart.test.tsx` PRIMEIRO (Wave-0 RED — mocke recharts/ResizeObserver se jsdom exigir, no padrão dos testes de componente do repo): assertam (1) com dados, os rótulos de série/categoria e os valores formatados via formatCents aparecem; (2) com array vazio, o empty-state pt-BR exato aparece; (3) o `aria-label` correto está presente. Depois cria `src/components/receita-gasto-chart.tsx` (`"use client"`) e `src/components/category-distribution-chart.tsx` (`"use client"`) copiando RESEARCH §Pattern 1: `ChartContainer` com um `ChartConfig satisfies` mapeando cada série/categoria a `{ label, color: "var(--token)" }` — receita=`var(--income)`, gasto=`var(--consumption)` no primeiro; ramp `var(--chart-1)`..`var(--chart-5)` no segundo (NÃO os money tokens, para uma fatia nunca parecer "income"); `ChartTooltip content={<ChartTooltipContent formatter={(v) => formatCents(Number(v))} />}`; empty-state com a copy da UI-SPEC quando a série é vazia; `aria-label` da UI-SPEC §Copywriting; um total/legenda rotulado acompanhando o gráfico (Pitfall 6). Props são `{ data }` puros — os componentes NÃO buscam dados (RSC passa na Task 3). Faz os dois testes virarem GREEN.</action>
  <verify>
    <automated>npm test -- receita-gasto-chart category-distribution-chart 2>&1 | tail -12 && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Os dois componentes existem e exportam `ReceitaGastoChart` / `CategoryDistributionChart`, ambos `"use client"`.
    - `grep -c "formatCents" src/components/receita-gasto-chart.tsx src/components/category-distribution-chart.tsx` ≥ 2 (um em cada).
    - receita-gasto usa `var(--income)` e `var(--consumption)`; distribution usa `var(--chart-1)` (ramp, não money tokens) — checável por grep.
    - Ambos contêm `aria-label` e a copy de empty-state pt-BR da UI-SPEC ("Sem dados para o gráfico" / "Nenhum gasto neste mês").
    - `npm test -- receita-gasto-chart category-distribution-chart` GREEN; `npx tsc --noEmit` limpo.
  </acceptance_criteria>
  <done>Os dois charts renderizam dados + empty-state, formatam dinheiro via formatCents, leem CSS-vars (auto-tema) e têm aria-label — testes Wave-0 GREEN. Ainda não montados no dashboard (Task 3).</done>
</task>

<task type="auto">
  <name>Task 3: Montar os charts no dashboard (dados de views existentes) + re-skin gauge/adherence</name>
  <read_first>
    - src/app/(app)/dashboard/page.tsx (RSC atual — Promise.all de v_adherence_month/_ytd/categories/v_income_month; onde adicionar leituras e montar os charts acima da lista de aderência)
    - src/components/receita-gasto-chart.tsx + category-distribution-chart.tsx (Task 2 — shapes de props)
    - src/components/limite-gauge.tsx (lógica de fill/clamp/aria — NÃO tocar; só tokens via globals.css + tratamento radial opcional)
    - src/components/adherence-bar.tsx (idem — direction-aware fill vem de lib/adherence.ts)
    - .planning/phases/07-identidade-visual-e-polimento/07-UI-SPEC.md (§Data-Viz; §Screen Contracts dashboard: charts acima da lista, 2-col lg / stacked <md; §Responsive charts <md; §Accessibility)
    - .planning/phases/07-identidade-visual-e-polimento/07-PATTERNS.md (§limite-gauge/adherence-bar — token re-skin only, NÃO tocar fill-token logic/clamp/aria)
    - src/lib/month.ts (yearBounds/monthLabel/eachMonth helpers para a janela ~12 meses SP-pinned)
  </read_first>
  <action>Edita `src/app/(app)/dashboard/page.tsx` (RSC) para alimentar os charts SEM criar query/view/migration nova — usa SÓ views existentes sob o client RLS-scoped: (1) série receita-vs-gasto dos últimos ~12 meses civis (SP-pinned via month.ts) lendo `v_income_month` (receita por mês) e `v_category_totals` (gasto por mês — somar as categorias de consumo do mês), montando o array `{ mes, receita, gasto }[]`; (2) distribuição do mês selecionado lendo `v_category_totals` filtrado ao `?mes` corrente, montando `{ categoria, cents }[]`. Monta `<ReceitaGastoChart data={...} />` e `<CategoryDistributionChart data={...} mes={...} />` acima da lista de aderência num grid responsivo (2-col `lg`, stacked `<md` por UI-SPEC §Screen Contracts), cada um acompanhado do seu total/legenda rotulado. Re-skina `LimiteGauge` e `AdherenceBar` APENAS no nível de token/treatment visual: como `--primary`/`--income`/`--consumption`/`--destructive` e os tokens de `meiStatusTokens`/`adherenceTokens` já viraram navy+gold direction-aware pela Plan 01, os fills já seguem a nova paleta — adiciona no máximo o tratamento radial/arc sutil da gauge mencionado na UI-SPEC, SEM tocar a lógica de fill-token, o clamp math, nem o `role="progressbar"`/`aria-valuetext` (os testes de comportamento de gauge/adherence devem seguir verdes). Mantém os estados empty/erro existentes do dashboard. Confirma: nenhuma chamada a `.rpc(`/novo `.from('v_` que não exista; nenhuma migration.</action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5 && npm test 2>&1 | tail -6 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ReceitaGastoChart" "src/app/(app)/dashboard/page.tsx"` ≥ 1 e `grep -c "CategoryDistributionChart"` ≥ 1.
    - O dashboard lê apenas views existentes — `grep -E "v_income_month|v_category_totals" "src/app/(app)/dashboard/page.tsx"` casa; nenhuma string de view inexistente nem `.rpc(` nova.
    - Sem nova migration: `git status --short supabase/migrations/` não lista arquivo novo.
    - `limite-gauge.tsx` e `adherence-bar.tsx` mantêm `role="progressbar"` e `aria-valuetext` (grep) e a lógica de clamp inalterada (sem mudança nas funções de width/ratio).
    - `npx tsc --noEmit` limpo; `npm test` ≥559 passed (gauge/adherence behavior tests verdes); `npm run build` compila.
  </acceptance_criteria>
  <done>O dashboard exibe os dois gráficos alimentados por views existentes (sem query/view/migration nova), gauge e adherence seguem a nova paleta direction-aware sem mudança de lógica, e build + suíte seguem verdes. UI-04/05/06 entregues.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC (RLS-scoped) ↔ chart client component | dados de view já escopados por RLS descem como props; o chart é client puro, sem fetch próprio |
| npm install ↔ client bundle | recharts é a única dep nova; pode alargar o bundle ou (em tese) carregar segredo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Information Disclosure | chart consome dado cross-user | mitigate | Charts leem SÓ dados RSC-fetched de views `security_invoker` RLS-scoped; nenhuma query/view nova (a regra "no new query" é o controle). Acceptance criteria proíbem `.rpc(`/view inexistente nova. |
| T-07-07 | Information Disclosure | recharts amplia bundle / segredo no client | mitigate | Re-rodar `scripts/check-bundle-secrets.sh .next/static` após o build (Task 1) — segredo service-role permanece ausente; recharts é viz client-only sem import server. |
| T-07-08 | Tampering | supply chain (recharts) | mitigate | recharts passou o seam de legitimidade (verdict OK, 52.8M dl/wk, no postinstall — RESEARCH §Package Legitimacy Audit). Override react-is é só re-resolução de versão, sem install novo. |
| T-07-SC | Tampering | npm install (recharts) | mitigate | Único pacote novo, [OK] no audit; sem `[ASSUMED]`/`[SUS]` → sem checkpoint humano bloqueante exigido; gate via next build + suíte completa. |
</threat_model>

<verification>
- `npm run build` compila com recharts + override (sem erro react-is); `scripts/check-bundle-secrets.sh .next/static` exit 0.
- `npm test` ≥559 passed (charts Wave-0 GREEN + gauge/adherence behavior verdes).
- Dashboard lê só views existentes; sem migration nova; sem `.rpc(` nova.
- Charts formatam dinheiro via formatCents, têm aria-label e empty-state pt-BR, auto-temizam por CSS-var.
</verification>

<success_criteria>
- UI-04: gráfico de evolução mensal receita vs gasto no dashboard.
- UI-05: gráfico de distribuição de gastos por categoria do mês.
- UI-06: gauge MEI + adherence bars direction-aware na nova paleta, lógica intacta.
- Fronteira re-skin respeitada: sem query/view/migration nova; bundle sem segredo.
</success_criteria>

<output>
Create `.planning/phases/07-identidade-visual-e-polimento/07-03-SUMMARY.md` when done
</output>
