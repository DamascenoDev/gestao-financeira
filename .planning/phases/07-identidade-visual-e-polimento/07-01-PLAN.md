---
phase: 07-identidade-visual-e-polimento
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
  - src/components/theme-provider.tsx
  - src/components/theme-toggle.tsx
  - src/components/theme-toggle.test.tsx
autonomous: true
requirements: [UI-01, UI-02]
must_haves:
  truths:
    - "Toda a chrome do app renderiza navy+gold (não mais grayscale+teal) em light mode"
    - "O usuário alterna entre Claro / Escuro / Sistema e a escolha persiste entre refreshes"
    - "Não há flash de tema (FOUC) nem warning de hydration ao carregar"
    - "As 559 verdes da suíte continuam verdes (mudança é color-only) e o next build compila"
    - "Headings/marca usam --font-heading real (não mais o no-op --font-sans auto-referente)"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Substrato OKLCH navy+gold light+dark + @theme inline mirrors + font-bug fix"
      contains: "oklch(0.76 0.13 88)"
    - path: "src/app/layout.tsx"
      provides: "ThemeProvider no root + suppressHydrationWarning + --font-inter-tight registrada"
      contains: "ThemeProvider"
    - path: "src/components/theme-provider.tsx"
      provides: "next-themes wrapper client component"
      exports: ["ThemeProvider"]
    - path: "src/components/theme-toggle.tsx"
      provides: "Controle 3-vias Claro/Escuro/Sistema mount-guarded"
      exports: ["ThemeToggle"]
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/theme-provider.tsx"
      via: "wraps {children} + <Toaster/>"
      pattern: "<ThemeProvider"
    - from: "src/components/ui/sonner.tsx"
      to: "next-themes useTheme()"
      via: "toasts seguem o tema assim que o provider existe"
      pattern: "useTheme"
    - from: "src/components/theme-toggle.tsx"
      to: "next-themes useTheme/setTheme"
      via: "setTheme('light'|'dark'|'system')"
      pattern: "setTheme"
---

<objective>
Reescreve o substrato de tokens de `globals.css` para a identidade private-banking navy+gold (OKLCH, light + dark, semântica de dinheiro/status preservada e re-tunada), conserta os dois bugs latentes (a auto-referência `--font-sans` e o ThemeProvider não-cabeado), e cabeia o `next-themes` no root layout — entregando dark mode alternável e persistente ponta-a-ponta. Esta é a fatia-substrato BLOQUEANTE da Phase 7: todo screen e componente depende destes tokens e do provider.

Cobre D-locked: paleta navy+gold (CONTEXT §Direção estética), dark mode next-themes (CONTEXT §Identidade), semântica de cor preservada (CONTEXT §Preservar). Implementa UI-01 (tokens globais) + UI-02 (dark mode completo, alternável, persistente).

Purpose: o flip de tema é puramente swap de CSS-var; nenhum componente lê cor hardcoded, então re-tunar tokens + cabear o provider re-temiza ~20 rotas de uma vez sem tocar lógica.
Output: globals.css reescrito (navy+gold light+dark + @theme inline mirrors), layout.tsx com ThemeProvider + font de heading, theme-provider.tsx, theme-toggle.tsx, e o teste Wave-0 do toggle.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-identidade-visual-e-polimento/07-UI-SPEC.md
@.planning/phases/07-identidade-visual-e-polimento/07-RESEARCH.md
@.planning/phases/07-identidade-visual-e-polimento/07-PATTERNS.md

# Source-of-truth files this plan rewrites/extends
@src/app/globals.css
@src/app/layout.tsx
@src/components/ui/sonner.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 — teste do ThemeToggle (mount-guard + 3 vias)</name>
  <read_first>
    - .planning/phases/07-identidade-visual-e-polimento/07-RESEARCH.md (§Validation Architecture → Wave 0 Gaps; §Pattern 2 mount-guard)
    - .planning/phases/07-identidade-visual-e-polimento/07-UI-SPEC.md (§Dark Mode Strategy → toggle 3-vias; §Copywriting → "Tema"/"Claro"/"Escuro"/"Sistema")
    - src/components/user-menu.tsx (analog do dropdown + lucide + aria-label)
    - src/components/ui/sonner.tsx (único consumidor next-themes hoje — convenção de import)
  </read_first>
  <action>Cria `src/components/theme-toggle.test.tsx` PRIMEIRO (Nyquist Wave-0, antes de existir o componente de produção — collection não pode crashar, então mocke `next-themes` com `vi.mock('next-themes', ...)` retornando `useTheme` controlável). Testa o contrato do `ThemeToggle`: (1) antes de `mounted` retorna null (sem render que cause hydration mismatch — assert que nada com os labels aparece no primeiro paint simulado) e após o effect de mount renderiza os três rótulos pt-BR exatos "Claro", "Escuro" e "Sistema" (UI-SPEC §Copywriting); (2) clicar/selecionar cada opção chama `setTheme` com `'light'`, `'dark'`, `'system'` respectivamente; (3) cada controle tem nome acessível (aria-label ou texto). Use `@testing-library/react` + `userEvent` como nos testes de componente existentes. O teste FALHA até o Task 2 criar o componente — é o RED do Wave-0.</action>
  <verify>
    <automated>npm test -- theme-toggle 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/theme-toggle.test.tsx` existe e referencia `setTheme` com os três valores `light`/`dark`/`system`.
    - O arquivo de teste contém os três literais pt-BR `Claro`, `Escuro`, `Sistema`.
    - `grep -c "setTheme" src/components/theme-toggle.test.tsx` retorna ≥ 3.
    - O teste roda (collection não crasha) mesmo antes do componente existir — `next-themes` está mockado.
  </acceptance_criteria>
  <done>O teste do ThemeToggle existe, é coletável, e descreve mount-guard + 3-vias + setTheme; está RED (componente ainda não existe) — vira GREEN no Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reescrever tokens navy+gold + cabear ThemeProvider + conserto do font-bug</name>
  <read_first>
    - src/app/globals.css (substrato atual — :root linhas 57-97, .dark 99-138, @theme inline 7-55, font-bug 10-12, print 160-176)
    - src/app/layout.tsx (registro de fontes 6-14, <html> 27-30, <Toaster/> 33)
    - .planning/phases/07-identidade-visual-e-polimento/07-UI-SPEC.md (§Color System — TODAS as tabelas OKLCH light+dark: chrome, gold, semantic money/status, chart ramp, sidebar; §Brand; §Typography)
    - .planning/phases/07-identidade-visual-e-polimento/07-PATTERNS.md (§globals.css — preservar linhas 1-5/140-176; conserto font 46-56; regra @theme inline mirror; §layout.tsx — adicionar Inter_Tight + wrap ThemeProvider)
    - .planning/phases/07-identidade-visual-e-polimento/07-RESEARCH.md (§Pattern 2 next-themes; §Pattern 3 token rewrite; Pitfalls 2/3/4)
    - src/components/ui/sonner.tsx (analog do theme-provider.tsx — convenção "use client" + import next-themes)
  </read_first>
  <behavior>
    - ThemeProvider renderiza os filhos (passthrough de props para NextThemesProvider) — teste de smoke opcional; o comportamento load-bearing é o do ThemeToggle (Task 1) que vira GREEN.
    - ThemeToggle: mount-guard (null antes do mount) → 3 rótulos pt-BR → setTheme('light'|'dark'|'system') no clique.
    - Token rewrite é color-only: NENHUM teste de comportamento das 559 muda de resultado.
  </behavior>
  <action>Reescreve `src/app/globals.css` substituindo os blocos `:root` (linhas ~57-97) e `.dark` (~99-138) pelos valores OKLCH EXATOS da UI-SPEC §Color System — chrome navy (hue ≈255-258), `--primary` gold light `oklch(0.76 0.13 88)` / dark `oklch(0.82 0.13 88)` com `--primary-foreground` navy-ink, semânticos re-tunados (`--income` 152, `--expense` = foreground neutro nunca vermelho, `--allocation` 250, `--consumption` 70, `--destructive` 27), ramp de charts `--chart-1..5` (gold/navy/allocation/consumption/200 por UI-SPEC, NÃO mais grayscale), e todos os sidebar tokens navy (sidebar dark mais fundo que o canvas). PRESERVA os NOMES de todos os tokens (cada `var(--token)` consumidor continua funcionando — re-skin, não rename) e preserva verbatim linhas 1-5 (`@import` + `@custom-variant dark`) e 140-176 (`@layer base` + print stylesheet que força branco). Conserta o font-bug (PATTERNS 46-56): em `@theme inline` troca `--font-sans: var(--font-sans)` por `--font-sans: var(--font-geist-sans)` e `--font-heading: var(--font-sans)` por `--font-heading: var(--font-inter-tight)`. Espelha em `@theme inline` qualquer token novo/renomeado necessário (`--color-destructive-foreground` se adicionado; confirma que `--color-chart-1..5` e os semânticos já mapeados continuam) para as utilities Tailwind (`bg-chart-1`, `text-allocation`) resolverem. Cria `src/components/theme-provider.tsx` (analog sonner.tsx: `"use client"` + wrapper de `NextThemesProvider` repassando props — exporta `ThemeProvider`). Edita `src/app/layout.tsx`: importa `Inter_Tight` de `next/font/google` com `variable: "--font-inter-tight"`, `weight: ["600"]`, `subsets: ["latin"]`; adiciona `interTight.variable` ao className do `<html>` e adiciona `suppressHydrationWarning` ao `<html>`; envolve `{children}` + `<Toaster/>` no `<ThemeProvider attribute="class" defaultTheme="system" enableSystem disableTransitionOnChange>` (per UI-SPEC §Dark Mode + RESEARCH Pattern 2). Cria `src/components/theme-toggle.tsx` (`"use client"`, mount-guard com useState+useEffect, `useTheme` de next-themes, ícones lucide `sun`/`moon`/`monitor`, dropdown na gramática de composição base-ui `render={}` do user-menu.tsx, rótulos pt-BR "Claro"/"Escuro"/"Sistema") fazendo o teste do Task 1 virar GREEN. NÃO toca print stylesheet, NÃO renomeia token algum, NÃO hardcoda hex/oklch em componente.</action>
  <verify>
    <automated>npm test -- theme-toggle 2>&1 | tail -10 && npx tsc --noEmit 2>&1 | tail -5 && npm test 2>&1 | tail -8 && npm run build 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "oklch(0.76 0.13 88)" src/app/globals.css` ≥ 1 (gold light `--primary`).
    - `grep -v '^[[:space:]]*/\*' src/app/globals.css | grep -c "var(--font-geist-sans)"` ≥ 1 e `grep -c "var(--font-sans)" src/app/globals.css` para `--font-sans:` NÃO é mais auto-referente (a linha `--font-sans: var(--font-sans)` não existe).
    - `grep -c "195" src/app/globals.css` == 0 — o hue teal antigo foi removido de todos os tokens.
    - `src/app/layout.tsx` contém `ThemeProvider`, `suppressHydrationWarning` e `font-inter-tight`.
    - `src/components/theme-provider.tsx` exporta `ThemeProvider`; `src/components/theme-toggle.tsx` exporta `ThemeToggle`.
    - `npm test -- theme-toggle` GREEN; `npx tsc --noEmit` limpo; `npm test` mantém ≥559 passed (color-only não quebra comportamento); `npm run build` compila.
  </acceptance_criteria>
  <done>globals.css é navy+gold em light+dark com nomes de token preservados e font-bug consertado; ThemeProvider está cabeado no root com suppressHydrationWarning; ThemeToggle 3-vias funciona (teste GREEN); a suíte inteira segue verde e o build compila. Dark mode é alternável e persistente ponta-a-ponta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser localStorage ↔ app | next-themes lê/grava a preferência de tema no localStorage do cliente (sem PII, sem dado financeiro) |
| client bundle ↔ servidor | nenhuma nova importação server-side; o substrato é CSS puro + provider client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | theme localStorage | accept | Tema é preferência de UI, sem PII/valor financeiro; next-themes (dep confiável) gerencia. Sem dado sensível atravessa a fronteira. |
| T-07-02 | Tampering | globals.css token rewrite | mitigate | Mudança color-only; a suíte de 559 testes de comportamento (que NÃO assertam cor) é o gate de regressão — qualquer quebra de seletor é conserto de teste, nunca de lógica de segurança (UI-SPEC: "the test wins"). |
| T-07-03 | Spoofing/Tampering | suppressHydrationWarning no <html> | accept | Aplicado SÓ ao `<html>` por exigência do next-themes (muta a classe server↔client); não suprime warnings de conteúdo nem oculta XSS — escopo mínimo documentado pelo next-themes. |
</threat_model>

<verification>
- `npm test` → ≥559 passed (baseline mantido; color-only não muda comportamento).
- `npx tsc --noEmit` → limpo.
- `npm run build` → compila com os novos tokens + a fonte Inter Tight adicionada.
- `npm test -- theme-toggle` → GREEN (mount-guard + 3-vias + setTheme).
- Grep gate: nenhum hue teal `195` sobrevive em `globals.css`; nenhuma auto-referência `--font-sans: var(--font-sans)`.
</verification>

<success_criteria>
- Chrome navy+gold aplicada em light mode; dark mode alternável (Claro/Escuro/Sistema) e persistente via next-themes.
- Zero FOUC / zero warning de hydration (suppressHydrationWarning + mount-guard + disableTransitionOnChange).
- Semântica de dinheiro/status preservada (income verde, expense neutro, teto vermelho, gold reservado para marca/ação) com NOMES de token intactos.
- Suíte inteira verde + build limpo — re-skin não quebrou comportamento.
</success_criteria>

<output>
Create `.planning/phases/07-identidade-visual-e-polimento/07-01-SUMMARY.md` when done
</output>
