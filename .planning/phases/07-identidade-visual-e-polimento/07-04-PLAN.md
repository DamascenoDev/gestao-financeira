---
phase: 07-identidade-visual-e-polimento
plan: 04
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/components/extrato-table.tsx
  - src/components/import-review-table.tsx
  - src/components/nf-table.tsx
  - src/components/reserva-ledger-table.tsx
autonomous: true
requirements: [UI-07]
must_haves:
  truths:
    - "No mobile (<md) o Extrato, a revisão de importação, as NFs e o ledger de reserva renderizam um card por linha em vez de tabela densa"
    - "Cada card mostra Descrição/Tomador + badge de categoria na linha de cima e Data + Valor mono na de baixo, reusando os mesmos componentes de célula"
    - "No desktop (≥md) as tabelas densas seguem idênticas — comportamento, seleção e props congelados"
    - "A seleção/SelectionActionBar segue funcionando no mobile (tap-select + barra flutuante)"
  artifacts:
    - path: "src/components/extrato-table.tsx"
      provides: "Branch de card <md reusando AmountCell/CategoryBadge/ddMM; tabela ≥md intacta"
      contains: "md:hidden"
    - path: "src/components/import-review-table.tsx"
      provides: "Branch de card <md (memory-miss accent preservado)"
      contains: "md:hidden"
    - path: "src/components/nf-table.tsx"
      provides: "Branch de card <md (Tomador + AtividadeBadge + AmountCell)"
      contains: "md:hidden"
    - path: "src/components/reserva-ledger-table.tsx"
      provides: "Branch de card <md (entradas/saídas)"
      contains: "md:hidden"
  key_links:
    - from: "src/components/extrato-table.tsx"
      to: "AmountCell / CategoryBadge / ddMM"
      via: "card mobile reusa as MESMAS células (só o wrapper muda)"
      pattern: "AmountCell|CategoryBadge"
    - from: "src/components/extrato-table.tsx"
      to: "src/components/selection-action-bar.tsx"
      via: "seleção mobile mantém a SelectionActionBar flutuante (comportamento congelado)"
      pattern: "SelectionActionBar"
---

<objective>
Adiciona o colapso tabela→card mobile (`<md`) às quatro tabelas densas — `extrato-table`, `import-review-table`, `nf-table`, `reserva-ledger-table` — reusando os mesmos componentes de célula (`AmountCell`, `CategoryBadge`/`AtividadeBadge`, `ddMM`, `TruncCell`) num layout de card por linha. Trabalho NET-NEW (RESEARCH Pitfall 5: não existe pattern de card-collapse hoje; todas as quatro são `<table>` desktop-only). Props e comportamento de cada tabela ficam congelados — só o wrapper de layout `<md` é adicionado.

Cobre D-locked: tabelas densas viram cards no mobile (CONTEXT §Polimento+mobile; UI-SPEC §Responsive §Screen Contracts). Implementa a parte tabelas→cards de UI-07 (a parte nav adapta foi entregue na Plan 02).

Purpose: tornar as telas densas usáveis e bonitas no celular sem mudar dados, seleção ou queries.
Output: as quatro tabelas com um branch de card responsivo, células reusadas, comportamento idêntico.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-identidade-visual-e-polimento/07-UI-SPEC.md
@.planning/phases/07-identidade-visual-e-polimento/07-PATTERNS.md

@src/components/extrato-table.tsx
@src/components/nf-table.tsx
@src/components/import-review-table.tsx
@src/components/reserva-ledger-table.tsx
@src/components/amount-cell.tsx
@src/components/category-badge.tsx
@src/hooks/use-mobile.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Colapso card &lt;md no Extrato + Import review (com seleção)</name>
  <read_first>
    - src/components/extrato-table.tsx (colunas 289-342: occurred_on→ddMM, description→TruncCell, category_id→InlineCategoryCell, amount_cents→AmountCell; rowSelection/SelectionActionBar lines ~359-370; footer de totais 423-440)
    - src/components/import-review-table.tsx (irmã da ExtratoTable — getRowId por dedupe_key, InlineCategoryCell, memory-miss accent âmbar, OriginBadge, SelectionActionBar)
    - src/components/amount-cell.tsx + category-badge.tsx (células a reusar verbatim)
    - src/hooks/use-mobile.ts (useIsMobile, 768px)
    - .planning/phases/07-identidade-visual-e-polimento/07-UI-SPEC.md (§Responsive dense tables <md → um card por linha: Descrição/Categoria topo, Data/Valor mono baixo; SelectionActionBar flutua no rodapé mobile; §Screen Contracts)
    - .planning/phases/07-identidade-visual-e-polimento/07-PATTERNS.md (§Dense tables → mobile card collapse — NET-NEW, props congelados, reusar células; switch via classes responsivas `hidden md:table` / `md:hidden`)
  </read_first>
  <action>No `src/components/extrato-table.tsx`: adiciona um branch de renderização mobile mantendo a `<Table>` existente como `hidden md:table` (desktop intacto) e, abaixo, uma lista `md:hidden` de cards — um card por linha do `table.getRowModel().rows`, reusando as MESMAS células: topo do card = `TruncCell`/descrição + a `InlineCategoryCell` (badge de categoria editável inline — comportamento congelado), linha de baixo = `ddMM(occurred_on)` + `<AmountCell cents kind="expense" signed={false} />` mono à direita; cada card carrega o `Checkbox` de seleção (mesmo `row.getIsSelected()/toggleSelected`) para que a `SelectionActionBar` flutuante já cabeada continue operando no mobile (tap-select). O footer de totais por categoria também colapsa numa lista compacta `md:hidden`. NÃO muda colunas, getRowId, o modelo de seleção, nem as actions (bulkReclassify/updateTransaction). Repete o MESMO padrão em `src/components/import-review-table.tsx` preservando o accent âmbar de memory-miss (`border-l-consumption`) e o OriginBadge no card, e o dialog "Qual reserva?" inline intacto. Switch via classes Tailwind responsivas (preferir sobre `useIsMobile` para SSR estável; usar `useIsMobile` só se um comportamento JS exigir).</action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5 && npm test -- extrato-table import-review-table 2>&1 | tail -10 && npm run build 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `extrato-table.tsx` contém `md:hidden` (branch card) e `hidden md:table` (ou `md:table`) para a tabela desktop; reusa `AmountCell` e `InlineCategoryCell`/`CategoryBadge` no card (grep).
    - `import-review-table.tsx` contém `md:hidden` e preserva o accent `border-l-consumption` + OriginBadge no card.
    - getRowId, rowSelection e SelectionActionBar permanecem (grep `getRowId`, `rowSelection`, `SelectionActionBar`) — comportamento de seleção congelado.
    - `npm test -- extrato-table import-review-table` GREEN (testes de comportamento existentes intactos); `npx tsc --noEmit` limpo; `npm run build` compila.
  </acceptance_criteria>
  <done>Extrato e revisão de importação renderizam cards <md (reusando as células), tabelas idênticas ≥md, e a seleção/SelectionActionBar segue funcionando no mobile. Build + testes verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Colapso card &lt;md no NF table + Reserva ledger table</name>
  <read_first>
    - src/components/nf-table.tsx (body 200-223: Tomador/descrição + AtividadeBadge + AmountCell income + total do ano + edit/delete)
    - src/components/reserva-ledger-table.tsx (entradas/saídas in/out + datas + valores)
    - src/components/extrato-table.tsx (o branch de card do Task 1 como referência de padrão)
    - src/components/amount-cell.tsx + category-badge.tsx (atividade-badge segue a mesma convenção)
    - .planning/phases/07-identidade-visual-e-polimento/07-UI-SPEC.md (§Responsive dense tables <md; §Screen Contracts NF/Ledger)
    - .planning/phases/07-identidade-visual-e-polimento/07-PATTERNS.md (§Dense tables — nf/ledger reusam o mesmo padrão de extrato; props congelados)
  </read_first>
  <action>No `src/components/nf-table.tsx`: aplica o MESMO padrão do Task 1 — `<Table>` vira `hidden md:table`, adiciona uma lista `md:hidden` de cards: topo = Tomador/descrição + `AtividadeBadge`, baixo = data `ddMM` + `<AmountCell>` (income, mono) à direita; mantém os controles de edit/delete por linha (via `deleteMeiInvoice`/dialog) acessíveis no card; o total do ano colapsa num rodapé compacto. No `src/components/reserva-ledger-table.tsx`: idem — card por entrada do ledger com o tipo (entrada/saída in/out) + data + valor mono (sinal por kind, nunca por valor). NÃO muda props, ordenação, nem actions. Switch via classes responsivas. Reusa as células existentes (`AmountCell`, badges, helper de data) verbatim.</action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5 && npm test -- nf-table reserva-ledger 2>&1 | tail -10 && npm run build 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `nf-table.tsx` e `reserva-ledger-table.tsx` contêm `md:hidden` (branch card) + `md:table`/`hidden md:table` (tabela desktop).
    - Os cards reusam `AmountCell` + as badges existentes (grep `AmountCell`, `AtividadeBadge`/badge).
    - As actions por linha (delete/edit MEI; ledger) e as props permanecem inalteradas (grep `deleteMeiInvoice` em nf-table; nenhuma mudança de assinatura de prop).
    - `npm test -- nf-table reserva-ledger` GREEN; `npx tsc --noEmit` limpo; `npm run build` compila.
  </acceptance_criteria>
  <done>NF table e reserva ledger renderizam cards <md reusando as células, tabelas idênticas ≥md, actions por linha intactas. Build + testes verdes — UI-07 (tabelas→cards) completo nas quatro tabelas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client render ↔ dados de linha | as quatro tabelas re-renderizam dados de linha já fornecidos (RLS-scoped); nenhuma query/action nova |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | Tampering | seleção/bulk no card mobile | mitigate | Card reusa o MESMO `rowSelection`/`getRowId`/SelectionActionBar e as MESMAS actions (bulkReclassify/updateTransaction/delete) — sem novo caminho de mutação; os testes de comportamento de cada tabela seguem verdes como gate. |
| T-07-10 | Information Disclosure | render de linha mobile | accept | Apenas layout responsivo dos mesmos dados de linha já exibidos no desktop; nenhum campo novo é exposto. |
</threat_model>

<verification>
- `npx tsc --noEmit` limpo; `npm run build` compila.
- `npm test -- extrato-table import-review-table nf-table reserva-ledger` GREEN (comportamento congelado).
- Cada tabela tem branch `md:hidden` (card) + tabela `md:table` (desktop), reusando células e mantendo seleção/actions.
</verification>

<success_criteria>
- UI-07 (tabelas→cards): as quatro tabelas densas colapsam para cards no mobile reusando as células existentes.
- Desktop idêntico; seleção/bulk e actions por linha congeladas; suíte verde.
</success_criteria>

<output>
Create `.planning/phases/07-identidade-visual-e-polimento/07-04-SUMMARY.md` when done
</output>
