---
phase: 07-identidade-visual-e-polimento
plan: 05
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - src/components/auth-shell.tsx
  - src/app/(auth)/layout.tsx
  - src/app/icon.svg
autonomous: true
requirements: [UI-03]
must_haves:
  truths:
    - "As telas de login/signup mostram um layout de duas colunas com painel navy de marca (BrandMark + wordmark + value prop), não mais o form cru"
    - "No mobile o painel navy vira uma faixa-cabeçalho compacta sobre o form"
    - "O formulário de auth existente fica inalterado (comportamento de login/signup congelado)"
    - "O favicon é o tile navy + trio de barras douradas (auto-temizado, SVG único)"
  artifacts:
    - path: "src/components/auth-shell.tsx"
      provides: "Layout de identidade duas colunas envolvendo o auth-form"
      exports: ["AuthShell"]
    - path: "src/app/(auth)/layout.tsx"
      provides: "Envolve children no AuthShell (guard inverso de auth preservado)"
      contains: "AuthShell"
    - path: "src/app/icon.svg"
      provides: "Favicon navy tile + trio gold"
      contains: "rect"
  key_links:
    - from: "src/app/(auth)/layout.tsx"
      to: "src/components/auth-shell.tsx"
      via: "return <AuthShell>{children}</AuthShell>"
      pattern: "AuthShell"
    - from: "src/components/auth-shell.tsx"
      to: "src/components/brand-mark.tsx"
      via: "painel navy renderiza <BrandMark size={32} /> + wordmark"
      pattern: "BrandMark"
---

<objective>
Substitui o form de auth cru por uma `AuthShell` de duas colunas com identidade de produto: painel navy de marca à esquerda (`BrandMark` 32px + wordmark com "Financeira" em gold permitido no hero + value prop) e o formulário de auth existente à direita sobre `--card`. No mobile o painel navy vira faixa-cabeçalho compacta. Adiciona o favicon SVG navy+gold. Comportamento de login/signup congelado.

Cobre D-locked: tela de login/landing com cara de produto, não form cru (CONTEXT §Identidade, UI-03; UI-SPEC §Screen Contracts login/landing; §Brand — gold "Financeira" permitido SÓ no hero de auth). Completa UI-03 (a marca no chrome veio na Plan 02; aqui a identidade de auth/landing).

Purpose: a primeira tela que o usuário vê transmite "private banking", não um formulário genérico.
Output: AuthShell, (auth)/layout.tsx envolvendo o form, favicon icon.svg.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-identidade-visual-e-polimento/07-UI-SPEC.md
@.planning/phases/07-identidade-visual-e-polimento/07-PATTERNS.md

@src/app/(auth)/layout.tsx
@src/components/auth-form.tsx
@src/components/brand-mark.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: AuthShell (duas colunas) + favicon navy+gold</name>
  <read_first>
    - src/app/(auth)/layout.tsx (passthrough atual: guard inverso de auth com getClaims + `return children`)
    - src/components/auth-form.tsx (Card-based form — conteúdo do painel direito, INALTERADO)
    - src/components/brand-mark.tsx (BrandMark da Plan 02 — usado no painel + base do favicon)
    - .planning/phases/07-identidade-visual-e-polimento/07-UI-SPEC.md (§Screen Contracts login/landing → AuthShell duas colunas: esquerda navy brand panel com BrandMark+wordmark+value prop+MEI/private framing, direita o form em --card; mobile → faixa navy compacta; §Brand favicon SVG; §Copywriting value prop exata)
    - .planning/phases/07-identidade-visual-e-polimento/07-PATTERNS.md (§auth-shell — analog (auth)/layout.tsx + auth-form.tsx; comportamento congelado)
  </read_first>
  <action>Cria `src/components/auth-shell.tsx` — exporta `AuthShell({ children })`, layout de duas colunas (`md`: lado a lado; `<md`: empilhado, painel vira faixa-cabeçalho): painel esquerdo/topo sobre fundo navy (`--sidebar`/`--primary` chrome conforme UI-SPEC) com `<BrandMark size={32} />`, a wordmark "Gestão Financeira" em `font-heading` 600 com "Financeira" em gold (`text-primary` — permitido SÓ aqui per UI-SPEC §Brand), a value prop pt-BR EXATA da UI-SPEC §Copywriting ("Sua gestão financeira pessoal — privada, precisa e sob seu controle.") e o enquadramento private/MEI; painel direito sobre `--card` renderizando `{children}` (o form existente, intacto). Sem ilustração (deferida). Edita `src/app/(auth)/layout.tsx` para envolver o resultado no AuthShell: mantém VERBATIM o guard inverso (`getClaims` → redirect /dashboard se autenticado) e troca `return children` por `return <AuthShell>{children}</AuthShell>`. Cria `src/app/icon.svg` — o favicon = tile navy arredondado + trio de barras douradas ascendentes (mesma linguagem da BrandMark, valores OKLCH/token ou hex equivalente já que favicon é asset estático single-theme; preferir as cores navy+gold da identidade). NÃO toca o auth-form.tsx nem as actions de auth.</action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5 && npm test 2>&1 | tail -6 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/auth-shell.tsx` exporta `AuthShell`, importa e renderiza `BrandMark`, e contém a value prop exata "Sua gestão financeira pessoal — privada, precisa e sob seu controle."
    - `src/app/(auth)/layout.tsx` contém `AuthShell` envolvendo children E preserva o guard `getClaims`/`redirect('/dashboard')`.
    - `src/app/icon.svg` existe e contém ≥3 `<rect` (tile + trio).
    - `auth-form.tsx` e as actions de auth permanecem inalterados (sem mudança em src/actions/auth.ts).
    - `npx tsc --noEmit` limpo; `npm test` ≥559 passed; `npm run build` compila (rotas /auth/login + /auth/signup).
  </acceptance_criteria>
  <done>As telas de auth mostram a AuthShell de duas colunas com painel navy de marca + value prop, o form existente intacto à direita, o painel colapsa para faixa no mobile, e o favicon navy+gold existe. Comportamento de auth congelado; build + suíte verdes. UI-03 completo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauth ↔ auth pages | a AuthShell é chrome; o guard inverso de auth e as actions de login/signup ficam intactos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-11 | Spoofing | (auth)/layout guard inverso | mitigate | O guard `getClaims → redirect('/dashboard')` é preservado verbatim (acceptance criteria exige o grep); a AuthShell só envolve, não altera o fluxo de auth. |
| T-07-12 | Information Disclosure | AuthShell / favicon | accept | Chrome puramente estático/presentation; nenhum dado de usuário, nenhuma query. Favicon é asset estático sem segredo. |
</threat_model>

<verification>
- `npx tsc --noEmit` limpo; `npm test` ≥559 passed; `npm run build` compila as rotas de auth.
- AuthShell envolve o form com painel navy + value prop exata; guard inverso preservado; auth-form/actions intactos.
- icon.svg navy+gold presente.
</verification>

<success_criteria>
- UI-03: login/landing com identidade de produto (painel navy + marca + value prop), não form cru; favicon navy+gold.
- Comportamento de auth congelado — suíte verde.
</success_criteria>

<output>
Create `.planning/phases/07-identidade-visual-e-polimento/07-05-SUMMARY.md` when done
</output>
