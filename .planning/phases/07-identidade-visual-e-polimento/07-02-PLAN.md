---
phase: 07-identidade-visual-e-polimento
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/components/brand-mark.tsx
  - src/components/bottom-nav.tsx
  - src/components/app-sidebar.tsx
  - src/components/user-menu.tsx
  - src/app/(app)/layout.tsx
autonomous: true
requirements: [UI-03, UI-07]
must_haves:
  truths:
    - "A sidebar mostra a BrandMark navy+gold no topo e o item de nav ativo é gold com indicador à esquerda"
    - "O ThemeToggle aparece no UserMenu (top bar) — o usuário troca o tema de qualquer página (app)"
    - "No mobile (<md) uma BottomNav persistente dá acesso aos destinos primários com alvos ≥48px; o item ativo é gold"
    - "A marca é inline-SVG token-driven — auto-temiza em light e dark sem PNG por tema"
  artifacts:
    - path: "src/components/brand-mark.tsx"
      provides: "Tile navy + trio de barras douradas ascendentes (inline SVG, var(--token))"
      exports: ["BrandMark"]
    - path: "src/components/bottom-nav.tsx"
      provides: "Nav mobile <md dos destinos primários, ativo=gold, ≥48px"
      exports: ["BottomNav"]
    - path: "src/components/app-sidebar.tsx"
      provides: "Sidebar re-skin: BrandMark no header + active-gold + indicador"
      contains: "BrandMark"
    - path: "src/components/user-menu.tsx"
      provides: "ThemeToggle integrado no dropdown"
      contains: "ThemeToggle"
  key_links:
    - from: "src/components/app-sidebar.tsx"
      to: "src/components/brand-mark.tsx"
      via: "header da sidebar renderiza <BrandMark/>"
      pattern: "BrandMark"
    - from: "src/components/user-menu.tsx"
      to: "src/components/theme-toggle.tsx"
      via: "ThemeToggle dentro do DropdownMenuContent"
      pattern: "ThemeToggle"
    - from: "src/components/bottom-nav.tsx"
      to: "src/hooks/use-mobile.ts"
      via: "useIsMobile gate (<md)"
      pattern: "useIsMobile"
    - from: "src/app/(app)/layout.tsx"
      to: "src/components/bottom-nav.tsx"
      via: "BottomNav montada no shell (app)"
      pattern: "BottomNav"
---

<objective>
Aplica a identidade da marca no shell do app: a `BrandMark` navy+gold inline-SVG na sidebar, o item de nav ativo em gold com indicador à esquerda, o `ThemeToggle` no `UserMenu`, e uma `BottomNav` mobile persistente que adapta a navegação para o celular. Re-skin de chrome — comportamento de navegação e auth congelado.

Cobre D-locked: marca/logo simples (CONTEXT §Identidade, UI-03), nav adapta sidebar→bottom-nav no mobile (CONTEXT §Polimento+mobile, UI-07), gold reservado para ação/marca/ativo (UI-SPEC §Color 60/30/10). Implementa parte de UI-03 (marca no chrome — login/landing fica na Plan 05) + parte de UI-07 (nav adapta — tabelas→cards ficam na Plan 04).

Purpose: dar ao app a "cara de produto" na navegação e tornar o tema acessível em qualquer página; preparar o terreno mobile sem tocar dados.
Output: BrandMark, BottomNav, app-sidebar re-skin, ThemeToggle no UserMenu, BottomNav montada no shell (app).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-identidade-visual-e-polimento/07-UI-SPEC.md
@.planning/phases/07-identidade-visual-e-polimento/07-PATTERNS.md

@src/components/app-sidebar.tsx
@src/components/user-menu.tsx
@src/app/(app)/layout.tsx
@src/hooks/use-mobile.ts
@src/components/category-badge.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: BrandMark (inline SVG) + re-skin da sidebar (active-gold + indicador)</name>
  <read_first>
    - src/components/app-sidebar.tsx (NAV_ITEMS shape, active-state lines 51-52/58, header lines 39-45)
    - src/components/category-badge.tsx (analog do SVG token-driven aria-hidden/role — CategoryDot)
    - .planning/phases/07-identidade-visual-e-polimento/07-RESEARCH.md (§Code Examples → BrandMark inline SVG: rect tile fill="var(--primary)" + trio de barras gold)
    - .planning/phases/07-identidade-visual-e-polimento/07-UI-SPEC.md (§Brand/Logo — tile navy + trio gold, 24px sidebar; §Screen Contracts → app shell: active=gold text + gold left-indicator + accent bg; §Color 60/30/10 — gold reservado)
    - .planning/phases/07-identidade-visual-e-polimento/07-PATTERNS.md (§brand-mark — analog CategoryDot; §app-sidebar — copiar NAV_ITEMS + active)
  </read_first>
  <action>Cria `src/components/brand-mark.tsx` — exporta `BrandMark({ size = 24 })`, inline SVG token-driven copiando a convenção `aria-hidden`/`role`/`var(--token)` do `CategoryDot` em category-badge.tsx: um `<rect>` tile arredondado (`rx` ≈ --radius-lg) preenchido com a cor navy-tile e o trio de barras ascendentes (3 `<rect>` de altura crescente) na cor gold, mapeando os fills conforme UI-SPEC §Brand para que ambos light e dark leiam corretamente (confirme os papéis tile=navy/glyph=gold contra a tabela de tokens — NÃO hardcode hex; use `var(--primary)` etc.). Edita `src/app/app-sidebar.tsx`... (na verdade `src/components/app-sidebar.tsx`): no `SidebarHeader` substitui o texto solto por `<BrandMark size={24} />` + a wordmark "Gestão Financeira" em `font-heading` 600 (oculta no colapso-ícone como já é hoje); re-skina o item ativo para a gramática UI-SPEC — gold text + `--sidebar-accent` bg + um indicador de borda-esquerda 2px gold (a className atual `data-active:bg-muted data-active:text-primary` evolui para usar os sidebar tokens + o indicador; o `--primary` agora É gold pelos tokens da Plan 01, então o texto ativo já fica gold). Mantém o NAV_ITEMS, o `usePathname` active-detection e a composição `render={<Link/>}` intactos (comportamento de nav congelado).</action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5 && npm test 2>&1 | tail -6 && npm run build 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/brand-mark.tsx` exporta `BrandMark` e contém ≥3 elementos `<rect` (tile + trio) e usa `var(--` (sem hex hardcoded).
    - `grep -c "BrandMark" src/components/app-sidebar.tsx` ≥ 1.
    - O active-state da sidebar referencia um indicador/borda gold + sidebar-accent (não mais `bg-muted` solto) — checável por grep de `border-l`/`sidebar-accent`/`data-active` no arquivo.
    - NAV_ITEMS e `usePathname` permanecem (grep `NAV_ITEMS` e `usePathname`).
    - `npx tsc --noEmit` limpo; `npm test` ≥559 passed; `npm run build` compila.
  </acceptance_criteria>
  <done>A BrandMark navy+gold inline-SVG aparece no topo da sidebar, o item ativo é gold com indicador à esquerda, e a navegação segue funcionando inalterada. Build + suíte verdes.</done>
</task>

<task type="auto">
  <name>Task 2: ThemeToggle no UserMenu + BottomNav mobile montada no shell</name>
  <read_first>
    - src/components/user-menu.tsx (dropdown atual: email · sep · Privacidade e conta · sep · Sair)
    - src/components/theme-toggle.tsx (criado na Plan 01 — o controle 3-vias a integrar)
    - src/app/(app)/layout.tsx (shell: SidebarProvider + header + main; onde montar a BottomNav)
    - src/hooks/use-mobile.ts (useIsMobile, 768px)
    - .planning/phases/07-identidade-visual-e-polimento/07-UI-SPEC.md (§Dark Mode → toggle no UserMenu + espelho no drawer mobile; §Responsive → BottomNav destinos primários gold ≥48px; §Spacing → bottom-nav ≥48px touch)
    - .planning/phases/07-identidade-visual-e-polimento/07-PATTERNS.md (§bottom-nav — copiar app-sidebar NAV_ITEMS subset + active; gate useIsMobile; §Component composition base-ui render={})
  </read_first>
  <action>Edita `src/components/user-menu.tsx`: insere o `<ThemeToggle/>` dentro do `DropdownMenuContent` (acima de "Privacidade e conta" ou entre os separators conforme a gramática do dropdown), com um `DropdownMenuLabel` "Tema" se o toggle não trouxer o próprio rótulo; mantém o item Sair e o link Conta intactos (AUTH-04 congelado). Cria `src/components/bottom-nav.tsx` (`"use client"`) copiando do app-sidebar o shape `{ href, label, icon }` para o SUBSET de destinos primários (Dashboard, Extrato, Importar, MEI, Reservas — UI-SPEC §Responsive) e o active-detection `pathname === href || pathname.startsWith(href + '/')`; renderiza uma barra fixa no rodapé (`fixed bottom-0`, sobre `--background`/`--card`, borda-topo) com itens icon+label, alvos de toque ≥48px, item ativo em `text-primary` (gold); gate com `useIsMobile` de use-mobile.ts para renderizar SÓ `<md` (retorna null no desktop). Monta `<BottomNav/>` no `src/app/(app)/layout.tsx` dentro do `SidebarInset` (após o `<main>`), e dá ao `<main>` um padding-bottom no mobile para a barra não cobrir conteúdo. Comportamento de navegação/auth do shell congelado — só adiciona chrome.</action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5 && npm test 2>&1 | tail -6 && npm run build 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ThemeToggle" src/components/user-menu.tsx` ≥ 1 (import + uso).
    - `src/components/bottom-nav.tsx` exporta `BottomNav`, importa `useIsMobile` e contém os 5 hrefs primários (`/dashboard`, `/extrato`, `/importar`, `/mei`, `/reservas`).
    - `bottom-nav.tsx` referencia `text-primary` no active-state e tem altura/alvo de toque ≥48px (`h-12`/`min-h-12`/`py-3` equivalente).
    - `grep -c "BottomNav" "src/app/(app)/layout.tsx"` ≥ 1.
    - `npx tsc --noEmit` limpo; `npm test` ≥559 passed; `npm run build` compila.
  </acceptance_criteria>
  <done>O ThemeToggle está no UserMenu (tema alternável de qualquer página app) e a BottomNav mobile aparece <md com destinos primários gold-ativos e alvos ≥48px, montada no shell. Build + suíte verdes; navegação inalterada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client routing ↔ app | BrandMark/BottomNav são presentation/client; nenhuma nova rota, query ou action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering | app-sidebar / bottom-nav active-state | mitigate | NAV_ITEMS e active-detection copiados verbatim do sidebar provado; nenhuma rota nova é introduzida (BottomNav reusa o subset). Comportamento de navegação coberto pela suíte existente que segue verde. |
| T-07-05 | Information Disclosure | UserMenu dropdown | accept | ThemeToggle é client-only (localStorage), sem exposição de dado; o email já era exibido no UserMenu (inalterado). |
</threat_model>

<verification>
- `npx tsc --noEmit` limpo; `npm test` ≥559 passed; `npm run build` compila.
- BrandMark inline-SVG token-driven presente na sidebar; active-state gold + indicador.
- ThemeToggle no UserMenu; BottomNav <md com alvos ≥48px e ativo gold, montada no shell.
</verification>

<success_criteria>
- Shell com identidade: marca navy+gold, nav ativo gold com indicador, tema alternável de qualquer página.
- Navegação mobile adapta via BottomNav persistente (UI-07 parte nav).
- Comportamento de navegação/auth congelado — suíte verde.
</success_criteria>

<output>
Create `.planning/phases/07-identidade-visual-e-polimento/07-02-SUMMARY.md` when done
</output>
