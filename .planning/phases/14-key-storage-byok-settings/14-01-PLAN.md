---
phase: 14-key-storage-byok-settings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/lib/schemas/ai-settings.test.ts
  - src/lib/ai/provider-factory.test.ts
  - src/actions/ai-settings.test.ts
autonomous: false
requirements: [BYOK-01, BYOK-03, BYOK-04]
user_setup: []

must_haves:
  truths:
    - "@ai-sdk/google and @ai-sdk/anthropic are installed at the researched majors (3.0.x)"
    - "npx tsc --noEmit and npm run build pass with the two new packages present"
    - "Three Wave 0 test files exist and run (RED — the modules they target do not exist yet)"
  artifacts:
    - path: "package.json"
      provides: "@ai-sdk/google + @ai-sdk/anthropic dependencies (no @ai-sdk/deepseek)"
      contains: "@ai-sdk/google"
    - path: "src/lib/schemas/ai-settings.test.ts"
      provides: "Wave 0 RED test for the provider enum + apiKey shape (BYOK-01)"
      min_lines: 20
    - path: "src/lib/ai/provider-factory.test.ts"
      provides: "Wave 0 RED test: each provider maps, unknown throws (BYOK-04)"
      min_lines: 20
    - path: "src/actions/ai-settings.test.ts"
      provides: "Wave 0 RED test: mapProviderError 401/429/network → friendly pt-BR, no leak (BYOK-03)"
      min_lines: 20
  key_links:
    - from: "package.json"
      to: "node_modules/@ai-sdk/google"
      via: "npm install of the two first-party Vercel providers"
      pattern: "@ai-sdk/(google|anthropic)"
---

<objective>
Install the two BYOK provider SDKs (`@ai-sdk/google`, `@ai-sdk/anthropic`) behind a blocking human-verify legitimacy checkpoint, and lay down the three Wave 0 test scaffolds (Nyquist) that the downstream `lib/ai` + schema + action code will turn GREEN.

Purpose: Packages are the hard prerequisite for the provider factory + test-connection ping (Phase 14) and for the real classification call (Phase 15). The legitimacy seam returned `SUS` (`too-new` false positive — see RESEARCH §Package Legitimacy Audit), so per protocol the install is gated by ONE `checkpoint:human-verify` before `npm install`. The Wave 0 tests exist now so no later task ships without an automated verify (14-VALIDATION Wave 0).

Output: `@ai-sdk/google@3.0.x` + `@ai-sdk/anthropic@3.0.x` in `package.json`; three RED test files under `src/lib/schemas`, `src/lib/ai`, `src/actions`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-key-storage-byok-settings/14-RESEARCH.md
@.planning/phases/14-key-storage-byok-settings/14-VALIDATION.md

# The seam these scaffolds + packages serve (do NOT modify it this phase)
@src/lib/classifier/suggest.ts
@src/lib/classifier/suggest.test.ts
# Existing vitest config — `server-only` is aliased to a no-op so server-only modules
# import cleanly under jsdom (relevant to the action test in Wave 0)
@vitest.config.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Legitimacy checkpoint — confirm the two AI SDK package names + versions before install</name>
  <what-built>
    Nothing yet — this gate precedes the `npm install`. The package-legitimacy seam returned `SUS`
    (`too-new`) for both packages. RESEARCH §Package Legitimacy Audit documents this as a FALSE
    POSITIVE: the `vercel/ai` monorepo republishes every `@ai-sdk/*` patch nearly daily, so the
    latest patch's publish date is always 1–2 days old even though these are years-old, first-party
    Vercel libraries (5.38M/wk + 7.22M/wk downloads, repo github.com/vercel/ai, no postinstall
    scripts, explicitly locked in this project's CLAUDE.md stack table).
  </what-built>
  <how-to-verify>
    1. Confirm the two packages are the intended first-party Vercel providers, at the researched majors:
       - `@ai-sdk/google` → https://www.npmjs.com/package/@ai-sdk/google (expect 3.0.x; 3.0.83 as of 2026-06-18)
       - `@ai-sdk/anthropic` → https://www.npmjs.com/package/@ai-sdk/anthropic (expect 3.0.x; 3.0.85 as of 2026-06-18)
    2. Confirm repo is github.com/vercel/ai for both and there is no `postinstall` script.
    3. Confirm `@ai-sdk/deepseek` is NOT being installed (deferred — CLSAI-F1).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the install, or name a concern (wrong package / wrong version).</resume-signal>
  <files>(none — verification-only gate before npm install)</files>
  <action>
    HUMAN CHECKPOINT (autonomous:false) — do NOT run `npm install` until the user approves. Present the
    two package names + researched majors (`@ai-sdk/google@3.0.x`, `@ai-sdk/anthropic@3.0.x`) and the
    `SUS`/`too-new` false-positive rationale from RESEARCH §Package Legitimacy Audit. Pause for the user
    to verify both packages on npmjs.com per <how-to-verify> and confirm `@ai-sdk/deepseek` is excluded.
    This is never auto-approvable (legitimacy checkpoints ignore auto_advance).
  </action>
  <verify>Manual: the user confirms both package names, the 3.0.x major, the github.com/vercel/ai repo, no postinstall, and that deepseek is excluded — then types "approved".</verify>
  <done>The user has approved the two SUS packages (names + 3.0.x versions) on npmjs.com; deepseek confirmed excluded; install may proceed in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Install @ai-sdk/google + @ai-sdk/anthropic (NOT deepseek); confirm build clean</name>
  <files>package.json, package-lock.json</files>
  <action>
    After the legitimacy checkpoint is approved, install the two first-party providers at the
    researched majors: run `npm install @ai-sdk/google@3.0.83 @ai-sdk/anthropic@3.0.85`. Do NOT
    install `@ai-sdk/deepseek` (deferred — CLSAI-F1, RESEARCH §Deferred). `ai@6.0.x` and `zod@4.4.x`
    are already present — do NOT re-add them at different majors (would break the locked stack).
    These packages are pure JS — do NOT add them to `serverExternalPackages` in next.config.ts
    (only pdf-parse needs that; RESEARCH/ARCHITECTURE confirm no next.config change). At install
    time, re-verify the published versions are still 3.0.x (`npm view @ai-sdk/google version`,
    `npm view @ai-sdk/anthropic version`) since AI SDK republishes daily (RESEARCH Valid-until
    2026-07-18); if a newer 3.0.x patch exists, that is fine — stay within the 3.0 major. Then prove
    the tree compiles: `npx tsc --noEmit` and `npm run build` both pass.
  </action>
  <verify>
    <automated>grep -q '"@ai-sdk/google"' package.json && grep -q '"@ai-sdk/anthropic"' package.json && ! grep -q '"@ai-sdk/deepseek"' package.json && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>package.json lists @ai-sdk/google + @ai-sdk/anthropic at 3.0.x and NOT @ai-sdk/deepseek; tsc --noEmit and npm run build both pass.</done>
</task>

<task type="auto">
  <name>Task 3: Create the three Wave 0 RED test scaffolds (Nyquist)</name>
  <files>src/lib/schemas/ai-settings.test.ts, src/lib/ai/provider-factory.test.ts, src/actions/ai-settings.test.ts</files>
  <action>
    Create three failing tests that pin the contracts the downstream plans must satisfy. They are
    RED now (the modules under test do not exist until Plan 03/04) — that is intended; the harness
    treats them as Wave 0 gaps to be filled. Seed each from 14-VALIDATION + RESEARCH §Validation
    Architecture:
    - `src/lib/schemas/ai-settings.test.ts` (BYOK-01): import `aiSettingsSchema` from
      `@/lib/schemas/ai-settings`; assert it ACCEPTS `{ provider: 'gemini', apiKey: '<non-empty>' }`
      and `{ provider: 'claude', ... }`, and REJECTS a non-enum provider (e.g. 'deepseek', 'openai')
      and an empty apiKey. (This is the live enum gate for BYOK-01.)
    - `src/lib/ai/provider-factory.test.ts` (BYOK-04): import `modelFor` from
      `@/lib/ai/provider-factory`; assert `modelFor('gemini', 'gemini-2.5-flash-lite', 'k')` and
      `modelFor('claude', 'claude-haiku-4-5', 'k')` each return a defined value (a LanguageModel),
      and that an unknown provider throws. Do NOT make a network call.
    - `src/actions/ai-settings.test.ts` (BYOK-03): import the exported error-mapper (Plan 04 will
      export `mapProviderError`) and assert a simulated 401/403 → the "Chave inválida…" pt-BR copy,
      429 → the "Sem créditos ou cota esgotada…" copy, and a generic/network error → the "Não foi
      possível testar agora…" copy (exact strings from 14-UI-SPEC §Copywriting). Assert the returned
      string never contains `sk-`, `AIza`, or the word "stack". Mock the provider error object —
      no real provider call.
    Match the existing test grammar in `suggest.test.ts` (vitest `describe/it/expect`, `@/`-alias
    imports). The `server-only` guard is aliased to a no-op in vitest.config.ts, so the action test
    can import the server module transitively without tripping the client guard.
  </action>
  <verify>
    <automated>test -f src/lib/schemas/ai-settings.test.ts && test -f src/lib/ai/provider-factory.test.ts && test -f src/actions/ai-settings.test.ts && grep -lq "sk-" src/actions/ai-settings.test.ts</automated>
  </verify>
  <done>Three test files exist; running `npx vitest run src/lib/schemas/ai-settings.test.ts src/lib/ai/provider-factory.test.ts src/actions/ai-settings.test.ts` fails ONLY because the target modules are not yet implemented (RED), not because of a syntax/setup error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | Untrusted third-party code enters the build via `npm install` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-SC | Tampering | npm installs (`@ai-sdk/google`, `@ai-sdk/anthropic`) | mitigate | Blocking human-verify checkpoint (Task 1) confirms both `SUS` packages against npmjs.com before install; both are first-party Vercel, no postinstall, verified repo. `@ai-sdk/deepseek` excluded. |
</threat_model>

<verification>
- `npx tsc --noEmit` and `npm run build` pass with the two providers installed.
- `@ai-sdk/deepseek` is absent from package.json.
- The three Wave 0 test files exist and fail only for "module not implemented" reasons.
</verification>

<success_criteria>
- @ai-sdk/google + @ai-sdk/anthropic installed at 3.0.x; deepseek absent.
- Build + typecheck clean.
- Three Wave 0 RED scaffolds in place (BYOK-01/03/04).
</success_criteria>

<output>
Create `.planning/phases/14-key-storage-byok-settings/14-01-SUMMARY.md` when done
</output>
