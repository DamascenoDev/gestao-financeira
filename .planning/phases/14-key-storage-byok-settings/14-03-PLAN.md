---
phase: 14-key-storage-byok-settings
plan: 03
type: execute
wave: 2
depends_on: ["14-01", "14-02"]
files_modified:
  - src/lib/schemas/ai-settings.ts
  - src/lib/ai/settings.ts
  - src/lib/ai/provider-factory.ts
  - src/lib/ai/settings.server.ts
files_modified_tests:
  - src/lib/schemas/ai-settings.test.ts
  - src/lib/ai/provider-factory.test.ts
autonomous: true
requirements: [BYOK-01, BYOK-04]

must_haves:
  truths:
    - "aiSettingsSchema accepts provider in {gemini,claude} + a non-empty apiKey, and rejects any other provider or an empty key"
    - "modelFor(provider, model, apiKey) returns a per-call configured @ai-sdk model for gemini + claude, and throws on an unknown provider"
    - "getDecryptedAiSettings() reads the caller's own key ONLY inside a server-only module via the get_ai_api_key RPC"
    - "The client-safe registry (settings.ts) exposes provider labels + cheap default model ids and contains NO key access and NO 'server-only'"
  artifacts:
    - path: "src/lib/schemas/ai-settings.ts"
      provides: "Zod aiSettingsSchema (provider enum + apiKey) + inferred types"
      contains: "aiSettingsSchema"
    - path: "src/lib/ai/settings.ts"
      provides: "Client-safe provider/model registry (labels + DEFAULT_MODEL)"
      contains: "DEFAULT_MODEL"
    - path: "src/lib/ai/provider-factory.ts"
      provides: "modelFor(provider, model, apiKey) → LanguageModel"
      exports: ["modelFor"]
    - path: "src/lib/ai/settings.server.ts"
      provides: "server-only getDecryptedAiSettings() via the get_ai_api_key RPC"
      contains: "server-only"
  key_links:
    - from: "src/lib/ai/settings.server.ts"
      to: "public.get_ai_api_key()"
      via: "supabase.rpc('get_ai_api_key') under the RLS cookie client"
      pattern: "rpc\\(['\"]get_ai_api_key"
    - from: "src/lib/ai/provider-factory.ts"
      to: "@ai-sdk/google + @ai-sdk/anthropic"
      via: "createGoogleGenerativeAI({apiKey}) / createAnthropic({apiKey})"
      pattern: "create(GoogleGenerativeAI|Anthropic)"
---

<objective>
Build the `lib/ai` + schema layer: the Zod validation boundary, the client-safe provider/model registry, the per-call BYOK provider factory, and the `server-only` decrypt DAL. Turn the Plan 01 Wave 0 tests for the schema + factory GREEN.

Purpose: This is the typed, security-partitioned substrate the actions (Plan 04) and the RSC/form (Plan 05) consume — and the exact factory + decrypt DAL Phase 15 reuses for real classification. The split between `settings.ts` (client-safe registry, needed by the form for display labels) and `settings.server.ts` (`import 'server-only'`, the ONLY module that ever sees plaintext) is the structural guarantee that the key path can never be pulled into the client bundle (Pitfall 1).

Output: `src/lib/schemas/ai-settings.ts`, `src/lib/ai/settings.ts`, `src/lib/ai/provider-factory.ts`, `src/lib/ai/settings.server.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-key-storage-byok-settings/14-RESEARCH.md
@.planning/phases/14-key-storage-byok-settings/14-VALIDATION.md

# Grammar to mirror
@src/lib/schemas/mei.ts
@src/lib/supabase/admin.ts
@src/lib/supabase/server.ts
# The Wave 0 RED tests this plan must turn GREEN
@src/lib/schemas/ai-settings.test.ts
@src/lib/ai/provider-factory.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: aiSettingsSchema (Zod) + client-safe provider/model registry</name>
  <files>src/lib/schemas/ai-settings.ts, src/lib/ai/settings.ts</files>
  <behavior>
    - aiSettingsSchema accepts { provider: 'gemini', apiKey: 'AIza...' } and { provider: 'claude', apiKey: 'sk-ant-...' }
    - aiSettingsSchema rejects provider 'deepseek' / 'openai' / '' and rejects an empty apiKey
    - settings.ts exports AI_PROVIDERS = ['gemini','claude'], PROVIDER_LABEL, DEFAULT_MODEL with the cheap ids
  </behavior>
  <action>
    Create `src/lib/schemas/ai-settings.ts` mirroring `schemas/mei.ts` (co-located Zod, exported
    inferred types, pt-BR messages): export `AI_PROVIDERS = ['gemini','claude'] as const` and
    `aiSettingsSchema = z.object({ provider: z.enum(AI_PROVIDERS, { message: 'Selecione um provedor' }),
    apiKey: z.string().trim().min(1, 'Cole sua chave da API') })` plus the inferred type. The provider
    enum is the live BYOK-01 gate; deepseek is excluded by construction. Do NOT include a `model` field
    in the user-facing schema — the model is a hard-coded cheap default chosen server-side per provider
    (no UI picker; CLSAI-F2 deferred).

    Create `src/lib/ai/settings.ts` as the CLIENT-SAFE registry (NO `import 'server-only'`, NO key
    access, NO supabase import): export `AI_PROVIDERS`/`AiProvider` (or re-export from the schema to
    keep one source of truth), `PROVIDER_LABEL` (`gemini → 'Gemini (Google)'`, `claude → 'Claude
    (Anthropic)'` — the 14-UI-SPEC Select option copy) and `DEFAULT_MODEL` (`gemini →
    'gemini-2.5-flash-lite'`, `claude → 'claude-haiku-4-5'` — the cheap hard-coded defaults; bare
    aliases, no date suffix). The form imports labels from here; the decrypt DAL must NEVER be imported
    from here.
  </action>
  <verify>
    <automated>npx vitest run src/lib/schemas/ai-settings.test.ts && grep -q "DEFAULT_MODEL" src/lib/ai/settings.ts && ! grep -q "server-only" src/lib/ai/settings.ts</automated>
  </verify>
  <done>The Wave 0 schema test is GREEN; settings.ts exports the registry and carries no server-only/key access.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: provider-factory.ts — modelFor(provider, model, apiKey) per-call BYOK instantiation</name>
  <files>src/lib/ai/provider-factory.ts</files>
  <behavior>
    - modelFor('gemini', 'gemini-2.5-flash-lite', 'k') returns a defined LanguageModel
    - modelFor('claude', 'claude-haiku-4-5', 'k') returns a defined LanguageModel
    - modelFor with an unknown provider throws (exhaustive switch with a never default)
  </behavior>
  <action>
    Create `src/lib/ai/provider-factory.ts` per RESEARCH §Code Examples: import
    `createGoogleGenerativeAI` from `@ai-sdk/google`, `createAnthropic` from `@ai-sdk/anthropic`,
    and `type { LanguageModel }` from `ai`. Export `modelFor(provider: AiProvider, model: string,
    apiKey: string): LanguageModel` — a switch that returns `createGoogleGenerativeAI({ apiKey })(model)`
    for 'gemini' and `createAnthropic({ apiKey })(model)` for 'claude', with an exhaustive `never`
    default that throws on an unknown provider. BYOK: the apiKey is the user's own, passed per call —
    NEVER from an env var, NEVER AI Gateway. Do NOT install or import `@ai-sdk/deepseek`. Reuse the
    `AiProvider` type from settings.ts/the schema so provider names have one source of truth. If the
    v3.0.x `createX({ apiKey })(model)` call shape differs from the example (Assumption A5), adapt to
    the installed package's actual signature.
  </action>
  <verify>
    <automated>npx vitest run src/lib/ai/provider-factory.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>The Wave 0 factory test is GREEN (gemini + claude map, unknown throws); tsc passes.</done>
</task>

<task type="auto">
  <name>Task 3: settings.server.ts — server-only getDecryptedAiSettings() via the RPC</name>
  <files>src/lib/ai/settings.server.ts</files>
  <action>
    Create `src/lib/ai/settings.server.ts` mirroring the `admin.ts` `import 'server-only'` discipline
    (line 1: `import 'server-only'` so any client import fails the build). Export
    `getDecryptedAiSettings(): Promise<{ provider: 'gemini'|'claude'; model: string; apiKey: string } |
    null>`: build the RLS cookie client via `@/lib/supabase/server` `createClient()`, then in parallel
    `supabase.from('ai_settings').select('provider, model').maybeSingle()` and
    `supabase.rpc('get_ai_api_key')`; if either the row or the key is missing, return null (the no-key
    state — drives the graceful pre-IA fallback). Return `{ provider, model, apiKey }` otherwise. This
    is the ONLY module in the app that handles the decrypted key; it must never return it to a client
    or pass it as a prop. It is consumed by `testConnection` (Plan 04, this phase) and by
    `suggestCategory` (Phase 15). Do NOT add a unit test that makes a real Supabase call (Supabase
    integration tests are env-flaky per MEMORY — the decrypt path is covered by the manual RLS smoke
    in Plan 02 and the write-only-key checkpoint in Plan 05).
  </action>
  <verify>
    <automated>head -1 src/lib/ai/settings.server.ts | grep -q "server-only" && grep -q "rpc('get_ai_api_key')" src/lib/ai/settings.server.ts && npx tsc --noEmit</automated>
  </verify>
  <done>settings.server.ts is server-only, reads the key via the get_ai_api_key RPC, returns null on no-key, and the tree type-checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings.server.ts (server) ↔ everything else | Decrypted key is confined to this server-only module; the `import 'server-only'` guard makes a client import a build error |
| user input → aiSettingsSchema | Untrusted provider/apiKey crosses the Zod boundary before any DB/provider call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05 | Information Disclosure | Decrypt module pulled into the client bundle | mitigate | `import 'server-only'` on line 1 of settings.server.ts; the client-safe registry lives in a separate settings.ts with no key access. (Pitfall 1) |
| T-14-06 | Tampering / Input Validation | Non-enum provider reaching the DB/provider call | mitigate | `aiSettingsSchema` z.enum(['gemini','claude']) gate (BYOK-01) + the SQL CHECK constraint as defense-in-depth |
</threat_model>

<verification>
- `npx vitest run src/lib/schemas/ai-settings.test.ts src/lib/ai/provider-factory.test.ts` GREEN.
- `settings.server.ts` line 1 is `import 'server-only'`; `settings.ts` has none.
- `npx tsc --noEmit` passes.
</verification>

<success_criteria>
- aiSettingsSchema constrains provider to {gemini,claude} + non-empty apiKey (BYOK-01).
- modelFor maps both providers per-call and throws on unknown (BYOK-04).
- getDecryptedAiSettings reads the caller's own key server-only via the RPC (BYOK-04).
- Client-safe registry carries no key path.
</success_criteria>

<output>
Create `.planning/phases/14-key-storage-byok-settings/14-03-SUMMARY.md` when done
</output>
