---
phase: 22-sugest-o-de-palavra-chave-inline-batch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/import-review-table.tsx
  - src/components/import-review-table.test.tsx
autonomous: true
requirements: [KW-07]
tags: [keyword, import-review, inline]

must_haves:
  truths:
    - "On a row the user classified by hand (origin === 'manual'), a discreet '+ palavra-chave' pill appears on the chip row inside the import review grid."
    - "On rows classified by memória / palavra-chave / não classificada, the pill is ABSENT (the grid stays quiet)."
    - "Clicking the pill opens a small popover prefilled with the row's normalized descriptor_norm, editable, that on Salvar calls the EXISTING addKeyword(row.category_id, term) in the category the user just picked."
    - "A successful create flips the inline control to a disabled 'criada ✓' for that row for the rest of the review session; a duplicate also flips it (toast.info); a server error keeps the popover open with a FieldError."
  artifacts:
    - path: "src/components/import-review-table.tsx"
      provides: "Inline KW-07 control mounted in InlineReviewCategoryCell chip row (gated origin === 'manual'), reusing addKeyword; per-row 'criada ✓' session state"
      contains: "addKeyword"
    - path: "src/components/import-review-table.test.tsx"
      provides: "Extended tests: pill renders only on manual rows; Salvar calls addKeyword(category_id, term); criada ✓ flip; absent on non-manual"
  prohibitions:
    - "No call to confirmImport, no write to transactions/merchant_patterns from the inline path — it writes ONLY category_keywords via addKeyword."
    - "No new server action for inline (reuse addKeyword verbatim)."
    - "No gating on a non-existent 'IA'/'ia' origin; the gate is strictly row.origin === 'manual'."
    - "No re-normalization of descriptor_norm at render; only the user's EDITED term passes through normalizeKeyword (via addKeyword)."
  key_links:
    - from: "src/components/import-review-table.tsx (InlineReviewCategoryCell)"
      to: "src/actions/category-keywords.ts (addKeyword)"
      via: "startTransition(async () => addKeyword(row.category_id, term)) on Salvar"
      pattern: "addKeyword\\("
---

<objective>
Deliver KW-07: an opt-in, per-row "+ palavra-chave" control inline in the import review grid (`import-review-table.tsx`) that turns a hand-classified row's descriptor into a `category_keywords` entry — reusing the EXISTING `addKeyword` action verbatim, with NO change to `confirmImport` and NO new server action.

Purpose: Let the user capture a merchant→category keyword at the exact moment they confirm a manual classification, without leaving the grid and without auto-creating anything.
Output: Modified `import-review-table.tsx` (inline control + popover + session "criada ✓" state) and extended `import-review-table.test.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-sugest-o-de-palavra-chave-inline-batch/22-CONTEXT.md
@.planning/phases/22-sugest-o-de-palavra-chave-inline-batch/22-RESEARCH.md
@.planning/phases/22-sugest-o-de-palavra-chave-inline-batch/22-PATTERNS.md
@.planning/phases/22-sugest-o-de-palavra-chave-inline-batch/22-UI-SPEC.md

@src/components/import-review-table.tsx
@src/actions/category-keywords.ts
@src/components/category-keywords-dialog.tsx
@src/components/import-review-table.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mount the gated inline "+ palavra-chave" control + edit popover in InlineReviewCategoryCell</name>
  <read_first>
    - src/components/import-review-table.tsx — ReviewRow.origin union (line 227: `'memória' | 'palavra-chave' | 'manual' | 'não classificada'`, NO 'IA' member); classifyRow (343-360, sets origin: 'manual'); applyAllSuggestions (370-392, applied AI suggestion also flips origin → 'manual'); InlineReviewCategoryCell (891-1027); the chip row insertion point (the `<div className="flex flex-wrap items-center gap-1">` at line 977, after ConfidenceTag); existing imports (lines 12 lucide `Sparkles, Trash2`; line 15 `toast` from sonner; line 38 Button; ui/popover NOT yet imported; addKeyword NOT yet imported).
    - src/actions/category-keywords.ts — addKeyword signature + result union (lines 29-95): `addKeyword(categoryId: string, keyword: string): Promise<{ ok: true } | { duplicate: true } | { error: string }>`; it already normalizes via normalizeKeyword, dedupes, owner-gates, and revalidatePath('/categorias').
    - src/components/category-keywords-dialog.tsx — the Salvar/onSubmit recipe (lines 80-112): `useTransition`, branch `'error' in r` → FieldError, `'duplicate' in r` → toast.info, else toast.success; `Field`/`FieldLabel`/`FieldError`/`Input maxLength={60}`; `React.useId()` for the input id (line 66).
    - src/components/ui/popover.tsx — the Popover/PopoverTrigger/PopoverContent primitive (vendored; same family as the grid).
    - .planning/phases/22-sugest-o-de-palavra-chave-inline-batch/22-UI-SPEC.md — §Copywriting Contract (Inline KW-07 table: exact pt-BR copy "+ palavra-chave", aria-label "Criar palavra-chave para esta categoria", "Palavra-chave", "Salvar"/"Salvando…", "Cancelar", "criada ✓", success/duplicate toasts) and §Interaction (placement on the chip row, gate, focus/keyboard).
  </read_first>
  <files>src/components/import-review-table.tsx</files>
  <action>
    Add an inline keyword-suggestion control inside `InlineReviewCategoryCell` (src/components/import-review-table.tsx), mounted on the existing chip row — the `flex flex-wrap items-center gap-1` div at line 977, immediately after `<ConfidenceTag row={row} />` — and rendered ONLY when `row.origin === 'manual'` (per KW-07 / CONTEXT lock). Do NOT add a new grid column; reuse the existing pill geometry so the Categoria column never reflows.

    Implementation (you MAY extract a small local sub-component such as `KeywordInlineSuggest` inside this same file, or a new `src/components/keyword-inline-suggest.tsx` — Claude's discretion per CONTEXT; if you create a new file, add it to files_modified in the SUMMARY and import it here):

    1. Add imports at the top of `import-review-table.tsx`: `addKeyword` from `@/actions/category-keywords`; `Popover, PopoverContent, PopoverTrigger` from `@/components/ui/popover`; `Field, FieldError, FieldLabel` from `@/components/ui/field`; `Input` from `@/components/ui/input`; the `Tags` and `Check` icons from `lucide-react` (extend the existing line-12 lucide import). Do NOT import `normalizeKeyword` for re-normalization at render — `addKeyword` already normalizes; only echo it in the toast for the message (mirror `category-keywords-dialog.tsx` which derives the echoed value).
    2. Track per-row "criada ✓" in `ImportReviewTable` component state as a session-scoped `Set<string>` of row ids (`const [createdKeywordRows, setCreatedKeywordRows] = React.useState<Set<string>>(() => new Set())`), with a `markKeywordCreated(rowId)` callback that returns a new Set (immutable update). Thread `createdKeywordRows.has(row.id)` and `markKeywordCreated` down to `InlineReviewCategoryCell` as props (alongside the existing `onClassify`). This state is intentionally NOT persisted (UI-SPEC: need not survive reload).
    3. The idle control: a discreet pill (reuse `SuggestionSlot`'s pill geometry `inline-flex min-h-5 w-fit items-center gap-1 rounded-4xl px-2 py-0.5 text-xs font-medium` with the neutral `bg-muted text-muted-foreground` treatment — NOT gold `--primary`, per UI-SPEC §Color) wrapped as the `PopoverTrigger`. Label text "+ palavra-chave" with the `Tags` icon (`className="size-3"`); `aria-label="Criar palavra-chave para esta categoria"`. When `createdKeywordRows.has(row.id)` is true, render instead a disabled "criada ✓" pill (the `Check` icon + text, muted, not a button) and do NOT render the popover trigger.
    4. The popover (`PopoverContent`, content width ~`w-72`): a single `Field` with `FieldLabel htmlFor={inputId}` "Palavra-chave" (use `React.useId()` for `inputId`), `Input` prefilled with `row.descriptor_norm` (editable, `maxLength={60}`), the optional helper line ("Será usada para classificar futuras faturas em {categoryName}." — resolve `categoryName` from `current?.name`), a `FieldError`, and a `flex gap-2` footer with "Cancelar" (`variant="outline"`, closes the popover without writing) and "Salvar" (primary; pending label "Salvando…").
    5. The Salvar handler — mirror `category-keywords-dialog.tsx:80-112` exactly: keep local `value`/`error` state + `useTransition`; on submit call `startTransition(async () => { const r = await addKeyword(row.category_id!, value); if ('error' in r) { setError(r.error); return } if ('duplicate' in r) toast.info(\`"${normalizeKeyword(value.trim())}" já está cadastrada.\`); else toast.success(\`"${normalizeKeyword(value.trim())}" adicionada a ${categoryName}.\`); markKeywordCreated(row.id); closePopover() })`. (Import `normalizeKeyword` ONLY for the echoed toast string, matching the stored value — never to re-derive a render value.) On `{ ok }` AND `{ duplicate }` flip to "criada ✓" (the keyword now exists either way); on `{ error }` keep the popover open and show the `FieldError` (do NOT flip).
    6. Keyboard/focus: Enter submits Salvar; Esc closes without writing; focus returns to the pill on close (Base UI Popover defaults — do not override). Reuse the existing `current` lookup (`categories.find((c) => c.id === row.category_id)`) for `categoryName`.

    Do NOT touch `confirmImport`, `classifyRow`, `applyAllSuggestions`, or any write to `transactions`/`merchant_patterns`. The only server call this task introduces is `addKeyword`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "import-review-table" || echo "no type errors in import-review-table"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "row.origin === 'manual'" src/components/import-review-table.tsx` returns the gate guarding the inline control (the pill is rendered behind this condition).
    - `grep -c "addKeyword" src/components/import-review-table.tsx` ≥ 1 (the inline path calls the existing action).
    - `grep -vE '^\s*(//|\*|/\*)' src/components/import-review-table.tsx | grep -c "confirmImport" ` is unchanged from baseline EXCEPT the existing import + existing call sites — the task adds NO new `confirmImport` reference (the inline path never commits the import). Verify by diffing: no new `confirmImport(` call is introduced.
    - The literal pt-BR strings "+ palavra-chave", "criada ✓", "Criar palavra-chave para esta categoria", "Salvar" appear in the file (UI-SPEC §Copywriting Contract).
    - `npx tsc --noEmit` reports no new errors attributable to import-review-table.tsx.
  </acceptance_criteria>
  <done>The inline "+ palavra-chave" control renders only on `origin === 'manual'` rows, opens a popover prefilled with `descriptor_norm`, calls `addKeyword(row.category_id, term)` on Salvar, flips to a disabled "criada ✓" on success/duplicate, and keeps the popover open with a FieldError on error — with no change to `confirmImport` or any `transactions`/`merchant_patterns` write.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend import-review-table.test.tsx — gating + Salvar + criada ✓</name>
  <read_first>
    - src/components/import-review-table.test.tsx — the existing harness: `confirmImportMock` + `vi.mock('@/actions/import')` (24-27), `next/navigation` mock (29-31), `CATEGORIES` fixture (34-37), `SERVER_SUMMARY` (39-45), `makeRow(overrides)` factory (47-65, defaults `origin: 'não classificada'`, `category_id: null`), the `at<T>` index helper (67+). Read the whole file to match its render(...) + screen query style.
    - src/components/category-keywords-dialog.test.tsx — the action-mock + toast-mock + fireEvent.change/click + flush-transition pattern (lines 15-87): `vi.mock('@/actions/category-keywords', () => ({ addKeyword: vi.fn(async () => ({ ok: true })) }))`, `vi.mock('sonner', ...)`, `expect(addKeyword).toHaveBeenCalledWith(...)`, the `await Promise.resolve()` double-flush.
    - .planning/phases/22-sugest-o-de-palavra-chave-inline-batch/22-RESEARCH.md §Validation Architecture — the KW-07 test rows (pill only on manual; Salvar calls addKeyword; criada ✓; absent on non-manual).
  </read_first>
  <files>src/components/import-review-table.test.tsx</files>
  <action>
    Extend `src/components/import-review-table.test.tsx` (do NOT rewrite the existing suite). Add `vi.mock('@/actions/category-keywords', () => ({ addKeyword: vi.fn(async () => ({ ok: true })) }))` and `vi.mock('sonner', () => ({ toast: { success: vi.fn(), info: vi.fn(), error: vi.fn() } }))` at the top with the other mocks (if a sonner mock is not already present; if it is, extend it). Add a new `describe('KW-07 inline keyword suggestion', ...)` block with these cases, using the existing `makeRow`/`CATEGORIES`/`SERVER_SUMMARY` fixtures and rendering `<ImportReviewTable .../>`:

    1. "renders the '+ palavra-chave' pill ONLY on a manually-classified row": render a grid with one row `makeRow({ category_id: 'cat-mercado', origin: 'manual' })` and assert `screen.getByRole('button', { name: 'Criar palavra-chave para esta categoria' })` (or `getByText('+ palavra-chave')`) is in the document.
    2. "does NOT render the pill on memória/palavra-chave/não-classificada rows": render rows with `origin: 'memória'` (and `category_id` set), `origin: 'palavra-chave'`, and `origin: 'não classificada'` (`category_id: null`) and assert the affordance is absent (`queryByText('+ palavra-chave')` / `queryByRole(... 'Criar palavra-chave...')` is null).
    3. "Salvar calls addKeyword(row.category_id, term) and flips to criada ✓": render a `origin: 'manual'`, `category_id: 'cat-transporte'`, `descriptor_norm: 'uber trip 99'` row; click the pill to open the popover; the term input is prefilled with `'uber trip 99'`; click "Salvar"; flush; assert `addKeyword` was called with `('cat-transporte', 'uber trip 99')` and that a "criada ✓" indicator now renders (and the pill no longer offers a second create).
    4. "duplicate still flips to criada ✓ (toast.info)": set the addKeyword mock to resolve `{ duplicate: true }` for this case (`vi.mocked(addKeyword).mockResolvedValueOnce({ duplicate: true })`); after Salvar+flush, assert `toast.info` was called and the control shows "criada ✓".
    5. "error keeps the popover open (no flip)": set addKeyword to resolve `{ error: 'Não foi possível salvar a palavra-chave.' }`; after Salvar+flush, assert the popover input is still present and "criada ✓" is NOT shown.

    Follow the existing file's import style and the `await Promise.resolve()` double-flush from the dialog test to settle the transition.
  </action>
  <verify>
    <automated>npx vitest run src/components/import-review-table.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/import-review-table.test.tsx` passes, including the new `KW-07 inline keyword suggestion` describe block (≥ 5 cases).
    - The suite asserts the pill is ABSENT on at least the three non-manual origins (`memória`, `palavra-chave`, `não classificada`).
    - The suite asserts `addKeyword` is called with `(row.category_id, descriptor_norm)` on Salvar (not a raw/re-normalized value — the action does the normalization).
    - No existing test in the file regresses (the full file is green).
  </acceptance_criteria>
  <done>`import-review-table.test.tsx` proves the inline pill renders only on `manual` rows, Salvar calls `addKeyword(category_id, term)`, success/duplicate flip to "criada ✓", and error keeps the popover open — all with `addKeyword` mocked (no real server boundary).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (grid) → server action `addKeyword` | The edited keyword term + the row's category_id cross from untrusted client state into the server action. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | inline → addKeyword | accept (already mitigated) | `addKeyword` re-runs `idSchema` (uuid) on categoryId, `keywordSchema` (trim/min1/max60) + `normalizeKeyword` + literal-count-0 reject on the term, and owner-gates via `getClaims().claims.sub` (never the client). This plan reuses it verbatim and adds NO new server surface — the existing mitigation covers V4/V5. |
| T-22-02 | Elevation | inline → addKeyword (foreign category_id) | accept (already mitigated) | RLS `with check (auth.uid() = user_id)` + the FK to the caller's own categories: a category_id the user does not own inserts nothing. No change. |
| T-22-SC | Tampering | npm/pip/cargo installs | mitigate | RESEARCH §Package Legitimacy Audit: this plan installs ZERO packages (no `npm install`, no `npx shadcn add`). No `[ASSUMED]`/`[SUS]` package → no install checkpoint needed. |
</threat_model>

<verification>
- `npx vitest run src/components/import-review-table.test.tsx` — green, including the new KW-07 block.
- `npx tsc --noEmit` — no new type errors.
- Manual reasoning gate (no human checkpoint this plan; covered by tests): the inline control is invisible on every non-`manual` origin, and Salvar's only side effect is `addKeyword` → `category_keywords` (never `confirmImport`/`transactions`/`merchant_patterns`).
</verification>

<success_criteria>
- KW-07 satisfied: on a hand-classified (origin `manual`) review row, the user gets an opt-in inline control to create a `category_keywords` keyword for that descriptor in the just-picked category, with NO auto-creation and NO change to the import-commit path.
- The control is gated strictly on `row.origin === 'manual'` and flips to a session-scoped "criada ✓" after create/duplicate.
- Tests prove gating, the `addKeyword` call shape, the criada-✓ flip, and the error-keeps-open behavior.
</success_criteria>

<output>
Create `.planning/phases/22-sugest-o-de-palavra-chave-inline-batch/22-01-SUMMARY.md` when done.
</output>
