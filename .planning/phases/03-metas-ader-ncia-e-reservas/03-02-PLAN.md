---
phase: 03-metas-ader-ncia-e-reservas
plan: 02
type: tdd
wave: 2
depends_on: ["03-01"]
files_modified:
  - tests/budget-target-crud.test.ts
  - tests/budget-target-direction.test.ts
  - tests/adherence-month.test.ts
  - tests/adherence-consistency.test.ts
  - tests/adherence-ytd.test.ts
  - tests/reserva-crud.test.ts
  - tests/reserva-aporte.test.ts
  - tests/reserva-saida.test.ts
  - tests/reserva-balance.test.ts
  - tests/reserva-idor.test.ts
  - src/lib/adherence.test.ts
  - tests/view-leak.test.ts
  - tests/rls-isolation.test.ts
autonomous: true
requirements: [BUD-01, BUD-02, BUD-03, BUD-04, RSV-01, RSV-02, RSV-03, RSV-04, RSV-05]
user_setup: []

must_haves:
  truths:
    - "Every Wave-0 behavior from 03-VALIDATION.md has a failing (RED) integration/unit test that pins the contract before the UI slices implement it"
    - "The allocation-grouping test (RSV-03) asserts an aporte raises the alocação realized cents and leaves every consumo total unchanged — the #1 double-counting pitfall"
    - "view-leak.test.ts and rls-isolation.test.ts are extended to cover the three new views and three new tables"
    - "src/lib/adherence.test.ts pins the 80/100 status mapping per direction and the bp→percent / division-by-zero behavior (unit, no DB)"
  artifacts:
    - path: "tests/reserva-aporte.test.ts"
      provides: "RSV-02/03 highest-value test: Reserva-tx → linked ledger 'in' + counts as alocação not consumo"
      contains: "alocacao"
    - path: "tests/reserva-saida.test.ts"
      provides: "RSV-04: overdraw rejected via RPC, never negative (incl. concurrent)"
      contains: "register_reserva_saida"
    - path: "tests/reserva-idor.test.ts"
      provides: "IDOR: forged reserva_id rejected by ownership re-derive; raw insert accepts it (proves RLS insufficient)"
      contains: "reserva"
    - path: "tests/adherence-consistency.test.ts"
      provides: "BUD-02/03: single-month year → identical adherence_bp from v_adherence_month and v_adherence_ytd"
      contains: "v_adherence_ytd"
    - path: "src/lib/adherence.test.ts"
      provides: "unit: status thresholds + percent format + /0 guard"
      contains: "adherence"
    - path: "tests/view-leak.test.ts"
      provides: "extended: user B reads 0 of A's adherence/balance view rows"
      contains: "v_reserva_balance"
    - path: "tests/rls-isolation.test.ts"
      provides: "extended TABLES with budget_targets, reservas, reserva_ledger"
      contains: "budget_targets"
  key_links:
    - from: "tests/*.test.ts"
      to: "tests/helpers/local-supabase.ts"
      via: "serviceClient/userClient two-user harness (reused verbatim)"
      pattern: "local-supabase"
    - from: "tests/reserva-saida.test.ts"
      to: "register_reserva_saida RPC"
      via: "rpc() overdraw assertion"
      pattern: "register_reserva_saida"
---

<objective>
Write the nine Wave-0 failing integration tests + the adherence unit test from 03-VALIDATION.md / RESEARCH "Wave 0 Gaps", and extend the two existing isolation tests (view-leak, rls-isolation) to cover the three new views and three new tables. These tests pin every Phase-3 correctness contract — adherence math, monthly↔YTD consistency, derived balance, never-negative saída, the aporte alocação-grouping invariant, and IDOR rejection — BEFORE the UI slices implement against them (Nyquist).

Purpose: 03-VALIDATION.md set `wave_0_complete: false`; this plan makes it true. The tests clone proven Phase-1/2 harnesses (two-user local-supabase helper, view-leak, category-idor) — no new framework. They START RED where they exercise actions/UI not yet built (Plans 03-05), and GREEN where they exercise the Plan-01 migrations/views/RPC directly. (D: 03-VALIDATION 3-W0-01..09; RESEARCH Validation Architecture test map.)

Output: 11 new/extended test files. Migration-level tests (CRUD, balance, saída, adherence math, consistency, leak, RLS, IDOR-raw-half) pass against the Plan-01 schema now; action-level assertions (direction-default, aporte sub-flow, IDOR ownership-check via the actions) stay RED until Plans 03-05.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-metas-ader-ncia-e-reservas/03-VALIDATION.md
@.planning/phases/03-metas-ader-ncia-e-reservas/03-RESEARCH.md
@tests/helpers/local-supabase.ts
@tests/view-leak.test.ts
@tests/category-idor.test.ts
@tests/rls-isolation.test.ts
@tests/seed-categories.test.ts
@src/lib/adherence.ts
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Reserva + adherence integration tests (RED) — balance, saída, aporte-grouping, IDOR, CRUD, leak, RLS</name>
  <files>tests/reserva-balance.test.ts, tests/reserva-saida.test.ts, tests/reserva-aporte.test.ts, tests/reserva-idor.test.ts, tests/reserva-crud.test.ts, tests/view-leak.test.ts, tests/rls-isolation.test.ts</files>
  <read_first>tests/helpers/local-supabase.ts (readLocalConfig/serviceClient/userClient + the createUser pattern with admin.auth.admin.createUser + signInWithPassword), tests/category-idor.test.ts (the two-half IDOR proof: raw insert ACCEPTS foreign FK, ownership check REJECTS it — clone for reserva_id), tests/view-leak.test.ts (extend the describe with the new views), tests/rls-isolation.test.ts (the TABLES array + rowByTable insert map).</read_first>
  <behavior>
    - reserva-balance (RSV-05): seed a reserva + ledger ins/outs via the service client → v_reserva_balance.saldo_cents == Σ in − Σ out; a reserva with no ledger reads 0; no stored balance column exists.
    - reserva-saida (RSV-04): register_reserva_saida(reserva, amount ≤ saldo) inserts an 'out' and returns an id; amount > saldo raises (error not null, message contains 'saldo'); after an overdraw attempt saldo is unchanged and never negative; two near-concurrent saídas that together exceed the balance → at most the ones that fit succeed, saldo never < 0 (Promise.all then assert saldo >= 0).
    - reserva-aporte (RSV-02/03 — HIGHEST VALUE): inserting a transaction in a kind='alocacao' Reserva category + a linked reserva_ledger 'in' entry raises the alocação realized total in v_adherence_month AND leaves every kind='consumo' category total unchanged; the same cents never appear in a consumo line (the double-counting guard).
    - reserva-idor (clone category-idor): a RAW reserva_ledger insert with another user's reserva_id ACCEPTS (proves RLS alone does not close the FK); the ownership check (`select id from reservas where id = $1` under the caller's RLS) returns 0 for the foreign id and 1 for the owned id.
    - reserva-crud (RSV-01): create/update/delete a reserva; alvo optional (null allowed); deleting a reserva cascades its ledger.
    - view-leak (extend): user B reads ZERO of user A's v_adherence_month, v_adherence_ytd, v_reserva_balance rows.
    - rls-isolation (extend): add budget_targets, reservas, reserva_ledger to TABLES with neutral insert rows; user B cannot SELECT/INSERT/UPDATE/DELETE user A's rows in each.
  </behavior>
  <action>
Clone the two-user harness from tests/helpers/local-supabase.ts verbatim (do NOT add a new framework). Each file is an integration test guarded to the local Docker stack. Seed data with the serviceClient (bypasses RLS for setup) and assert behavior through a userClient (RLS-active) exactly as view-leak/category-idor do.

For reserva-aporte (the highest-value test, Pitfall 3): create a user, find their seed Investimentos + Reserva (kind='alocacao') and a consumo category; seed income for the month; record a baseline of v_adherence_month per-category realized; insert a Reserva-category transaction + a linked reserva_ledger 'in'; re-read v_adherence_month and assert the alocação total rose by the aporte amount while every consumo total is byte-identical to baseline. This pins RSV-03 regardless of whether the grouping is finalized in the view or the app.

For reserva-saida, drive register_reserva_saida via `userClient.rpc('register_reserva_saida', { p_reserva_id, p_amount_cents, p_occurred_on, p_note })`. Assert the overdraw path returns a non-null error whose message includes 'saldo', and that a Promise.all of two oversized saídas leaves saldo_cents >= 0.

Extend tests/view-leak.test.ts by adding a describe that seeds a reserva + ledger + a budget_target for user A, then asserts user B's select on each new view filtered by user A's id returns length 0. Extend tests/rls-isolation.test.ts by appending 'budget_targets', 'reservas', 'reserva_ledger' to TABLES and adding their neutral insert shapes to rowByTable (budget_targets needs a category_id — use a category the test creates for user A; reserva_ledger needs a reserva_id + kind + amount_cents + occurred_on).

Do NOT use watch mode. Each file uses afterAll to delete its throwaway users.
  </action>
  <verify>
    <automated>SUPABASE_DISABLE_TELEMETRY=1 npx vitest run reserva-balance reserva-saida reserva-aporte reserva-idor reserva-crud view-leak rls-isolation 2>&1 | tail -20</automated>
  </verify>
  <done>All seven files exist and run against the local stack. Migration-level assertions (balance, saída overdraw, leak, RLS, raw-IDOR-accept, CRUD, aporte-grouping) PASS against the Plan-01 schema. The ownership-check half of reserva-idor passes (it tests the select-based guard directly). No watch flags.</done>
</task>

<task type="tdd">
  <name>Task 2: Budget-target + adherence math/consistency tests + adherence.ts unit test (RED)</name>
  <files>tests/budget-target-crud.test.ts, tests/budget-target-direction.test.ts, tests/adherence-month.test.ts, tests/adherence-consistency.test.ts, tests/adherence-ytd.test.ts, src/lib/adherence.test.ts</files>
  <read_first>tests/helpers/local-supabase.ts, src/lib/adherence.ts (the status mapper + percent formatter signatures from Plan 01), src/lib/month.ts (yearBounds/currentYear), RESEARCH.md Patterns 2/3 (the meta_cents + adherence_bp formulas the tests assert) and Pitfalls 1/2/7.</read_first>
  <behavior>
    - budget-target-crud (BUD-01): insert a budget_target round-trips; the unique(user_id, category_id) makes a second target for the same category an upsert (one meta per category), not a duplicate.
    - budget-target-direction (BUD-01): the default direction for a kind='consumo' category is 'teto' and for kind='alocacao' is 'alvo'; a user override to the opposite persists. (The default lives in the action/form — this assertion stays RED until Plan 03 ships upsertBudgetTarget; for now assert the schema accepts both directions and the DB stores whatever is written.)
    - adherence-month (BUD-02): for known income + spend, v_adherence_month.meta_cents == (income_cents * percent_bp + 5000)/10000 (integer half-up) and adherence_bp == realized*1e8/(income*bp); income 0 → adherence_bp null (no NaN).
    - adherence-consistency (BUD-02/03 — the consistency proof): for a year with ONE populated month, per-category adherence_bp from v_adherence_month equals that from v_adherence_ytd.
    - adherence-ytd (BUD-03): across multiple months in the civil year, v_adherence_ytd accumulates income and spend over [YYYY-01-01..now] correctly.
    - adherence.test.ts (BUD-04, unit, no DB): the status mapper returns 'no-limite'/'aproximando'/'estourou' for a teto at 79/80/100 %% and 'abaixo'/'quase-la'/'atingido' for an alvo at the same thresholds; null bp → 'sem-receita'; the percent formatter renders '72,5%' and never 'NaN%'/'Infinity%'.
  </behavior>
  <action>
The five integration tests clone the two-user harness and exercise the Plan-01 views/tables. adherence-consistency is the load-bearing one (Pitfall 7): seed income + spend in exactly one civil month, then assert v_adherence_month and v_adherence_ytd produce the same adherence_bp per category — this proves the "one percent_bp, two windows" design holds.

src/lib/adherence.test.ts is a pure unit test (jsdom/globals, no Supabase) importing the Plan-01 adherence.ts. Pin the exact 80/100 thresholds per direction and the percent-format/division-by-zero behavior. This file is fast (<2s) and must stay GREEN once Plan 01's adherence.ts exists; the integration files run against the local stack.

budget-target-direction's action-default assertion is allowed to start RED (it references upsertBudgetTarget from Plan 03); structure it so the schema/DB half passes now and the action half is a clearly-labeled pending assertion that Plan 03 turns green. Do not use watch mode.
  </action>
  <verify>
    <automated>SUPABASE_DISABLE_TELEMETRY=1 npx vitest run budget-target-crud budget-target-direction adherence-month adherence-consistency adherence-ytd src/lib/adherence.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>All six files exist. src/lib/adherence.test.ts is GREEN (pure unit). The view-math integration tests (crud, month, consistency, ytd) PASS against the Plan-01 schema. budget-target-direction's action-default half is RED-pending for Plan 03. No watch flags.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → local Postgres | Tests use the service key (RLS bypass) for setup and a user JWT for the RLS-active assertions; helper is hard-guarded to localhost (threat T-1-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-T1 | Information Disclosure | adherence/balance views | mitigate | view-leak extension asserts user B reads 0 of A's rows (proves security_invoker) |
| T-03-T2 | Tampering / IDOR | reserva_id FK | mitigate | reserva-idor proves raw insert accepts a foreign id (RLS insufficient) AND the ownership select rejects it |
| T-03-T3 | Integrity | aporte double-count | mitigate | reserva-aporte pins that an aporte raises ONLY the alocação total (Pitfall 3) |
| T-03-T4 | Tampering | concurrent saída overdraw | mitigate | reserva-saida Promise.all assertion: saldo never < 0 (Pitfall 4) |
| T-03-SC | Tampering | test deps | accept | No new packages; tests reuse the vendored vitest + local-supabase helper |
</threat_model>

<verification>
- `SUPABASE_DISABLE_TELEMETRY=1 npx vitest run` runs the full suite (local stack up); the Phase-1/2 suite stays GREEN and the new migration-level tests pass.
- The action/UI-level assertions (direction-default, aporte via the action, IDOR via the action) are clearly RED-pending and reference the plan that turns them green.
- No watch-mode flags; feedback latency < 35s per 03-VALIDATION.
</verification>

<success_criteria>
- The nine Wave-0 files from 03-VALIDATION exist plus the adherence unit test; view-leak and rls-isolation extended.
- The allocation-grouping invariant (RSV-03) is pinned by a passing test against the Plan-01 view.
- 03-VALIDATION.md can be marked `wave_0_complete: true` / `nyquist_compliant: true`.
</success_criteria>

<output>
Create `.planning/phases/03-metas-ader-ncia-e-reservas/03-02-SUMMARY.md` when done
</output>
