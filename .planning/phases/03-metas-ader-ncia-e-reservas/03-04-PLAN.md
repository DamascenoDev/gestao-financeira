---
phase: 03-metas-ader-ncia-e-reservas
plan: 04
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - src/actions/reservas.ts
  - src/actions/reservas.test.ts
  - src/components/reserva-card.tsx
  - src/components/reserva-progress.tsx
  - src/components/reserva-form.tsx
  - src/components/saida-form.tsx
  - src/components/reserva-ledger-table.tsx
  - src/app/(app)/reservas/page.tsx
  - src/app/(app)/reservas/[id]/page.tsx
autonomous: true
requirements: [RSV-01, RSV-04, RSV-05]
user_setup: []

must_haves:
  truths:
    - "User creates a named reserva with an optional alvo; the progress bar appears ONLY when an alvo is set"
    - "Each reserva shows a saldo derived from the v_reserva_balance view (Σ in − Σ out), never computed or stored client-side"
    - "User registers a saída via the atomic RPC; a withdrawal greater than the saldo is rejected client-side AND server-side and never leaves a negative balance"
    - "User views a per-reserva ledger (entradas/saídas, newest first) with links back to the source transação when present"
  artifacts:
    - path: "src/actions/reservas.ts"
      provides: "createReserva/updateReserva/deleteReserva (RSV-01) + registerSaida via RPC (RSV-04) + assertOwnedReserva (IDOR)"
      exports: ["createReserva", "updateReserva", "deleteReserva", "registerSaida"]
    - path: "src/app/(app)/reservas/page.tsx"
      provides: "RSC reading reservas + v_reserva_balance — new nav surface"
      contains: "v_reserva_balance"
    - path: "src/app/(app)/reservas/[id]/page.tsx"
      provides: "per-reserva ledger detail + saldo + saída action"
      contains: "reserva_ledger"
    - path: "src/components/reserva-progress.tsx"
      provides: "wraps shadcn progress; renders ONLY when alvo_cents set"
      contains: "alvo"
    - path: "src/components/saida-form.tsx"
      provides: "valor ≤ saldo validated client + server"
      contains: "registerSaida"
  key_links:
    - from: "src/app/(app)/reservas/page.tsx"
      to: "public.v_reserva_balance"
      via: "RLS-scoped supabase.from('v_reserva_balance').select()"
      pattern: "v_reserva_balance"
    - from: "src/actions/reservas.ts"
      to: "register_reserva_saida RPC"
      via: "supabase.rpc('register_reserva_saida', …)"
      pattern: "register_reserva_saida"
    - from: "src/actions/reservas.ts"
      to: "reservas (ownership re-derive)"
      via: "assertOwnedReserva select id where id=$1 under RLS"
      pattern: "assertOwnedReserva"
---

<objective>
Vertical slice: the user can CREATE a named reserva (optional alvo), SEE its derived saldo and (when there is an alvo) a progress bar, REGISTER a validated saída that can never overdraw, and VIEW the per-reserva ledger history. Build the reservas Server Action (CRUD + registerSaida via the atomic RPC + assertOwnedReserva IDOR guard), the ReservaCard/ReservaProgress/ReservaForm/SaidaForm/ReservaLedgerTable components, the /reservas RSC, and the per-reserva detail/ledger route.

Purpose: Delivers RSV-01 (create reserva + optional alvo), RSV-04 (saída with history, never negative), RSV-05 (derived balance + conditional progress bar) as one end-to-end capability. The aporte sub-flow (RSV-02/03) that links a "Reserva" transaction into a reserva is the next slice (Plan 05), which imports ReservaForm and the reservas this plan creates. (D: CONTEXT Reservas decisions; UI-SPEC §3; Pattern 4/5; IDOR re-derive locked.)

Output: reservas action + test, five reserva components, the /reservas page + detail route.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-metas-ader-ncia-e-reservas/03-CONTEXT.md
@.planning/phases/03-metas-ader-ncia-e-reservas/03-UI-SPEC.md
@.planning/phases/03-metas-ader-ncia-e-reservas/03-RESEARCH.md

# Patterns to mirror:
@src/actions/categories.ts
@src/actions/transactions.ts
@src/lib/schemas/reserva.ts
@src/lib/money.ts
@src/app/(app)/receitas/page.tsx
@src/components/receita-form.tsx
@src/components/amount-cell.tsx
@src/components/extrato-table.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: reservas Server Action — CRUD + registerSaida via atomic RPC + assertOwnedReserva (IDOR)</name>
  <files>src/actions/reservas.ts, src/actions/reservas.test.ts</files>
  <read_first>src/actions/categories.ts (CRUD action shape: 'use server', idSchema uuid guard, getClaims, revalidatePath, discriminated results) + transactions.ts (assertOwnedCategories — clone it as assertOwnedReserva), src/lib/schemas/reserva.ts (reservaSchema + saidaSchema from Plan 01), src/lib/money.ts (parseBRLToCents throw-on-invalid), RESEARCH.md "Server Action: register saída via atomic RPC" + "Ownership helper for reservas" (exact skeletons), tests/reserva-idor.test.ts + reserva-saida.test.ts from Plan 02 (the contracts this action must satisfy).</read_first>
  <behavior>
    - createReserva({nome, alvo?}): inserts a reserva (alvo parsed via parseBRLToCents when present, else null) and returns { ok: true }; empty nome → Zod error; bad money → 'Valor monetário inválido.'
    - updateReserva(id, {nome, alvo?}): edits an owned reserva; setting alvo empty stores null (removes the progress bar); a forged id touches 0 rows (RLS).
    - deleteReserva(id): removes an owned reserva (ledger cascades).
    - registerSaida({reservaId, amount, occurredOn, note?}): parseBRLToCents(amount); getClaims; assertOwnedReserva(reservaId) → else 'Reserva inválida.'; then supabase.rpc('register_reserva_saida', {p_reserva_id, p_amount_cents, p_occurred_on, p_note}); an overdraw (RPC raises, message includes 'saldo') maps to 'A saída não pode ser maior que o saldo da reserva.'; any other error → 'Não foi possível registrar a saída.'; success revalidatePath('/reservas').
    - A forged/foreign reservaId is rejected by assertOwnedReserva before the RPC (the carried IDOR fix; the RPC is hardened too as backstop).
    - No session → 'Sessão expirada.'
  </behavior>
  <action>
Create src/actions/reservas.ts mirroring categories.ts/transactions.ts verbatim in structure. Add a private assertOwnedReserva(supabase, id): select id from reservas where id = $1 under the RLS-active client → exactly 1 row means owned, else false (verbatim clone of assertOwnedCategories applied to reserva_id — FKs are not RLS-aware, Phase-2 lesson). Implement createReserva/updateReserva/deleteReserva (Zod via reservaSchema, getClaims, parseBRLToCents for alvo, revalidatePath('/reservas')) and registerSaida (Zod via saidaSchema, parseBRLToCents for amount, assertOwnedReserva, then the rpc call with the friendly error mapping above). Do NOT do an app-side read-balance-then-insert — the RPC is the authoritative atomic guard (Pitfall 4); the action only re-derives ownership for a friendly error.

Write src/actions/reservas.test.ts mirroring categories.test.ts's mock-supabase style (chainable from().insert()/update()/delete()/select()/eq(), rpc(), getClaims()). Use RFC-4122 v4 UUID fixtures. Cover every behavior; for registerSaida mock the rpc to return both the success and the overdraw-error shapes and assert the error mapping. Assert assertOwnedReserva rejects when select returns 0 rows.
  </action>
  <verify>
    <automated>SUPABASE_DISABLE_TELEMETRY=1 npx vitest run reservas 2>&1 | tail -15</automated>
  </verify>
  <done>reservas.ts exports createReserva/updateReserva/deleteReserva/registerSaida; assertOwnedReserva guards every FK/RPC write; registerSaida maps the overdraw error to the friendly copy; the test file is GREEN; tsc clean. The Plan-02 reserva-idor ownership-check assertion holds against this action's guard.</done>
</task>

<task type="auto">
  <name>Task 2: Reserva components — Card, Progress, Form, SaidaForm, LedgerTable</name>
  <files>src/components/reserva-card.tsx, src/components/reserva-progress.tsx, src/components/reserva-form.tsx, src/components/saida-form.tsx, src/components/reserva-ledger-table.tsx</files>
  <read_first>src/components/receita-form.tsx (manual-state + useTransition + sonner dialog to mirror in ReservaForm/SaidaForm), src/components/amount-cell.tsx (entrada green +, saída neutral −), src/components/money-input.tsx (MoneyInput + isValidMoney), src/components/ui/progress.tsx (the Plan-01 vendored component), UI-SPEC §3 + §"Component Inventory" for ReservaCard/ReservaProgress/ReservaLedgerTable/ReservaForm/SaidaForm contracts + the Copywriting Contract for exact pt-BR strings.</read_first>
  <action>
reserva-progress.tsx (ReservaProgress, RSV-05): wraps the shadcn `progress`. Renders ONLY when alvo_cents != null — when there is no alvo, render NOTHING (not a 0% bar). Shows saldo/alvo + % (mono, tabular-nums). Fill --primary, switching to --income at ≥100% with an "Alvo atingido" badge. role="progressbar" + aria-valuenow/min/max + aria-valuetext.

reserva-card.tsx (ReservaCard): a --card surface — nome · saldo as a mono hero (AmountCell neutral, ALWAYS the view-derived value, never client-computed) · ReservaProgress (only if alvo) · a dropdown-menu of actions (Registrar saída → SaidaForm dialog, Ver extrato → link to /reservas/[id], Editar → ReservaForm dialog, Excluir → alert-dialog with the "Esta reserva tem {n} movimentos…" copy).

reserva-form.tsx (ReservaForm): create/edit dialog mirroring receita-form — nome (input, required), alvo (MoneyInput, optional). Editing alvo to empty removes the progress bar. Calls createReserva/updateReserva; sonner "Reserva criada". Export it so Plan 05's ReservaPicker can open "+ Nova reserva".

saida-form.tsx (SaidaForm, RSV-04): dialog — valor (MoneyInput), data (popover + calendar), descrição (optional). Validate valor ≤ saldo atual BOTH client-side (block submit + inline FieldError "A saída não pode ser maior que o saldo da reserva ({R$}).") AND server-side (registerSaida is authoritative). On success sonner "Saída registrada". The ≤ saldo error is a FieldError (aria-invalid + text), never just a disabled button.

reserva-ledger-table.tsx (ReservaLedgerTable): a `table` — Data (dd/MM) · Tipo (badge Entrada/Saída) · Descrição · Valor (AmountCell: entrada green +, saída neutral −) · vínculo (link to the source transação when transaction_id set). Newest first. Mobile collapses to one card per movimento (mirror the Extrato mobile pattern).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "alvo" src/components/reserva-progress.tsx && grep -q "registerSaida" src/components/saida-form.tsx && grep -q "createReserva" src/components/reserva-form.tsx</automated>
  </verify>
  <done>Five components compile; ReservaProgress renders only with an alvo; SaidaForm validates ≤ saldo client+server with a FieldError; ReservaCard shows the view-derived saldo; ReservaForm is exported for Plan 05; LedgerTable shows entrada/saída with txn links.</done>
</task>

<task type="auto">
  <name>Task 3: /reservas list RSC + per-reserva ledger detail route</name>
  <files>src/app/(app)/reservas/page.tsx, src/app/(app)/reservas/[id]/page.tsx</files>
  <read_first>src/app/(app)/receitas/page.tsx (RSC: createClient, RLS-scoped view read, Empty primitive, error state), UI-SPEC §3 (list states, the empty copy, the detail panel contract), src/components/reserva-card.tsx + reserva-ledger-table.tsx (this task's consumers from Task 2).</read_first>
  <action>
reservas/page.tsx (RSC, new nav surface — the sidebar item was added in Plan 01): h1 "Reservas" + a "Nova reserva" teal CTA (ReservaForm dialog). Read the user's reservas joined with v_reserva_balance (RLS-scoped — the saldo ALWAYS comes from the view, never computed here). Render a responsive grid of ReservaCard. States per UI-SPEC: Empty-no-reservas (Empty primitive "Nenhuma reserva ainda" + the "Crie uma reserva (ex.: Apê, Carro)…" body + "Nova reserva" CTA); Loading (skeleton cards); Error (inline text-destructive "Não foi possível carregar as reservas. Tente recarregar a página.").

reservas/[id]/page.tsx (RSC detail/ledger): read the one reserva + its v_reserva_balance saldo + its reserva_ledger rows (RLS-scoped, newest first). Header with nome + saldo + alvo progress; then ReservaLedgerTable; primary action "Registrar saída" (SaidaForm). Empty-ledger state: "Nenhum movimento nesta reserva ainda. Um lançamento na categoria Reserva cria a primeira entrada." A not-found / not-owned id renders a clean not-found (RLS returns no row).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "v_reserva_balance" "src/app/(app)/reservas/page.tsx" && grep -q "reserva_ledger" "src/app/(app)/reservas/[id]/page.tsx" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>/reservas and /reservas/[id] compile and build; the saldo ALWAYS comes from v_reserva_balance (never client-computed); list + detail render all states; the detail ledger lists movimentos newest-first with txn links.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → registerSaida / createReserva | reserva_id + money strings cross here; validated by Zod + assertOwnedReserva |
| registerSaida → register_reserva_saida RPC | the atomic never-negative guard (balance read + insert in one transaction) |
| RSC → v_reserva_balance | RLS-scoped read; the view is security_invoker |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Tampering / IDOR | reserva_ledger.reserva_id | mitigate | assertOwnedReserva re-derives ownership before the RPC; the RPC reads the balance under the caller's RLS so a foreign reserva yields null (Pitfall 6); pinned by reserva-idor (Plan 02) |
| T-03-04-02 | Tampering / Integrity | concurrent / over-balance saída | mitigate | register_reserva_saida RPC: balance read + insert in one function body → never-negative, race-safe (Pitfall 4); pinned by reserva-saida |
| T-03-04-03 | Integrity | client-computed/stored balance | mitigate | saldo ALWAYS from v_reserva_balance (Σ in − Σ out); the UI never computes or mutates it (RSV-05) |
| T-03-04-04 | Information Disclosure | v_reserva_balance / reservas / reserva_ledger | mitigate | security_invoker view + RLS on both tables; RSC reads RLS-scoped; leak + RLS tests (Plan 02) |
| T-03-04-SC | Tampering | deps | accept | No new packages; reuses the Plan-01 vendored `progress` + existing shadcn |
</threat_model>

<verification>
- `npx vitest run reservas` GREEN; the Plan-02 reserva-idor / reserva-saida action assertions hold against this action.
- `npx tsc --noEmit` clean; `npm run build` compiles /reservas and /reservas/[id].
- Manual (Plan 06 walkthrough): create reserva with + without alvo (bar only when alvo), register a saída over the saldo (blocked client + server), confirm derived saldo.
</verification>

<success_criteria>
- reservas action: CRUD + registerSaida via the atomic RPC + assertOwnedReserva IDOR guard, tested.
- Five components: ReservaProgress conditional on alvo, SaidaForm ≤-saldo validated client+server, ReservaCard view-derived saldo, ReservaForm exported for Plan 05, LedgerTable.
- /reservas + detail route reading reservas + v_reserva_balance + reserva_ledger RLS-scoped.
- RSV-01/04/05 delivered end-to-end.
</success_criteria>

<output>
Create `.planning/phases/03-metas-ader-ncia-e-reservas/03-04-SUMMARY.md` when done
</output>
