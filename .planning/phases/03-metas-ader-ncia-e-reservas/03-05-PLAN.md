---
phase: 03-metas-ader-ncia-e-reservas
plan: 05
type: execute
wave: 4
depends_on: ["03-01", "03-02", "03-04"]
files_modified:
  - src/actions/transactions.ts
  - src/actions/transactions.test.ts
  - src/components/reserva-picker.tsx
  - src/components/transacao-form.tsx
  - src/components/extrato-table.tsx
autonomous: true
requirements: [RSV-02, RSV-03]
user_setup: []

must_haves:
  truths:
    - "Classifying a transaction into the is_reserva category reveals a 'Qual reserva?' picker and, on submit, creates a linked 'in' ledger entry in the chosen reserva (RSV-02)"
    - "An aporte counts as an investment allocation (it raises the alocação total on the dashboard) and never as a consumo spend (RSV-03)"
    - "The chosen reserva_id is validated server-side by owner before the ledger write (carried IDOR fix)"
    - "Re-classifying a transaction away from Reserva (or deleting it) removes its linked ledger entry so the balance re-derives — no silent orphan"
  artifacts:
    - path: "src/actions/transactions.ts"
      provides: "createTransactionWithReserva + syncReservaLedgerForTransaction helper + aporte-undo in updateTransaction/deleteTransaction"
      exports: ["createTransactionWithReserva"]
    - path: "src/components/reserva-picker.tsx"
      provides: "'qual reserva?' select + inline '+ Nova reserva' (opens ReservaForm)"
      contains: "ReservaPicker"
    - path: "src/components/transacao-form.tsx"
      provides: "conditional ReservaPicker when categoria is is_reserva (progressive disclosure)"
      contains: "ReservaPicker"
  key_links:
    - from: "src/components/transacao-form.tsx"
      to: "createTransactionWithReserva"
      via: "submit when categoria is_reserva sends reservaId"
      pattern: "createTransactionWithReserva"
    - from: "src/actions/transactions.ts"
      to: "reserva_ledger (linked 'in' entry)"
      via: "insert { reserva_id, kind:'in', transaction_id } after ownership re-derive"
      pattern: "reserva_ledger"
    - from: "src/actions/transactions.ts"
      to: "reservas (ownership re-derive)"
      via: "assertOwnedReserva before the ledger write"
      pattern: "assertOwnedReserva"
---

<objective>
Vertical slice that closes the reserva loop: when a transaction is classified into the seed "Reserva" category (identified by the is_reserva flag, not the name), the user is asked WHICH reserva, and the action writes both the transaction and a linked `in` ledger entry — an aporte that counts as investment allocation and never as consumo spend. Build createTransactionWithReserva + the syncReservaLedgerForTransaction helper (which also handles edit/undo), the ReservaPicker component, the conditional picker inside transacao-form, and the inline-re-tag dialog in the Extrato.

Purpose: Delivers RSV-02 (Reserva transaction → "qual reserva?" → linked ledger entry) and RSV-03 (aporte = investment allocation, never consumption). This is the final functional slice; after it the Phase-3 success criteria 4 and 5 are met end-to-end. (D: CONTEXT aporte-via-action + is_reserva flag; UI-SPEC §4; Pattern 6; Open Question 3 → syncReservaLedgerForTransaction; Open Question 2 → is_reserva flag.)

Output: extended transactions action + test, the ReservaPicker, the conditional picker in transacao-form, the inline-re-tag dialog in the Extrato.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-metas-ader-ncia-e-reservas/03-CONTEXT.md
@.planning/phases/03-metas-ader-ncia-e-reservas/03-UI-SPEC.md
@.planning/phases/03-metas-ader-ncia-e-reservas/03-RESEARCH.md

# Patterns + integration points:
@src/actions/transactions.ts
@src/actions/transactions.test.ts
@src/components/transacao-form.tsx
@src/components/extrato-table.tsx
@src/components/reserva-form.tsx
@src/lib/schemas/transaction.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: createTransactionWithReserva + syncReservaLedgerForTransaction + aporte-undo</name>
  <files>src/actions/transactions.ts, src/actions/transactions.test.ts</files>
  <read_first>src/actions/transactions.ts (extend, do not rewrite — reuse assertOwnedCategories, the Zod/getClaims/parseBRLToCents/revalidatePath boilerplate; createTransaction/updateTransaction/deleteTransaction are the methods to wrap), src/actions/transactions.test.ts (the mock-supabase test style + UUID fixtures to extend), RESEARCH.md "Pattern 6: Aporte from a Reserva transaction" (the exact action skeleton + the assertOwnedReserva clone) + Open Question 3 (the syncReservaLedgerForTransaction recommendation), src/actions/reservas.ts (assertOwnedReserva from Plan 04 — import or re-derive the same RLS-scoped ownership check).</read_first>
  <behavior>
    - createTransactionWithReserva(formData incl. reservaId?): inserts the transaction; if the category is the user's is_reserva category, requires reservaId (else 'Selecione uma reserva.'), validates ownership (else 'Reserva inválida.'), and inserts a linked reserva_ledger { reserva_id, kind:'in', amount_cents, transaction_id, occurred_on }.
    - An aporte raises the reserva's saldo and the dashboard alocação total; it never records a consumo spend (kind='alocacao' category → never in a consumo teto — pinned by the Plan-02 reserva-aporte test).
    - A non-Reserva category path behaves exactly like the existing createTransaction (no ledger entry).
    - A forged/foreign reservaId is rejected before the ledger write (assertOwnedReserva).
    - updateTransaction: when the new category is is_reserva, sync a fresh 'in' ledger entry; when it is NOT, delete any existing entry for that transaction (the edit/undo path — balance re-derives, no orphan). The unique(transaction_id) index keeps the re-link idempotent.
    - deleteTransaction: explicitly delete any linked ledger entry so the balance drops immediately.
  </behavior>
  <action>
Extend src/actions/transactions.ts. Add a private helper isReservaCategory(supabase, categoryId): select is_reserva from categories where id = $1 under RLS → true when the owned category's is_reserva flag is set (keys off the FLAG from Plan 01's migration, never the literal name — CAT-02 lets the user rename). Add assertOwnedReserva (clone of assertOwnedCategories applied to reservas, or import it from actions/reservas.ts).

Add a private syncReservaLedgerForTransaction(supabase, userId, txnId, categoryId, amountCents, occurredOn, reservaId?): delete any existing reserva_ledger row where transaction_id = txnId; if isReservaCategory(categoryId), require + ownership-check reservaId, then insert a fresh { user_id, reserva_id, kind:'in', amount_cents, transaction_id: txnId, occurred_on }. The unique(transaction_id) index makes this idempotent. This single helper is called by both createTransactionWithReserva and updateTransaction so the edit/undo and create paths share one consistent code path (Open Question 3 resolution).

Add createTransactionWithReserva(formData): the existing Zod/parseBRLToCents/getClaims/assertOwnedCategories flow, then insert the transaction returning its id, then call syncReservaLedgerForTransaction. revalidatePath('/extrato'), '/reservas', '/dashboard'. Extend updateTransaction to call syncReservaLedgerForTransaction after the update (delete-old + maybe-insert-new) and to revalidate the same three paths. Extend deleteTransaction to delete any linked ledger entry for the txn before/with the transaction delete. Keep createTransaction (non-reserva path) intact for callers that never touch Reserva, OR route everything through createTransactionWithReserva — keep the existing createTransaction export so transacao-form's non-reserva path and existing tests stay valid.

Extend src/actions/transactions.test.ts: add cases for the aporte path (Reserva category + reservaId → transaction insert + ledger insert with kind:'in' and the transaction_id link), the missing-reservaId rejection, the forged-reservaId rejection, the non-Reserva path (no ledger write), and the undo path (re-classify away from Reserva deletes the ledger entry). Mock isReservaCategory via the categories select returning { is_reserva: true/false }. Use RFC-4122 v4 UUID fixtures. The existing 14 transaction tests must stay GREEN.
  </action>
  <verify>
    <automated>SUPABASE_DISABLE_TELEMETRY=1 npx vitest run transactions 2>&1 | tail -15</automated>
  </verify>
  <done>createTransactionWithReserva inserts the transaction + a linked 'in' ledger entry for an is_reserva category, rejects missing/forged reservaId, leaves the non-Reserva path unchanged; updateTransaction/deleteTransaction keep the ledger consistent (delete-old, maybe-insert-new); the existing 14 tests + new aporte/undo tests are GREEN; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: ReservaPicker + conditional picker in transacao-form + inline-re-tag dialog in the Extrato</name>
  <files>src/components/reserva-picker.tsx, src/components/transacao-form.tsx, src/components/extrato-table.tsx</files>
  <read_first>src/components/transacao-form.tsx (the manual-state form to extend with a conditional field — the categoryId state already exists), src/components/extrato-table.tsx (InlineCategoryCell — where an inline re-tag to Reserva must open a focused picker dialog), src/components/reserva-form.tsx (ReservaForm from Plan 04, opened by the picker's "+ Nova reserva"), UI-SPEC §4 (the full sub-flow contract: progressive disclosure inside the dialog, focused dialog on inline re-tag, the picker contract + copy, accounting note).</read_first>
  <action>
reserva-picker.tsx (ReservaPicker): a `select` of the user's reservas (by nome), required when the categoria is Reserva, label "Qual reserva?", helper "Este lançamento será registrado como aporte nesta reserva." Includes an inline "+ Nova reserva" affordance that opens ReservaForm (from Plan 04) without losing the transação in progress — on create, return to the picker with the new reserva pre-selected. If no reserva exists yet, show an inline empty state with the "+ Nova reserva" affordance and block submit until one is chosen/created. Required-field a11y (accessible name "Qual reserva?"); the conditional reveal is announced, not a silent DOM swap. The picker needs the list of reservas + a flag identifying whether the selected category is the is_reserva one — pass both from the parent.

transacao-form.tsx (extend, RSV-02): the form already tracks categoryId. Pass the categories enriched with an is_reserva flag and the user's reservas. When the selected category is is_reserva, render the ReservaPicker as a CONDITIONAL field BELOW the categoria select (progressive disclosure inside the SAME dialog — not a second modal). On submit, when the category is is_reserva, route to createTransactionWithReserva with the reservaId in the FormData; otherwise keep the existing createTransaction/updateTransaction path. Block submit if Reserva is selected but no reservaId is chosen. Keep the existing button copy ("Adicionar"/"Salvar").

extrato-table.tsx (extend InlineCategoryCell): when an inline re-tag selects the is_reserva category (there is no open dialog here), open a small focused `dialog` containing just the ReservaPicker; on confirm call createTransactionWithReserva-equivalent path (or updateTransaction routed through the picker) so the aporte entry is created/linked. Re-tagging AWAY from Reserva uses the existing updateTransaction, which now removes the linked ledger entry (Task 1). Surface the re-derived saldo honestly — no silent orphan.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "ReservaPicker" src/components/reserva-picker.tsx && grep -q "ReservaPicker" src/components/transacao-form.tsx && grep -q "createTransactionWithReserva\|ReservaPicker" src/components/extrato-table.tsx && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>ReservaPicker renders the "Qual reserva?" select + "+ Nova reserva"; transacao-form reveals it conditionally for the is_reserva category and routes to createTransactionWithReserva; the Extrato inline re-tag opens a focused picker dialog; tsc clean; build compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → createTransactionWithReserva | reserva_id + category_id + money cross here; validated by Zod + assertOwnedCategories + assertOwnedReserva |
| transaction insert → reserva_ledger | the linked 'in' entry; the unique(transaction_id) index enforces one aporte per txn |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Tampering / IDOR | reserva_ledger.reserva_id | mitigate | assertOwnedReserva re-derives ownership before the ledger write; the is_reserva check + ownership are server-side (Pitfall 6); pinned by reserva-idor / reserva-aporte (Plan 02) |
| T-03-05-02 | Integrity | aporte double-counted | mitigate | the aporte is kind='alocacao' (is_reserva category); it lands ONLY in the alocação total, never a consumo line (Pitfall 3); pinned by reserva-aporte |
| T-03-05-03 | Integrity | orphaned ledger entry on edit/undo | mitigate | syncReservaLedgerForTransaction deletes-old + maybe-inserts-new; deleteTransaction removes the linked entry; unique(transaction_id) keeps re-link idempotent (Open Question 3) |
| T-03-05-04 | Tampering | name-match brittleness | mitigate | the aporte trigger keys off the is_reserva FLAG, never the literal name (CAT-02 rename-safe; Open Question 2) |
| T-03-05-SC | Tampering | deps | accept | No new packages; reuses Plan-04 ReservaForm + existing shadcn |
</threat_model>

<verification>
- `npx vitest run transactions` GREEN (existing 14 + new aporte/undo tests); the Plan-02 reserva-aporte test passes end-to-end (aporte raises alocação, never consumo).
- `npx tsc --noEmit` clean; `npm run build` compiles.
- Manual (Plan 06 walkthrough): log a Reserva transaction, confirm the picker appears and links; re-classify away and confirm the saldo re-derives.
</verification>

<success_criteria>
- createTransactionWithReserva + syncReservaLedgerForTransaction: aporte path creates a linked 'in' entry, rejects missing/forged reservaId, keeps edit/undo consistent.
- ReservaPicker + conditional disclosure in transacao-form + focused dialog on inline Extrato re-tag.
- The aporte counts as alocação (never consumo) and keys off the is_reserva flag.
- RSV-02/03 delivered end-to-end; Phase-3 success criteria 4 and 5 met.
</success_criteria>

<output>
Create `.planning/phases/03-metas-ader-ncia-e-reservas/03-05-SUMMARY.md` when done
</output>
