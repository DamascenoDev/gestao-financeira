---
phase: 03-metas-ader-ncia-e-reservas
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - src/actions/budget-targets.ts
  - src/actions/budget-targets.test.ts
  - src/components/adherence-bar.tsx
  - src/components/adherence-row.tsx
  - src/components/adherence-summary-strip.tsx
  - src/components/meta-dialog.tsx
  - src/app/(app)/dashboard/page.tsx
autonomous: true
requirements: [BUD-01, BUD-02, BUD-03, BUD-04]
user_setup: []

must_haves:
  truths:
    - "User sets a % meta per category with a direction (teto/alvo) defaulting from the category kind; one meta per category (upsert)"
    - "Dashboard /dashboard shows monthly adherence (realized X% vs meta Y% per category) with direction-aware semantic color and an 80%/100% alert affordance"
    - "Dashboard shows an Anual (YTD) view from the same ledger that reads visibly consistent with the monthly view"
    - "Alocação categories (Investimentos + Reserva) render as one combined line; an aporte counts there, never in a consumo line"
    - "No-income period shows the 'sem receita' copy, never NaN%"
  artifacts:
    - path: "src/actions/budget-targets.ts"
      provides: "upsertBudgetTarget (BUD-01, IDOR-checked category ownership) + deleteBudgetTarget"
      exports: ["upsertBudgetTarget", "deleteBudgetTarget"]
    - path: "src/app/(app)/dashboard/page.tsx"
      provides: "RSC reading v_adherence_month (?mes) + v_adherence_ytd (civil year) — replaces the placeholder"
      contains: "v_adherence_month"
    - path: "src/components/adherence-bar.tsx"
      provides: "custom direction-aware bar with meta-marker tick + aria progressbar"
      contains: "progressbar"
    - path: "src/components/adherence-row.tsx"
      provides: "per-category row: badge + bar + R$ + % + meta% + status label/glyph"
      contains: "AdherenceRow"
    - path: "src/components/meta-dialog.tsx"
      provides: "% input + Teto/Alvo switch + live R$ preview, calls upsertBudgetTarget"
      contains: "upsertBudgetTarget"
  key_links:
    - from: "src/app/(app)/dashboard/page.tsx"
      to: "public.v_adherence_month / public.v_adherence_ytd"
      via: "RLS-scoped supabase.from(view).select()"
      pattern: "v_adherence_(month|ytd)"
    - from: "src/components/meta-dialog.tsx"
      to: "upsertBudgetTarget action"
      via: "useTransition form submit"
      pattern: "upsertBudgetTarget"
    - from: "src/actions/budget-targets.ts"
      to: "categories (ownership re-derive)"
      via: "select id where id = categoryId under RLS before upsert"
      pattern: "categories"
---

<objective>
Vertical slice: the user can DEFINE a per-category % meta with a direction and immediately SEE adherence on the dashboard — the product's core-value screen. Build the budget_targets Server Action (Zod + getClaims + category-ownership re-derive + unique-upsert), the MetaDialog (% input, Teto/Alvo switch defaulting from kind, live R$ preview), the custom AdherenceBar/AdherenceRow/AdherenceSummaryStrip components, and the real dashboard RSC that reads v_adherence_month for ?mes and v_adherence_ytd for the civil year, replacing the Phase-1 placeholder.

Purpose: Delivers BUD-01 (define meta), BUD-02 (monthly adherence), BUD-03 (YTD consistency), BUD-04 (80/100 alerts) as one end-to-end user capability. After this plan a user with income + transactions sets a meta and sees their aderência. (D: CONTEXT Aderência/Dashboard decisions; UI-SPEC §1-2; Open Question 1 grouping rendered as the combined alocação line.)

Output: budget-targets action + test, four dashboard components, the real /dashboard page.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-metas-ader-ncia-e-reservas/03-CONTEXT.md
@.planning/phases/03-metas-ader-ncia-e-reservas/03-UI-SPEC.md
@.planning/phases/03-metas-ader-ncia-e-reservas/03-RESEARCH.md

# Patterns to mirror:
@src/actions/transactions.ts
@src/lib/schemas/budget-target.ts
@src/lib/adherence.ts
@src/lib/month.ts
@src/lib/money.ts
@src/app/(app)/receitas/page.tsx
@src/components/transacao-form.tsx
@src/components/category-badge.tsx
@src/components/amount-cell.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: budget-targets Server Action (BUD-01) with IDOR-checked ownership + unique-upsert</name>
  <files>src/actions/budget-targets.ts, src/actions/budget-targets.test.ts</files>
  <read_first>src/actions/transactions.ts (the 'use server' + safeParse → {error} + getClaims + assertOwnedCategories pattern; the moneyWriteError/DbError style), src/actions/categories.test.ts (the action-test mock-supabase style with .upsert/.select/.in), src/lib/schemas/budget-target.ts (budgetTargetSchema from Plan 01), RESEARCH.md "Server Action: upsert budget target" (exact skeleton).</read_first>
  <behavior>
    - upsertBudgetTarget with a valid {categoryId, percentBp, direction} owned by the caller upserts on (user_id, category_id) and returns { ok: true }.
    - A second call for the same category UPDATES the existing meta (one meta per category), not a duplicate.
    - A forged/foreign categoryId is rejected with 'Categoria inválida.' (ownership re-derive returns 0 rows) — never written.
    - percentBp outside 0<bp<=10000, a non-uuid categoryId, or a bad direction → a friendly Zod error, no write.
    - No session → 'Sessão expirada.'
    - deleteBudgetTarget(categoryId) removes the meta for an owned category and returns { ok: true }.
  </behavior>
  <action>
Create src/actions/budget-targets.ts mirroring transactions.ts verbatim in structure: 'use server'; import revalidatePath, the budgetTargetSchema, createClient; export type ActionResult. upsertBudgetTarget(input): safeParse(budgetTargetSchema) → {error} on failure; getClaims() → userId else 'Sessão expirada.'; re-derive category ownership server-side (select id from categories where id = categoryId under the RLS-active client → exactly 1 row, else 'Categoria inválida.') — this is the carried Phase-2 IDOR fix (FKs are not RLS-aware); then supabase.from('budget_targets').upsert({ user_id, category_id, percent_bp, direction, updated_at }, { onConflict: 'user_id,category_id' }); on error return 'Não foi possível salvar a meta.'; revalidatePath('/dashboard'); return { ok: true }. Add deleteBudgetTarget(categoryId): validate uuid, getClaims, delete from budget_targets where category_id = categoryId (RLS scopes to owner), revalidatePath('/dashboard').

Write src/actions/budget-targets.test.ts mirroring categories.test.ts's mock-supabase approach (mock createClient with a chainable from().upsert()/select()/eq()/delete() and getClaims()). Use RFC-4122 v4 UUID fixtures (zod v4 .uuid() validates version/variant — the Phase-2 lesson). Cover every behavior above. Do NOT inline SQL or code blocks beyond the test file itself.
  </action>
  <verify>
    <automated>SUPABASE_DISABLE_TELEMETRY=1 npx vitest run budget-targets 2>&1 | tail -15</automated>
  </verify>
  <done>upsertBudgetTarget upserts on conflict, rejects foreign category_id, validates percentBp/direction, gates on session; deleteBudgetTarget works; the test file is GREEN; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: AdherenceBar/Row/SummaryStrip + MetaDialog</name>
  <files>src/components/adherence-bar.tsx, src/components/adherence-row.tsx, src/components/adherence-summary-strip.tsx, src/components/meta-dialog.tsx</files>
  <read_first>src/lib/adherence.ts (status mapper + status→token map + percent formatter from Plan 01), src/lib/money.ts (formatCents, parseBRLToCents, centsToEditableBRL), src/components/transacao-form.tsx (manual-state + useTransition + sonner dialog pattern to mirror in MetaDialog), src/components/category-badge.tsx + amount-cell.tsx (reuse verbatim), UI-SPEC §"Semantic Color", §"Component Inventory", §1-2 for exact contracts, copy, and the direction-aware color table.</read_first>
  <action>
adherence-bar.tsx (custom, NOT shadcn progress — UI-SPEC Charting Decision): an h-2 track (bg-muted) + a direction-aware fill whose width clamps at 100% (but the % label may read >100%) + a meta-marker tick at the meta% position. The fill token comes from lib/adherence.ts's status→token map (teto: consumption/destructive; alvo: muted/allocation/income). Expose role="progressbar" + aria-valuenow/min/max + aria-valuetext (e.g. "Alimentação: 85% da meta de 30%, aproximando do teto"). Color is never the sole signal — the row pairs it with a text label + glyph.

adherence-row.tsx (AdherenceRow): one per category — CategoryBadge (name + dot + kind) · AdherenceBar · realized R$ (AmountCell neutral) · realized % (mono, status color via the formatter) · meta Y% (text-muted-foreground) · status label (No limite/Aproximando/Estourou for teto; Abaixo/Quase lá/Atingido for alvo) + lucide glyph (triangle-alert ≥80, octagon-alert teto ≥100, check alvo ≥100). Percentages mono, tabular-nums, maximumFractionDigits 1. When adherence_bp is null render the "sem receita no período" copy, never NaN%. The alocação line is the combined Investimentos+Reserva row with a tooltip/helper "Inclui aportes de reserva."

adherence-summary-strip.tsx (AdherenceSummaryStrip): período label · receita líquida do período as a 28px mono hero (text-income) · a terse count ("2 categorias estouradas · 1 meta atingida").

meta-dialog.tsx (MetaDialog, BUD-01): mirror transacao-form's manual-state + useTransition + toast pattern. A surface listing the user's categories, each with: CategoryBadge, a % numeric input (0–100, one decimal → convert to percentBp on submit), a Teto↔Alvo `switch` defaulting from the category kind (consumo→teto, alocacao→alvo; user-editable), and a live R$ preview = formatCents(Math.round(incomeCents * percentBp / 10000)) recomputed as the user types. Soft-warn (not block) when the SUM of teto metas > 100% ("Suas metas de teto somam mais de 100% da receita."). On save call upsertBudgetTarget; sonner success "Meta de {categoria} salva"; clearing the % calls deleteBudgetTarget. All copy pt-BR per UI-SPEC Copywriting Contract.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "progressbar" src/components/adherence-bar.tsx && grep -q "upsertBudgetTarget" src/components/meta-dialog.tsx && grep -q "AdherenceRow\|adherence" src/components/adherence-row.tsx</automated>
  </verify>
  <done>Four components compile type-clean; AdherenceBar exposes progressbar a11y + meta tick; MetaDialog wires to upsertBudgetTarget with the kind-default switch + live R$ preview; status color/label/glyph driven by lib/adherence.ts.</done>
</task>

<task type="auto">
  <name>Task 3: Real dashboard RSC reading the adherence views (replaces placeholder)</name>
  <files>src/app/(app)/dashboard/page.tsx</files>
  <read_first>src/app/(app)/receitas/page.tsx (the RSC pattern: await searchParams, toMonthKeyOrCurrent(params.mes), createClient, RLS-scoped view reads, Empty primitive, error/empty states), src/lib/month.ts (toMonthKeyOrCurrent, monthLabel, currentYear, yearBounds), UI-SPEC §1 (period tabs Mensal/Anual, summary strip, per-category list, all four states, alocação grouping note), src/components/month-selector.tsx context (the global ?mes drives Mensal).</read_first>
  <action>
Replace the placeholder dashboard/page.tsx with the real core-value screen (RSC). Read ?mes via toMonthKeyOrCurrent. h1 "Metas e aderência" + a "Definir metas" teal CTA opening MetaDialog (pass the user's non-archived categories + the period's receita líquida for the live preview). Period toggle (`tabs`): Mensal (default, reads v_adherence_month filtered to month_key = ?mes) and Anual (YTD) (reads v_adherence_ytd filtered to the current civil year via currentYear()); label the YTD window "Acumulado de {ano} (jan–{mês})". Both views read RLS-scoped (the server client runs under the user's JWT). Render AdherenceSummaryStrip + one AdherenceRow per category that has a meta, ordered consumo-then-alocação and STABLE across both tabs; render the alocação categories as one combined line (Investimentos + Reserva) per Open Question 1.

States per UI-SPEC: Empty-no-metas (Empty primitive, "Você ainda não definiu metas" + "Definir metas" CTA); Empty-no-receita ("Sem receita líquida em {período}…" with a link to /receitas — the division-by-zero guard surfaced as copy, never NaN%); Loading (skeleton rows keeping header + tabs); Error (inline text-destructive "Não foi possível carregar a aderência. Tente recarregar a página."). The dashboard IS the alert surface (BUD-04) — no toast/notification system; alerts are the inline badges/glyphs on each row plus the summary count.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "v_adherence_month" "src/app/(app)/dashboard/page.tsx" && grep -q "v_adherence_ytd" "src/app/(app)/dashboard/page.tsx" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>/dashboard compiles and builds; reads both adherence views RLS-scoped; renders Mensal + Anual tabs, summary strip, per-category rows with the combined alocação line, and all four states; no-income shows the sem-receita copy, never NaN%.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → upsertBudgetTarget | percentBp + category_id cross here; validated by Zod + ownership re-derive |
| RSC → adherence views | RLS-scoped read under the user's JWT; views are security_invoker |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering / IDOR | budget_targets.category_id | mitigate | upsertBudgetTarget re-derives category ownership (select id where id=$1 under RLS) before the FK write (Phase-2 lesson); pinned by budget-targets.test + reserva-idor analog |
| T-03-03-02 | Information Disclosure | v_adherence_month/_ytd | mitigate | views are security_invoker (Plan 01); RSC read is RLS-scoped; leak test (Plan 02) |
| T-03-03-03 | Integrity | NaN%/Infinity% | mitigate | view nullif/case guard + adherence.ts null→'sem-receita' + dashboard copy (Pitfall 2) |
| T-03-03-04 | Integrity | aporte in a consumo line | mitigate | alocação rendered as the combined kind='alocacao' line; per Plan-01 view grouping (Pitfall 3) |
| T-03-03-SC | Tampering | deps | accept | No new packages; reuses vendored shadcn + lucide |
</threat_model>

<verification>
- `npx vitest run budget-targets` GREEN; the Plan-02 budget-target-direction action-default half turns GREEN now.
- `npx tsc --noEmit` clean; `npm run build` compiles /dashboard.
- Manual (Plan 06 walkthrough): set metas, log data, confirm teto red-over / alvo green-at-target and the YTD↔Mensal consistency.
</verification>

<success_criteria>
- upsertBudgetTarget: Zod + getClaims + ownership re-derive + unique-upsert; deleteBudgetTarget present.
- Dashboard replaces the placeholder, reads both views RLS-scoped, renders direction-aware rows + alerts + the combined alocação line + all four states.
- BUD-01/02/03/04 delivered end-to-end.
</success_criteria>

<output>
Create `.planning/phases/03-metas-ader-ncia-e-reservas/03-03-SUMMARY.md` when done
</output>
