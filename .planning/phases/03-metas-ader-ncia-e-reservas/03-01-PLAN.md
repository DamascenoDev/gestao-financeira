---
phase: 03-metas-ader-ncia-e-reservas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0011_budget_targets.sql
  - supabase/migrations/0012_categories_is_reserva.sql
  - supabase/migrations/0013_reservas.sql
  - supabase/migrations/0014_adherence_views.sql
  - supabase/migrations/0015_reserva_balance_view.sql
  - supabase/migrations/0016_register_reserva_saida.sql
  - src/types/database.types.ts
  - src/lib/month.ts
  - src/lib/adherence.ts
  - src/lib/schemas/budget-target.ts
  - src/lib/schemas/reserva.ts
  - src/components/ui/progress.tsx
  - src/components/app-sidebar.tsx
autonomous: true
requirements: [BUD-01, BUD-02, BUD-03, BUD-04, RSV-01, RSV-02, RSV-03, RSV-04, RSV-05]
user_setup: []

must_haves:
  truths:
    - "Migrations 0011-0016 are applied to the LOCAL stack and src/types/database.types.ts reflects budget_targets, reservas, reserva_ledger, the three new views, categories.is_reserva, and register_reserva_saida"
    - "The seed 'Reserva' category carries is_reserva=true on every new signup AND on existing rows (backfill); the aporte trigger keys off this flag, never the literal name"
    - "v_adherence_month, v_adherence_ytd, v_reserva_balance are all security_invoker=true"
    - "register_reserva_saida is SECURITY INVOKER and rejects a withdrawal greater than the derived balance"
    - "Reservas nav item exists in the (app) sidebar pointing at /reservas"
  artifacts:
    - path: "supabase/migrations/0011_budget_targets.sql"
      provides: "budget_targets table (percent_bp basis-points, direction, unique(user_id,category_id)) + RLS + grants + index"
      contains: "create table if not exists public.budget_targets"
    - path: "supabase/migrations/0012_categories_is_reserva.sql"
      provides: "categories.is_reserva flag + backfill + handle_new_user update (D: Open Question 2 resolution)"
      contains: "is_reserva"
    - path: "supabase/migrations/0013_reservas.sql"
      provides: "reservas + reserva_ledger tables + RLS + grants + indexes + unique(transaction_id)"
      contains: "create table if not exists public.reserva_ledger"
    - path: "supabase/migrations/0014_adherence_views.sql"
      provides: "v_adherence_month + v_adherence_ytd (security_invoker, alocação grouped by kind)"
      contains: "security_invoker = true"
    - path: "supabase/migrations/0015_reserva_balance_view.sql"
      provides: "v_reserva_balance = Σ in − Σ out (security_invoker)"
      contains: "v_reserva_balance"
    - path: "supabase/migrations/0016_register_reserva_saida.sql"
      provides: "atomic never-negative saída RPC (security invoker)"
      contains: "register_reserva_saida"
    - path: "src/lib/month.ts"
      provides: "currentYear() + yearBounds() in America/Sao_Paulo (YTD window)"
      contains: "yearBounds"
    - path: "src/lib/adherence.ts"
      provides: "pure status mapping (adherence_bp + direction → status/token) + bp→percent helpers"
      contains: "export"
    - path: "src/lib/schemas/budget-target.ts"
      provides: "Zod schema: categoryId uuid, percentBp 1..10000, direction enum"
      exports: ["budgetTargetSchema"]
    - path: "src/lib/schemas/reserva.ts"
      provides: "Zod schemas for reserva create/edit + saída"
      exports: ["reservaSchema", "saidaSchema"]
    - path: "src/components/ui/progress.tsx"
      provides: "shadcn progress component (reserva alvo bar)"
      contains: "Progress"
    - path: "src/components/app-sidebar.tsx"
      provides: "Reservas nav item"
      contains: "/reservas"
  key_links:
    - from: "supabase/migrations/0014_adherence_views.sql"
      to: "public.v_income_month / public.v_category_totals"
      via: "FROM/JOIN in the adherence CTEs"
      pattern: "v_income_month"
    - from: "supabase/migrations/0016_register_reserva_saida.sql"
      to: "public.v_reserva_balance"
      via: "balance read inside the RPC body (security invoker)"
      pattern: "v_reserva_balance"
    - from: "src/lib/month.ts"
      to: "date-fns-tz formatInTimeZone"
      via: "America/Sao_Paulo year boundaries"
      pattern: "America/Sao_Paulo"
---

<objective>
Build the entire Phase 3 data + helper substrate in one atomic, [BLOCKING] step: the five new migrations (budget_targets, reservas + reserva_ledger, the two adherence views, the derived-balance view, the saída RPC) plus the `is_reserva` category flag that resolves the "Reserva" stable-handle open question, applied to the LOCAL stack with `src/types/database.types.ts` regenerated. Add the pure app-side helpers (year boundaries, adherence status mapping), the two Zod schemas, the `progress` component, and the Reservas nav item.

Purpose: Everything in Phase 3 (Wave-0 tests in Plan 02, the dashboard slice in Plan 03, the reservas slice in Plan 04, the aporte sub-flow in Plan 05) depends on these tables, views, RPC, types, schemas, and the nav surface existing first. Putting the shared nav change here (not in a sibling slice) prevents a cross-sibling file conflict between Plan 03 and Plan 04. (D: CONTEXT locked decisions for budget_targets/reservas/ledger/views/RPC; Open Question 2 → is_reserva flag; Open Question 1 → alocação grouping in the view.)

Output: 6 migration files applied locally, regenerated typed client, `lib/month.ts` year helpers, `lib/adherence.ts`, two Zod schemas, `progress` component, sidebar Reservas item.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-metas-ader-ncia-e-reservas/03-CONTEXT.md
@.planning/phases/03-metas-ader-ncia-e-reservas/03-RESEARCH.md

# Verbatim patterns to mirror (read before writing the migrations):
@supabase/migrations/0002_categories.sql
@supabase/migrations/0007_views.sql
@supabase/migrations/0010_reassign_validate_ownership.sql
@supabase/migrations/0005_transactions.sql
@src/lib/month.ts
@src/components/app-sidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Migrations 0011-0016 + is_reserva flag, applied to LOCAL stack + regenerated types</name>
  <files>supabase/migrations/0011_budget_targets.sql, supabase/migrations/0012_categories_is_reserva.sql, supabase/migrations/0013_reservas.sql, supabase/migrations/0014_adherence_views.sql, supabase/migrations/0015_reserva_balance_view.sql, supabase/migrations/0016_register_reserva_saida.sql, src/types/database.types.ts</files>
  <read_first>supabase/migrations/0002_categories.sql (RLS shape + handle_new_user seed), supabase/migrations/0007_views.sql (security_invoker view pattern + grant select to authenticated), supabase/migrations/0010_reassign_validate_ownership.sql (atomic security-invoker RPC shape + P0001 raise), supabase/migrations/0005_transactions.sql (transactions columns: category_id, amount_cents>0, occurred_on date, on delete restrict). RESEARCH.md Patterns 1-6 contain the verbatim SQL sketches for every file below.</read_first>
  <action>
Write the six migrations following RESEARCH.md Patterns 1-6 EXACTLY, each reusing the Phase-1/2 RLS shape verbatim ((select auth.uid()) = user_id USING+WITH CHECK, TO authenticated, grant select/insert/update/delete to authenticated+service_role, a user_id index per table). This task is [BLOCKING]: after writing, apply to the LOCAL stack and regenerate types before any dependent work.

0011_budget_targets.sql (BUD-01): table budget_targets(id, user_id FK auth.users on delete cascade, category_id FK categories on delete cascade, percent_bp int check (percent_bp > 0 and percent_bp <= 10000), direction text check in ('teto','alvo'), created_at, updated_at, unique(user_id, category_id)). Index budget_targets_user_idx on (user_id). RLS + grants + "own budget_targets" policy. Store percent as INTEGER basis-points (3000 = 30%), never float (D: RESEARCH Pitfall 1 — no float in money). The direction default-from-kind is NOT a DB default; it is an action/form affordance (Plan 03).

0012_categories_is_reserva.sql (Open Question 2 → flag, NOT name-match): alter table categories add column if not exists is_reserva boolean not null default false. Backfill: update categories set is_reserva = true where name = 'Reserva' and kind = 'alocacao' (one-time, runs against existing rows). Then `create or replace function public.handle_new_user()` — copy the existing body from 0002 verbatim and add `is_reserva` to the seed insert column list, setting true ONLY for the 'Reserva' row and false for the other 10 (an explicit value per row, ASCII enum values unchanged). The aporte trigger (Plan 05) keys off this flag, never the literal name (CAT-02 lets the user rename it).

0013_reservas.sql (RSV-01/02/04/05): table reservas(id, user_id FK cascade, nome text not null, alvo_cents bigint check (alvo_cents is null or alvo_cents > 0), is_archived boolean default false, created_at). Index reservas_user_idx. table reserva_ledger(id, user_id FK cascade, reserva_id FK reservas on delete cascade, kind text check in ('in','out'), amount_cents bigint not null check (amount_cents > 0) — ALWAYS positive, sign derives from kind, transaction_id uuid references transactions on delete set null, occurred_on date not null, note text not null default '', created_at). Indexes reserva_ledger_reserva_idx, reserva_ledger_user_idx, and a partial unique index reserva_ledger_txn_uniq on (transaction_id) where transaction_id is not null (one aporte entry per source transaction → idempotent re-link). RLS + grants + "own reservas" + "own reserva_ledger" policies.

0014_adherence_views.sql (BUD-02/03/04, RSV-03): create both v_adherence_month and v_adherence_ytd with (security_invoker = true) — MANDATORY (without it the view runs as definer and leaks every user's sums; proven by view-leak.test.ts). Both views read v_income_month / v_category_totals / transactions / budget_targets / categories. meta_cents = (income_cents * percent_bp + 5000) / 10000 in INTEGER SQL (half-up, rounded once). adherence_bp = guarded with `case when (income_cents * percent_bp) = 0 then null else (realized_cents * 10000 * 10000) / nullif(income_cents * percent_bp, 0) end` (Pitfall 2: never NaN). CRITICAL alocação grouping (Open Question 1 → grouping in the VIEW): realized cents for kind='alocacao' metas is the SUM of ALL kind='alocacao' spend in the period (Investimentos + Reserva together), so a reserva aporte counts as investment allocation and NEVER as consumo spend; consumo (teto) totals filter to kind='consumo' per category. Carry the kind column on each row so the app can render the combined alocação line. v_adherence_ytd uses the SAME percent_bp, the SAME rounding, the SAME alocação grouping — only the window differs (civil year via left(month_key,4) / to_char(occurred_on,'YYYY')); this guarantees monthly↔YTD consistency (BUD-03). grant select to authenticated on both.

0015_reserva_balance_view.sql (RSV-05): create v_reserva_balance with (security_invoker = true) = per reserva, coalesce(sum(in)) − coalesce(sum(out)) as saldo_cents, carrying user_id, reserva_id, nome, alvo_cents. Balance is ALWAYS derived here, never a stored column. grant select to authenticated.

0016_register_reserva_saida.sql (RSV-04): create register_reserva_saida(p_reserva_id uuid, p_amount_cents bigint, p_occurred_on date, p_note text default '') returns uuid, language plpgsql, security invoker, set search_path = public — mirror reassign_and_delete_category's shape. Body: reject p_amount_cents null/<=0 (P0001 'Valor inválido'); read v_saldo from v_reserva_balance where reserva_id = p_reserva_id and user_id = (select auth.uid()) — a foreign/nonexistent reserva returns null under the caller's RLS → raise 'Reserva inexistente ou sem permissão'; if p_amount_cents > v_saldo raise 'Saída maior que o saldo da reserva' (P0001); else insert the 'out' ledger row and return its id. The balance-read + insert in one function body is the atomic guard against the concurrent-overdraw TOCTOU race (Pitfall 4). grant execute to authenticated.

After writing all six: run `npm run db:reset` (applies 0001-0016 to the LOCAL stack on :55321; remote is deferred per STATE blocker), then `npm run gen:types` to regenerate src/types/database.types.ts. Do NOT push to remote. Local Docker stack must be running (`supabase start`) first.
  </action>
  <verify>
    <automated>npm run db:reset && npm run gen:types && grep -q "budget_targets" src/types/database.types.ts && grep -q "v_adherence_month" src/types/database.types.ts && grep -q "v_reserva_balance" src/types/database.types.ts && grep -q "register_reserva_saida" src/types/database.types.ts && grep -q "is_reserva" src/types/database.types.ts && grep -c "security_invoker = true" supabase/migrations/0014_adherence_views.sql | grep -qv '^0$' && grep -q "security_invoker = true" supabase/migrations/0015_reserva_balance_view.sql && grep -q "security invoker" supabase/migrations/0016_register_reserva_saida.sql</automated>
  </verify>
  <done>db reset applies cleanly; database.types.ts contains budget_targets, reservas, reserva_ledger, v_adherence_month, v_adherence_ytd, v_reserva_balance, register_reserva_saida, and categories.is_reserva. All three views are security_invoker; the saída RPC is security invoker. No remote push performed.</done>
</task>

<task type="auto">
  <name>Task 2: App-side helpers — year boundaries, adherence status mapping, Zod schemas, progress component, Reservas nav</name>
  <files>src/lib/month.ts, src/lib/adherence.ts, src/lib/schemas/budget-target.ts, src/lib/schemas/reserva.ts, src/components/ui/progress.tsx, src/components/app-sidebar.tsx</files>
  <read_first>src/lib/month.ts (extend, do not rewrite — it owns America/Sao_Paulo month math via date-fns-tz formatInTimeZone), src/components/app-sidebar.tsx (NAV_ITEMS array + lucide imports), src/lib/schemas/transaction.ts (Zod schema style), RESEARCH.md "Civil-year helper" + "Server Action: upsert budget target" + Pattern 5 saída schema for exact field shapes.</read_first>
  <action>
Extend src/lib/month.ts with currentYear() (formatInTimeZone(new Date(), 'America/Sao_Paulo', 'yyyy') → 'YYYY') and yearBounds(year) ({ first: `${year}-01-01`, last: `${year}-12-31` }) for the YTD window (BUD-03). Reuse the existing TZ constant; do not duplicate it.

Create src/lib/adherence.ts as a PURE presentation module (no DB, no money math beyond integer bp→percent formatting): export a function that maps (adherence_bp: number | null, direction: 'teto'|'alvo') → a status discriminant ('sem-receita' | for teto: 'no-limite'|'aproximando'|'estourou' | for alvo: 'abaixo'|'quase-la'|'atingido') using the 80%/100% thresholds (adherence_bp is realized÷meta in basis-points of the meta: 8000 = 80%, 10000 = 100%); adherence_bp null → 'sem-receita'. Export the status→token mapping per UI-SPEC §"Semantic Color — Direction-Aware Adherence" (teto: <80 consumption/muted, 80-100 consumption+alert, ≥100 destructive; alvo: <80 muted, 80-100 allocation, ≥100 income). Export a bp→pt-BR percent formatter helper using Intl.NumberFormat('pt-BR',{style:'percent',maximumFractionDigits:1}) that never returns NaN%/Infinity% (guard null → the "sem receita" copy is rendered by the caller). This file is imported by both the dashboard slice (Plan 03) and its unit test (Plan 02).

Create src/lib/schemas/budget-target.ts: export budgetTargetSchema = z.object({ categoryId: z.string().uuid('Selecione uma categoria'), percentBp: z.number().int().gt(0).lte(10000), direction: z.enum(['teto','alvo']) }) and its inferred type. Mirror transaction.ts style (single source of truth for form + action).

Create src/lib/schemas/reserva.ts: export reservaSchema (nome: z.string().trim().min(1,'Informe o nome').max(60); alvo: z.string().optional() — raw pt-BR money string, parsed server-side via parseBRLToCents, optional) and saidaSchema (reservaId uuid; amount: raw pt-BR money string min(1,'Informe o valor'); occurredOn regex /^\d{4}-\d{2}-\d{2}$/; note: optional string). Export inferred types.

Add the shadcn `progress` component: run `npx shadcn@latest add progress` (writes src/components/ui/progress.tsx; official registry, registries: {} confirmed). If the CLI is unavailable, hand-author a minimal Radix-free progress div per UI-SPEC (role="progressbar", aria-valuenow/min/max, h-2 track + fill) — the RESEARCH Environment table lists this fallback.

Extend src/components/app-sidebar.tsx NAV_ITEMS: add { href: '/reservas', label: 'Reservas', icon: PiggyBank } (import PiggyBank from lucide-react). Keep the existing Dashboard/Receitas/Categorias/Extrato items and the active-styling render pattern unchanged. The Dashboard item already points at /dashboard (Plan 03 makes that route real — no nav change needed for it).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "yearBounds" src/lib/month.ts && grep -q "currentYear" src/lib/month.ts && grep -q "export" src/lib/adherence.ts && grep -q "budgetTargetSchema" src/lib/schemas/budget-target.ts && grep -q "saidaSchema" src/lib/schemas/reserva.ts && test -f src/components/ui/progress.tsx && grep -q "/reservas" src/components/app-sidebar.tsx</automated>
  </verify>
  <done>tsc clean; month.ts exports currentYear + yearBounds; adherence.ts exports the status mapper + percent formatter; both Zod schemas exist; progress.tsx vendored; sidebar has a Reservas nav item.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Server Action / RPC | Untrusted percentBp, category_id, reserva_id, money strings cross here (consumed by Plans 03-05; this plan lays the DB guards) |
| Postgres view / RPC → caller | security_invoker boundary: a view/RPC without it would run as definer and leak every user's sums |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | v_adherence_month / v_adherence_ytd / v_reserva_balance | mitigate | `with (security_invoker = true)` on all three (Pitfall 5); leak test in Plan 02 |
| T-03-02 | Information Disclosure | budget_targets / reservas / reserva_ledger | mitigate | RLS `(select auth.uid()) = user_id` USING+WITH CHECK + grants + user_id index on every table; two-user RLS test in Plan 02 |
| T-03-03 | Tampering / Integrity | register_reserva_saida | mitigate | atomic security-invoker RPC reads v_reserva_balance + inserts in one function body → never-negative, race-safe (Pitfall 4) |
| T-03-04 | Integrity | adherence alocação grouping | mitigate | aporte (kind='alocacao') sums ONLY into the alocação total; consumo totals filter kind='consumo' (Pitfall 3); pinned by allocation-grouping test in Plan 02 |
| T-03-05 | Tampering | percent_bp / amount_cents domain | mitigate | check (percent_bp > 0 and <= 10000); check (amount_cents > 0) on ledger; no float |
| T-03-SC | Tampering | npm/shadcn installs | mitigate | No external npm packages this phase (RESEARCH Package Legitimacy Audit: none); `progress` is shadcn-official vendored source, no third-party registry (registries: {}) |
</threat_model>

<verification>
- `npm run db:reset` applies all migrations 0001-0016 to the local stack with no error.
- `npm run gen:types` regenerates types containing every new relation/function/column.
- `npx tsc --noEmit` is clean after the helper + schema additions.
- The three new views are security_invoker; the saída RPC is security invoker; every new table has RLS + grants + a user_id index.
</verification>

<success_criteria>
- Migrations 0011-0016 written per RESEARCH Patterns 1-6, applied LOCAL only, types regenerated.
- is_reserva flag added, backfilled, and seeded via handle_new_user (stable handle, not name-match).
- Alocação grouping computed in the view; monthly and YTD share one percent_bp + rounding + grouping.
- lib/month.ts year helpers, lib/adherence.ts status mapping, two Zod schemas, progress component, Reservas nav item all present and type-clean.
</success_criteria>

<output>
Create `.planning/phases/03-metas-ader-ncia-e-reservas/03-01-SUMMARY.md` when done
</output>
