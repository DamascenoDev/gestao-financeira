---
phase: 08-substrato-carro-crud-navega-o
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - src/components/carro-form.tsx
  - src/components/carro-card.tsx
  - src/app/(app)/carros/page.tsx
  - src/app/(app)/carros/[id]/page.tsx
  - src/app/(app)/carros/loading.tsx
  - src/components/app-sidebar.tsx
  - src/components/bottom-nav.tsx
autonomous: true
requirements: [CAR-01, CAR-06]

must_haves:
  truths:
    - "A lista /carros mostra todos os carros não-arquivados (identidade: apelido + modelo/placa/ano + combustível badge); com o filtro 'mostrar arquivados' ligado os arquivados também aparecem com badge 'Arquivado'"
    - "Usuário cadastra um carro pelo botão gold 'Novo carro' e edita/arquiva/desarquiva pelo card (dialog + toggle com toast)"
    - "Existe uma aba 'Carros' (ícone Car) na sidebar (após Reservas) e na bottom-nav mobile (6º item); /carros e /carros/[id] resolvem sob o guard de auth"
    - "/carros/[id] mostra o detalhe mínimo (apelido + modelo/placa/ano + combustível + badge arquivado + ações editar/arquivar); um id não-próprio cai em not-found"
  artifacts:
    - path: "src/components/carro-form.tsx"
      provides: "Create/edit Dialog (Zod + react-hook-form grammar, sonner, { ok } | { error })"
      min_lines: 80
    - path: "src/components/carro-card.tsx"
      provides: "Identity card (apelido + modelo/placa/ano + combustivel badge + archived badge + edit/archive actions)"
      min_lines: 60
    - path: "src/app/(app)/carros/page.tsx"
      provides: "/carros list RSC (grid + Novo carro CTA + mostrar-arquivados filter + empty/error)"
    - path: "src/app/(app)/carros/[id]/page.tsx"
      provides: "minimal car detail RSC (identity + actions, notFound for foreign id)"
  key_links:
    - from: "src/components/app-sidebar.tsx"
      to: "/carros"
      via: "NAV_ITEMS entry { href:'/carros', label:'Carros', icon: Car } after Reservas"
      pattern: "/carros"
    - from: "src/components/bottom-nav.tsx"
      to: "/carros"
      via: "NAV_ITEMS entry (6th item) { href:'/carros', icon: Car }"
      pattern: "/carros"
    - from: "src/app/(app)/carros/page.tsx"
      to: "carros table"
      via: "RLS-active supabase select (is_archived filter)"
      pattern: "from\\('carros'\\)"
---

<objective>
Ship the user-facing Carro slice: the `/carros` list (identity-card grid + "Novo carro" CTA + "mostrar arquivados" filter + empty/loading/error), the CarroForm create/edit dialog, the archive/unarchive toggle, the minimal `/carros/[id]` detail, and the "Carros" nav entry in both the sidebar and the mobile bottom-nav. After this plan, a user can create, edit, archive, unarchive, and navigate to their cars — the end-to-end vertical slice of CAR-01 + CAR-06.

Purpose: Completes Phase 8's user-visible capability on top of the Plan-01 substrate and Plan-02 server layer. Reuses the locked Phase-7 design system and the reservas/page + reserva-form/card grammar verbatim — zero new visual primitives, zero new npm deps. Identity only this phase: KPIs (gasto total, km/l) are deferred to Phases 9-11 (do NOT render placeholder zeros).

Output: `carro-form.tsx`, `carro-card.tsx`, `carros/page.tsx`, `carros/[id]/page.tsx`, `carros/loading.tsx`, plus the NAV_ITEMS edits in `app-sidebar.tsx` and `bottom-nav.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-substrato-carro-crud-navega-o/08-CONTEXT.md
@.planning/phases/08-substrato-carro-crud-navega-o/08-UI-SPEC.md
@src/components/reserva-form.tsx
@src/components/reserva-card.tsx
@src/app/(app)/reservas/page.tsx
@src/components/app-sidebar.tsx
@src/components/bottom-nav.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the "Carros" nav entry to sidebar + bottom-nav</name>
  <files>src/components/app-sidebar.tsx, src/components/bottom-nav.tsx</files>
  <read_first>
    - src/components/app-sidebar.tsx (the NAV_ITEMS const + active-detection + icon import grammar)
    - src/components/bottom-nav.tsx (the NAV_ITEMS const, 5 items today, min-h-12 ≥48px touch targets, active = text-primary)
    - .planning/phases/08-substrato-carro-crud-navega-o/08-UI-SPEC.md §Screen Contracts 1 (exact placement + the Car icon)
  </read_first>
  <action>
    In `src/components/app-sidebar.tsx`: import `Car` from lucide-react (add to the existing lucide import) and insert `{ href: '/carros', label: 'Carros', icon: Car }` into `NAV_ITEMS` immediately AFTER the `/reservas` (PiggyBank) entry and before `/mei` (FileText) — per CONTEXT + UI-SPEC §1. Active detection and the gold-active styling are the existing frozen grammar (no change).

    In `src/components/bottom-nav.tsx`: import `Car` from lucide-react and add the SAME `{ href: '/carros', label: 'Carros', icon: Car }` entry to its `NAV_ITEMS`, taking the bar from 5 to 6 items (required by success criterion #2). Items stay `flex-1 min-h-12` (≥48px), active = `text-primary` (frozen). Place it adjacent to the other primary destinations; the 6-item row must stay legible at 360px (label `text-xs`, icon `size-5` — already the pattern). No layout-class changes beyond the new item.
  </action>
  <verify>
    <automated>grep -q "/carros" src/components/app-sidebar.tsx && grep -q "/carros" src/components/bottom-nav.tsx && grep -q "Car" src/components/app-sidebar.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source: both NAV_ITEMS arrays contain the `/carros` entry with the lucide `Car` icon; sidebar entry sits between Reservas and MEI.
    - Behavior: the sidebar shows Carros after Reservas; the bottom-nav now has 6 items; active detection reuses the existing `pathname.startsWith` grammar (gold active).
    - Command: grep confirms `/carros` + `Car` in both files; `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>"Carros" appears in the sidebar (after Reservas) and the bottom-nav (6th item) with the Car icon; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: CarroForm dialog + CarroCard identity card</name>
  <files>src/components/carro-form.tsx, src/components/carro-card.tsx</files>
  <read_first>
    - src/components/reserva-form.tsx (the create/edit Dialog grammar to clone verbatim: manual-state + useTransition, controlled/uncontrolled open, re-seed on open without useEffect, Field/FieldGroup/FieldLabel/FieldError, sonner toast, { ok } | { error } handling, DialogClose Cancelar + gold submit)
    - src/components/reserva-card.tsx (the identity-card + per-item actions grammar: Card/CardContent, DropdownMenu of actions, controlled child dialogs, useTransition action handlers + toast)
    - src/components/ui/select.tsx (the Select primitive for combustivel-padrao)
    - .planning/phases/08-substrato-carro-crud-navega-o/08-UI-SPEC.md §3 (Dialog fields + copy), §4 (archive toggle), §2 (card contents)
    - src/actions/carros.ts (createCarro/updateCarro/archiveCarro/unarchiveCarro signatures)
  </read_first>
  <action>
    Create `src/components/carro-form.tsx` (`'use client'`) cloning `reserva-form.tsx`: exported, self-contained, accepts optional controlled `open`/`onOpenChange` + custom `trigger`, manual-state + `useTransition`, re-seed from props on open (no useEffect). Fields per UI-SPEC §3: Apelido (`Input`, required, error "Informe o apelido"), Modelo (`Input`, optional), Placa (`Input`, optional, no mask), Ano (numeric `Input`, optional, error "Ano inválido" when out of sane bound), Combustível padrão (`Select` with fixed options Flex · Gasolina · Etanol · Diesel · GNV plus a no-selection state since optional). Title "Novo carro" (create) / "Editar carro" (edit). On submit call `createCarro` or `updateCarro(edit.id, input)`; on `{ error }` `toast.error`, on success `toast.success('Carro adicionado'|'Carro atualizado')` + close. Footer: `DialogClose` "Cancelar" + gold submit "Salvar" (disabled while pending). Default trigger = gold "Novo carro" button. Validate client-side mirroring carroSchema before calling the action (the action re-validates server-side).

    Create `src/components/carro-card.tsx` (`'use client'`) cloning `reserva-card.tsx`'s structure but IDENTITY ONLY (no KPIs, no AmountCell, no money — those are Phases 9-11; do NOT render placeholder zeros). Card content: primary line = apelido (`text-sm` weight 600, links to `/carros/[id]`); secondary line = modelo · placa · ano joined with a middot (`text-xs text-muted-foreground`, OMIT any null field); optional combustível-padrão as a small neutral `Badge` if set; an "Arquivado" neutral `Badge` (variant secondary/outline) when `is_archived`. Per-card actions via a `DropdownMenu` (MoreHorizontal, matching ReservaCard): "Editar" (opens the controlled CarroForm in edit mode) and "Arquivar"/"Desarquivar" (calls archiveCarro/unarchiveCarro in a useTransition, toast "Carro arquivado"/"Carro desarquivado" — soft reversible toggle, NO AlertDialog, NO destructive styling). Archived state is visual-neutral (muted, never red, never gold).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "createCarro\|updateCarro" src/components/carro-form.tsx && grep -q "archiveCarro\|unarchiveCarro" src/components/carro-card.tsx && grep -Lq "AmountCell\|formatCents" src/components/carro-card.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source: `carro-form.tsx` mirrors reserva-form (controllable dialog, the 5 fields, sonner, gold submit); `carro-card.tsx` mirrors reserva-card structure with identity-only content and the dropdown edit/archive actions.
    - Behavior: form copy matches UI-SPEC §Copywriting (Novo carro / Editar carro / Salvar / Cancelar / "Informe o apelido" / "Ano inválido" / the toasts); card omits null identity fields, shows combustivel/archived badges, archive is a reversible toggle with toast and no money/KPIs.
    - Command: `npx tsc --noEmit` clean; grep confirms the actions are wired and no money helper (`AmountCell`/`formatCents`) appears in the card (KPIs deferred).
  </acceptance_criteria>
  <done>CarroForm and CarroCard exist, mirroring the reserva grammar, identity-only, wired to the Plan-02 actions; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: /carros list page + minimal /carros/[id] detail + loading skeleton</name>
  <files>src/app/(app)/carros/page.tsx, src/app/(app)/carros/[id]/page.tsx, src/app/(app)/carros/loading.tsx</files>
  <read_first>
    - src/app/(app)/reservas/page.tsx (the list RSC grammar: RLS-active read, the `grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-3` grid, the header h1 + form-trigger row, the Empty primitive block, the inline text-destructive error)
    - src/app/(app)/reservas/[id]/page.tsx (the detail RSC grammar: await params, maybeSingle read, notFound() on missing/foreign id, header layout)
    - src/components/card-skeleton.tsx (the Phase-7 CardSkeleton for loading.tsx — never a spinner)
    - .planning/phases/08-substrato-carro-crud-navega-o/08-UI-SPEC.md §2 (list), §5 (minimal detail), §Copywriting (empty/error copy)
  </read_first>
  <action>
    Create `src/app/(app)/carros/page.tsx` (RSC) cloning `reservas/page.tsx`. Read the user's carros RLS-scoped: `supabase.from('carros').select('id, apelido, modelo, placa, ano, combustivel_padrao, is_archived').order('apelido', { ascending: true })`. Read the "mostrar arquivados" filter from a `?arquivados=1` URL searchParam (acceptable per UI-SPEC §2 — Extrato uses URL params; a pure client toggle is also acceptable, pick one and be consistent). When the filter is OFF, render only `is_archived === false` cars; when ON, render all (archived carry the "Arquivado" badge via CarroCard). Header: `text-xl` h1 "Carros" + the gold "Novo carro" CTA (the default CarroForm trigger) on the right + the "Mostrar arquivados" toggle control (shadcn `Switch` or `Checkbox`+label — match the project's boolean-filter convention; default OFF). Grid: `grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-3` of CarroCard. Empty state (no cars, filter OFF): shadcn `Empty` with the Car icon, title "Nenhum carro ainda", body "Cadastre um carro para começar a acompanhar seus gastos e abastecimentos." + one gold "Novo carro" CTA. Error: inline `text-destructive` "Não foi possível carregar os carros. Tente recarregar a página." Do NOT render any gasto/km/l KPIs (deferred).

    Create `src/app/(app)/carros/[id]/page.tsx` (RSC) cloning `reservas/[id]/page.tsx`. `await params`, read the one carro RLS-scoped: `from('carros').select(...).eq('id', id).maybeSingle()`; a null/foreign id → `notFound()` (RLS returns no row). Header: car apelido as `text-xl` h1; secondary line modelo · placa · ano (`text-sm text-muted-foreground`, null fields omitted); "Arquivado" badge if archived; action area with "Editar" (the same CarroForm edit dialog) and the Arquivar/Desarquivar toggle (reuse the CarroCard action affordances or inline equivalents). Body: a single `Card` listing the car's fields as a simple definition list (apelido, modelo, placa, ano, combustível-padrão; null optionals show "—" consistently). NO money, NO charts, NO tables, NO "coming soon" placeholders for the deferred sections.

    Create `src/app/(app)/carros/loading.tsx` rendering a `CardSkeleton` grid (the Phase-7 component, never a spinner) so the list streams with the layout chrome visible.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build && grep -q "from('carros')" "src/app/(app)/carros/page.tsx" && grep -q "notFound" "src/app/(app)/carros/[id]/page.tsx" && grep -q "Nenhum carro ainda" "src/app/(app)/carros/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `/carros/page.tsx` reads carros RLS-scoped with the is_archived filter, renders the CarroCard grid + Novo carro CTA + mostrar-arquivados toggle + Empty/error; `[id]/page.tsx` reads one carro and notFound()s a foreign/missing id; `loading.tsx` renders CardSkeleton.
    - Behavior: list shows non-archived by default, archived appear with badge when the filter is on; empty/error copy matches UI-SPEC; detail shows identity + actions only, no money/charts/placeholders; both routes inherit the (app) auth guard + chrome.
    - Command: `npx tsc --noEmit` clean; `npm run build` compiles the two new routes; grep confirms the RLS read, the notFound guard, and the empty copy.
  </acceptance_criteria>
  <done>/carros and /carros/[id] resolve under the auth guard with the list grid + filter + empty/loading/error and a minimal identity detail; build compiles the new routes; tsc clean.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 03 share)

**New components:** `src/components/carro-form.tsx` (create/edit Dialog), `src/components/carro-card.tsx` (identity card)
**New routes:** `src/app/(app)/carros/page.tsx` (list), `src/app/(app)/carros/[id]/page.tsx` (minimal detail), `src/app/(app)/carros/loading.tsx` (CardSkeleton)
**New NAV item:** "Carros" (lucide `Car`) added to `app-sidebar.tsx` (after Reservas) and `bottom-nav.tsx` (6th mobile item)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /carros, /carros/[id] | Both routes sit in the (app) segment under the existing auth guard |
| RSC read → carros table | The list/detail reads run under the RLS-active server client (own rows only) |
| client form/card actions → server actions | All writes go through the Plan-02 actions (Zod + assertOwnedCarro), not direct DB access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10 | Information Disclosure | /carros, /carros/[id] reads | mitigate | RLS-active server client scopes reads to the caller; a foreign/missing id on the detail → notFound() (RLS returns no row); inherits the (app) auth guard |
| T-08-11 | Elevation of Privilege / IDOR | edit/archive from the card/detail | mitigate | All mutations route through the Plan-02 server actions which re-derive ownership via assertOwnedCarro; the UI never writes the DB directly |
| T-08-12 | Information Disclosure | service-role key in client bundle | accept | This plan adds only RSC reads + client components calling server actions; no client module imports the service-role admin client (SEC-01 secret-bundle audit stays green) |
| T-08-13 | Tampering | carro_id accounting via the UI | accept | This phase's UI never tags transactions (CAR-02 is Phase 9); the card/detail are identity-only, so no accounting surface is touched (D4 preserved by construction) |
</threat_model>

<verification>
- `npm run build` compiles `/carros` and `/carros/[id]`; `npx tsc --noEmit` clean.
- Sidebar shows "Carros" after Reservas; bottom-nav has 6 items (grep `/carros` in both nav files).
- `/carros` empty/error copy matches UI-SPEC; the card is identity-only (no money helper imported).
- Full suite remains ≥599 green (`npm test`).
</verification>

<success_criteria>
- A user can create, edit, archive, and unarchive a car via /carros, and navigate to /carros/[id].
- The "mostrar arquivados" filter hides/reveals archived cars (badge when shown).
- "Carros" is in the sidebar (after Reservas) and the bottom-nav (6 items); both routes resolve under the auth guard.
- The detail is identity-only (no money/charts/placeholders); build + tsc clean; full suite green.
</success_criteria>

<output>
Create `.planning/phases/08-substrato-carro-crud-navega-o/08-03-SUMMARY.md` when done
</output>
