---
phase: 11-detalhe-do-carro-gr-fico-de-consumo
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/app/(app)/carros/[id]/page.tsx
  - tests/carro-categoria-aggregation.test.ts
autonomous: true
requirements: [CAR-05]
must_haves:
  truths:
    - "/carros/[id] shows the car header plus 3 KPI cards: km/l médio, R$/km, gasto total — mono tabular-nums, '—' when null"
    - "/carros/[id] shows gasto por categoria (CarroCategoriaBars) from the carro_id-tagged lançamentos, ordered by valor desc"
    - "/carros/[id] shows the km/l-over-time consumption line chart (CarroConsumoChart), null intervals omitted"
    - "The existing Phase-10 AbastecimentoHistory section is preserved, integrated under the new layout"
    - "The gasto-por-categoria aggregation reads only the caller's own carro_id-tagged transactions and never touches metas (D4)"
  artifacts:
    - path: "src/app/(app)/carros/[id]/page.tsx"
      provides: "Enriched detail: KPI cards + inline gasto-por-categoria aggregation + CarroCategoriaBars + CarroConsumoChart, Phase-10 abastecimento section kept"
      contains: "CarroConsumoChart"
    - path: "tests/carro-categoria-aggregation.test.ts"
      provides: "Integration test: per-category sums correct, RLS isolation (user B sees zero), non-destructive to metas/categories"
  key_links:
    - from: "src/app/(app)/carros/[id]/page.tsx"
      to: "src/components/carro-consumo-chart.tsx"
      via: "import + render with the km/l series from v_abastecimento_consumo"
      pattern: "CarroConsumoChart"
    - from: "src/app/(app)/carros/[id]/page.tsx"
      to: "src/components/carro-categoria-bars.tsx"
      via: "import + render with the inline per-category aggregation"
      pattern: "CarroCategoriaBars"
    - from: "src/app/(app)/carros/[id]/page.tsx"
      to: "transactions / categories"
      via: "RLS-scoped inline aggregation of amount_cents by category_id where carro_id = id"
      pattern: "\\.eq\\('carro_id'"
---

<objective>
Compose the full enriched `/carros/[id]` detail page (CAR-05): the car header, 3 KPI stat cards (km/l médio · R$/km · gasto total from `v_carro_resumo`), the gasto-por-categoria bars (a new INLINE RSC aggregation feeding `CarroCategoriaBars`), the km/l-over-time consumption line chart (`CarroConsumoChart` fed from `v_abastecimento_consumo`), and the EXISTING Phase-10 `AbastecimentoHistory` section integrated into the layout in the UI-SPEC section order — then re-run the SEC-01 bundle-secret audit now that a new chart client component is in the bundle.

Purpose: Land the capstone presentation surface. The gasto-por-categoria aggregation is implemented as an INLINE RSC query (not a new view) — it has exactly one consumer, RLS already scopes it, and avoiding a `0029` view keeps the phase pure-presentation with no migration/types-drift gate (CONTEXT: "prefer the view ONLY if cleanly reusable; an inline RSC query is acceptable and lighter").
Output: the enriched detail page + an integration test proving the aggregation sums, RLS isolation, and non-destructiveness; SEC-01 re-audit exit 0. (CAR-05.1, CAR-05.2, CAR-05.3, CAR-05.4, CAR-05.5)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-detalhe-do-carro-gr-fico-de-consumo/11-CONTEXT.md
@.planning/phases/11-detalhe-do-carro-gr-fico-de-consumo/11-UI-SPEC.md
@.planning/phases/11-detalhe-do-carro-gr-fico-de-consumo/11-VALIDATION.md
@.planning/phases/11-detalhe-do-carro-gr-fico-de-consumo/11-01-PLAN.md
@src/app/(app)/carros/[id]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inline gasto-por-categoria aggregation + integration test</name>
  <files>src/app/(app)/carros/[id]/page.tsx, tests/carro-categoria-aggregation.test.ts</files>
  <read_first>
    - src/app/(app)/carros/[id]/page.tsx — the FROZEN detail RSC: the carro read, the abastecimentos read, the `v_abastecimento_consumo` read (kmPorLitroById map), the `v_carro_resumo` read, the transacoes picker logic, and the existing render. ADD the per-category aggregation read; do NOT touch the abastecimentos/picker logic.
    - tests/carro-view-leak.test.ts — the integration-test pattern (local Supabase stack, `readLocalConfig`/`serviceClient`/`userClient` from tests/helpers/local-supabase, createUser helper, two-user RLS-isolation assertions). Clone this shape for the aggregation test.
    - tests/carro-consumo.test.ts — the seeding pattern for carros/abastecimentos/transactions against the local stack.
    - CLAUDE.md "Categoria point-in-time na linha da transação" + 11-CONTEXT.md §Dados (group by point-in-time `category_id`, read-only, does NOT read into metas — D4 lente).
  </read_first>
  <action>
    In `/carros/[id]/page.tsx`, add an RLS-scoped inline aggregation for gasto-por-categoria. Read the caller's transactions for this carro: `supabase.from('transactions').select('amount_cents, category_id, categories(name)').eq('carro_id', id)`. RLS already scopes to the owner; `.eq('carro_id', id)` plus the owned-carro read (the page already `notFound()`s a foreign id) keeps this to the caller's own tagged lançamentos. Group in TypeScript by `category_id` (the point-in-time category on the row — never by name; CLAUDE.md locked decision), summing `amount_cents` per category, resolving the display name from the embedded `categories(name)` (fallback "Sem categoria" if the join is null). Produce `CarroCategoriaDatum[]` `{ categoria, valorCents }` (the component sorts desc; you may pre-sort too). Money stays in integer cents end-to-end; do NOT read into `budget_targets`/metas or any adherence view (D4: the carro tag is a non-accounting lens). This query is read-only and additive — it writes nothing.

    Create `tests/carro-categoria-aggregation.test.ts` against the local Supabase stack (clone `carro-view-leak.test.ts` setup): seed user A with a carro, two categories (e.g. "Manutenção", "Combustível"), and several transactions tagged to the carro across both categories (plus one untagged transaction that must NOT appear in the carro's total). Assert: (a) the per-category sums equal the seeded amounts grouped by `category_id`, in integer cents; (b) the untagged transaction is excluded; (c) RLS isolation — user B reading the same carro_id aggregation sees ZERO rows (no leak); (d) non-destructive — after the aggregation read, the transactions' `category_id`/`amount_cents` and any `budget_targets` rows are byte-identical (the read changed nothing — assert by re-reading and comparing, mirroring the D4 invariant proven in carro-tag-nondestructive.test.ts). Mark it as a local-stack integration test (it.skip-style guard if the stack is down, following the repo's existing local-test convention).
  </action>
  <verify>
    <automated>npm test -- carro-categoria-aggregation && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The aggregation groups by point-in-time `category_id` (not name), sums `amount_cents` in integer cents, and excludes untagged transactions.
    - Integration test green: per-category sums correct, untagged excluded, user B sees ZERO (RLS isolation), and transactions/budget_targets unchanged after the read (D4 non-destructive).
    - The read is RLS-scoped via the standard server client (no `admin.ts`, no service-role); it never reads `budget_targets`/adherence views.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>The detail RSC computes gasto-por-categoria from the carro's own tagged lançamentos grouped by point-in-time category, integer-cents, proven correct + RLS-isolated + non-destructive by an integration test.</done>
</task>

<task type="auto">
  <name>Task 2: Compose the enriched detail layout (KPI cards + bars + chart + Phase-10 history)</name>
  <files>src/app/(app)/carros/[id]/page.tsx</files>
  <read_first>
    - src/app/(app)/carros/[id]/page.tsx — keep the carro header (apelido h1 + modelo·placa·ano + Arquivado badge + CarroDetailActions) and the ENTIRE Phase-10 abastecimento section (AbastecimentoForm + AbastecimentoHistory + transacoes picker) verbatim. Compose the new sections around them per the UI-SPEC order.
    - src/components/carro-consumo-chart.tsx (Plan 01) — `CarroConsumoChart` props `{ data: CarroConsumoDatum[] }`, `CarroConsumoDatum = { data: string; kmPorLitro: number }`. The component drops null/0 points and shows its own empty state below 2 valid points.
    - src/components/carro-categoria-bars.tsx (Plan 01) — `CarroCategoriaBars` props `{ data: CarroCategoriaDatum[] }`.
    - src/components/receita-gasto-chart.tsx — confirm the chart sits inside a Card and is accompanied by a labeled figure (AbastecimentoHistory's averages block satisfies the "labeled total accompanies the chart" rule).
    - src/lib/carro/consumo.ts — `kmPerLitroLabel`, `reaisPerKmLabel` for the KPI cards.
    - src/lib/money.ts — `formatCents` for gasto total; date formatting for the chart x-label uses the SP-pinned `dd/MM` helper already used by AbastecimentoHistory (reuse the same date util — do NOT introduce a new date lib).
    - .planning/phases/11-detalhe-do-carro-gr-fico-de-consumo/11-UI-SPEC.md §Screen Contracts item 1 (full section order, KPI card markup, responsive stacking, states) + §Color + §Typography + §Copywriting.
  </read_first>
  <action>
    Compose the page in the UI-SPEC top→bottom order, keeping the existing header and the Phase-10 abastecimento block:

    1. Header — unchanged (apelido `text-xl` h1, modelo·placa·ano `text-sm text-muted-foreground` with null fields omitted, Arquivado badge, CarroDetailActions).

    2. KPI stat cards — a `grid grid-cols-1 gap-6 sm:grid-cols-3`. Read the three figures from the already-fetched `v_carro_resumo` resumo row (extend the existing `v_carro_resumo` select to also pull `gasto_total_cents`). Each card: a `Card`/`CardContent` with a `text-xs text-muted-foreground` label and a `text-xl font-mono font-semibold tabular-nums` value. km/l médio → `kmPerLitroLabel(resumo?.km_por_litro_medio)`; R$/km → `reaisPerKmLabel(resumo?.reais_por_km_medio)`; Gasto total → `gasto_total_cents > 0 ? formatCents(gasto_total_cents) : '—'` (treat 0/no-data as `—`, never "R$ 0,00" — D4/UI-SPEC null rule). You may inline three `Card`s or extract a tiny local `CarroKpiCard` helper (your discretion — UI-SPEC §Component Inventory). Gasto total is neutral `foreground`, never red.

    3. Gasto por categoria — an `h2 text-sm font-semibold` "Gasto por categoria" then `<CarroCategoriaBars data={categoriaData} />` (the Task-1 aggregation). The component renders its own empty line when there is no tagged spend.

    4. Consumo (km/l) — an `h2 text-sm font-semibold` "Consumo (km/l)" then a `Card` wrapping `<CarroConsumoChart data={consumoSeries} />`. Build `consumoSeries` from the EXISTING `v_abastecimento_consumo` read: select `occurred_on, km_por_litro` ordered by `occurred_on` asc (chronological for the time axis — note the existing read orders by odometro desc for the history; add an ordered copy or a second select for the chart), drop rows whose `km_por_litro` is null (CONTEXT: null intervals omitted), and map each to `{ data: <dd/MM SP-pinned label of occurred_on>, kmPorLitro: km_por_litro }`. Format the date with the same SP-pinned `dd/MM` util AbastecimentoHistory uses (no new date dependency). The component additionally guards <2 valid points → empty copy.

    5. Abastecimentos — keep the EXISTING Phase-10 section verbatim (AbastecimentoForm + AbastecimentoHistory averages/table→card + RowActions). Do NOT rebuild it. Place it last under its existing "Abastecimentos" h2.

    Wrap all sections in the existing `section.mx-auto.max-w-3xl.flex.flex-col.gap-6` (or widen if the KPI grid needs it — keep the single-column section stack per UI-SPEC responsive rule). Loading/error/not-found: keep the existing `notFound()` for a foreign/missing id; the section-level loading is handled by the segment `loading.tsx` (CardSkeleton/ChartSkeleton — inherited Phase 7; if no `loading.tsx` exists for this segment, that polish is inherited and not required to change here). pt-BR `R$` throughout; `—` for every null KPI/cell.

    Keep money in integer cents until `formatCents`/`kmPerLitroLabel`/`reaisPerKmLabel` at the display edge. No business-logic/metas change (D4 lente).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `/carros/[id]` renders, in order: header → 3 KPI cards (km/l médio, R$/km, gasto total) → gasto por categoria (CarroCategoriaBars) → consumo chart (CarroConsumoChart) → the existing Phase-10 Abastecimentos section.
    - KPI cards show `kmPerLitroLabel`/`reaisPerKmLabel`/`formatCents` with `—` for null/zero-no-data; gasto total is neutral foreground (not red).
    - The chart series drops null-km/l intervals and is ordered chronologically; <2 valid points → the component's pt-BR empty copy.
    - The Phase-10 AbastecimentoHistory section is preserved verbatim (AbastecimentoForm + history + averages); not rebuilt.
    - `npx tsc --noEmit` clean; `npm run build` compiles `/carros/[id]`.
  </acceptance_criteria>
  <done>The detail page is the full CAR-05 capstone: header, KPIs, category bars, consumption chart, and the integrated Phase-10 abastecimento history — pure presentation, pt-BR, '—' null discipline, building clean.</done>
</task>

<task type="auto">
  <name>Task 3: SEC-01 bundle-secret re-audit (no regression after the new chart client component)</name>
  <files>src/app/(app)/carros/[id]/page.tsx</files>
  <read_first>
    - scripts/check-bundle-secrets.sh — the SEC-01 audit script; greps `.next/static` for service-role/secret leakage; exit 0 = clean.
    - .planning/phases/11-detalhe-do-carro-gr-fico-de-consumo/11-CONTEXT.md §Re-skin only / segurança (re-run the bundle-secret audit after the chart client component lands).
    - .planning/phases/06-endurecimento-lgpd-isolamento-auditoria/ SEC-01 closure notes (06-04) — the audit must run against a FRESH build.
  </read_first>
  <action>
    No code change in this task — it is the verification that the new client component (`carro-consumo-chart.tsx`, now in the `/carros/[id]` client bundle via the import added in Task 2) introduced no secret into `.next/static`. Run a fresh `npm run build` then `bash scripts/check-bundle-secrets.sh .next/static` and confirm exit 0. If the audit fails, the regression is a service-role/secret import reaching a client component — trace and remove it (the chart must stay pure-data; no `@/lib/supabase/admin`, no `process.env.SUPABASE_SECRET_KEY`, no `NEXT_PUBLIC_` secret). Record the green audit in the SUMMARY (SEC-01 non-regress).
  </action>
  <verify>
    <automated>npm run build && bash scripts/check-bundle-secrets.sh .next/static</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/check-bundle-secrets.sh .next/static` exits 0 against a fresh build (SEC-01 does not regress).
    - The new `CarroConsumoChart` client component carries no service-role/secret/env into the client bundle.
  </acceptance_criteria>
  <done>The bundle-secret audit is green against a fresh build with the new chart client component in the bundle — SEC-01 holds.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (this plan)

- `src/app/(app)/carros/[id]/page.tsx` — enriched detail: 3 KPI cards (km/l médio · R$/km · gasto total from `v_carro_resumo`), inline gasto-por-categoria aggregation → `CarroCategoriaBars`, `CarroConsumoChart` from `v_abastecimento_consumo`, and the preserved Phase-10 AbastecimentoHistory section, in UI-SPEC order.
- `tests/carro-categoria-aggregation.test.ts` — integration test (per-category sums, untagged excluded, RLS isolation user B = zero, D4 non-destructive).
- SEC-01 bundle-secret re-audit recorded green (no new artifact; gate result).

## NOTE — decisions recorded for the SUMMARY
- **No new view / no migration `0029`:** gasto-por-categoria is an INLINE RSC aggregation (one consumer, RLS-scoped, lighter; CONTEXT-sanctioned). No `gen:types` drift gate this phase.
- **WR-02 (same-odometer double-count in `v_abastecimento_consumo`):** NOT fixed this phase. Per CONTEXT, the fix touches the load-bearing interval math for a near-impossible degenerate data shape already partly covered by the `km<=0` guard; it is documented as a KNOWN LIMITATION in the SUMMARY and deliberately skipped (no `0029`). The SUMMARY must carry this note so it is tracked.
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Postgres (RSC) | The detail RSC aggregates `transactions` by `category_id` for `carro_id = id` with the caller's RLS-scoped client; RLS isolates rows to the owner. |
| RSC → client component | `CarroConsumoChart` (client) receives only the pre-built km/l series as props — no DB client, no env, no secret. |
| build → client bundle | A new client component must not pull a secret into `.next/static`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | Information Disclosure | inline gasto-por-categoria aggregation | mitigate | RLS-scoped standard server client + `.eq('carro_id', id)` on an already-`notFound`-guarded owned carro; integration test proves user B sees ZERO. No new view → no `security_invoker` leak surface. |
| T-11-06 | Tampering | metas/contabilidade | mitigate | D4 lente: the aggregation reads `transactions` only, never `budget_targets`/adherence views, and writes nothing; integration test asserts transactions/budget_targets byte-identical after the read. |
| T-11-01 | Information Disclosure | `carro-consumo-chart.tsx` in the `/carros/[id]` client bundle | mitigate | Pure-data props; Task 3 runs `scripts/check-bundle-secrets.sh .next/static` against a fresh build (exit 0) to prove no secret regression — the load-bearing SEC-01 gate for the new client component. |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No new dependency this plan (recharts + react-is override shipped Phase 7). Supply-chain surface unchanged. |
</threat_model>

<verification>
- `npm test -- carro-categoria-aggregation` green (integration: sums + RLS isolation + non-destructive).
- `npx tsc --noEmit` clean; `npm run build` compiles `/carros/[id]`.
- `bash scripts/check-bundle-secrets.sh .next/static` exit 0 (SEC-01 non-regress).
- Full suite green at wave close: `npm test` (baseline ~720 + the new aggregation/component tests).
</verification>

<success_criteria>
- `/carros/[id]` is the full CAR-05 capstone: header + 3 KPI cards + gasto-por-categoria bars + km/l consumption chart + the integrated Phase-10 abastecimento history, in UI-SPEC order.
- gasto-por-categoria is an inline RLS-scoped aggregation grouped by point-in-time `category_id`, proven correct + isolated + non-destructive (D4) by integration test.
- No new view, no migration, no `gen:types` drift; WR-02 documented as a known limitation in the SUMMARY (not fixed).
- SEC-01 bundle-secret audit exit 0 against a fresh build; `npx tsc --noEmit` clean; build compiles `/carros/[id]`; full suite green.
</success_criteria>

<output>
Create `.planning/phases/11-detalhe-do-carro-gr-fico-de-consumo/11-03-SUMMARY.md` when done. MUST record: (1) the no-view/inline-aggregation decision, (2) the WR-02 known-limitation note, (3) the green SEC-01 re-audit.
</output>
