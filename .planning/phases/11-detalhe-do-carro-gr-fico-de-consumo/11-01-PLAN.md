---
phase: 11-detalhe-do-carro-gr-fico-de-consumo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/carro-consumo-chart.tsx
  - src/components/carro-categoria-bars.tsx
  - tests/carro-consumo-chart.test.tsx
  - tests/carro-categoria-bars.test.tsx
autonomous: true
requirements: [CAR-05]
must_haves:
  truths:
    - "CarroConsumoChart renders a token-aware km/l line from valid intervals and a pt-BR empty state below 2 points"
    - "CarroConsumoChart drops null-km/l points before rendering the line"
    - "CarroCategoriaBars renders one neutral magnitude bar per categoria, ordered by valor desc, with a mono formatCents label"
    - "CarroCategoriaBars renders a single muted line when there is no tagged spend"
  artifacts:
    - path: "src/components/carro-consumo-chart.tsx"
      provides: "Client recharts line chart (km/l over time), pure-data props, --chart-1 token, pt-BR tooltip, null-interval drop, empty state"
      min_lines: 40
    - path: "src/components/carro-categoria-bars.tsx"
      provides: "Horizontal magnitude bars (categoria + bg-muted track + neutral fill + formatCents mono label), empty muted line"
      min_lines: 30
    - path: "tests/carro-consumo-chart.test.tsx"
      provides: "Data + empty + pt-BR tooltip-formatter + null-omit assertions"
    - path: "tests/carro-categoria-bars.test.tsx"
      provides: "Render + magnitude (width ratio) + empty assertions"
  key_links:
    - from: "src/components/carro-consumo-chart.tsx"
      to: "src/components/ui/chart.tsx"
      via: "ChartContainer/ChartTooltip/ChartTooltipContent import"
      pattern: "from '@/components/ui/chart'"
    - from: "src/components/carro-consumo-chart.tsx"
      to: "src/lib/carro/consumo.ts"
      via: "kmPerLitroLabel for tooltip pt-BR formatting"
      pattern: "kmPerLitroLabel"
    - from: "src/components/carro-categoria-bars.tsx"
      to: "src/lib/money.ts"
      via: "formatCents for the mono amount label"
      pattern: "formatCents"
---

<objective>
Create the two net-new Phase-11 presentation leaf components — `CarroConsumoChart` (recharts km/l-over-time line) and `CarroCategoriaBars` (gasto-por-categoria magnitude bars) — and their Wave-0 component tests. These are pure-data client components (the RSC supplies the series); they define the interface the detail-page wiring (Plan 03) consumes.

Purpose: Isolate the two new visual primitives behind a tested, pure-data contract so the detail-page RSC wiring is a thin compose step. Both mirror frozen Phase-7 analogs (`ReceitaGastoChart`, `AdherenceBar`) verbatim in structure — no new tokens, no new deps (recharts + the react-is override shipped Phase 7).
Output: `carro-consumo-chart.tsx`, `carro-categoria-bars.tsx`, and two vitest component suites covering data render, empty state, pt-BR formatting, and null discipline. (CAR-05.2, CAR-05.4)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-detalhe-do-carro-gr-fico-de-consumo/11-CONTEXT.md
@.planning/phases/11-detalhe-do-carro-gr-fico-de-consumo/11-UI-SPEC.md
@.planning/phases/11-detalhe-do-carro-gr-fico-de-consumo/11-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CarroConsumoChart — km/l-over-time line (recharts via shadcn chart)</name>
  <files>src/components/carro-consumo-chart.tsx, tests/carro-consumo-chart.test.tsx</files>
  <read_first>
    - src/components/receita-gasto-chart.tsx — the FROZEN line/bar chart analog (template): 'use client', ChartContainer with a `chartConfig` keyed series, `ChartTooltip`+`ChartTooltipContent` with a `formatter`, `CartesianGrid vertical={false}`, `XAxis` with `tickLine={false} axisLine={false} tickMargin={8}`, `accessibilityLayer`, `min-h-[240px] w-full`, the labeled-empty `min-h-[240px]` flex block. Mirror this structure for a LineChart.
    - src/components/ui/chart.tsx — exports ChartContainer, ChartTooltip, ChartTooltipContent, ChartConfig (the only chart primitives to import).
    - src/lib/carro/consumo.ts — `kmPerLitroLabel(kmPorLitro: number | null)` → "12,4 km/l" or '—'; reuse it for the tooltip formatter (pt-BR one-decimal discipline).
    - .planning/phases/11-detalhe-do-carro-gr-fico-de-consumo/11-UI-SPEC.md §Screen Contracts item 4 (chart spec) + §Copywriting (empty copy) + §Color (--chart-1 gold series).
  </read_first>
  <behavior>
    - Given a series of >=2 valid `{ data: string (dd/MM label), kmPorLitro: number }` points, the component renders the chart wrapper (ChartContainer present) and does NOT render the empty-state copy.
    - Given a series with fewer than 2 valid points (0 or 1), the component renders the empty block with the exact pt-BR copy "Sem dados de consumo ainda" and "Registre abastecimentos com tanque cheio para ver a curva de km/l aqui." and renders NO chart.
    - The tooltip `formatter` returns a pt-BR km/l string via `kmPerLitroLabel` (e.g. 12.4 → "12,4 km/l"); a test asserts the formatter output directly.
    - aria-label is exactly "Consumo de km/l ao longo do tempo".
  </behavior>
  <action>
    Create `src/components/carro-consumo-chart.tsx` as a `'use client'` component mirroring `ReceitaGastoChart`'s structure but as a recharts `LineChart`. Export a `CarroConsumoDatum` type `{ data: string; kmPorLitro: number }` (data = the pre-formatted dd/MM SP-pinned x label; the RSC formats dates — this component is pure data and does NO date math). Define `chartConfig` as `{ kmPorLitro: { label: 'km/l', color: 'var(--chart-1)' } } satisfies ChartConfig` so the `.dark` flip re-themes the line with zero JS (the gold primary series per UI-SPEC §Color). Accept `{ data: CarroConsumoDatum[] }`.

    The CALLER (Plan 03 RSC) is responsible for dropping null-km/l intervals; this component additionally guards by filtering to finite, positive `kmPorLitro` points so a stray null never plots a gap-filled 0 (UI-SPEC: "Never plot a 0 or a gap-filled value"). Compute `validPoints = data.filter(d => Number.isFinite(d.kmPorLitro) && d.kmPorLitro > 0)`.

    When `validPoints.length < 2`: render the empty block — the same `min-h-[240px]` centered flex grammar as `ReceitaGastoChart`'s empty block, with the h-equivalent heading text "Sem dados de consumo ainda" (`text-foreground font-heading text-base`) and body "Registre abastecimentos com tanque cheio para ver a curva de km/l aqui." (`text-sm text-muted-foreground`). No spinner. Wrap the whole component in `<section aria-label="Consumo de km/l ao longo do tempo" className="flex flex-col gap-3">`.

    When `validPoints.length >= 2`: render `ChartContainer config={chartConfig} className="min-h-[240px] w-full"` wrapping a `LineChart data={validPoints} accessibilityLayer` with `CartesianGrid vertical={false}`, `XAxis dataKey="data" tickLine={false} axisLine={false} tickMargin={8}`, a `ChartTooltip` whose content is `<ChartTooltipContent formatter={(value) => kmPerLitroLabel(Number(value))} />` (pt-BR via the consumo helper), and a `Line type="monotone" dataKey="kmPorLitro" stroke="var(--color-kmPorLitro)" dot strokeWidth={2}` with visible dots. Import `Line, LineChart, CartesianGrid, XAxis` from `recharts`. No fenced examples — follow `ReceitaGastoChart`'s import + JSX shape exactly, swapping Bar→Line.

    Create `tests/carro-consumo-chart.test.tsx` (@testing-library/react + jsdom): (a) render with 3 valid points → assert NO empty copy present and the aria-labelled section is in the document; (b) render with 0 points and with 1 point → assert "Sem dados de consumo ainda" present; (c) render with a series containing a null/0 kmPorLitro mixed with one valid point → assert it falls to the empty state (only 1 valid → <2); (d) a direct unit assertion that `kmPerLitroLabel(12.4) === '12,4 km/l'` and `kmPerLitroLabel(null) === '—'` (proves the tooltip pt-BR + null discipline without needing recharts SVG). Do NOT assert on SVG geometry or computed colors — recharts SVG is not measurable in jsdom (deferred to the human-verify checkpoint in Plan 04).
  </action>
  <verify>
    <automated>npm test -- carro-consumo-chart && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `carro-consumo-chart.test.tsx` green: data-render (no empty copy), empty-state (0 and 1 point), null-omit (mixed series → empty), and the pt-BR/null tooltip-formatter unit assertions all pass.
    - Component is `'use client'`, imports only `ChartContainer/ChartTooltip/ChartTooltipContent/ChartConfig` from `@/components/ui/chart` and `kmPerLitroLabel` from `@/lib/carro/consumo`; no service-role/secret import, no data fetch (pure props).
    - Series color resolves via `var(--chart-1)` through ChartContainer's `--color-kmPorLitro` (token-aware; no hardcoded hex).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>The km/l line chart renders valid intervals with a pt-BR tooltip, omits null/0 points, shows the pt-BR empty copy below 2 valid points, and is fully unit-tested for everything jsdom can observe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CarroCategoriaBars — gasto-por-categoria magnitude bars</name>
  <files>src/components/carro-categoria-bars.tsx, tests/carro-categoria-bars.test.tsx</files>
  <read_first>
    - src/components/adherence-bar.tsx — the FROZEN horizontal-bar visual grammar to MIRROR (not import): a `bg-muted relative h-2 w-full overflow-hidden rounded-full` track + an inner fill `<div>` with `style={{ width: `${ratioPct}%` }}`. Do NOT import AdherenceBar — its meta/aria-meta semantics (progressbar, meta-marker, direction tokens) do NOT fit a meta-less magnitude bar (UI-SPEC §Screen Contracts item 3). Mirror only the track/fill markup.
    - src/lib/money.ts — `formatCents(cents: number)` → pt-BR "R$ 1.234,56"; use for the mono amount label.
    - .planning/phases/11-detalhe-do-carro-gr-fico-de-consumo/11-UI-SPEC.md §Screen Contracts item 3 + §Color (neutral `bg-muted-foreground` fill, never gold, never the income/consumption/allocation semantic tokens) + §Copywriting (empty copy).
  </read_first>
  <behavior>
    - Given N categorias, renders N bar rows ordered by valor desc; each row shows the categoria name (text-sm) on the left and `formatCents(valor)` mono tabular-nums on the right.
    - The widest bar (largest valor) has width 100%; a bar at half the max has width 50% (fill ratio = valor / maiorValor).
    - Given an empty list, renders exactly the single muted line "Nenhum gasto vinculado a este carro." and no bars.
    - Each row exposes an accessible name combining categoria + formatted valor (color/length never the sole signal).
  </behavior>
  <action>
    Create `src/components/carro-categoria-bars.tsx`. It may be a server component (no client interactivity needed) — pure presentational. Export a `CarroCategoriaDatum` type `{ categoria: string; valorCents: number }`. Accept `{ data: CarroCategoriaDatum[] }`.

    When `data` is empty: render a single `<p className="text-sm text-muted-foreground">Nenhum gasto vinculado a este carro.</p>` (UI-SPEC empty copy) — no track, no zero bars.

    Otherwise: sort a copy of `data` by `valorCents` desc; compute `maiorValor = max(valorCents)` (guard 0 → all bars empty width, but data is non-empty positive cents so this holds). Render a `flex flex-col gap-2` list. Each row: a `flex items-center justify-between gap-4` header with the categoria name (`text-sm`) left and `formatCents(valorCents)` (`font-mono text-sm font-semibold tabular-nums`) right; below it the track — mirror AdherenceBar markup: `<div className="bg-muted relative h-2 w-full overflow-hidden rounded-full">` containing `<div className="bg-muted-foreground h-full rounded-full" style={{ width: `${(valorCents / maiorValor) * 100}%` }} />`. The fill is the NEUTRAL `bg-muted-foreground` (a proportional-magnitude view, never gold, never the semantic money tokens — UI-SPEC §Color). Give each row an accessible name: put `aria-label={`${categoria}: ${formatCents(valorCents)}`}` on the row container, and `aria-hidden` on the decorative track so the visible text label + mono amount carry the value (color is never the sole signal).

    Create `tests/carro-categoria-bars.test.tsx` (@testing-library/react): (a) render 3 categorias with distinct valores → assert all three names + their `formatCents` amounts are present, and rows appear in valor-desc order (query order / textContent order); (b) magnitude: assert the largest-valor row's fill `style.width` is "100%" and a row at half the max is "50%" (read the `[data-slot]` or query the inner div via container.querySelector and check inline style — width ratio is observable in jsdom even though SVG is not); (c) empty: render `data={[]}` → assert "Nenhum gasto vinculado a este carro." present and no bar track rendered. Add a `data-slot="categoria-fill"` attribute to the fill div to make the width queryable.
  </action>
  <verify>
    <automated>npm test -- carro-categoria-bars && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `carro-categoria-bars.test.tsx` green: render+order (valor desc), magnitude (100% / 50% width ratio), and empty-line assertions all pass.
    - Fill uses `bg-muted-foreground` (neutral) — NOT gold, NOT `--income`/`--consumption`/`--allocation` (grep the file: no semantic-money token, no `--chart-`/`--primary` in the fill).
    - Does NOT import `AdherenceBar`; mirrors only the track/fill markup. Uses `formatCents` for the amount; amount is mono `tabular-nums`.
    - Each row carries an accessible name (categoria + valor); the bar is never the sole carrier of the number.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>The magnitude bars render one ordered row per categoria with a neutral fill proportional to the max and a mono pt-BR amount, fall to the single muted empty line when there is no tagged spend, and are unit-tested for render/order/magnitude/empty.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (this plan)

- `src/components/carro-consumo-chart.tsx` — new client recharts line chart (km/l over time), token-aware `--chart-1`, pt-BR tooltip via `kmPerLitroLabel`, null/0-interval drop, pt-BR empty state, pure-data props.
- `src/components/carro-categoria-bars.tsx` — new magnitude bars (categoria + neutral `bg-muted-foreground` fill + `formatCents` mono label), valor-desc order, pt-BR empty line.
- `tests/carro-consumo-chart.test.tsx` — Wave-0 component tests (data, empty, pt-BR formatter, null-omit).
- `tests/carro-categoria-bars.test.tsx` — Wave-0 component tests (render, order, magnitude ratio, empty).
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC → client component | The chart/bars are `'use client'` leaf components; they receive only already-fetched, RLS-scoped, pre-aggregated data as props — no DB client, no env, no secret crosses into them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information Disclosure | `carro-consumo-chart.tsx` (new client bundle) | mitigate | Pure-data props only; no `@/lib/supabase/*`, no `process.env`, no service-role import. The full bundle-secret re-audit (SEC-01) runs in Plan 03 after the detail page imports this. |
| T-11-02 | Information Disclosure | `carro-categoria-bars.tsx` | mitigate | Server-renderable presentational component; receives aggregated `{categoria, valorCents}` only — never the raw transaction rows or another user's data. |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No new dependency this plan (recharts + react-is override shipped Phase 7). No install task → supply-chain surface unchanged. |
</threat_model>

<verification>
- `npm test -- carro-consumo-chart carro-categoria-bars` green.
- `npx tsc --noEmit` clean.
- Full suite remains green at wave close: `npm test` (baseline ~720).
</verification>

<success_criteria>
- Both new components exist, are pure-data, and pass their Wave-0 tests (data + empty + pt-BR + null/magnitude discipline).
- Chart series is token-aware (`--chart-1`); bars fill is neutral (`bg-muted-foreground`); no hardcoded colors, no semantic-money tokens on the bars.
- Neither component imports a Supabase client, env, or secret.
- `npx tsc --noEmit` clean; full suite green.
</success_criteria>

<output>
Create `.planning/phases/11-detalhe-do-carro-gr-fico-de-consumo/11-01-SUMMARY.md` when done.
</output>
