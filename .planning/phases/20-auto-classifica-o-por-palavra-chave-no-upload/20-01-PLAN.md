---
phase: 20-auto-classifica-o-por-palavra-chave-no-upload
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/classifier/keywords.ts
  - src/lib/classifier/keywords.test.ts
  - src/lib/parsers/types.ts
  - src/actions/import.ts
  - src/actions/import.test.ts
autonomous: true
requirements: [KW-02, KW-03, KW-04, KW-05]

must_haves:
  truths:
    - "Um descritor cujo descriptor_norm CONTÉM uma palavra-chave cadastrada chega pré-classificado para a categoria daquela keyword (KW-02), com classification_source='palavra-chave', sem chamar a IA"
    - "A ordem do pipeline é memória → palavra-chave → IA: um hit de memória prevalece e a palavra-chave só roda no ramo de memory-miss (KW-03)"
    - "Quando >1 keyword casa, a keyword mais longa vence; empate de comprimento desempata pela menor categories.sort (KW-04)"
    - "Uma linha classificada por keyword NÃO entra no missNorms, então a IA é chamada com um lote menor (KW-03/CLSAI-03)"
    - "matchKeyword('', rules) retorna null; uma rule com keyword '' nunca casa (guard)"
  prohibitions:
    - "Um hit de memória NUNCA é sobrescrito por uma keyword (a keyword só roda no else do memory-miss)"
    - "Uma linha com keyword hit NUNCA entra no missNorms e NUNCA é passada a classifyDescriptors"
    - "A keyword NUNCA seta reserva_id (category-only; reserva_id permanece null)"
    - "O matcher NUNCA re-normaliza o keyword nem o descriptor_norm (ambos já vêm de normalizeDescriptor)"
    - "PASS 2 NUNCA sobrescreve uma linha de keyword (o gate category_id !== null já a protege)"
  artifacts:
    - path: "src/lib/classifier/keywords.ts"
      provides: "matchKeyword puro + KeywordRule/KeywordMatch types"
      exports: ["matchKeyword", "KeywordRule", "KeywordMatch"]
      min_lines: 30
    - path: "src/lib/classifier/keywords.test.ts"
      provides: "Testes unitários do matcher (substring, longest-wins, empate, vazio)"
    - path: "src/lib/parsers/types.ts"
      provides: "ClassificationSource estendido com 'palavra-chave'"
      contains: "palavra-chave"
    - path: "src/actions/import.ts"
      provides: "Fetch RLS-scoped de category_keywords + keyword pass no PASS 1"
      contains: "matchKeyword"
  key_links:
    - from: "src/actions/import.ts"
      to: "src/lib/classifier/keywords.ts"
      via: "matchKeyword(raw.descriptor_norm, keywordRules) no ramo de memory-miss do PASS 1"
      pattern: "matchKeyword\\("
    - from: "src/actions/import.ts"
      to: "category_keywords (Supabase, RLS)"
      via: "supabase.from('category_keywords').select('category_id, keyword, categories(sort)')"
      pattern: "from\\('category_keywords'\\)"
---

<objective>
Inserir a camada determinística **palavra-chave** entre o PASS 1 (memória) e a chamada de IA em `src/actions/import.ts`, fechando o pipeline memória → palavra-chave → IA (KW-02/03/04/05-lógica).

Purpose: Uma linha cujo `descriptor_norm` contém uma keyword cadastrada (Phase 19) chega pré-classificada de graça, sem clique e sem queimar uma chamada de IA — encolhendo o lote enviado ao modelo.
Output: Novo matcher puro `matchKeyword` (+ teste), o union `ClassificationSource` estendido, UM fetch RLS-scoped de `category_keywords`, o keyword pass no PASS 1, e os testes de pipeline (ordem, exclusão do missNorms, longest-wins) na harness existente.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-auto-classifica-o-por-palavra-chave-no-upload/20-CONTEXT.md
@.planning/phases/20-auto-classifica-o-por-palavra-chave-no-upload/20-RESEARCH.md
@.planning/phases/20-auto-classifica-o-por-palavra-chave-no-upload/20-VALIDATION.md

# Ground edits in the actual source — read these first.
@src/lib/classifier/memory.ts
@src/lib/parsers/types.ts
</context>

<artifacts_this_phase_produces>
NEW symbols introduced by THIS plan (downstream plan 20-02 consumes the `'palavra-chave'` ClassificationSource value):
- `matchKeyword(descriptorNorm, rules)` — new pure fn in `src/lib/classifier/keywords.ts`
- `KeywordRule { categoryId, keyword, sort }` + `KeywordMatch { categoryId }` — new exported interfaces in `keywords.ts`
- `'palavra-chave'` — new member of the `ClassificationSource` union in `src/lib/parsers/types.ts`
- The `keywordRules` upfront fetch + the keyword pass on the memory-miss branch in `src/actions/import.ts`

NO new file beyond `keywords.ts` (+ its test). NO migration (uses Phase 19's `category_keywords`). NO PROD push.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Matcher puro matchKeyword + testes unitários (Wave 0 test scaffold)</name>
  <files>src/lib/classifier/keywords.ts, src/lib/classifier/keywords.test.ts</files>
  <read_first>
    - src/lib/classifier/memory.ts (o contrato hit/null `MemoryHit { category_id } | null` que matchKeyword espelha — mas puro/síncrono, sem Supabase)
    - src/lib/normalize.ts (header: descriptor_norm é derivado UMA vez; o matcher NUNCA re-normaliza)
    - 20-RESEARCH.md Pattern 1 (assinatura + algoritmo) e Code Examples §Matcher unit tests (casos exatos)
  </read_first>
  <behavior>
    - Test "substring match": matchKeyword('uber trip', rules) → { categoryId: 'transp' } quando rules contém { keyword: 'uber' }
    - Test "longest wins" (KW-04): com rules {'mercado'→alim, 'mercado livre'→mkt}, matchKeyword('compra mercado livre sp', rules) → { categoryId: 'mkt' }
    - Test "no match → null": matchKeyword('padaria ze', rules) → null
    - Test "'' matches nothing": matchKeyword('', rules) → null
    - Test "empate de comprimento → menor sort vence" (KW-04): rules [{keyword:'pão',sort:2,categoryId:'a'},{keyword:'pão',sort:1,categoryId:'b'}], matchKeyword('pão de queijo', tie) → { categoryId: 'b' }
    - Test defensivo "rule com keyword '' nunca casa": uma rule { keyword: '' } é ignorada (não casa tudo)
  </behavior>
  <action>
    Crie `src/lib/classifier/keywords.ts` espelhando o contrato hit/null de `memory.ts`, porém PURO e SÍNCRONO (a lista de keywords é pré-buscada uma vez, então o match é um scan em memória, não um point-read).

    Exporte a interface `KeywordRule` com os campos `categoryId: string`, `keyword: string` (saída de normalizeDescriptor — Phase 19 já guarda normalizado), `sort: number` (categories.sort — fonte determinística do desempate). Exporte `KeywordMatch { categoryId: string }` (category-only — reserva fica de fora).

    Exporte `matchKeyword(descriptorNorm: string, rules: KeywordRule[]): KeywordMatch | null`:
    - Guard: `descriptorNorm === ''` retorna null (sentinela de não-match do normalize).
    - Itere as rules; pule qualquer rule cujo `keyword === ''` (defensivo — uma keyword vazia daria includes-match em tudo).
    - Considere apenas rules cujo `keyword` está contido em `descriptorNorm` (substring `descriptorNorm.includes(rule.keyword)`).
    - Eleja a vencedora: keyword de MAIOR comprimento; empate de comprimento (categorias diferentes) → menor `sort` vence.
    - Retorne `{ categoryId: best.categoryId }` ou null.
    - NÃO re-normalize nada (anti-pattern documentado — drift entre chave de match e chave de display). TS estrito, sem `any`.

    Crie `src/lib/classifier/keywords.test.ts` (Vitest, puro, sem mocks) cobrindo os casos em `<behavior>` — espelhe o estilo de um teste de normalize.
  </action>
  <verify>
    <automated>npx vitest run src/lib/classifier/keywords.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `keywords.test.ts` cobre substring, longest-wins (KW-04), empate-por-sort (KW-04), '' e rule-'' guard, todos verdes.
    - `npx tsc --noEmit` limpo (sem `any`, KeywordRule/KeywordMatch exportados e tipados).
  </acceptance_criteria>
  <done>matchKeyword é puro, longest-wins com desempate por sort, '' não casa, e os testes unitários passam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ClassificationSource += 'palavra-chave' + fetch RLS-scoped + keyword pass no PASS 1</name>
  <files>src/lib/parsers/types.ts, src/actions/import.ts, src/actions/import.test.ts</files>
  <read_first>
    - src/actions/import.ts linhas 421-435 (pré-fetch de categoryList), 455-488 (PASS 1 com lookupMemory + missNorms), 490-525 (a UMA chamada de IA + PASS 2 que pula category_id !== null), 780-882 (confirmImport learn loop — REFERÊNCIA, NÃO mexer)
    - src/lib/parsers/types.ts linha 53 (ClassificationSource) + ParsedReviewRow
    - src/lib/classifier/keywords.ts (matchKeyword/KeywordRule — produzidos na Task 1)
    - src/actions/import.test.ts linhas 84-238 (makeBuilder: selectCols, o dispatch `.then` por tabela, a branch `from === 'categories'` ~197), 143-147 (merchant_patterns/memoryHits), 277-296 (classifyDescriptors spy + aiSettings), 305-318 (beforeEach resets)
    - 20-RESEARCH.md Pattern 2 (fetch join `categories(sort)`), Pattern 3 (o diff exato 474-477), Pitfall 4 (`k.categories?.sort ?? 0`), Code Examples §Pipeline ordering test
  </read_first>
  <behavior>
    - Test "ordering memória>keyword>IA": memoryHits cobre o descritor "netflix"; keywordRows tem {keyword:'uber'→cat-transp}; um terceiro descritor é true-miss. Assert: a linha uber tem category_id='cat-transp' e classification_source='palavra-chave'; classifyDescriptors é chamado UMA vez com SÓ o descritor true-miss (a linha uber está AUSENTE do array passado a classifyDescriptors); a linha netflix mantém source='memória'.
    - Test "longest-wins end-to-end" (KW-04): keywordRows [{keyword:'mercado',sort:0→cat-alim},{keyword:'mercado livre',sort:1→cat-mkt}]; um descritor "mercado livre ..." → category_id='cat-mkt'.
    - Test "keyword não polui o lote da IA": o array em classifyDescriptors.mock.calls[0][0] NÃO contém o descriptor_norm da linha de keyword.
    - Test "memória prevalece": um descritor que é hit de memória E casaria uma keyword mantém source='memória' (a keyword não roda).
  </behavior>
  <action>
    Edite `src/lib/parsers/types.ts:53` — estenda o union para `'memória' | 'palavra-chave' | 'manual' | 'sugerida' | null`. Atualize o comentário se necessário.

    Em `src/actions/import.ts`, logo após o pré-fetch de `categoryList` (~linha 435) e antes do PASS 1, adicione UM fetch RLS-scoped de keywords (mirror do padrão batched-fetch, sem query por linha — anti-pattern WR-02):
    - `supabase.from('category_keywords').select('category_id, keyword, categories(sort)')`.
    - Mapeie para `KeywordRule[]`: `{ categoryId: k.category_id, keyword: k.keyword, sort: k.categories?.sort ?? 0 }`. Use o optional-chaining + `?? 0` para satisfazer o strict-null (a FK garante a linha; sort default é 0). Se o type gerado tipar `categories` como array (A1), use `k.categories?.[0]?.sort ?? 0` — verifique em `src/types/database.types.ts` no momento da edição. NÃO selecione `id` (não é usado).
    - NÃO adicione filtro `user_id` em app-code — a RLS de `0036_category_keywords` (Phase 19) escopa o fetch ao caller (mesmo padrão de categories/merchant_patterns).
    - Importe `matchKeyword` e o type `KeywordRule` de `@/lib/classifier/keywords`.

    No PASS 1, substitua o `else { missNorms.add(...) }` (linhas ~474-477) por: no memory-miss, tente `const kw = matchKeyword(raw.descriptor_norm, keywordRules)`; se `kw`, seta `categoryId = kw.categoryId` e `source = 'palavra-chave'` (reservaId fica null — category-only, KW-CONTEXT) e NÃO adicione a `missNorms` (a linha é binding pre-fill, mirror da memória, KW-02); senão (true miss), `missNorms.add(raw.descriptor_norm)` exatamente como hoje. Toda a montagem de `rows.push({...})` abaixo fica intocada.

    NÃO altere o PASS 2 — o gate `if (row.category_id !== null) continue` (linha ~519) já impede que uma linha de keyword seja sobrescrita por uma sugestão de IA (precedência keyword>IA cai de graça — KW-03). NÃO altere o confirmImport (~780-882): o learn loop é category-gated e origin-agnóstico, então uma linha de keyword já aprende merchant→category como memória/manual hoje (KW-05 não precisa de mudança no confirm). NÃO toque na label `classification_source: r.categoryId ? 'memória' : null` da linha ~791 (aproximação coarse pré-existente da transação persistida; a proveniência review-time vive na parsed row).

    Estenda `src/actions/import.test.ts` na harness existente:
    - Adicione um state `let keywordRows: ...[] = []` (resetado em `beforeEach` para `[]`) e uma branch no dispatch `.then` do `makeBuilder`: `if (from === 'category_keywords') return resolve({ data: keywordRows, error: null })` (mirror da branch `from === 'categories'` ~197). A forma de cada linha mock: `{ category_id, keyword, categories: { sort } }`.
    - Adicione os testes em `<behavior>` no describe de `ingestStatement`, reusando memoryHits, o spy classifyDescriptors e aiSettings (não-null para acionar o caminho de IA). Asserte a exclusão do missNorms via `classifyDescriptors.mock.calls[0][0]`.
  </action>
  <verify>
    <automated>npx vitest run src/actions/import.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Os 4 testes de pipeline (ordem, longest-wins, exclusão do lote de IA, memória-prevalece) passam.
    - `npx tsc --noEmit` limpo (union estendido, fetch tipado, sem `any`).
    - A suíte existente de `import.test.ts` permanece verde (nenhuma regressão no PASS 2 / confirm).
    - O fetch de `category_keywords` NÃO tem filtro `user_id` em app-code (RLS-scoped).
  </acceptance_criteria>
  <done>O PASS 1 classifica linhas por keyword (binding, source='palavra-chave'), exclui-as do missNorms, memória prevalece, e os testes mockados de pipeline passam sem live Supabase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client upload → Server Action (ingestStatement) | descritor de fatura cruza aqui; já normalizado server-side (WR-01) |
| Server Action → Postgres (category_keywords) | leitura escopada por RLS sob a sessão do caller |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Information Disclosure | `category_keywords` fetch em import.ts | mitigate | Fetch roda no client RLS-ativo (`createClient()`); a policy `using (auth.uid() = user_id)` de 0036 retorna SÓ as rules do caller. NENHUM filtro `user_id` em app-code — o gate é o Postgres, igual a categories/merchant_patterns. |
| T-20-02 | Tampering / Elevation | category_id forjado via keyword rule | accept | A keyword aponta para uma `category_id` FK-ligada a `categories` sob a mesma RLS do usuário; Phase 19 `addKeyword` deriva `user_id` de `getClaims().sub`, nunca do client — uma keyword só pode apontar para uma categoria própria. Sem nova superfície. |
| T-20-03 | Tampering | matcher determinístico | accept | matchKeyword é puro/in-memory sobre dados já validados+normalizados no write-time (Phase 19). Sem trust no modelo (match é determinístico, não toca a LLM — REDUZ a superfície de IA). |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado nesta fase (sem registro, sem install). Único import novo: símbolos internos de `keywords.ts`. Gate de legitimidade: não aplicável. |
</threat_model>

<verification>
- `npx vitest run src/lib/classifier/keywords.test.ts` — matcher (KW-02/KW-04) verde.
- `npx vitest run src/actions/import.test.ts` — pipeline (KW-02/03/04) + sem regressão verde.
- `npx tsc --noEmit` — strict TS limpo (union estendido + fetch tipado).
- Por-wave merge: `npx vitest run src/lib/classifier src/actions/import.test.ts`.
</verification>

<success_criteria>
- matchKeyword puro, longest-wins com desempate por sort, '' não casa (KW-02/KW-04).
- PASS 1: memory-miss → keyword hit seta category_id + source='palavra-chave', binding, excluída do missNorms (KW-02/KW-03).
- Memória prevalece; keyword roda antes da IA; lote de IA encolhe (KW-03).
- confirmImport intocado — keyword aprende como hoje (KW-05).
- ClassificationSource estende 'palavra-chave'; tsc limpo.
</success_criteria>

<output>
Create `.planning/phases/20-auto-classifica-o-por-palavra-chave-no-upload/20-01-SUMMARY.md` when done.
</output>
