---
phase: 15-classification-wire
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/lib/classifier/suggest.ts
  - src/lib/classifier/suggest.test.ts
  - src/actions/import.ts
  - src/actions/import.test.ts
  - tests/pii-guard.test.ts
  - "src/app/(app)/importar/page.tsx"
autonomous: true
requirements: [CLSAI-01, CLSAI-02, CLSAI-03, CLSAI-05, CLSAI-06]
must_haves:
  truths:
    - "An upload where every descriptor is a memory hit triggers ZERO AI calls (memory-first)"
    - "M rows sharing N unique miss descriptors trigger exactly ONE batched AI call carrying N descriptors"
    - "A memory-miss row gets row.suggestion attached but row.category_id stays null (no auto-commit)"
    - "No key / provider error → empty suggestions, the upload + review grid stay fully usable"
    - "suggestCategory delegates to a 1-item classify and returns null when there is no key"
    - "merchant_patterns is still written ONLY in confirmImport (the v1.3 learn loop is untouched)"
  artifacts:
    - path: "src/actions/import.ts"
      provides: "Two-pass ingest reshape (PASS1 memory / COLLECT unique misses / ONE classify / PASS2 map-back) + maxDuration"
      contains: "classifyDescriptors"
    - path: "src/lib/classifier/suggest.ts"
      provides: "suggestCategory delegates to classifyDescriptors (1-item PII-safe wrapper)"
      contains: "classifyDescriptors"
    - path: "src/actions/import.test.ts"
      provides: "0-call / 1-call-N-unique / no-auto-commit / fallback-no-key ingest edges"
    - path: "tests/pii-guard.test.ts"
      provides: "Updated (b)/(c) — payload-only-descriptorNorm guard, no-ai-umbrella invariant kept"
  key_links:
    - from: "src/actions/import.ts"
      to: "src/lib/ai/classify.ts"
      via: "ONE classifyDescriptors call over the unique miss descriptor_norm set"
      pattern: "classifyDescriptors\\("
    - from: "src/actions/import.ts"
      to: "src/lib/ai/settings.server.ts"
      via: "getDecryptedAiSettings() gates the AI path; null → skip (no key)"
      pattern: "getDecryptedAiSettings\\("
    - from: "src/lib/classifier/suggest.ts"
      to: "src/lib/ai/classify.ts"
      via: "suggestCategory delegates to a 1-item classifyDescriptors"
      pattern: "classifyDescriptors\\("
---

<objective>
Wire the real AI call into the ingest pipeline. Reshape `src/actions/import.ts`'s per-row loop into TWO passes around ONE batched `classifyDescriptors` call (Plan 01): PASS 1 keeps the unchanged `lookupMemory` front door and collects the SET of unique miss `descriptor_norm`; if that set is non-empty AND a key exists, make exactly ONE call; PASS 2 attaches `row.suggestion` per matching row WITHOUT ever setting `row.category_id`. Rewrite `suggestCategory` into a thin PII-safe 1-item wrapper that delegates to `classifyDescriptors`. Set `maxDuration ≥ 60` on the correct import segment. Update the three affected test files.

Purpose: This is the wire that turns the inert seam into the milestone's core value (memory-first AI suggestions on cache-miss) while preserving every locked invariant — memory-first zero-call (CLSAI-02), one batched call (CLSAI-03), no auto-commit (CLSAI-05), and graceful fallback (CLSAI-06). The v1.3 ingest→review→confirm→learn pipeline and the `confirmImport` learn loop are untouched.

Output: reshaped `import.ts`, rewritten `suggestCategory`, `maxDuration` raised, and `import.test.ts` / `pii-guard.test.ts` / `suggest.test.ts` updated.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-classification-wire/15-RESEARCH.md
@.planning/phases/15-classification-wire/15-CONTEXT.md
@.planning/phases/15-classification-wire/15-01-SUMMARY.md

# The files this plan edits — read for exact current shape before reshaping:
@src/actions/import.ts
@src/lib/classifier/suggest.ts
@src/lib/ai/settings.server.ts
@src/lib/ai/classify.ts
@tests/pii-guard.test.ts
@src/lib/classifier/suggest.test.ts
</context>

<artifacts_this_phase_produces>
This plan produces the remaining two phase artifacts:
- **The `import.ts` two-pass wire** — PASS1 memory / COLLECT unique misses / ONE `classifyDescriptors` / PASS2 map-back, attaching `row.suggestion` and never `row.category_id`; plus an additive non-blocking `iaIndisponivel` note on the summary.
- **The `suggestCategory` wrapper rewrite** — now a 1-item PII-safe delegate to `classifyDescriptors` that reads `getDecryptedAiSettings()` itself and returns null on no-key.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite suggestCategory as a 1-item delegate; fix suggest.test.ts to mock the DAL</name>
  <files>src/lib/classifier/suggest.ts, src/lib/classifier/suggest.test.ts</files>
  <read_first>
    - src/lib/classifier/suggest.ts (the current null-seam body + the validateSuggestion wrapper which stays EXACTLY as-is)
    - src/lib/ai/settings.server.ts (getDecryptedAiSettings → { provider, model, apiKey } | null; null = no key)
    - src/lib/ai/classify.ts (classifyDescriptors — Plan 01; the wrapper delegates to a 1-item call)
    - src/lib/classifier/suggest.test.ts (the existing tests asserting null + no-fetch — must stay GREEN by mocking getDecryptedAiSettings → null; RESEARCH Open Q3 / Assumption A4)
    - .planning/phases/15-classification-wire/15-RESEARCH.md (Pattern 6 — the wrapper shape and the suggest.test.ts caveat)
  </read_first>
  <behavior>
    - suggestCategory('mercado livre', CATEGORIES) with getDecryptedAiSettings mocked → null still returns null and never calls the provider (the no-key + no-fetch invariants the existing tests pin).
    - suggestCategory still returns null for an injection-style descriptor and for an empty category list.
    - validateSuggestion behavior is unchanged (its existing tests stay GREEN untouched).
  </behavior>
  <action>
    Rewrite the `suggestCategory` BODY (keep the exported signature `(descriptorNorm: string, categories: { id: string; name: string }[]) => Promise<string | null>`): call `getDecryptedAiSettings()`; if it returns null, return null (no key → pre-IA fallback, no provider fetch). Otherwise delegate to `classifyDescriptors([descriptorNorm], categories, aiSettings)` and return `map.get(descriptorNorm)?.categoryId ?? null`. Remove the `void descriptorNorm/void categories/return null` v1 stub and the dead FUTURE comment block. Keep `validateSuggestion` EXACTLY as it is (still the SEC-03 gate, now also called inside classify). Update the file head comment from "v1 returns null for EVERY input" to describe the wired 1-item PII-safe delegate (only descriptor_norm egresses; output enum-gated; null on no-key) — by concept, do not paste a negative-grep literal.

    Then update `src/lib/classifier/suggest.test.ts`: add `vi.mock('@/lib/ai/settings.server', () => ({ getDecryptedAiSettings: vi.fn(async () => null) }))` (and mock `@/lib/ai/provider-factory` if needed) so the no-key path is deterministic without a live Supabase. The three `suggestCategory returns null` assertions and the `makes no fetch/network call` assertion MUST stay GREEN under the mock. The `validateSuggestion` describe block stays unchanged. Update the file head comment to reflect that null now comes from the mocked no-key DAL, not a hard-coded stub.
  </action>
  <verify>
    <automated>npx vitest run src/lib/classifier/suggest.test.ts</automated>
  </verify>
  <done>`suggestCategory` delegates to `classifyDescriptors` and returns null on no-key; `suggest.test.ts` is GREEN with `getDecryptedAiSettings` mocked → null (null + no-fetch invariants hold); `validateSuggestion` tests unchanged and GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reshape import.ts into two passes around ONE batched classify call (+ summary note)</name>
  <files>src/actions/import.ts, src/actions/import.test.ts</files>
  <read_first>
    - src/actions/import.ts (the ingest loop ~:391-457: categoryList pre-fetch, the per-row lookupMemory + per-row `await suggestCategory(...)` to REPLACE, the IngestSummary build, the parsed_rows persist; and the imports block at the top to add classifyDescriptors + getDecryptedAiSettings)
    - src/lib/ai/classify.ts (classifyDescriptors signature — Plan 01)
    - src/lib/ai/settings.server.ts (getDecryptedAiSettings — server-only DAL)
    - src/lib/parsers/types.ts (ParsedReviewRow.suggestion — Plan 01)
    - src/actions/import.test.ts (the existing ingestStatement describe + the configurable mock state: memoryHits, supabaseMock; add classifyDescriptors + getDecryptedAiSettings mocks and the new edge tests)
    - .planning/phases/15-classification-wire/15-RESEARCH.md (Pattern 4 — the exact two-pass reshape; Pitfalls 1/3/4)
    - .planning/phases/15-classification-wire/15-VALIDATION.md (edges: 0-call-when-all-hits, 1-call-N-unique, no-auto-commit, fallback-no-key)
  </read_first>
  <behavior>
    - 0-call-when-all-hits (CLSAI-02): an upload where every parsed descriptor_norm is in memoryHits → classifyDescriptors NOT invoked (spy count 0); getDecryptedAiSettings NOT called.
    - 1-call-N-unique (CLSAI-03): an upload with M rows but N unique miss descriptor_norm (M>N via duplicates) → classifyDescriptors called exactly ONCE, with an array of the N unique descriptors.
    - no-auto-commit (CLSAI-05): when classifyDescriptors returns a Map with a non-null categoryId for a miss descriptor, the resulting row has row.suggestion = { categoryId, confidence, source:'ia' } AND row.category_id === null. A memory-hit row is never overwritten by a suggestion.
    - fallback-no-key (CLSAI-06): getDecryptedAiSettings mocked → null → no classifyDescriptors call, rows have no suggestion, the upload returns normally (rows + summary), summary carries the non-blocking iaIndisponivel note.
    - fallback-empty-map (CLSAI-06): getDecryptedAiSettings returns settings but classifyDescriptors returns an empty Map → upload returns normally, no suggestion attached, iaIndisponivel note set.
  </behavior>
  <action>
    In `src/actions/import.ts`: add `classifyDescriptors` (from `@/lib/ai/classify`) and `getDecryptedAiSettings` (from `@/lib/ai/settings.server`) to the imports. Replace the single per-row loop (~:415-448) with the two-pass shape from RESEARCH Pattern 4:

    PASS 1 — keep the per-row `lookupMemory` logic EXACTLY (HIT → category_id + reserva_id + source 'memória'); on a MISS, add `raw.descriptor_norm` to a `Set<string>` and push the row with `category_id: null, source: null` (do NOT call `suggestCategory` per row — remove the per-row `await suggestCategory(...)` from the hot path entirely).

    COLLECT — `const uniqueMisses = [...missNorms]`.

    ONE call — if `missNorms.size > 0`: `const aiSettings = await getDecryptedAiSettings()`; if null → set a local `iaIndisponivel = true` (no key, expected) and skip; else `const suggestions = await classifyDescriptors(uniqueMisses, categoryList, aiSettings)`; if `suggestions.size === 0` set `iaIndisponivel = true`. If `missNorms.size === 0`, skip the AI path entirely (the memory-first zero-call path) — do NOT call getDecryptedAiSettings.

    PASS 2 — map results back: for each row with `category_id === null`, look up `suggestions.get(row.descriptor_norm)`; if present and its `categoryId !== null`, set `row.suggestion = { categoryId: s.categoryId, confidence: s.confidence, source: 'ia' }`. NEVER set `row.category_id` from a suggestion (CLSAI-05) and never overwrite a memory hit. Do not call any AI function inside the per-row loop.

    Add an OPTIONAL `iaIndisponivel?: boolean` to the `IngestSummary` interface (additive — see the interface near :69) and set it on the summary when the AI was skipped/failed; it is non-blocking (Phase 16 surfaces it). Leave `total/novas/naoClassificadas/duplicadas/descartadas` math exactly as-is. Persist `parsed_rows` (now carrying optional `row.suggestion`) unchanged — it is the same additive jsonb write. Do NOT touch `confirmImport` or any `merchant_patterns` write.

    Then update `src/actions/import.test.ts`: add `vi.mock('@/lib/ai/classify', ...)` exposing a controllable `classifyDescriptors` spy and `vi.mock('@/lib/ai/settings.server', ...)` exposing a controllable `getDecryptedAiSettings` spy; add configurable mock state + a beforeEach reset (mirror the existing `memoryHits` pattern). Add the five `<behavior>` edge tests under the `ingestStatement` describe. Use an OFX fixture whose descriptors you can map to memoryHits/misses; for 1-call-N-unique, pick a fixture (or seed parsed rows) with duplicate descriptors so M>N. Keep all existing ingestStatement tests GREEN.
  </action>
  <verify>
    <automated>npx vitest run src/actions/import.test.ts</automated>
  </verify>
  <done>`import.ts` runs the two-pass reshape; the five new ingest edges are GREEN (0-call all-hits, 1-call-N-unique, no-auto-commit with category_id null, fallback-no-key, fallback-empty-map); all pre-existing ingestStatement tests stay GREEN; `npx tsc --noEmit` passes. `confirmImport` and `merchant_patterns` writes untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update pii-guard.test.ts to the payload-only-descriptorNorm invariant; raise maxDuration</name>
  <files>tests/pii-guard.test.ts, src/app/(app)/importar/page.tsx</files>
  <read_first>
    - tests/pii-guard.test.ts (the current (a) AI-deps invariant to KEEP, and the (b) suggestCategory-null / (c) no-fetch assertions to REPLACE with the payload guard)
    - src/lib/ai/classify.ts (classifyDescriptors — the test spies modelFor/doGenerate to inspect the sent prompt)
    - src/lib/ai/provider-factory.ts (modelFor — mock target for the doGenerate spy)
    - .planning/phases/15-classification-wire/15-RESEARCH.md (the "Updated pii-guard assertions (b)/(c)" code example — spy doGenerate, assert prompt contains the descriptor_norm and NO amount/raw/date; Pitfall 5)
    - src/app/(app)/importar/page.tsx (current `export const maxDuration = 30`)
    - .planning/phases/15-classification-wire/15-RESEARCH.md (Pitfall 6 + Open Q1 — maxDuration ≥ 60 on the segment that runs the work; the action-vs-page inheritance open question)
  </read_first>
  <behavior>
    - (a) KEPT: the AI deps remain exactly `['@ai-sdk/anthropic', '@ai-sdk/google']` — no `ai` umbrella, no DeepSeek (this catches an accidental umbrella re-add from wiring the call).
    - (b)/(c) REPLACED by one payload guard: calling classifyDescriptors with a descriptor_norm and CATEGORIES, the prompt passed to the mocked doGenerate CONTAINS the descriptor_norm string, and does NOT match a PII pattern (R$ / dd/mm/yyyy / amount / occurred_on / descriptor_raw).
  </behavior>
  <action>
    In `tests/pii-guard.test.ts`: KEEP the `(a)` "AI deps limited to the BYOK provider set (no ai umbrella, no DeepSeek)" test unchanged — it is the load-bearing invariant that wiring the call must not break. REPLACE the `(b)` `suggestCategory returns null` tests and the `(c)` `makes no fetch call` block with a single payload-egress guard per the RESEARCH code example: mock `@/lib/ai/provider-factory` so `modelFor` returns `{ doGenerate: vi.fn().mockResolvedValue({ content:[{type:'text',text: JSON.stringify({results:[]})}], finishReason:'stop', usage:{inputTokens:1,outputTokens:1} }) }`, call `classifyDescriptors(['padaria sao joao'], CATEGORIES, FAKE_SETTINGS)`, then `const sent = JSON.stringify(doGenerate.mock.calls[0]?.[0]?.prompt)` and assert `sent` contains `'padaria sao joao'` and does NOT match `/R\$|\d{2}\/\d{2}\/\d{4}|amount|occurred_on|descriptor_raw/`. Update the file head comment from "(b) suggestCategory returns null / (c) no network call" to the new invariant (the wired call egresses ONLY descriptor_norm; the PII contract is now enforced by the payload guard, not by the absence of a call) — by concept, do not paste a negative-grep literal. Import `classifyDescriptors` and drop the now-unused `suggestCategory` import if nothing else uses it.

    In `src/app/(app)/importar/page.tsx`: raise `export const maxDuration = 30` to `export const maxDuration = 60` (CONTEXT locks ≥ 60; parse + one batched AI call). Update the trailing comment to note it now also bounds one batched LLM classify, not just parse. Because Vercel segment-vs-action inheritance is an open question (RESEARCH Open Q1 / Assumption A3), if `import.ts` (the `'use server'` action) does NOT already carry a route-segment `maxDuration`, ALSO add `export const maxDuration = 60` to `import.ts` so the segment that actually runs the work is bounded regardless of inheritance — the code change is autonomous; the live-PROD confirmation is a manual-only verify (below), not a blocker.
  </action>
  <verify>
    <automated>npx vitest run tests/pii-guard.test.ts</automated>
  </verify>
  <done>`pii-guard.test.ts` keeps the (a) no-umbrella invariant GREEN and asserts the doGenerate prompt carries only descriptor_norm (no PII); `maxDuration` is 60 on the import segment(s); `npx tsc --noEmit` passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ingest action → LLM provider | `descriptor_norm` set crosses to the third-party LLM (BYOK key); untrusted suggestions cross back into row data. |
| suggestion → persisted row | a suggestion must never become `row.category_id` (that would be auto-commit → silent learn on confirm). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05 | Tampering / Repudiation | import.ts PASS 2 | mitigate | `row.category_id` is NEVER set from a suggestion; only `row.suggestion`; `merchant_patterns` write stays solely in `confirmImport` (human confirm). import.test.ts asserts miss rows keep `category_id === null` with a suggestion present |
| T-15-06 | Denial of Service | import.ts AI path | mitigate | the AI path is guarded (no key → skip) and `classifyDescriptors` is the never-throw boundary (Plan 01); the ingest action proceeds with an empty Map; `iaIndisponivel` is non-blocking |
| T-15-07 | Information Disclosure | import.ts prompt source | mitigate | only the unique `descriptor_norm` Set is passed to `classifyDescriptors`; no amount/date/raw row field is forwarded (SEC-03); pii-guard payload assertion enforces |
| T-15-08 | Denial of Service | importar segment maxDuration | accept | `maxDuration` raised to 60 on the page (+ action if needed); the action-vs-page inheritance is verified live in PROD (manual-only, deploy-time) — build/test on LOCAL has no timeout |
| T-15-SC | Tampering | npm installs | accept | Phase 15 installs ZERO packages — no install task, no checkpoint (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- `npx vitest run src/actions/import.test.ts src/lib/classifier/suggest.test.ts tests/pii-guard.test.ts` — all GREEN.
- Per-wave merge gate: `npm test` (full ~797+ suite) + `npx tsc --noEmit` + `npm run build`.
- Memory-first proof: the 0-call test asserts `classifyDescriptors` spy count 0 when all descriptors are hits (CLSAI-02).
- One-call proof: the N-unique test asserts the spy is called once with the deduped descriptor array (CLSAI-03).
- No-auto-commit proof: miss rows have `category_id === null` with a `suggestion` present; `merchant_patterns` capture sink stays empty until `confirmImport` (CLSAI-05).
</verification>

<manual_verifications>
| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Real AI suggestion on a new-merchant upload | CLSAI-01 | Needs a real BYOK key + LOCAL stack | Configure a Gemini/Claude key at /conta/configuracoes-ia (LOCAL), upload a statement with a brand-new merchant → the miss row receives `row.suggestion` (visible once Phase 16 renders it; this phase: confirm via the persisted parsed_rows / a temporary log) |
| maxDuration ≥ 60 covers parse + 1 AI call in PROD | CLSAI-06 | Depends on the Vercel deploy + segment-inheritance behavior | After deploy, confirm `maxDuration` bounds the `ingestStatement` invocation (Open Q1) — upload a statement with several new merchants and verify no 504 |
</manual_verifications>

<success_criteria>
- All-hits upload → ZERO `classifyDescriptors` calls (CLSAI-02).
- M rows / N unique misses → exactly ONE batched call with N descriptors (CLSAI-03).
- A miss row gets `row.suggestion` but `row.category_id` stays null (CLSAI-05); `merchant_patterns` written only in `confirmImport`.
- No key / empty Map → upload + review grid fully usable, non-blocking `iaIndisponivel` note (CLSAI-06).
- `suggestCategory` is a 1-item delegate returning null on no-key (CLSAI-01 seam contract).
- `maxDuration` is ≥ 60 on the import segment(s); pii-guard payload guard GREEN, no-umbrella invariant kept (SEC-03).
</success_criteria>

<output>
Create `.planning/phases/15-classification-wire/15-02-SUMMARY.md` when done.
</output>
