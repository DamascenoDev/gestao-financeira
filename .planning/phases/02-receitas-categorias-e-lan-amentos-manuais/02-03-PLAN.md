---
phase: 02-receitas-categorias-e-lan-amentos-manuais
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/actions/categories.ts
  - src/app/(app)/categorias/page.tsx
  - src/components/categoria-form.tsx
  - src/components/category-badge.tsx
  - src/components/category-delete-dialog.tsx
autonomous: true
requirements: [CAT-02, CAT-03]
user_setup: []

must_haves:
  truths:
    - "User creates, renames and removes categories (CAT-02)"
    - "Deleting a category that has transactions is blocked; the user is offered Arquivar or Reatribuir e remover instead (CAT-02)"
    - "Reassign-and-delete atomically moves the transactions to a target category, then removes the source"
    - "Each category has a consumo/alocação kind toggle that persists; an optional swatch color (CAT-03)"
    - "Seed categories are editable but the UI warns when editing a feature category (e.g. Reserva)"
  artifacts:
    - path: "src/actions/categories.ts"
      provides: "createCategory, renameCategory, setKind, setColor, archiveCategory, deleteCategory, reassignAndDelete"
      exports: ["createCategory", "deleteCategory", "reassignAndDelete", "setKind"]
    - path: "src/app/(app)/categorias/page.tsx"
      provides: "RSC: category list with usos (tx_count from v_category_totals) + kind toggle + color"
      min_lines: 40
    - path: "src/components/categoria-form.tsx"
      provides: "dialog: nome + kind switch + 8-swatch color picker + feature-category warning"
    - path: "src/components/category-badge.tsx"
      provides: "color dot + name, optional kind variant (reused by Extrato)"
    - path: "src/components/category-delete-dialog.tsx"
      provides: "alert-dialog: blocked-with-txns -> Arquivar / Reatribuir e remover"
  key_links:
    - from: "src/actions/categories.ts"
      to: "reassign_and_delete_category RPC"
      via: "supabase.rpc('reassign_and_delete_category', { src, dst })"
      pattern: "reassign_and_delete_category"
    - from: "src/app/(app)/categorias/page.tsx"
      to: "v_category_totals"
      via: "tx_count per category for the usos column + delete-block pre-check"
      pattern: "v_category_totals"
    - from: "src/components/category-delete-dialog.tsx"
      to: "src/actions/categories.ts"
      via: "deleteCategory blocked result drives archive/reassign branch"
      pattern: "blocked"
---

<objective>
Categorias vertical slice: the user manages their category set — create, rename, mark consumo vs alocação, pick a swatch color, and safely remove categories with a delete-block + archive/reassign flow backed by an atomic RPC. End-to-end: Zod-validated Server Actions + an RSC list showing each category's usos (tx_count) and an inline kind toggle.

Purpose: Closes CAT-02/03. After this slice the user can do something new: rename a seed category, flip Investimentos to alocação, color-code categories, and attempt to delete a used category — getting the graceful Arquivar / Reatribuir e remover choice instead of a destructive error.

Output: src/actions/categories.ts (full CRUD + archive + atomic reassign-and-delete), the Categorias page, and the reusable category-badge (consumed by the Extrato slice) + categoria-form + category-delete-dialog. Turns the Wave-0 tests category-delete and category-kind GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-receitas-categorias-e-lan-amentos-manuais/02-RESEARCH.md
@.planning/phases/02-receitas-categorias-e-lan-amentos-manuais/02-UI-SPEC.md

# Contracts created by 02-01 (consume, do not redefine):
@src/lib/schemas/category.ts
@src/types/database.types.ts
# Phase 1 seed (which categories exist + which are alocação/feature):
@supabase/migrations/0002_categories.sql
# Patterns to mirror:
@src/actions/auth.ts
@src/components/auth-form.tsx
# Wave-0 tests this slice must turn green:
@tests/category-delete.test.ts
@tests/category-kind.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Category Server Actions (create/rename/kind/color/archive + delete-block + atomic reassign)</name>
  <files>src/actions/categories.ts</files>
  <read_first>RESEARCH.md Pattern 2 (FK RESTRICT), Pitfall 5 (catch 23503 / pre-check via v_category_totals), the deleteCategory code example, Open Question 2 (atomic reassign RPC); src/actions/auth.ts (boundary pattern); src/lib/schemas/category.ts; tests/category-delete.test.ts + tests/category-kind.test.ts for exact expected behavior.</read_first>
  <behavior>
    - createCategory(formData): inserts a category (name, kind default consumo, optional color) for the user
    - renameCategory(id, name) / setKind(id, kind) / setColor(id, color): persist single-field edits (CAT-03 kind toggle persists)
    - archiveCategory(id): sets is_archived=true; history kept, category hidden from pickers
    - deleteCategory(id): pre-checks tx_count via v_category_totals -> returns { blocked: true, txCount } when > 0; else deletes (23503 RESTRICT is the backstop)
    - reassignAndDelete(src, dst): calls the reassign_and_delete_category RPC -> transactions moved to dst, src removed, atomically
  </behavior>
  <action>
Create src/actions/categories.ts ('use server') mirroring auth.ts: safeParse with lib/schemas/category.ts, getClaims() for the owner, return { error } on failure, revalidatePath('/categorias') on success. deleteCategory returns the discriminated result { ok: true } | { error: string } | { blocked: true; txCount: number }: pre-check by summing tx_count from v_category_totals for the category (cleaner UX) with ON DELETE RESTRICT (error 23503) as the backstop — never surface a raw DB error toast (Pitfall 5). reassignAndDelete invokes supabase.rpc('reassign_and_delete_category', { src, dst }) so the move+delete is atomic (no half-applied state, Open Question 2). archiveCategory flips is_archived (reuse the Phase 1 column). setKind persists consumo<->alocacao (CAT-03). Do not hard-delete a used category from app code — RESTRICT + the blocked branch own that.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npx vitest run category-delete category-kind && npx tsc --noEmit</automated>
  </verify>
  <done>category-delete.test.ts GREEN (direct delete of a used category blocked; reassignAndDelete moves txns + removes source; archive keeps history and hides from pickers) and category-kind.test.ts GREEN (kind toggle persists); tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Categorias page + category-badge / categoria-form / category-delete-dialog</name>
  <files>src/app/(app)/categorias/page.tsx, src/components/categoria-form.tsx, src/components/category-badge.tsx, src/components/category-delete-dialog.tsx</files>
  <read_first>UI-SPEC section 2 (Categorias) — columns cor+nome / Tipo switch / usos / ações, the 8-swatch color picker (slate|red|amber|green|teal|blue|violet|pink), special-category warning copy, blocked-delete alert-dialog copy "Esta categoria tem {n} transações..." with [Arquivar] [Reatribuir e remover]; UI-SPEC Copywriting Contract; src/components/auth-form.tsx (RHF pattern); src/lib/utils.ts (cn).</read_first>
  <action>
Build category-badge.tsx: a swatch dot (mapped from the swatch key to the mid-chroma OKLCH color) + name, with an optional kind variant (Consumo amber / Alocação indigo badge). Keep it self-contained — the Extrato slice (02-04) imports it for the Categoria column. Build categoria-form.tsx: a dialog (RHF + zodResolver over the category schema) with nome (input), tipo (switch consumo/alocação default consumo, CAT-03), and the 8-swatch color picker; when the edited category is a feature seed category (e.g. name === 'Reserva'), render the inline muted warning "Esta categoria é usada pelo fluxo de reservas — alterá-la pode afetar relatórios." Build category-delete-dialog.tsx: an alert-dialog driven by deleteCategory's result — when { blocked, txCount } it shows "Esta categoria tem {n} transações. Você não pode excluí-la diretamente." and offers Arquivar (archiveCategory) or Reatribuir e remover (a target-category select -> reassignAndDelete); when not blocked it shows the standard destructive confirm "Excluir categoria — Esta ação não pode ser desfeita."

Build src/app/(app)/categorias/page.tsx (RSC): list categories (excluding/labelling archived) joined with their tx_count from v_category_totals for the usos column; render cor+nome (CategoryBadge), an inline Tipo switch (calls setKind), usos (muted), and the ações menu (editar -> categoria-form, excluir -> category-delete-dialog). Include the "Nova categoria" CTA and the empty-state copy from UI-SPEC.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npx tsc --noEmit && npm run build && grep -q "Reatribuir e remover" "src/components/category-delete-dialog.tsx"</automated>
  </verify>
  <done>/categorias lists categories with usos + inline kind toggle + color; Nova categoria + edit dialog with the feature-category warning; delete opens the blocked alert-dialog offering Arquivar / Reatribuir e remover (exact UI-SPEC copy); category-badge reusable by Extrato; tsc + build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form → category Server Action | Untrusted name/kind/color/ids cross here |
| deleteCategory → categories/transactions | A delete must never orphan spend history |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-CAT-FK | Tampering / Integrity | deleteCategory | mitigate | v_category_totals pre-check returns { blocked } when tx_count>0; ON DELETE RESTRICT (02-01) backstops a stray delete (23503 caught, never a raw 500); reassignAndDelete RPC is atomic |
| T-02-CAT-VAL | Tampering | createCategory / rename / setColor | mitigate | Zod schema (name 1-60, kind enum, color enum of 8 swatch keys) at the action boundary; rejects free hex / arbitrary kind |
| T-02-CAT-RLS | Information Disclosure | category reads + tx_count | mitigate | RLS on categories (Phase 1) + security_invoker v_category_totals (02-01); actions resolve owner via getClaims() |
</threat_model>

<verification>
- npx vitest run category-delete category-kind GREEN.
- npx tsc --noEmit and npm run build clean.
- Manual (deferred to 02-05 checkpoint): blocked-delete alert-dialog Arquivar / Reatribuir e remover flow.
</verification>

<success_criteria>
- CAT-02: create/rename/remove categories; delete-with-transactions blocked with Arquivar / Reatribuir e remover; reassign is atomic.
- CAT-03: consumo/alocação toggle persists; optional swatch color; feature-category warning shown on seed categories.
</success_criteria>

<output>
Create `.planning/phases/02-receitas-categorias-e-lan-amentos-manuais/02-03-SUMMARY.md` when done
</output>
