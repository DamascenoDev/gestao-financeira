---
phase: 02-receitas-categorias-e-lan-amentos-manuais
plan: 05
type: execute
wave: 3
depends_on: ["02-02", "02-03", "02-04"]
files_modified: []
autonomous: false
requirements: [INC-02, TXN-03, TXN-04]
user_setup: []

must_haves:
  truths:
    - "Full suite (vitest + tsc) is green before the human walkthrough"
    - "The three manual-only behaviors from 02-VALIDATION.md are visually confirmed against the running local app"
  artifacts: []
  key_links: []
---

<objective>
End-of-phase human verification: the three behaviors that 02-VALIDATION.md marks manual-only (browser interaction / UI choice) are confirmed against the running local dev app, after an automated full-suite gate. Closes the loop on INC-02 (edit-choice dialog), TXN-03 (filter URL round-trip), and TXN-04 (bulk-select -> apply) which integration tests cover at the data layer but not at the interaction layer.

Purpose: The UI-SPEC design contract and the selection/filter UX cannot be fully proven by headless tests. This checkpoint verifies the actual user experience before /gsd-verify-work.

Output: A confirmed visual sign-off (or a punch list of issues) for the Receitas, Categorias, and Extrato screens.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-receitas-categorias-e-lan-amentos-manuais/02-UI-SPEC.md
@.planning/phases/02-receitas-categorias-e-lan-amentos-manuais/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite gate before human verification</name>
  <files></files>
  <action>
Confirm the local Supabase stack is running, then run the full suite and type check. All Phase 1 + Phase 2 Wave-0 tests must be green (the seven feature tests turned green by 02-02/03/04, plus view-leak, month, rls-isolation, seed, money, bundle-secret). Start the dev server in the background for the human walkthrough (npm run dev). If any test is red, STOP and report which — do not proceed to the checkpoint.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <done>Full vitest suite green and tsc --noEmit clean; dev server reachable for the walkthrough.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
The three Phase 2 screens are live on the local dev app inside the new sidebar + global MonthSelector shell:
- Receitas: receita líquida hero + recurring/avulsa income with the INC-02 "só neste mês vs template" edit choice
- Categorias: list with usos + inline consumo/alocação toggle + color, and the blocked-delete Arquivar / Reatribuir e remover flow
- Extrato: dense filterable table (month + multi-category, URL-persisted), per-category totals, and multi-row bulk re-classify via the SelectionActionBar
  </what-built>
  <how-to-verify>
With the dev server running (default http://localhost:3000), signed in:

1. INC-02 edit-choice (Receitas):
   - Go to Receitas. Create a recurring income (Fonte "Salário", a value, recorrente on, a day). Confirm it appears and the "receita líquida do mês" hero reflects it.
   - Edit that occurrence's value and pick "Alterar só em {mês}". Switch the MonthSelector to the next month and back: the edited month keeps the new value; the template/other month is unchanged. Add an avulsa (recorrente off, a date) and confirm a second avulsa in the same month is allowed and the hero sums both.

2. TXN-03 filter URL round-trip (Extrato):
   - Go to Extrato. Change the month via the MonthSelector and select 1-2 categories in the category filter. Confirm the list updates, the per-category + grand totals update, and the browser URL contains ?mes=...&cat=.... Reload the page: the same filter survives (state is in the URL).

3. TXN-04 bulk re-classify (Extrato):
   - Add a few transactions in the current month across categories. Select several rows (try the header select-all and a shift-click range). Confirm the SelectionActionBar shows "{n} selecionadas". Pick a target category and click Reclassificar. Confirm all selected rows move to that category and a "{n} transações reclassificadas" toast appears (with Desfazer where shown).

Also sanity-check the design contract: teal is the only accent (CTAs/active nav), income is green with a +, gasto is neutral (not red), money is mono/right-aligned, the Extrato rows are dense.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues (per screen) to fix.</resume-signal>
</task>

</tasks>

<verification>
- Automated: full vitest suite + tsc green (Task 1 gate).
- Human: INC-02 edit-choice, TXN-03 filter URL persistence, TXN-04 bulk re-classify confirmed visually; design contract (accent/income/expense/density) sanity-checked.
</verification>

<success_criteria>
- Full suite green before the walkthrough.
- All three manual-only behaviors from 02-VALIDATION.md confirmed on the running app.
- Reviewer signs off "approved" (or a punch list is captured for a gap-closure pass).
</success_criteria>

<output>
Create `.planning/phases/02-receitas-categorias-e-lan-amentos-manuais/02-05-SUMMARY.md` when done
</output>
