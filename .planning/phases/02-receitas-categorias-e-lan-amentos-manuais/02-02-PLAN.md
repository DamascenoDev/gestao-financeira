---
phase: 02-receitas-categorias-e-lan-amentos-manuais
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/actions/incomes.ts
  - src/app/(app)/receitas/page.tsx
  - src/components/receita-form.tsx
  - src/components/money-input.tsx
  - src/components/amount-cell.tsx
autonomous: true
requirements: [INC-01, INC-02, INC-03, INC-04]
user_setup: []

must_haves:
  truths:
    - "User creates a recurring income template (Salário/Pensão) that materializes one editable occurrence for the selected month (INC-01)"
    - "Editing a single month's occurrence changes only that occurrence — not the template, not other months (INC-02)"
    - "Re-opening a month never duplicates or clobbers an edited occurrence (idempotent materialize-on-read)"
    - "User adds an ad-hoc (avulsa) income with no template; multiple avulsas in one month are allowed (INC-03)"
    - "The Receitas page shows the 'receita líquida do mês' hero = SUM of all month incomes from v_income_month (INC-04)"
  artifacts:
    - path: "src/actions/incomes.ts"
      provides: "ensureMonthOccurrences, createIncomeTemplate, updateOccurrence, updateTemplate, createAdhocIncome, deleteOccurrence"
      exports: ["ensureMonthOccurrences", "createIncomeTemplate", "createAdhocIncome"]
    - path: "src/app/(app)/receitas/page.tsx"
      provides: "RSC: materializes month, lists occurrences, renders líquida hero from v_income_month"
      min_lines: 40
    - path: "src/components/receita-form.tsx"
      provides: "dialog form: fonte + valor + recorrente toggle (day-of-month) vs avulsa (date)"
    - path: "src/components/money-input.tsx"
      provides: "R$ input parsed via parseBRLToCents at submit"
    - path: "src/components/amount-cell.tsx"
      provides: "mono tabular income amount with + and text-income"
  key_links:
    - from: "src/app/(app)/receitas/page.tsx"
      to: "v_income_month"
      via: "supabase.from('v_income_month') for the ?mes hero"
      pattern: "v_income_month"
    - from: "src/app/(app)/receitas/page.tsx"
      to: "src/actions/incomes.ts"
      via: "ensureMonthOccurrences(monthKey) on render"
      pattern: "ensureMonthOccurrences"
    - from: "src/actions/incomes.ts"
      to: "income_occurrences"
      via: "upsert onConflict user_id,template_id,month_key ignoreDuplicates"
      pattern: "ignoreDuplicates"
---

<objective>
Receitas vertical slice: the user records where money comes from and immediately sees the "receita líquida do mês" — the Phase 3 metas denominator. End-to-end: a dialog form writes income templates/occurrences/avulsas via Zod-validated Server Actions, the RSC materializes the selected month idempotently and reads the live sum from v_income_month.

Purpose: Closes INC-01/02/03/04. After this slice the user can do something new: open Receitas, add a salary that recurs, override one month's value without touching the template, log a one-off receipt, and watch the líquida figure update.

Output: src/actions/incomes.ts (full income CRUD + materialize-on-read), the Receitas page with the líquida hero, and the reusable money-input / amount-cell / receita-form components. Turns the Wave-0 income tests (income-month, income-occurrence, income-adhoc) GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-receitas-categorias-e-lan-amentos-manuais/02-RESEARCH.md
@.planning/phases/02-receitas-categorias-e-lan-amentos-manuais/02-UI-SPEC.md

# Contracts created by 02-01 (consume, do not redefine):
@src/lib/schemas/income.ts
@src/lib/month.ts
@src/types/database.types.ts
# Patterns to mirror:
@src/actions/auth.ts
@src/components/auth-form.tsx
@src/lib/money.ts
# Wave-0 tests this slice must turn green:
@tests/income-month.test.ts
@tests/income-occurrence.test.ts
@tests/income-adhoc.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Income Server Actions (templates, occurrences, avulsa, materialize-on-read)</name>
  <files>src/actions/incomes.ts</files>
  <read_first>RESEARCH.md Pattern 1 (materialize-on-read sketch + ensureMonthOccurrences code), Open Question 1 (derive avulsa month_key from occurred_on); src/actions/auth.ts (boundary pattern: safeParse -> { error }, getClaims() for user_id, revalidatePath); src/lib/schemas/income.ts; tests/income-occurrence.test.ts + tests/income-adhoc.test.ts for the exact expected behavior.</read_first>
  <behavior>
    - ensureMonthOccurrences(monthKey): upserts one occurrence per active template with onConflict 'user_id,template_id,month_key' + ignoreDuplicates:true; re-running is a no-op (no dup, edited values preserved) — INC-01/INC-02 idempotency
    - createIncomeTemplate(formData): inserts a template AND its occurrence for the current month
    - updateOccurrence(id, formData): edits ONLY that occurrence row (INC-02) — template untouched
    - updateTemplate(id, formData): edits the template (affects future months' materialization), not past occurrences
    - createAdhocIncome(formData): inserts an occurrence with template_id NULL, month_key derived server-side from occurred_on (INC-03)
    - deleteOccurrence(id): removes a single occurrence
  </behavior>
  <action>
Create src/actions/incomes.ts ('use server') mirroring auth.ts exactly: each action safeParses FormData with the schemas from lib/schemas/income.ts, returns { error } on failure (first issue message), resolves the user via supabase.auth.getClaims() (claims.sub), parses money via parseBRLToCents (catch -> "Valor monetário inválido."), and revalidatePath('/receitas') on success. Use the shared ActionResult type ({ error: string } | { ok: true }).

ensureMonthOccurrences(monthKey): read active templates (RLS-scoped), map each to an occurrence row (user_id, template_id, source snapshot, amount_cents, month_key, occurred_on computed from day_of_month clamped to the month's last day), and upsert with { onConflict: 'user_id,template_id,month_key', ignoreDuplicates: true } so re-opening a month never overwrites an INC-02 edit. createIncomeTemplate inserts the template then materializes the current month. updateOccurrence touches only income_occurrences (INC-02). createAdhocIncome inserts template_id NULL with month_key derived from occurred_on via lib/month (do NOT let the user set month_key inconsistently). Never store negative money; amount_cents >= 0 enforced by the schema + DB check.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npx vitest run income-occurrence income-adhoc && npx tsc --noEmit</automated>
  </verify>
  <done>income-occurrence.test.ts and income-adhoc.test.ts GREEN: occurrence edits do not touch the template/other months, re-materialize is idempotent, multiple avulsas per month allowed; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Receitas page + reusable money-input / amount-cell / receita-form</name>
  <files>src/app/(app)/receitas/page.tsx, src/components/receita-form.tsx, src/components/money-input.tsx, src/components/amount-cell.tsx</files>
  <read_first>UI-SPEC section 1 (Receitas) — hero "Receita líquida do mês" 28px mono/600 text-income, table columns Fonte/Tipo/Valor/ações, recurring-edit dialog copy "Alterar só em {mês}" vs "Alterar o template", empty/loading/error states + Copywriting Contract; src/components/auth-form.tsx (RHF + zodResolver + field primitive + useTransition + toast); src/lib/money.ts (formatCents); tests/income-month.test.ts.</read_first>
  <action>
Build money-input.tsx (client): a text input with an R$ affix that stores the raw string and is parsed via parseBRLToCents at submit; on throw surface a FieldError "Valor monetário inválido." (display existing values via formatCents). Build amount-cell.tsx: mono tabular-nums, right-aligned, leading + and text-income for income. Build receita-form.tsx: a dialog form (mirror auth-form.tsx RHF pattern, zodResolver over the income schemas) with Fonte (text/Select suggestions Salário/Pensão/Outros), Valor (MoneyInput), a "recorrente" switch — if on, show day-of-month; if off (avulsa), show a date popover+calendar (pt-BR). The recurring-occurrence edit path opens a dialog offering the explicit INC-02 choice "Alterar só em {mês}" (updateOccurrence) vs "Alterar o template (próximos meses)" (updateTemplate) — never silent.

Build src/app/(app)/receitas/page.tsx (RSC): read ?mes (default currentMonthKey()), call ensureMonthOccurrences(mes) to materialize, then query income_occurrences for the month + v_income_month for the líquida total. Render the hero (label text-xs muted over the 28px mono/600 text-income figure via formatCents), the occurrences table (Fonte · Tipo badge Recorrente/Avulsa · Valor AmountCell · ações), the "Nova receita" CTA opening receita-form, and the empty/loading/error states with the exact pt-BR copy from UI-SPEC.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npx vitest run income-month && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>income-month.test.ts GREEN (líquida = SUM via v_income_month); /receitas renders the hero + occurrences table + Nova receita dialog with the INC-02 edit-choice; money-input/amount-cell reusable; empty/loading/error copy matches UI-SPEC; tsc + build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form → income Server Action | Untrusted Fonte/Valor/date cross here |
| RSC → v_income_month | Sum must run under the caller's RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-INC-VAL | Tampering | createIncomeTemplate / createAdhocIncome | mitigate | Zod schema (lib/schemas/income.ts) at the action boundary; parseBRLToCents throws on invalid -> friendly error; amount_cents >= 0 DB check |
| T-02-INC-RLS | Information Disclosure | income_templates / income_occurrences reads | mitigate | RLS from 02-01 (USING + WITH CHECK); actions never filter by user_id in app code alone; getClaims() resolves the owner for inserts |
| T-02-INC-DUP | Integrity | materialize-on-read | mitigate | unique(user_id,template_id,month_key) + upsert ignoreDuplicates makes re-open a no-op (no dup, no clobber of an INC-02 edit) |
</threat_model>

<verification>
- npx vitest run income-occurrence income-adhoc income-month all GREEN.
- npx tsc --noEmit and npm run build clean.
- Manual (deferred to 02-05 checkpoint): INC-02 "só neste mês vs template" dialog choice.
</verification>

<success_criteria>
- INC-01: recurring template materializes one editable occurrence for the month.
- INC-02: editing one month's occurrence leaves the template and other months untouched; re-open is idempotent.
- INC-03: ad-hoc income with no template; multiple avulsas per month allowed.
- INC-04: Receitas hero shows the receita líquida do mês from v_income_month.
</success_criteria>

<output>
Create `.planning/phases/02-receitas-categorias-e-lan-amentos-manuais/02-02-SUMMARY.md` when done
</output>
