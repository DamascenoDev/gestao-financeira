---
phase: 12-produ-o-live-verify
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0029_consumo_same_odometer_fix.sql
  - tests/carro-consumo.test.ts
  - .planning/milestones/v1.2-phases/09-etiquetar-gastos-da-fatura-ao-carro/09-01-SUMMARY.md
  - .planning/milestones/v1.2-phases/09-etiquetar-gastos-da-fatura-ao-carro/09-02-SUMMARY.md
  - .planning/milestones/v1.2-phases/09-etiquetar-gastos-da-fatura-ao-carro/09-03-SUMMARY.md
  - .planning/milestones/v1.2-phases/10-abastecimento-h-brido-consumo/10-01-SUMMARY.md
  - .planning/milestones/v1.2-phases/10-abastecimento-h-brido-consumo/10-02-SUMMARY.md
  - .planning/milestones/v1.2-phases/10-abastecimento-h-brido-consumo/10-03-SUMMARY.md
autonomous: true
requirements: [DEBT-01, DEBT-02]

must_haves:
  truths:
    - "Migration 0029 is applied to the LOCAL stack and v_abastecimento_consumo no longer understates km/l (and no longer overstates R$/km) when two tanque_cheio fills share the EXACT same odometro_km (WR-02 / DEBT-01)"
    - "The same-odometer edge has a RED-then-GREEN integration test in tests/carro-consumo.test.ts proving the prior interval's litros_intervalo/custo_intervalo_cents no longer sweep in the sibling fill's liters/cost"
    - "All pre-existing carro-consumo proofs (happy path 12.5 km/l, WR-06 non-positive guard, preco_litro-not-stored) stay GREEN after 0029"
    - "The six v1.2 SUMMARY frontmatters (09-01/02/03, 10-01/02/03) carry requirements_completed for CAR-02 (09-x) and CAR-03/CAR-04 (10-x) (DEBT-02)"
  artifacts:
    - path: "supabase/migrations/0029_consumo_same_odometer_fix.sql"
      provides: "create-or-replace of v_abastecimento_consumo anchoring each interval's liters/cost lower bound on the prior full-tank fill's id (not odometro_km), security_invoker preserved"
      contains: "security_invoker = true"
    - path: "tests/carro-consumo.test.ts"
      provides: "same-odometer WR-02 regression proof appended to the existing consumption-view suite"
      contains: "same odometer"
  key_links:
    - from: "supabase/migrations/0029_consumo_same_odometer_fix.sql"
      to: "public.v_abastecimento_consumo"
      via: "create or replace view ... with (security_invoker = true)"
      pattern: "create or replace view public.v_abastecimento_consumo"
    - from: "tests/carro-consumo.test.ts"
      to: "v_abastecimento_consumo"
      via: "RLS-active user client read of the same-odometer carro"
      pattern: "from\\('v_abastecimento_consumo'\\)"
---

<objective>
Close the last accepted tech debt of v1.2 BEFORE any data enters production: write and apply migration 0029 (WR-02 / DEBT-01) on the LOCAL stack, proving with a RED→GREEN test that v_abastecimento_consumo stops understating km/l and overstating R$/km when two tanque_cheio fills share the EXACT same odometro_km. Then perform the DEBT-02 doc-hygiene chore — add `requirements_completed` (CAR-02 / CAR-03 / CAR-04) to the six v1.2 SUMMARY frontmatters that were left empty at milestone close. This is the ONLY real code work in Phase 12 and is fully autonomous; it must be GREEN locally so production is born (via 12-02's db push of 0001-0029) with the corrected view.

Purpose: per D-07 step (a)/(b), the view fix and doc hygiene happen first, on the local stack, so the remote `db push` in Plan 12-02 carries the corrected SQL and the audit trail is complete before live verification.
Output: `supabase/migrations/0029_consumo_same_odometer_fix.sql` applied locally + a passing same-odometer regression test + six corrected SUMMARY frontmatters.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-produ-o-live-verify/12-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 0029 — fix the same-odometer sweep-in in v_abastecimento_consumo (WR-02 / DEBT-01)</name>
  <files>supabase/migrations/0029_consumo_same_odometer_fix.sql, tests/carro-consumo.test.ts</files>
  <read_first>
    - supabase/migrations/0028_carros_fix.sql (the CURRENT view definition — full_fills / fills / intervals CTEs; the two correlated subqueries that compute litros_intervalo and custo_intervalo_cents bound by `s.odometro_km > f.prev_full_odometro AND s.odometro_km <= f.odometro_km`; security_invoker = true; the km_rodados<=0 guard)
    - supabase/migrations/0027_carros.sql (original view + v_carro_resumo which reads OFF v_abastecimento_consumo with avg(...) filter — leave v_carro_resumo untouched per the 0028 precedent)
    - tests/carro-consumo.test.ts (the existing harness: createUser/userClient/serviceClient clone; the happy-path carro = 12.5 km/l, the WR-06 guard carro, the preco_litro-not-stored proof — the new test appends to this file's existing beforeAll/describe shape)
    - .planning/milestones/v1.2-MILESTONE-AUDIT.md §"Tech Debt" item 1 (the authoritative WR-02 statement: "the later fill's liters/cost are swept into the prior interval's litros_intervalo/custo_intervalo_cents subquery … Fix = migration 0029 anchoring each interval subquery lower bound on the prior full-tank fill's id, not odometro_km")
  </read_first>
  <behavior>
    The WR-02 edge (RED before 0029, GREEN after): a carro has THREE tanque_cheio fills where fill #2 and fill #3 share the EXACT same odometro_km (e.g. 30000 → 30500 → 30500). The bug: the closing interval at fill #2 (30000→30500) uses `s.odometro_km <= f.odometro_km` (= 30500) in its litros/custo subqueries, so fill #3's liters AND cost (which also sit at odometro_km 30500) are SWEPT INTO that interval. Result: litros_intervalo is inflated → km_por_litro is understated; custo_intervalo_cents is inflated → reais_por_km is overstated. The km_rodados>0 guard already drops the #2→#3 zero-length interval but does NOT stop the sibling sweep-in into #1→#2.
    - Test A (the WR-02 regression — this is the one that fails on 0028, passes on 0029): seed a carro with fills at odometro 30000 (litros L0), 30500 (litros L2, cost C2), 30500 (litros L3, cost C3), all tanque_cheio. Assert the ONE surfaced interval (30000→30500) has litros_intervalo = L2 ONLY (not L2+L3) and custo_intervalo_cents = C2 ONLY (not C2+C3), so km_por_litro = 500/L2 and reais_por_km = C2/500 — NOT dragged down/up by the sibling at the same odometer.
    - Test B (no regression): the existing happy-path carro still reads exactly one interval at 12.5 km/l and R$/km ≈ 48; the existing WR-06 guard carro still surfaces no negative/zero-delta rows; preco_litro is still not a stored column. (These are the tests already in the file — they must stay GREEN.)
  </behavior>
  <action>
Write `supabase/migrations/0029_consumo_same_odometer_fix.sql` as a FIX-ONLY `create or replace view public.v_abastecimento_consumo with (security_invoker = true)` — touch NOTHING structural (no table/column/RLS/grant/index changes), exactly the shape of 0028. Re-issue v_carro_resumo NOT at all (its avg(...) filter already excludes the now-corrected rows; the interval logic must live in ONE place per the 0028/0027 precedent and the retrospective Key Lesson 2).

The fix (DEBT-01 / WR-02): anchor each interval's litros_intervalo and custo_intervalo_cents subquery LOWER bound on the prior full-tank fill's IDENTITY, not its odometro_km, so a sibling tanque_cheio fill sharing the same odometro_km is NOT swept in. Use the deterministic full-tank ordering already established in 0028 — `order by odometro_km, occurred_on, created_at, id` — to define "the prior full-tank fill" unambiguously, then make the interval's membership predicate select the abastecimentos strictly AFTER that prior fill in that SAME total order and up to/including the closing fill. Concretely: carry the prior full-tank fill's ordering key (or its id plus the ordering tuple) into the intervals CTE, and replace the bare `s.odometro_km > f.prev_full_odometro` / `s.odometro_km <= f.odometro_km` membership test with one that compares on the full ordering tuple (odometro_km, occurred_on, created_at, id) so two rows at the same odometro land deterministically on exactly one side of the boundary — the closing fill's own liters/cost count toward its interval, the next fill's do NOT. Keep the WR-06 guards intact: the intervals WHERE still requires `prev_full_odometro is not null` and `km_rodados > 0`, and the km_por_litro / reais_por_km CASE branches still guard `km_rodados <= 0` and zero/null litros → null. Preserve the leading INVARIANTS comment block style from 0028 (centavos bigint; litros is volume; security_invoker mandatory) and add a WR-02 note describing the id-anchored lower bound.

Then append the WR-02 regression to `tests/carro-consumo.test.ts`: in the existing beforeAll seed a third carro (e.g. `carroSameOdoId`) with the three-fill same-odometer shape from <behavior> Test A, and add a `describe('v_abastecimento_consumo same-odometer sweep-in guard (WR-02, DEBT-01)')` block asserting litros_intervalo and custo_intervalo_cents for the single surfaced interval exclude the sibling fill (so km_por_litro and reais_por_km are NOT dragged). Reuse the file's userClient(userA.jwt, config) RLS-active read pattern; clean up the new carro in afterAll if the harness deletes per-carro (it deletes the user, so no extra teardown is needed).

Apply locally and prove RED→GREEN: with the local stack up (`npx supabase status` resolves the API on 127.0.0.1:55321), FIRST run the NEW test against the pre-0029 view to confirm it FAILS (RED — the sweep-in), then `supabase db reset` (or `supabase migration up`) to apply 0001-0029 locally, then run the full carro-consumo suite to confirm GREEN. Do NOT write any fenced SQL/TS here — author it directly in the two files.
  </action>
  <verify>
    <automated>cd /home/dmsc/Dev/Projetos/Pessoal/gestao-financeira && grep -q "create or replace view public.v_abastecimento_consumo" supabase/migrations/0029_consumo_same_odometer_fix.sql && grep -q "security_invoker = true" supabase/migrations/0029_consumo_same_odometer_fix.sql && SUPABASE_DISABLE_TELEMETRY=1 npx vitest run tests/carro-consumo.test.ts 2>&1 | tail -8 && npx tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <done>0029 exists as a security_invoker create-or-replace of v_abastecimento_consumo; the new same-odometer test was RED on 0028 and is GREEN on 0029; the four pre-existing carro-consumo proofs stay GREEN; tsc clean; migrations 0001-0029 apply cleanly to the local stack.</done>
</task>

<task type="auto">
  <name>Task 2: DEBT-02 — backfill requirements_completed into the six v1.2 SUMMARY frontmatters</name>
  <files>.planning/milestones/v1.2-phases/09-etiquetar-gastos-da-fatura-ao-carro/09-01-SUMMARY.md, .planning/milestones/v1.2-phases/09-etiquetar-gastos-da-fatura-ao-carro/09-02-SUMMARY.md, .planning/milestones/v1.2-phases/09-etiquetar-gastos-da-fatura-ao-carro/09-03-SUMMARY.md, .planning/milestones/v1.2-phases/10-abastecimento-h-brido-consumo/10-01-SUMMARY.md, .planning/milestones/v1.2-phases/10-abastecimento-h-brido-consumo/10-02-SUMMARY.md, .planning/milestones/v1.2-phases/10-abastecimento-h-brido-consumo/10-03-SUMMARY.md</files>
  <read_first>
    - .planning/milestones/v1.2-MILESTONE-AUDIT.md §"Tech Debt" item 2 + the cross-phase-hygiene deferred list (the authoritative mapping: CAR-02 ← phase 9 etiquetagem; CAR-03/CAR-04 ← phase 10 abastecimento/consumo; carried only by REQUIREMENTS.md checkbox + phase VERIFICATION, not by per-plan SUMMARY frontmatter)
    - The six SUMMARY files themselves (note the two frontmatter shapes: 09-01/10-01/10-02 use a `phase:`/`plan:`/`subsystem:` shape with NO `requirements:` key; 09-02/09-03/10-03 already carry a `requirements:` list — add the new key consistently with each file's existing style)
  </read_first>
  <action>
Add a `requirements_completed` frontmatter key to each of the six v1.2 SUMMARY files (DEBT-02 / D-09 — a trivial chore folded into this plan, not its own plan). Per the milestone audit mapping: the three Phase-9 SUMMARYs (09-01, 09-02, 09-03 — carro-tagging) get `requirements_completed: [CAR-02]`; the three Phase-10 SUMMARYs (10-01 = consumption-view migration, 10-02 = abastecimento server layer, 10-03 = abastecimento UI) get `requirements_completed: [CAR-03, CAR-04]`. Where a file already has a `requirements:` list (09-02, 09-03, 10-03) keep that line and add `requirements_completed` alongside it (the SUMMARY-completed key the milestone CLI/audit reads); where a file has the leaner `phase:`/`plan:` shape (09-01, 10-01, 10-02) insert `requirements_completed` near the top of the frontmatter. Do NOT change any body content, metrics, or other keys — this is frontmatter-only audit-trail hygiene. Edit each file in place with the Edit tool (never rewrite the whole file).
  </action>
  <verify>
    <automated>cd /home/dmsc/Dev/Projetos/Pessoal/gestao-financeira && c=$(grep -l "requirements_completed" .planning/milestones/v1.2-phases/09-etiquetar-gastos-da-fatura-ao-carro/09-0[123]-SUMMARY.md .planning/milestones/v1.2-phases/10-abastecimento-h-brido-consumo/10-0[123]-SUMMARY.md 2>/dev/null | wc -l) && test "$c" -eq 6 && grep -h "requirements_completed" .planning/milestones/v1.2-phases/09-etiquetar-gastos-da-fatura-ao-carro/09-0[123]-SUMMARY.md .planning/milestones/v1.2-phases/10-abastecimento-h-brido-consumo/10-0[123]-SUMMARY.md</automated>
  </verify>
  <done>All six v1.2 SUMMARY files carry requirements_completed; the three Phase-9 files list CAR-02, the three Phase-10 files list CAR-03 + CAR-04; no body or other-key edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local migration → remote project (next plan) | 0029 must be correct + non-destructive BEFORE 12-02 pushes 0001-0029 to production; a wrong view here would ship to prod |
| view definer vs caller RLS | a DEFINER view bypasses the caller's RLS and leaks every user's consumption rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-viewleak (E) | Elevation of Privilege / Info Disclosure | `v_abastecimento_consumo` losing `security_invoker` in the 0029 re-issue | mitigate | 0029 keeps `with (security_invoker = true)` (asserted by the grep gate in Task 1 + the existing carro-view-leak.test.ts staying GREEN) |
| T-12-badmath (T) | Tampering (data integrity) | wrong same-odometer fix silently corrupting km/l-R$/km averages shipped to prod | mitigate | RED→GREEN regression test pins the exact expected litros_intervalo/custo (sibling excluded) before the fix is accepted; the four prior proofs stay GREEN |
| T-12-clobber (T) | Tampering | 0029 accidentally altering tables/RLS/grants instead of being view-only | mitigate | fix-only create-or-replace (0028 precedent); no table/column/RLS/grant/index statements; `db diff` in 12-02 reviews the delta before remote push |
</threat_model>

<verification>
- 0029 is a security_invoker create-or-replace of v_abastecimento_consumo only (no structural DDL).
- The same-odometer WR-02 test was RED against 0028 and is GREEN against 0029; the happy-path, WR-06 guard, and preco_litro-not-stored proofs remain GREEN.
- Migrations 0001-0029 apply cleanly to the local stack; tsc clean.
- Six v1.2 SUMMARY frontmatters carry requirements_completed (CAR-02 / CAR-03 / CAR-04).
</verification>

<success_criteria>
- DEBT-01 closed locally: v_abastecimento_consumo no longer understates km/l / overstates R$/km on the same-odometer edge, proven by a regression test; production will be born with the fix via 12-02's db push.
- DEBT-02 closed: the six v1.2 SUMMARYs carry the correct requirements_completed audit trail.
- Nothing remote touched yet (that is 12-02); local stack fully green.
</success_criteria>

<output>
Create `.planning/phases/12-produ-o-live-verify/12-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
