---
phase: 12-produ-o-live-verify
plan: 03
type: execute
wave: 3
depends_on: ["12-02"]
files_modified: []
autonomous: false
requirements: [INC-02, TXN-03, TXN-04]
user_setup:
  - service: app (production)
    why: "Live-verify of the receitas/categorias/extrato surface against the PRODUCTION *.vercel.app URL deployed once by Plan 12-02 (D-08 — no re-deploy). The walkthrough is browser-interactive against the user's personal account."
    dashboard_config:
      - task: "Open the *.vercel.app production URL recorded in 12-02-SUMMARY.md and sign in with the personal account created in 12-02"
        location: "browser (production)"

must_haves:
  truths:
    - "The production *.vercel.app URL from 12-02 (the SAME bundle, no re-deploy per D-08) serves the receitas/categorias/extrato screens to the signed-in personal account, talking to the remote Supabase (sa-east-1) with RLS active"
    - "INC-02 confirmed in production: a recurring income edited with 'Alterar só em {mês}' changes only that month; the template/other months are unchanged; the 'receita líquida do mês' hero sums recurring + avulsa correctly — this net income is the denominator DEPLOY-05's goal adherence will use"
    - "TXN-03 confirmed in production: the Extrato month + multi-category filter round-trips through the URL (?mes=...&cat=...) and survives a page reload against the remote DB"
    - "TXN-04 confirmed in production: multi-row bulk re-classify via the SelectionActionBar moves the selected rows to a target category and persists to the remote DB"
  artifacts: []
  key_links: []
---

<objective>
Live-verify the Phase-2 manual-only behaviors (INC-02 income edit-choice, TXN-03 filter URL round-trip, TXN-04 bulk re-classify) in PRODUCTION — by SEQUENCING AND EXECUTING the already-written walkthrough `.planning/phases/02-receitas-categorias-e-lan-amentos-manuais/02-05-PLAN.md` against the single *.vercel.app bundle Plan 12-02 deployed (D-07 step 4, first verify after auth). This plan does NOT re-author the walkthrough mechanics; it runs 02-05's checkpoint against the production URL + the remote Supabase, instead of the local stack the original plan targeted. No re-deploy occurs (D-08). The receitas/net-income surface verified here is the foundation DEPLOY-05's goal-adherence half (verified in Plan 12-04) and core-value upload (Plan 12-05) build on. Every step is browser-interactive against production → autonomous: false.

Purpose: prove INC-02 / TXN-03 / TXN-04 work live in production (not just on the local stack where 02-05 was originally written to run), the first walkthrough in the D-07 sequence after the 12-02 auth foundation.
Output: a confirmed production sign-off (or a punch list) for the Receitas, Categorias, and Extrato screens; verification evidence captured in 12-03-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-produ-o-live-verify/12-CONTEXT.md
@.planning/phases/02-receitas-categorias-e-lan-amentos-manuais/02-05-PLAN.md
@.planning/phases/12-produ-o-live-verify/12-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live-verify receitas/categorias/extrato in production (executes 02-05 against the 12-02 bundle)</name>
  <files>none (verification only — produces sign-off, no symbols)</files>
  <read_first>
    - .planning/phases/02-receitas-categorias-e-lan-amentos-manuais/02-05-PLAN.md (the authoritative walkthrough — its Task 2 checkpoint holds the exact INC-02 / TXN-03 / TXN-04 steps; this plan RUNS those steps, do not re-author them; the ONLY delta is the target is the PRODUCTION *.vercel.app URL + remote Supabase, not the local dev server)
    - .planning/phases/12-produ-o-live-verify/12-02-SUMMARY.md (the recorded production *.vercel.app URL — the single bundle to verify against; NO re-deploy per D-08)
    - .planning/phases/02-receitas-categorias-e-lan-amentos-manuais/02-UI-SPEC.md (the design contract sanity-check: teal-only accent, income green with +, gasto neutral never red, money mono/right-aligned, dense Extrato rows)
  </read_first>
  <what-built>The Phase-2 surface (receitas líquida hero + recurring/avulsa income with the INC-02 edit-choice; categorias list with consumo/alocação toggle + blocked-delete Arquivar/Reatribuir flow; the dense filterable URL-persisted Extrato with per-category totals + SelectionActionBar bulk re-classify), now served by the production *.vercel.app bundle from 12-02 against the remote Supabase. All underlying logic was automated GREEN in Plans 02-02/03/04; this is the production interaction confirmation.</what-built>
  <how-to-verify>
Open the production *.vercel.app URL recorded in 12-02-SUMMARY.md (the SAME deploy — do NOT run `vercel --prod` again, D-08) and sign in with the personal account created in 12-02. Then walk 02-05's three checks AGAINST PRODUCTION:

1. INC-02 edit-choice (Receitas, /receitas):
   - Create a recurring income (Fonte "Salário", a value, recorrente on, a day). Confirm it appears and the "receita líquida do mês" hero reflects it.
   - Edit that occurrence's value and pick "Alterar só em {mês}". Switch the global MonthSelector to the next month and back: the edited month keeps the new value; the template/other month is unchanged.
   - Add an avulsa (recorrente off, a date); confirm a second avulsa in the same month is allowed and the hero sums both. (This hero net-income value is the denominator Plan 12-04 will check goal % against.)

2. TXN-03 filter URL round-trip (Extrato, /extrato):
   - Change the month via the MonthSelector and select 1-2 categories in the category filter. Confirm the list + per-category + grand totals update and the browser URL contains ?mes=...&cat=.... Reload the page (against the remote DB): the same filter survives.

3. TXN-04 bulk re-classify (Extrato, /extrato):
   - Add a few transactions in the current month across categories. Select several rows (header select-all and a shift-click range). Confirm the SelectionActionBar shows "{n} selecionadas". Pick a target category, click Reclassificar. Confirm all selected rows move and a "{n} transações reclassificadas" toast appears, and the change persists on reload (written to the remote DB).

Sanity-check the design contract live: teal is the only accent (CTAs/active nav), income is green with a +, gasto is neutral (not red), money is mono/right-aligned, Extrato rows are dense.
  </how-to-verify>
  <resume-signal>Type "approved" once INC-02 edit-choice, TXN-03 URL filter persistence, and TXN-04 bulk re-classify all behave correctly on the PRODUCTION URL, or describe any issue (per screen) — issues become a `/gsd-plan-phase 12 --gaps` punch list.</resume-signal>
  <action>HUMAN CHECKPOINT (autonomous:false) — execute the 02-05 walkthrough against the production *.vercel.app URL from 12-02 (NOT the local stack the original 02-05 targeted, and with NO re-deploy per D-08). The exact steps live in <how-to-verify>; the source-of-truth steps live in 02-05-PLAN.md Task 2. Pause for the user to sign in to production and walk INC-02 / TXN-03 / TXN-04. Browser interaction + remote-DB persistence cannot be automated.</action>
  <verify><human-check>On the production *.vercel.app URL: a recurring income edited "só neste mês" changes only that month and the hero sums recurring+avulsa; the Extrato month+category filter round-trips through ?mes=&cat= and survives reload; bulk re-classify moves the selected rows and persists on reload. Design contract (accent/income/expense/density) holds.</human-check></verify>
  <acceptance_criteria>
    - Logged-in personal user on the production *.vercel.app URL (12-02 bundle) loads /receitas and the "receita líquida do mês" hero renders a non-zero value after a recurring income is added.
    - Editing a recurring income with "Alterar só em {mês}" leaves the template/adjacent months unchanged (observed by switching the MonthSelector forward and back).
    - Navigating /extrato with a month + 1-2 categories selected produces a URL containing both ?mes= and &cat=, and a full page reload restores the same filtered view + totals from the remote DB.
    - Selecting ≥2 rows and applying Reclassificar moves all selected rows to the chosen category, shows the "{n} transações reclassificadas" toast, and the reassignment survives a reload (persisted remotely).
  </acceptance_criteria>
  <done>INC-02 / TXN-03 / TXN-04 confirmed live in production against the 12-02 bundle; design contract holds; no re-deploy occurred (D-08). Sign-off (or punch list) recorded.</done>
</task>

</tasks>

<artifacts_produced>
This live-verify plan produces NO new source symbols. It produces VERIFICATION EVIDENCE only:
- `.planning/phases/12-produ-o-live-verify/12-03-SUMMARY.md` — the production sign-off (or punch list) for INC-02 / TXN-03 / TXN-04.
- Seed data the user creates during the walkthrough (a recurring income, an avulsa, a few transactions) lives in the user's own production account, scoped by RLS. No test fixtures, no migrations, no code.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → production app (authenticated) | the signed-in personal session drives writes to the remote Supabase; RLS must scope every write to the user's user_id |
| production app → remote Supabase | the same RLS that held locally in Phase 2 must hold on the remote project (already asserted at the table level by 12-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03-rls (I) | Information Disclosure | receitas/transações reads/writes leaking across users on the remote project | mitigate | 12-02 already asserted no table is "Unrestricted" remotely; this walkthrough operates only as the single signed-in user (single-user v1) and confirms reads/writes reflect only that account |
| T-12-03-SC (T) | Tampering | npm/pip/cargo installs | accept | no packages installed — this is a verification-only plan against an already-deployed bundle |
</threat_model>

<verification>
- The walkthrough runs against the production *.vercel.app URL from 12-02 (the SAME bundle, no re-deploy — D-08).
- Human-verified live in production: INC-02 income edit-choice scopes to the chosen month; TXN-03 filter round-trips through the URL and survives reload; TXN-04 bulk re-classify persists to the remote DB.
- Design contract (teal-only accent, income green+, gasto neutral, mono money, dense rows) sanity-checked in production.
</verification>

<success_criteria>
- INC-02 / TXN-03 / TXN-04 confirmed live in production (re-proven against the remote stack, not just the local stack 02-05 originally targeted) — the first walkthrough in the D-07 sequence after the 12-02 auth foundation.
- The receitas/net-income surface that DEPLOY-05's goal adherence (Plan 12-04) and core-value upload (Plan 12-05) depend on is confirmed working in production.
- No re-deploy occurred. Reviewer signs off "approved" (or a punch list is captured for gap closure).
</success_criteria>

<output>
Create `.planning/phases/12-produ-o-live-verify/12-03-SUMMARY.md` when done (record the production sign-off or the issue list).
</output>
