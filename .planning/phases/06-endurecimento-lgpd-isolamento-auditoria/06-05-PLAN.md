---
phase: 06-endurecimento-lgpd-isolamento-auditoria
plan: 05
type: execute
wave: 4
depends_on: ["06-02", "06-03", "06-04"]
files_modified: []
autonomous: false
requirements: [DATA-01, DATA-02, SEC-01]
must_haves:
  truths:
    - "The full Phase 6 suite is green and the secret-bundle gate passes against a real build before any human step"
    - "A human confirms the transactions CSV downloads correctly (pt-BR) from the Extrato and Conta"
    - "A human confirms the LGPD bundle downloads with all sections"
    - "A human confirms the type-to-confirm APAGAR delete signs out — run only against a throwaway LOCAL user"
  artifacts: []
  key_links: []
---

<objective>
Human-verify walkthrough for Phase 6 — the dangerous-delete checkpoint plus the two download confirmations that only a browser can prove. Everything in this phase is automated and tested; what remains is what a test cannot assert: that the file downloads land in the browser correctly, the type-to-confirm friction feels right, and the delete actually signs the user out.

This is the FINAL plan of the FINAL phase. It runs only after 06-02 (CSV slice), 06-03 (LGPD export+delete), and 06-04 (SEC-01 audits) are green. The destructive delete walkthrough is run ONLY against a throwaway LOCAL user (create one, delete it, confirm it's gone) — NEVER against a real account.

Purpose: close DATA-01/DATA-02/SEC-01 with a human sign-off on the browser-only behaviors before declaring the milestone hardened.
Output: a confirmed walkthrough; no files modified.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-endurecimento-lgpd-isolamento-auditoria/06-VALIDATION.md
@.planning/phases/06-endurecimento-lgpd-isolamento-auditoria/06-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated phase gate — full suite + tsc + build + secret audit</name>
  <files></files>
  <action>
Run the Phase 6 gate to prove everything automatable is green before the human steps. With the LOCAL stack running (`supabase start`):
1. `SUPABASE_DISABLE_TELEMETRY=1 npx vitest run` — the full suite (all Phase 1-6 tests, including the 8 Wave-0 tests now GREEN: transactions-csv, lgpd-export, lgpd-delete, lgpd-delete-isolation, isolation-matrix, storage-isolation, bundle-secret-grep, pii-guard).
2. `npx tsc --noEmit` — clean.
3. `npm run build` — compiles (incl. /conta and /extrato).
4. `bash scripts/check-bundle-secrets.sh .next/static` — exits 0 (the service-role secret is absent from the real built client bundle even though delete uses it server-side).
Report the suite count + that all four gates pass. If any fails, STOP — do not proceed to the human walkthrough; surface the failure for a gap-closure plan.
  </action>
  <verify>
    <automated>SUPABASE_DISABLE_TELEMETRY=1 npx vitest run && npx tsc --noEmit && npm run build && bash scripts/check-bundle-secrets.sh .next/static</automated>
  </verify>
  <done>Full suite GREEN, tsc clean, build compiles, secret-bundle gate exits 0. The phase is automation-complete; ready for the human-only browser checks.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    DATA-01/DATA-02 browser surfaces: the transactions CSV export (Extrato + Conta), the LGPD "Baixar meus dados" bundle, and the type-to-confirm "APAGAR" account-delete that signs the user out. All logic is automated + green (Task 1); these are the browser-only confirmations.
  </what-built>
  <how-to-verify>
1. Start the app against the LOCAL stack (`npm run dev`, local Supabase running). Sign in.
2. **Transactions CSV (DATA-01):** On `/extrato`, pick a month with transactions, click "Exportar transações (CSV)". Confirm a `transacoes-{yyyy-MM}.csv` downloads and opens in Excel/LibreOffice pt-BR with: a BOM (no mojibake on acentos), `;` columns, comma decimals (e.g. `R$ 1.234,56`), columns Data (dd/MM/yyyy) · Descrição · Categoria · Tipo (Consumo/Alocação) · Valor, and the category NAME resolved. Repeat the same button on `/conta`.
3. **LGPD bundle (DATA-02 export):** On `/conta`, click "Baixar meus dados". Confirm a `meus-dados-{yyyy-MM-dd}.json` downloads; open it and confirm it has a `tables` object with all your sections (receitas, transações, categorias, metas, reservas, MEI, padrões, statements metadata) and a `csv` section with transactions + MEI strings.
4. **Type-to-confirm delete (DATA-02 delete) — THROWAWAY LOCAL USER ONLY:** Sign up a NEW throwaway local user (e.g. `descartavel@example.test`), add a row or two so there's data, then on `/conta` open the "Apagar conta" dialog. Confirm: the consequences list is visible; the "Apagar conta" button is DISABLED until you type exactly `APAGAR` (try lowercase/partial — stays disabled); initial focus is on Cancelar (a stray Enter cancels); on confirm it shows "Apagando…", deletes, and signs you out to `/auth/login`. Then try to sign in again as that user — it must fail (the account is gone). Confirm your ORIGINAL account still works (the delete was surgical).
  </how-to-verify>
  <resume-signal>Type "approved" if all three downloads/flows behave as described, or describe any issue (wrong CSV encoding, missing bundle section, delete not gating on APAGAR, delete not signing out, or any collateral to another account).</resume-signal>
</task>

</tasks>

<verification>
- Task 1 automated gate green (suite + tsc + build + secret audit) BEFORE the human step.
- Human confirms: pt-BR transactions CSV downloads (Extrato + Conta); LGPD bundle downloads with all sections; type-to-confirm APAGAR delete signs out and is surgical (throwaway LOCAL user only).
</verification>

<success_criteria>
- All Phase 6 automation is green (full suite, tsc, build, secret-bundle gate).
- DATA-01: transactions CSV export confirmed in the browser (pt-BR correct).
- DATA-02: LGPD bundle download + type-to-confirm delete + sign-out confirmed (throwaway LOCAL user).
- SEC-01: the isolation/Storage/secret/PII proofs are green (06-04) — no browser step needed, asserted by Task 1.
- Phase 6 — and the milestone hardening — is human-verified.
</success_criteria>

<output>
Create `.planning/phases/06-endurecimento-lgpd-isolamento-auditoria/06-05-SUMMARY.md` when done.
</output>
