---
phase: 06-endurecimento-lgpd-isolamento-auditoria
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/components/export-transactions-button.tsx
  - src/app/(app)/conta/page.tsx
  - src/app/(app)/extrato/page.tsx
  - src/components/app-sidebar.tsx
  - src/components/user-menu.tsx
autonomous: true
requirements: [DATA-01]
must_haves:
  truths:
    - "User can export the current month's transactions to a pt-BR CSV from the Extrato header"
    - "User can reach a Privacidade e conta screen from both the sidebar and the user menu"
    - "The exported CSV resolves each transaction's category name (point-in-time) and Consumo/Alocação type"
  artifacts:
    - path: "src/components/export-transactions-button.tsx"
      provides: "Client button: serializes RLS-scoped transactions via transactionsToCsv → Blob download transacoes-{yyyy-MM}.csv"
      contains: "transactionsToCsv"
    - path: "src/app/(app)/conta/page.tsx"
      provides: "Privacidade e conta screen shell (h1 + subtitle + DataExportSection placeholder with the transactions CSV affordance)"
    - path: "src/components/app-sidebar.tsx"
      provides: "Conta nav item (last, after MEI)"
      contains: "/conta"
  key_links:
    - from: "src/components/export-transactions-button.tsx"
      to: "src/lib/transactions/csv.ts"
      via: "transactionsToCsv import"
      pattern: "transactionsToCsv"
    - from: "src/app/(app)/extrato/page.tsx"
      to: "src/components/export-transactions-button.tsx"
      via: "header affordance with the month's RLS-scoped rows"
      pattern: "ExportTransactionsButton"
---

<objective>
DATA-01 vertical slice: the user can export their transactions to a pt-BR CSV, and the new Privacidade e conta screen exists (shell + nav). After this plan, a real user can do something new — click "Exportar transações (CSV)" on the Extrato (and on Conta) and get a correctly-formatted `transacoes-{yyyy-MM}.csv` of the current month's ledger.

This is a UI/wiring slice over the `transactionsToCsv` serializer shipped in 06-01. It reuses the Phase-5 `ExportCsvButton` shape exactly (outline button, `Download` glyph, Blob download, sonner toast). It also creates the Conta route shell + nav entry that 06-03 will fill with the LGPD export+delete sections — created here so 06-03 only extends one file.

Purpose: deliver the transactions-CSV half of DATA-01 (the MEI CSV half already shipped in Phase 5 — reference, do not rebuild) and stand up the Conta surface.
Output: `ExportTransactionsButton` component, `(app)/conta/page.tsx` shell, Extrato header wiring, Conta nav item + UserMenu "Privacidade e conta" link.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-endurecimento-lgpd-isolamento-auditoria/06-UI-SPEC.md

# The substrate this slice consumes (from 06-01):
@src/lib/transactions/csv.ts

# Reuse these shapes EXACTLY:
@src/components/export-csv-button.tsx
@src/app/(app)/extrato/page.tsx
@src/components/app-sidebar.tsx
@src/components/user-menu.tsx
@src/lib/money.ts
@src/lib/month.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: ExportTransactionsButton + Extrato header wiring (DATA-01)</name>
  <files>src/components/export-transactions-button.tsx, src/app/(app)/extrato/page.tsx</files>
  <read_first>
    - src/components/export-csv-button.tsx (the EXACT Blob-download shape to mirror: outline Button, Download glyph, createObjectURL/a.click()/revokeObjectURL, sonner toast)
    - src/lib/transactions/csv.ts (transactionsToCsv signature + TransactionCsvRow shape from 06-01)
    - src/app/(app)/extrato/page.tsx (where the month's transactions + categories are already read RLS-scoped; the header `<div>` holding h1 + TransacaoForm)
    - 06-UI-SPEC.md §"Component Inventory" ExportTransactionsButton row + §"Copywriting Contract" (CTA "Exportar transações (CSV)", success "Transações exportadas.", error "Não foi possível exportar as transações. Tente novamente.")
    - src/lib/month.ts (the ?mes helper for the filename window)
  </read_first>
  <action>
Create `src/components/export-transactions-button.tsx` mirroring `export-csv-button.tsx`: a `'use client'` outline `Button` with the `Download` glyph, copy "Exportar transações (CSV)". Props: the already-RLS-scoped `rows: TransactionCsvRow[]` (resolved category_name + category_kind, occurred_on, description, amount_cents) the caller's page holds, plus the `mes` (yyyy-MM) for the filename. On click: `transactionsToCsv(rows)` → `Blob([csv], { type: 'text/csv;charset=utf-8;' })` → download named `transacoes-{mes}.csv` → `toast.success('Transações exportadas.')`. On an empty period, the button still exports a header-only CSV (valid file) — do not crash, do not hide. Accessible name "Exportar transações em CSV". Do NOT read data in this component; do NOT compute money here (the serializer owns formatting). The secret never touches this path — it's pure client serialization of rows the RLS-scoped RSC already returned.

Wire it into `src/app/(app)/extrato/page.tsx`: the page already reads the month's `transactions` and `categories` RLS-scoped. Build the `TransactionCsvRow[]` by joining each transaction to its category (resolve `category_name` point-in-time via the existing `categoryById` map, and `category_kind` from the category's `kind`; a null category → name 'Sem categoria', kind null). Render `<ExportTransactionsButton rows={...} mes={mes} />` in the header row beside the existing "Novo lançamento" CTA. Keep the existing month/category-filter behavior — the CSV reflects the current `?mes` window the Extrato shows (UI-SPEC: "the CSV matches what the Extrato shows"). The categories query currently selects `id, name, color, is_reserva`; add `kind` to that select so Tipo can be resolved.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E 'export-transactions-button|extrato/page' || echo "extrato CSV wiring typechecks"</automated>
  </verify>
  <done>ExportTransactionsButton serializes the month's RLS-scoped rows to `transacoes-{yyyy-MM}.csv` via transactionsToCsv with resolved category name + Consumo/Alocação type; it sits in the Extrato header; `npm run build` compiles /extrato; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Conta route shell + nav entry + UserMenu link</name>
  <files>src/app/(app)/conta/page.tsx, src/components/app-sidebar.tsx, src/components/user-menu.tsx</files>
  <read_first>
    - src/components/app-sidebar.tsx (NAV_ITEMS array + the active-item styling — add "Conta" LAST, after MEI)
    - src/components/user-menu.tsx (the dropdown — add "Privacidade e conta" above Sair with a separator)
    - src/app/(app)/extrato/page.tsx (an existing RSC page shape: createClient, h1, section wrapper)
    - 06-UI-SPEC.md §"Screen Contracts" 1. Privacidade e conta + §"Nav delta" (Conta last, lucide `shield`; UserMenu "Privacidade e conta" above Sair) + §"Copywriting Contract"
  </read_first>
  <action>
Create `src/app/(app)/conta/page.tsx` as a server component shell for the Privacidade e conta screen (UI-SPEC §1): h1 "Privacidade e conta" (one `text-xl font-semibold`) + a `text-sm text-muted-foreground` subtitle "Exporte ou apague seus dados a qualquer momento." Then a `DataExportSection` — a `--card` section (use the vendored `card` primitive) with weight-600 title "Exportar meus dados", a `text-sm` body and what's-included list per the UI-SPEC copy, and — as the secondary affordance available now — an `ExportTransactionsButton`. The transactions CSV on Conta is scoped to the current `?mes` (read it the same way Extrato does: createClient + monthBounds + the month's transactions joined to categories for category_name/kind). Leave a clearly-marked placeholder/comment where 06-03 will add the LGPD bundle button (Section A completion) and the AccountDeleteZone (Section B) — do NOT implement export/delete here; this plan only stands up the shell + the transactions affordance. Match the inherited design system (no teal CTA; export buttons are calm outline). No money hero on this screen.

In `src/components/app-sidebar.tsx`: add `{ href: '/conta', label: 'Conta', icon: <lucide shield> }` as the LAST entry in `NAV_ITEMS` (after MEI). Import the glyph from `lucide-react`. Active styling unchanged (it already matches `/conta/*` via the existing `startsWith` logic).

In `src/components/user-menu.tsx`: add a `DropdownMenuItem` "Privacidade e conta" linking to `/conta` ABOVE the Sair item, with a `DropdownMenuSeparator` between them (the existing separator sits under the email label — add the new item + a separator so the order is: email · sep · "Privacidade e conta" · sep · Sair). Use a `Link`/router push consistent with the existing dropdown's interaction.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E 'conta/page|app-sidebar|user-menu' || echo "conta shell + nav typecheck"</automated>
  </verify>
  <done>/conta renders the Privacidade e conta shell (h1 + subtitle + DataExportSection with the transactions CSV button + a marked placeholder for the 06-03 LGPD bundle + delete zone); "Conta" is the last sidebar item; UserMenu has a "Privacidade e conta" link above Sair; `npm run build` compiles /conta; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC (RLS-scoped read) → client button | the CSV button only ever receives rows RLS already restricted to the caller |
| client → file system | Blob download is client-only; no server secret in this path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Information Disclosure | ExportTransactionsButton exporting another user's rows | mitigate | the button serializes ONLY rows the RLS-scoped RSC returned (no fetch in the component); RLS makes "only my rows" structural |
| T-06-08 | Tampering | a malicious description breaking CSV columns | mitigate | the `field()` escaper in transactionsToCsv (06-01) quotes `;"\n`; pinned by 06-01 Task 2 Test 4 |
| T-06-SC | Tampering | npm installs | accept | zero new packages; only reuses vendored shadcn + lucide already present |
</threat_model>

<verification>
- `npm run build` compiles `/extrato` and `/conta`.
- `npx tsc --noEmit` clean.
- Manual (06-05): the Extrato/Conta "Exportar transações (CSV)" downloads a pt-BR CSV (BOM, `;`, comma decimals, resolved category + Tipo) for the current month.
- "Conta" appears last in the sidebar and as "Privacidade e conta" in the UserMenu.
</verification>

<success_criteria>
- User exports the current month's transactions to `transacoes-{yyyy-MM}.csv` from the Extrato header (DATA-01).
- The CSV resolves category name (point-in-time) + Consumo/Alocação type, formats money pt-BR, and escapes odd fields.
- The Privacidade e conta screen exists (shell + nav + UserMenu link) with the transactions CSV affordance, ready for 06-03 to add the LGPD bundle + delete zone.
- Build + tsc clean.
</success_criteria>

<output>
Create `.planning/phases/06-endurecimento-lgpd-isolamento-auditoria/06-02-SUMMARY.md` when done.
</output>
