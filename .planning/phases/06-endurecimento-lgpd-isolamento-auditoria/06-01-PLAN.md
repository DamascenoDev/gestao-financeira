---
phase: 06-endurecimento-lgpd-isolamento-auditoria
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/data/owned-tables.ts
  - src/lib/supabase/admin.ts
  - src/lib/transactions/csv.ts
  - src/lib/transactions/csv.test.ts
  - tests/rls-isolation.test.ts
  - tests/isolation-matrix.test.ts
  - tests/storage-isolation.test.ts
  - tests/lgpd-export.test.ts
  - tests/lgpd-delete.test.ts
  - tests/lgpd-delete-isolation.test.ts
  - tests/bundle-secret-grep.test.ts
  - tests/pii-guard.test.ts
autonomous: true
requirements: [DATA-01, DATA-02, SEC-01]
must_haves:
  truths:
    - "The 14 user-owned tables exist in one central constant that both the export bundle and the isolation matrix iterate"
    - "A server-only service-role admin client module exists and fails the build if any client component imports it"
    - "transactionsToCsv serializes RLS-scoped transaction rows to a pt-BR CSV (BOM + ; + comma decimals)"
    - "All 8 Wave-0 tests exist and fail for the right reason (missing implementation), not a harness crash"
  artifacts:
    - path: "src/lib/data/owned-tables.ts"
      provides: "The canonical OWNED_TABLES list of all 14 user-owned tables (+ per-table isolation INSERT shapes)"
      contains: "OWNED_TABLES"
    - path: "src/lib/supabase/admin.ts"
      provides: "Server-only service-role admin client (createAdminClient) — DELETE ONLY"
      contains: "import 'server-only'"
    - path: "src/lib/transactions/csv.ts"
      provides: "transactionsToCsv pure serializer mirroring src/lib/mei/csv.ts"
      exports: ["transactionsToCsv"]
    - path: "src/lib/transactions/csv.test.ts"
      provides: "DATA-01 unit test: BOM + ; + pt-BR decimals + ;\"\\n escaping"
    - path: "tests/isolation-matrix.test.ts"
      provides: "SEC-01 4-verb × 14-table data-driven isolation matrix (RED until owned-tables shapes complete)"
    - path: "tests/lgpd-export.test.ts"
      provides: "DATA-02 export completeness test (RED until 06-03 ships exportMyData)"
    - path: "tests/lgpd-delete.test.ts"
      provides: "DATA-02 full-delete test (RED until 06-03 ships deleteMyAccount core)"
    - path: "tests/lgpd-delete-isolation.test.ts"
      provides: "DATA-02 delete-A-leaves-B-intact test (RED until 06-03)"
    - path: "tests/storage-isolation.test.ts"
      provides: "SEC-01 Storage 2-user + no getPublicUrl audit"
    - path: "tests/pii-guard.test.ts"
      provides: "SEC-01 no-AI-dep + suggestCategory-null guard"
  key_links:
    - from: "tests/isolation-matrix.test.ts"
      to: "src/lib/data/owned-tables.ts"
      via: "import OWNED_TABLES"
      pattern: "owned-tables"
    - from: "src/lib/transactions/csv.ts"
      to: "src/lib/money"
      via: "formatCents"
      pattern: "formatCents"
---

<objective>
Substrate + Wave-0 for Phase 6 hardening: the three shared pure modules every later slice consumes, plus all 8 failing tests that pin the phase's behavior before any feature code exists.

This plan ships NO product feature. It creates the single central owned-table list (so a table can't ship without an isolation test or escape the LGPD export), the server-only service-role admin client (the first legitimate use of `SUPABASE_SECRET_KEY` — guarded so it can never reach the client bundle), and the `transactionsToCsv` pure serializer. It then writes the 8 Wave-0 tests: the ones whose implementation lands here go GREEN now; the ones whose implementation lands in 06-02/06-03/06-04 are authored RED with `it`/`it.todo` naming the exact behavior the later plan must satisfy.

Purpose: lock the contracts and the proofs first so the feature slices (06-02 CSV, 06-03 LGPD export+delete) are pure fill-in against a central list + a failing test, and the SEC-01 proof (06-04) is just "make the matrix green against a real build". [BLOCKING] — every later Phase 6 plan depends on this one.
Output: `src/lib/data/owned-tables.ts`, `src/lib/supabase/admin.ts`, `src/lib/transactions/csv.ts` (+ test), and 8 Wave-0 test files (new + extended), with the local suite running and failing only for missing-implementation reasons.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-endurecimento-lgpd-isolamento-auditoria/06-RESEARCH.md
@.planning/phases/06-endurecimento-lgpd-isolamento-auditoria/06-VALIDATION.md

# Mirror these EXACTLY (pattern + style):
@src/lib/mei/csv.ts
@src/lib/supabase/server.ts
@src/lib/money.ts
@src/lib/classifier/suggest.ts

# Extend these (already cover a partial set — promote to the central list):
@tests/rls-isolation.test.ts
@tests/import-storage-rls.test.ts
@tests/bundle-secret-grep.test.ts
@tests/helpers/local-supabase.ts
@scripts/check-bundle-secrets.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Central owned-tables list + server-only admin client</name>
  <files>src/lib/data/owned-tables.ts, src/lib/supabase/admin.ts</files>
  <read_first>
    - src/lib/supabase/server.ts (the RLS server-client pattern + the `server-only` guard)
    - 06-RESEARCH.md §"Owned-Table Inventory (the canonical 14)" (the table list + cascade facts) and §"Pattern 1" / Code Example "src/lib/supabase/admin.ts"
    - tests/rls-isolation.test.ts (the existing 8-table `TABLES` array + the per-table INSERT `rowByTable` shapes to lift)
  </read_first>
  <action>
Create `src/lib/data/owned-tables.ts` as the SINGLE source of truth for the 14 user-owned tables (per 06-RESEARCH §Owned-Table Inventory): export a const `OWNED_TABLES` listing exactly these 14 table names in a stable order — profiles, categories, income_templates, income_occurrences, transactions, budget_targets, reservas, reserva_ledger, statements, merchant_patterns, csv_import_profiles, mei_settings, mei_year_flags, mei_invoices. Type it `as const` so it's a readonly tuple usable as a literal-union type (export the element type too). Resolves Open Question #1 (csv_import_profiles IS included — it's user-owned, LGPD-favorable). Alongside the names, export a record `ISOLATION_INSERT_SHAPES` mapping each table to a minimal RLS-rejectable INSERT row owned by a placeholder user id — promote the existing `rowByTable` shapes from rls-isolation.test.ts (8 tables) and ADD the 6 missing shapes from the verified migration columns (NOT-NULL columns only; a placeholder uuid for any FK since WITH CHECK rejects on user_id BEFORE the FK resolves): statements `{ user_id, storage_path, format: 'ofx', content_hash }`, merchant_patterns `{ user_id, descriptor_norm, category_id: <uuid> }`, csv_import_profiles `{ user_id, header_signature, mapping: {} }`, mei_settings `{ user_id, mei_start_date: '2026-01-01' }`, mei_year_flags `{ user_id, year: 2026 }`, mei_invoices `{ user_id, issued_on: '2026-06-01', amount_cents: 100, tomador: 'X', activity_type: 'servicos' }`. Make the shapes a function `(userId) => Record<...>` or a record keyed by table, whichever keeps the isolation test a clean iteration. Document at the top that BOTH the LGPD export bundle (06-03) and the isolation matrix (06-04) iterate this list, so adding a v2 table auto-extends both.

Create `src/lib/supabase/admin.ts` mirroring 06-RESEARCH's Code Example exactly: first line `import 'server-only'`; `createAdminClient(): SupabaseClient<Database>` reading `process.env.NEXT_PUBLIC_SUPABASE_URL` + `process.env.SUPABASE_SECRET_KEY` (the server-only env var — NEVER `NEXT_PUBLIC_`), throwing if either is missing, returning `createClient<Database>(url, secret, { auth: { autoRefreshToken: false, persistSession: false } })`. Document that this is the FIRST legitimate server-side use of the secret, is DELETE-ONLY, must NEVER be imported by a client component (the `server-only` import makes the build fail loudly if it is), and must NEVER be used for normal reads (those use the RLS server client). Do NOT wire it into any action yet — 06-03 consumes it.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E 'owned-tables|admin\.ts' || echo "no type errors in new substrate modules"</automated>
  </verify>
  <done>owned-tables.ts exports OWNED_TABLES (14 entries) + the 14 isolation INSERT shapes; admin.ts exists with `import 'server-only'` + createAdminClient reading SUPABASE_SECRET_KEY; both typecheck. Neither is imported by any client component.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: transactionsToCsv pure serializer (DATA-01) + unit test</name>
  <files>src/lib/transactions/csv.ts, src/lib/transactions/csv.test.ts</files>
  <read_first>
    - src/lib/mei/csv.ts (mirror EXACTLY: BOM via String.fromCharCode(0xFEFF), `;` delimiter, `\r\n`, formatCents)
    - src/lib/mei/csv.test.ts (mirror the assertion style for BOM/delimiter/pt-BR parity)
    - src/lib/money.ts (formatCents signature)
    - 06-UI-SPEC.md §"Component Inventory" `transactionsToCsv` row + §"Money & Number Formatting" (columns: Data dd/MM/yyyy · Descrição · Categoria · Tipo (Consumo/Alocação) · Valor)
  </read_first>
  <behavior>
    - Test 1: output starts with the UTF-8 BOM (String.fromCharCode(0xFEFF)), uses `;` delimiter and `\r\n` line endings, ends with a trailing `\r\n` (byte-for-byte parity with meiReportToCsv shape).
    - Test 2: money column renders pt-BR via formatCents (e.g. 123456 → "R$ 1.234,56" with comma decimal); a row serializes Data as dd/MM/yyyy.
    - Test 3: the Tipo column maps category kind to "Consumo"/"Alocação"; an undefined/empty category still serializes a valid line (e.g. "Sem categoria").
    - Test 4: a field containing `;`, `"`, or a newline is quoted and inner `"` doubled (RFC-4180 escape), so a malicious/odd description can't break the column layout.
  </behavior>
  <action>
Create `src/lib/transactions/csv.ts` mirroring `src/lib/mei/csv.ts`: a `BOM = String.fromCharCode(0xfeff)` (generated in code, NEVER a literal invisible char — the mei/csv.ts comment is load-bearing), `DELIMITER = ';'`, a `field(value)` escaper that quotes when the value contains `;`/`"`/CR/LF and doubles inner quotes, and `transactionsToCsv(rows)` joining a header row + one row per transaction with `\r\n`, returning `BOM + lines.join('\r\n') + '\r\n'`. Define an exported `TransactionCsvRow` interface: `occurred_on` (YYYY-MM-DD), `description`, `category_name` (resolved point-in-time name — Open Question #2: name in the CSV, raw id stays in the JSON bundle), `category_kind` ('consumo' | 'alocacao' | null), `amount_cents` (number | bigint). Columns per UI-SPEC: Data (format occurred_on YYYY-MM-DD → dd/MM/yyyy), Descrição, Categoria (category_name or 'Sem categoria'), Tipo (consumo→'Consumo', alocacao→'Alocação', null→''), Valor (formatCents). Money ONLY through formatCents. Do NOT format money by hand; do NOT emit a negative number (transactions are positive, sign is semantic).

Write `src/lib/transactions/csv.test.ts` asserting the 4 behaviors above. Mirror `src/lib/mei/csv.test.ts` assertion style. This test goes GREEN now (implementation ships in this same task).
  </action>
  <verify>
    <automated>npx vitest run src/lib/transactions/csv.test.ts</automated>
  </verify>
  <done>transactionsToCsv emits BOM + `;` + `\r\n` + pt-BR money + escaped fields; csv.test.ts is GREEN; mirrors mei/csv.ts exactly.</done>
</task>

<task type="auto">
  <name>Task 3: Author the 8 Wave-0 tests (extend 2, create 6) — RED where impl is deferred</name>
  <files>tests/rls-isolation.test.ts, tests/isolation-matrix.test.ts, tests/storage-isolation.test.ts, tests/lgpd-export.test.ts, tests/lgpd-delete.test.ts, tests/lgpd-delete-isolation.test.ts, tests/bundle-secret-grep.test.ts, tests/pii-guard.test.ts</files>
  <read_first>
    - 06-VALIDATION.md §"Per-Task Verification Map" (the 8 test ids 6-W0-01..08, their commands + behaviors) and §"Wave 0 Requirements"
    - 06-RESEARCH.md §"Code Examples" (delete isolation sketch, storage-public audit sketch, PII-egress guard sketch)
    - tests/rls-isolation.test.ts + tests/import-storage-rls.test.ts (the 2-user harness to reuse — createUser/serviceClient/userClient)
    - tests/helpers/local-supabase.ts (serviceClient/userClient/readLocalConfig — reuse verbatim)
    - tests/bundle-secret-grep.test.ts + scripts/check-bundle-secrets.sh (extend the secret audit)
    - src/lib/classifier/suggest.ts + src/lib/classifier/suggest.test.ts (the null seam the PII guard re-asserts)
  </read_first>
  <action>
Author the 8 Wave-0 tests per 06-VALIDATION. Reuse `tests/helpers/local-supabase.ts` (serviceClient/userClient/createUser pattern) verbatim — do NOT re-implement user creation. Each test that depends on a not-yet-shipped action must FAIL for a missing-implementation reason (named `it`) or be a named `it.todo` pointing at the owning plan — never a harness crash, never a false green.

1. EXTEND `tests/rls-isolation.test.ts` (6-W0-05 partial): replace the local 8-entry `TABLES` array with `OWNED_TABLES` imported from `src/lib/data/owned-tables.ts`, and replace the inline `rowByTable` with `ISOLATION_INSERT_SHAPES`. The existing 4-verb loop auto-extends to 14 tables. (This is the data-driven promotion — Pitfall 4.)
2. CREATE `tests/isolation-matrix.test.ts` (6-W0-05): the comprehensive 4-verb × 14-table matrix driven entirely by `OWNED_TABLES` + `ISOLATION_INSERT_SHAPES` — B cannot SELECT/INSERT/UPDATE/DELETE any of A's rows. If this duplicates the extended rls-isolation, keep rls-isolation as the canonical matrix and make isolation-matrix import/assert the same data-driven coverage at the granularity 06-VALIDATION names (`npx vitest run isolation-matrix`); do not author two divergent matrices — one shared source, one entry point.
3. CREATE `tests/storage-isolation.test.ts` (6-W0-06): extend the `import-storage-rls.test.ts` proofs — B cannot read/list/delete A's `{userA.id}/` objects — AND a `getPublicUrl`-absence audit: `grep -rn 'getPublicUrl' src/` must return no matches (wrap execFileSync; grep exits non-zero when clean → treat as pass), AND assert the `statements` bucket is private (`admin.storage.getBucket('statements')` → `data.public === false`).
4. CREATE `tests/lgpd-export.test.ts` (6-W0-02): seed a row in each of the 14 tables for user A + a Storage object, call the export path, assert the bundle has a key for EVERY entry in `OWNED_TABLES` and only A's rows. Since `exportMyData` ships in 06-03, author this RED: name the assertions and reference the not-yet-exported `exportMyData`; if the import would crash collection, use named `it.todo` per behavior (completeness, only-my-rows) so 06-03 flips them.
5. CREATE `tests/lgpd-delete.test.ts` (6-W0-03): RED for 06-03 — after deleting A, every one of A's 14 tables returns 0 rows AND A's Storage `{id}/` object is gone AND `admin.auth.admin.getUserById(A)` fails. Author as named `it.todo`/RED referencing the deferred `deleteMyAccount` core.
6. CREATE `tests/lgpd-delete-isolation.test.ts` (6-W0-04): RED for 06-03 — deleting A leaves every one of B's 14 rows + B's Storage object intact (the load-bearing "doesn't touch B" guarantee).
7. EXTEND `tests/bundle-secret-grep.test.ts` (6-W0-07): keep it meaningful now that the secret is actually used server-side. Add (or document a wave-4 step for) a real `next build` → `bash scripts/check-bundle-secrets.sh .next/static` exiting 0; the script already greps `sb_secret_|service_role|SUPABASE_SECRET_KEY`. The "absent ⇒ pass" contract stays; the new assertion is that after a real build the markers are absent.
8. CREATE `tests/pii-guard.test.ts` (6-W0-08): assert `package.json` has NO `ai` and no `@ai-sdk*` dependency (read deps+devDeps), AND `await suggestCategory('padaria sao joao', [...])` returns null, AND the classifier path makes no network call (re-assert the existing suggest.test.ts contract). This goes GREEN now (no AI dep today).

After authoring, run the LOCAL suite once (requires `supabase start`) to confirm every failure is a missing-implementation/RED reason — NOT a helper crash, NOT a collection error. Mark `06-VALIDATION.md` frontmatter `wave_0_complete: true` and `nyquist_compliant: true`.
  </action>
  <verify>
    <automated>SUPABASE_DISABLE_TELEMETRY=1 npx vitest run src/lib/transactions/csv.test.ts tests/pii-guard.test.ts tests/storage-isolation.test.ts tests/isolation-matrix.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>All 8 Wave-0 tests exist (2 extended, 6 created); GREEN ones (transactions-csv, pii-guard, storage-isolation, isolation-matrix data-driven) pass; deferred ones (lgpd-export/delete/delete-isolation) are RED/it.todo naming the 06-03 behavior they pin; rls-isolation now iterates OWNED_TABLES (14); 06-VALIDATION marked wave_0_complete + nyquist_compliant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client component → server module | a `'use client'` file must NEVER transitively import `admin.ts` (would bundle the secret) |
| build output → world | `.next/static` is world-readable; the secret must never appear there |
| test helper → Supabase | `local-supabase.ts` is hard-guarded to 127.0.0.1; service-role key never points remote |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure / Elevation | `src/lib/supabase/admin.ts` (SUPABASE_SECRET_KEY) | mitigate | `import 'server-only'` first line; never `NEXT_PUBLIC_`; not imported anywhere yet (06-03 only); bundle-secret audit (Task 3) stays green |
| T-06-03 | Information Disclosure | `OWNED_TABLES` drift vs schema | mitigate | one central constant; isolation matrix + export both iterate it; a missing table fails the export-completeness test (06-03) |
| T-06-05 | Tampering / Info Disclosure | RLS gap on a table the matrix skips | mitigate | promote the 8→14 table matrix data-driven over OWNED_TABLES so no table escapes coverage (Pitfall 4) |
| T-06-06 | Information Disclosure (LGPD) | PII egress to a future LLM | mitigate | pii-guard asserts no `ai`/`@ai-sdk` dep + `suggestCategory` null (Pitfall 6) |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | this phase installs ZERO packages (06-RESEARCH §Package Legitimacy: N/A); no install task; if a zip lib is ever proposed, run the legitimacy gate first |
</threat_model>

<verification>
- `npx tsc --noEmit` clean (new substrate modules typecheck against database.types).
- `supabase start` running, then `SUPABASE_DISABLE_TELEMETRY=1 npx vitest run`: every failure is a named RED/it.todo for a 06-03-deferred behavior — no harness crash, no collection error.
- `grep -rn "import 'server-only'" src/lib/supabase/admin.ts` matches (the guard is present).
- No `'use client'` file imports `admin.ts` (grep clean).
</verification>

<success_criteria>
- `src/lib/data/owned-tables.ts` lists all 14 owned tables + their isolation INSERT shapes; it is the only place the table set is defined.
- `src/lib/supabase/admin.ts` is server-only, reads SUPABASE_SECRET_KEY, exports createAdminClient, is unused by any client component.
- `transactionsToCsv` (DATA-01) emits pt-BR BOM/;/comma CSV with escaped fields; its unit test is GREEN.
- 8 Wave-0 tests exist; the implementable ones are GREEN, the 06-03-deferred ones are RED/it.todo naming their target behavior; rls-isolation iterates OWNED_TABLES (14 tables).
- 06-VALIDATION marked wave_0_complete + nyquist_compliant.
</success_criteria>

<output>
Create `.planning/phases/06-endurecimento-lgpd-isolamento-auditoria/06-01-SUMMARY.md` when done.
</output>
