---
phase: 25-fix-de-scroll-na-cria-o-de-palavra-chave
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/actions/category-keywords.ts
  - src/actions/category-keywords.test.ts
autonomous: true
requirements: [UX-01]
must_haves:
  truths:
    - "addKeywordInline persiste a keyword (mesmas 4 guards + dup + insert) SEM chamar revalidatePath"
    - "addKeyword continua chamando revalidatePath('/categorias') no sucesso (path /categorias inalterado → SC3)"
    - "Ambas as actions retornam a mesma union AddKeywordResult ({ok}|{duplicate}|{error})"
  artifacts:
    - path: "src/actions/category-keywords.ts"
      provides: "Helper privado insertKeyword (não-exportado) + export addKeywordInline; addKeyword preservado"
      contains: "export async function addKeywordInline"
    - path: "src/actions/category-keywords.test.ts"
      provides: "Asserts: addKeywordInline NÃO revalida + paridade de guards/dup/23505; addKeyword ainda revalida"
      contains: "addKeywordInline"
  key_links:
    - from: "src/actions/category-keywords.ts (addKeyword)"
      to: "src/actions/category-keywords.ts (insertKeyword helper)"
      via: "addKeyword delega ao helper e adiciona revalidatePath no {ok}"
      pattern: "insertKeyword"
    - from: "src/actions/category-keywords.ts (addKeywordInline)"
      to: "src/actions/category-keywords.ts (insertKeyword helper)"
      via: "addKeywordInline delega ao helper SEM revalidatePath"
      pattern: "insertKeyword"
---

<objective>
Cortar a causa-raiz do scroll jump (UX-01) no lado servidor: extrair o core de `addKeyword` num helper privado não-exportado (`insertKeyword`) — bit-idêntico às 4 guards + dup pre-check + insert + backstop 23505 de hoje — e expor uma segunda action `addKeywordInline` que roda o helper SEM `revalidatePath`. `addKeyword` continua sendo helper + `revalidatePath('/categorias')`, mantendo o refresh legítimo da página `/categorias` (SC3). Implementa D-01/D-02/D-03.

Purpose: Num Server Action, `revalidatePath` invalida o Router Cache do client e re-renderiza a rota ativa (`/importar/[statementId]`), resetando o scroll. A action inline sem revalidate elimina esse sinal de refresh — sem tocar o contrato compartilhado nem o path `/categorias`.
Output: `src/actions/category-keywords.ts` ganha `insertKeyword` (privado) + `addKeywordInline` (exportado); `addKeyword` preservado. `src/actions/category-keywords.test.ts` estendido com a paridade de guards e a asserção de revalidate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-fix-de-scroll-na-cria-o-de-palavra-chave/25-CONTEXT.md
@.planning/phases/25-fix-de-scroll-na-cria-o-de-palavra-chave/25-RESEARCH.md
@.planning/phases/25-fix-de-scroll-na-cria-o-de-palavra-chave/25-VALIDATION.md

# Arquivos a alterar (já em contexto via RESEARCH § Pattern 1 + Code Examples):
@src/actions/category-keywords.ts
@src/actions/category-keywords.test.ts
</context>

<artifacts_this_phase_produces>
Símbolos NOVOS criados neste plano (source-grounding — não são drift):
- `insertKeyword(categoryId: string, keyword: string): Promise<AddKeywordResult>` — helper PRIVADO não-exportado em `src/actions/category-keywords.ts`. Core bit-idêntico ao `addKeyword` atual (4 guards → dup pre-check `maybeSingle` → insert → backstop 23505). SEM `revalidatePath`.
- `addKeywordInline(categoryId: string, keyword: string): Promise<AddKeywordResult>` — action EXPORTADA nova. Roda `insertKeyword` SEM revalidate. Mesma union de retorno que `addKeyword`.

Símbolos PRESERVADOS (não renomear, não alterar contrato):
- `addKeyword` — continua exportada, agora = `insertKeyword` + `revalidatePath('/categorias')` no `{ok}`.
- `AddKeywordResult` — union inalterada (`{ok:true} | {duplicate:true} | {error:string}`).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — estender o teste de category-keywords com paridade de guards e contrato de revalidate</name>
  <files>src/actions/category-keywords.test.ts</files>
  <read_first>
    - src/actions/category-keywords.test.ts — harness vitest existente: mock de `next/cache` (`revalidatePath` espião, L24-26), mock de `@/lib/supabase/server` (L114), import de `addKeyword` (L118-124), `beforeEach` com `revalidatePath.mockClear()` (L135), bloco `describe('addKeyword', …)` (L148) com os casos de sucesso (assert `revalidatePath` chamado com `/categorias`, L161), guards (`*`/`**`/''/`'   '`/`/`/`***`/61-chars), dup pre-check, 23505, uuid inválido, sessão expirada. Os asserts modelo de `revalidatePath` ficam em L161/L361/L390/L419.
    - src/actions/category-keywords.ts (L30-96) — `AddKeywordResult`, as 4 guards exatas, dup `maybeSingle`, insert, 23505, `revalidatePath(CATEGORIAS_PATH)` no fim.
  </read_first>
  <behavior>
    Novo bloco `describe('addKeywordInline', …)` espelhando os casos de `addKeyword`, MAIS um par de asserts de contraste de revalidate. Importar `addKeywordInline` junto de `addKeyword` (L118-124). Casos a cobrir (RED — a action ainda não existe, então o import quebra a suíte; Task 2 a cria):
    - Sucesso (`{ok:true}`): keyword nova válida → retorna `{ok:true}` E `expect(revalidatePath).not.toHaveBeenCalled()`.
    - Contraste explícito: um teste que chama `addKeyword(CAT_ID,'uber')` → `revalidatePath` chamado com `/categorias`; e um que chama `addKeywordInline(CAT_ID,'uber')` → `revalidatePath` NÃO chamado. (Prova SC1 vs SC3 lado a lado.)
    - Paridade de guards (mesmos retornos que addKeyword, sem revalidate em nenhum): `'*'`/`'**'`/`'***'` → `{error}` (use ao menos uma letra/número além de `*`); `''`/`'   '`/`'/'` → `{error}` (informe uma palavra-chave / dados inválidos conforme o caso atual); `'a'.repeat(61)` → `{error}`; uuid inválido (`'not-a-uuid'`) → `{error: 'Identificador inválido.'}`.
    - Dup pre-check (`maybeSingle` retorna existing) → `{duplicate:true}` e `revalidatePath` NÃO chamado.
    - Backstop 23505 no insert → `{duplicate:true}` e `revalidatePath` NÃO chamado.
    - Glob válido (`'UBER*'`): persiste normalizado preservando o `*` (espelha o caso L184 de addKeyword) → `{ok:true}`, sem revalidate.
    Reutilizar os mesmos helpers de mock do arquivo (não duplicar a fábrica do supabase). Manter os asserts de `addKeyword` existentes intactos (SC3 não pode regredir).
  </behavior>
  <action>
    Estender `src/actions/category-keywords.test.ts`: adicionar `addKeywordInline` ao import existente de `@/actions/category-keywords`. Criar um bloco `describe('addKeywordInline', …)` com os casos descritos em <behavior>, reaproveitando os mesmos mocks de supabase/`next-cache` e o `beforeEach` que faz `revalidatePath.mockClear()`. Para cada caso de sucesso/duplicate/erro de `addKeywordInline`, assertar `expect(revalidatePath).not.toHaveBeenCalled()`. Adicionar UM teste de contraste direto (addKeyword revalida `/categorias`; addKeywordInline não) para fixar SC1 vs SC3. NÃO alterar nenhum assert existente de `addKeyword`/`removeKeyword`/`approveKeywordSuggestions`. Após este task a suíte fica VERMELHA (import de `addKeywordInline` inexistente) — esperado no ciclo RED.
  </action>
  <verify>
    <automated>npx vitest run src/actions/category-keywords.test.ts 2>&1 | grep -qiE "addKeywordInline|Cannot find|is not (a function|exported)|error TS2305" && echo "RED-OK: testes referenciam addKeywordInline (ainda não existe)" || (echo "esperava falha por addKeywordInline ausente" && exit 1)</automated>
  </verify>
  <done>O teste importa e exercita `addKeywordInline` (sucesso sem revalidate, paridade de guards, dup, 23505, contraste com addKeyword). A suíte falha SÓ porque `addKeywordInline` ainda não existe (ciclo RED). Nenhum assert pré-existente de `addKeyword` foi removido/alterado.</done>
  <acceptance_criteria>
    - `addKeywordInline` está no import de `@/actions/category-keywords` no topo do teste.
    - Existe `describe('addKeywordInline', …)` com ≥6 casos (sucesso, contraste de revalidate, ≥1 guard de `*`, dup, 23505, uuid inválido).
    - Todo caso de `addKeywordInline` que chega ao insert/dup assere `revalidatePath` NÃO chamado; o caso de contraste assere `addKeyword` chamando `/categorias`.
    - Os blocos `describe('addKeyword'…)`/`describe('removeKeyword'…)`/`describe('approveKeywordSuggestions'…)` permanecem byte-equivalentes (nenhum assert removido).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extrair helper privado insertKeyword + expor addKeywordInline (GREEN)</name>
  <files>src/actions/category-keywords.ts</files>
  <read_first>
    - src/actions/category-keywords.ts (L1-96) — imports (`revalidatePath` de `next/cache` L3; `keywordSchema` L6; `normalizeKeyword` L7; `createClient` L8); `AddKeywordResult` (L32); `CATEGORIAS_PATH = '/categorias'` (L34); `idSchema` (L41); corpo de `addKeyword` (L44-96): guard idSchema → keywordSchema → `normalizeKeyword==''` → literal-count-0 (`normalized.replace(/\*/g,'')===''`) → `getClaims` userId → dup `maybeSingle` → insert → 23505 → `revalidatePath(CATEGORIAS_PATH)`.
    - 25-RESEARCH.md § Pattern 1 (Code Example, L110-157) — a forma EXATA do helper + as duas actions. Nota de discrição (L157): revalidar SÓ em `{ok}` preserva o comportamento atual (duplicate hoje retorna ANTES do revalidate).
  </read_first>
  <behavior>
    Após criar `insertKeyword` + `addKeywordInline`, a suíte do Task 1 fica VERDE e os asserts de `addKeyword` continuam passando. Comportamento observável:
    - `insertKeyword(categoryId, keyword)`: as 4 guards na MESMA ordem e com as MESMAS mensagens pt-BR de hoje; `getClaims().claims.sub` para o `user_id` (NUNCA do client); dup pre-check `maybeSingle`; insert; 23505 → `{duplicate:true}`; erro genérico → `{error:'Não foi possível salvar a palavra-chave.'}`. NUNCA chama `revalidatePath`.
    - `addKeyword`: delega a `insertKeyword`; quando o resultado é `{ok}`, chama `revalidatePath(CATEGORIAS_PATH)`; retorna o resultado do helper. (Duplicate/erro NÃO revalidam — idêntico ao comportamento atual, onde duplicate retorna antes do revalidate.)
    - `addKeywordInline`: retorna `insertKeyword(...)` direto, SEM revalidate.
  </behavior>
  <action>
    Refatorar `src/actions/category-keywords.ts` conforme D-02 (RESEARCH § Pattern 1): mover o corpo de `addKeyword` (L48-92, da guard idSchema até o tratamento de 23505 inclusive, MENOS o `revalidatePath` final) para uma função privada não-exportada `async function insertKeyword(categoryId: string, keyword: string): Promise<AddKeywordResult>` — bit-idêntica (mesmas guards, mesma ordem, mesmas mensagens, mesmo `getClaims`/dup/insert/23505). Reescrever `addKeyword` como: `const r = await insertKeyword(categoryId, keyword); if ('ok' in r) revalidatePath(CATEGORIAS_PATH); return r`. Adicionar `export async function addKeywordInline(categoryId: string, keyword: string): Promise<AddKeywordResult> { return insertKeyword(categoryId, keyword) }`. Manter `'use server'`, todos os imports, `AddKeywordResult`, `CATEGORIAS_PATH`, `idSchema` e o JSDoc relevante. NÃO tocar `removeKeyword`/`approveKeywordSuggestions`/`getKeywordSuggestions` (fora de escopo — também revalidam `/categorias`, por design).
  </action>
  <verify>
    <automated>npx vitest run src/actions/category-keywords.test.ts</automated>
  </verify>
  <done>`insertKeyword` (privado) contém o core; `addKeyword` = helper + `revalidatePath` no `{ok}`; `addKeywordInline` (exportado) = helper sem revalidate. Toda a suíte de `category-keywords.test.ts` passa (addKeyword ainda revalida `/categorias`; addKeywordInline não revalida; paridade de guards verde).</done>
  <acceptance_criteria>
    - `insertKeyword` existe, NÃO é exportado, e NÃO referencia `revalidatePath` em seu corpo.
    - `addKeyword` chama `revalidatePath(CATEGORIAS_PATH)` somente no ramo `{ok}` e segue exportado.
    - `addKeywordInline` é exportado, retorna `insertKeyword(...)` e nunca chama `revalidatePath`.
    - As 4 guards (idSchema → keywordSchema → `normalizeKeyword==''` → literal-count-0) permanecem na mesma ordem com as mesmas mensagens pt-BR.
    - `npx vitest run src/actions/category-keywords.test.ts` passa 100%; `npx tsc --noEmit` sem erros novos neste arquivo.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Server Action (`addKeyword`/`addKeywordInline`) | `categoryId` + `keyword` vêm do client; entrada não confiável que cruza para escrita no banco |
| Server Action → Postgres (insert em `category_keywords`) | escrita autenticada escopada por `user_id` do servidor + RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Elevation of Privilege | insertKeyword (insert em category_keywords) | mitigate | `user_id` vem de `getClaims().claims.sub` (NUNCA do client) + RLS `auth.uid()=user_id`; preservado bit-idêntico do `addKeyword` atual. `idSchema` uuid no `categoryId` (defense-in-depth, evita 22P02). |
| T-25-02 | Tampering | keyword forjada / literal-count-0 (`*`/`**`) | mitigate | `keywordSchema` (trim/min1/max60) + `normalizeKeyword` + rejeição de literal-count-0 — guards bit-idênticas migradas para o helper; nenhuma é afrouxada. |
| T-25-03 | Information Disclosure | erro cru do DB no insert | mitigate | Helper retorna mensagem genérica (`'Não foi possível salvar a palavra-chave.'`); 23505 vira `{duplicate}`; nenhum erro cru vaza — preservado. |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote instalado nesta fase (RESEARCH § Package Legitimacy Audit: N/A). Sem superfície de slopsquat. |

**Nota:** mover o core para `insertKeyword` NÃO cria fronteira de confiança nova — as mesmas guards, `getClaims` e RLS rodam idênticos. A única diferença observável é a AUSÊNCIA do `revalidatePath` em `addKeywordInline` (efeito de cache do cliente, não de segurança). Sem ameaças de severidade `high`.
</threat_model>

<verification>
- `npx vitest run src/actions/category-keywords.test.ts` passa 100% (paridade de guards verde; addKeyword revalida `/categorias`; addKeywordInline não revalida).
- `npx tsc --noEmit` não introduz erros novos.
- Inspeção: `insertKeyword` não-exportado e sem `revalidatePath`; `addKeyword` revalida só no `{ok}`; `addKeywordInline` exportado e sem revalidate.
- Diff de `database.types.ts` vazio (sem schema delta — esta fase não toca o banco; ver RESEARCH § Runtime State Inventory).
</verification>

<success_criteria>
- D-01/D-02/D-03 implementados: action inline separada (`addKeywordInline`), via helper privado compartilhado (`insertKeyword`), retornando a mesma `AddKeywordResult`.
- SC3 protegido: o path `/categorias` continua recebendo `revalidatePath` via `addKeyword` (inalterado).
- Base para SC1 entregue: existe uma action de persistência de keyword SEM revalidate, pronta para o caller inline (Plan 02) consumir.
</success_criteria>

<output>
Create `.planning/phases/25-fix-de-scroll-na-cria-o-de-palavra-chave/25-01-SUMMARY.md` when done
</output>
