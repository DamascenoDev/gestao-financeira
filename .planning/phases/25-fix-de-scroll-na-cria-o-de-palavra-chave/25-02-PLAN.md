---
phase: 25-fix-de-scroll-na-cria-o-de-palavra-chave
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - src/components/import-review-table.tsx
  - src/components/import-review-table.test.tsx
autonomous: false
requirements: [UX-01, UX-02]
must_haves:
  truths:
    - "O caller inline usa addKeywordInline (não addKeyword) → criar keyword inline NÃO reseta o scroll (UX-01/SC1)"
    - "Ao criar keyword inline, as outras linhas que casam são re-classificadas ao vivo client-side, sem refresh (UX-02/SC4)"
    - "Re-classify aplica em category_id===null e sobrescreve origin∈{memória,palavra-chave}; NUNCA toca origin==='manual' (SC5)"
    - "Linhas recém-casadas recebem origin==='palavra-chave' e nenhum confidence (espelha o pipeline de upload)"
  artifacts:
    - path: "src/components/import-review-table.tsx"
      provides: "Export puro reclassifyRowsWithKeyword + lift-state (novo callback do pai) + swap do caller para addKeywordInline"
      contains: "export function reclassifyRowsWithKeyword"
    - path: "src/components/import-review-table.test.tsx"
      provides: "Testes da função pura: null+memória+palavra-chave sobrescrevem; manual preserva; provenance→'palavra-chave'; degenerada (*) no-op"
      contains: "reclassifyRowsWithKeyword"
  key_links:
    - from: "KeywordInlineSuggest.onSubmit"
      to: "addKeywordInline (Plan 01)"
      via: "swap do caller addKeyword → addKeywordInline"
      pattern: "addKeywordInline"
    - from: "KeywordInlineSuggest (onSubmit, ramo {ok}/{duplicate})"
      to: "ImportReviewTable setRows"
      via: "callback do pai (lift state) → setRows(prev => reclassifyRowsWithKeyword(...))"
      pattern: "reclassifyRowsWithKeyword"
    - from: "reclassifyRowsWithKeyword"
      to: "src/lib/classifier/keywords.ts (compileRule/matchKeyword)"
      via: "matcher puro client-safe, sem round-trip"
      pattern: "compileRule|matchKeyword"
---

<objective>
Entregar o fim do scroll jump no caller (UX-01) e a re-classificação ao vivo da grid (UX-02), tudo client-side e sem refresh. Trocar o caller inline `addKeyword` → `addKeywordInline` (Plan 01). Após o sucesso (`{ok}`/`{duplicate}`) do create inline, rodar uma função PURA exportada `reclassifyRowsWithKeyword(rows, categoryId, normalizedKeyword): ReviewRow[]` — usando o matcher puro `compileRule`/`matchKeyword` — sobre as linhas em estado, aplicando a keyword nova em linhas `category_id === null` OU `origin ∈ {memória, palavra-chave}`, NUNCA `origin === 'manual'`, marcando as casadas com `origin: 'palavra-chave'`. Implementa D-03/D-04/D-05.

Purpose: O caller sem revalidate corta o scroll jump na raiz (SC1); o re-classify via `setRows` imutável atualiza só os campos das linhas-alvo sem refetch/remount, preservando scroll + seleção (SC4) e respeitando a intenção manual (SC5).
Output: `src/components/import-review-table.tsx` ganha o export puro `reclassifyRowsWithKeyword`, um novo callback de lift-state (ex. `onKeywordPersisted`) threaded `ImportReviewTable → cell → InlineReviewCategoryCell → KeywordInlineSuggest`, e o swap do caller. `src/components/import-review-table.test.tsx` ganha os testes da função pura.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-fix-de-scroll-na-cria-o-de-palavra-chave/25-CONTEXT.md
@.planning/phases/25-fix-de-scroll-na-cria-o-de-palavra-chave/25-RESEARCH.md
@.planning/phases/25-fix-de-scroll-na-cria-o-de-palavra-chave/25-VALIDATION.md

# Depende do Plan 01 (addKeywordInline já existe):
@.planning/phases/25-fix-de-scroll-na-cria-o-de-palavra-chave/25-01-SUMMARY.md

# Arquivos a alterar / reusar (já em contexto via RESEARCH § Pattern 2 + Code Examples):
@src/components/import-review-table.tsx
@src/components/import-review-table.test.tsx
@src/lib/classifier/keywords.ts
@src/lib/normalize.ts
</context>

<artifacts_this_phase_produces>
Símbolos NOVOS criados neste plano (source-grounding — não são drift):
- `reclassifyRowsWithKeyword(rows: ReviewRow[], categoryId: string, normalizedKeyword: string): ReviewRow[]` — função PURA EXPORTADA de `src/components/import-review-table.tsx` (espelha os exports puros existentes `lowConfidenceFirst` L224 e `confirmToastMessage` L296). Aplica a keyword via `compileRule`/`matchKeyword`; alvo `category_id===null` OU `origin∈{memória,palavra-chave}`; nunca `manual`; casadas → `origin:'palavra-chave'`.
- Novo callback de prop de lift-state (nome à discrição, ex. `onKeywordPersisted`) carregando `(categoryId: string, normalizedKeyword: string)` — threaded por TRÊS níveis: `ImportReviewTable` (handler que chama `setRows`) → coluna `category_id` cell (L572-581) → `InlineReviewCategoryCell` (props L955-973) → `KeywordInlineSuggest` (props L1126-1136, disparado no `onSubmit` L1158-1177).

ATENÇÃO ao nome: NÃO reusar `onKeywordCreated` — esse nome JÁ existe e mapeia para `markKeywordCreated` (o "criada ✓"). Escolher um nome distinto (ex. `onKeywordPersisted`) para o callback de re-classify.

Símbolos PRESERVADOS:
- `KeywordInlineSuggest`, `InlineReviewCategoryCell`, `markKeywordCreated`/`onKeywordCreated`, `classifyRow`, `setRows`, `ReviewRow`, `lowConfidenceFirst`, `confirmToastMessage` — todos mantidos; só RECEBEM o novo callback adicional.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — testes da função pura reclassifyRowsWithKeyword (RED)</name>
  <files>src/components/import-review-table.test.tsx</files>
  <read_first>
    - src/components/import-review-table.test.tsx (L1-110) — imports do bloco superior (`@testing-library/react`, vitest) e o import de `@/components/import-review-table` (L4-…) onde os exports puros são trazidos; `describe('ImportReviewTable — suggestion affordances')` (L110); `describe('KW-07 inline keyword suggestion')` (L457).
    - src/components/import-review-table.tsx — `ReviewRow` (L240-279): campos `descriptor_norm`, `category_id`, `origin` ('memória'|'palavra-chave'|'manual'|'não classificada'), `suggestion?`; exports puros modelo `lowConfidenceFirst` (L224) e `confirmToastMessage` (L296).
    - src/lib/classifier/keywords.ts — `compileRule(categoryId, keyword, sort): KeywordRule|null` (null para literal-count-0), `matchKeyword(descriptorNorm, rules): KeywordMatch|null`. Matcher NUNCA re-normaliza; passar valores já normalizados.
    - 25-RESEARCH.md § Pattern 2 (L159-193) + Pitfalls 1-4 (L229-251) — semântica exata; em especial: `origin: 'ia'` NÃO existe; "IA não aplicada" = `category_id===null`; keyword degenerada (`*`) → `compileRule` null → no-op.
  </read_first>
  <behavior>
    Novo bloco `describe('reclassifyRowsWithKeyword', …)` (não precisa de render — função pura). Importar `reclassifyRowsWithKeyword` do módulo. Construir um pequeno fixture de `ReviewRow[]` (helper local que monta uma row com os campos mínimos: `id`, `descriptor_norm`, `category_id`, `origin`, e os demais campos obrigatórios com valores inertes). Casos (RED — função ainda não existe → import quebra):
    - Aplica em NÃO-classificada: row `{category_id:null, origin:'não classificada', descriptor_norm:'uber trip 123'}` + keyword `'uber'` na catX → vira `{category_id:'catX', origin:'palavra-chave'}`.
    - Sobrescreve MEMÓRIA: row `{category_id:'catY', origin:'memória', descriptor_norm:'pedido ifood centro'}` + keyword `'ifood'` na catX → vira `{category_id:'catX', origin:'palavra-chave'}`.
    - Sobrescreve PALAVRA-CHAVE: row `{origin:'palavra-chave', category_id:'catY'}` casando → vira catX + origin 'palavra-chave'.
    - PRESERVA MANUAL: row `{origin:'manual', category_id:'catY', descriptor_norm:'uber trip'}` + keyword `'uber'` → INALTERADA (mesma referência ou deep-equal ao original; category_id catY, origin 'manual').
    - NÃO casa → linha inalterada.
    - Provenance + sem confidence: a linha casada tem `origin === 'palavra-chave'` e NÃO ganha `confidence` (a row não passa a ter `suggestion` inventada; `suggestion` pré-existente fica intacta).
    - Glob: keyword `'UBER*'` (com `*` preservado) casa `'uber trip 123'` → reclassifica (passar a keyword já normalizada; NÃO re-normalizar no teste).
    - Degenerada: keyword `'*'` (ou `''`) → `compileRule` null → retorna as rows INALTERADAS (no-op, nenhuma vira a catX).
    - Imutabilidade: rows não-alvo mantêm identidade referencial (a função retorna a MESMA row quando não muda).
  </behavior>
  <action>
    Estender `src/components/import-review-table.test.tsx`: adicionar `reclassifyRowsWithKeyword` ao import de `@/components/import-review-table`. Criar `describe('reclassifyRowsWithKeyword', …)` com os casos de <behavior>, usando um helper local `makeRow(partial)` que preenche os campos obrigatórios de `ReviewRow` (não inventar campos fora do union). NÃO usar render/`@testing-library` (função pura). NÃO testar `origin === 'ia'` (não existe — Pitfall 1). Manter os blocos existentes intactos. Após este task a suíte fica VERMELHA (import inexistente) — ciclo RED.
  </action>
  <verify>
    <automated>npx vitest run src/components/import-review-table.test.tsx 2>&1 | grep -qiE "reclassifyRowsWithKeyword|Cannot find|is not (a function|exported)|error TS2305" && echo "RED-OK: testes referenciam reclassifyRowsWithKeyword (ainda não existe)" || (echo "esperava falha por reclassifyRowsWithKeyword ausente" && exit 1)</automated>
  </verify>
  <done>O teste importa e exercita `reclassifyRowsWithKeyword` cobrindo: null+memória+palavra-chave sobrescrevem; manual preserva; provenance vira 'palavra-chave' sem confidence; glob casa; degenerada (`*`/`''`) é no-op; imutabilidade preservada. Suíte falha SÓ por a função não existir (RED). Nenhum teste pré-existente alterado.</done>
  <acceptance_criteria>
    - `reclassifyRowsWithKeyword` está no import de `@/components/import-review-table`.
    - `describe('reclassifyRowsWithKeyword', …)` cobre ≥7 casos incluindo explicitamente a preservação de `origin==='manual'` e o no-op da keyword degenerada.
    - Nenhum caso referencia `origin === 'ia'`.
    - Os blocos `describe` existentes permanecem byte-equivalentes.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exportar reclassifyRowsWithKeyword + lift-state + swap do caller para addKeywordInline (GREEN)</name>
  <files>src/components/import-review-table.tsx</files>
  <read_first>
    - src/components/import-review-table.tsx — import de `addKeyword` (L78); exports puros `lowConfidenceFirst` (L224)/`confirmToastMessage` (L296) como modelo de onde colocar o novo export puro; `ReviewRow` (L240-279); `classifyRow` (L383-400) e `applyAllSuggestions` (L413-441) como modelo de `setRows((prev)=>prev.map(...))`; `markKeywordCreated` (L365-371) como modelo de callback de lift-state com `React.useCallback`; a coluna `category_id` que renderiza `InlineReviewCategoryCell` (L568-582); a lista de deps do `useMemo` das colunas (L645-655); `InlineReviewCategoryCell` props (L955-973) e o ponto onde renderiza `KeywordInlineSuggest` (L1061-1068); `KeywordInlineSuggest` props (L1126-1136) e o `onSubmit` (L1158-1177) com o caller `addKeyword` em L1162 e o `onCreated(row.id)` em L1174; o segundo render-site dos props (L872-873).
    - src/lib/classifier/keywords.ts — `compileRule`/`matchKeyword` (assinaturas já lidas).
    - 25-RESEARCH.md § Pattern 2 Code Example (L163-192) + Code Examples (L255-282) — a forma do export puro, do hook no onSubmit e do callback de pai.
  </read_first>
  <behavior>
    Após esta task a suíte do Plan 02 Task 1 fica VERDE e nada da suíte existente regride. Comportamento observável:
    - `reclassifyRowsWithKeyword(rows, categoryId, normalizedKeyword)`: `compileRule(categoryId, normalizedKeyword, 0)`; se `null` → retorna `rows` (no-op). Senão `rows.map`: se `origin==='manual'` → retorna a row (inalterada); se NÃO (`category_id===null || origin==='memória' || origin==='palavra-chave'`) e `matchKeyword(r.descriptor_norm, [rule]) !== null` → `{...r, category_id: categoryId, origin: 'palavra-chave' as const}`; demais → row inalterada. `suggestion` nunca é tocada; nenhum `confidence` é atribuído à row.
    - Caller inline usa `addKeywordInline` (Plan 01) — criar keyword inline não dispara `revalidatePath` → sem scroll jump.
    - Após `{ok}`/`{duplicate}` (NUNCA em `'error' in r`), o `KeywordInlineSuggest` chama o novo callback do pai com `(row.category_id!, normalized)`; o pai roda `setRows((prev) => reclassifyRowsWithKeyword(prev, categoryId, normalized))` → outras linhas casando atualizam ao vivo, sem refresh.
  </behavior>
  <action>
    Em `src/components/import-review-table.tsx`:
    1. Adicionar `addKeywordInline` ao import de `@/actions/category-keywords` (L78) e TROCAR o caller em L1162 de `addKeyword(...)` para `addKeywordInline(...)`. Se `addKeyword` não for mais usado em nenhum lugar do arquivo, remover do import; senão manter ambos.
    2. Adicionar o import `import { compileRule, matchKeyword } from '@/lib/classifier/keywords'` (junto dos imports do topo).
    3. Adicionar o export puro `reclassifyRowsWithKeyword` perto de `lowConfidenceFirst`/`confirmToastMessage`, com a lógica de <behavior> (RESEARCH § Pattern 2). Tipar estritamente; usar `origin: 'palavra-chave' as const` para estreitar ao union.
    4. Lift-state: no `ImportReviewTable`, criar um handler `React.useCallback((categoryId: string, normalizedKeyword: string) => setRows((prev) => reclassifyRowsWithKeyword(prev, categoryId, normalizedKeyword)), [])`. Threadar um novo prop (ex. `onKeywordPersisted`) — DISTINTO de `onKeywordCreated` — pela cadeia: passar nos DOIS render-sites de `InlineReviewCategoryCell` (coluna L572-581 e o site L866-873) e incluir o handler na lista de deps do `useMemo` das colunas (L645-655); declarar na assinatura de `InlineReviewCategoryCell` (L955-973) e repassar a `KeywordInlineSuggest` (L1061-1068); declarar na assinatura de `KeywordInlineSuggest` (L1126-1136).
    5. No `onSubmit` de `KeywordInlineSuggest` (L1158-1177): após `onCreated(row.id)` e SOMENTE no caminho de sucesso (após os guards `'error' in r` / branch duplicate-ou-ok), chamar `onKeywordPersisted(row.category_id!, normalized)` usando o `normalized` já computado (`normalizeKeyword(value.trim())`, L1159). NUNCA chamar no ramo de erro (Pitfall 3). Em `{duplicate}` chamar também (a keyword existe — alinha a grid; A2/Open Question 1, ponto de UAT).
    NÃO usar `router.refresh()`/refetch em lugar nenhum do path inline (reintroduziria o bug). NÃO atribuir `confidence`. NÃO tocar o gate de render do controle (`origin === 'manual' && category_id !== null`, L1061).
  </action>
  <verify>
    <automated>npx vitest run src/components/import-review-table.test.tsx src/actions/category-keywords.test.ts</automated>
  </verify>
  <done>`reclassifyRowsWithKeyword` exportada e verde nos testes; caller inline usa `addKeywordInline`; novo callback de lift-state threaded ImportReviewTable → cell → InlineReviewCategoryCell → KeywordInlineSuggest, disparando `setRows(reclassify…)` só em sucesso. Suítes de grid + actions passam; `tsc --noEmit` sem erros novos.</done>
  <acceptance_criteria>
    - `reclassifyRowsWithKeyword` é EXPORTADA, pura (sem hooks/IO), e: no-op quando `compileRule` retorna null; nunca altera `origin==='manual'`; casadas em alvo viram `category_id=categoryId, origin='palavra-chave'`; não atribui `confidence`.
    - O caller em L1162 chama `addKeywordInline` (não `addKeyword`).
    - Existe um novo callback de prop (≠ `onKeywordCreated`) threaded pelos três níveis, e o `useMemo` das colunas inclui o handler nas deps.
    - O re-classify é disparado SÓ no caminho de sucesso (`{ok}`/`{duplicate}`), nunca em `'error' in r`.
    - `npx vitest run src/components/import-review-table.test.tsx src/actions/category-keywords.test.ts` passa 100%; `npx tsc --noEmit` sem erros novos.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT vivo — scroll preservado + re-classify ao vivo (UX-01/UX-02)</name>
  <action>Checkpoint de verificação humana — nada a implementar. Validar no browser (Chrome MCP / dev) os comportamentos de runtime do Router Cache que jsdom não reproduz (D-06): scroll preservado, re-classify ao vivo, manual intocado, `/categorias` refletindo a keyword.</action>
  <what-built>
    Criar palavra-chave inline ("+ palavra-chave") numa linha da grid de revisão de importação agora (a) preserva o scroll (caller via `addKeywordInline`, sem `revalidatePath`) e (b) re-classifica ao vivo as outras linhas que casam, sem refresh — aplicando em não-classificadas e sobrescrevendo memória/IA-não-aplicada, nunca tocando linhas manuais.
  </what-built>
  <how-to-verify>
    1. `npm run dev` e logar (dev aponta para PROD Supabase — usar uma importação real/de teste).
    2. Subir uma fatura (OFX/CSV) com várias linhas, abrir `/importar/[id]`. Garantir que a grid tem: pelo menos uma linha NÃO classificada cujo descritor casaria uma keyword (ex. dois descritores contendo "UBER"), e pelo menos uma linha classificada à mão (`origin = manual`).
    3. Rolar até uma linha lá embaixo. Classificar uma linha à mão para revelar o "+ palavra-chave" e criar uma keyword que case OUTRAS linhas (ex. `uber`).
    4. Confirmar SC1/UX-01: ao salvar, a página NÃO pula pro topo — o scroll fica onde estava.
    5. Confirmar SC4/UX-02: as outras linhas que casam a keyword (não-classificadas e as classificadas por memória/IA) atualizam a categoria ao vivo, com badge de origem "palavra-chave", SEM refresh.
    6. Confirmar SC5: nenhuma linha com origem "manual" (classificada à mão) muda.
    7. Confirmar SC3: abrir `/categorias` e ver a keyword nova cadastrada na categoria correta.
    8. (A2 — Open Question) Repetir o passo 3 com uma keyword JÁ cadastrada (duplicate): o toast "já está cadastrada" aparece E a grid ainda re-alinha as linhas casando — confirmar que esse comportamento agrada (recomendação da pesquisa: SIM).
  </how-to-verify>
  <resume-signal>Digite "approved" se scroll preservado + re-classify ao vivo + manual intocado + /categorias reflete a keyword; ou descreva os problemas.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Server Action (`addKeywordInline`) | persistência da keyword continua server-side (mesmas guards + RLS do Plan 01) |
| matcher rodando no client (`reclassifyRowsWithKeyword`) | opera SÓ sobre `ReviewRow[]` em estado (keywords + `descriptor_norm` já no client) — NÃO é fronteira de confiança nova |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-04 | Denial of Service | ReDoS via glob `*` no matcher client-side | mitigate | `compileRule`/`globToRegExp` são linear-time ReDoS-safe (`keywords.ts:31-44`, anchored `^…$`, sem quantificador aninhado) — INALTERADOS; rodar no client opera sobre input do próprio usuário. |
| T-25-05 | Tampering | keyword degenerada (`*`/`**`) "casaria tudo" na grid | mitigate | `compileRule` retorna `null` para literal-count-0 (`keywords.ts:85`) → `reclassifyRowsWithKeyword` faz no-op; defesa em profundidade alinhada ao reject do cadastro (Plan 01). |
| T-25-06 | Spoofing/Integrity | override indevido de classificação manual do usuário | mitigate | Guard duro `if (r.origin === 'manual') return r` ANTES de qualquer match — intenção explícita do usuário nunca é tocada (D-05/SC5). |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote instalado nesta fase (RESEARCH § Package Legitimacy Audit: N/A). |

**Nota:** rodar o matcher client-side NÃO amplia a superfície — keywords e `descriptor_norm` já vivem no estado da grid; a escrita real (Plan 01) continua 100% server-side com RLS. A persistência de provenance no confirm é RE-DERIVADA server-side via o mesmo `matchKeyword` (RESEARCH § Runtime State Inventory) — grid e persist concordam sem trabalho extra. Sem ameaças de severidade `high`.
</threat_model>

<verification>
- `npx vitest run src/components/import-review-table.test.tsx src/actions/category-keywords.test.ts` passa 100%.
- `npm test` (suíte completa) verde no merge da wave.
- `npx tsc --noEmit` sem erros novos.
- UAT Chrome MCP (checkpoint): scroll preservado (SC1/UX-01) + re-classify ao vivo das linhas casando (SC4/UX-02) + manual intocado (SC5) + `/categorias` reflete a keyword (SC3).
- Diff de `database.types.ts` vazio (sem schema delta).
</verification>

<success_criteria>
- D-03/D-04/D-05 implementados: caller inline via `addKeywordInline`; re-classify client-side puro via `compileRule`/`matchKeyword`; política de sobrescrita correta (null + memória/palavra-chave; nunca manual; provenance 'palavra-chave', sem confidence).
- UX-01 (SC1): criar keyword inline preserva o scroll (sem revalidate no path inline).
- UX-02 (SC4/SC5): demais linhas casando re-classificadas ao vivo sem refresh; `origin==='manual'` preservado.
- SC3 não regride: keyword persiste e aparece em `/categorias` (via Plan 01 `addKeyword` no caminho de `/categorias`, intocado).
- A2 (Open Question 1) confirmado no UAT: re-classify roda também em `{duplicate}`.
</success_criteria>

<output>
Create `.planning/phases/25-fix-de-scroll-na-cria-o-de-palavra-chave/25-02-SUMMARY.md` when done
</output>
