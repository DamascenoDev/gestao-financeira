---
phase: 28-v-nculo-reverso-por-valor-consumo-sem-double-count
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/carro/abastecimento-match.ts
  - src/lib/carro/abastecimento-match.test.ts
  - src/lib/parsers/types.ts
  - src/lib/schemas/import.ts
  - src/lib/ownership.ts
autonomous: true
requirements: [CAR-09, CAR-11]
tags: [abastecimento, vinculo-reverso, value-match, importacao]

must_haves:
  truths:
    - "D-01: a parcela casa quando o valor do lançamento (em centavos) está no conjunto {floor(valor_total_cents/N), ceil(valor_total_cents/N)}; à-vista casa quando valor == amount_cents exato — aritmética inteira, nunca float, nunca janela de centavos arbitrária"
    - "D-02: os candidatos de match são os abastecimentos NÃO-vinculados do usuário que casam o predicado D-01 — SEM filtro de data (a data nunca trava elegibilidade)"
    - "D-03: quando >1 abastecimento casa o valor de uma linha, a função escolhe UM — o de occurred_on mais próximo do lançamento; empate resolve pelo mais antigo (FIFO determinístico)"
    - "D-04: a atribuição é greedy 1:1 — cada abastecimento candidato é consumido por no máximo uma linha, e para parcelado a função respeita as parcelas já existentes + as já atribuídas nesta fatura, emitindo no máx 1 parcela nova por fatura (invariante estrutural, não só validada)"
    - "assertOwnedAbastecimento(supabase, id) re-deriva posse do abastecimentoId server-side em tri-state ('owned' | 'not-owned' | 'error'), espelhando assertOwnedCarro — FKs do Postgres NÃO são RLS-aware"
    - "ParsedReviewRow.abastecimentoMatch é um campo OPCIONAL não-vinculante (palpite), nunca aplicado a category_id/carro_id no parse — espelha ParsedReviewRow.suggestion"
  prohibitions:
    - "NUNCA usar float, Number division não-inteira, ou tolerância de centavos arbitrária no predicado de valor (D-01: floor/ceil de divisão inteira sobre bigint/centavos)"
    - "NUNCA adicionar filtro por data/janela temporal na elegibilidade dos candidatos (D-02: data só desempata em D-03)"
    - "NUNCA gerar mais de uma sugestão por linha nem permitir que dois abastecimentos casem a mesma linha (D-03/D-04 1:1)"
  artifacts:
    - path: "src/lib/carro/abastecimento-match.ts"
      provides: "Pure value-match logic: D-01 predicate (matchesAvista/parcelaCentsSet), D-03 nearest-by-date tiebreak, D-04 greedy 1:1 assignment producing per-row abastecimentoMatch"
      min_lines: 60
    - path: "src/lib/carro/abastecimento-match.test.ts"
      provides: "Pure unit tests for D-01 floor/ceil predicate, no-date-filter candidates, nearest-by-date + FIFO tiebreak, greedy 1:1 + ≤1 parcela/fatura"
    - path: "src/lib/ownership.ts"
      provides: "assertOwnedAbastecimento tri-state ownership re-derive"
      contains: "assertOwnedAbastecimento"
    - path: "src/lib/parsers/types.ts"
      provides: "ParsedReviewRow.abastecimentoMatch optional non-binding field"
      contains: "abastecimentoMatch"
    - path: "src/lib/schemas/import.ts"
      provides: "confirmImportRowSchema link fields (abastecimentoId + kind + parcelaNum)"
      contains: "abastecimentoId"
  key_links:
    - from: "src/lib/carro/abastecimento-match.ts"
      to: "src/lib/money.ts"
      via: "integer-cents arithmetic only (no parseBRLToCents needed inside the predicate; values arrive as integer cents)"
      pattern: "Math\\.(floor|ceil)"
---

<objective>
Lançar os **contratos de fundação** do vínculo reverso por valor — a camada pura e os tipos
que todos os outros planos da fase 28 consomem. Sem tocar em `ingestStatement`/`confirmImport`
nem na UI (esses são Planos 02/03/04).

Quatro artefatos:
1. **`src/lib/carro/abastecimento-match.ts`** (NOVO) — a lógica PURA do match: o predicado de
   valor D-01 (floor/ceil em centavos inteiros), o desempate por data mais próxima D-03, e a
   atribuição greedy 1:1 D-04 que produz, por linha, o `abastecimentoMatch` (≤1 parcela nova
   por fatura, estrutural). É testável isoladamente — é o coração testável de CAR-09/CAR-11.
2. **`src/lib/ownership.ts`** — `assertOwnedAbastecimento` tri-state (molde: `assertOwnedCarro`).
3. **`src/lib/parsers/types.ts`** — o campo opcional `abastecimentoMatch` em `ParsedReviewRow`
   (molde: `suggestion?`).
4. **`src/lib/schemas/import.ts`** — os campos de vínculo no `confirmImportRowSchema`
   (molde: `carroId`).

Purpose: Isolar a lógica de match (D-01/D-03/D-04) num módulo puro com testes determinísticos,
e fixar os tipos/schema/ownership que o pass de ingest (Plano 02), o link-write (Plano 03) e a
grid (Plano 04) vão importar — sem que nenhum deles precise inventar contratos.
Output: 1 módulo puro novo + 1 suíte de testes unitários + 3 extensões de contrato.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-v-nculo-reverso-por-valor-consumo-sem-double-count/28-CONTEXT.md
@.planning/phases/28-v-nculo-reverso-por-valor-consumo-sem-double-count/28-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Módulo puro de value-match (D-01 + D-03 + D-04) + testes unitários</name>
  <files>src/lib/carro/abastecimento-match.ts, src/lib/carro/abastecimento-match.test.ts</files>
  <read_first>
    - src/lib/carro/consumo.ts (molde de módulo PURO testável neste diretório — funções sem I/O, só aritmética de centavos)
    - src/lib/carro/consumo.test.ts (molde EXATO da suíte vitest pura: describe/it/expect, casos de centavos, guards de null — espelhar este estilo)
    - src/lib/money.ts (centavos inteiros; o predicado D-01 é aritmética inteira, NUNCA float)
    - src/lib/parsers/types.ts (RawTransaction.amount_cents + occurred_on que o match consome; o shape de ParsedReviewRow.suggestion como molde do abastecimentoMatch)
    - supabase/migrations/0039_abastecimento_parcelado.sql L43-76 (parcelas_total marca parcelado quando > 1; valor_total_cents é o custo parcelado; à-vista usa amount_cents)
  </read_first>
  <behavior>
    - D-01 à-vista: `valorLancamentoCents === amountCents` (igualdade exata em inteiros). Casos: 10000===10000 → true; 9999 → false.
    - D-01 parcela: dado `valorTotalCents` e `parcelasTotal` N, o conjunto-alvo é `{Math.floor(valorTotalCents/N), Math.ceil(valorTotalCents/N)}`; o lançamento casa quando seu valor está nesse conjunto. Caso canônico do CONTEXT D-01: total 10000 (R$100,00) ÷ 3 → floor 3333, ceil 3334 → as três parcelas reais 3334+3333+3333 TODAS casam {3333,3334}. Caso divisão exata: 9000 ÷ 3 → {3000} (floor==ceil) → só 3000 casa.
    - D-02 (sem filtro de data): a função de candidatos recebe a lista de abastecimentos não-vinculados JÁ buscada pelo chamador (Plano 02) e NÃO aplica nenhum corte por data — todos os que casam o predicado são candidatos. Teste: dois abastecimentos com o MESMO valor e datas a meses de distância são AMBOS candidatos.
    - D-03 (desempate por data mais próxima): quando >1 candidato casa o valor de uma linha, escolher o de `occurred_on` mais próximo (menor |diff de dias|) do `occurred_on` da linha; empate de distância → o mais antigo (occurred_on, depois created_at/id) — determinístico/FIFO. Teste: linha em 2026-03-10 com candidatos 2026-03-08 e 2026-03-20 → escolhe 2026-03-08.
    - D-04 (greedy 1:1 + ≤1 parcela/fatura): a função de atribuição percorre as linhas, cada candidato é consumido por no máx uma linha (a melhor leva; as demais que casariam o mesmo abastecimento ficam SEM match). Para um parcelado, conta as parcelas já na junção (passadas pelo chamador) + as já atribuídas NESTA fatura e PARA de sugerir quando as N parcelas estão completas → no máx 1 parcela nova por fatura. Testes: (a) duas linhas casando o mesmo abastecimento à-vista → só uma recebe match; (b) parcelado N=3 com 2 parcelas já na junção + 1 linha casando → 1 match (parcelaNum=3); (c) parcelado N=3 com 3 parcelas já completas → 0 matches; (d) parcelado N=3 com 0 parcelas + 2 linhas casando o valor → exatamente 1 match (parcelaNum=1), a segunda linha sem match.
    - O retorno por linha é o shape de `abastecimentoMatch` (Claude's discretion no shape exato, CONTEXT L114-117): `{ abastecimentoId, kind: 'avista' | 'parcela', parcelaNum?, carroId, carroApelido }`.
  </behavior>
  <action>
    Criar `src/lib/carro/abastecimento-match.ts` como módulo PURO (sem `'use server'`, sem
    cliente Supabase — só recebe dados já buscados e devolve atribuições). Exportar tipos +
    funções (nomes à discrição, sugestão):
    - Um tipo `AbastecimentoMatchCandidate` = `{ id, carroId, carroApelido, occurredOn, parcelasTotal, valorTotalCents, amountCents, jaParceladas }` (o chamador do Plano 02 monta isto a partir do fetch dos abastecimentos não-vinculados + do count na junção).
    - `parcelaTargetCents(valorTotalCents: number, parcelasTotal: number): Set<number>` — devolve `new Set([Math.floor(v/N), Math.ceil(v/N)])` (D-01 parcela). Aritmética inteira só.
    - `matchesValue(valorLancamentoCents: number, cand: AbastecimentoMatchCandidate): boolean` — à-vista (parcelasTotal null ou <=1) usa igualdade exata com `amountCents`; parcelado usa `.has(...)` do Set de `parcelaTargetCents` (D-01).
    - `assignAbastecimentoMatches(rows, candidates)` — a atribuição greedy 1:1 D-04 + desempate D-03. Recebe as linhas (com occurredOn + valor em centavos) e os candidatos; devolve um `Map<rowId, AbastecimentoMatch>`. Implementar como: para cada linha, achar os candidatos que casam o valor E ainda têm capacidade (à-vista não-consumido; parcelado com parcelas restantes > 0 contando já-na-junção + já-atribuídas-nesta-fatura), escolher o mais próximo por data (D-03, empate → mais antigo), marcar o candidato consumido (à-vista: removido do pool; parcelado: incrementa o contador atribuído-nesta-fatura e atribui `parcelaNum = jaParceladas + atribuídasNestaFatura`). NUNCA float, NUNCA filtro de data na elegibilidade.
    Criar `src/lib/carro/abastecimento-match.test.ts` espelhando `consumo.test.ts` (vitest puro):
    cobrir TODOS os casos do bloco <behavior> acima (à-vista exato, conjunto {floor,ceil}, divisão
    exata, sem-filtro-de-data, nearest-by-date + FIFO, greedy 1:1, ≤1 parcela/fatura, parcelado
    completo → 0). Sem mock de DB — entradas são objetos puros.
  </action>
  <verify>
    <automated>npx vitest run src/lib/carro/abastecimento-match.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/carro/abastecimento-match.test.ts` passa com TODOS os casos do <behavior> verdes.
    - Source assertion: `grep -nE 'Math\.(floor|ceil)' src/lib/carro/abastecimento-match.ts` retorna ≥1 (predicado D-01 é divisão inteira).
    - Source assertion: o módulo NÃO importa nada de `@supabase` nem usa `'use server'` — `grep -c "use server\|@supabase" src/lib/carro/abastecimento-match.ts` retorna 0 (módulo puro).
    - Behavior assertion: o teste do caso "duas linhas casam o mesmo abastecimento à-vista" prova que só UMA recebe match (D-04 1:1).
    - Behavior assertion: o teste do caso "parcelado N=3, 2 já na junção" prova `parcelaNum === 3` e exatamente 1 match (≤1 parcela/fatura).
  </acceptance_criteria>
  <done>O módulo puro de match existe com D-01/D-03/D-04 implementados em aritmética inteira; a suíte unitária cobre o conjunto {floor,ceil}, o sem-filtro-de-data, o nearest-by-date+FIFO e o greedy 1:1 ≤1-parcela/fatura, e passa.</done>
</task>

<task type="auto">
  <name>Task 2: Contratos — assertOwnedAbastecimento + ParsedReviewRow.abastecimentoMatch + campos de vínculo no schema</name>
  <files>src/lib/ownership.ts, src/lib/parsers/types.ts, src/lib/schemas/import.ts</files>
  <read_first>
    - src/lib/ownership.ts L113-166 (assertOwnedCarro / assertOwnedTransaction tri-state — molde verbatim do assertOwnedAbastecimento; OwnershipResult já definido)
    - src/lib/parsers/types.ts L68-90 (ParsedReviewRow + o campo suggestion? como molde do abastecimentoMatch, incluindo o tom do JSDoc "NÃO-vinculante")
    - src/lib/schemas/import.ts L36-48 (confirmImportRowSchema + o campo carroId L45 como molde dos campos de vínculo; o tipo inferido ConfirmImportRow L48)
    - supabase/migrations/0039_abastecimento_parcelado.sql L85-95 (junção: parcela_num int > 0; kind avista/parcela é derivado de parcelas_total)
  </read_first>
  <action>
    (a) Em `src/lib/ownership.ts`, adicionar `assertOwnedAbastecimento(supabase: Client, id:
    string): Promise<OwnershipResult>` espelhando VERBATIM o corpo de `assertOwnedCarro`
    (L159-166): `select('id').eq('id', id)`; `if (error) return 'error'`; `return data?.length
    === 1 ? 'owned' : 'not-owned'`. Tabela `abastecimentos`. JSDoc mirror de assertOwnedCarro
    citando o re-derive de posse do `abastecimentoId` no confirmImport (WR-01, FKs não RLS-aware).
    (b) Em `src/lib/parsers/types.ts`, adicionar a `ParsedReviewRow` o campo OPCIONAL
    `abastecimentoMatch?: { abastecimentoId: string; kind: 'avista' | 'parcela'; parcelaNum?:
    number; carroId: string; carroApelido: string }`, espelhando o JSDoc/optionalidade de
    `suggestion?` (L81-89): "palpite NÃO-vinculante; NUNCA aplicado a category_id/carro_id sem
    confirmação; opcional para manter os parsers puros + linhas persistidas antigas byte-compat".
    Reusar o tipo de `abastecimento-match.ts` (Task 1) se conveniente, ou re-declarar o shape
    idêntico — manter um único shape canônico.
    (c) Em `src/lib/schemas/import.ts`, estender `confirmImportRowSchema` com os campos do
    vínculo, espelhando `carroId` (nullable/optional + tipos zod): `abastecimentoId: z.string()
    .uuid().optional()`, `abastecimentoKind: z.enum(['avista','parcela']).optional()`,
    `parcelaNum: z.number().int().positive().optional()`. Atualizar o JSDoc do schema notando
    que o cliente passa só a ESCOLHA do vínculo e o servidor re-deriva posse do `abastecimentoId`
    (WR-01). Manter o `ConfirmImportRow = z.infer<...>` na linha existente.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "assertOwnedAbastecimento" src/lib/ownership.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passa (TypeScript estrito, sem `any`).
    - Source: `grep -c "assertOwnedAbastecimento" src/lib/ownership.ts` ≥ 1 e a função retorna `Promise<OwnershipResult>` tri-state.
    - Source: `grep -c "abastecimentoMatch" src/lib/parsers/types.ts` ≥ 1 e o campo é opcional (`?`).
    - Source: `grep -c "abastecimentoId" src/lib/schemas/import.ts` ≥ 1 e é `.optional()`.
    - Behavior: o shape de `abastecimentoMatch` em types.ts é idêntico (mesmas chaves) ao retorno de `assignAbastecimentoMatches` (Task 1) — um único shape canônico.
  </acceptance_criteria>
  <done>assertOwnedAbastecimento existe em tri-state; ParsedReviewRow ganha abastecimentoMatch opcional não-vinculante; confirmImportRowSchema aceita abastecimentoId+kind+parcelaNum opcionais; tsc passa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none crossed in this plan) | Plano 01 é só lógica pura + tipos + schema + um helper de ownership. Nenhum write, nenhum input de cliente é processado aqui. O `assertOwnedAbastecimento` é a MITIGAÇÃO consumida no Plano 03 (onde o boundary cliente→confirmImport é cruzado). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Elevation of Privilege | assertOwnedAbastecimento | mitigate | Helper criado aqui; o re-derive efetivo do `abastecimentoId` (IDOR) acontece no Plano 03 confirmImport. Tri-state fail-safe: 'error'/'not-owned' nunca emitem write. |
</threat_model>

<verification>
- `npx vitest run src/lib/carro/abastecimento-match.test.ts` — toda a lógica pura D-01/D-03/D-04 verde.
- `npx tsc --noEmit` — os contratos novos tipam sob strict.
- `grep -nE 'Math\.(floor|ceil)' src/lib/carro/abastecimento-match.ts` — predicado D-01 é inteiro.
</verification>

<success_criteria>
- Módulo puro `abastecimento-match.ts` com D-01 (floor/ceil), D-03 (nearest-by-date + FIFO), D-04 (greedy 1:1 + ≤1 parcela/fatura), 100% testado por unidade sem DB.
- `assertOwnedAbastecimento` tri-state pronto para o IDOR re-derive do Plano 03.
- `ParsedReviewRow.abastecimentoMatch` + os campos de vínculo do schema prontos para os Planos 02/03/04 consumirem.
- Zero floats no predicado de valor; zero filtro de data na elegibilidade.
</success_criteria>

<output>
Create `.planning/phases/28-v-nculo-reverso-por-valor-consumo-sem-double-count/28-01-SUMMARY.md` when done
</output>
