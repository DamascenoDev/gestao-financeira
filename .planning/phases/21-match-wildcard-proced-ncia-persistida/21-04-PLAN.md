---
phase: 21-match-wildcard-proced-ncia-persistida
plan: 04
type: execute
wave: 2
depends_on:
  - 21-01-match-wildcard-proced-ncia-persistida
  - 21-02-match-wildcard-proced-ncia-persistida
  - 21-03-match-wildcard-proced-ncia-persistida
files_modified:
  - src/actions/import.ts
  - src/actions/import.test.ts
autonomous: true
requirements: [KW-09, KW-10]
must_haves:
  truths:
    - "No upload, um descritor `uber trip 123` casa a keyword glob `UBER*` e chega pré-classificado naquela categoria (KW-09 ponta-a-ponta)"
    - "Uma linha classificada por keyword, ao confirmar, persiste classification_source='palavra-chave' em transactions (KW-10)"
    - "Uma linha classificada por memória persiste 'memória'; um pick manual/IA (sem match memória/keyword) mantém o coarse 'memória' (sem regressão)"
    - "O glob é compilado UMA vez por regra no pre-fetch, não por linha (sem N×M new RegExp)"
  artifacts:
    - path: "src/actions/import.ts"
      provides: "compileRule no pre-fetch (~444) + re-derivação server-side da procedência no commit (~829)"
      contains: "compileRule"
  key_links:
    - from: "src/actions/import.ts pre-fetch keywordRules (~444)"
      to: "compileRule de keywords.ts"
      via: "mapeia kwRows → KeywordRule[] via compileRule (glob compilado uma vez)"
      pattern: "compileRule"
    - from: "src/actions/import.ts commit insert (~829)"
      to: "matchKeyword + lookupMemory sobre r.base.descriptor_norm"
      via: "re-deriva classification_source server-side: memória→'memória', keyword→'palavra-chave', senão coarse 'memória'"
      pattern: "classification_source"
---

<objective>
KW-09 (wiring ponta-a-ponta) + KW-10 (re-derivação no commit). Liga as três peças dos Planos 01/02/03 no pipeline de `src/actions/import.ts`:

1. **KW-09 wiring (pre-fetch ~444):** o pre-fetch ÚNICO de `category_keywords` hoje mapeia `kwRows → KeywordRule[]` cru. Troca para construir as regras via `compileRule` (Plano 02) — compilando o glob UMA vez por regra (não N×M por linha; RESEARCH §Perf note, §Anti-Patterns). A keyword agora pode conter `*` (Plano 01) e o matcher (Plano 02) a trata como glob. O contrato de fetch WR-02 não muda.
2. **KW-10 (commit ~829):** corrige `classification_source: r.categoryId ? 'memória' : null` — o bug exato. Re-deriva a procedência SERVER-SIDE sobre `r.base.descriptor_norm` (re-roda memória → keyword, com as memory lookups BATCHED por WR-02), mapeando memória→'memória', keyword→'palavra-chave', senão mantém o coarse 'memória' (sem regressão para picks manuais/IA). Aplica um GUARD de igualdade de categoria: rotula 'palavra-chave' (e 'memória') SOMENTE quando a categoria re-derivada == `r.categoryId` persistida; se o usuário sobrescreveu a categoria na grid, NÃO reivindica procedência keyword/memória (mantém coarse 'memória') — honra CONTEXT Area-3 "sem procedência falsa".

Purpose: Tornar o glob útil no upload real e gravar a procedência honesta no confirm (per D em 21-CONTEXT.md; WR-01 servidor é a fonte da verdade).
Output: import.ts com compileRule no pre-fetch + re-derivação batched no commit + testes ponta-a-ponta (glob match + persistência 'palavra-chave').
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-match-wildcard-proced-ncia-persistida/21-CONTEXT.md
@.planning/phases/21-match-wildcard-proced-ncia-persistida/21-RESEARCH.md
@.planning/phases/21-match-wildcard-proced-ncia-persistida/21-01-PLAN.md
@.planning/phases/21-match-wildcard-proced-ncia-persistida/21-02-PLAN.md
@.planning/phases/21-match-wildcard-proced-ncia-persistida/21-03-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Compilar o glob no pre-fetch via compileRule (KW-09 wiring)</name>
  <files>src/actions/import.ts, src/actions/import.test.ts</files>
  <read_first>
    - src/actions/import.ts (linha 8 importa `matchKeyword, type KeywordRule`; o pre-fetch ~438-455 que mapeia kwRows → keywordRules; PASS 1 ~486-507 onde matchKeyword roda por linha e seta source='palavra-chave' ~502)
    - src/lib/classifier/keywords.ts (DEPOIS do Plano 02: compileRule + KeywordRule estendido com glob/literals — o helper a usar no pre-fetch)
    - src/actions/import.test.ts (a fixture `keywordRows` ~35-39 e os testes de PASS 1 que asseguram classification_source — ~661-728; o mock builder roteia `category_keywords` select)
    - .planning/phases/21-match-wildcard-proced-ncia-persistida/21-RESEARCH.md (Pattern 2 compile-once no pre-fetch; §Perf note; §Anti-Patterns "Compiling the glob regex per match")
  </read_first>
  <behavior>
    - Upload com uma keyword glob `UBER*` cadastrada: um descritor `uber trip 123` chega na review row pré-classificado naquela categoria com classification_source='palavra-chave' (KW-09 ponta-a-ponta)
    - Upload com `*ifood*`: um descritor `pedido ifood centro` chega pré-classificado naquela categoria
    - Substring sem `*` (`mercado`) continua casando como no v1.5 (os testes de PASS 1 existentes ~661-728 permanecem verdes)
    - Memória ainda prevalece sobre keyword (um hit de memória nunca é sobrescrito pela keyword — comportamento PASS 1 inalterado)
    - O glob é compilado uma vez por regra (não por linha) — assertável indiretamente: o pre-fetch usa compileRule
  </behavior>
  <action>
    Importe `compileRule` de '@/lib/classifier/keywords' em import.ts. No pre-fetch (~444-455), troque o `.map((k) => ({ categoryId, keyword, sort }))` cru por um map que chama `compileRule(k.category_id, k.keyword, k.categories?.sort ?? 0)` e FILTRA os nulls (regras degeneradas/vazias retornam null) — ex.: map + filter de não-null, mantendo o tipo `KeywordRule[]`. NÃO mude a query de fetch (`category_keywords` select com `categories(sort)`) — o contrato WR-02 é o mesmo. NÃO mude o PASS 1 nem a linha ~502 (review-time source='palavra-chave' já existe e está correta) — o matchKeyword (Plano 02) agora trata `*` como glob automaticamente. Atualize o comentário do pre-fetch para registrar que as regras são compiladas uma vez (glob→RegExp por regra) aqui.
  </action>
  <verify>
    <automated>npx vitest run src/actions/import.test.ts -t "palavra-chave"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "compileRule" src/actions/import.ts` retorna ≥1 (importado e usado no pre-fetch)
    - Novo teste em import.test.ts: keywordRows com `{ keyword: 'UBER*', categories:{sort:0} }` + um rawRow cujo descriptor_norm é `uber trip 123` → a review row sai com a categoria daquela regra E classification_source='palavra-chave'
    - Novo teste: `*ifood*` casa `pedido ifood centro`
    - Os testes de PASS 1 existentes (~661-728: uber substring, netflix memória, padaria palavra-chave) continuam verdes — sem regressão v1.5
    - `npx tsc --noEmit` passa
  </acceptance_criteria>
  <done>Pre-fetch compila as regras via compileRule (glob uma vez por regra); glob casa no upload ponta-a-ponta; PASS 1 v1.5 intacto.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-derivar classification_source server-side no commit (KW-10)</name>
  <files>src/actions/import.ts, src/actions/import.test.ts</files>
  <read_first>
    - src/actions/import.ts (o commit handler ~798-835: o loop de inserts; a linha ~829 `classification_source: r.categoryId ? 'memória' : null` — o bug; o batched pattern de dupSet ~466 e recurring ~789 a espelhar; authoritativeRows ~760-768 com r.base.descriptor_norm e r.categoryId)
    - src/lib/classifier/memory.ts (lookupMemory(supabase, descriptorNorm) → MemoryHit | null com category_id)
    - src/lib/classifier/keywords.ts (matchKeyword + compileRule — DEPOIS do Plano 02)
    - src/actions/import.test.ts (o describe('confirmImport') ~780; o helper row()/persist() ~799-830; o capture sink `transactionInserts` ~64/128; as fixtures memoryHits ~32 e keywordRows ~35)
    - .planning/phases/21-match-wildcard-proced-ncia-persistida/21-RESEARCH.md (Pattern 4 re-derive + o "Design note (memory category guard)" + Assumption A2; §Pitfall 4 BATCH as memory lookups; §Code Examples read-back query)
  </read_first>
  <behavior>
    - Uma linha classificada por keyword (descriptor casa uma keyword glob ou substring; categoryId == a categoria da keyword), ao confirmar, persiste classification_source='palavra-chave' no insert payload de transactions (KW-10 — a verdade central)
    - Uma linha classificada por memória (descriptor tem hit de memória na mesma categoria) persiste 'memória'
    - Uma linha com categoryId presente que NÃO casa nem memória nem keyword (pick manual / sugestão IA aceita) persiste o coarse 'memória' (sem regressão)
    - Uma linha SEM categoryId persiste null (inalterado)
    - GUARD de igualdade: se o descriptor casa memória/keyword para a categoria X mas o usuário sobrescreveu para a categoria Y na grid (r.categoryId == Y ≠ X), persiste o coarse 'memória' (NÃO 'palavra-chave'/'memória' da categoria errada) — sem procedência falsa
    - As memory lookups no commit são BATCHED (uma query .in() sobre os descriptor_norm distintos classificados), não N point-reads
  </behavior>
  <action>
    No commit handler, ANTES do loop de inserts (~804), pre-busque DUAS coisas UMA vez (espelhando os batched dupSet ~466 / recurring ~789): (a) as keyword rules via o MESMO fetch+compileRule do pre-fetch do PASS 1 (Task 1) — reutilize/extraia para não duplicar; (b) os hits de memória das linhas classificadas em UMA query batched: colete os `r.base.descriptor_norm` distintos das authoritativeRows com `r.categoryId` presente, faça UM `merchant_patterns.select('descriptor_norm, category_id').in('descriptor_norm', [...])`, e construa um Map descriptor_norm→category_id (RLS escopa ao caller; sem filtro app-layer de user_id — igual ao import path; §Pitfall 4). NÃO use lookupMemory por linha dentro do loop (anti-pattern WR-02).

    No loop de inserts, substitua `classification_source: r.categoryId ? 'memória' : null` por uma re-derivação server-side sobre `r.base.descriptor_norm` (WR-01 — nunca confia no source do cliente): se `r.categoryId` é null → source null; senão, consulte o Map de memória: se o hit existe E `hit.category_id === r.categoryId` → 'memória'; senão rode `matchKeyword(r.base.descriptor_norm, keywordRules)` e se `kw && kw.categoryId === r.categoryId` → 'palavra-chave'; senão (manual/IA, ou categoria sobrescrita) → coarse 'memória'. Aplique o GUARD de igualdade de categoria em AMBOS memória e keyword (per Pattern 4 Design note + Assumption A2 — honra CONTEXT "sem procedência falsa"; este é o caminho recomendado, mais estrito que o bare re-run). Atualize o comentário WR-02 das linhas ~821-828 para refletir que a procedência agora é re-derivada e honesta (não mais coarse-only), citando KW-10. NÃO mude o resto do payload (amount/kind/occurred_on/dedupe_key/carro_id) nem o learn loop de merchant_patterns ~894 (intacto).
  </action>
  <verify>
    <automated>npx vitest run src/actions/import.test.ts -t "confirmImport"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "classification_source" src/actions/import.ts` mostra a re-derivação (não mais o ternário `r.categoryId ? 'memória' : null`)
    - Novo teste em confirmImport: uma row cujo descriptor_norm casa uma keyword (via keywordRows fixture) e categoryId == a categoria da keyword → `transactionInserts[0].classification_source === 'palavra-chave'`
    - Novo teste: uma row com hit de memória (memoryHits fixture) na mesma categoria → classification_source === 'memória'
    - Novo teste (sem regressão): uma row com categoryId mas sem match memória/keyword → classification_source === 'memória' (coarse); uma row sem categoryId → null
    - Novo teste (guard): descriptor casa keyword para categoria X mas r.categoryId == Y → classification_source === 'memória' (coarse), NÃO 'palavra-chave'
    - As memory lookups no commit são batched: `grep -n "\.in('descriptor_norm'" src/actions/import.ts` aparece no commit handler (não um lookupMemory por linha dentro do loop de inserts)
    - Os testes de confirmImport existentes (kind/IDOR/dedupe/recurring/learn) continuam verdes
    - `npx tsc --noEmit` passa
  </acceptance_criteria>
  <done>O commit re-deriva a procedência server-side batched com guard de igualdade de categoria; keyword→'palavra-chave', memória→'memória', manual/IA/sobrescrito→coarse 'memória'; testes verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client confirm payload → commit insert | o source do cliente NÃO é confiável; a procedência é re-derivada da base server-persistida (WR-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-09 | Spoofing (false provenance) | re-derivação no commit | mitigate | guard de igualdade de categoria (`hit.category_id === r.categoryId`) — uma categoria sobrescrita na grid não reivindica 'palavra-chave'/'memória' falsa (RESEARCH Pattern 4 Design note, Assumption A2; CONTEXT "sem procedência falsa") |
| T-21-10 | Tampering (client source) | classification_source set | mitigate | re-derivado SOBRE r.base.descriptor_norm (server-trusted), nunca o source do cliente (WR-01) |
| T-21-11 | DoS (N point-reads) | commit memory lookups | mitigate | UMA query batched `.in('descriptor_norm', [...])` + Map em memória; espelha dupSet/recurring; keyword rules já em memória (§Pitfall 4) |
| T-21-12 | Access Control | merchant_patterns/transactions reads | accept | RLS escopa ao caller automaticamente; sem filtro app-layer de user_id (igual ao import path; RESEARCH §Security V4) |
| T-21-SC | Tampering | npm/pip/cargo installs | n/a | Nenhum pacote instalado (RESEARCH §Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `npx vitest run src/actions/import.test.ts` verde (novos casos KW-09 wiring + KW-10 re-derive + existentes)
- `npx tsc --noEmit` sem erros
- Phase gate: full suite `npx vitest run` verde + smoke live em PROD (upload com keyword `UBER*`, confirmar, verificar o badge 'palavra-chave' persiste) — depende do Plano 03 db push aplicado
</verification>

<success_criteria>
- KW-09 ponta-a-ponta: glob casa no upload (pré-fill 'palavra-chave' na grid), substring v1.5 intacto, glob compilado uma vez por regra
- KW-10: keyword-classified confirmado persiste 'palavra-chave'; memória→'memória'; manual/IA/sobrescrito→coarse 'memória' (sem regressão)
- Re-derivação server-side batched com guard de igualdade de categoria (sem procedência falsa, WR-01)
</success_criteria>

<artifacts_this_phase_produces>
Símbolos/arquivos novos ou modificados em toda a Phase 21 (referência para o executor; consumidor downstream):

| Símbolo / Arquivo | Plano | Tipo | Descrição |
|-------------------|-------|------|-----------|
| `normalizeKeyword(raw: string): string` | 21-01 | export fn (src/lib/normalize.ts) | Variante keyword-aware de normalizeDescriptor que PRESERVA `*` |
| `addKeyword` (modificado) | 21-01 | server action (src/actions/category-keywords.ts) | Usa normalizeKeyword + rejeita literal-count-0 |
| `globToRegExp(keyword: string): RegExp` | 21-02 | fn (src/lib/classifier/keywords.ts) | Glob→regex ancorado ReDoS-safe (escape canônico + `*`→`.*` + `^…$`) |
| `compileRule(categoryId, keyword, sort): KeywordRule \| null` | 21-02 | export fn (src/lib/classifier/keywords.ts) | Compila uma regra uma vez (glob/literals derivados; null para degenerado) |
| `KeywordRule` (estendido) | 21-02 | interface (src/lib/classifier/keywords.ts) | + `glob: RegExp \| null`, `literals: number` |
| `matchKeyword` (modificado) | 21-02 | export fn (src/lib/classifier/keywords.ts) | Especificidade por literal-count + rung substring>glob; glob via rule.glob.test |
| `supabase/migrations/0037_transactions_classification_source_palavra_chave.sql` | 21-03 | migration | Drop+recreate do CHECK ampliando para 'palavra-chave' |
| import.ts pre-fetch + commit (modificado) | 21-04 | server action (src/actions/import.ts) | compileRule no pre-fetch + re-derivação batched da procedência no commit |

Sem novos pacotes npm (RESEARCH §Package Legitimacy Audit: not applicable). `ClassificationSource` (src/lib/parsers/types.ts) já inclui 'palavra-chave' — sem mudança de tipo.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/21-match-wildcard-proced-ncia-persistida/21-04-SUMMARY.md` when done
</output>
